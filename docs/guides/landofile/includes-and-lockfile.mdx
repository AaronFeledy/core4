---
id: includes-and-lockfile
provider: test
platforms: [darwin, linux, win32]
timeout: 60000
tags: [beta, docs, landofile, includes, lockfile]
---

<Guide>
  <Scenario id="local-include-merge" render>
    <Step name="scaffold">
      <UseFixture name="includes-lockfile-demo" />
      <Variable name="include" value="./fragments/web.yml" display="A local relative Landofile fragment merged before the app's inline keys" />
      <Variable name="localIncludeCache" value="local include paths are tracked as parser provenance" display="Command-index cache fingerprints include the resolved local include bytes, so changing a local fragment invalidates cached app commands even after the merged Landofile shape has dropped includes" />
      <Variable name="serializer" value="@lando/sdk/landofile" display="The published Landofile parser/emitter preserves encoded map order for round trips and fails closed on invalid map keys or non-serializable values before lockfile state is written" />
      <Variable name="lockfile" value=".lando.lock.yml" display="Remote git and npm includes are locked by resolved revision and SHA-256 content hash; local includes are not locked" />
    </Step>
    <Step name="inspect">
      <Run command="lando app:config --format=json" />
    </Step>
  </Scenario>

  <Scenario id="remote-lock-remediation" render={false} reason="git and npm includes require network access; unit tests cover lock creation, byte-stable no-op reads, and drift failures.">
    <Step name="lock">
      <Variable name="gitLock" value="commit SHA + sha256(fragment bytes)" display="git includes record the resolved commit SHA and fragment content hash" />
      <Variable name="npmLock" value="package version + sha256(fragment bytes)" display="npm includes record the resolved package version and fragment content hash" />
      <Variable name="remediation" value="lando app:includes:update" display="Lock drift fails closed with LandofileLockMismatchError and points at the update command" />
      <Variable name="refresh" value="lando app:includes:update" display="Re-resolves every remote include, recomputes SHA-256 checksums, rewrites .lando.lock.yml only when drift exists, and prunes stale lock entries" />
      <Variable name="scopedRefresh" value="lando app:includes:update SOURCE..." display="Naming sources refreshes only those includes and merges the refreshed entries into the existing lockfile without pruning other sources; unknown sources fail closed listing the known sources" />
      <Variable name="offline" value="lando app:includes:update --no-network" display="Resolves git and npm includes from lockfile and content-addressed cache state only; a missing lock entry or cached artifact fails closed and tells you to rerun app:includes:update with network access" />
      <Variable name="check" value="lando app:includes:update --check" display="Runs the same refresh comparison without writing; if drift is found, the rendered result sets exit code 1 and tells you to run app:includes:update" />
      <Variable name="verify" value="lando app:includes:verify" display="Runs the refresh comparison without writing so CI can fail when .lando.lock.yml is missing, stale, or mismatched" />
      <Variable name="corruptLock" value="discarded before comparison" display="If .lando.lock.yml cannot be decoded as Lando's lockfile shape, the StateStore-backed reader treats it as an empty lock so verify/update report missing remote entries instead of preserving corrupt bytes" />
      <Variable name="verifyReport" value="{ ok, entries, mismatches }" display="Text and JSON output report ok, mismatch, missing, and stale statuses; non-ok results set exit code 1" />
      <Variable name="report" value="{ added, updated, unchanged, removed, drift, wrote, checkMode, noNetwork, requestedSources }" display="Text and JSON output report per-include added, updated, and unchanged statuses, removed stale sources, whether drift exists, whether the lockfile was written, whether --check or --no-network was used, and which sources were requested" />
    </Step>
  </Scenario>

  <Scenario id="landofile-parser-input-limits" render={false} reason="Parser resource limits are covered by SDK parser unit tests with synthetic Landofile content">
    <Step name="parser-limits">
      <Variable name="contentCap" value="1 MiB UTF-8 input" display="The published Landofile parser rejects oversized input with LandofileParseError before include or lockfile state is processed" />
      <Variable name="depthCap" value="64 nested levels" display="Deep block indentation and inline-array nesting fail closed with LandofileParseError instead of recursing indefinitely" />
    </Step>
  </Scenario>
</Guide>
