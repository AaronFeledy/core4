---
id: convert-from-lando-3
provider: test
platforms: [darwin, linux, win32]
timeout: 90000
tags: [beta, docs, landofile, lando3, translate]
---

# Convert a Lando 3 app to Lando 4

```sh
lando4 app:config:translate --from lando3
lando4 app:config:translate --from lando3 --write
```

Run the first command from the app directory. It prints the Lando 4 Landofile layers your Lando 3 files would become, plus a diagnostic for every key it generated, rewrote, dropped, or could not carry. Nothing is written. Run the second when you like what you see. Lando 3's `lando` keeps running the app the whole time, because the old files are kept as backups beside the new ones.

Lando 4 never translates a Lando 3 file on the fly. Every `lando4` app command loads v4 only, so conversion is a step you take once, on purpose, and can undo.

## Detect or say so

```sh
lando4 app:config:translate --detect --format=json
```

`--detect` reports which translator matches your files without translating. A file with unambiguous Lando 3 spellings (`recipe:` plus `config:`, `portforward`, `overrides`, `api: 3`, a `.lando.recipe.yml` layer) matches `lando3` with `exact` confidence, and a bare `lando4 app:config:translate` picks it for you.

A Lando 3 file that is also valid v4 shape (a `name`, a couple of services with only `type`) is not auto-detected as Lando 3. Detection reports `lando4` instead, and the default run round-trips the file as v4. Pass `--from lando3` explicitly when you know where the file came from.

## Preview the layers

```sh
lando4 app:config:translate --from lando3
lando4 app:config:translate --from lando3 --format=json
```

The YAML output labels each block with a `# <path>` comment. The JSON preview carries `targets` (one `layer`, `path`, `content` per file), `diagnostics`, and `deletions`.

Each Lando 3 layer converts to the v4 layer of the same name:

| Lando 3 file | Lando 4 file |
| --- | --- |
| `.lando.base.yml` | `.lando.base.yml` |
| `.lando.dist.yml` | `.lando.dist.yml` |
| `.lando.upstream.yml` | `.lando.upstream.yml` |
| `.lando.yml` | `.lando.yml` |
| `.lando.local.yml` | `.lando.local.yml` |
| `.lando.user.yml` | `.lando.user.yml` |
| `.lando.recipe.yml` | folded into `.lando.dist.yml`, then removed |

`.lando.recipe.yml` has no v4 counterpart. Its recipe and config land in `.lando.dist.yml`, and the preview lists the file under `deletions` with the reason. `--write` removes it in the same transaction that writes the others, with a backup like every other replaced file.

### Recipes

A Lando 3 `recipe:` with its `config:` map becomes the matching bundled Lando 4 recipe with `options`:

| Lando 3 recipe | Lando 4 recipe |
| --- | --- |
| `drupal10`, `drupal11` | `drupal` (with `drupal: "10"` or `"11"` pinned) |
| `wordpress` | `wordpress` |
| `lamp` | `lamp` |
| `lemp` | `lemp` |
| `laravel` | `laravel` |
| `symfony` | `symfony` |
| `backdrop` | `backdrop` |
| `joomla` | `joomla` |
| `mean` | `mean` |

`config` keys the Lando 4 recipe has no option for are reported as `dropped`, once each, with a remediation telling you where to set the value by hand. Values the option does not accept (a PHP version the recipe no longer ships, a `webroot` without a leading `/`) stop the conversion with a `ConfigTranslateError` that names the accepted shape.

Hosting-platform recipes (`pantheon`, `platformsh`, `lagoon`, `acquia`), `drupal7` through `drupal9`, and any recipe Lando 4 does not bundle are rejected. The preview is empty, `--write` refuses, and your files do not change. Keep those apps on Lando 3 or replace the recipe with explicit v4 services first.

### Relocation

Lando 4 layers merge by adding and overriding. They cannot remove a key a lower layer set. When a Lando 3 higher layer removed something (a `portforward: false` in `.lando.local.yml` over a `portforward: true` in `.lando.yml`), the smallest affected unit moves up to the highest layer that last changed it, and the earlier layers stop defining it. One `needs-review` diagnostic per relocation names the key path, the layer it moved to, and the source files involved.

## Read the diagnostics

| Kind | Meaning | Blocks `--write` |
| --- | --- | --- |
| `generated` | Lando 4 produced this from a recipe or a default | No |
| `rewritten` | Same intent, new spelling (`portforward` became `ports`) | No |
| `dropped` | No Lando 4 equivalent; set it by hand if you still need it | No |
| `needs-review` | Converted, but look at it before you start the app | No |
| `non-portable` | Previews, but will not write | Yes |
| `unsupported` | Cannot be expressed; empties the preview | Yes |

Each diagnostic carries `sourceId`, `keyPath`, `message`, and `remediation`. Preview and `--write` emit the same list, so what you reviewed is what lands.

Two families of `needs-review` come from your commands rather than your keys. Any string that mentions `LANDO_INFO`, `LANDO_MOUNT`, `settings.lando.php`, or `wp-config` gets a review note: Lando 4 sets typed `LANDO_DB_*` and `LANDO_SERVICE_*` variables and never adds a `LANDO_INFO` alias, so a settings file that parses it needs your eyes. Any string that calls `lando <verb>` gets a reminder that `lando` still means Lando 3 after conversion.

`lando pull`, `lando push`, and `lando share` are `unsupported`. Lando 4 does not emulate hoster sync. Remove the command, move the workflow to `lando4 remote:add` and dataset `pull`/`push`, or keep that app on Lando 3.

## What converts and what doesn't

Nothing falls through silently. When a key has both a supported form and a rejected form, the rejected form blocks `--write`.

| Lando 3 input | Result | Blocks `--write` |
| --- | --- | --- |
| A bundled recipe from the table above, with its `config:` | The matching Lando 4 recipe with `options` | No |
| A service type and version Lando 4 ships (`php:8.3`, `mariadb:11.4`, `redis:7`) | The same service | No |
| Supported `portforward`, override fields, `build_as_root`, `build`, `run_as_root`, `run`, `proxy` routes, `tooling`, `events`, and text `!load` / `!import` | Native Lando 4 keys, with a `rewritten` or `generated` note | No |
| `moreHttpPorts`, `plugins`, `pluginDirs`, tooling `level`, `usage`, `examples`, and `interactive` | `dropped`, with where to set it by hand | No |
| `keys` | `dropped`. Lando 4 has no key list: the SSH agent sidecar loads your default `~/.ssh` keys, and hardware-backed or 1Password keys use [host agent forwarding](../subsystems/ssh-host-agent.mdx) with `sshAgent: { sidecar: false }` | No |
| A version Lando 4 doesn't ship (PHP 7.4, Node 18, MariaDB 10.6, Postgres 14, Solr 8) | `unsupported`, naming the versions you can pick | Yes |
| Compose `tty`, `stdin_open`, `links`, `network_mode`, or `container_name`; image build `ssh`; Varnish with several backends; Elasticsearch or OpenSearch `plugins` | `unsupported` | Yes |
| `lando pull`, `lando push`, or `lando share` in a command | `unsupported` | Yes |
| Hosting-platform recipes, `drupal7` through `drupal9`, and unbundled recipes | Rejected with `ConfigTranslateError` | Yes |

The versions that convert are exactly the ones listed in [Service type versions](../../reference/service-types.mdx). Lando 4 never builds an image for a version it doesn't ship. Bump the version in your Lando 3 file first, check the app still works on Lando 3, then convert.

## Write with backups

```sh
lando4 app:config:translate --from lando3 --write
lando4 app:config:lint
```

`--write` commits every layer through one managed-file transaction. Before any byte moves, each file it will replace or remove gets an immutable backup beside it named `<file>.bak.<sha256>`, where the digest is the SHA-256 of the original bytes. The write summary lists `written`, `backups`, and `removed`.

Three guarantees come with that transaction:

- Interrupted mid-write? The next `lando4` app command in that directory recovers the transaction or refuses to load until it is resolved. It never loads a half-converted app.
- Edited a file between preview and `--write`? The write fails and the originals stay untouched. Run the preview again.
- Want Lando 3 back? Copy each `.bak.<sha256>` file over its original name and delete `.lando.dist.yml` if the conversion created it. Lando 4 does not adopt Lando 3 containers or volumes, so the Lando 3 app is exactly where you left it.

After the write, `lando4 app:config:lint` reports the result valid, and `lando4 start` runs it as an ordinary Lando 4 app. Review every `needs-review` diagnostic before that first start.

## Convert one layer

```sh
lando4 app:config:translate --from lando3 --file .lando.local.yml
lando4 app:config:translate --from lando3 --file .lando.local.yml --write
```

`--file <layer>` writes only that layer. The lower layers are read as v4 context. Lando refuses, before writing anything, when doing the job right would need edits to a layer you did not select. The most common case is a `.lando.yml` that is still Lando 3: the error tells you to convert it first or run the full conversion without `--file`.

This is the fix for a shared repo where `.lando.yml` was converted upstream and only your `.lando.local.yml` is still Lando 3.

## Load before you convert

Lando 4 refuses Lando 3 files with a tagged error instead of guessing:

| Situation | Error | Remediation it prints |
| --- | --- | --- |
| `.lando.yml` is Lando 3 | `Lando3LandofileDetected` | `lando4 app:config:translate --from lando3 --write` |
| `.lando.yml` is v4, another layer is Lando 3 | `LandofileDialectMixError` | `lando4 app:config:translate --from lando3 --file <layer> --write` |

Every `lando4` app command (`info`, `start`, `exec`, `app:config:lint`) fails the same way until you convert. Lando 3's `lando` keeps working on the same files throughout.

<Guide>
  <Scenario id="detect-lando3-layers" render>
    <Step name="detect">
      <UseFixture name="lando3-layers" />
      <Run command="lando4 app:config:translate --detect --format=json" />
      <Verify command="lando4 app:config:translate --detect --format=json" expect={{ stdout: { regex: "\"translator\":\\s*\"lando3\"" } }} />
      <Verify command="lando4 app:config:translate --detect --format=json" expect={{ stdout: { regex: "\"confidence\":\\s*\"exact\"" } }} />
      <Verify command="lando4 app:config:translate --detect --format=json" expect={{ stdout: { regex: "recipe-layer" } }} />
    </Step>
    <Step name="detect-cleanup">
      <Cleanup />
    </Step>
  </Scenario>

  <Scenario id="preview-layers-and-relocation" render>
    <Step name="preview">
      <UseFixture name="lando3-layers" />
      <Run command="lando4 app:config:translate --from lando3" />
      <Verify command="lando4 app:config:translate --from lando3 --format=json" expect={{ stdout: { regex: "\"mode\":\\s*\"preview\"" } }} />
      <Verify command="lando4 app:config:translate --from lando3 --format=json" expect={{ stdout: { regex: "\"layer\":\\s*\"dist\"" } }} />
      <Verify command="lando4 app:config:translate --from lando3 --format=json" expect={{ stdout: { regex: "\"layer\":\\s*\"canonical\"" } }} />
      <Verify command="lando4 app:config:translate --from lando3 --format=json" expect={{ stdout: { regex: "\"layer\":\\s*\"local\"" } }} />
      <Verify command="lando4 app:config:translate --from lando3 --format=json" expect={{ stdout: { regex: "\"kind\":\\s*\"generated\"[\\s\\S]*Lando 4 recipe lamp generated this layer" } }} />
      <Verify command="lando4 app:config:translate --from lando3 --format=json" expect={{ stdout: { regex: "\"kind\":\\s*\"rewritten\"[\\s\\S]*Rewrote portforward as a ports publication" } }} />
      <Verify command="lando4 app:config:translate --from lando3 --format=json" expect={{ stdout: { regex: "\"kind\":\\s*\"needs-review\"[\\s\\S]*settings\\.lando\\.php or wp-config may parse LANDO_INFO" } }} />
      <Verify command="lando4 app:config:translate --from lando3 --format=json" expect={{ stdout: { regex: "\"kind\":\\s*\"needs-review\"[\\s\\S]*services\\.cache moved to the local layer because Lando 4 layers cannot remove it later" } }} />
      <Verify command="lando4 app:config:translate --from lando3 --format=json" expect={{ stdout: { regex: "\"deletions\":\\s*\\[\\s*\\{\\s*\"sourceId\":\\s*\"\\.lando\\.recipe\\.yml\"" } }} />
      <Verify file="lando3-layers/.lando.yml" expect={{ regex: "portforward: true" }} />
    </Step>
    <Step name="preview-cleanup">
      <Cleanup />
    </Step>
  </Scenario>

  <Scenario id="write-with-backups-and-recipe-removal" render>
    <Step name="write">
      <UseFixture name="lando3-layers" />
      <Run command="lando4 app:config:translate --from lando3 --write --format=json" />
      <Verify file="lando3-layers/.lando.dist.yml" expect={{ regex: "id: lamp" }} />
      <Verify file="lando3-layers/.lando.dist.yml" expect={{ regex: "runtime: 4" }} />
      <Verify file="lando3-layers/.lando.yml" expect={{ regex: "name: convert-demo" }} />
      <Verify file="lando3-layers/.lando.yml" expect={{ not: { regex: "portforward" } }} />
      <Verify file="lando3-layers/.lando.local.yml" expect={{ regex: "type: redis:7" }} />
      <Verify file="lando3-layers/.lando.yml.bak.d4d3d1bebb0dcaf589d5f595b353daa45884ff215e5372321679e86a23fd6512" expect={{ regex: "portforward: true" }} />
      <Verify file="lando3-layers/.lando.local.yml.bak.760c9d5ec10cf91bca3567255f32bf8faf962df7d9e87cfe1b9c1a9d244cc51c" expect={{ regex: "portforward: false" }} />
      <Verify file="lando3-layers/.lando.recipe.yml.bak.9ece7bf02ceb80a3a93b086bd12b57d42a6f9382a308493ba92cb3857e80a858" expect={{ regex: "recipe: lamp" }} />
    </Step>
    <Step name="lint-converted-app">
      <Run command="lando4 app:config:lint --format=json" />
      <Verify command="lando4 app:config:lint --format=json" expect={{ stdout: { regex: "\"valid\":\\s*true" } }} />
      <Verify command="lando4 app:config:translate --detect --format=json" expect={{ stdout: { not: { regex: "\\.lando\\.recipe\\.yml\"" } } }} />
    </Step>
    <Step name="write-cleanup">
      <Cleanup />
    </Step>
  </Scenario>

  <Scenario id="unsupported-blocks-write" render>
    <Step name="preview-blocked">
      <UseFixture name="lando3-blocked" />
      <Run command="lando4 app:config:translate --from lando3 --format=json" />
      <Verify command="lando4 app:config:translate --from lando3 --format=json" expect={{ stdout: { regex: "\"kind\":\\s*\"unsupported\"[\\s\\S]*Legacy lando pull hoster commands are not emulated by Lando 4" } }} />
      <Verify command="lando4 app:config:translate --from lando3 --format=json" expect={{ stdout: { regex: "\"kind\":\\s*\"dropped\"[\\s\\S]*config\\.xdebug has no option on the Lando 4 lamp recipe" } }} />
      <Verify command="lando4 app:config:translate --from lando3 --format=json" expect={{ stdout: { regex: "\"targets\":\\s*\\[\\s*\\]" } }} />
    </Step>
    <Step name="write-refused">
      <Run command="lando4 app:config:translate --from lando3 --write --format=json" expectExit={1} />
      <Verify errorTag="ConfigTranslateError" expect={{ regex: "Cannot write translation: unsupported" }} />
      <Verify errorTag="ConfigTranslateError" expect={{ regex: "Resolve the reported diagnostics before using --write" }} />
      <Verify file="lando3-blocked/.lando.yml" expect={{ regex: "cmd: lando pull" }} />
    </Step>
    <Step name="blocked-cleanup">
      <Cleanup />
    </Step>
  </Scenario>

  <Scenario id="unavailable-versions-block-write" render>
    <Step name="preview-versions">
      <UseFixture name="lando3-old-versions" />
      <Run command="lando4 app:config:translate --from lando3 --format=json" />
      <Verify command="lando4 app:config:translate --from lando3 --format=json" expect={{ stdout: { regex: "\"kind\":\\s*\"unsupported\"[\\s\\S]*php version 7\\.4 is not available in Lando 4" } }} />
      <Verify command="lando4 app:config:translate --from lando3 --format=json" expect={{ stdout: { regex: "mariadb version 10\\.6 is not available in Lando 4" } }} />
      <Verify command="lando4 app:config:translate --from lando3 --format=json" expect={{ stdout: { regex: "Choose a supported php version: 8\\.1" } }} />
      <Verify command="lando4 app:config:translate --from lando3 --format=json" expect={{ stdout: { regex: "\"targets\":\\s*\\[\\s*\\]" } }} />
    </Step>
    <Step name="write-refused-versions">
      <Run command="lando4 app:config:translate --from lando3 --write --format=json" expectExit={1} />
      <Verify errorTag="ConfigTranslateError" expect={{ regex: "Cannot write translation: unsupported" }} />
      <Verify file="lando3-old-versions/.lando.yml" expect={{ regex: "type: php:7\\.4" }} />
    </Step>
    <Step name="versions-cleanup">
      <Cleanup />
    </Step>
  </Scenario>

  <Scenario id="pantheon-rejected" render>
    <Step name="reject-hoster-recipe">
      <UseFixture name="pantheon-app" />
      <Run command="lando4 app:config:translate --from lando3 --format=json" expectExit={1} />
      <Verify errorTag="ConfigTranslateError" expect={{ regex: "recipe pantheon is a hosting-platform recipe with no Lando 4 counterpart" }} />
      <Run command="lando4 app:config:translate --from lando3 --write --format=json" expectExit={1} />
      <Verify errorTag="ConfigTranslateError" expect={{ regex: "Run the app with Lando 3 or replace the recipe with explicit v4 services" }} />
      <Verify file="pantheon-app/.lando.yml" expect={{ regex: "recipe: pantheon" }} />
      <Verify file="pantheon-app/.lando.yml" expect={{ not: { regex: "runtime: 4" } }} />
    </Step>
    <Step name="pantheon-cleanup">
      <Cleanup />
    </Step>
  </Scenario>

  <Scenario id="single-layer-refusal" render>
    <Step name="refuse-local-over-lando3-base">
      <UseFixture name="lando3-layers" />
      <Run command="lando4 app:config:translate --from lando3 --file .lando.local.yml --format=json" expectExit={1} />
      <Verify errorTag="ConfigTranslateError" expect={{ regex: "\\.lando\\.yml is not a v4 Landofile fragment" }} />
      <Verify errorTag="ConfigTranslateError" expect={{ regex: "run a full conversion without --file" }} />
      <Verify file="lando3-layers/.lando.local.yml" expect={{ regex: "portforward: false" }} />
    </Step>
    <Step name="refusal-cleanup">
      <Cleanup />
    </Step>
  </Scenario>

  <Scenario id="single-layer-write" render>
    <Step name="convert-local-layer">
      <UseFixture name="v4-with-lando3-local" />
      <Run command="lando4 app:config:translate --from lando3 --file .lando.local.yml --write --format=json" />
      <Verify file="v4-with-lando3-local/.lando.local.yml" expect={{ regex: "type: mariadb:11\\.4" }} />
      <Verify file="v4-with-lando3-local/.lando.local.yml" expect={{ not: { regex: "portforward" } }} />
      <Verify file="v4-with-lando3-local/.lando.local.yml.bak.bc2dca1f2d30f8401b264e035249049142fccf7ea749e0c3ae0a571a6cae1c81" expect={{ regex: "portforward: true" }} />
      <Verify file="v4-with-lando3-local/.lando.yml" expect={{ regex: "name: mixed-demo" }} />
      <Verify command="lando4 app:config:lint --format=json" expect={{ stdout: { regex: "\"valid\":\\s*true" } }} />
    </Step>
    <Step name="single-layer-cleanup">
      <Cleanup />
    </Step>
  </Scenario>

  <Scenario id="loader-error-before-conversion" render>
    <Step name="lando3-canonical-refused">
      <UseFixture name="lando3-layers" />
      <Run command="lando4 info --format=json" expectExit={1} />
      <Verify errorTag="Lando3LandofileDetected" expect={{ regex: "lando4 app:config:translate --from lando3 --write" }} />
    </Step>
    <Step name="loader-cleanup">
      <Cleanup />
    </Step>
  </Scenario>

  <Scenario id="loader-error-mixed-dialects" render>
    <Step name="lando3-secondary-layer-refused">
      <UseFixture name="v4-with-lando3-local" />
      <Run command="lando4 info --format=json" expectExit={1} />
      <Verify errorTag="LandofileDialectMixError" expect={{ regex: "lando4 app:config:translate --from lando3 --file \\.lando\\.local\\.yml --write" }} />
    </Step>
    <Step name="mixed-cleanup">
      <Cleanup />
    </Step>
  </Scenario>
</Guide>

## Next

- [Run Lando 4 next to Lando 3](../tutorial/from-lando-3.mdx)
- [Translate a config into a v4 Landofile](./config-translate.mdx)
- [Carry common Lando 3 app settings into Lando 4](./lando3-transition-options.mdx)
