---
id: config-lint
provider: test
platforms: [darwin, linux, win32]
timeout: 60000
tags: [beta, docs, landofile, config, lint]
---

# Lint a Landofile

```sh
lando app:config:lint
```

That validates the current app's Landofile against the canonical schema. It runs the JSON Schema only: no translators, no doctor checks, no provider probes. A clean Landofile exits `0`.

## Print JSON

```sh
lando app:config:lint --format=json
```

JSON output is a stable `ConfigLintResult` for editor integrations:

```
{ app, file, valid, violations: [{ path, message, suggestedFix?, line?, column? }] }
```

This shape is frozen by the schema artifact-set gate. `--format` defaults to `text`. Pass `--format=json` for machine output.

## Read schema violations

A schema violation exits non-zero. Each violation carries a dot-joined `path`, a `message`, and an optional `suggestedFix`. Unknown keys surface as excess-property violations (schema-only, never a `NotImplementedError`).

## Read rejected Compose keys

A rejected Compose key keeps its full authored path, for example `services.web.deploy.replicas`. The `suggestedFix` comes from the pinned Compose disposition matrix instead of a generic unknown-key message.

## Run it from doctor

`lando doctor --app` runs the same canonical-schema lint as part of the combined doctor report, under the `app-config-lint` check name. Both commands call the same `lintLandofile` pass.

<Guide>
  <Scenario id="clean-landofile" render>
    <Step name="lint">
      <UseFixture name="config-lint-demo" />
      <Run command="lando app:config:lint" />
    </Step>
  </Scenario>
</Guide>
