---
id: app-name-edge-cases
provider: test
timeout: 90000
tags: [beta, docs, landofile, naming]
---

# Name an app

`lando init` accepts lowercase kebab-case names. Spaces and uppercase letters fail validation.

A hand-authored Landofile can still use a display name that `init` would reject. Lando keeps that display name and derives a safe runtime slug for networks, hostnames, aliases, caches, and provider labels.

<Guide>
  <Scenario id="init-validates-app-names" render>
    <Step name="reject-non-kebab-name">
      <Variable name="invalidName" value="My App" display="My App" />
      <Run command="lando init --recipe node-api --name '{{invalidName}}' --yes --format=json" expectExit={1} />
      <Verify errorTag="PromptValidationError" expect={{ regex: "App name must be lowercase kebab-case\\." }} />
    </Step>
  </Scenario>

  <Scenario id="landofile-normalizes-runtime-name" render>
    <Step name="load-authored-name">
      <UseFixture name="non-kebab-landofile" />
    </Step>
    <Step name="inspect-raw-name">
      <Run command="lando app:config --format=json" />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "\"app\":\\s*\"My App\"" } }} />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "\"name\":\\s*\"My App\"" } }} />
    </Step>
    <Step name="start-with-normalized-runtime-name">
      <Run command="lando start --format=json" />
      <Verify event="post-start" />
    </Step>
    <Step name="cleanup">
      <Cleanup />
    </Step>
  </Scenario>
</Guide>

## Read the planner naming rules

The planner keeps the Landofile `name` as the user-facing display name. It derives the runtime slug by lowercasing ASCII letters, replacing each run of non-alphanumeric characters with one hyphen, and removing edge hyphens. A name with no ASCII letters or digits falls back to a stable hash of the app root.

Runtime slugs are capped at 57 characters. The `lando-` prefix consumes six characters, so a per-app network name stays within the DNS limit of 63 characters. For example, `c,o,m,m,a` becomes `c-o-m-m-a`, `bad name!` becomes `bad-name`, and `UPPER` becomes `upper`.

Default routes use `<service>.<slug>.lndo.site`. Shared-network aliases use `<service>.<slug>.internal`. The original display name never reaches those DNS surfaces.

A 100-character lowercase name is preserved in `lando app:config`, but its runtime slug is the first 57 characters. The network and route stay valid instead of producing an overlong label.
