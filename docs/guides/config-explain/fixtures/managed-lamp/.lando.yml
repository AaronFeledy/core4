name: explain-demo
recipe:
  id: lamp
  options:
    composer: "2"
    database: mariadb:11.4
    name: explain-demo
    php: "8.2"
    webroot: /app
  producer:
    contentDigest: sha256:bf832eeeb192425e56f0d69ee1989b7f28b3bcc19a5e3ed95d3ed7dba0f1c2e5
    manifestVersion: 0.1.0
    packageName: "@lando/recipe-lamp"
    recipeId: lamp
    sourceKind: bundled
  version: 0.1.0
runtime: 4
services:
  appserver:
    composer: "{{ recipe.composer }}"
    dependsOn:
      - database
    framework: none
    port: 80
    type: "php:{{ recipe.php }}"
    webroot: /app
  database:
    type: "{{ recipe.database }}"
tooling:
  composer:
    cmds:
      - composer
    description: "Run Composer inside the appserver service."
    service: appserver
  php:
    cmds:
      - php
    description: "Run the PHP CLI inside the appserver service."
    service: appserver
