name: catalog-endpoints
services:
  web:
    type: nginx
    endpoints:
      - _tag: internal
        name: browser
        protocol: http
        port: 80
    routes:
      - hostname: catalog-endpoints.lndo.site
        endpoint: browser
  cache:
    type: redis
    endpoints: []
