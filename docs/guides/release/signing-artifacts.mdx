---
id: release-signing-artifacts
provider: test
platforms: [linux, darwin, win32, wsl]
timeout: 60000
tags: [beta, docs, release, signing]
---

# Signing release artifacts

Platform signing breaks down like this:

- **macOS** — Developer ID, `notarytool`, and `stapler`.
- **Windows** — Authenticode `signtool` plus keyless `cosign`.
- **Linux** — GPG/cosign checksum manifests.

