---
id: release-linux-acceptance-rehearsal
provider: test
platforms: [linux, wsl]
timeout: 60000
tags: [beta, docs, release, acceptance]
---

# Linux acceptance rehearsal

`core/test/build/linux-acceptance-criteria-1-9.test.ts` runs focused
release and update spot checks that prove criteria 1-9 pass on the
reference `linux-x64` platform:

- **Criterion 1** — checksum-manifested release outputs, and a compiled
  `linux-x64` binary that launches and reports its version, proven by
  local rehearsal `SHA256SUMS` plus a nightly distribution-rehearsal
  `linux-x64 --version` run.
- **Criterion 2** — gated timing evidence for single-platform and matrix
  release budgets, from CI timeouts plus release compile budget logging.
- **Criteria 3-5** — linux-x64 signing policy, supply-chain artifacts, and
  cosign verification, proven in `release.test.ts` (`SHA256SUMS`, SBOM,
  SLSA, `cosign verify-blob`).
- **Criteria 6-9** — signed manifest trust, `lando update` replacement,
  launch-probe rollback, and `EACCES` without silent elevation, proven in
  `update-manifest.test.ts` (signature verify, self-update,
  `UpdateLaunchProbeError` rollback, `UpdatePermissionError`).

