---
id: release-local-rehearsal
provider: test
platforms: [linux, darwin, win32, wsl]
timeout: 60000
tags: [beta, docs, release]
---

# Local release rehearsal

Use `LOCAL_REHEARSAL=1` to run the release pipeline through a chosen stage
without signing credentials. `--binary --through-stage=7-compile` runs the
binary pipeline through the current-platform compile stage; `--library
--through-stage=11-manifest` runs the library pipeline through manifest
writing. Missing signing credentials warning-skip in local rehearsal.

