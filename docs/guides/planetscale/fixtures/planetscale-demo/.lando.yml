name: planetscale-demo
services:
  app:
    type: "node:22"
    primary: true
    env_file:
      - .env
  db:
    type: mysql
    creds:
      user: lando
      password: lando
      database: planetscale_demo
tooling:
  npm:
    service: app
    cmd: npm
  node:
    service: app
    cmd: node
