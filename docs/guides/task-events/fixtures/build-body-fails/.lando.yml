name: task-events-body-fails
services:
  web:
    type: "node:22"
    primary: true
tooling:
  build:
    service: ":host"
    cmds:
      - cmd: echo "body" >> markers.txt
      - cmd: exit 3
events:
  pre-build:
    - cmd: echo "before" >> markers.txt
      service: ":host"
  post-build:
    - cmd: echo "after" >> markers.txt
      service: ":host"
