name: task-events-post-fails
services:
  web:
    type: "node:22"
    primary: true
tooling:
  build:
    service: ":host"
    cmd: echo "build ran"
events:
  post-build:
    - cmd: echo "post-build is about to fail" && exit 7
      service: ":host"
