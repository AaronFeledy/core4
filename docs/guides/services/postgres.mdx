---
id: postgres
provider: test
timeout: 90000
tags: [beta, docs, postgres, service, database]
---

# Add PostgreSQL

Put `type: postgres` in your services block. Lando pulls `postgres:16` and listens on `5432`:

```yaml
name: postgres-demo
services:
  db:
    type: postgres
```

Set `creds:` when you want explicit values. Omit `password` and Lando generates one. Do not assume it is `lando`.

```yaml
services:
  db:
    type: postgres
    creds:
      user: lando
      database: postgres-demo
```

Start it. Lando puts the database on the app network:

<Guide>
  <Scenario id="happy-path" render>
    <Step name="start">
      <UseFixture name="postgres-demo" />
      <Run command="lando start" />
      <Verify event="post-start" />
    </Step>
    <Step name="cleanup">
      <Cleanup />
      <Run command="lando destroy -y" />
    </Step>
  </Scenario>

## Connect from another service

Other services use the service name as the hostname. Here that's `db`. Defaults:

| Setting | Value |
| --- | --- |
| Host | `db` |
| Database | the app name (`postgres-demo`); override with `database:` under `services.db` |
| User | `lando` |
| Password | generated when omitted; read it from `lando info` |

The password is generated when you omit it, so read the exact value from `lando info`. Don't hardcode it. From inside the app network the connection URL is:

```
postgresql://lando:<password>@db:5432/postgres-demo
```

## Open the client

The `postgres` type ships the `psql` client task. After start:

```sh
lando psql
```

Import a dump with `lando db:import`. See [Import and export app databases](../tooling/db-import-export.mdx). You can still pipe into `psql` with `lando exec`.

## Use your own postgresql.conf

Set `config.server` to an app-relative file when the stock config isn't enough:

```yaml
services:
  db:
    type: postgres
    config:
      server: ./config/postgresql.conf
```

Lando mounts that file read-only at `/etc/lando/postgresql.conf` and starts the server with `-c config_file=/etc/lando/postgresql.conf`. It has to be a regular file, not a directory.

The path has to stay inside the app root. Absolute paths, paths that climb out of the app root, and symlinks that point outside it are rejected when Lando plans the app, before anything starts.

If you also set `command:` or `entrypoint:` on the service, yours wins. Lando won't append the `-c config_file=...` flag to a command you wrote yourself, so pass it in your own command if you still want the file loaded.

## Pin a version

Set `image:` when `postgres:16` isn't the version you want:

```yaml
services:
  db:
    type: postgres
    image: postgres:15
```

</Guide>
