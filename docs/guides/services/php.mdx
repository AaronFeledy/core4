---
id: php
provider: test
timeout: 90000
tags: [beta, docs, php, service, runtime]
---

<Guide>
  <Scenario id="happy-path" render>
    <Step name="scaffold">
      <UseFixture name="php-demo" />
      <Variable name="serviceType" value="php:8.3" display="Declare type: php:8.3 for the primary service (PHP has no bare type: php alias — pin a version)" />
      <Variable name="supportedVersions" value="php:8.2, php:8.3" display="Supported PHP service versions" />
      <Variable name="defaultImage" value="php:8.3-apache" display="Default Docker image — the -apache variant serves your code over HTTP on port 80" />
      <Variable name="defaultPort" value="80" display="Default HTTP port the bundled Apache serves on" />
      <Variable name="appMount" value="/app" display="Your repository is bind-mounted read-write at /app" />
    </Step>
    <Step name="start">
      <Run command="lando start" />
      <Verify event="post-start" />
    </Step>
    <Step name="cleanup">
      <Cleanup />
      <Run command="lando destroy -y" />
    </Step>
  </Scenario>

  <Scenario id="framework-presets" render={false} reason="The framework field sets the served webroot for common PHP frameworks without hand-editing the document root">
    <Step name="frameworks">
      <Variable name="supportedFrameworks" value="drupal, wordpress, laravel, symfony, none" display="Set framework: under the service to apply a preset" />
      <Variable name="drupalRoot" value="framework: drupal → webroot /app/web" display="Drupal serves from the web/ subdirectory" />
      <Variable name="laravelRoot" value="framework: laravel → webroot /app/public" display="Laravel and Symfony serve from the public/ subdirectory" />
      <Variable name="wordpressRoot" value="framework: wordpress → webroot /app" display="WordPress and none serve from the app root" />
    </Step>
  </Scenario>

  <Scenario id="composer-tooling" render={false} reason="PHP projects typically expose Composer as a tooling alias targeting the service">
    <Step name="composer">
      <Variable name="toolingAlias" value="lando composer install" display="Declare a composer tooling alias targeting the php service to manage dependencies" />
      <Variable name="phpCli" value="lando php -v" display="A php alias runs the CLI inside the service" />
    </Step>
  </Scenario>

  <Scenario id="version-override" render={false} reason="Pin a different supported PHP minor version, or set image to use a php-fpm or CLI base image instead of -apache">
    <Step name="note">
      <Variable name="pinVersion" value="type: php:8.2" display="Pin PHP 8.2 by setting type: php:8.2 under services.app" />
      <Variable name="fpmImage" value="image: php:8.3-fpm" display="Use image: php:8.3-fpm under services.app to pair PHP-FPM with a separate nginx service" />
    </Step>
  </Scenario>
</Guide>
