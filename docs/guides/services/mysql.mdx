---
id: mysql
provider: test
timeout: 90000
tags: [beta, docs, mysql, service, database]
---

<Guide>
  <Scenario id="happy-path" render>
    <Step name="scaffold">
      <UseFixture name="mysql-demo" />
      <Variable name="serviceType" value="mysql" display="Declare type: mysql in your services block" />
      <Variable name="defaultImage" value="mysql:8.0" display="Default Docker image" />
      <Variable name="defaultPort" value="3306" display="Default TCP port" />
    </Step>
    <Step name="start">
      <Run command="lando start" />
      <Verify event="post-start" />
    </Step>
    <Step name="cleanup">
      <Cleanup />
      <Run command="lando destroy -y" />
    </Step>
  </Scenario>

  <Scenario id="connection-details" render={false} reason="Documents the in-container connection credentials a dependent app uses; lando info surfaces the same values once the app is running">
    <Step name="credentials">
      <Variable name="host" value="db" display="Connect from another service using the service name db as the hostname" />
      <Variable name="database" value="mysql-demo" display="Default database name — the app name (set database: under services.db to override)" />
      <Variable name="user" value="lando" display="Default application user" />
      <Variable name="password" value="lando" display="Default application password" />
      <Variable name="rootPassword" value="derived per app; read it from lando info" display="The root password is derived per app and surfaced by lando info, not a fixed value" />
      <Variable name="internalUrl" value="mysql://lando:lando@db:3306/mysql-demo" display="Connection URL from inside the app network" />
    </Step>
  </Scenario>

  <Scenario id="cli-access" render={false} reason="The fixture can define a tooling alias that opens the mysql client against the running database">
    <Step name="client">
      <Variable name="toolingAlias" value="lando mysql" display="Declare a tooling alias targeting the db service to open the mysql client" />
      <Variable name="importNote" value="lando db-import dump.sql" display="A common pattern is a db-import tooling alias that pipes a SQL file into the client" />
    </Step>
  </Scenario>

  <Scenario id="version-override" render={false} reason="Use image to pin a specific MySQL version when the default mysql:8.0 image is not desired">
    <Step name="note">
      <Variable name="pinImage" value="image: mysql:8.4" display="Pin MySQL 8.4 via image: mysql:8.4 under services.db" />
    </Step>
  </Scenario>
</Guide>
