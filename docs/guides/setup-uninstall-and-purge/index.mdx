---
id: setup-uninstall-and-purge
provider: test
defaultLayer: e2e
platforms: [darwin, linux, win32]
timeout: 120000
tags: [beta, docs, setup, uninstall, "@smoke"]
---

# Uninstall and purge

Uninstall is host-destructive once confirmed, so preview it first.
`uninstall --dry-run` prints the full owned, user-owned, skipped, and
manual-remediation plan without changing the system. Without `--yes`,
destructive execution is refused, and the same plan tells you to rerun
after review. `--yes` is required before any Lando-owned toolchain or data
target gets removed.

## Keep data vs. purge

```sh
# Removes Lando-owned toolchain targets while preserving user data roots,
# app data, global app state, and app-affecting caches
uninstall --yes --keep-data

# Opts into deliberate removal of Lando-owned data and cache roots
uninstall --yes --purge
```

**With `--purge`**, Lando will:
- **Stop and remove** any running Lando-labeled containers (both `dev.lando.app` and `com.lando.app` labels)
- **Prune** unused Lando networks (labeled `dev.lando.network=true`)
- **Prune** unused Lando volumes (labeled `dev.lando.volume=true`)
- Remove user data root (`~/.local/share/lando` or `$LANDO_USER_DATA_ROOT`)
- Remove user cache root (`~/.cache/lando` or `$LANDO_USER_CACHE_ROOT`)
- Remove user config root (`~/.config/lando` or `$LANDO_USER_CONF_ROOT`)

If Lando cannot query the container runtime (docker/podman unavailable,
timeout, or error), `--purge` **fails closed**: it exits with non-zero
status and does **not** delete your data. This ensures uninstall never
orphans running containers by deleting state while they're still up.

A binary outside Lando's managed bin directory is never removed
automatically — the plan prints exact manual removal steps for it instead.

<Guide>
  <Scenario id="keep-data-preserves-state" render>
    <Step name="scaffold">
      <Run command="lando init --recipe node-api --name keep-data-demo --yes" />
    </Step>
    <Step name="start">
      <Run command="lando start" />
      <Verify
        expect={{
          exitCode: 0,
          stdout: { contains: "running" }
        }}
      />
    </Step>
    <Step name="uninstall-keep-data">
      <Run command="lando uninstall --yes --keep-data" />
    </Step>
    <Step name="verify-data-preserved">
      <Verify
        command="test -d ~/.local/share/lando && echo 'preserved' || test -d ~/Library/Application\\ Support/lando && echo 'preserved' || echo 'missing'"
        expect={{
          stdout: { contains: "preserved" }
        }}
      />
    </Step>
    <Step name="cleanup">
      <Cleanup />
      <Run command="lando destroy -y" />
    </Step>
  </Scenario>

  <Scenario id="purge-cleans-leftovers" render>
    <Step name="scaffold">
      <Run command="lando init --recipe node-api --name purge-demo --yes" />
    </Step>
    <Step name="start-app">
      <Run command="lando start" />
      <Verify
        expect={{
          exitCode: 0,
          stdout: { contains: "running" }
        }}
      />
    </Step>
    <Step name="purge-cleans-up">
      <Run command="lando uninstall --yes --purge" />
    </Step>
    <Step name="verify-no-lando-containers">
      <Verify
        command={`
          errors=0
          podman_out=""
          docker_out=""
          
          if podman ps -a --filter label=dev.lando.app --format '{{.Names}}' 2>/dev/null; then
            podman_out=$(podman ps -a --filter label=dev.lando.app --format '{{.Names}}' 2>/dev/null)
          else
            errors=$((errors + 1))
          fi
          
          if docker ps -a --filter label=dev.lando.app --format '{{.Names}}' 2>/dev/null; then
            docker_out=$(docker ps -a --filter label=dev.lando.app --format '{{.Names}}' 2>/dev/null)
          else
            errors=$((errors + 1))
          fi
          
          if [ $errors -eq 2 ]; then
            echo "ERROR: Neither podman nor docker available" >&2
            exit 1
          fi
          
          if [ -n "$podman_out" ] || [ -n "$docker_out" ]; then
            echo "ERROR: Found leftover containers" >&2
            echo "Podman: $podman_out" >&2
            echo "Docker: $docker_out" >&2
            exit 1
          fi
          
          echo "OK: No leftover containers"
        `}
        expect={{
          exitCode: 0,
          stdout: { contains: "OK: No leftover containers" }
        }}
      />
    </Step>
    <Step name="verify-no-lando3-containers">
      <Verify
        command={`
          errors=0
          podman_out=""
          docker_out=""
          
          if podman ps -a --filter label=com.lando.app --format '{{.Names}}' 2>/dev/null; then
            podman_out=$(podman ps -a --filter label=com.lando.app --format '{{.Names}}' 2>/dev/null)
          else
            errors=$((errors + 1))
          fi
          
          if docker ps -a --filter label=com.lando.app --format '{{.Names}}' 2>/dev/null; then
            docker_out=$(docker ps -a --filter label=com.lando.app --format '{{.Names}}' 2>/dev/null)
          else
            errors=$((errors + 1))
          fi
          
          if [ $errors -eq 2 ]; then
            echo "ERROR: Neither podman nor docker available" >&2
            exit 1
          fi
          
          if [ -n "$podman_out" ] || [ -n "$docker_out" ]; then
            echo "ERROR: Found leftover containers" >&2
            echo "Podman: $podman_out" >&2
            echo "Docker: $docker_out" >&2
            exit 1
          fi
          
          echo "OK: No leftover containers"
        `}
        expect={{
          exitCode: 0,
          stdout: { contains: "OK: No leftover containers" }
        }}
      />
    </Step>
    <Step name="cleanup">
      <Cleanup />
    </Step>
  </Scenario>
</Guide>

## Resumable reports

A failed confirmed uninstall writes a resumable report containing
completed, failed, skipped, and manual steps, at
`<userDataRoot>/uninstall/report.json` or
`<os-tmpdir>/lando-uninstall-*/lando-uninstall-report.json`. If a purge
failure already removed the data root, the report falls back to that
private tmpdir path instead. After remediation, rerun the same `keep-data`
or `purge` command to resume the remaining owned removals.
