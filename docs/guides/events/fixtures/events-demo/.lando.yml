name: events-demo
services:
  web:
    type: "node:22"
    primary: true
events:
  pre-start:
    - cmd: echo preparing-start
      service: ":host"
  post-start:
    - task: warm-cache
  pre-stop:
    - cmd: echo before-stop-first
      service: web
    - cmd: echo before-stop-second
      service: web
  post-stop:
    - cmd: echo after-stop
      service: web
tooling:
  warm-cache:
    service: web
    cmd: echo cache-ready
