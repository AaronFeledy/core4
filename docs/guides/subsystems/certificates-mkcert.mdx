---
id: certificates-mkcert
provider: test
platforms: [darwin, linux, win32]
timeout: 60000
tags: [beta, docs, subsystems, certs, mkcert, ca]
---

# Trust local HTTPS certificates

Run `lando setup` so Lando installs mkcert and trusts the local CA on this machine. After that, browsers trust the HTTPS URLs Traefik serves.

```sh
lando setup
lando doctor
```

You do not start Traefik by hand. See [Use the Traefik proxy](./proxy-traefik.mdx).

## Issue a service certificate

```yaml
services:
  appserver:
    certs: true
```

Disable issuance:

```yaml
services:
  appserver:
    certs: false
```

Supply your own cert and key:

```yaml
services:
  appserver:
    certs:
      cert: ./certs/appserver.crt
      key: ./certs/appserver.key
```

The object form requires both `cert` and `key`. Custom paths must stay inside the app root and be readable.

With `certs: true`, Lando issues a leaf that covers the service name, its internal alias, `hostnames:`, proxied hostnames, `localhost`, and `127.0.0.1`.

Inside the service the cert lands at:

```text
LANDO_SERVICE_CERT=/etc/lando/certs/leaf/<service>.crt
LANDO_SERVICE_KEY=/etc/lando/certs/leaf/<service>.key
```

`certs: false` leaves both unset.

## Skip host trust

```sh
lando setup --skip-install-ca
```

That provisions mkcert without writing to the host trust store. Browsers will warn on HTTPS until you trust the CA yourself.

If `certs: true` runs before setup, the command fails and tells you to run `lando setup`.

## Verify

```sh
lando doctor
```

The certs check should pass after setup.
