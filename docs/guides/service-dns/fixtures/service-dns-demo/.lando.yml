name: service-dns-demo
services:
  app:
    type: "php:8.3"
    primary: true
    db_client: auto
  database:
    type: mysql
    creds:
      user: app
      password: secret
      database: app
tooling:
  db-query:
    service: app
    cmd: mysql -h database -u app app -e "SELECT 1"
