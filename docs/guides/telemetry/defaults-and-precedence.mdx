---
id: telemetry-defaults-and-precedence
provider: test
platforms: [darwin, linux, win32, wsl]
timeout: 60000
tags: [beta, docs, telemetry, config]
---

# Telemetry defaults and precedence

CLI runs construct telemetry as enabled when no flag, env overlay, or config value overrides it —
`GlobalConfig` decoding treats an omitted telemetry setting as enabled for CLI config
(`telemetry.enabled=true`).

Library runtimes default the other way: telemetry stays off unless the embedding host explicitly
opts in with `telemetry: true` at runtime construction.

## Precedence chain

The highest-precedence telemetry source wins, in this order:

```text
runtime option > env > config > default
```

- An env overlay (`LANDO_CONFIG__TELEMETRY__ENABLED=0`) disables telemetry before runtime
  construction, even if `config.yml` enables it.
- A valid global config setting (`telemetry.enabled: false`) disables CLI telemetry when no env
  overlay is present.
- An explicit runtime telemetry option (`telemetry: false`) wins before env and config resolution.

<Guide>
  <Scenario id="telemetry-config-check" render={false} reason="Defaults and precedence above are covered by the CLI runtime telemetry tests, env-overlay service tests, and makeLandoRuntime tests without needing a provider-backed app. This scenario keeps one real CLI check in the guide harness.">
    <Step name="config-check">
      <Run command="lando config --format=json" />
      <Verify command="lando config --format=json" expect={{ stdout: { regex: "\"config\":\\s*\\{" } }} />
    </Step>
  </Scenario>
</Guide>
