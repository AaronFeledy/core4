---
id: everyday-commands
provider: test
timeout: 60000
tags: [beta, docs, cli]
---

# Run the everyday CLI loop

Start the app, inspect it, work inside it, then tear it down.

```sh
lando start
lando info
```

If you only memorize four of these, make them `start`, `info`, `exec`, and `destroy`.

## Everyday commands

| Command | What it does |
| --- | --- |
| `lando start` | Start this app. Alias of `app:start`. |
| `lando info` | Print services, URLs, and connection details. Alias of `app:info`. |
| `lando exec -- <cmd>` | Run a one-off command in the primary service. Alias of `app:exec`. |
| `lando logs` | Stream service logs. Alias of `app:logs`. |
| `lando stop` | Stop this app. Keeps volumes. Alias of `app:stop`. |
| `lando restart` | Stop then start. Alias of `app:restart`. |
| `lando rebuild` | Recreate containers from the current Landofile. Alias of `app:rebuild`. |
| `lando destroy -y` | Remove this app's containers and networks. Volumes stay unless you pass `--volumes` or `--purge`. Alias of `app:destroy`. |
| `lando doctor` | Report setup, provider, proxy, and cert health. Alias of `meta:doctor`. |
| `lando poweroff -y` | Stop every Lando app on this host. Alias of `apps:poweroff`. |
| `lando list` | List apps on this host. Alias of `apps:list`. |
| `lando share` | Start a public tunnel. Use `--target` for a non-primary service or extra port. Alias of `app:share`. |

## Work inside a service

```sh
lando exec -- node --version
lando exec db -- psql --version
lando logs --service app --follow
```

`lando ssh` opens an interactive `sh -l` in the primary service. See [Open a shell in a service](./ssh.mdx).

## Stop, restart, rebuild

```sh
lando stop
lando start
lando restart
lando rebuild
```

`restart` cycles running processes. `rebuild` recreates containers. Use `rebuild` after you change service types, images, or build steps.

## Wait for another command to finish

`start`, `stop`, `restart`, `rebuild`, and `destroy` take a per-app advisory lock so two processes cannot apply provider state at the same time. A second command waits and prints that another Lando command holds the app lock. After two minutes it fails. Set `LANDO_APP_LOCK_TIMEOUT_MS` (milliseconds) to wait longer or shorter.

Read-only commands (`info`, `logs`, `app:config:lint`) do not take the lock.

The lock key is the stable app id plus the canonical (realpath) app root at acquire time. If you move the app directory while a mutate is running, a command against the new path sees a different key and does not wait on the first lock.

A mutating command that shells out to another `lando` mutate on the same app reuses the lock it already holds. It does not deadlock.

## Tear down

`destroy` deletes this app's containers and networks. Volumes stay unless you pass `--volumes` or `--purge`.

```sh
lando destroy -y
```

`-y` skips the confirmation prompt.

To stop everything Lando is running:

```sh
lando list
lando poweroff -y
```

`lando list` marks an app as stale when its recorded root no longer exists.
Pruning also requires the provider to confirm that no matching containers or
volumes remain. Remove those exact stale records with:

```sh
lando list --prune
```

Pruning removes only the applied-plan and directory-cache records reported by
the command. Missing or unreadable provider state is not enough evidence to
delete a record.

<Guide>
  <Scenario id="list-and-prune-empty-inventory" render>
    <Step name="list-apps">
      <Run command="lando list" />
      <Verify command="lando list" expect={{ stdout: { regex: "No Lando apps applied on this host|APP\\s+STATUS" } }} />
    </Step>
    <Step name="prune-stale-inventory">
      <Run command="lando list --prune" />
      <Verify command="lando list --prune" expect={{ stdout: { regex: "Pruned 0 stale inventory entries" } }} />
    </Step>
  </Scenario>
</Guide>

`poweroff` reaches beyond this app. Run it only when you mean to stop every app. `--keep-global` leaves shared host services such as the proxy up. `--keep-scratch` leaves scratch apps up.

## Check the host

```sh
lando doctor
```

Run doctor after `lando setup`. The default provider is `lando` (managed Podman). After a successful `lando setup --provider=docker`, later doctor and start stay on Docker. `LANDO_PROVIDER` does not persist.

## Next

Walk the same loop as a tutorial: [Start, inspect, and tear down an app](../tutorial/app-lifecycle.mdx). Refresh the plan without starting services: [Refresh a stale app cache](./cache-refresh.mdx). For renderer modes, `--debug`, and `--log-level`, see [See more CLI output](./verbosity-and-debug.mdx). Point a tunnel at Storybook or Vite: [Share a service or extra port](./share-target.mdx).
