---
id: terminal-ui-polish
provider: test
platforms: [darwin, linux, win32, wsl]
timeout: 60000
tags: [beta, docs, cli, renderer]
---

<Guide>
  <Scenario id="spaceship-console-language" render={false} reason="Renderer unit tests drive deterministic task-event fixtures at 80, 100, and 120 columns, so guide generation does not need to spawn an interactive TTY">
    <Step name="direction">
      <Variable name="tone" value="compact spaceship operations console" display="The default lando renderer presents task progress like mission-control telemetry, not generic colorful logs" />
      <Variable name="palette" value="deep-space base, cyan/teal telemetry accents, amber warning, red blocked, green online" display="Color creates hierarchy, but every status also carries text so status is never color-only" />
      <Variable name="borders" value="LANDO OPS panel separators" display="Frames use quiet panel separators, cockpit-style section headers, and dimmed detail tails rather than dense ASCII art" />
    </Step>
    <Step name="status-chips">
      <Variable name="running" value="[RUNNING]" display="Active tasks and parent frames use a scanner/progress state with a text chip" />
      <Variable name="online" value="[ONLINE]" display="Completed work renders with an explicit online chip plus the existing success glyph" />
      <Variable name="cached" value="[CACHED]" display="Completed work reused from cache renders with a cached chip plus the success glyph, derived from a delimited summary marker such as (cached) or · cached" />
      <Variable name="skipped" value="[SKIPPED]" display="Work skipped because it was unnecessary renders with a skipped chip plus the success glyph instead of a silent omission, derived from a (skipped) or · skipped summary marker" />
      <Variable name="blocked" value="[BLOCKED]" display="Failures render with a blocked chip, exit/remediation text, and a red accent in TTY mode" />
      <Variable name="wait" value="[WAIT]" display="Declared-but-not-started children stay visible as wait-state rows" />
    </Step>
    <Step name="layout-rules">
      <Variable name="headings" value="LANDO OPS headings" display="Every panel opens with a compact cockpit heading so users can identify the current phase before reading task rows" />
      <Variable name="spacing" value="two-space row rhythm" display="Task rows, detail tails, and footer telemetry keep consistent spacing so dense output remains scannable" />
      <Variable name="icons" value="glyph plus text status" display="Glyphs such as check, cross, warning, and wait markers never stand alone; each state also carries a status chip or remediation text" />
      <Variable name="dimming" value="dimmed detail tails" display="Low-priority task detail lines and telemetry separators dim behind the status rows while preserving readable contrast" />
      <Variable name="progressRails" value="progress rails stay semantic" display="Rails and scanner motion are reserved for live RUNNING work; static summaries use telemetry footers instead of decorative animation" />
    </Step>
    <Step name="widths-and-fallbacks">
      <Variable name="fixtures" value="80 columns, 100 columns, 120 columns" display="Golden fixtures prove common terminal widths stay readable, with long paths wrapped inside the panel rhythm" />
      <Variable name="machineModes" value="plain, json, non-TTY, and CI output stay undecorated" display="Machine-readable and non-interactive paths keep stable parseable output; decorative ANSI belongs only to TTY lando frames" />
      <Variable name="example" value="@lando/renderer-lando contributes.renderers: [lando]" display="Renderer authors can inspect the bundled first-party plugin as the maintained example for renderer contribution metadata" />
    </Step>
    <Step name="anti-patterns">
      <Variable name="rainbow" value="no undifferentiated rainbow logs" display="Color is constrained to telemetry accents and status severity instead of cycling arbitrary rainbow output" />
      <Variable name="purple" value="no SaaS-purple gradients" display="The palette avoids default purple-on-white SaaS styling in favor of deep-space contrast and cyan/teal accents" />
      <Variable name="ascii" value="no dense ASCII art" display="Panel borders stay sparse and scan-friendly; decoration must not make small terminals harder to read" />
      <Variable name="motion" value="no decorative motion" display="Animation belongs only to live state transitions, never as unrelated ornament" />
    </Step>
  </Scenario>
  <Scenario id="opentui-substrate-baseline" render={false} reason="Import-boundary, compiled-binary, headless frame, and PTY tests exercise the native renderer substrate without requiring guide generation to own a terminal">
    <Step name="lazy-substrate">
      <Variable name="substrate" value="@opentui/core ^0.4.3" display="The bundled renderer owns the OpenTUI dependency and loads it lazily only for an interactive TTY prompt or task tree" />
      <Variable name="machineModes" value="level-none, plain, json, non-TTY, and CI never load OpenTUI" display="Cold-start and machine-readable paths stay independent of the native renderer substrate" />
    </Step>
    <Step name="split-footer-task-tree">
      <Variable name="liveRegion" value="split-footer with captured stdout" display="Running task rows stay pinned at the bottom while messages and completed summaries commit once to native terminal scrollback above them" />
      <Variable name="resize" value="reset and replay semantic scrollback" display="A terminal resize reflows the current footer and replays retained committed lines without cursor rewinds, duplicates, or torn regions" />
      <Variable name="fullTail" value="Enter uses alternate screen; Esc restores split-footer" display="Output capture is suspended before the screen-mode transition and restored only after returning to split-footer" />
      <Variable name="animation" value="100 ms spinner threshold at no more than 30 fps" display="Continuous rendering starts only when a spinner is visible and stops on progress, completion, failure, or scope cleanup" />
    </Step>
    <Step name="degradation">
      <Variable name="fallback" value="line input for the remainder of the process" display="A missing native binding or unsupported terminal never fails the command; one debug notice records the permanent degradation" />
      <Variable name="compiled" value="one target-matching native library embedded in each release binary" display="Relocated binaries need no node_modules or loose native-library sidecar" />
    </Step>
  </Scenario>
</Guide>
