---
id: compose-compatibility
provider: test
platforms: [linux, darwin, win32, wsl]
timeout: 60000
tags: [beta, docs, config, compose]
---

<Guide>
  <Scenario id="compose-key-matrix" render={false} reason="The schema-backed matrix is locked by build tests; this guide publishes the user-facing contract without running provider-backed Compose workloads">
    <Step name="accepted-top-level-keys">
      <Variable name="accepted" value="services, volumes, networks, configs, secrets, include, x-*" display="These Compose top-level keys are accepted directly in a Landofile when their values match Lando's supported schema subset" />
      <Variable name="deprecated" value="version" display="Compose version is accepted for compatibility, ignored by Lando, and should be removed from new Landofiles" />
      <Variable name="rejected" value="profiles, extensions, arbitrary Compose project keys" display="Unsupported Compose top-level keys fail closed with remediation instead of being silently dropped" />
    </Step>
  </Scenario>

  <Scenario id="accepted-compose-keys" render={false} reason="Focused schema and lint tests exercise accepted key decoding and lint output">
    <Step name="portable-subset">
      <Variable name="services" value="services" display="Declare service definitions using the supported ServiceConfig fields" />
      <Variable name="resources" value="volumes, networks, configs, secrets" display="Declare named Compose resources with the supported portable fields" />
      <Variable name="include" value="include" display="Reference Compose fragments through the supported compatibility surface" />
      <Variable name="extensions" value="x-*" display="Preserve top-level Compose extension metadata under x-prefixed keys" />
    </Step>
  </Scenario>

  <Scenario id="rejected-key-remediation" render={false} reason="app:config:lint tests assert the exact structured remediation text">
    <Step name="fail-closed">
      <Variable name="profiles" value="profiles" display="Rejected; split profile-specific config into separate Landofile fragments and select them with includes:" />
      <Variable name="extensions" value="extensions" display="Rejected; Compose extensions must use x-* keys or provider-specific providers.&lt;id&gt; data" />
      <Variable name="unknown" value="unknown top-level key" display="Rejected by the canonical schema with a remove-or-translate remediation" />
    </Step>
  </Scenario>
</Guide>
