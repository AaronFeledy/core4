---
id: scratch-gc
provider: test
platforms: [darwin, linux, win32]
timeout: 60000
tags: [beta, docs, scratch, gc]
---

# Reap orphaned scratch apps

`lando scratch:gc` reports leftover scratch apps. It deletes nothing until you pass `--prune`.

```sh
lando scratch:gc
```

Dry run first. Then prune. Scratch gc is a janitor with a clipboard, not a flamethrower.

## Inspect

```sh
lando scratch:gc
lando scratch:list
```

`lando scratch:gc` is the alias of `apps:scratch:gc`. The report prints `inspected`, `reaped`, and `errors` counts.

Without `--prune`, Lando only counts orphans. Registry entries, cache directories, and provider resources stay put.

## Prune

```sh
lando scratch:gc --prune
```

`--prune` removes orphaned scratch ids: provider resources, the scratch cache directory, and the registry entry.

A missing resource or directory still counts as success. A second `--prune` should report no remaining work.

Orphans include:

- a cache directory with no registry entry
- a provider resource tagged with a scratch id that is gone from the registry
- a foreground registry entry whose owner process is gone

Destroy a known id instead of sweeping:

```sh
lando scratch:destroy <id>
```

## Next

- [List scratch apps](./list-and-info.mdx)
- [Start a scratch app from a recipe](./scratch-from-recipe.mdx)
