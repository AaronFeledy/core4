---
id: posix-installer
provider: test
platforms: [linux, darwin, wsl]
timeout: 60000
tags: [beta, docs, installer]
---

<Guide>
  <Scenario id="install-posix-binary" render={false} reason="The installer unit tests drive scripts/install.sh against local file:// release fixtures with fake GPG verification, so guide generation never downloads release artifacts or mutates the host">
    <Step name="detect-platform">
      <Variable name="linuxX64" value="Linux x86_64 glibc -> linux-x64" display="The POSIX installer maps a glibc linux-x64 host to the linux-x64 release artifact" />
      <Variable name="linuxArm64" value="Linux arm64 glibc -> linux-arm64" display="The installer also maps Linux arm64 to the matching release artifact" />
      <Variable name="musl" value="Unsupported Linux libc: musl" display="Unsupported libc constraints fail before any binary is downloaded or installed" />
    </Step>
    <Step name="resolve-channel">
      <Variable name="stable" value="https://update.lando.dev/v4/stable.json" display="stable is the default channel manifest" />
      <Variable name="next" value="LANDO_CHANNEL=next" display="next resolves to the next channel manifest" />
      <Variable name="dev" value="LANDO_CHANNEL=dev" display="dev resolves to the dev channel manifest" />
    </Step>
    <Step name="verify-and-install">
      <Variable name="downloads" value="binary + SHA256SUMS + SHA256SUMS.asc" display="The installer downloads the binary, checksum manifest, and GPG checksum signature for the detected platform" />
      <Variable name="verify" value="gpg --batch --verify SHA256SUMS.asc SHA256SUMS" display="Checksum manifests must verify before the binary is trusted" />
      <Variable name="installDir" value="${LANDO_INSTALL_DIR:-&lt;userDataRoot&gt;/bin}/lando" display="The verified binary installs to the chosen install dir, creating it when absent" />
    </Step>
  </Scenario>
</Guide>
