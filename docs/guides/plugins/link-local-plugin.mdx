---
id: link-local-plugin
provider: test
platforms: [darwin, linux, win32, wsl]
timeout: 60000
tags: [beta, docs, plugins, authoring]
---

# Link a local plugin

Link defaults to the current plugin authoring directory:

```sh
lando meta:plugin:link
```

An explicit path is resolved to an absolute source directory before
tracking:

```sh
lando meta:plugin:link ../lando-plugin-demo
```

