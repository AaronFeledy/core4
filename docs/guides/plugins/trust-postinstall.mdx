---
id: trust-postinstall
provider: test
platforms: [darwin, linux, win32, wsl]
timeout: 60000
tags: [beta, docs, plugins, trust]
---

# Trust plugin postinstall

Untrusted installs never run package `postinstall`. The tarball can land. The script does not.

```sh
lando plugin:trust @lando/plugin-php
lando plugin:add @lando/plugin-php --trust
```

Untrusted packages can sit on disk. Their postinstall does not get a microphone.

## Trust a name

```sh
lando plugin:trust @lando/plugin-php
lando plugin:trust list
```

Names persist in `<userConfRoot>/plugin-trust.yml`. Org globs are not up.

`--trust` and `-y` on `plugin:add` also persist that name. Non-interactive add without `--trust` fails with InteractionRequiredError when the package has no `scripts.postinstall` (the same prompt path as explicit `--trust`). If the package has `scripts.postinstall` and you omit `--trust`, the tarball still lands and the script is skipped (`trustSource: untrusted`).

## Trust a checkout

Absolute path only.

```sh
lando meta:plugin:trust-authoring-root /absolute/path/to/plugin
```

## When postinstall runs

`plugin:add` unpacks without running scripts. A trusted name with a non-empty `scripts.postinstall` then runs install in that package directory.

A name that is not trusted skips that step.

## Verify

```sh
lando plugin:trust list
lando plugin:trust @lando/plugin-php
```

Then add with `--trust` only for packages you want to run as host code.

## Next

- [List trusted plugins](./trust-management.mdx)
