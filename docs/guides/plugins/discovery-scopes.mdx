---
id: discovery-scopes
provider: test
platforms: [darwin, linux, win32, wsl]
timeout: 60000
tags: [beta, docs, plugins, discovery]
---

<Guide>
  <Scenario id="plugin-discovery-order" render={false} reason="Core registry tests create isolated system, user, and app plugin registries without requiring live plugin packages">
    <Step name="sources">
      <Variable name="system" value="bundled plugins" display="System plugins are the compiled-in bundled plugin set shipped with Lando" />
      <Variable name="user" value="&lt;userDataRoot&gt;/plugins/registry.json" display="User plugins are discovered from the managed per-user plugin registry written by lando plugin:add" />
      <Variable name="app" value="&lt;appRoot&gt;/.lando/plugins/registry.json" display="App plugins are discovered from the current app root's local plugin registry" />
    </Step>
    <Step name="precedence">
      <Variable name="order" value="app &gt; user &gt; system" display="When the same plugin id appears in more than one source, the highest-precedence source wins" />
      <Variable name="warning" value="conflict warning" display="A conflict emits a warning naming the overriding source and the replaced lower-precedence source" />
    </Step>
  </Scenario>
</Guide>
