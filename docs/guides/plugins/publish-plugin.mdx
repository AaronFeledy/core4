---
id: publish-plugin
provider: test
platforms: [darwin, linux, win32, wsl]
timeout: 60000
tags: [beta, docs, plugins, authoring]
---

# Publish a plugin

Dry-run from the plugin root. That rebuilds a stale `dist/`, runs tests, lists the artifact, and does not talk to a registry.

```sh
lando meta:plugin:publish --dry-run
```

Dress rehearsal first. A live registry publish is not a supported how-to in this Beta.

## Flags that work

```sh
lando meta:plugin:publish --dry-run --tag latest
lando meta:plugin:publish --dry-run --no-test
```

`--tag` defaults to `latest`. Empty or whitespace tags fail.

`--no-test` skips the retest. A stale `dist/package.json` still triggers `meta:plugin:build`.

## Verify

```sh
lando meta:plugin:publish --dry-run
```

The command should exit 0.

## Next

- [Declare SDK compatibility](./sdk-compatibility.mdx)
