---
id: composer-php
provider: test
timeout: 60000
tags: [beta, docs, tooling, php, composer]
---

<Guide>
  <Scenario id="tooling-bootstrap" render={false} reason="Documents cache-backed tooling routing without running provider-backed commands">
    <Step name="scaffold">
      <UseFixture name="composer-php-demo" />
      <Variable name="serviceType" value="php:8.3" display="Use a PHP service as the command target" />
      <Variable name="toolingTask" value="composer" display="Declare a tooling task named composer" />
      <Variable name="bootstrap" value="tooling" display="Command discovery reads the lightweight tooling runtime" />
    </Step>
    <Step name="route-task">
      <Variable name="bareCommand" value="lando composer --version" display="Run the task by its bare command from the app root" />
      <Variable name="canonicalCommand" value="lando app:composer --version" display="Run the same task by its canonical app command id" />
      <Variable name="hotPath" value="The router reads only a fresh app command cache before provider-backed execution starts" display="Both command forms use the same cache-only route" />
      <Variable name="staleCache" value="lando app cache refresh, lando start, or lando rebuild" display="A stale or missing command cache fails with commands that refresh it" />
      <Variable name="execution" value="runTooling" display="After routing, the normal tooling path still enforces version, provider, environment, and runtime policies" />
    </Step>
  </Scenario>

  <Scenario id="no-provider-discovery" render={false} reason="Documents which services are available before provider-backed execution starts">
    <Step name="contract">
      <Variable name="providedServices" value="LandofileService, CommandRegistry" display="Services available at the tooling bootstrap level" />
      <Variable name="omittedServices" value="RuntimeProvider, RuntimeProviderRegistry, AppPlanner, ToolingEngine" display="Services intentionally unavailable until a later app/provider path needs them" />
    </Step>
  </Scenario>
</Guide>
