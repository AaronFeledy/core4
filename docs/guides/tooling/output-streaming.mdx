---
id: output-streaming
provider: test
platforms: [linux, darwin, win32, wsl]
timeout: 60000
tags: [beta, docs, tooling, renderer, task-tree]
---

# Stream tooling output

Run a tooling command from the app root:

```sh
lando composer install
```

That publishes a task tree: a parent task for the command, one child per target service, streamed stdout/stderr lines on each child, then a complete or fail close with the exit code.

A non-zero exit follows the same shape. Stderr still streams. The failing child closes with the exit code, and the parent records the failure in its counts.

## Pick a renderer mode

| Mode | Behavior |
| --- | --- |
| `lando` (default), `verbose` | Concurrent task tree. A parent line plus an indented child per service, each with a spinner and the most recent output lines. |
| `plain` | Flat stream. One `[stepId]`-prefixed line per output event, no tree, no spinners. |
| `json` | Structured `task.*` events on stderr. |

<Guide>
  <Scenario id="output-streaming-config-check" render={false} reason="The task-tree event contract and renderer modes above are covered by focused renderer and tooling event tests without running provider-backed tooling in guide tests. This scenario keeps one real CLI check in the guide harness.">
    <Step name="config-check">
      <Run command="lando config --format=json" />
      <Verify command="lando config --format=json" expect={{ stdout: { regex: "\"config\":\\s*\\{" } }} />
    </Step>
  </Scenario>
</Guide>
