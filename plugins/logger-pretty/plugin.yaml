# @lando/logger-pretty — pretty-printed Logger plugin.
#
# Bundled as a no-op for MVP because Effect's default Logger.pretty is built in.

name: "@lando/logger-pretty"
version: "0.0.0"
api: 4
description: "Pretty-printed Logger plugin."
enabled: true
updateable: true
entry: ./src/index.ts

contributes:
  loggers:
    - pretty
