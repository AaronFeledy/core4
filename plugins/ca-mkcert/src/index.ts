/**
 * `@lando/ca-mkcert` — mkcert-backed CertificateAuthority.
 *
 * Pulls the `mkcert` binary on first use (not bundled into core).
 *
 * Status: stub.
 */
export const PLUGIN_NAME = "@lando/ca-mkcert" as const;
