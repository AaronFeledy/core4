# Generated by scripts/codegen.ts — do not hand-edit. Edit scripts/build-rails-journey-workflow.ts instead.
name: rails-journey

on:
  pull_request:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

defaults:
  run:
    shell: bash

permissions:
  contents: read

jobs:
  rails-journey-linux-x64:
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5

      - name: Isolate Lando roots
        run: |
          echo "LANDO_USER_CONF_ROOT=$RUNNER_TEMP/lando-conf" >> "$GITHUB_ENV"
          echo "LANDO_USER_DATA_ROOT=$RUNNER_TEMP/lando-data" >> "$GITHUB_ENV"
          echo "LANDO_USER_CACHE_ROOT=$RUNNER_TEMP/lando-cache" >> "$GITHUB_ENV"

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: .bun-version

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build linux-x64 binary
        run: |
          mkdir -p dist
          VERSION=$(git describe --tags --always --dirty 2>/dev/null || echo "0.0.0-dev")
          bun run scripts/build-compiled-binary.ts --target bun-linux-x64 --outfile ./dist/lando --version "$VERSION" --minify --sourcemap=external

      - name: Setup Go for Linux Podman source build
        uses: actions/setup-go@d35c59abb061a4a6fb18e82ac0862c26744d6ab5 # v5.5.0
        with:
          go-version: 1.25.6

      - name: Setup Rust for Linux helper source builds
        uses: dtolnay/rust-toolchain@4e529fb27e59237866a6523e61ab248308c068b4 # 1.88.0

      - name: Install Linux Podman source-build prerequisites
        run: |
          set -euo pipefail
          UBUNTU_SNAPSHOT=20260902T000000Z
          printf 'APT::Snapshot "%s";\n' "$UBUNTU_SNAPSHOT" | sudo tee /etc/apt/apt.conf.d/50lando-runtime-bundle-snapshot
          sudo apt-get update
          PACKAGES=(build-essential libbtrfs-dev libcap-dev libdevmapper-dev libglib2.0-dev libseccomp-dev libsqlite3-dev libsystemd-dev patch pkg-config protobuf-compiler uidmap)
          sudo apt-get install -y --no-install-recommends --allow-downgrades --reinstall "${PACKAGES[@]}"
          for PACKAGE in "${PACKAGES[@]}"; do
            INSTALLED_VERSION="$(dpkg-query -W -f='${Version}' "$PACKAGE")"
            POLICY="$(apt-cache policy "$PACKAGE")"
            CANDIDATE_VERSION="$(awk '/Candidate:/ { print $2 }' <<< "$POLICY")"
            test "$INSTALLED_VERSION" = "$CANDIDATE_VERSION"
            if ! awk -v candidate="$CANDIDATE_VERSION" -v snapshot="https://snapshot.ubuntu.com/ubuntu/$UBUNTU_SNAPSHOT" '
              $1 == "***" && $2 == candidate { candidate_block = 1; next }
              candidate_block && $1 ~ /^[0-9]+$/ { if ($2 == snapshot) found = 1; next }
              candidate_block { candidate_block = 0 }
              END { exit found ? 0 : 1 }
            ' <<< "$POLICY"; then
              echo "::error title=runtime-bundle-apt-snapshot::$PACKAGE candidate $CANDIDATE_VERSION did not resolve from Ubuntu snapshot $UBUNTU_SNAPSHOT"
              exit 1
            fi
          done

      - name: Assemble current-commit linux-x64 runtime bundle
        run: bun run scripts/assemble-runtime-bundle.ts --platform linux-x64

      - name: Build local runtime bundle manifest
        run: |
          RUNTIME_VERSION="$(bun -e 'import { readRuntimeBundleSources } from "./scripts/runtime-bundle-sources.ts"; process.stdout.write((await readRuntimeBundleSources()).runtimeVersion)')"
          MANIFEST="$(bun run scripts/build-runtime-bundle.ts --local --platform linux-x64 --runtime-version "$RUNTIME_VERSION")"
          echo "LANDO_RUNTIME_BUNDLE_MANIFEST=$MANIFEST" >> "$GITHUB_ENV"

      - name: Provision rootless runtime prerequisites
        run: |
          sudo apt-get update
          sudo apt-get install -y uidmap fuse-overlayfs
          if ! grep -q "^$(id -un):" /etc/subuid; then
            echo "$(id -un):100000:65536" | sudo tee -a /etc/subuid
          fi
          if ! grep -q "^$(id -un):" /etc/subgid; then
            echo "$(id -un):100000:65536" | sudo tee -a /etc/subgid
          fi
          sudo sysctl net.ipv4.ip_unprivileged_port_start=0
          if test -e /proc/sys/kernel/apparmor_restrict_unprivileged_userns; then
            sudo sysctl kernel.apparmor_restrict_unprivileged_userns=0
          fi
          if ! grep -q "systemd.unified_cgroup_hierarchy" /proc/cmdline; then
            echo "::notice title=ci-runtime::cgroups v2 unified hierarchy assumed via Ubuntu LTS default (systemd.unified_cgroup_hierarchy)"
          fi
          sudo mkdir -p /sys/fs/cgroup/user.slice/user-$(id -u).slice
          sudo chown -R "$(id -un)" "/sys/fs/cgroup/user.slice/user-$(id -u).slice" || true

      - name: Configure rootless overlay storage
        run: |
          cat > "${{ runner.temp }}/lando-storage.conf" <<EOF
          [storage]
          driver = "overlay"

          [storage.options.overlay]
          mount_program = "${{ runner.temp }}/lando-data/runtime/bin/fuse-overlayfs"
          EOF
          echo "CONTAINERS_STORAGE_CONF=${{ runner.temp }}/lando-storage.conf" >> "$GITHUB_ENV"

      - name: Setup provider
        run: |
          export XDG_RUNTIME_DIR="${XDG_RUNTIME_DIR:-/run/user/$(id -u)}"
          mkdir -p "$XDG_RUNTIME_DIR"
          dist/lando setup --yes --provider=lando --skip-install-ca --skip-shell-integration --skip-file-sync

      - name: Run Rails journey
        run: bun run scripts/rails-journey.ts --binary dist/lando --app-dir ${{ runner.temp }}/rails-app --report ${{ runner.temp }}/rails-journey.json

      - name: Teardown
        if: always()
        run: |
          BINARY="$GITHUB_WORKSPACE/dist/lando"
          (cd "${{ runner.temp }}/rails-app/rails-journey" && "$BINARY" destroy -y) || true
          "$BINARY" poweroff || true

      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: rails-journey-linux-x64
          path: ${{ runner.temp }}/rails-journey.json
          if-no-files-found: ignore
          retention-days: 14

  rails-journey-linux-arm64:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5

      - name: Isolate Lando roots
        run: |
          echo "LANDO_USER_CONF_ROOT=$RUNNER_TEMP/lando-conf" >> "$GITHUB_ENV"
          echo "LANDO_USER_DATA_ROOT=$RUNNER_TEMP/lando-data" >> "$GITHUB_ENV"
          echo "LANDO_USER_CACHE_ROOT=$RUNNER_TEMP/lando-cache" >> "$GITHUB_ENV"

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: .bun-version

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build linux-arm64 binary
        run: |
          mkdir -p dist
          VERSION=$(git describe --tags --always --dirty 2>/dev/null || echo "0.0.0-dev")
          bun run scripts/build-compiled-binary.ts --target bun-linux-arm64 --outfile ./dist/lando --version "$VERSION" --minify --sourcemap=external

      - name: Setup Go for Linux Podman source build
        uses: actions/setup-go@d35c59abb061a4a6fb18e82ac0862c26744d6ab5 # v5.5.0
        with:
          go-version: 1.25.6

      - name: Setup Rust for Linux helper source builds
        uses: dtolnay/rust-toolchain@4e529fb27e59237866a6523e61ab248308c068b4 # 1.88.0

      - name: Install Linux Podman source-build prerequisites
        run: |
          set -euo pipefail
          UBUNTU_SNAPSHOT=20260902T000000Z
          printf 'APT::Snapshot "%s";\n' "$UBUNTU_SNAPSHOT" | sudo tee /etc/apt/apt.conf.d/50lando-runtime-bundle-snapshot
          sudo apt-get update
          PACKAGES=(build-essential libbtrfs-dev libcap-dev libdevmapper-dev libglib2.0-dev libseccomp-dev libsqlite3-dev libsystemd-dev patch pkg-config protobuf-compiler uidmap)
          sudo apt-get install -y --no-install-recommends --allow-downgrades --reinstall "${PACKAGES[@]}"
          for PACKAGE in "${PACKAGES[@]}"; do
            INSTALLED_VERSION="$(dpkg-query -W -f='${Version}' "$PACKAGE")"
            POLICY="$(apt-cache policy "$PACKAGE")"
            CANDIDATE_VERSION="$(awk '/Candidate:/ { print $2 }' <<< "$POLICY")"
            test "$INSTALLED_VERSION" = "$CANDIDATE_VERSION"
            if ! awk -v candidate="$CANDIDATE_VERSION" -v snapshot="https://snapshot.ubuntu.com/ubuntu/$UBUNTU_SNAPSHOT" '
              $1 == "***" && $2 == candidate { candidate_block = 1; next }
              candidate_block && $1 ~ /^[0-9]+$/ { if ($2 == snapshot) found = 1; next }
              candidate_block { candidate_block = 0 }
              END { exit found ? 0 : 1 }
            ' <<< "$POLICY"; then
              echo "::error title=runtime-bundle-apt-snapshot::$PACKAGE candidate $CANDIDATE_VERSION did not resolve from Ubuntu snapshot $UBUNTU_SNAPSHOT"
              exit 1
            fi
          done

      - name: Assemble current-commit linux-arm64 runtime bundle
        run: bun run scripts/assemble-runtime-bundle.ts --platform linux-arm64

      - name: Build local runtime bundle manifest
        run: |
          RUNTIME_VERSION="$(bun -e 'import { readRuntimeBundleSources } from "./scripts/runtime-bundle-sources.ts"; process.stdout.write((await readRuntimeBundleSources()).runtimeVersion)')"
          MANIFEST="$(bun run scripts/build-runtime-bundle.ts --local --platform linux-arm64 --runtime-version "$RUNTIME_VERSION")"
          echo "LANDO_RUNTIME_BUNDLE_MANIFEST=$MANIFEST" >> "$GITHUB_ENV"

      - name: Provision rootless runtime prerequisites
        run: |
          sudo apt-get update
          sudo apt-get install -y uidmap fuse-overlayfs
          if ! grep -q "^$(id -un):" /etc/subuid; then
            echo "$(id -un):100000:65536" | sudo tee -a /etc/subuid
          fi
          if ! grep -q "^$(id -un):" /etc/subgid; then
            echo "$(id -un):100000:65536" | sudo tee -a /etc/subgid
          fi
          sudo sysctl net.ipv4.ip_unprivileged_port_start=0
          if test -e /proc/sys/kernel/apparmor_restrict_unprivileged_userns; then
            sudo sysctl kernel.apparmor_restrict_unprivileged_userns=0
          fi
          if ! grep -q "systemd.unified_cgroup_hierarchy" /proc/cmdline; then
            echo "::notice title=ci-runtime::cgroups v2 unified hierarchy assumed via Ubuntu LTS default (systemd.unified_cgroup_hierarchy)"
          fi
          sudo mkdir -p /sys/fs/cgroup/user.slice/user-$(id -u).slice
          sudo chown -R "$(id -un)" "/sys/fs/cgroup/user.slice/user-$(id -u).slice" || true

      - name: Configure rootless overlay storage
        run: |
          cat > "${{ runner.temp }}/lando-storage.conf" <<EOF
          [storage]
          driver = "overlay"

          [storage.options.overlay]
          mount_program = "${{ runner.temp }}/lando-data/runtime/bin/fuse-overlayfs"
          EOF
          echo "CONTAINERS_STORAGE_CONF=${{ runner.temp }}/lando-storage.conf" >> "$GITHUB_ENV"

      - name: Setup provider
        run: |
          export XDG_RUNTIME_DIR="${XDG_RUNTIME_DIR:-/run/user/$(id -u)}"
          mkdir -p "$XDG_RUNTIME_DIR"
          dist/lando setup --yes --provider=lando --skip-install-ca --skip-shell-integration --skip-file-sync

      - name: Run Rails journey
        run: bun run scripts/rails-journey.ts --binary dist/lando --app-dir ${{ runner.temp }}/rails-app --report ${{ runner.temp }}/rails-journey.json

      - name: Teardown
        if: always()
        run: |
          BINARY="$GITHUB_WORKSPACE/dist/lando"
          (cd "${{ runner.temp }}/rails-app/rails-journey" && "$BINARY" destroy -y) || true
          "$BINARY" poweroff || true

      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: rails-journey-linux-arm64
          path: ${{ runner.temp }}/rails-journey.json
          if-no-files-found: ignore
          retention-days: 14

  rails-journey-darwin-arm64:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: [self-hosted, lando-virt, macOS, ARM64]
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5

      - name: Isolate Lando roots
        run: |
          echo "LANDO_USER_CONF_ROOT=$RUNNER_TEMP/lando-conf" >> "$GITHUB_ENV"
          echo "LANDO_USER_DATA_ROOT=$RUNNER_TEMP/lando-data" >> "$GITHUB_ENV"
          echo "LANDO_USER_CACHE_ROOT=$RUNNER_TEMP/lando-cache" >> "$GITHUB_ENV"

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: .bun-version

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build darwin-arm64 binary
        run: |
          mkdir -p dist
          VERSION=$(git describe --tags --always --dirty 2>/dev/null || echo "0.0.0-dev")
          bun run scripts/build-compiled-binary.ts --target bun-darwin-arm64 --outfile ./dist/lando --version "$VERSION" --minify --sourcemap=external

      - name: Setup provider
        run: dist/lando setup --yes --provider=lando --skip-install-ca --skip-shell-integration --skip-file-sync

      - name: Run Rails journey
        run: bun run scripts/rails-journey.ts --binary dist/lando --app-dir ${{ runner.temp }}/rails-app --report ${{ runner.temp }}/rails-journey.json

      - name: Teardown
        if: always()
        run: |
          BINARY="$GITHUB_WORKSPACE/dist/lando"
          (cd "${{ runner.temp }}/rails-app/rails-journey" && "$BINARY" destroy -y) || true
          "$BINARY" poweroff || true

      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: rails-journey-darwin-arm64
          path: ${{ runner.temp }}/rails-journey.json
          if-no-files-found: ignore
          retention-days: 14

  rails-journey-darwin-x64:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: [self-hosted, lando-virt, macOS, X64]
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5

      - name: Isolate Lando roots
        run: |
          echo "LANDO_USER_CONF_ROOT=$RUNNER_TEMP/lando-conf" >> "$GITHUB_ENV"
          echo "LANDO_USER_DATA_ROOT=$RUNNER_TEMP/lando-data" >> "$GITHUB_ENV"
          echo "LANDO_USER_CACHE_ROOT=$RUNNER_TEMP/lando-cache" >> "$GITHUB_ENV"

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: .bun-version

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build darwin-x64 binary
        run: |
          mkdir -p dist
          VERSION=$(git describe --tags --always --dirty 2>/dev/null || echo "0.0.0-dev")
          bun run scripts/build-compiled-binary.ts --target bun-darwin-x64 --outfile ./dist/lando --version "$VERSION" --minify --sourcemap=external

      - name: Setup provider
        run: dist/lando setup --yes --provider=docker --skip-install-ca --skip-shell-integration --skip-file-sync

      - name: Run Rails journey
        run: bun run scripts/rails-journey.ts --binary dist/lando --app-dir ${{ runner.temp }}/rails-app --report ${{ runner.temp }}/rails-journey.json

      - name: Teardown
        if: always()
        run: |
          BINARY="$GITHUB_WORKSPACE/dist/lando"
          (cd "${{ runner.temp }}/rails-app/rails-journey" && "$BINARY" destroy -y) || true
          "$BINARY" poweroff || true

      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: rails-journey-darwin-x64
          path: ${{ runner.temp }}/rails-journey.json
          if-no-files-found: ignore
          retention-days: 14

  rails-journey-windows-x64:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: [self-hosted, lando-virt, Windows, X64]
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5

      - name: Isolate Lando roots
        run: |
          echo "LANDO_USER_CONF_ROOT=$RUNNER_TEMP/lando-conf" >> "$GITHUB_ENV"
          echo "LANDO_USER_DATA_ROOT=$RUNNER_TEMP/lando-data" >> "$GITHUB_ENV"
          echo "LANDO_USER_CACHE_ROOT=$RUNNER_TEMP/lando-cache" >> "$GITHUB_ENV"

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: .bun-version

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build windows-x64 binary
        run: |
          mkdir -p dist
          VERSION=$(git describe --tags --always --dirty 2>/dev/null || echo "0.0.0-dev")
          bun run scripts/build-compiled-binary.ts --target bun-windows-x64 --outfile ./dist/lando-windows-x64.exe --version "$VERSION" --minify --sourcemap=external

      - name: Assemble current-commit win32-x64 runtime bundle
        run: bun run scripts/assemble-runtime-bundle.ts --platform win32-x64

      - name: Build local runtime bundle manifest
        run: |
          RUNTIME_VERSION="$(bun -e 'import { readRuntimeBundleSources } from "./scripts/runtime-bundle-sources.ts"; process.stdout.write((await readRuntimeBundleSources()).runtimeVersion)')"
          MANIFEST="$(bun run scripts/build-runtime-bundle.ts --local --platform win32-x64 --runtime-version "$RUNTIME_VERSION")"
          echo "LANDO_RUNTIME_BUNDLE_MANIFEST=$MANIFEST" >> "$GITHUB_ENV"

      - name: Setup provider
        run: dist/lando-windows-x64.exe setup --yes --provider=lando --skip-install-ca --skip-shell-integration --skip-file-sync --no-interactive

      - name: Run Rails journey
        run: bun run scripts/rails-journey.ts --binary dist/lando-windows-x64.exe --app-dir ${{ runner.temp }}/rails-app --report ${{ runner.temp }}/rails-journey.json

      - name: Teardown
        if: always()
        run: |
          BINARY="$GITHUB_WORKSPACE/dist/lando-windows-x64.exe"
          (cd "${{ runner.temp }}/rails-app/rails-journey" && "$BINARY" destroy -y) || true
          "$BINARY" poweroff || true

      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: rails-journey-windows-x64
          path: ${{ runner.temp }}/rails-journey.json
          if-no-files-found: ignore
          retention-days: 14

  rails-journey-windows-arm64:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: [self-hosted, lando-virt, Windows, ARM64]
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v5

      - name: Isolate Lando roots
        run: |
          echo "LANDO_USER_CONF_ROOT=$RUNNER_TEMP/lando-conf" >> "$GITHUB_ENV"
          echo "LANDO_USER_DATA_ROOT=$RUNNER_TEMP/lando-data" >> "$GITHUB_ENV"
          echo "LANDO_USER_CACHE_ROOT=$RUNNER_TEMP/lando-cache" >> "$GITHUB_ENV"

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version-file: .bun-version

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build windows-arm64 binary
        run: |
          mkdir -p dist
          VERSION=$(git describe --tags --always --dirty 2>/dev/null || echo "0.0.0-dev")
          bun run scripts/build-compiled-binary.ts --target bun-windows-arm64 --outfile ./dist/lando-windows-arm64.exe --version "$VERSION" --minify --sourcemap=external

      - name: Assemble current-commit win32-arm64 runtime bundle
        run: bun run scripts/assemble-runtime-bundle.ts --platform win32-arm64

      - name: Build local runtime bundle manifest
        run: |
          RUNTIME_VERSION="$(bun -e 'import { readRuntimeBundleSources } from "./scripts/runtime-bundle-sources.ts"; process.stdout.write((await readRuntimeBundleSources()).runtimeVersion)')"
          MANIFEST="$(bun run scripts/build-runtime-bundle.ts --local --platform win32-arm64 --runtime-version "$RUNTIME_VERSION")"
          echo "LANDO_RUNTIME_BUNDLE_MANIFEST=$MANIFEST" >> "$GITHUB_ENV"

      - name: Setup provider
        run: dist/lando-windows-arm64.exe setup --yes --provider=lando --skip-install-ca --skip-shell-integration --skip-file-sync --no-interactive

      - name: Run Rails journey
        run: bun run scripts/rails-journey.ts --binary dist/lando-windows-arm64.exe --app-dir ${{ runner.temp }}/rails-app --report ${{ runner.temp }}/rails-journey.json

      - name: Teardown
        if: always()
        run: |
          BINARY="$GITHUB_WORKSPACE/dist/lando-windows-arm64.exe"
          (cd "${{ runner.temp }}/rails-app/rails-journey" && "$BINARY" destroy -y) || true
          "$BINARY" poweroff || true

      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: rails-journey-windows-arm64
          path: ${{ runner.temp }}/rails-journey.json
          if-no-files-found: ignore
          retention-days: 14

