---
id: lamp
provider: test
timeout: 60000
tags: [recipe, lamp, php]
---

# LAMP

`lando init --recipe lamp` scaffolds Apache, PHP, and MariaDB.

```sh
lando init --recipe lamp --name=my-lamp-app --yes
lando start
lando info
```

`lando start` prints the app URL. `lando info` repeats it. TODO: capture

`lando destroy -y` removes the app containers and volumes.

<Guide>
  <Scenario id="reader-path" render>
    <Step name="scaffold">
      <Variable name="appName" value="lamp" display="my-lamp-app" />
      <Run command="lando init --recipe lamp --name={{appName}} --yes" />
    </Step>
    <Step name="start">
      <Run command="lando start" />
      <Verify event="post-start" />
    </Step>
    <Step name="cleanup">
      <Cleanup />
      <Run command="lando destroy -y" />
    </Step>
  </Scenario>
</Guide>
