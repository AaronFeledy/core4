---
id: lamp
provider: test
timeout: 60000
tags: [recipe, lamp, php]
---

# LAMP

`lando init --recipe lamp` scaffolds Apache, PHP, and MariaDB or MySQL.

```sh
lando init --recipe lamp --name=my-lamp-app --yes
lando start
lando info
```

`--yes` uses PHP 8.3, MariaDB 11.4, Composer 2, and webroot `/app`. Pass `--answer` to change those.

```sh
lando init --recipe lamp --name=my-lamp-app --yes \
  --answer=php=8.1 \
  --answer=database=mysql:8.0 \
  --answer=composer=2.7.7 \
  --answer=webroot=/app/public
```

`lando start` prints the app URL. `lando info` repeats it.

`lando destroy -y` removes the app containers and networks. Volumes stay unless you pass `--volumes` or `--purge`.

<Guide>
  <Scenario id="reader-path" render>
    <Step name="scaffold">
      <Variable name="appName" value="lamp" display="my-lamp-app" />
      <Run command="lando init --recipe lamp --name={{appName}} --yes" />
    </Step>
    <Step name="start">
      <Run command="lando start" />
      <Verify event="post-start" />
    </Step>
    <Step name="info">
      <Run command="lando info" />
    </Step>
    <Step name="cleanup">
      <Cleanup />
      <Run command="lando destroy -y" />
    </Step>
  </Scenario>

  <Scenario id="non-default-options" render>
    <Step name="init">
      <Variable name="appName" value="lamp-mysql" display="my-lamp-mysql" />
      <Run command="lando init --recipe lamp --name={{appName}} --yes --answer=php=8.1 --answer=database=mysql:8.0 --answer=composer=2.7.7 --answer=webroot=/app/public" />
    </Step>
    <Step name="inspect">
      <Run command="lando app:config --format=json" />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "\"type\":\\s*\"php:8\\.1\"" } }} />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "\"type\":\\s*\"mysql:8\\.0\"" } }} />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "\"composer\":\\s*\"2\\.7\\.7\"" } }} />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "\"webroot\":\\s*\"/app/public\"" } }} />
    </Step>
  </Scenario>
</Guide>
