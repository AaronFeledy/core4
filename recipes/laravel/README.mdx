---
id: laravel
provider: test
timeout: 60000
tags: [recipe, laravel, php]
---

# Laravel

`lando init --recipe laravel` scaffolds PHP, Composer, MariaDB or PostgreSQL, Redis, Artisan, and an optional queue worker.

```sh
lando init --recipe laravel --name=my-laravel-app --yes
lando start
lando info
```

`--yes` uses PHP 8.3, MariaDB 11.4, Composer 2, webroot `/app/public`, and no worker. Pass `--answer` to change those.

```sh
lando init --recipe laravel --name=my-laravel-app --yes \
  --answer=php=8.1 \
  --answer=database=postgres:16 \
  --answer=composer=2.7.7 \
  --answer=webroot=/app/public \
  --answer=worker=true
```

`lando start` prints the app URL. `lando info` repeats it.

`lando destroy -y` removes the app containers and networks. Volumes stay unless you pass `--volumes` or `--purge`.

<Guide>
  <Scenario id="reader-path" render>
    <Step name="scaffold">
      <Variable name="appName" value="laravel" display="my-laravel-app" />
      <Run command="lando init --recipe laravel --name={{appName}} --yes" />
    </Step>
    <Step name="start">
      <Run command="lando start" />
      <Verify event="post-start" />
    </Step>
    <Step name="info">
      <Run command="lando info" />
    </Step>
    <Step name="cleanup">
      <Cleanup />
      <Run command="lando destroy -y" />
    </Step>
  </Scenario>

  <Scenario id="non-default-options" render>
    <Step name="init">
      <Variable name="appName" value="laravel-worker" display="my-laravel-worker" />
      <Run command="lando init --recipe laravel --name={{appName}} --yes --answer=php=8.1 --answer=database=postgres:16 --answer=composer=2.7.7 --answer=webroot=/app/public --answer=worker=true" />
    </Step>
    <Step name="inspect">
      <Run command="lando app:config --format=json" />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "\"type\":\\s*\"php:8\\.1\"" } }} />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "\"type\":\\s*\"postgres:16\"" } }} />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "\"composer\":\\s*\"2\\.7\\.7\"" } }} />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "\"webroot\":\\s*\"/app/public\"" } }} />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "\"via\":\\s*\"cli\"" } }} />
    </Step>
  </Scenario>
</Guide>
