---
id: wordpress
provider: test
timeout: 60000
tags: [recipe, wordpress, php]
---

# WordPress

`lando init --recipe wordpress` scaffolds PHP, MariaDB, WP-CLI, and Composer.

```sh
lando init --recipe wordpress --name=my-wordpress-app --yes
lando start
lando info
```

`lando start` prints the app URL. `lando info` repeats it. TODO: capture

`lando destroy -y` removes the app containers and volumes.

<Guide>
  <Scenario id="reader-path" render>
    <Step name="scaffold">
      <Variable name="appName" value="wordpress" display="my-wordpress-app" />
      <Run command="lando init --recipe wordpress --name={{appName}} --yes" />
    </Step>
    <Step name="start">
      <Run command="lando start" />
      <Verify event="post-start" />
    </Step>
    <Step name="cleanup">
      <Cleanup />
      <Run command="lando destroy -y" />
    </Step>
  </Scenario>
</Guide>
