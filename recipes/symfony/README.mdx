---
id: symfony
provider: test
timeout: 60000
tags: [recipe, symfony, php]
---

# Symfony

`lando init --recipe symfony` scaffolds PHP, Composer, PostgreSQL or MariaDB, Redis, and the Symfony console.

```sh
lando init --recipe symfony --name=my-symfony-app --yes
lando start
lando info
```

`--yes` uses PHP 8.3, PostgreSQL 16, Composer 2, and webroot `/app/public`. Pass `--answer` to change those.

```sh
lando init --recipe symfony --name=my-symfony-app --yes \
  --answer=php=8.5 \
  --answer=database=mariadb:11.4 \
  --answer=composer=2 \
  --answer=webroot=/app/public
```

`lando start` prints the app URL. `lando info` repeats it.

`lando destroy -y` removes the app containers and networks. Volumes stay unless you pass `--volumes` or `--purge`.

<Guide>
  <Scenario id="reader-path" render>
    <Step name="scaffold">
      <Variable name="appName" value="symfony" display="my-symfony-app" />
      <Run command="lando init --recipe symfony --name={{appName}} --yes" />
    </Step>
    <Step name="start">
      <Run command="lando start" />
      <Verify event="post-start" />
    </Step>
    <Step name="info">
      <Run command="lando info" />
    </Step>
    <Step name="cleanup">
      <Cleanup />
      <Run command="lando destroy -y" />
    </Step>
  </Scenario>

  <Scenario id="non-default-options" render>
    <Step name="init">
      <Variable name="appName" value="symfony-mariadb" display="my-symfony-mariadb" />
      <Run command="lando init --recipe symfony --name={{appName}} --yes --answer=php=8.5 --answer=database=mariadb:11.4 --answer=composer=2 --answer=webroot=/app/public" />
    </Step>
    <Step name="inspect">
      <Run command="lando app:config --format=json" />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "\"type\":\\s*\"php:8\\.5\"" } }} />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "\"type\":\\s*\"mariadb:11\\.4\"" } }} />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "\"composer\":\\s*\"2\"" } }} />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "\"webroot\":\\s*\"/app/public\"" } }} />
    </Step>
  </Scenario>
</Guide>
