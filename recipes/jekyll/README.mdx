---
id: jekyll
provider: test
timeout: 60000
tags: [recipe, jekyll, ruby, static]
---

# Jekyll

`lando init --recipe jekyll` scaffolds a Jekyll site with a Ruby builder and an nginx sidecar.

```sh
lando init --recipe jekyll --name=my-jekyll --yes
cd my-jekyll
```

Jekyll declares no recipe options, so the only prompt is the app name and `--yes` answers it. Pass an undeclared `--answer` and init fails instead of quietly ignoring it.

The scaffold writes only `.lando.yml`. Park `builder` with compile tooling before the first start, install gems and scaffold the site, then restore the serve command. Open the **builder** URL from `lando info` after that restore.

For park, install, scaffold, build, browse, and cleanup steps, see [Run the Jekyll recipe](/guides/recipes/jekyll-workflow/).

When you are finished:

```sh
lando destroy -y
```

<Guide>
  <Scenario id="recipe-defaults" render={false} reason="Test provider validates recipe defaults without claiming live Jekyll output">
    <Step name="scaffold">
      <Variable name="appName" value="jekyll" display="my-jekyll" />
      <Run command="lando init --recipe jekyll --name={{appName}} --yes" />
    </Step>
    <Step name="confirm-defaults">
      <Run command="lando app:config --format=json" />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "\"builder\":\\s*\\{[^}]*\"type\":\\s*\"ruby:3\\.3\"" } }} />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "bundle exec jekyll serve --host 0.0.0.0 --port 4000" } }} />
      <Verify command="lando app:config --format=json" expect={{ stdout: { regex: "\"web\":\\s*\\{[^}]*\"type\":\\s*\"static:nginx\"" } }} />
    </Step>
    <Step name="cleanup">
      <Cleanup />
      <Run command="lando destroy -y" />
    </Step>
  </Scenario>
</Guide>
