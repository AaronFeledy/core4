services:
  api:
    image: example/api:1.0
    depends_on:
      database:
        condition: service_healthy
        restart: true
        required: true
  database:
    image: postgres:17
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 30s
      retries: 3
