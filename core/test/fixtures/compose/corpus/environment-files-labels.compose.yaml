services:
  worker:
    image: example/worker:1.0
    environment:
      - APP_ENV=production
      - QUEUE=critical
    env_file:
      - .env
      - worker.env
    labels:
      - com.example.role=worker
      - com.example.tier=background
    working_dir: /workspace
