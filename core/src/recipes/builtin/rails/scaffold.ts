/**
 * Auxiliary asset sources the rails recipe ships beside its Landofile.
 *
 * The bytes live here so the still-bound renderer and the published snapshot
 * inventory hash and write one source of truth.
 */
export const RAILS_GEMFILE = 'source "https://rubygems.org"\n';
