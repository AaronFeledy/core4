/**
 * `RuntimeProviderRegistry` Live Layer.
 *
 * Selection: explicit per-context choice → Landofile global →
 * global config `defaultProvider` → manifest `defaultFor:` → sole installed
 * → error.
 *
 * Status: stub.
 */
export { RuntimeProviderRegistry } from "@lando/sdk/services";
