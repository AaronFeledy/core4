Branch: docs/scanner-retirement-prd
Source PRDs: spec/scanner-retirement/prd-scanner-retirement-*.md
Spec source of truth: spec/02,03,13 (§2.7, §3.3–§3.4, §13.4, §13.8) + ROADMAP Phase 5 concurrent-meta supersession
Execution checklist: .omo/plans/finish-scanner-retirement.md (dual high-accuracy review approved, digest 8386f05f4c1b3fd3532393f01c4774c80ca40ac3585f6a141e08d91c1ac35abf)
Origin: architecture review (2026-08-09/10) — architecture-by-scanner after core-seams; engine still hosts managed-file + planner/update god-modules; codegen:check still conflates drift with typecheck/deprecations; ~14 boundary package.json aliases

## Conventions

- Story IDs US-551..US-560; priority order is execution order.
- Internal/infra only — Guide Coverage None on every PRD.
- Behavior-preserving moves only; no ManagedFile/update/planner semantics changes; no SDK churn.
- Full-suite lock is always `bun test` (or explicit shard --run commands). Never `bun run test` alone.
- US-559 alias collapse + CI regen + PR-tier test rewrites = one commit.
- Engine MUST NOT depend on @lando/managed-file (SDK tag only); core is the Live consumer.
- Keep per-rule scripts/check-*.ts files; collapse package.json aliases only.
- Do not add typecheck to portable static-checks-platform.
- Preserve runLaunchProbe as single ProcessRunner.run (no runProbe migration in US-556).
- Do not touch unrelated dirty working-tree files from other waves.

## Codebase Patterns (carry forward)

- Package-local tests need scripts/test-shards.ts INCLUDE_GLOBS entry.
- New release packages need prepare-npm-dev-packages.ts before engine.
- Owner-excluding rules use scope.roots filter (redaction/network), not carveOuts.prefixes.
- OWNER_EXCLUDING_RULES / CORE_AND_PLUGIN_RULE_IDS in workspace-roots.test.ts must move with the rule.
- NIGHTLY_TIER_TESTS excludes some generator suites from PR shards; ci-workflow.test.ts is PR-tier and must be updated when CI steps change.
- Mid-wave alias deletion without CI regen breaks main.
- Run `codegen:check` after committing intentional committed-workflow output; before the commit, pure drift correctly reports that workflow as changed.

## Status

- US-551: **passes** — PRD set authored; normative contract applied to §2.7, §3.3–§3.4, §13.4/§13.8, ROADMAP Phase 5, architecture-simplicity deferral notes, core-seams follow-on pointer.
- US-552: **passes** — `@lando/managed-file` owns the moved implementation; root-typechecked production consumers and package wiring now resolve through the private seam.
- US-553..US-559: **passes**; US-560: **pending** exact-tree full-suite evidence.

## 2026-08-10 - US-551
- Authorized `@lando/managed-file` private seam; engine `planner/` + `update/` module trees; drift-only `codegen:check`; `check:boundaries` package.json surface with stable rule IDs and retained shim scripts.
- Superseded managed-file / gate-surface deferrals from architecture-simplicity and ROADMAP Phase 5 concurrent-meta note.
- No product code changed; implementation stories remain US-552+.
---

## 2026-08-10 13:23 CDT - US-552 Adjudication
- Contradiction: SR-02 US-552 requires `engine/src/managed-file/` to be "gone" and also requires "Tests pass; typecheck passes; lint passes", while SR-02 US-553 reserves "bootstrap-layer-renderers updated" and "init, testing helper" consumer repointing. Deleting the engine source first therefore leaves root-typechecked core importers unresolved. Normative §3.4 requires the final Live implementation to come from `@lando/managed-file`, but does not require this intermediate broken ordering.
- Decision: narrowed the boundary in `prd.json` so US-552 now includes only the root-typechecked production repointing needed by the source move (generator and regenerated minimal layer, init, testing helper, and core dependency). US-553 now owns the remaining test-only consumers, focused proof, and final first-party import sweep.
- Follow-up story: US-553 — finish managed-file test-consumer migration and prove the legacy import path is clean.
---

## 2026-08-10 15:18 CDT - US-552
- Extracted the managed-file ownership implementation into the private `@lando/managed-file` package, wired it into workspace, release, TypeScript, and bootstrap composition, and kept the engine dependent only on the SDK service tag.
- Verification: typecheck and lint passed; package typecheck and generated-output boundary passed; npm-dev ordering plus engine-closure spot checks passed 14/14; root and service package imports resolved successfully.
- **Learnings for future iterations:**
  - A package source move must repoint generators, generated bootstrap imports, and every root-typechecked production consumer in the same increment; test-only import sweeps may follow only across an explicit adjudicated boundary.
---

## 2026-08-10 15:44 CDT - US-552 Review
- Fixed the generated release workflow so `@lando/managed-file` is built, dry-run published, published, and readiness-checked before its dependents; synchronized the source PRD with the recorded US-552/US-553 adjudication.
- Review notes (deferred/rejected): remaining test-only legacy imports remain US-553 work; package-DAG registration and shared scan-tier roots are explicitly owned by US-554; the untouched nightly CI-codegen guide-scenario count assertion fails at 15 expected versus 19 generated and is outside this story.
- Guide-Coverage-Skip: Internal package-seam extraction changes no user-visible behavior or public contract.
- Verification: typecheck and lint passed; explicit-base guide drift, release-workflow tests 2/2, npm-dev plus engine-closure tests 14/14, package import smoke, generated-output boundary, and managed-file boundary passed. The unrelated nightly CI-codegen suite had 15 passes and the one baseline failure noted above.
- **Learnings for future iterations:**
  - Adding a package to the release inventory requires regenerating every committed workflow derived from that inventory in the same story.
---

## 2026-08-10 16:48 CDT - Babysit
- PR: #728 https://github.com/AaronFeledy/core4/pull/728
- CI outcome: pass
- Review feedback: fixed (registered managed-file package DAG + workspace roots; repointed stale engine managed-file test imports)
- Drift check: clean
- Merge: merged (squash)
---

## 2026-08-10 16:54 CDT - US-553
- Verified the prior managed-file review completed the remaining test-consumer migration: core unit, library, and contract coverage resolves through `@lando/managed-file`, and the legacy engine import sweep is clean. No new code was required.
- Verification: typecheck and lint passed; 61 focused managed-file tests passed across six files with a positive count.
- **Learnings for future iterations:**
  - Review fixes can complete the next explicitly bounded story; verify the merged tree before inventing duplicate migration work.
---

## 2026-08-10 17:13 CDT - US-553 Review
- Reviewed the consumer migration against the source PRD; no in-scope blocker or code fix was found.
- Review notes (deferred/rejected): rejected the isolated review-worktree QA failure because that detached worktree lacked generated bootstrap sources; the prepared story worktree passed the same six-file suite with 62 tests. US-554 owns the remaining `engine/src/managed-file` boundary-rule, fixture, and inventory strings, which do not match US-553's exact legacy import sweep.
- Guide-Coverage-Skip: Internal test-import migration changes no user-visible behavior or public contract.
- Verification: explicit-base guide drift, typecheck, and lint passed; focused managed-file spot checks passed 62/62 across six files and 104/104 across the seven-file adjacent blast radius; Bugbot found no bugs.
---

## 2026-08-10 18:59 CDT - US-554
- Added `@lando/managed-file` to the shared runtime scan tier and converted the managed-file boundary into an owner-excluding residual rule with no carve-outs; package direction, fixtures, inventory, and durable ownership guidance now agree on the private seam.
- Verification: typecheck and lint passed; 141 relevant tests across 12 files passed; managed-file, package-dag, and import-cycle boundary gates passed.
- **Learnings for future iterations:**
  - When review work pre-lands part of a later story, preserve it and lock the exact policy with focused tests rather than recreating the implementation.
---

## 2026-08-10 19:16 CDT - US-554 Review
- Corrected the managed-file contract wording so it no longer implies named-consumer exemptions that conflict with the owner-excluding rule's empty carve-outs.
- Review notes (deferred/rejected): rejected no findings; all five review-work lanes and Bugbot otherwise passed without in-scope blockers.
- Guide-Coverage-Skip: Internal boundary-policy and specification wording changes have no user-visible behavior or public contract impact.
- Verification: explicit-base guide drift, typecheck, and lint passed; focused boundary-policy tests passed 20/20; managed-file, package-dag, and import-cycle gates passed.
---

## 2026-08-10 20:09 CDT - US-555
- Added package-local characterization coverage for ownership marker round-trips, every codec format including canonical Landofile YAML, and the in-memory service decision algorithm across create, update, skip, adoption, drift, and refuse-clobber outcomes.
- Verification: typecheck and lint passed; the package script passed 11/11 and the focused package-plus-core managed-file lock passed 111/111; managed-file, package-dag, and import-cycle boundaries passed. The aggregate boundary run retained only the pre-existing spec-reference findings in unchanged ignored `.omo/**` planning files.
- **Learnings for future iterations:**
  - Package-local service tests can bind `makeManagedFileService` to a narrow in-memory `ManagedFileBackend`, preserving core integration coverage while locking the owning package's decision behavior directly.
---

## 2026-08-10 20:32 CDT - US-555 Review
- Reviewed the package-local marker, codec, and service-decision characterization lock; no in-scope code blocker or review fix was found.
- Review notes (deferred/rejected): rejected the missing root `bun test` transcript as a Review blocker because the loop contract reserves the full suite for CI and forbids running it locally; rejected the stale top-level status summary as pre-existing wave bookkeeping outside US-555's acceptance criteria.
- Guide-Coverage-Skip: Package-local characterization tests and a package test script change no user-visible behavior or public contract.
- Verification: explicit-base guide drift, typecheck, and lint passed; the package script passed 11/11 and the focused package-plus-core managed-file lock passed 120/120; all branch-relevant boundary rules passed, with only the pre-recorded ignored `.omo/**` spec-reference baseline remaining; Bugbot found no bugs. The root full-suite lock remains CI-owned per the Review host-safety policy.
---

## 2026-08-10 21:08 CDT - Babysit
- PR: none (no PR for branch us-555-managed-file-seam-tests; branch has no upstream)
- CI outcome: n/a
- Review feedback: none
- Merge: deferred (missing PR; push step should create/refresh the PR)
---

## 2026-08-10 21:13 CDT - US-555
- Verified the package-local managed-file seam lock complete on the existing branch; no new code was required.
- Verification: typecheck and lint passed; the package script passed 11/11, the focused package-plus-core lock passed 116/116, and the story-owned boundary rules passed. The aggregate boundary run retained only the pre-recorded unchanged `.omo/**` spec-reference baseline.
- **Learnings for future iterations:**
  - When a reviewed story returns to Build before Push, revalidate the existing branch and record completion without inventing implementation work.
---

## 2026-08-10 22:57 CDT - US-556
- Split self-update into focused error, manifest, verification, Windows replacement, staging, and orchestration modules while preserving the exact operation export surface and existing CLI import path.
- Verification: typecheck and lint passed; 40 focused update tests, 30 adjacent library export tests, and the Linux criteria 6-9 acceptance wrapper passed; import-cycle, telemetry inventory, and all story-relevant boundary rules passed. The aggregate boundary run retained only the pre-recorded unchanged `.omo/**` spec-reference baseline.
- **Learnings for future iterations:**
  - For behavior-preserving module decomposition, lock runtime exports before moving code and keep type-only exports explicit in the stable barrel; this makes both leaked internals and missing public symbols observable.
---

## 2026-08-10 23:21 CDT - US-556 Review
- Corrected result-schema ownership to match the manifest concern, added the required concern header, and extended the stable barrel lock to cover the exact type-only export set as well as runtime values.
- Review notes (deferred/rejected): rejected the unrun full local suite as a blocker because the Review host-safety contract reserves it for CI; Bugbot, security, QA, and code-quality review found no other in-scope defects.
- Guide-Coverage-Skip: Internal module ownership and export-lock corrections change no user-visible behavior or public contract.
- Verification: explicit-base guide drift, typecheck, lint, and import-cycle passed; focused update tests passed 41/41 and adjacent export tests passed 30/30.
---

## 2026-08-11 00:35 CDT - US-557
- Decomposed app planning into bounded service-type, capability, file-sync, storage, authored-config, endpoint, extension, naming, and assembly modules while preserving the exact planner facade and generated consumer paths.
- Verification: typecheck, lint, and import-cycle passed; 148 focused planner and adjacent behavior tests passed across eight files; the 55-line facade and all planner modules stayed within their required limits. The root full-suite lock remains CI-owned under the loop host-safety policy.
- **Learnings for future iterations:**
  - Run scoped Biome formatting after a pure module move and before the gate wave so import ordering and type-only imports are normalized without touching unrelated files.
---

## 2026-08-11 00:55 CDT - US-557 Review
- Reviewed the planner decomposition against the source PRD and the approved execution checklist; no in-scope blocker was found and no code change was required.
- Review notes (deferred/rejected): rejected restoring `normalizeAppSlug` and `APP_SLUG_MAX_LENGTH` to the facade because both the acceptance criterion and the dual-reviewed plan fix that surface at exactly eight symbols, the two symbols have zero consumers repo-wide, and re-exporting them would add dead seams the pre-ship tenets forbid. Rejected restoring the pre-move accumulator lifetime because every call site invokes `plan()` freshly — including the `Effect.suspend` resolver, which re-invokes it per execution — so the difference is unobservable, and threading shared mutable state back across the execution boundary would reintroduce cross-execution leakage.
- Guide-Coverage-Skip: Internal engine module decomposition changes no user-visible behavior or public contract.
- Verification: explicit-base guide drift, typecheck, lint plus deprecations, and import-cycle passed; focused planner tests passed 148/148 across eleven files and all changed-file diagnostics were clean. Bugbot found no bugs, and the security and QA lanes reported no defects. The root full-suite lock remains CI-owned under the loop host-safety policy.
- **Learnings for future iterations:**
  - When a general export-stability requirement collides with an enumerated export list, the enumerated surface plus real consumer evidence governs; confirm a removed export has consumers before treating it as a regression.
---

## 2026-08-11 02:27 CDT - US-558
- Made `codegen:check` a two-stage generated-drift gate while keeping deprecation enforcement in lint and typecheck in separate CI jobs outside the portable static-check matrix; updated the regression locks and contributor runbook to match.
- Verification: typecheck and lint/deprecations passed; 83 focused gate, workflow, and runbook tests passed, the portable-matrix codegen lock passed with a positive count, and the real `codegen:check` completed with a clean catalog. The unrelated nightly guide-step count retained its pre-recorded 15-versus-19 baseline failure.
- **Learnings for future iterations:**
  - Keep composite gate names aligned with a single failure class; neighboring semantic and type gates should remain separately invocable and separately documented.
---

## 2026-08-11 02:53 CDT - US-558 Review
- Corrected authoritative release/deprecation specifications and the Linux acceptance proof map that still described the retired four-stage `codegen:check`; preserved the release orchestrator's independent deprecation gate and lint-owned PR enforcement.
- Review notes (deferred/rejected): rejected the nightly `codegen-ci` 15-versus-19 guide-step count as a pre-recorded unrelated baseline; rejected adding a fake scenario when focused `dev:guides` reported no generated output for this existing zero-scenario proof-map guide, and used its positive-count acceptance test plus guide lint instead.
- Verification: explicit-base guide drift, guide lint, typecheck, lint/deprecations, and the real `codegen:check` passed; focused gate and acceptance tests passed 53/53. The spec-reference gate retained only the pre-existing ignored `.omo/**` findings.
- **Learnings for future iterations:**
  - A zero-scenario proof-map guide can be prose-valid while the focused guide driver has no generated test to run; validate it with its owned positive-count acceptance test rather than inventing an always-green scenario.
---

## 2026-08-11 03:02 CDT - US-558 Verify
- Compiled `core/dist/lando`, ran `lando setup` in lando4-dev, and proved pure-drift `codegen:check` (`package.json` two-stage script, 50 focused gate tests, clean real `codegen:check`). Adjacent compiled-binary smoke: `--version`/`--help`, `doctor`, `init --recipe lamp`, `global:start`, app `start`/`info`/`list`, `exec -s appserver -- php -v`, tooling `php`; destroy/global:stop cleanup succeeded.
- **Out-of-scope finding:** `.local/bin/lando4-dev --check` fails on leftover `uidmap` package in the sandbox (dirty baseline from prior runs); does not block managed-runtime setup.
- **Out-of-scope finding:** After `apps:poweroff` left an orphan `rootlessport` holding `127.0.0.1:38080`, so `global:start` failed until the orphan was killed; suspected incomplete managed-runtime teardown.
- **Out-of-scope finding:** Fresh `lando init --recipe lamp` + `start` served `502` via Traefik and in-container Apache returned `403` for `/` even with `index.php` present; exec/tooling PHP worked. Suspected bare lamp webroot/proxy defaults, unrelated to codegen gate changes.
- **Learnings for future iterations:**
  - Prefer absolute path to `core/dist/lando` after `cd` into a temp app; relative `./core/dist/lando` breaks once the cwd leaves the repo.
---

## 2026-08-11 04:02 CDT - US-559
- Collapsed fifteen boundary package aliases into one `check:boundaries` gate, regenerated CI with one shared Boundary gates step, and kept the per-rule scripts and stable rule IDs available for focused test and debugging use.
- Updated contributor guidance and regression locks so the aggregate gate preserves telemetry, Compose coverage, runtime-manifest validation, and the portable matrix's no-typecheck contract.
- Verification: typecheck, lint/deprecations, codegen:check, clean-worktree check:boundaries, guide lint/drift, and the touched guide scenario passed; 52 PR-tier lock tests plus the focused codegen-CI boundary lock passed. The full nightly codegen-CI file retained only its pre-recorded unrelated 15-versus-19 guide-step baseline.
- **Learnings for future iterations:**
  - Prove aggregate repository scanners from a clean worktree when ignored planning artifacts are known to violate a rule; separately record the dirty-checkout baseline instead of changing unrelated ignored files.
---

## 2026-08-11 06:22 CDT - US-560 Adjudication
- Contradiction: SR-04 US-560 requires "Final verification wave (plan F1–F4): ... scope fidelity," and plan F4 says "every touched path maps to a todo"; read literally, closure must map four guide-runner paths merged by PR #729 / squash `54211a9c` to this plan. The SR-04 scope instead owns the drift gate, boundary alias surface, managed-file seam, planner/update decomposition, and closure, while its Guide Coverage section permits guide MDX changes only to repoint stale gate strings. The scope audit therefore rejected reclassifying or reverting `core/test/codegen/guide-scenarios.test.ts`, `scripts/build-guide-scenarios.ts`, `scripts/test-reporters/run-guide-scenarios.test.ts`, and `scripts/test-reporters/run-guide-scenarios.ts`, whose serialization/timeout changes were unrelated merged work.
- Decision: narrowed the US-560 F4 acceptance criterion in `prd.json` to require todo mapping for scanner-retirement-owned paths and to permit exclusions only when closure evidence names the unrelated PR/commit and exact paths; the four PR #729 / `54211a9c` guide-runner paths are the explicit exclusion for this wave.
- Follow-up story: none — the excluded guide-runner behavior is already merged work outside the scanner-retirement contract.
---

## 2026-08-11 06:48 CDT - US-560
- Closed the scanner-retirement wave with no production-code change: US-551..US-559 all read `passes: true`, and the verification contract was re-run from the story branch.
- Closure metrics: boundary package aliases collapsed from 15 to one aggregate gate (net -14) while all 14 rule ids remain individually invocable; the managed-file boundary rule verdict is keep and owner-excluding; the planner facade is 55 LOC and the update facade is 41 LOC; engine has no `@lando/managed-file` package edge; and the portable static-checks matrix still has no typecheck step.
- Plan F1-F3 compliance, quality, and manual-QA checks passed. The F4 scope audit mapped every scanner-retirement-owned path to todos 2-13 across PRs #728-#735. Its sole exclusion is unrelated PR #729 / `54211a9c` guide-runner work in `core/test/codegen/guide-scenarios.test.ts`, `scripts/build-guide-scenarios.ts`, `scripts/test-reporters/run-guide-scenarios.test.ts`, and `scripts/test-reporters/run-guide-scenarios.ts`.
- Full-suite artifact: this branch began with zero commits beyond `origin/main` and only spec-metadata edits, so its code tree matched merged PR #735, whose CI passed every unit shard plus library, recipe, schema, guide, integration, build, and static-check job. That CI run is the full-suite artifact of record because the loop host-safety policy forbids a local full `bun test`.
- Verification: root and managed-file typechecks passed; lint/deprecations passed; all 14 boundary rules passed from a clean detached worktree; `codegen:check` passed twice with a clean catalog; managed-file tests passed 11/11, update-manifest tests passed 33/33, planner module-boundary tests passed 2/2, and 45 story-owned boundary/CI lock tests passed. The unchanged dirty-checkout `.omo/**` spec-reference findings and nightly codegen-CI 15-versus-19 guide-step assertion remain the pre-recorded unrelated baselines.
- **Learnings for future iterations:**
  - When a closure branch has no code commits beyond `main` and only metadata edits, record the tree-identity argument and use the merged PR's green CI as the full-suite artifact instead of violating the local full-suite prohibition.
---

## 2026-08-12 17:23 CDT - US-560
- Verified the completed wave closure on the existing story branch; no new code was required and every story remains marked complete.
- Verification: typecheck and lint/deprecations passed; codegen drift and managed-file package typecheck passed; 79 focused managed-file, update, planner, and gate-lock tests passed; all 14 boundary rules passed from a clean detached worktree. The full-suite artifact remains the already-recorded green CI run for the identical merged code tree because local full-suite execution is prohibited.
- **Learnings for future iterations:**
  - When ignored planning files trip the aggregate spec-reference rule, retain the dirty-checkout evidence and prove the shipped tree from a clean detached worktree rather than changing unrelated local artifacts.
---

## 2026-08-12 17:39 CDT - US-560 Review
- Returned closure to Build because the cited PR #735 CI artifact is not tree-identical to this branch's PR #736 base and omitted the nightly `codegen-ci` test that PR #736 fixed; US-560 therefore lacks the required positive-count full-suite artifact for the exact code tree. Also identified that the recorded façade metrics need the required before/after form: planner `1849 -> 55` LOC and update `1421 -> 41` LOC.
- Review notes (deferred/rejected): rejected the hands-on QA PASS because it accepted the incorrect PR #735 tree-identity premise; rejected no Bugbot findings because Bugbot reported no bugs. Guide-Coverage-Skip: Internal closure metadata changes no user-visible behavior or public contract.
- Verification: explicit-base guide drift, typecheck, lint/deprecations, and 53 focused tests across six adjacent files passed; Bugbot found no bugs. Full-suite proof remains blocked pending green CI for the exact tree with the nightly-tier test included.
---

## 2026-08-12 18:06 CDT - US-560 Drift Audit
- Trigger: 3 cycles, 68 changed lines, 3 commits; escalation declined below the six-cycle threshold.
- Category 2: production-path gate and seam proofs remain intact, but the PR #735 full-suite artifact does not prove the PR #736 tree; US-560 remains pending exact-final-tree CI.
- Category 3: removed unused managed-file ledger read/write backend methods; retained the package root and codec exports required by their contracts.
- Category 5: US-558 and US-559 pass flips are required by closure and backed by merged PRs #734 and #735; no collateral branch edits remain.
- Category 6: synchronized the adjudicated F4 boundary, restored the before/after façade-metric requirement, and corrected stale status bookkeeping.
- Category 7: reclassified earlier full-suite deferrals as US-560-owned CI evidence; the nightly 15-versus-19 baseline is resolved by PR #736.
- Fixed: deleted dead backend ceremony and restored one consistent closure contract; no split or excision was required and `passes` remains false.
- Split/excise decisions: none; no follow-up story or gate-conflict adjudication was required.
- Verification: typecheck and lint/deprecations passed; 67 managed-file tests and 49 gate/workflow tests passed with positive counts, including nightly `codegen-ci` 16/16; full-suite exact-final-tree CI remains unverified.
---
