# PRD: MVP-07 — CI + binary workflow artifacts

## Introduction

Per the user's directive — *binary builds and testing in CI as early as possible* — MVP no longer waits until Alpha to wire CI. This PRD is new (it does not exist in the original [`spec/ROADMAP.md`](../../spec/ROADMAP.md) Phase 1 deliverable list); the roadmap itself is amended in the same change-set so the two stay in sync.

What this PRD ships at MVP:

- **Full CI on Linux x64 only.** Static gates (typecheck, lint, unit + Effect-service tests), the binary build (`bun build --compile`), and the full provider-integration suite (running against a Podman service in the runner) — all per-PR.
- **Workflow artifacts only.** The compiled `dist/lando` is uploaded as a workflow artifact every run. No GitHub Release publishing, no tagged binaries, no `dev` channel, no installer scripts, no signing — those wait for Alpha onward.

What this PRD explicitly does *not* ship:

- macOS or Windows runners (Alpha+).
- linux-arm64 runner (Alpha — multi-arch matrix grows the CI cost curve).
- Nightly distribution rehearsal, weekly provider matrix, scenario-tutorial gates (Beta+).
- Self-update, signing, SBOM, SLSA — RC.

Depends on: **PRD-01 (SDK)**, **PRD-02 (Foundation — `bun run build` works locally)**, **PRD-03 (Effect services)**, **PRD-04 (Providers — integration tests run on Podman socket)**, **PRD-05 (Bundled services)**, **PRD-06 (CLI commands — exit-criteria smoke test)**.

In dependency order this PRD lands **last** in MVP — it wraps every other PRD's gates into a runner-executable workflow. It can be started the moment all the other PRDs are green locally; it doesn't need to wait for them all to be merged.

## Goals

- Every PR to `main` runs the full local `bun run typecheck && bun run lint && bun test && bun run build` gate on a Linux x64 GitHub-hosted runner.
- The full provider-integration suite (PRD-04 `*.integration.test.ts`) runs on the same runner with a Podman socket made available by the workflow setup.
- The compiled `dist/lando` is uploaded as a per-run workflow artifact named `lando-linux-x64`.
- A simple smoke test asserts the uploaded artifact runs (`./lando --version`, `./lando --help`).
- Failures in any stage block the PR — these are gates, not advisory.
- The workflow file is regenerated by the same codegen tooling PRD-02 owns, so drift between local and CI is impossible.

## User Stories

### US-001: `.github/workflows/ci.yml` exists and gates the static-checks pipeline

**Description:** As an open-source contributor, I get fast typecheck/lint/test feedback on my PR. As release engineering, no PR merges with a broken `tsc -b` or `biome check`.

**Acceptance Criteria:**
- [ ] Failing test in `core/test/build/ci-workflow.test.ts` parses `.github/workflows/ci.yml` (using a YAML parser already in the workspace) and asserts:
  - The workflow's `on:` triggers include `pull_request` against `main` and `push` against `main`.
  - A job named `static-checks` exists, runs on `ubuntu-22.04` (or `ubuntu-latest` pinned in a comment), with a step matrix that calls `bun install`, `bun run typecheck`, `bun run lint`, `bun test --filter=!*.integration` (the static suite).
  - The job uses Bun via `oven-sh/setup-bun@v2` (or whatever current pinned version) with `bun-version-file: .bun-version` (or matches `package.json#engines.bun`).
  - The job sets `permissions:` to the minimum needed (`contents: read`).
- [ ] Test asserts the workflow does *not* set `pull_request_target`, does *not* check out forks with write tokens, does *not* expose secrets in fork PRs.
- [ ] Test passes after the workflow file is added.
- [ ] Typecheck/lint/whole-workspace tests pass.

### US-002: CI runs the binary build and uploads it as a workflow artifact

**Description:** As a contributor reviewing a PR, I want to download the compiled binary that PR produces so I can test it locally without rebuilding.

**Acceptance Criteria:**
- [ ] Failing test in `core/test/build/ci-workflow.test.ts` (extends US-001's parse) asserts:
  - A job named `build-linux-x64` exists, depends on `static-checks` (`needs: [static-checks]`).
  - It runs `bun run build` and asserts `dist/lando` exists.
  - It runs a smoke step: `./dist/lando --version`, `./dist/lando --help`, both must exit 0.
  - It uploads `dist/lando` via `actions/upload-artifact@v4` with `name: lando-linux-x64`, `retention-days: 7`.
- [ ] Test asserts the smoke step runs *before* the upload — a broken binary never gets uploaded.
- [ ] After this story merges, a real PR run on GitHub produces a downloadable `lando-linux-x64` artifact (manual verification — document this in the PRD as a one-time check, not a perpetual test).
- [ ] Test passes after the workflow's `build-linux-x64` job is added.
- [ ] Typecheck/lint/whole-workspace tests pass.

### US-003: CI runs the full provider integration suite against a Podman socket

**Description:** As release engineering, the architectural assertion ("core never shells out to provider binaries; we talk to a private Podman socket") is verified on every PR — not just on the developer's box.

**Acceptance Criteria:**
- [ ] Failing test in `core/test/build/ci-workflow.test.ts` asserts:
  - A job named `provider-integration-linux-x64` exists, depends on `static-checks` (`needs: [static-checks]`).
  - It includes a setup step that starts Podman in the runner (e.g. `podman system service --time=0 unix:///tmp/podman.sock &` followed by a wait-for-socket check) and exports `LANDO_TEST_PODMAN_SOCKET=/tmp/podman.sock`.
  - It runs `bun test core/test/scenario` AND `bun test plugins/provider-lando/test --filter=integration` AND `bun test plugins/provider-docker/test --filter=integration` (Docker tests are still gated on `LANDO_TEST_DOCKER_SOCKET` being set; if Docker isn't available in the runner — which is the default — those tests xfail without failing the job).
  - The job runs the MVP exit-criteria scenario test (PRD-06 US-009) explicitly via `bun test core/test/scenario/mvp-exit-criteria.scenario.test.ts`.
  - A teardown step (always-run, even on failure) tears down any leftover containers + the Podman service, so a flaky run doesn't poison cache.
- [ ] Test asserts the job's timeout is set to ≤20 minutes (CI cost guard at MVP — Beta loosens this).
- [ ] Test asserts the job uploads, on failure, a diagnostic bundle (Podman logs, `dist/lando` if built, last 100 lines of each test output) so debugging a CI-only failure is possible.
- [ ] Test passes after the workflow's `provider-integration-linux-x64` job is added.
- [ ] Typecheck/lint/whole-workspace tests pass.

### US-004: CI workflow is the *only* permitted source of truth for CI configuration

**Description:** As release engineering, I don't want bespoke `.yml` files duplicating CI logic. Stale workflows from the OCLIF/Node scaffold (per [`AGENTS.md`](../../AGENTS.md)) must be removed or renamed.

**Acceptance Criteria:**
- [ ] Failing test in `core/test/build/ci-workflow.test.ts` lists every file under `.github/workflows/` and asserts:
  - Exactly one is `ci.yml` (the file this PRD owns).
  - Any pre-existing scaffold workflow (test, release, npm) has been deleted or moved to `.github/workflows/_archived/` with a header comment explaining the deferral.
- [ ] Test passes after stale workflows are removed.
- [ ] Typecheck/lint/whole-workspace tests pass.

### US-005: `scripts/codegen.ts` regenerates the CI workflow

**Description:** As a contributor, I should never need to hand-edit `.github/workflows/ci.yml` for things the codegen owns (Bun version pin, test directory inclusion, plugin matrix). Hand-edits drift; codegen doesn't.

**Acceptance Criteria:**
- [ ] Failing test in `core/test/scripts/codegen-ci.test.ts` runs `bun run codegen` against a fresh checkout, then re-runs it, and asserts no diff in `.github/workflows/ci.yml`.
- [ ] Test asserts the workflow file has a generated-file header comment: `# Generated by scripts/codegen.ts — do not hand-edit. Edit scripts/build-ci-workflow.ts instead.`
- [ ] The generator script lives at `scripts/build-ci-workflow.ts` and is invoked by `scripts/codegen.ts` (PRD-02 US-006). It pulls the Bun pin from `package.json#engines.bun`, the test directory list from `bunfig.toml` (PRD-02 US-009), and the workspace plugin list from `BUNDLED_PLUGINS` (PRD-05 US-007).
- [ ] Test asserts that bumping `package.json#engines.bun` and re-running codegen produces a diff that *only* touches the Bun version string in `ci.yml` (no other unrelated changes).
- [ ] Test passes after `scripts/build-ci-workflow.ts` lands.
- [ ] Typecheck/lint/whole-workspace tests pass.

### US-006: CI failure modes are debuggable

**Description:** When CI fails, the failure should be reproducible locally with one documented command. No "works on my machine" black boxes.

**Acceptance Criteria:**
- [ ] Failing test in `core/test/build/ci-runbook.test.ts` asserts a `docs/ci-runbook.md` file exists (or, if docs aren't being added at MVP, a section in `core/CONTRIBUTING.md` or `AGENTS.md`) and contains:
  - The exact local command that reproduces each CI job (`bun run typecheck`, `bun run lint`, `bun test --filter=!*.integration`, `bun run build`, `LANDO_TEST_PODMAN_SOCKET=... bun test core/test/scenario`).
  - The Podman setup command pattern used by `provider-integration-linux-x64` (so a contributor can spin up the same socket locally).
  - The artifact-download path for inspecting a failed binary.
- [ ] Test asserts the runbook is referenced from the top-level [`README.md`](../../README.md) and from each PR template (the PR template addition is a one-line link, not a full review checklist).
- [ ] Test passes after the runbook is added.
- [ ] Typecheck/lint/whole-workspace tests pass.

### US-007: CI is required for merge to `main`

**Description:** As release engineering, no human can override CI failure to merge into `main`. (This is GitHub branch-protection config, not workflow config — the test asserts the policy through GitHub's API or by reading a `.github/branch-protection.yml` if we add one.)

**Acceptance Criteria:**
- [ ] Failing test in `core/test/build/branch-protection.test.ts` asserts that one of the following is true:
  - A documented `.github/branch-protection.yml` (managed by a tool like Probot Settings) declares `required_status_checks` containing `static-checks`, `build-linux-x64`, `provider-integration-linux-x64`.
  - OR a section in `AGENTS.md` / `docs/ci-runbook.md` explicitly documents that branch protection is configured manually in the GitHub UI with those three required checks, and the test parses that section.
- [ ] At MVP this story may pass with the documented-only path — actual GitHub branch-protection config is a repo-admin task, not a code change. The test ensures the policy is *recorded somewhere* so it can't be silently undone.
- [ ] Test passes after the docs/config addition.
- [ ] Typecheck/lint/whole-workspace tests pass.

## Functional Requirements

- FR-1: `.github/workflows/ci.yml` is the single CI workflow at MVP. It contains exactly three jobs: `static-checks`, `build-linux-x64`, `provider-integration-linux-x64`.
- FR-2: All three jobs run on `ubuntu-22.04` (pinned, with the rationale documented in a workflow comment — `ubuntu-latest` drift is a known footgun).
- FR-3: `static-checks` does not require Podman. It runs on every PR. Wall-clock budget: ≤5 minutes (advisory at MVP, hard cap at Beta).
- FR-4: `build-linux-x64` produces `dist/lando`, runs `--version` and `--help` smoke checks, uploads as `lando-linux-x64` artifact with 7-day retention. Wall-clock budget: ≤5 minutes.
- FR-5: `provider-integration-linux-x64` starts a private Podman socket in the runner, exports `LANDO_TEST_PODMAN_SOCKET`, runs the integration + scenario suites, tears down on success or failure. Wall-clock budget: ≤20 minutes.
- FR-6: Failure of any job blocks the PR. Failures upload a diagnostic bundle (Podman logs, partial test output, the binary if built) for post-hoc debugging.
- FR-7: The workflow file is generated by `scripts/build-ci-workflow.ts`; hand-edits are forbidden (enforced by the generated-file header + the idempotency test in US-005).
- FR-8: No GitHub Release is created at MVP. No tag is pushed. No public artifact URL exists. PR-run artifacts are the only output.
- FR-9: No secrets are used in the workflow at MVP (no signing, no publishing). The workflow can run on fork PRs without secret leakage risk.
- FR-10: The Docker integration test path remains *opportunistic* — if a contributor enables `LANDO_TEST_DOCKER_SOCKET` in their fork, they can validate locally; the default CI runner does not provide Docker, so those tests xfail without failing the job.

## Non-Goals

- **No GitHub Release / tagged pre-release.** Alpha promotes the artifact to a `dev`-channel pre-release.
- **No nightly cron.** Beta adds nightly distribution rehearsal (`spec/13`).
- **No multi-platform matrix.** Linux x64 only at MVP; Alpha adds linux-arm64; Beta adds darwin-{x64,arm64}, windows-x64.
- **No weekly provider matrix.** Beta runs the cross-provider matrix (`@lando/provider-lando` Linux + macOS, Docker Desktop, Docker Engine, Podman Desktop, Podman, Lima, OrbStack).
- **No code signing, notarization, SBOM, SLSA, cosign.** RC.
- **No self-update path or update manifest.** RC.
- **No installer scripts** (`install.sh`, `install.ps1`). RC.
- **No `npm publish` of `@lando/sdk` or `@lando/core`.** Alpha publishes `4.0.0-alpha.N` on the `dev` tag; MVP is private.
- **No telemetry exports from CI.** RC.
- **No PR-comment "preview build" bot.** Out of scope; the artifact download is sufficient at MVP.
- **No CI metrics dashboard.** Beta+ if we ship one at all; MVP just relies on GitHub's built-in workflow runs view.

## Technical Considerations

- **Podman in GitHub-hosted runners.** `ubuntu-22.04` ships with Podman ≥4.x. The setup step starts a private socket via `podman system service --time=0 unix:///tmp/podman.sock &` and waits for the socket file to exist before tests run. Rootless mode (no `sudo`) — that exercises the same code path the dev box uses. If a future runner image drops Podman, fall back to `apt-get install -y podman` in the setup step (slow, ~30s).
- **Test concurrency.** `static-checks` and `build-linux-x64` can run in parallel; `provider-integration-linux-x64` depends on `static-checks` to avoid burning a Podman runner on a tree that won't typecheck. Total wall-clock for a green PR: ~25 minutes.
- **Artifact size.** `bun build --compile` produces a ~50–100MB self-contained binary. 7-day retention × ~10 PRs/day × 100MB = ~7GB rolling — comfortably within GitHub's free-tier artifact storage.
- **Secret hygiene.** The workflow declares `permissions: { contents: read }` at the top level. Jobs that need write (none at MVP) would scope it per-job. Fork PRs run with read-only tokens by default — no secrets exposed.
- **CI-only failures.** The runbook (US-006) is critical: if a contributor can't reproduce a CI failure locally, debugging stalls. The Podman setup pattern in CI must be documented as a copy-pasteable shell snippet.
- **Stale workflows.** `AGENTS.md` notes `.github/workflows/*.yml` are stale OCLIF/Node scaffold. US-004 cleans this up. Don't `git rm` them blindly — read each one, confirm nothing in the org's GitHub Actions UI references them, then remove.
- **Generator self-test.** `scripts/build-ci-workflow.ts` should produce a YAML doc that the workflow validator (`actionlint`, optional but recommended) accepts. If `actionlint` is in the dev toolchain, US-005's test runs it against the generated file as a final assertion.

## Success Metrics

- A "trivial typo" PR (e.g. a comment fix) gets green CI within 10 minutes of push.
- A PR that breaks `tsc -b` is rejected by `static-checks` within 3 minutes — never reaches the provider-integration job.
- The MVP exit-criteria scenario test (PRD-06 US-009) runs green in CI on every push to `main`.
- Zero PRs reach `main` without a green `provider-integration-linux-x64` run.
- Zero hand-edits to `.github/workflows/ci.yml` after this PRD lands — every change goes through `scripts/build-ci-workflow.ts`.
- Mean time to debug a CI-only failure ≤30 minutes (anecdotal at MVP; tracked from Beta when the runbook gets used in anger).

## Open Questions

- Should we adopt `actionlint` as a dev dep + CI gate now, or defer to Alpha? Default: adopt now — it's a 2MB binary and catches workflow regressions early. If it's not already in the toolchain, document the install in `docs/ci-runbook.md`.
- The "diagnostic bundle on failure" pattern (US-003) needs a documented shape — what files, what naming, what retention. Default: `failure-diagnostics-${{ github.run_id }}.tar.gz` containing `podman.log`, `dist/lando` if built, `core/test/**/*.log` (any test that wrote a log file). Implementer fills the exact contents.
- Branch protection in US-007 is *policy*, not code. Should we adopt Probot Settings (`.github/settings.yml`) to manage it as code? Default: Beta. At MVP, the documented runbook + a manual one-time setup is acceptable.
- `bun test`'s `--filter` semantics for excluding integration tests: confirm the exact flag in current Bun (`--testNamePattern`? `--filter`? path-based exclusion?). Implementer pins the right invocation.
- Is `ubuntu-22.04` or `ubuntu-24.04` the right pin? Default: `22.04` — older but more stable Podman; revisit at Alpha when 24.04 is the LTS.
