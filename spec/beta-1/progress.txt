## Codebase Patterns
- Public SDK JSON Schema artifacts are generated from `JSON_SCHEMA_NAMES` via `scripts/build-schema-snapshot.ts`; keep the registry, snapshot scope, and `dist/schemas/<kebab-name>.json` artifact loop in lockstep.
- Deprecation runtime usage should emit through `EventService` from `DeprecationService.use` after recording the use and before any severity error; telemetry consumes that same event from a scoped subscriber, not a second deprecation path.
- Deprecation registry population belongs in the plugins bootstrap layer via `DeprecationPluginRegistryLive`; keep the minimal `DeprecationServiceLive` empty and O(1), and call `register` only from registration/bootstrap walks, never from use hot paths.
- Registry plugin postinstall trust keys on the requested package identity (`plugin:add` spec after version stripping), never package-controlled manifest names; authoring-root trust is only for local/link flows and must not apply to managed registry cache paths.
- Plugin install trust is a two-phase path: package extraction/manifest validation is inert and must not run lifecycle scripts; only a trusted result may run a package postinstall through `bunSelfInstall` / BunSelfRunner so pre/post bun-self-exec events and the recursion guard are preserved.
- Schema deprecation metadata should use the shared `SchemaDeprecationAnnotationId` annotation and AST unwrapping helpers so JSON Schema, generated reference content, and optional/union-wrapped fields stay in sync.
- Library-mode executable guides: select with `<Run runtime="library">` carrying explicit `code` (executed inside the generated `Effect.gen` body with `context`/`Effect`/`expect`/`LandoCore`/`LandoTesting` in scope) and `displayCode` (reader-visible host TS). A scenario must be all-library or all-cli/shell; mixed runs are rejected at generation time. Per-frame runtime metadata is intentionally NOT on the PublicTranscript schema — runtime is derived per scenario.

## 2026-06-08 10:12 CDT - US-200 Review
- Reviewed `lando setup` orchestration against PRD-01. Build step committed the docker AC5 isAvailable probe but left the equivalent podman fix uncommitted in the working tree; committed it as a focused review fix so no half-finished change reaches the push step. Podman's bundled construction already fails with remediation via construction-time `api.info` capability introspection, so the change is defense-in-depth/consistency (replaces a latent `isAvailable: true` lie with a real probe) and keeps the docker/podman availability surface symmetric. Added podman availability unit test.
- Review notes: AC1 (meta:setup at minimal + top-level alias), AC2/AC3 (deterministic orchestration + skip flags), AC4 (provider setup.flags), AC5 (docker probes daemon, podman caps-introspection) all verified against PRD; no rejected/deferred findings of note.
- Verification: typecheck, lint passed; targeted tests green (setup.test.ts + docker/podman availability = 21 pass; full podman provider suite = 66 pass / 2 skip).
- **Learnings for future iterations:** Bundled runtime providers gate availability two ways — docker uses STATIC capabilities so its `isAvailable` probe is the only reachability gate, while podman/lando introspect the socket (`api.info`) at construction so `registry.select` itself fails with remediation before `isAvailable` is ever read. Keep both providers' `isAvailable` honest (probe, never hardcode `true`) even when construction already gates them.
---
## 2026-06-08 15:59 CDT - US-201
- Completed the deferred file-sync resume path by making the first accelerated `app:start` publish clear progress messaging before it finishes Mutagen setup, preserving the existing setup-time deferred marker and native-provider no-op behavior.
- Verification: typecheck, lint, focused setup/start tests, and serialized full test suite passed.
- **Learnings for future iterations:** File-sync resume messaging should go through the task-tree progress seam (`task.detail`) so renderer modes receive the same user-visible setup status without direct stdout/stderr writes.
---
## 2026-06-08 16:35 CDT - US-201 Review
- Fixed a destructive regression in the deferred file-sync resume path: the build step moved `engine.setup()` inside `startFileSyncSessions`, whose only error handler is `Effect.tapError(rollbackAppliedApp)` (destroys volumes + state). A transient Mutagen-download failure on the first accelerated `app:start` would therefore tear down the just-applied app. Setup failure now degrades to an un-accelerated start with clear stderr messaging instead of propagating, matching the pre-existing "engine unavailable -> skip" contract and removing the throw-vs-still-unavailable inconsistency. Added a regression test asserting no session creation, no provider.destroy, and the degrade message.
- Review notes (rejected/deferred): Oracle flagged that the bundled app-layer `FileSyncEngine` (`makeFileSyncEngine()` with no `userDataRoot` + `makeUnavailableMutagenClient`) makes `setup()` a no-op and `isAvailable` permanently false, so AC4's resume is dormant in production. Rejected as out-of-scope for US-201: the diff does not touch the app layer/plugin, and a real fix needs the not-yet-implemented host-CLI-backed Mutagen client ("supplied by a later layer") — wiring `userDataRoot` alone changes nothing while the client stays fail-closed. Tracked as a known gap for the host-CLI-client story.
- Verification: typecheck, lint, and focused start/setup/rebuild/restart scenario tests passed.
- **Learnings for future iterations:** Any optional/best-effort step added inside `startFileSyncSessions` is under `rollbackAppliedApp` (destroy volumes+state); swallow its failure or a transient blip destroys a successfully-applied app.
---
## 2026-06-08 16:30 CDT - US-201 Verify
- Verified US-201 end-to-end in the `lando4-dev` sandbox: `cd core && bun run build` (compiled binary) succeeds; `bun run typecheck` and `bun run lint` clean; `core/test/cli/setup.test.ts` = 23 pass (incl. the linux-x64 compiled-setup install-dir test); `start`/`rebuild`/`restart` scenarios + `plugins/file-sync-mutagen` suites = 83 pass / 1 skip. US-201 acceptance fully green.
- The one skipped suite (`scaffolds an app and starts it against the live Podman socket`) was exercised against a real rootless Podman socket. It is gated on `LANDO_TEST_PODMAN_SOCKET` and requires the SAME preconditions CI configures: pre-pulled images (provider never auto-pulls — all three providers return `pullArtifact: unavailable`), `net.ipv4.ip_unprivileged_port_start=0` (the global `traefik` proxy publishes host ports 80/443), and a free host port 80. With those satisfied the app start path runs; none of the failures encountered were Lando defects — each was an environment precondition.
- **Diagnostics fix shipped:** the container runtime providers swallowed the actual Podman/Docker create/start failure reason behind a generic "run `lando destroy`" message — the real cause (e.g. "rootlessport cannot expose privileged port 80, add `net.ipv4.ip_unprivileged_port_start`" or "bind: address already in use") was captured in `details.body` but never rendered. Added a shared `withApiReason(message, details)` helper that parses the Podman/Docker JSON error body (`{ cause, message, response }`) and appends the secret-redacted reason to the user-facing message. Wired it through every swallow site: `provider-lando` bring-up (create/start/inspect/network), bring-down (stop/remove/network/volume), and inspect; `provider-docker`'s `serviceStartFailure`/`unavailable` builders (create/start/network connect/inspect); and `provider-podman`'s named-pipe info probe (reusing the exported helper, since podman delegates container lifecycle to `provider-lando`). Tests cover the surfaced reason + secret redaction and the non-JSON-body no-op for both lando and docker.
- **Learnings for future iterations:** Running the live Podman scenario outside CI needs CI's `Setup Podman` + `Pre-pull container images` preconditions replicated (sysctl `ip_unprivileged_port_start=0`, pre-pulled `traefik:v3.3`/`node:lts`/`postgres:16`/etc., free port 80). The lando provider is pull-free by design, so image presence is a hard precondition, not a fallback.
---
## 2026-06-08 18:59 CDT - US-202
- Implemented setup network-trust preflight so `lando setup` resolves proxy and CA settings before provider or Mutagen download work, fails early with classified remediation, and threads resolved proxy/TLS options into the Lando runtime bundle and Mutagen downloader fetch paths.
- Verification: typecheck, lint, targeted setup/download/SDK compatibility tests, compiled parity targets, and serial full test suite passed.
- **Learnings for future iterations:** Setup download paths should consume one resolved network-trust object from the setup preflight; plugin downloaders can use Bun fetch's `proxy` and `tls.ca` options directly while still respecting `NO_PROXY` bypasses.
---
## 2026-06-08 20:50 CDT - US-202 Build
- Finished the in-flight setup network-trust story by committing the remaining probe hardening: configured proxy/CA trust now triggers an actual pre-download HEAD probe with injectable fetch, uses Bun fetch proxy/TLS options, classifies HTTP 407 before provider setup, and includes platform-specific remediation in network-trust failures.
- Verification: focused setup tests, setup CLI help surface, typecheck, lint, and full tests passed.
- **Learnings for future iterations:** Network-dependent setup behavior should keep the resolver pure and inject only the fetch/probe seam in command tests; this proves ordering and proxy/TLS options without requiring real corporate network infrastructure.
---
## 2026-06-08 21:10 CDT - US-202 Review
- Fixed a misclassification in the setup network-trust preflight: `validateSetupNetworkTrust` re-ran `classifySetupNetworkFailure` on the probe's already-classified error, so through the real `lando setup` path an HTTP 407 proxy-authentication failure was downgraded to `blocked-registry` (wrong remediation, violating AC4/AC5). The existing 407 test only exercised the probe directly and missed it. Removed the redundant re-classification and added a full-path regression test asserting the `proxy-authentication` kind survives to the command boundary.
- Review notes: AC1 config-proxy-precedence is all-or-nothing (a partial `network.proxy` ignores env for the unset field) — accepted as a reasonable block-level precedence reading, not a blocker. `fetchInitForNetwork` is duplicated across core + the two plugin packages — accepted given the package boundaries (no shared seam without new SDK surface).
- Verification: typecheck, lint, full `bun test` (3202 pass / 27 skip / 0 fail) passed.
- **Learnings for future iterations:** Classify-on-throw seams (probe catch) must not be re-classified by their callers; assert the precise error kind through the *command* path, not just the probe, or the regression hides behind a direct-probe unit test.
---
## 2026-06-08 21:15 CDT - US-202 Verify
- Built the compiled binary and verified US-202 end-to-end inside `lando4-dev`: `setup --provider=podman --yes` completed cleanly; missing custom CA and proxy-auth scenarios failed before provider work with distinct `SetupNetworkTrustError` messages/remediation naming `network.ca.certs`/`LANDO_NETWORK_CA_CERTS` and `network.proxy`/`HTTP_PROXY`/`HTTPS_PROXY`; `NO_PROXY=github.com` bypass allowed setup to complete; adjacent `meta:setup`, Docker-unavailable remediation, setup help, and doctor help smoked successfully.
---
## 2026-06-08 21:30 CDT - Babysit
- PR: #319 https://github.com/AaronFeledy/core4/pull/319
- CI outcome: pass
- Review feedback: fixed (guide-drift gate flagged provider-lando/file-sync-mutagen surface paths owned by alpha-3 PRDs; added a truthful `Guide-Coverage-Skip` PR-body line since the changes only thread shared proxy/TLS network config through existing download paths, then pushed an empty commit so CI re-read the updated body)
- Merge: merged (squash)
---
## 2026-06-08 22:58 CDT - US-203
- Shipped the setup privilege and shell PATH integration surface: setup now carries `PrivilegeService` through provider/CA setup, applies shell profile integration through the privilege seam with failure reporting, and the concrete privilege layer handles Linux sudo askpass helpers.
- `lando shellenv` now emits safe, non-mutating `<userDataRoot>/bin` snippets for POSIX and PowerShell, with source/compiled validation parity for supported `--shell` values.
- Verification: typecheck, lint, build, full tests, compiled shellenv CLI QA, and Oracle reviewer gate passed.
- **Learnings for future iterations:** Shellenv stays a read-only stdout command; persistent shell profile changes belong to setup and must route through `PrivilegeService`, with compiled/source flag validation kept in parity.
---
## 2026-06-09 11:04 CDT - US-204
- Completed setup readiness reporting: setup writes atomic per-step evidence, preserves failed partial summaries, successful reruns replace interrupted readiness with a ready summary, and doctor renders the latest redacted setup-readiness diagnostic for support triage.
- Verification: focused setup/doctor tests, typecheck, lint, full tests, and source CLI doctor QA passed.
- **Learnings for future iterations:**
  - Read persisted diagnostic summaries through a sanitizing seam; persisted files may be hand-edited or come from older versions, so renderers should not assume secrets were redacted at write time.
---
## 2026-06-09 11:29 CDT - US-204
- Tightened setup readiness completion after review: absent CA/proxy/shell integration services now appear as unavailable readiness steps with remediation instead of disappearing, and doctor preserves the provider id from the setup run in its readiness diagnostic.
- Verification: focused setup/doctor regressions, typecheck, lint, full tests, source CLI doctor QA, and Oracle reviewer gate passed.
- **Learnings for future iterations:**
  - Readiness summaries should enumerate every expected setup stage even when the live runtime lacks an optional service; omitted steps are ambiguous for support.
---
## 2026-06-09 12:10 CDT - US-204 Review
- Hardened secret redaction for the new setup-readiness evidence/remediation that now persists network/proxy/download failure `cause.message` to a support-consumed file (`readiness.json` → `lando doctor`). The shared `redactString` previously only redacted env-style `KEY=value`, so a corporate proxy URL with embedded credentials (`http://user:pass@proxy`), a `Bearer <token>` header, or a signed-download `?token=`/`X-Amz-Signature=` query param would leak into readiness output, violating AC5 "without leaking secrets". Added URL-userinfo, bearer-token, and key-anchored secret-query-param redaction (anchored so substring matches like `?design=` are not redacted) plus a regression test.
- Review notes (rejected): `installed` vs `satisfied` step vocabulary — AC2 ("reported as satisfied with evidence, not silently skipped") is met since every step records non-empty evidence; distinguishing newly-done from already-present needs service-contract changes (out of scope). Swallowed readiness writes (`catchAll`→void) — intentional resilience; failing real host setup because a diagnostic write failed would be worse, and doctor already degrades on a missing summary. Partial-summary false `ready` after an abnormal SIGKILL between steps — real but edge-case; the modeled interruption (a step throwing) correctly records `failed`, idempotent rerun resume works, and no AC requires accurate completeness reporting after a hard kill — left as a noted limitation to keep the diff focused.
- Verification: typecheck, lint, full `bun test` (3225 pass / 27 skip / 0 fail) passed.
- **Learnings for future iterations:** When a story starts persisting raw exception messages to support/diagnostic artifacts, the redaction seam must cover URL-embedded credentials and signed-URL query secrets, not just env-style `KEY=value` — proxy/download errors routinely carry both.
---

## 2026-06-09 12:54 CDT - Babysit
- PR: #322 https://github.com/AaronFeledy/core4/pull/322
- CI outcome: pass
- Review feedback: fixed (Bugbot Medium "shell readiness duplicated" — setup now replaces readiness steps by id so doctor surfaces the actual shell-profile failure; documented new setup-readiness doctor check to clear guide-drift gate)
- Merge: merged (squash)
---

## 2026-06-09 14:21 CDT - US-205
- Shipped the first-class uninstall preview surface: `meta:uninstall`/`uninstall` now register at minimal bootstrap, render the full dry-run/refusal plan across provider runtime/machines, Mutagen assets, CA trust, global state, caches, binary, shell entries, and data/cache roots, and refuse destructive execution without `--yes`.
- Verification: typecheck, lint, build, focused uninstall/dispatch tests, source+compiled CLI dry-run/refusal QA, and full tests passed.
- **Learnings for future iterations:** Promoting a deferred command to MVP requires removing it from deferred plans and every hard-coded deferred parity probe while adding both the OCLIF MVP allowlist entry and the compiled `run.ts` branch. Installed-binary ownership checks must normalize slash styles so Windows-style `<userDataRoot>\bin\lando.exe` paths classify as Lando-owned.
---

## 2026-06-09 14:42 CDT - US-205 Review
- Reviewed `lando uninstall` preview against PRD-01 US-205: minimal-bootstrap registration + `uninstall` alias, dry-run/refusal/`--yes` gating, the 11-item owned/user-owned/skipped/manual plan, and the read-only `buildUninstallPlan` (no rm/destroy/trust-store/shell mutation even with `--yes`) all verified. No code changes needed — both reviewer lanes passed with no blocking issues.
- Review notes (deferred): `installedBinaryStatus` uses normalized string-prefix containment (trailing-slash guard prevents `/bin2` false positives, Windows drive paths case-folded) — adequate for preview-only US-205, but US-206 must harden to canonical `resolve()`/`relative()` containment (resolving `..`/symlinks) before this plan drives real deletion. Repeated `join()` per step + optional `assertNever(status)` left as-is (NITPICK, not worth churning a focused review diff).
- Verification: typecheck, lint passed; focused uninstall + not-implemented + deferred-commands + dispatch-parity suites green (43 pass / 0 fail). Working tree clean since the build commit, so the build step's full-suite run stands.
- **Learnings for future iterations:** When a preview/plan command (US-205) hands its plan to a later destructive command (US-206), path-ownership checks that are "good enough" for a read-only preview (string-prefix containment) must be re-hardened to canonical-path containment before any deletion executes — review the ownership seam, not just the new deletion code, when the destructive story lands.
---
## 2026-06-09 16:50 CDT - US-206
- Completed destructive uninstall execution modes: keep-data now removes owned toolchain targets while preserving app-affecting data and global state, purge opts into owned data/cache removal, user-owned binaries remain manual, and partial failures persist a resumable report.
- Verification: red/green uninstall TDD, source and compiled CLI QA, guide scenarios, typecheck, lint, build, full tests, and Oracle reviewer gate passed.
- **Learnings for future iterations:**
  - Destructive uninstall needs mode-aware planning even for previews; keep-data should mark protected data roots skipped while purge should keep them eligible for owned removal.
---

## 2026-06-09 17:31 CDT - US-206 Review
- Fixed a destructive-command preview accuracy bug introduced by US-206: bare `uninstall --dry-run` and bare refused `uninstall` printed `mode: keep-data` yet built the raw unprotected plan (data roots/global state shown as `owned (destructive)`), so the preview overstated what default `--yes` execution actually does (keep-data preserves them). The non-executing branch now plans with the resolved `mode` instead of `requestedMode`, so `--dry-run` matches `--yes` for every flag combination; updated the inventory test and added a `--dry-run --purge` preview test.
- Review notes (rejected/out-of-scope): catastrophic-path guards for env-derived `userDataRoot`/`userCacheRoot` before `rm -rf` — no AC requires it, the roots come from pre-existing shared resolvers used by all Lando file ops, and a guard system is a separate hardening story (noted as a future defense-in-depth follow-up). `installedBinaryStatus` Windows `..` lexical containment — not reachable since real `process.execPath` is OS-normalized. Purge partial-failure report recreating `<userDataRoot>/uninstall/report.json` and the caches/user-cache-root double-target — per-spec report location and a pre-existing US-205 plan shape (force-rm no-op), respectively.
- Verification: typecheck, lint, and focused uninstall + dispatch-parity + manifest + deferred/not-implemented suites pass. Full `bun test` shows only non-deterministic flakiness in heavy subprocess/build suites (compile-binary, dev:guides, ci-codegen, biome-lint, docs:scenario) — a different subset each run, all green in isolation, none touching uninstall.
- **Learnings for future iterations:** When a read-only preview command (US-205) gains a destructive executor with mode selection (US-206), the dry-run/refusal preview must plan with the *resolved* default mode, not the *explicitly requested* mode — otherwise `--dry-run` and `--yes` diverge and the safety preview lies about what will be destroyed.
---

## [2026-06-09 18:19 CDT] - Babysit
- PR: #324 https://github.com/AaronFeledy/core4/pull/324
- CI outcome: pass
- Review feedback: fixed (Bugbot Medium: failure report recreated a purged data root — guarded report write with existsSync(userDataRoot) + regression test)
- Merge: merged (squash)
---

## 2026-06-09 19:30 CDT - US-207
- Locked source↔compiled dispatch parity for setup/shellenv/uninstall: compiled `runCompiledCli` now rejects unknown flags (`rejectUnknownFlag`) for `meta:setup`/`meta:uninstall` and aligns the `--host-proxy` invalid-value exit code to OCLIF's `2`; shellenv parsing is shared through the exported `shellenvShellFromInput` helper. Added behavioral parity coverage (alias≡canonical for setup/shellenv/uninstall, `--purge` dry-run, unknown-flag, dry-run, refusal), a dual-dispatch argv-parser parity case, a renderer-boundary test proving uninstall results flow through the Renderer seam, the `compiled-binary-setup-parity` executable guide + INDEX row, and hardened the parity normalizer to strip the Linux `/proc/self/exe " (deleted)"` runtime marker so a rebuilt `dist/lando` stays at parity.
- Verification: typecheck (tsc -b), biome, check:renderer-boundary, check:guide-coverage, lint:guides, the guide scenario (dev:guides --once), 120 focused setup/uninstall/shellenv/parity/renderer/deferred/not-implemented tests, manual QA driving BOTH `bun core/bin/lando.ts` and `core/dist/lando` for the alias/dry-run/refusal/host-proxy cases, and a 3× determinism re-run of the dispatch-parity suite all pass.
- **Multi-agent collision (transparency):** a concurrent agent on this same branch/tree had independently implemented most of US-207 (the run.ts/shellenv/dual-dispatch/dispatch-parity/guide changes) but left it uncommitted and went idle ~18:35; its rogue full-suite `bun test` was rebuilding `core/dist/lando` mid-run and caused intermittent `ENOENT`/`(deleted)` failures in the compiled-binary parity tests until terminated. I reviewed that work diff-by-diff (coherent + correct), layered my complementary hardening on top, verified the combined tree against every gate, and committed only the US-207 unit — deliberately EXCLUDING unrelated concurrent artifacts (`spec/alpha/`, `spec/alpha2/`, `spec/beta/`, `core/test/build/alpha-platform-deferrals.test.ts`).
- **Learnings for future iterations:** Before diagnosing compiled-binary test flakiness, check for a rogue/leftover background `bun test`/`build:compile` rebuilding `core/dist/lando` — a concurrent rebuild surfaces as `ENOENT`/`/proc/self/exe " (deleted)"`, which looks like a test bug but is environmental; parity normalizers must strip that `(deleted)` marker. When two workers land on one story in one working tree, stage explicit per-file lists (never `git add -A`) to keep unrelated concurrent work out of the commit.
---

## 2026-06-09 20:35 CDT - US-207 Build
- Finished the in-flight dispatch parity branch by hardening the uninstall renderer exit-code seam and adding shellenv unknown-flag parity coverage, preserving identical source and compiled behavior for setup/shellenv/uninstall.
- Verification: focused parity/renderer/command suites, typecheck, lint, renderer-boundary check, full tests, and source+compiled CLI QA passed.
- **Learnings for future iterations:** Renderer result helpers that set process exit status should expose an injectable setter for tests, so renderer-boundary tests can assert exit behavior without leaking global `process.exitCode` between cases.
---

## 2026-06-09 22:00 CDT - US-207 Review
- review-work (Oracle) flagged that the compiled `$bunfs` argv parser diverged from OCLIF source dispatch for setup/shellenv/uninstall in exactly the way FR-14 forbids. Confirmed empirically against both paths and fixed the parity seam (`rejectInvalidInvocation`/`invocationParityError`) so the compiled binary now rejects, with exit 2 matching source: a value passed to a boolean flag (`uninstall --yes=false`/`--purge=false`/`--dry-run=false` — previously executed/purged in compiled mode while source refused — a real destructive divergence), extra positionals and tokens after `--`, and a value flag with no value or one followed by another recognized flag (`setup --provider`/`--provider --yes`/`--host-proxy`). Option flags now emit OCLIF's `expects one of these values: …` diagnostic, so `--shell --bogus` and bare `--shell`/`--host-proxy` are byte-identical to source. Added behavioral parity tests covering each case.
- Review notes (rejected/deferred): clustered short flags (`uninstall -yy`) — source accepts via OCLIF cluster expansion, compiled rejects; left as an accepted divergence (compiled is stricter, no safety risk, pathological pre-existing input, not worth replicating OCLIF cluster logic). `setup.test.ts` "compiled setup install dir" intermittently fails on `expect(build.exitCode).toBe(0)` — environmental `bun run build` flakiness under load (ETXTBSY), reproduces on committed HEAD too and passes in isolation; not caused by this change.
- Verification: typecheck, lint, dispatch-parity (28), setup (39), and uninstall/shellenv/renderer/dual-dispatch/deferred/not-implemented suites pass; manual QA driving both `bun core/bin/lando.ts` and `core/dist/lando` confirms exit-code + message parity for every fixed case.
- **Learnings for future iterations:** Dual-dispatch parity is not just unknown-flag rejection — the compiled hand-rolled parser must replicate OCLIF's full parse-error surface (boolean flags reject `=value`; value flags reject a missing value or a following recognized flag; no-arg commands reject positionals incl. after `--`). Verify parity empirically against BOTH `bin/lando.ts` and `dist/lando`, not by reading the parser, since `compiledCommandInputFromArgv` silently over-accepts where OCLIF errors.
---
## 2026-06-10 00:16 CDT - US-208
- Confirmed the Beta 1 Bun floor stays at `>=1.3.14`, documented the decision and target coverage, and aligned local, CI, and nightly binary compilation on `bun build --compile --bytecode` for every configured release target.
- Hardened the compiled entry for bytecode by removing top-level await and keeping compiled-mode dispatch detection stable when bytecode preserves the source entry URL.
- Verification: typecheck, lint, full tests, focused CI/runbook/compile/parity tests, and bytecode-built compiled CLI smoke passed.
- **Learnings for future iterations:**
  - `bun build --compile --bytecode` rejects top-level await in the CLI entry and can preserve `import.meta.url` as the source `.ts` path, so compiled-mode detection must use `process.execPath`/the executable name instead of relying only on `$bunfs` or the entry URL.
---

## 2026-06-10 08:35 CDT - US-209
- Closed the OCLIF major-version decision for Beta 1 by staying on OCLIF v4, preserving the current package ranges, and documenting that source-mode OCLIF remains paired with the permanent compiled `runCompiledCli` dual-dispatch parity contract.
- **Multi-agent collision + coherence fix (transparency):** this Build step was implemented concurrently in one working tree — the initial decision-doc/spec/ci-runbook/prd edits (08:22-08:24) landed from a sibling worker that was then interrupted by a looper-TUI quit (08:25:49) before completing its verification. On review I found the §14.2 table was left self-contradictory: the sibling added the `OCLIF major version` row to "Resolved since this draft" but did NOT remove the stale row from the "Open decisions" table, so the decision was listed as both open and resolved. Removed the stale open-decisions row so the spec is coherent, and added `core/test/build/oclif-major-lock.test.ts` whose third case asserts the open→resolved move (regression guard the sibling's ci-runbook test lacked).
- Verification: focused decision-note guards (`oclif-major-lock.test.ts` + `ci-runbook.test.ts` OCLIF case) = 7 pass / 0 fail; `bun run typecheck` (tsc -b) clean; `bun run lint` (biome) clean. NOT committed: the operator requested quit from the looper TUI after the sibling's edits, so the verified working tree is left for the operator to commit or discard.
- **Learnings for future iterations:** OCLIF major upgrades must be their own migration story because source-mode loading, manifest generation, hooks, help/plugin behavior, and compiled/source parity all have to move together while compiled dispatch stays hand-rolled. When a decision moves from "open" to "resolved" in the §14.2 table, it must be REMOVED from the Open-decisions table in the same edit, not just appended to Resolved — otherwise the spec contradicts itself and the guard test must assert both halves of the move.
---

## 2026-06-10 09:05 CDT - US-209 Build
- Finished the in-flight OCLIF major-lock branch: verified the decision-only story is complete and story-scoped, then committed it. Beta 1 stays on OCLIF v4; the decision note, the §14.2 open→resolved move, and the package-metadata sync guards are all in place, and the compiled `runCompiledCli` dual-dispatch parity contract is documented as preserved.
- Verification: focused decision guards (`oclif-major-lock.test.ts` + `ci-runbook.test.ts`) green, `bun run typecheck` clean, `bun run lint` clean, full `bun test` = 3291 pass / 27 skip / 0 fail.
- **Learnings for future iterations:** When a prior Build step leaves a decision story verified-but-uncommitted (operator quit before commit), the follow-up Build step's job is to re-verify and commit the exact story-scoped file set — not to re-implement. Stage an explicit file list so unrelated concurrent edits in the same tree never ride along.
---
## 2026-06-10 12:25 CDT - US-210
- Closed the provider auto-setup default decision as guided opt-in: `lando setup` remains the explicit setup surface, interactive/non-interactive/CI defaults share the same provider precedence, and unavailable system providers fail with remediation instead of triggering aggressive background provisioning.
- Added the setup-default guide and tests locking the decision note, resolved §14.2 row, guide index entry, and `--yes`/`--no-interactive` setup parsing behavior.
- Verification: typecheck, lint, build, focused setup/decision tests, guide coverage/lint, guide scenario generation, source+compiled CLI QA, and full tests passed.
- **Learnings for future iterations:** Decision stories that affect setup UX should be locked in three places at once: `docs/beta-1-decisions.md`, the §14.2 open/resolved decision table, and a small build test that asserts the user guide and guide index row exist so the PRD acceptance cannot drift from docs.
---
## 2026-06-10 13:50 CDT - US-211
- Shipped the Compose compatibility matrix: the schema now accepts the frozen top-level subset (`services`, `volumes`, `networks`, `configs`, `secrets`, `include`, `x-*`) plus deprecated `version`, and `app:config:lint` fails closed with targeted remediation for rejected Compose keys.
- Published the user guide and locked it with schema-constant tests so the guide, guide index, decision note, JSON Schema, and validation behavior stay aligned.
- Verification: red/green focused tests, typecheck, lint, guide coverage/lint/scenario generation, manual `app:config:lint --format=json` QA, and full tests passed.
- **Learnings for future iterations:** Effect Schema's `TemplateLiteral` index signature decodes `x-*` keys correctly, but its raw JSON Schema output needs a small Landofile-specific post-process to publish `additionalProperties:false` plus `^x-` `patternProperties` instead of the generic property-name schema.
---

## 2026-06-10 14:25 CDT - US-211 Review
- review-work (2 Oracles) returned PASS/PASS with no blockers; all six acceptance criteria met. Fixed one real regression both Oracles and hands-on QA flagged: `composeSuggestedFix` in `core/src/landofile/lint.ts` keyed off `path[0]` at any depth, so a nested unknown key under an accepted top-level key (e.g. `services.web.bogus`) got the generic top-level Compose message instead of the precise "Remove the unknown key" remediation. Constrained the matrix to `path.length === 1` and added a regression test locking nested-key behavior.
- Review notes (rejected): the `services: "Expected undefined"` duplicate ParseResult violation on nested errors is a pre-existing Effect quirk (`onExcessProperty:"error"` + `errors:"all"` on optional record-valued props) — reproduces with the pre-change plain `Schema.Struct`, so out of scope. `delete schema.propertyNames` vs `= undefined` in json-schema.ts — cosmetic nitpick with no functional impact (JSON.stringify omits undefined) and would churn the SDK snapshot; rejected.
- Verification: typecheck, lint, focused lint/compose/schema suites (45), and CLI QA of `app:config:lint --format=json` confirm precise nested remediation restored and top-level accepted/deprecated/rejected behavior intact.
---

## 2026-06-10 15:35 CDT - Babysit
- PR: #330 https://github.com/AaronFeledy/core4/pull/330
- CI outcome: pass
- Review feedback: fixed (scoped `composeSuggestedFix` to suppress top-level Compose remediation on nested parent-path violations + regression tests; added Guide-Coverage-Skip for internal-only lint.ts scoping change)
- Merge: merged (squash)
---
## 2026-06-10 16:05 CDT - US-212
- Closed the SSH-agent sidecar opt-out decision as reject-direct-mount: `sshAgent.sidecar: true` remains the default supported path, while `sshAgent.sidecar: false` is reserved and fails Landofile validation with remediation instead of silently enabling host-agent socket mounts.
- Published the decision in Beta 1 docs, moved the §14.2 row to resolved, and updated the SSH sidecar guide to identify the rejected direct-mount path.
- Verification: red/green schema, lint, decision-doc, SDK compatibility, guide lint, generated guide scenarios, CLI `app:config:lint --format=json` QA, typecheck, lint, and full tests passed.
- **Learnings for future iterations:** Reserved boolean opt-outs can be represented as `Schema.optional(Schema.Literal(true))` to accept omitted/default and explicit true while rejecting false with targeted lint remediation.
---
## 2026-06-10 18:05 CDT - Babysit
- PR: #331 https://github.com/AaronFeledy/core4/pull/331
- CI outcome: pass
- Review feedback: fixed (narrowed sshAgent lint remediation to the exact reserved-false schema violation shape `_tag: "Type"`/`Expected true, actual false` per Bugbot, and documented that remediation in the config-lint guide to clear the guide-drift gate)
- Merge: merged (squash)
---
## 2026-06-10 19:55 CDT - US-213
- Shipped the plugin trust surface and postinstall gate: trust state is schema-published as sorted unique plugin/root lists, trust commands persist and invalidate the plugin command cache, untrusted postinstall packages remain inert, and trusted postinstall execution routes through the recursion-guarded BunSelfRunner install path with pre/post lifecycle events.
- Added the plugin trust/postinstall guide and locked trust command, schema, and postinstall gating behavior with focused scenarios.
- Verification: typecheck, lint, build, guide coverage/lint/scenario generation, focused trust tests, source+compiled CLI QA, and full tests passed.
- **Learnings for future iterations:** Keep plugin package extraction inert; any trusted lifecycle script execution belongs in the separate BunSelfRunner install step after trust resolution so untrusted installs cannot run scripts and observers still get bun-self-exec events.
---
## 2026-06-10 20:30 CDT - US-213 Review
- Reviewed the plugin-trust + postinstall-gating commit against PRD-02 US-213 via the 5-lane review-work pass. All 8 acceptance criteria verified: schema-published sorted-unique trust lists (the store's `.sort()` now matches the schema's `previous < value` UTF-16 ordering so it can never persist a state its own schema rejects), both trust commands render exact lines and invalidate the plugin command cache, and trusted-only postinstall execution routes through the recursion-guarded `bunSelfInstall` path while untrusted (incl. non-interactive) installs stay inert. No in-scope blocker required a code change.
- Review notes (deferred to US-214, which explicitly owns trust scope/keying): (1) postinstall trust keys on attacker-controllable `manifest.name`/`landoPlugin.name` rather than the requested package identity, so a package could claim an already-trusted name and inherit postinstall permission; (2) `isAuthoringRootTrusted` is evaluated against the npm cache install target, so trusting a parent of the plugins root would auto-trust future registry plugins. Both are pre-existing in the trust-decision block (unchanged by this commit) and concern npm/registry + authoring-root trust scope — exactly US-214's AC. Recorded as US-214 inputs rather than fixed here to avoid pre-empting that story's scope decision.
- Review notes (rejected): `publishBunSelfEvent`'s `event as never` is not a schema bypass — `EventService.publish` takes the intentionally open `LandoEvent` interface and AC4 requires these ad-hoc tags. Retaining persistent trust after a failed `--trust` install is a defensible explicit-intent choice, not an escalation. The `Effect.promise` spawn pattern is pre-existing in `bunSelfRun` and not introduced by this story.
- Verification: typecheck, lint, full tests (3321 pass / 0 fail), focused trust scenarios, guide-coverage, renderer-boundary, and schema-snapshot codegen (no drift) all passed.
---

## 2026-06-10 21:45 CDT - US-214
- Finalized the plugin trust model: trust list/revoke now ship on the plugin trust command, trust is non-expiring until revoked, npm/registry trust is keyed to the requested package identity, and authoring-root trust is scoped away from registry cache installs.
- Published the flag model and source-scope decision, added the trust-management guide, and locked the decision/guide surface with build tests.
- Verification: typecheck, lint, renderer-boundary, focused trust tests, guide coverage/lint/scenario, source+compiled CLI QA, build, and full tests passed.
- **Learnings for future iterations:** Registry plugin postinstall trust must key on the requested package spec identity, not package-controlled manifest names; authoring-root trust is for local/link flows and must not make managed registry cache paths trusted.
---

## 2026-06-10 22:30 CDT - US-214 Review
- 5-lane review-work pass: Goal/Constraint (PASS), QA (PASS, host-safe gates), Security (PASS, manifest-name escalation confirmed closed — trust keys on requested `packageName`, authoring-root no longer applied to registry cache), Code Quality (raised dual-dispatch parity), Context Mining (raised stale spec refs). Fixed the one in-scope blocker: stale §14.2 "open/optional" trust references that contradicted the decision this story resolves — updated `spec/ROADMAP.md` (trust-gating + command-surface lines now read as resolved, list/revoke shipped, non-expiring) and `spec/README.md` (trust-row no longer labelled "open decisions").
- Review notes (rejected): Code Quality's MAJOR "extract a shared OCLIF/compiled arg parser + add trust parity tests" is out of scope — run.ts hand-parsing every command id is the permanent compiled-dispatch architecture (not introduced here), valid `list`/`revoke`/`<name>` input dispatches identically on both paths and the §13.1 structural parity test passes; the only divergences (tolerating an extra positional, missing-name error wording) are abnormal-input edge cases that both still error/exit non-zero. The "plugin literally named list/revoke" positional collision is acceptable for Beta 1 (such names remain revocable via `plugin:trust revoke <name>`) and matches the published grammar.
- Verification: focused decision build test green; edits are markdown-only with no test asserting the changed lines, so the build commit's typecheck/lint/full-suite results stand.
---
## 2026-06-11 01:58 CDT - US-215
- Shipped the canonical `DeprecationNotice` schema from the SDK schema surface and core schema re-export, including semver/docs URL validation, severity defaulting, remove-schedule validation, and the structural dedupe key.
- Extended the schema snapshot generator to emit the `dist/schemas/deprecation-notice.json` artifact and locked standalone plus nested JSON Schema metadata preservation without recursive `$ref` output.
- Verification: red/green focused tests, schema artifact QA, typecheck, lint, full tests, and high-rigor reviewer gate passed.
- **Learnings for future iterations:** Effect filtered schemas can drop or distort JSON Schema metadata; for canonical public schemas with refinements, attach a non-recursive `jsonSchema` fragment and assert both direct `JSONSchema.make(...)` and nested schema snapshots preserve metadata without `$defs` self-recursion.
---
## 2026-06-11 02:35 CDT - US-215 Review
- 5-lane review-work pass on the DeprecationNotice schema commit: Goal/Constraint (PASS, all 8 ACs met) and Security (PASS, NONE — semver regex is linear-time, docsUrl validated-not-fetched, build writes constant paths, biome spawn is fixed-argv). Fixed the one in-scope blocker: the canonical `dist/schemas/deprecation-notice.json` artifact (and snapshot) declared `docsUrl` as only `format: "uri"`, which a JSON-Schema validator accepts for `ftp:`/`mailto:`, while the runtime predicate accepts http(s) only — that drift breaks AC5's "round-trips losslessly". Added a case-insensitive `^[Hh][Tt][Tt][Pp][Ss]?://` pattern to the hand-written JSON Schema so the published artifact faithfully matches the runtime contract; regenerated the snapshot + artifact (runtime decode behavior unchanged).
- Review notes (rejected): (1) QA flagged runtime stripping unknown keys vs JSON Schema `additionalProperties:false` — this is standard Effect `Schema.Struct` behavior present 136× across the SDK snapshot (decode strips, JSON Schema emits `additionalProperties:false`), not introduced by this story. (2) Context Mining's "remove the decode-time 4.1.0 floor" — the floor *implements* AC3 ("removeIn validation rejects ... missing values once a notice is older than 12 months"); removing it would loosen functionality against the story's own acceptance criteria. The schema has no date field, so a semver-window proxy is the only decode-time expression; true date/release enforcement is US-222's release gate (§18.7), which the build correctly did not pre-empt. (3) Code Quality's plain-struct-export + separate `DeprecationNoticeChecked` convention (per `GuideFrontmatterChecked`) — conflicts with the literal AC3 requirement that the published `DeprecationNotice` validation rejects, and the build already avoids the JSON-schema-collapse regression via explicit `jsonSchema` annotations; deferred as an architectural preference, not an AC blocker.
- Verification: typecheck, lint, and full tests (3335 pass / 0 fail before the fix) green; focused schema/snapshot/exports/frontmatter tests (34 pass) green after the fix.
---

## 2026-06-11 12:38 CDT - US-216
- Shipped the central deprecation registry service: minimal bootstrap now carries `DeprecationService`, plugins bootstrap populates the registry off the hot path, runtime use records per-process counts, severity-error surfaces fail, and deprecated canonical command aliases are guarded against non-deprecated alias registration.
- Verification: focused RED→GREEN deprecation tests, typecheck, lint, full tests, manual library-surface QA, and high-rigor reviewer gate passed.
- **Learnings for future iterations:**
  - Keep deprecation registration in bootstrap/registry walks and the use path as a single keyed state update; future renderer/event/doctor stories should consume the same summary/lookup API rather than re-walking manifests.
---

## 2026-06-11 15:55 CDT - Babysit
- PR: #335 https://github.com/AaronFeledy/core4/pull/335
- CI outcome: pass
- Review feedback: rejected (Bugbot "Registry severity ignored on use" triaged as not-a-defect — §18.3 makes DeprecationUse.notice the runtime authority for `use`; register/lookup is a separate doctor/config index and no production `.use()` call sites exist, so the mismatch is unreachable; documented in a PR reply)
- Merge: merged (squash)
---

## 2026-06-11 17:35 CDT - US-217
- Shipped the typed `deprecation-used` lifecycle event: deprecated runtime use is recorded, published through `EventService`, visible to library subscribers, and consumed by telemetry from the same event stream without aborting the originating behavior on subscriber failure.
- Verification: focused deprecation/schema/export tests, library API QA, codegen regeneration check, typecheck, lint, and full tests passed.
- **Learnings for future iterations:**
  - Deprecation observers should subscribe late to the shared event stream; keep telemetry as an event consumer so future deprecation surfaces do not grow parallel reporting hooks.
---
## 2026-06-11 19:25 CDT - US-218
- Shipped schema-level deprecation annotation propagation: `DeprecationNotice` can now be attached to schemas and fields, JSON Schema output emits `deprecated: true` plus valid `x-deprecation`, generated reference markdown and schema documentation text use the same annotation metadata, and the schema snapshot gate rejects invalid emitted deprecation payloads.
- Verification: typecheck, lint, schema codegen, focused deprecation/schema/export tests, full tests, and Oracle reviewer gate passed.
- **Learnings for future iterations:** Schema deprecation metadata should use one custom Effect Schema annotation symbol and one AST unwrapping helper so JSON Schema, reference docs, and optional/union-wrapped fields cannot drift.
---

## 2026-06-12 07:55 CDT - US-251
- Shipped the release orchestrator stage contract: the release script now exposes a fixed 13-stage pipeline, preserves artifact-family filtering without reordering, routes argv-shaped and shell-shaped work through separate Bun runner paths, and reports first-stage failures with tagged release metadata for operators.
- Verification: RED focused test failed on the missing orchestrator export before implementation; focused release tests, typecheck, lint, manual injected-runner release surface, and Oracle review passed. Full test suite has one isolated pre-existing package-export identity failure in `sdk/test/library/exports.test.ts`, reproducible outside release files.
- **Learnings for future iterations:** Keep release stages data-driven and injectable; tests should exercise `runRelease` with fake runners so release-order, target filtering, and failure wrapping are proven without accidentally executing publish/signing commands.
---

## 2026-06-12 08:25 CDT - US-251 Review
- Review tightened the release stage contract: strict TypeScript issues were fixed, release errors now report concrete artifact-family metadata, and the publish stage remains part of binary traversal without executing npm publishing for binary-only runs.
- Review notes (deferred/rejected): Beta/next npm publishing remains follow-on release-publish scope; US-251 only locks ordering, runner shape, and error wrapping.
- Verification: focused release tests, typecheck, lint, and changed-file diagnostics passed.
- **Learnings for future iterations:** Release stage metadata should model the future artifact-family matrix even while a stage body is still stubbed, so later stories do not have to undo tests that encoded a temporary skip.
---

## 2026-06-12 09:10 CDT - US-219
- Shipped contract-field and manifest-field deprecations across plugin manifests, recipe manifests, Landofile tooling, built-in command/alias/flag/arg contracts, built-in event/render/service/route surfaces, and the schema/plugin registry walks that populate `DeprecationService`. Invalid deprecation notices now fail strict manifest decoding before plugin surfaces can be used, while command caches and provider/planner consumers preserve contribution object refs by id.
- Verification: focused RED→GREEN built-in contract walk test, focused deprecation/schema/manifest/cache/plugin-registry matrix, typecheck, lint, schema snapshot codegen, guide coverage/lint, story guide scenario, and compiled parity suites passed. Full `bun test` completed once with only self-induced concurrent guide/build races, then targeted reruns passed; isolated `core/test/scripts/dev-guides.test.ts` still shows an unrelated dev-guides harness failure (`no generated scenario output found for dev-guides-red`) outside this story's surfaces.
- **Learnings for future iterations:** Plugin contribution arrays can now contain either string ids or `{ id, deprecated }` objects, so every command-cache/planner/provider consumer must normalize with a contribution-id helper before sorting, fingerprinting, or resolving ids. Do not run `bun test` concurrently with `dev:guides` or `build:compile`; generated guide output and `core/dist/lando` are shared artifacts and will race.
---

## 2026-06-12 11:40 CDT - Babysit
- PR: #338 https://github.com/AaronFeledy/core4/pull/338
- CI outcome: pass
- Review feedback: fixed (Cursor Bugbot flagged library bundle stage building all workspaces; release stage now builds only release packages)
- Merge: merged (squash)
---
## 2026-06-12 11:50 CDT - US-240
- Shipped telemetry default precedence: global config now defaults telemetry on for CLI mode, CLI runtime construction resolves valid config/env before building the Telemetry layer, `LANDO_CONFIG__TELEMETRY__ENABLED=0` disables telemetry ahead of runtime construction, and `makeLandoRuntime` remains library default-off unless the embedding host opts in.
- Verification: focused telemetry/config/runtime/schema tests, typecheck, lint, and source CLI smoke passed. Full `bun test` is blocked by a pre-existing unrelated `sdk/test/library/exports.test.ts` `DeprecationNotice` core-vs-SDK identity failure, reproduced outside this story worktree.
- **Learnings for future iterations:** Keep shared CLI-runtime helpers outside `core/src/cli/`; OCLIF discovery/test harnesses can misinterpret new root CLI files as command modules.
---
## 2026-06-12 12:25 CDT - US-240 Review
- Fixed review blockers in telemetry precedence: explicit runtime telemetry now wins before env/config, level-none runtime options avoid global config I/O, malformed config without an explicit telemetry env preserves CLI default-on, and the required telemetry defaults guide plus SDK compatibility note are shipped.
- Review notes (deferred/rejected): full `bun test` remains blocked only by the pre-existing `sdk/test/library/exports.test.ts` DeprecationNotice core-vs-SDK identity failure reproduced outside this story.
- Verification: focused telemetry/config/runtime/schema tests, guide lint, guide coverage, typecheck, lint, and source CLI smoke passed.
- **Learnings for future iterations:** Runtime helpers imported by OCLIF init must special-case `bootstrap: "none"` so fast-path commands do not start reading global config before command execution.
---
## 2026-06-12 12:45 CDT - US-240 Verify
- Verified the freshly compiled binary for telemetry defaults and precedence through real `config` and `version` command surfaces, including env-disabled telemetry over config true and malformed config with/without explicit telemetry env.
- Setup sandbox blocker: `core4-agent2` does not contain `.local/DEV-ENV.md` or `.local/bin/lando4-dev`; US-240 has no provider/setup behavior, so verification used host-safe compiled config/runtime surfaces instead of fabricating a setup run.
- **Learnings for future iterations:** Secondary worktrees may not carry `.local/` sandbox helpers; verify prompt setup runs are only possible from worktrees that include those local harness files.
---

## 2026-06-12 13:30 CDT - Babysit
- PR: #339 https://github.com/AaronFeledy/core4/pull/339
- CI outcome: pass
- Review feedback: fixed (Cursor Bugbot Medium findings: built-in aliases now inherit the canonical command's deprecation notice, and unsupported tooling flags/args/deprecated metadata are rejected via a shared scanner wired through service discovery, config translate, and includes verify/update)
- Merge: merged (squash)
---

## 2026-06-12 14:58 CDT - US-220
- Shipped TSDoc deprecation enforcement for public TypeScript surfaces: `markDeprecated()` now records Effect-returning export usage through `DeprecationService.use` while preserving the wrapped callable, tagged errors can publish static deprecation metadata, and the lint gate rejects public `@deprecated` exports without runtime metadata or matching note/replacement text.
- Verification: focused deprecation/export compatibility tests, `check:deprecations`, typecheck, lint, and full tests passed.
- **Learnings for future iterations:** Keep deprecated public TypeScript exports Effect-returning when using `markDeprecated()` so runtime recording can ride the ambient `DeprecationService`; the TSDoc text must include the notice note and replacement so IDE/API output stays aligned with machine-readable metadata.
---

## 2026-06-12 15:23 CDT - US-220 Review
- Closed a soundness hole in the new `scripts/check-deprecations.ts` lint gate: a public deprecated export whose TSDoc `@deprecated` sat on the local declaration and was re-exported via a bare untagged `export { name }` slipped through unrecorded. The gate now resolves the local declaration's tag node so these exports are enforced like any other; added a regression test.
- Review notes (rejected): deprecated `interface`/type-only exports stay unflagged (cannot be wrapped by `markDeprecated()` at runtime); imported-notice text parity degrading to "non-empty TSDoc passes" kept as acceptable graceful degradation, not a blocker.
- Verification: focused deprecation/scripts/exports tests, full `core/test/deprecation` + `core/test/scripts` suites, typecheck, and lint (incl. `check:deprecations`) passed.
- **Learnings for future iterations:** TypeScript JSDoc attaches to the enclosing `VariableStatement`, not the `VariableDeclaration`, so any AST scanner enforcing tag rules on `const x = ...; export { x }` must resolve the statement, not the declaration.
---

## 2026-06-12 17:38 CDT - US-220 Build
- Finished the in-flight deprecation TSDoc enforcement branch by extending the lint gate to catch deprecated local function declarations that are later exposed through named exports without `markDeprecated`, matching the existing local variable/class named-export enforcement.
- Verification: focused red/green regression, deprecation lint script surface, typecheck, lint, and full tests passed.
- **Learnings for future iterations:** Named-export deprecation checks must collect every local declaration shape that can carry TSDoc (`const`, `function`, and `class`), otherwise a deprecated local can bypass runtime `markDeprecated` enforcement simply by being exported separately.
---

## 2026-06-12 18:35 CDT - US-220 Review
- Reviewed the build step's deprecation-gate hardening (explicit markDeprecated export-id overload + MISMATCHED enforcement, deprecated-function named-export catch, type-only skip, external re-export skip). No in-scope blockers found; made no code changes.
- Review notes (rejected): Oracle flagged three lint-gate shapes as unenforced — external re-exports (`export { x } from "./mod"`), `export default` expressions, and `export enum`/`export namespace`. Rejected as theoretical: the scanned source (sdk/src, core/src, plugins) currently has ZERO `@deprecated` tags, no deprecated default exports, and no enums/namespaces, so none is a real false-pass. The external re-export skip is the correct single-file-scan decision (cross-module bindings aren't in same-file localDeclarations, so checking them would false-positive on every source-tagged re-export); real deprecated runtime exports are enforced on their source declaration. Cross-module resolution / default+enum+namespace fail-cases are forward-looking hardening, out of scope for US-220.
- Verification: typecheck, lint (incl. check:deprecations gate), and full `bun test` (3400 pass / 0 fail) passed.
- **Learnings for future iterations:** If the deprecation model later tags a barrel-only re-export, `export default`, an enum, or a namespace as `@deprecated`, `scripts/check-deprecations.ts` will silently pass it — harden those AST shapes (and add cross-module resolution for re-exports) at that point.
---

## 2026-06-12 19:17 CDT - US-220 Verify
- Verified US-220 against the real compiled binary in `lando4-dev`: fresh `core` build, isolated `setup --provider=podman --yes --no-interactive`, temp-app `app:config:lint`, `list`, `doctor --format=json`, and binary `--version` all completed cleanly; host gates `check:deprecations`, focused deprecation/script suites, typecheck, lint, and full tests passed.
- Fixed a lint-gate soundness blocker: local functions named `markDeprecated` no longer satisfy the public `@deprecated` runtime-wrapper requirement; only imported `markDeprecated` bindings from the SDK-style import path are accepted, while aliased imports still pass.
- **Learnings for future iterations:** Deprecation enforcement should treat `markDeprecated` as an imported public SDK helper, not a magic local identifier, or user code can shadow the name and bypass runtime `DeprecationService.use` recording.
---

## 2026-06-12 20:05 CDT - Babysit
- PR: #340 https://github.com/AaronFeledy/core4/pull/340
- CI outcome: pass
- Review feedback: fixed (Cursor Bugbot across 5 rounds — distinct per-export ids for anonymous arrow exports, skip external/type-only re-exports, catch untagged deprecated function re-exports, require imported markDeprecated bindings so local/aliased stubs and fake property-access calls cannot bypass the gate, and reject the two-arg `markDeprecated(notice, "id")` form lacking an implementation argument)
- Merge: merged (squash)
---
## 2026-06-12 20:55 CDT - US-241
- Shipped telemetry opt-out controls: `lando config telemetry off` writes `telemetry.enabled: false`, `lando config telemetry status` reports the effective state and source, and CLI runtime telemetry resolution now exposes flag/env/config/default source precedence without changing library default-off behavior.
- Verification: RED focused command/source tests failed before implementation; focused telemetry/config/parity tests, guide coverage/lint/scenario generation, typecheck, lint, build, source+compiled CLI QA passed. Full `bun test` is blocked by the pre-existing `sdk/test/library/exports.test.ts` DeprecationNotice core-vs-SDK identity failure; deferred-command timeout failures from the full run passed in isolation.
- **Learnings for future iterations:** Telemetry status should read the same Effect-free config/env resolver used before runtime construction so opt-out reporting does not accidentally build services or touch plugin discovery.
---
## 2026-06-12 21:20 CDT - US-241 Verify
- Verified the freshly compiled binary for telemetry opt-out controls: `config telemetry off` writes `telemetry.enabled: false`, and `meta:config telemetry status` reports env override source correctly.
- Setup sandbox blocker: `core4-agent2` does not contain `.local/DEV-ENV.md` or `.local/bin/lando4-dev`, so the mandatory `lando setup` sandbox run could not be performed from this worktree without faking host setup verification.
- **Learnings for future iterations:** Secondary worktrees may not carry `.local/` sandbox helpers; verify steps that require `lando4-dev` must run from a worktree where those local harness files exist.
---
## 2026-06-12 22:45 CDT - US-242
- Shipped update outcome and deprecation telemetry wiring: `lando update` now records sanitized outcome categories through `Telemetry.record`, `deprecation-used` telemetry is consumed from the shared event stream with inventory-approved fields only, and telemetry-disabled runs suppress both telemetry paths without changing deprecation behavior.
- Verification: RED focused tests failed before implementation; focused update/deprecation/deferred tests, typecheck, lint, build, guide coverage/lint/scenario generation, source+compiled CLI update QA, and reviewer gate passed. Full tests have one pre-existing unrelated SDK/core `DeprecationNotice` identity failure reproduced in isolation.
- **Learnings for future iterations:** Telemetry event helpers should own the machine-readable inventory and construct exact payloads before calling `Telemetry.record`; never pass raw event/use/error objects into telemetry sinks.
---

## 2026-06-12 22:52 CDT - US-221 Review
- Fixed a latent deprecation double-emission introduced by the build step: `deprecation-used` had been added to the live renderer's `isRenderableEvent`, so a `renderEvents:true` command in json/verbose mode would emit each use live (once per `DeprecationService.use`) AND a deduped end-of-run summary, diverging from the source-mode/no-event dispatch paths. Removed it so the renderer-boundary end-of-run summary is the single authoritative path (Oracle-confirmed; satisfies AC5 alone).
- Review notes: AC4 (`doctor --deprecations` / `config --format yaml` ignore suppression) holds because suppression lives only at the renderer warning boundary — command-result output flows through `renderer.output` and is untouched by the `--no-deprecation-warnings` / `LANDO_DEPRECATION_WARNINGS=0` flag; the dedicated reports land in their own stories.
- Verification: typecheck, lint (incl. check:deprecations), renderer-boundary + deprecation + renderer suites, and the full `core/test/cli` suite passed.
- **Learnings for future iterations:** Deprecation diagnostics must have exactly one render path. The renderer-boundary end-of-run summary already covers every dispatch path (it runs even when the live event consumer is off); do NOT also mark `deprecation-used` renderable in `format.ts` or json/verbose event-rendering commands double-emit.
---

## 2026-06-12 22:55 CDT - US-221
- Completed CLI deprecation warning controls: renderer-boundary diagnostics now emit one human warning per used warn-level surface, summarize info-level uses at command end, preserve JSON `deprecation-used` diagnostics, and source/compiled dispatch both honor `--no-deprecation-warnings` plus `LANDO_DEPRECATION_WARNINGS=0` as renderer-only suppression.
- Published the CLI deprecation warnings/suppression guide and confirmed the guide scenario path.
- Verification: renderer-boundary focused tests, typecheck, lint, guide coverage/lint/scenario, build, source+compiled CLI suppression QA, and full tests passed.
- **Learnings for future iterations:** Deprecation renderer suppression belongs at the CLI output boundary after `DeprecationService.use` records and publishes `deprecation-used`; stripping the suppression flag before OCLIF/compiled parsing keeps it global without weakening machine-readable diagnostics.
---
## 2026-06-12 23:05 CDT - US-242 Verify
- Verified the story's host-safe compiled surfaces earlier in the loop (`bun run build`, source and compiled `lando update` smoke), but the required `lando setup` sandbox verification could not run from `core4-agent2` because `.local/DEV-ENV.md` and `.local/bin/lando4-dev` are absent in this secondary worktree.
- Blocker/regression fixed: none; this is the same secondary-worktree sandbox limitation recorded by recent telemetry stories, so no host setup run was fabricated.
- **Learnings for future iterations:** Secondary worktrees without `.local` sandbox helpers cannot satisfy Verify's provider/setup run; use a worktree with the local harness or record the blocker explicitly.
---

## 2026-06-13 01:14 - Babysit
- PR: #343 https://github.com/AaronFeledy/core4/pull/343
- CI outcome: pass
- Review feedback: fixed (Bugbot: deprecation suppression now silences only warn-level lines while still emitting the info-severity end-of-run summary, per US-221; earlier passthrough-`--` and scratch-suppression findings already resolved on branch)
- Merge: merged (squash)
---
## 2026-06-13 02:08 CDT - US-222
- Shipped the deprecation release gate: `codegen:check` now runs codegen, deprecation validation, and typecheck in order; the gate validates released/pending `since` values, old-notice `removeIn` schedules, future major/minor removal targets, and stale/overdue present surfaces across export wrappers and registry-style deprecation objects.
- Verification: focused red/green deprecation tests, lint, typecheck, codegen:check, serialized full test suite, and Oracle reviewer gate passed.
- **Learnings for future iterations:** Deprecation release-gate checks must inspect both `markDeprecated()` export wrappers and registry-style `{ id|name, deprecated }` objects, because runtime deprecations are registered from plugin/built-in contract manifests as well as public exports.
---

## 2026-06-13 03:12 CDT - US-243
- Scenario generation now emits a build-time PUBLIC reader transcript artifact alongside the existing gitignored generated scenario tests. `scripts/build-guide-scenarios.ts` gained `buildPublicTranscript`/`emitPublicTranscripts`, which walk the MDX AST and write `dist/transcripts/public/guides/<guideId>/<scenarioId>[.<variant>].json` for every rendered scenario+variant. Public frames cover step/run/verify/inspect/cleanup/inline/tab and carry guideId, scenarioId, variant, runtime, sourceFile, sourceLine, displayText, commandDisplay, and resultSummary; `<Hidden>` steps, `render={false}` scenarios, `<UseFixture>`, `<Variable>`, temp dirs, and internal event traces are excluded.
- Added additive `@lando/sdk` schemas `PublicTranscript`/`PublicTranscriptFrame` (registered in the JSON-schema registry, SDK_SCHEMA_NAMES, API_COMPATIBILITY, and the regenerated schema snapshot). The internal runtime transcript (`core/src/testing/scenario-context.ts`) is unchanged. Authored `docs/guides/authoring/public-transcripts.mdx` (doc-only) + INDEX row.
- Verification: typecheck, lint passed; focused schema, generator/codegen, scenario-context, ci-workflow, codegen-ci, schema-snapshot, and SDK backward-compat suites green (85 targeted tests). Manual QA: ran the generator and confirmed the real `go.mdx` public transcript on disk. Pre-existing unrelated failure noted: `sdk/test/library/exports.test.ts > core schema re-exports the canonical deprecation notice` fails identically with this branch's work stashed (module-identity quirk on main, not introduced here).
- **Learnings for future iterations:**
  - Public transcripts are a BUILD-TIME codegen product derived from the MDX AST, deliberately separate from the runtime internal transcript; the "verified" coupling holds because the same MDX generates both the gated asserting test and the public frame, so a static expectation-shaped resultSummary stays truthful. US-244 (Starlight consumption) should read these JSON artifacts rather than re-deriving from runtime.
  - `<Inline>` requires a `justification` prop; a bare `<Inline lang code />` fails `decodeInlinePropsEither`. Generated guide scenario tests + transcripts live under gitignored `dist/`/`test/scenarios/generated`, so there is no git-diff drift gate for them — they are validated by running the generated tests and the codegen scripts.
---
## 2026-06-13 04:15 CDT - Babysit
- PR: #345 https://github.com/AaronFeledy/core4/pull/345
- CI outcome: pass
- Review feedback: fixed (Bugbot: registry-style deprecation scanning now resolves identifier-bound notice objects via local notice bindings, not just inline literals; earlier "stale gate uses wrong release" finding already resolved on branch)
- Merge: merged (squash)
---
## 2026-06-13 04:17 CDT - US-223
- Shipped `lando doctor --deprecations`: doctor now adds a dedicated deprecation report from `DeprecationService.summary()` enriched through `lookup(kind,id)`, with plain columns plus structured JSON/YAML output for machine consumers.
- Source and compiled doctor dispatch both accept `--deprecations` and `--format=json|yaml|text`; machine formats suppress post-run deprecation diagnostics so stdout stays parseable while the report still contains deprecation data.
- Verification: red/green doctor report tests, source and compiled CLI QA in `lando4-dev`, build, typecheck, lint, full tests, and Oracle reviewer gate passed.
- **Learnings for future iterations:** Machine-readable commands that intentionally include deprecation data must suppress the renderer's post-run deprecation diagnostics entirely, not only warn-level messages, or info summaries can contaminate JSON/YAML stdout.
---
## 2026-06-13 06:05 CDT - US-223 Review
- Fixed two real review blockers in the doctor deprecations report: dropped the write-only `DoctorReport.format` field (never read; it leaked a CLI selector into the data model and diverged between OCLIF default `"text"` and the compiled path's `undefined`), and broadened machine-output suppression so any `--format=json|yaml` doctor run suppresses post-run deprecation diagnostics (previously gated on `--deprecations`, leaving plain-renderer `--format=json` stdout open to contamination).
- Review notes (rejected): Oracle's "add explicit `global-service` DeprecationSurfaceKind" finding — that union is a frozen/compat-locked SDK literal and §20.4 routes `globalServices: deprecated` through `manifest-contribution`; the report is kind-agnostic so global-service appears when present. Sort tie-breaker / source="core" minors — `summary()` dedups per `(kind,id)` (FR-3) so keys are unique.
- Verification: typecheck, lint (incl. check:deprecations), doctor-report suite, dispatch-parity, and renderer-flag scenarios all green; oclif compiled-manifest has no drift.
---
## 2026-06-13 07:29 CDT - US-224
- Shipped public SDK JSON Schema artifact publication: the generator now uses the public schema registry scope, covers Landofile/service/tooling/route/healthcheck/expression/error/event/plugin/recipe contracts, emits one stable draft-07 `dist/schemas` artifact per registry entry, and keeps recursive expression schemas exportable through stable `$defs` references.
- Verification: focused schema export tests, guide coverage/lint/scenario, typecheck, lint, and full tests passed.
- **Learnings for future iterations:**
  - Recursive Effect Schema contracts may need explicit reference definitions around `Schema.suspend` before JSON Schema generation can emit stable draft-07 output; lock them with artifact-generation tests, not just type exports.
---

## 2026-06-13 08:03 CDT - US-224 Review
- Fixed two real blockers in the public-schema JSON export: (1) `BeforeExitEvent` (the only `LandoEvent` payload absent from the registry) was missing a `dist/schemas` artifact, leaving AC1's "event payloads" coverage incomplete; (2) adding it exposed a build-introduced circular import — `json-schema.ts` imports the events barrel while `events/{app,lifecycle,provider,global}.ts` imported schema primitives back through the `schema/index.ts` barrel (which re-exports `json-schema.ts`), so a cold `import "@lando/sdk/events"` crashed with a TDZ ReferenceError and `bun test sdk/test/events/` failed in isolation. Repointed those events modules to concrete schema source modules so the events layer no longer depends on the schema barrel at runtime; refreshed the schema snapshot and the stale `sdk/AGENTS.md` single-source-of-truth note.
- Review notes (rejected/deferred): Oracle's "many public schema exports missing from registry" (MountInput, StorageInput, BuildBlock, brand primitives like AbsolutePath, Matcher*/Transcript*/Recipe*/ToolingVar* sub-shapes) — out of scope for US-224. These are sub-components reachable through their parent contracts' `$defs`, and forcing them in as standalone artifacts overlaps US-225 (registry completeness) and would trip US-226 annotation enforcement (bare brands/literals lack identifier/title/description). The build's curated 157-entry registry already covers every category AC1 enumerates; only the single missing event sibling was a true gap.
- Verification: typecheck, lint (incl. check:deprecations), full `bun test` (3471 pass, 0 fail), isolated `bun test sdk/test/events/`, and cold `@lando/sdk/events` import all green.
- **Learnings for future iterations:** When a `schema/` module (re-exported by `schema/index.ts`) needs to import event/other-layer schemas, the importing/imported modules must reference each other through CONCRETE source files, never the `../schema/index.ts` barrel — a barrel back-edge through `json-schema.ts` forms a runtime cycle that only crashes on certain entry orders (cold barrel import / isolated focused tests), passing the full suite via incidental load ordering.
---

## 2026-06-13 11:21 CDT - US-243 Review
- Fixed two confirmed defects in public transcript emission: (1) hidden-step `<Variable>` values could be interpolated into visible public `commandDisplay` (AC2 exclusion leak) — public frame variables are now collected from non-hidden steps only; (2) variant `tab` frames were attributed to the scenario line instead of the authored `<Tab>` source line — they now resolve the matching tab node's line via the existing axis-resolution. Added regression tests for both (hidden-variable non-leak; distinct per-variant tab source lines). Internal runtime transcript untouched.
- Review notes (rejected): per-frame denormalization of guideId/scenarioId/variant/runtime — AC3 is satisfied at the transcript envelope (constant per scenario+variant), matching the internal Transcript shape, so per-frame duplication is redundant. Security path-safety on `clearGuideId`/`variant` in the `rm`/output path — mirrors the pre-existing `emitGuideScenarioTests` pattern with maintainer-supplied/schema-validated inputs (data-exposure audit: NONE); out of scope for this story. `runtime:"cli"` hardcode and public-transcript redaction — explicitly downstream (US-245/US-249).
- Verification: typecheck, lint passed; public-transcript + scenario-context + guide-scenarios + schema-snapshot suites green; full codegen re-run emitted 29 artifacts with go/happy-path unchanged.
---

## 2026-06-13 12:00 CDT - Babysit
- PR: #347 https://github.com/AaronFeledy/core4/pull/347
- CI outcome: pass
- Review feedback: none (Bugbot reported no findings)
- Merge: merged (squash); resolved a prd.json/progress.txt union conflict from parallel main advancement (US-222/US-223 landed concurrently) before merge

## 2026-06-13 11:43 CDT - Babysit
- PR: #348 https://github.com/AaronFeledy/core4/pull/348
- CI outcome: pass
- Review feedback: fixed (resolved merge conflict with main across schema/guide/spec files, regenerated schema snapshot, and added a Guide-Coverage-Skip line for the incidental ast.ts import-cycle edit to satisfy the guide-drift gate)
- Merge: merged (squash)
---

## 2026-06-13 12:45 CDT - US-225
- Shipped the central public schema registry and metadata index: `@lando/sdk/schema` now exposes the registry, metadata index, and artifact filename helper; `@lando/core/schema` re-exports the same objects; schema snapshot generation writes the metadata index from that registry.
- Added the schema-registry guide and coverage row so the public registry surface is documented alongside the generated schema artifacts.
- Verification: typecheck, lint, focused schema/export tests, guide coverage/lint/scenario, and full tests passed.
- **Learnings for future iterations:** Treat `publicSchemaRegistry` as the single schema-publication source of truth; JSON Schema artifacts, metadata, docs inputs, and core re-exports should derive from it rather than introducing another schema list.
---
## 2026-06-13 18:03 CDT - US-225 Build
- Finished the in-flight schema registry branch by wiring the public schema registry into generated reference-page inputs and documenting/exporting the final registry helper surface. The SDK schema entry remains the canonical source, core re-exports it by identity, and the schema snapshot gate now locks JSON artifact metadata plus reference-page docs paths to the registry.
- Verification: focused schema/export/compatibility tests, schema snapshot codegen drift check, guide coverage, guide lint, typecheck, lint, and full tests passed.
- **Learnings for future iterations:** Schema reference-page helpers should consume `publicSchemaMetadataIndex` so docs paths, artifact paths, and registry order drift together under the schema snapshot gate rather than a separate hand-maintained docs list.
---

## 2026-06-13 19:20 CDT - US-225 Review
- Reviewed the central public schema registry + metadata index against PRD-04 acceptance criteria; no code changes required. Confirmed single source of truth (publicSchemaRegistry drives JSON_SCHEMA_NAMES, metadata index, JSON artifacts, reference-page inputs, and core re-export by identity), accurate kebab-case artifact paths, and clean git history.
- Review notes (deferred/rejected): Oracle flagged that `packageExport` uniformly claims `@lando/sdk/schema#<id>` while 54 registry entries (errors/events/expressions schemas) are not named top-level exports of the schema barrel. Deferred, not fixed: all are reachable from `@lando/sdk/schema` via the exported `publicSchemaRegistry` object (so AC #5's canonical-surface claim holds), spec §07 line 928 + PRD Technical Considerations designate `@lando/sdk/schema` as the documented entry point even when internal module names differ, and "fixing" it (re-export 54 schemas or re-point to subpaths) is an out-of-scope frozen-surface/type change that would churn the §13.2 snapshot and SDK compat fixtures. Future docs-navigation consumer (US-227) should revisit packageExport precision if a literal resolvable import is required.
- Verification: typecheck, lint, full `bun test` (3482 pass / 0 fail) passed.
---

## 2026-06-13 - US-272
- Promoted `@lando/core/testing` to a declared-stable surface on the `next` channel for Beta 1 (docs/components and docs/redactions stay unstable until GA), and made the deterministic in-memory `TestRuntime` satisfy every default service tag at each bootstrap level.
- `makeTestRuntime`/`provideTestRuntime` now provide level-precise layers: minimal adds Renderer/Telemetry/EventService/DeprecationService/PluginTrustStore/CacheService/PrivilegeService/SecretStore; provider adds PluginRegistry/RuntimeProviderRegistry/GlobalAppService; global adds AppPlanner; scratch adds AppPlanner/LandofileService/ScratchAppService (+ core-private ScratchRegistry/ScratchResourceScanner); app adds AppPlanner/LandofileService/CommandRegistry/ToolingEngine/FileSyncEngine. All doubles are deterministic, in-memory, and never touch real disk/network or process streams; the Renderer double records output into an in-memory call bucket. Heavy lookup/load methods fail deterministically with their own tagged errors rather than performing IO.
- Provider contract now also runs against the `RuntimeProvider` resolved from the TestRuntime layer (not just the directly-imported fake), and every `@lando/core/testing` export carries JSDoc and is asserted by both workspace and packed-install resolution tests. Shipped the executable guide `docs/guides/library/testing-runtime.mdx` (+ INDEX row).
- Verification: typecheck, lint, guide gates (lint:guides + check:guide-coverage), focused testing/library suites, the 9-file TestRuntime-consumer regression set, and the generated guide scenarios passed.
- **Learnings for future iterations:**
  - When widening what `TestRuntime` provides, keep it strictly additive and level-keyed: existing consumers merge their own overrides, so the doubles must not change behavior for tests that already supply a service. A per-`bootstrap` conditional return type keeps each level's provided service union precise without `as any`.
  - The deterministic doubles must never hit process streams (renderer-boundary gate) — route Renderer `output.stdout/stderr` into an in-memory call bucket on the runtime's `calls`.
  - The combined `core/test/library` + `core/test/testing` run can spuriously time out under heavy concurrent load on the heavy init-runner scenario; it passes cleanly in isolation. Run heavy scenario suites serially or with generous timeouts rather than treating a timeout as a regression.
---

## 2026-06-13 - US-272 Review
- A 5-agent review (goal/code-quality/context-mining) flagged that the newly-stable `@lando/core/testing` doubles were dishonest: the event bus dropped published events, the cache silently discarded writes, the deterministic time/random primitives were not re-exported, and the per-bootstrap layer return type over-claimed for union `bootstrap` inputs. Fixed all four: `EventService` is now a PubSub-backed in-memory recording bus, `CacheService` is a Map-backed in-memory cache with schema decode, `TestClock`/`TestContext` are re-exported (with workspace + packed-install coverage), and the bootstrap return type is distributive so a union input yields a sound union of layers.
- Review notes (rejected): keeping `ProcessRunner` at the `minimal` level — it is a pre-existing additive testing convenience consumers rely on, not bootstrap-parity drift, so removal was rejected. Full packed-install *type*-resolution coverage was deemed unnecessary because `tsc -b` already type-resolves the type exports through the workspace test imports; a lightweight compile-time type reference was added instead of a tarball typecheck. Security review found nothing.
- Verification: typecheck, lint, the focused testing/library suites, and the TestRuntime-consumer regression set passed.
- **Learnings for future iterations:**
  - `effect` re-exports `TestClock` and `TestContext` from its root barrel but NOT `TestRandom` (no `effect/TestRandom` subpath); `TestContext` is the combined layer that already provides the deterministic random service, so re-export `TestClock` + `TestContext` to surface Effect's deterministic test primitives.
  - Per-`bootstrap` conditional return types must be written distributively (`Bootstrap extends any ? … : never`) or a union-typed argument silently widens the provided-service set and lets callers resolve tags that won't exist at runtime.
  - In-memory service doubles that "succeed but discard" (no-op cache write, no-op event publish) are a correctness trap once the surface is declared stable — back them with real in-memory state so write→read and publish→subscribe round-trip.
---

## 2026-06-13 19:51 CDT - Babysit
- PR: #349 https://github.com/AaronFeledy/core4/pull/349
- CI outcome: pass
- Review feedback: fixed (four Cursor Bugbot findings across three triage rounds — scratch-registry double out of sync with the scratch service, cache double ignoring TTL, filesystem double allowing a path to be both file and directory, and trust-store path containment using naive prefix matching instead of resolve/relative; each backed by a regression test) plus a main-advancement merge-conflict resolution in the coordination files (prd.json/progress.txt/INDEX.md)
- Merge: merged (squash)
---

## 2026-06-13 - US-245
- Shipped library-mode executable guides: `<Run runtime="library" code={`...`} displayCode={`...`} />` now generates scenario tests that import and execute `@lando/core` library APIs directly through the provided `ScenarioContext` (`context.runtime` / `@lando/core/testing` helpers) instead of routing through `runCli`. `RunProps` became a three-form union (command | shell | library); the library form requires explicit `code` (executed) and `displayCode` (reader-visible), keeping display-vs-execute binding explicit and never inferred. Generated files that contain a library run switch to namespaced `import * as LandoCore`/`import * as LandoTesting` + `LandoTesting.withScenarioContext`, while CLI-only scenarios keep their existing imports unchanged. Public transcripts derive `runtime: "library"` when every run in a scenario is library-mode, emit the run frame with `commandDisplay = displayCode` + `resultSummary = "library code executed"`, and the generator rejects scenarios that mix cli/shell and library runs. Shipped the `docs/guides/embedding/library-mode-guide-scenarios.mdx` acceptance guide (host-safe `FileSystem.readText` via `context.runtime.layer` + `makeTestRuntime`) and its INDEX row; failures still source-map back to the authoring MDX `<Run>` line.
- Verification: typecheck, lint (biome + check:deprecations), lint:guides, check:guide-coverage, codegen:schema-snapshot determinism, and `bun test` (3507 pass / 27 skip) all green. The single failing test (`sdk/test/library/exports.test.ts` "core schema re-exports the canonical deprecation notice") is a pre-existing core-vs-SDK `DeprecationNotice` identity failure unrelated to this story — it reproduces on a clean tree with these changes stashed.
- **Learnings for future iterations:**
  - Adding a library-mode `<Run>` was a single coherent unit across SDK schema + generator + tests because the generated emit string contract and the asserting tests must match exactly; author them together rather than splitting the RED tests and the emitter across separate agents.
  - `expressionToValue` in `scripts/build-guide-scenarios.ts` now accepts no-interpolation backtick template-literal props and rejects `${...}`; multi-line guide code/displayCode props rely on this, so keep guide TS snippets interpolation-free.
  - `RunProps` is published SDK surface (JSON_SCHEMA registry + docs export + schema-snapshot + API_COMPATIBILITY list). Making it a `Schema.Union` with `.annotations({ identifier, title, description })` preserves the `$def` title/description in the regenerated JSON Schema (no metadata loss), so the union does NOT hit the "filter collapses the $def" snapshot trap — only struct-level `Schema.filter` does. Changing it only needs `bun run codegen:schema-snapshot`; the export name is unchanged so no API_COMPATIBILITY edit.
---

## 2026-06-13 23:58 CDT - US-245 Review
- Reviewed library-mode `<Run runtime="library">` generator + `RunProps` union against the source acceptance criteria; no code changes required. Verified the generator emits namespaced `LandoCore`/`LandoTesting` imports only for library scenarios, executes `code` while displaying `displayCode`, rejects mixed cli/library scenarios, derives public-transcript `runtime` from run mode, and preserves MDX `@source` line ownership. Ran the regenerated library scenarios live (both pass) plus the four codegen/component-props suites (56 pass).
- Review notes (rejected): a context-mining pass flagged "incomplete ScenarioContext: variant hardcoded to `{}`" as blocking. Rejected — `ScenarioContext.variant` is pre-existing shared infrastructure typed `Record<string, never>` in `withScenarioContext`, populated identically for CLI and library scenarios; this story does not touch `scenario-context.ts`. The `variant` field IS present on the context library runs receive (criterion satisfied); populating axis values is a separate cross-runtime infra change, out of scope here.
- Verification: typecheck, lint, lint:guides, check:guide-coverage passed; full `bun test` shows only the pre-existing, branch-independent `core schema re-exports the canonical deprecation notice` dual-module-instance failure (reproduces identically on origin/main; unrelated to this story).
---
