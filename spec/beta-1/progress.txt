# Ralph Progress Log — Lando v4 Beta 1 (contract-completion remediation)

Branch: ralph/beta-1
Source PRDs: spec/beta-1/prd-beta-1-{01..05}-*.md (US-372..US-395, remediation) + spec/beta-1/prd-beta-1-{06..09}-*.md (US-396..US-409, agent-native feature wave)
Spec source of truth: spec/{01..19}-*.md + the originating Alpha 4 PRDs cited per story
Origin: 2026-07-02 post-Alpha-4 gap audit (see prd-beta-1-00-index.md traceability table)

## Conventions

- Append one entry per completed story/verify/babysit cycle, newest at the top of the entry list, matching the beta-1 progress.txt format.
- Every entry records: what changed, verification evidence (gates + tests + manual QA), and learnings for future iterations.
- A story that relaxes a shipped contract (US-381 FileFormat, US-388 release posture, US-392 shell default) must record the decision and the synchronized spec/PRD/snapshot updates in its entry.

## Codebase Patterns

- To surface bundled-plugin manifest data (e.g. `setup.flags`) inside a CLI command, generate an `import type`-only literal-data module (see `core/src/cli/oclif/generated/setup-plugin-flags.ts` + `scripts/build-setup-plugin-flags.ts`). NEVER import `core/src/plugins/bundled.ts` from a command module: `run.ts` → `compiled-commands.ts` imports every command, so pulling `bundled.ts` there drags all bundled plugin Layers into the compiled CLI graph — a cold-start regression.
- New codegen scripts wire in three places: `package.json` (`codegen:<id>`), `scripts/codegen.ts` (catalog, ordered before any consumer generator), and a matching drift note. A flag/metadata generator that feeds `SetupCommand.flags` must run before `oclif-manifest` since that generator serializes the command's static flags.
- The `lando uninstall` command (`core/src/cli/commands/uninstall.ts`) is a lightweight `Effect.promise` with injectable seams (`remove`, `teardownRuntimeService`, `teardownProviderMachines`); provider-specific teardown that would otherwise import a plugin lives in a core `core/src/runtime/managed-*.ts` module (no plugin import) and is injected as a seam default. Test seams are threaded through `uninstallOptionsFromInput` in `meta/uninstall.ts` via `_`-prefixed input keys.
- Uninstall executor only runs steps where `destructive && status === "owned"` and then calls `remove(step.target)`. A step whose `target` is a logical NAME rather than a filesystem path (e.g. a provider machine) MUST be intercepted with its own branch + `continue` BEFORE `remove(step.target)`, or the name is `rm -rf`'d as a relative path.
- A CLI command's own `--format` flag is cosmetic (help/validation only): `LandoCommandBase` resolves the real result format centrally via `resolveResultFormat({argv})` (accepting `text|json|table|yaml|ndjson`), STRIPS `--format` from argv before `this.parse`, then OVERWRITES `flags.format` with the resolved value. JSON is emitted centrally through `spec.resultSchema` → `encodeCommandResult` in `runWithRendererHandling`, so a per-command `render()` must be human-only and may branch on the result shape (never build JSON). The compiled `run.ts` path mirrors this: `runCompiledCommand` reads `spec.resultSchema` from the active command id, so both paths JSON-encode identically once the OCLIF spec schema is a tagged union.
- Changing an OCLIF command's `static flags` requires regenerating `core/src/cli/oclif/compiled-manifest.ts` via `bun run codegen:oclif-manifest` (it serializes each command's static flag metadata); verify with `git diff` and run `core/test/cli/oclif-manifest.test.ts` + `core/test/cli/parity/dispatch-parity.test.ts`. `core/oclif.manifest.json` is gitignored; only `compiled-manifest.ts` is tracked.

---

## 2026-07-03 21:08 CDT - US-387 Review
- Review fixed a provider-layer regression coverage gap: the `makeRuntimeProvider` `ensureRuntime` path now has focused tests proving darwin fails without a bundled machine runner instead of falling back to system Podman, and proving the managed-runtime machine command targets `${runtimeBinDir}/podman` when a bundle path is available.
- Review notes (deferred/rejected): the pre-fix audit wording in `prd.json.notes` is retained as story provenance because `passes: true` plus this progress log record the resolved state; Windows `.exe` suffix handling in `managedRuntimePodmanArgv0` remains deferred as pre-existing helper behavior covered by provider-integration Windows CI, not a US-387 review blocker.
- Verification: typecheck, lint, `check:paths-boundary`, focused runtime-path/setup-machine/ownership/ensure-runtime spot checks, and edited-file LSP diagnostics passed.
---

## 2026-07-03 16:58 CDT - US-385 Review
- Review fixed a setup-state ownership regression: rerunning `lando setup` after Lando created the managed Podman machine no longer overwrites `machine.createdByLando: true` with `false` when the already-running VM is observed on the second setup pass, so a later `lando uninstall --purge` still tears down the Lando-owned machine automatically.
- Review also tightened the `meta:uninstall` command spec environment type so scenario tests see the command as self-contained (`R = never`) instead of widened to `unknown`.
- Review notes (deferred/rejected): `--keep-data` still tearing down owned provider machines is rejected as a US-385 blocker because this story's contract is `uninstall --purge`; revisit if product semantics require VM preservation under keep-data. Windows elevation through `PrivilegeService` remains deferred because the current privilege service has no Windows elevation implementation and US-385's live provider path stays env-gated.
- Verification: typecheck, lint passed; focused provider-machine/uninstall/setup-ownership spot checks passed (30 pass, 1 env-gated live integration skip); edited TypeScript files have clean LSP diagnostics.
- **Learnings for future iterations:** Machine ownership is historical once Lando creates the reserved machine; setup reruns must preserve an existing recorded `createdByLando: true` unless/until a future machine-identity story can prove the recorded VM was replaced.
---

## 2026-07-03 16:40 CDT - US-385
- `lando uninstall --purge` now tears down the Lando-owned managed provider machine (the macOS/Windows Podman VM) automatically instead of always listing `managed-provider-machines` as `manual`. Ownership is authoritative from recorded setup state: provider-lando setup now records `machine: { name: "lando", createdByLando }` in `setup-state.json`, where `createdByLando` is true only when `podman machine inspect` returned `missing` before create (Lando created the VM) and false when it adopted a pre-existing `lando` machine. `ensureMacOSPodmanMachine`/`ensureWindowsPodmanMachine` return that signal; the field is written only on darwin/win32 and omitted on Linux (no VM).
- A new core module `core/src/runtime/managed-provider-machine.ts` (no `@lando/provider-lando` import — it only mirrors the setup-state path convention and the `podman machine rm` argv, keeping the plugin out of CLI cold-start) classifies ownership from setup-state into owned/not-owned/ambiguous/absent, and tears down owned machines via `podman machine rm --force lando` behind an injectable spawn seam. Podman exit 125 (machine not found) is treated as an idempotent no-op so a rerun after partial/complete teardown converges. Corrupt or unexpectedly-named machine records degrade to `ambiguous → manual` so uninstall never removes a machine Lando did not create.
- The uninstall command derives the `managed-provider-machines` step status from that classification (owned→auto-teardown→`completed` "removed"; not-owned→`user-owned` with `podman machine rm lando` remediation; ambiguous→`manual`; absent→`skipped`) and tears the machine down through a `teardownProviderMachines` seam, intercepted before the path-removal branch so the machine name is never `rm`'d as a path. No public schema change: existing `UninstallStepStatus`/`UninstallStepOutcome`/`UninstallResultSchema` literals already cover every state, so no snapshot regen was needed.
- Verification: typecheck, lint, and the renderer/paths/state-store/probe boundary gates passed; zero schema-snapshot drift. RED→GREEN unit + scenario spot checks (provider-lando setup ownership, core classify/teardown, uninstall wiring, end-to-end scenario through the OCLIF wrapper reading a real on-disk setup-state) all green (96 pass, 3 skip across the affected suites, existing uninstall/runtime-service suites unchanged). A new env-gated live integration test (`LANDO_TEST_PODMAN_MACHINE_TEARDOWN=1`) exercises real podman teardown idempotency and is skipped by default. Manual CLI QA drove the real source binary: `lando uninstall --dry-run` now renders the machine step as owned/user-owned/skipped from recorded setup state instead of a hardcoded manual line.
- **Learnings for future iterations:**
  - Ownership of a provider machine cannot be inferred at uninstall time (the name `lando` alone is ambiguous with a user's own machine); it must be RECORDED at setup from the pre-create `inspect === "missing"` signal. Absent/corrupt/foreign-name records degrade to manual — never guess ownership into a destructive `podman machine rm`.
  - Reuse the `managed-runtime-service.ts` idiom for provider teardown from a core command: a core `managed-*` module with injectable process/spawn seams keeps the provider plugin out of the CLI cold-start graph while still owning the podman argv, and the uninstall command injects it as a seam default (threaded through `uninstallOptionsFromInput` for tests).
  - `exactOptionalPropertyTypes` makes `Options["optionalFn"]` include `| undefined`; when spreading a conditionally-present function seam from untyped input, cast to `NonNullable<Options["optionalFn"]>`, not `Options["optionalFn"]`.
---

## 2026-07-03 14:38 CDT - US-384 Verify
- Verified the freshly rebuilt compiled binary in `lando4-dev`: `lando setup` completed with the local runtime bundle manifest, then completed again using the provider-contributed `--runtime-bundle-url`/`--runtime-bundle-sha256` flags directly; compiled setup help now exposes those contributed flags, bad setup flags still fail with exit 2, setup JSON output remains canonical, and adjacent `version`, `shellenv`, and `doctor --format json` smokes passed.
- Fixed a real compiled-binary help regression at the root cause: compiled mode's custom command help renderer printed only description/usage/aliases and ignored command flag metadata, so provider-contributed setup flags parsed correctly but did not appear in `core/dist/lando setup --help`.
- **Learnings for future iterations:** When a story changes command metadata, verify the compiled help renderer as well as the generated OCLIF manifest; compiled mode owns a lightweight help path that can drift from source OCLIF help even when parsing metadata is present.
---

## 2026-07-03 14:22 CDT - US-384 Review
- Review found no code blockers in the `setup.flags` merge. It recorded the accepted Beta 1 scope explicitly in the source PRD and PRD index: bundled provider flags are exposed through the static OCLIF/compiled setup metadata, while runtime-discovered plugin flags are collision-checked at plugin load and dynamic external-flag parsing remains deferred to a later two-phase parser story.
- Review notes (deferred/rejected): raw runtime-discovered plugin flag parsing is deferred because OCLIF's command metadata is static and loading arbitrary plugins into the cold-start command graph would violate the story's cold-start constraint; duplicate delivery of the Lando runtime-bundle flags as typed fields plus `setupFlags` is accepted to preserve the existing provider-lando setup contract while adding the generic plugin flag channel.
- Verification: typecheck, lint passed; focused setup flag merge/surface/setup/deprecation/plugin collision suites passed; setup flag and OCLIF manifest codegen produced no tracked drift; review-only PRD JSON validity check passed.
---

## 2026-07-03 13:16 CDT - US-384
- `lando setup` now merges provider-plugin `setup.flags` contributions into the `meta:setup` flag surface on both the OCLIF source path and the compiled binary path. Bundled contributions flow through a new `import type`-only generated module (`core/src/cli/oclif/generated/setup-plugin-flags.ts`, built by `scripts/build-setup-plugin-flags.ts`, ordered before the oclif-manifest generator), merged by a pure `mergeSetupPluginFlags` that converts each contribution to an OCLIF flag and rejects any name colliding with a built-in or another plugin via the tagged `SetupFlagCollisionError`.
- The Lando runtime-bundle flags (`--runtime-bundle-url`/`--runtime-bundle-sha256`) moved OUT of the hardcoded core built-ins and are now genuinely sourced from `@lando/provider-lando`'s `setup.flags` contribution (they were previously decorative in the manifest and duplicated as built-ins). `host-proxy` and the `skip-*`/`provider` flags stay core built-ins. Parsed contributed values are delivered to the selected provider's `setup()` via a new additive `ProviderSetupOptions.setupFlags` map scoped to the plugin that registers the provider; the typed `runtimeBundleUrl`/`runtimeBundleSha256` conveniences are preserved.
- Collision detection is enforced twice: at merge time (bundled contributions, at command load) and at the bootstrap manifest pass (`DeprecationPluginRegistryLive`) for runtime-discovered user/app plugins, both failing with `SetupFlagCollisionError`. Per the Oracle-validated design, the compiled parse table is NOT dynamically mutated for runtime-discovered plugins — baking bundled provider flags into the compiled manifest satisfies Beta 1 parity; dynamic external-flag parsing is a later two-phase feature.
- Verification: typecheck, lint passed; new merge/collision/scenario suites plus the setup, deprecation-bootstrap, dispatch-parity, and generated-bootstrap-layer spot checks green; full codegen produced no unexpected drift and the schema snapshot fixture is unchanged (`ProviderSetupOptions` is a plain TS interface, so the additive field is an `API_COMPATIBILITY.md` bullet only). Manual QA: `bun bin/lando.ts setup --help` renders `--runtime-bundle-url`/`--runtime-bundle-sha256` sourced from the merge.
- **Learnings for future iterations:**
  - `ReturnType<typeof Flags.string>` resolves to the `multiple: true` overload (`string[]`), which breaks under `exactOptionalPropertyTypes`. Type merged OCLIF flags as `Interfaces.BooleanFlag<string | boolean | undefined> | Interfaces.OptionFlag<string | undefined>` instead.
  - Keep the collision authority (`SetupFlagCollisionError` + `findSetupFlagCollision` + `SETUP_BUILTIN_FLAG_NAMES`) in a neutral, OCLIF-free module (`core/src/plugins/setup-flags.ts`) so both the CLI merge and the bootstrap manifest pass can import it without a layering inversion; guard the reserved-name list against drift with a test comparing it to `SetupCommand.flags` keys.
  - The bootstrap "read every discovered manifest's `setup.flags`" hook already exists in `DeprecationPluginRegistryLive`; failing it with the tagged error is the minimal "collision fails at plugin load" path for runtime-discovered plugins without touching the high-blast-radius `PluginRegistry.list` error contract.
---

## 2026-07-03 07:11 CDT - US-380 Review
- Review fixed the missing public boundary coverage for the new paths member: `@lando/core/paths` package export, packed-package import, import-boundary, and live `PathsService` runtime tests now assert `managedFileLedger(appId)` resolves to the expected ledger path. Review also corrected the managed-file service header comment so it names the current primitive.
- Review notes (deferred/rejected): strict `PathsService` DI usage in the managed-file backend is not required here because repo rules sanction the pure `makeLandoPaths` primitive and the backend needs a per-call injected ledger root; public `appId` sanitization in path builders remains a pre-existing trusted-caller pattern shared with `appPluginsDir`/`appSnapshotsDir`, while the managed-file runtime still uses sanitized `deriveAppId` output.
- Verification: typecheck, lint, check:paths-boundary, check:managed-file-boundary, check:redaction-boundary passed; focused paths/config roots, path export/runtime/import-boundary, and managed-file service spot checks passed.
---

## 2026-07-03 07:01 CDT - US-380
- The managed-file ledger path is now a first-class member of the paths surface: `LandoPaths` (SDK) and the pure `makeLandoPaths` peer gain `managedFileLedger(appId)` returning `<userDataRoot>/managed-files/<appId>/ledger.json`, built with the platform-aware `joinFor` so win32/darwin/linux separators are honored (matching spec §7.5.1, which already documented the member).
- `core/src/config/roots.ts` no longer hand-rolls the `managed-files/`+`ledger.json` join; both `managedFileLedger` and `managedFilesRoot` (now `dirname` of the ledger file) delegate to the primitive. `core/src/managed-file/service.ts` migrated off the roots helper and resolves the ledger location through `makeLandoPaths({ userDataRoot }).managedFileLedger(appId)`, splitting it into the StateStore bucket's `root` dir + `key` filename via `dirname`/`basename` so the segment/filename live only in the primitive.
- Verification: typecheck, lint, check:paths-boundary, check:managed-file-boundary, check:redaction-boundary passed; schema snapshot regenerated with zero drift. Cross-platform matrix tests (linux/win32/darwin + overridden roots) plus config-paths units, managed-file service/events/contract, and paths-export/paths-runtime/import-boundary spot checks green. Manual QA drove the real runtime: the pure primitive, the roots delegation, and the live Effect `PathsService` tag all produced the expected ledger path (linux forward-slash + win32 backslash).
- **Learnings for future iterations:**
  - `LandoPaths` is a plain TS interface, not a `Schema.Struct` in the public schema registry, so adding a member is a pure additive service-field change: interface + `makeLandoPaths` + an `API_COMPATIBILITY.md` "Additive Beta service fields" bullet. `bun run codegen:schema-snapshot` produces no diff — run it to confirm zero drift, do not expect a snapshot change.
  - The StateStore ledger bucket needs a directory `root` + filename `key`, but the primitive exposes only the full ledger file path; derive `dir = dirname(file)` / `key = basename(file)` (neither is flagged by `check:paths-boundary`, which only catches `join(<root>, "plugins"|"scratch"|"bin")` literals).
  - `check:paths-boundary` does NOT yet flag the `managed-files` segment, so the delegation is a discipline choice (spec §7.5.1) rather than a gate requirement; keep new root-relative joins inside `makeLandoPaths` regardless.
---

## 2026-07-03 06:10 CDT - US-379 Review
- Review fixed a managed-file event redaction parity gap: `ManagedFileServiceLive` now passes `sourceEnv` into both the canonical `RedactionService` branch and the standalone fallback, so secret-bearing env values outside the `LANDO_SECRET_` store prefix are exact-value masked before event publication.
- Review notes (deferred/rejected): resolving the managed-file redactor at layer construction is accepted for current production contracts because `SecretStoreLive` is env-backed with no runtime registration API; if a mutable production secret store is introduced later, the event seam should resolve the redactor per operation.
- Verification: typecheck, lint, check:redaction-boundary, check:managed-file-boundary passed; focused managed-file env/event, service, contract, generated-bootstrap-layer, redaction, and redaction-boundary spot checks passed.
---

## 2026-07-03 06:05 CDT - US-379
- Managed-file lifecycle events now redact through the canonical `RedactionService` instead of a local empty `createSecretRedactor([])`. `ManagedFileServiceLive` resolves `RedactionService` optionally and builds a `secrets`-profile redactor (`forProfile("secrets")`), falling back to `createStandaloneRedactor("secrets")` when the service is absent so payloads are never retained raw; `makeLiveManagedFileEvents` now takes the resolved `redactText` seam. Every free-string payload field (`path`, `owner`, `summary`) it publishes is masked by the registry-backed redactor, so a value registered in the `SecretStore` can no longer leak through a managed-file event.
- The `minimal` bootstrap layer now provides `redactionLive` directly to the `ManagedFileServiceLive` layer (via the `build-bootstrap-layers.ts` generator + regenerated `minimal.ts`), because redaction was previously only an internal dep of `EventService` and thus invisible to the managed-file layer's `serviceOption(RedactionService)` resolution.
- Verification: typecheck, lint, check:redaction-boundary passed; new events-test proves a `SecretStore`-registered secret placed in an event `owner` field is masked to `[redacted]` (RED before the wiring, GREEN after), adjacent managed-file service/contract/events, runtime bootstrap-layer, redaction, and event-service spot checks green; generated-layer drift limited to the intended `minimal.ts` line.
- **Learnings for future iterations:**
  - The `check:redaction-boundary` gate is a pattern scanner over all of `core/src`+`plugins` (sentinel strings + ad-hoc secret regexes); it has no consumer inventory, so a new redaction consumer is covered automatically — no gate edit needed.
  - `EventServiceLive` resolves `RedactionService` via `serviceOption` (optional), so it builds without it; but that does NOT make `RedactionService` visible to a sibling layer that also `serviceOption`s it. To wire redaction into `ManagedFileServiceLive`, provide `redactionLive` to that layer explicitly (`Layer.mergeAll(eventServiceLive, redactionLive)`), not just through `EventService`.
  - Reuse the healthcheck/scanner redaction idiom for event seams: `Effect.serviceOption(RedactionService)` + `forProfile("secrets")` with a `createStandaloneRedactor("secrets")` fallback keeps the layer's `R = never` while still redacting when present.
---

## 2026-07-03 05:09 CDT - US-378 Review
- Review fixed stale Mutagen setup guide skip reasons that still pointed at the deleted plugin-local `download.test.ts`; the guide now cites the live `provision.test.ts` and shared downloader acceptance coverage that prove the tool-provisioning path.
- Review notes (deferred/rejected): deleted-path hits in historical progress/PRD text are intentionally retained as audit history and source story context, not active code or guide references.
- Verification: typecheck, lint passed; relevant Mutagen provision/import-boundary/contract/downloader acceptance and focused guide checks passed.
---

## 2026-07-03 - US-378
- Decision recorded: the bespoke Mutagen downloader is **redundant**. The shipped provisioning path is `provisionMutagen` in `plugins/file-sync-mutagen/src/provision.ts`, which delegates to the shared `provisionTool`/`resolveHostKey` helper (`@lando/sdk/tool-provisioning`) over the SDK `Downloader` service and is the only path the plugin's `engine` Layer and `index.ts` wire. The orphaned `plugins/file-sync-mutagen/src/download.ts` (556 LOC) and `plugins/file-sync-mutagen/test/download.test.ts` (837 LOC) duplicated fetch/checksum/install logic the tool-provisioning helper already owns, so they served no purpose `provision.ts` does not.
- No new code was needed: both files were already deleted when Mutagen migrated onto the tool-provisioning helper (PR #493). This story verified and closed the working-tree-discipline gap: repo-wide sweep confirms nothing imports the deleted `download.ts`/`./download` (remaining "download" hits are the unrelated SDK `Downloader` service, download *events*, and the GitHub `releases/download` URL literal), and `git status` is clean of these paths with no untracked product files.
- Verification: typecheck, lint passed; Mutagen provision/import-boundary/contract spot checks green (13 tests, 40 assertions) proving the `provisionMutagen` path stands alone. No CLI surface change.
- **Learnings for future iterations:**
  - When an audit-style story flags orphaned/untracked files, first check `git log --all --follow --name-status` on the path: the redundant Mutagen downloader had already been deleted in the tool-provisioning migration (#493), so the story was a verify-and-record close, not a re-deletion.
  - The mutagen plugin's real download path is `provisionMutagen` -> `provisionTool` (SDK `@lando/sdk/tool-provisioning`) over the injected `Downloader` service; there is no plugin-local downloader module, and there should not be one.
---

## 2026-07-03 03:57 CDT - US-377 Review
- Review confirmed US-377 satisfies the source PRD and tightened the runtime test so `waitForEvent` proves `filter` pass-through with a same-name event filtered out before the accepted event resolves. Review also corrected a strict branded-type assertion in the new test.
- Review notes (deferred/rejected): live-stream secret-redaction assertions remain rejected because `waitForEvent` intentionally has parity with `EventService.waitFor`, while `EventServiceLive` redacts only the history buffer returned by `query`.
- Verification: typecheck, lint passed; focused `wait-for-event` story test and adjacent `events`/`event-service` spot checks passed; edited TypeScript file has clean LSP diagnostics.
---

## 2026-07-03 03:49 CDT - US-377
- `waitForEvent` from `@lando/core/testing` now has real runtime behavior coverage instead of only an export-existence assertion. New `core/test/testing/wait-for-event.test.ts` proves the helper delegates to `EventService.waitFor`: it resolves the first matching event as the exact typed payload (honoring `filter`), returns byte-identical results to a direct `EventService.waitFor` call (parity), fails with the `EventError` `reason: "timeout"` contract when an explicit deadline elapses (driven deterministically through `TestClock`), and — unlike `expectEvent` — waits indefinitely when no timeout is supplied (still pending after a one-hour virtual clock advance).
- Verification: typecheck, lint passed; new suite green (4 tests) with a RED-integrity spot check confirming the payload assertions actually bite; adjacent `expectEvent` and `EventServiceLive` suites still green (7 tests). No CLI surface — the real-runtime surface is the test runner exercising `EventServiceLive`.
- **Learnings for future iterations:**
  - `waitForEvent`/`EventService.waitFor` return the raw live-stream payload; redaction applies only to the history buffer (`query`), so the "typed, redacted payload" contract is satisfied by parity (the bus carries redacted-at-source events) — do not assert live secret-redaction on the resolved event, it would be false.
  - The established timeout-test idiom for these helpers is `Effect.exit`+`Effect.fork` a waiter, `TestClock.adjust` past the deadline, `Fiber.join`, then `Cause.failureOption` on the failure exit; provide both `EventServiceLive` and `TestContext.TestContext`.
  - To prove indefinite waiting under a virtual clock: fork the waiter, `TestClock.adjust` a long span with nothing published, assert `Fiber.poll` is `Option.none`, then interrupt to clean up.
---

## 2026-07-03 02:39 CDT - US-376 Review
- Review confirmed the build commit satisfies US-376: doctor now wires the real runProbe-backed healthcheck/scanner runners, setup readiness was already on `runProbe`, and downloader had no retry loop to migrate. Review fixed strict test typing in the doctor fix suite by narrowing the subsystem classification assertion and using a typed `ProxyError` in the redaction fixture.
- Review notes (deferred/rejected): identity-only readiness for healthcheck/scanner is accepted because `subsystemDoctor` reports implementation wiring by service id and never calls `run()`/`scan()`; the commit title's downloader/setup wording is accepted because the progress entry documents those as audited no-ops; the HttpClient retry-surface drift noted by context mining is outside US-376.
- Verification: typecheck, lint, check:probe-boundary passed; focused doctor fix suite passed (14 tests), adjacent doctor/healthcheck/scanner/provider-readiness spot checks passed (51 tests); edited TypeScript file has clean LSP diagnostics.
---

## 2026-07-03 - US-376
- `lando doctor`'s subsystem diagnostics now wire the real `runProbe`-backed runners: `DefaultSubsystemDoctorLayer` (`core/src/cli/commands/doctor-subsystems.ts`) drops the `HealthcheckRunnerUnavailableLive`/`UrlScannerUnavailableLive` stubs and instead builds `HealthcheckRunnerLive` (with the bootstrap placeholder `RuntimeProvider`) and `UrlScannerLive` (placeholder `RuntimeProvider` + `HttpClientLive`), staying self-contained. Doctor now reports `healthcheck`→`provider-exec` and `scanner`→`http-probe` as ready/pass instead of degraded "run lando setup" — closing the US-374/US-375 "doctor reports the real runner, not the unavailable stub" criterion. The `Unavailable` stubs remain exported for explicit degraded-mode contexts.
- Audit of the other US-317 consumers confirmed nothing left to migrate: the doctor path has no hand-rolled retry/timeout loop (its retry-to-verdict logic lives in the now-real healthcheck/scanner runners, both already on `runProbe`); every `Downloader.download` call site is single-attempt by design with no retry loop to map; and `lando setup` provider/socket readiness already runs through `runProbe` (`plugins/provider-lando/src/ensure-runtime.ts` `verifyRuntimeReachable`). The `check:probe-boundary` allowlist is already minimal (`core/src/state/lock.ts` only), and its test already proves a reintroduced `Effect.retry`/`Schedule` loop fails the gate.
- Verification: typecheck, lint, `check:probe-boundary` passed; focused doctor suite (72 pass incl. new "wires the real runProbe-backed runners" assertion) plus adjacent scanner/healthcheck/generated-bootstrap-layers/probe-boundary spot checks green; the `meta-doctor.subsystems.ndjson` fixture was regenerated. Manual QA: ran `lando doctor --format ndjson` and confirmed `healthcheck`/`scanner` report `provider-exec`/`http-probe` ready while `proxy` stays `unavailable`.
- **Learnings for future iterations:**
  - `subsystemDoctor` reads only a subsystem's service `.id` (ready iff not `unavailable`/`disabled`), so a real Live layer can be wired for identity-only reporting using the bootstrap placeholder `runtimeProviderService` without ever exercising `run()`/`scan()` — keeping `DefaultSubsystemDoctorLayer` requirement-free for its tests.
  - Flipping a subsystem from stub→real changes the committed `meta-doctor.subsystems.ndjson` fixture and the `warned` counts in `doctor-report`/`doctor-subsystems-fix` tests; distinguish a subsystem's static `recovery` class (unchanged) from its runtime `status` (now pass) when updating those tests.
---

## 2026-07-03 02:10 CDT - Babysit
- PR: #505 https://github.com/AaronFeledy/core4/pull/505
- CI outcome: pass
- Review feedback: fixed guide-drift by adding the allowed PR-body `Guide-Coverage-Skip:` reason and rerunning the guide-scenarios matrix green; Bugbot inline comments were empty.
- Merge: merged (squash)
---

## 2026-07-03 01:04 CDT - US-375 Verify
- Verified the freshly rebuilt compiled binary in `lando4-dev`: staged a local runtime bundle, ran `core/dist/lando setup --yes --provider=lando --skip-install-ca --skip-shell-integration --skip-file-sync` successfully, then exercised `UrlScannerLive` through compiled `lando bun` against a real local HTTP server using `HttpClientLive`; scanner returned one green reachable endpoint with HTTP 200.
- Adjacent compiled smokes passed for `version`, `doctor --format json`, and `global:stop --help`; `global:stop --yes` cleaned partial global state and no US-375 temp app dirs or containers remained.
- Full throwaway-app `lando start` scanner path could not run in this sandbox because `global:start` requires Traefik to bind `127.0.0.1:80`, which is already in use in `lando4-dev`; after pre-pulling `mailpit`, `traefik`, and `node:lts`, the blocker remained an environmental port bind conflict rather than a scanner failure.
- **Learnings for future iterations:** If a verify story needs real `lando start` in `lando4-dev`, check/free port 80 before `global:start`; provider-lando also does not auto-pull global/app images, so pre-pull required images into the managed Podman store first.
---

## 2026-07-03 00:42 CDT - US-375 Review
- Review fixed the default scanner wiring so provider-and-above bootstrap layers now provide `UrlScannerLive` through the generated bootstrap source, backed by a generated-layer regression test. Review also corrected stale scanner API/guide guidance that still described the live scanner as unavailable, and split the oversized scanner test module into focused behavior/probe/live-layer files while preserving coverage.
- Review notes (deferred/rejected): doctor-subsystems still using `UrlScannerUnavailableLive` is deferred to US-376, whose acceptance criteria explicitly migrate doctor wiring; redirect-follow SSRF risk is non-blocking because redirects default to manual and user scanner config is not exposed yet, but future config work should validate redirected hops before enabling bounded follow behavior.
- Verification: typecheck, lint, check:probe-boundary, check:redaction-boundary passed; focused scanner/probe/sdk/doctor/generated-bootstrap spot checks passed (54 tests, 286 assertions); focused scanner guide pass, guide lint, and guide-drift gate passed/skipped cleanly; edited TypeScript files have clean LSP diagnostics and split scanner test files are under the 250 pure-LOC ceiling.
---

## 2026-07-03 - US-375
- The scanner subsystem now ships a real default: `core/src/subsystems/scanner/live.ts` exports `makeUrlScanner` (id `http-probe`), `UrlScannerLive` (Layer requiring `RuntimeProvider` + `HttpClient`), and `UrlScannerDefaultLayer`. Scanning derives host-facing `http(s)://localhost:<port><path>` targets from the provider's host-reachable runtime endpoints (stopped services and non-HTTP/port-less endpoints skipped) and drives each URL through `runProbe`: `retry` -> `maxAttempts`, `delaySeconds` -> fixed `delay`, `timeoutSeconds` -> per-attempt cap via `Effect.timeoutTo` plus `HttpRequest.timeoutMs`. Verdicts are ProbeOutcome-shaped: 2xx-or-`okCodes` -> green, any other HTTP response -> yellow (`detail: "HTTP <code>"`), transport failure/timeout -> red; all outbound requests go through the `HttpClient` egress chokepoint (`callerId: "url-scanner"`, `maxRedirects` 0 -> `redirect: "manual"`, >0 -> `"follow"`). `detectCollisions` reports host ports claimed by two or more distinct apps. Probe/attempt machinery lives in `scan-target.ts` to respect the module size ceiling.
- SDK additive change: `ScanEndpoint` gains optional `outcome` (`ProbeOutcome`) and `detail` (redacted structured detail) so scanners can report green/yellow/red without breaking the frozen shape; documented in `sdk/API_COMPATIBILITY.md` Compatibility notes. All endpoint detail and `ScannerError` messages are redacted through `RedactionService` (`forProfile("secrets")`, standalone-redactor fallback when absent) before leaving the scanner. The `unavailable` stub in `api.ts` remains the explicit degraded-mode layer; doctor wiring intentionally stays on the stub until the doctor migration story lands.
- Verification: typecheck, lint, check:probe-boundary, check:redaction-boundary passed; focused suite (14 TestClock tests: exact 20s fixed-backoff timing, waits-only-between-attempts, per-attempt timeout detail, okCodes classification, redaction on/off, collisions, SDK `runScannerContract`, layer wiring incl. provider-failure redaction) green; probe, sdk url-scanner contract, sdk backward-compat, doctor-subsystems, healthcheck, and plugin-abstraction-coverage spot checks green; manual QA drove `UrlScannerLive` end-to-end through the real `HttpClientLive` against a live `Bun.serve` server (green 200, yellow 503 with `HTTP 503` detail, red connection-refused with redacted transport detail, cross-app collision report).
- **Learnings for future iterations:**
  - The scanner mirrors the healthcheck-runner probe idiom exactly (never-failing attempts via `Effect.either` + `Effect.timeoutTo`, `AttemptStatus` in a `Ref`, `Effect.serviceOption(RedactionService)` with standalone fallback); reuse it for the US-376 doctor/downloader/setup migrations.
  - `HttpClientShape.request` requires `Scope`; wrap per-attempt calls in `Effect.scoped` to keep the service shape's `R = never`.
  - `ServiceRuntimeInfo.endpoints` are host-reachable ports (`lando info` renders them as `localhost:<port>`), so they are the right source for both scan URLs and cross-app port-collision claims.
  - `HttpRequest` has no redirect-count cap; `maxRedirects` maps binary (0 -> `"manual"`, >0 -> `"follow"`) — a follow-up config schema should keep that documented simplification.
  - Additive optional fields on SDK service-layer interfaces (not `Schema.Struct`s) need only an API_COMPATIBILITY Compatibility-notes bullet — no schema snapshot regen, no backward-compat fixture change.
---

## 2026-07-02 23:59 CDT - Babysit
- PR: #504 https://github.com/AaronFeledy/core4/pull/504
- CI outcome: pass
- Review feedback: fixed guide coverage drift for the runProbe-backed HealthcheckRunner documentation scenario
- Merge: merged (squash)
---

## 2026-07-02 23:02 CDT - US-374 Verify
- Verified the freshly rebuilt compiled binary in `lando4-dev`: staged a local runtime bundle, ran `core/dist/lando setup --yes --provider=lando --skip-install-ca --skip-shell-integration --skip-file-sync`, drove the provider-exec healthcheck runner through compiled `lando bun` with retry success, provider failure redaction, and timeout redaction, and smoke-tested adjacent compiled `version`, `doctor --help`, and live `doctor --format json` provider diagnostics.
- Fixed a real redaction regression found by the compiled scenario: when `RedactionService` is absent, standalone redaction now treats common secret-bearing env keys as exact redaction values, so provider healthcheck status cannot leak env-sourced secrets.
- **Learnings for future iterations:** The setup verify path must stage `dist/cache/runtime-bundle` and export `LANDO_RUNTIME_BUNDLE_MANIFEST`; a fresh `core` build can remove the prior local bundle and otherwise force the placeholder release manifest download path.
---

## 2026-07-02 - US-374
- The healthcheck subsystem now ships a real default runner: `core/src/subsystems/healthcheck/live.ts` exports `makeHealthcheckRunner` (provider-exec, id `provider-exec`), `HealthcheckRunnerLive` (Layer requiring `RuntimeProvider`), and `HealthcheckRunnerDefaultLayer`. Command healthchecks run through `RuntimeProvider.exec` driven by `runProbe`: `retries` -> `maxAttempts`, `intervalSeconds` -> fixed `delay`, `timeoutSeconds` -> per-attempt cap via `Effect.timeoutTo` (probe-boundary-safe), `startPeriodSeconds` -> grace sleep before the first attempt. Green resolves healthy; exhausted non-green resolves `healthy: false` with an exact `lastStatus` (`ok`/`exit <code>`/provider message); a final-attempt timeout fails `HealthcheckTimeoutError` whose message and `probe` payload embed the redacted command. `kind: none` short-circuits healthy/skipped; `http`/`tcp`/missing command fail `HealthcheckError`.
- `lastStatus`, timeout messages, and the `probe` payload are redacted through `RedactionService` (`forProfile("secrets")`, identity fallback when absent) before leaving the runner. The unavailable stub in `api.ts` remains as the explicit degraded-mode layer; doctor wiring intentionally stays on the stub until the doctor migration story lands.
- Verification: typecheck, lint, check:probe-boundary, check:renderer-boundary, check:redaction-boundary passed; focused suite (12 TestClock tests: attempt counts, 20s fixed-backoff timing, per-attempt timeout, startPeriod grace, redaction on/off, SDK `runHealthcheckContract`, layer wiring) green; doctor-subsystems, probe, healthcheck-contract, and plugin-abstraction-coverage spot checks green; manual QA drove `HealthcheckRunnerLive` end-to-end with a scripted provider under the real clock (green-on-3rd-attempt at ~2s wall, skip path, provider-failure path).
- **Learnings for future iterations:**
  - `RetryPolicy.timeout` is an OVERALL deadline across attempts, not per-attempt. For docker-shaped healthcheck plans (`timeoutSeconds` = per-attempt), cap the attempt effect itself with `Effect.timeoutTo` — allowed by check:probe-boundary — and leave `policy.timeout` unset; otherwise the shipped defaults (interval 10 > timeout 5) degenerate to one attempt. US-375/US-376 scanner/doctor migrations should reuse this pattern where config is per-attempt shaped.
  - Make `runProbe` attempts NEVER fail: wrap exec in `Effect.either` + `timeoutTo`, record a discriminated `AttemptStatus` in a `Ref`, and classify sentinels. That keeps the ProbeResult/verdict mapping and error construction in one place after the probe resolves.
  - `Effect.serviceOption(RedactionService)` inside the run effect keeps `HealthcheckRunnerShape`'s R = never while still redacting when the service is present (shell-runner idiom).
  - `TestRuntimeProvider` from `@lando/sdk/test` + spread-override (`{ ...TestRuntimeProvider, exec }`) is the cheap way to fake a provider for layer tests.
  - Codegen'd lint is strict about import sorting/format on new files: run `bun x biome check --write <files>` before `bun run lint` (`bunx` is not on PATH; use `bun x`).
---

## 2026-07-02 19:17 CDT - US-373 Review
- Review fixed a StateStore migration regression where read-only managed-file ledger access (`status`/`plan`) created the `managed-files` ledger root even when no ledger existed; read-only access now returns an empty ledger without creating directories, while mutation paths still create the root before opening the StateStore bucket.
- Review notes (deferred/rejected): the missing managed-file-level concurrent mutation test is non-blocking because the migrated code uses the existing `withAdvisoryLock` primitive and the state lock suite covers the stale-lock behavior moved from `json-bucket`.
- Verification: typecheck, lint, check:state-store-boundary, check:probe-boundary passed; focused spot checks passed for managed-file service/contract/events, state lock/contract/plugin-state, StateStore unit coverage, and state-store boundary script.
---

## 2026-07-02 - US-373
- The managed-file ledger now persists through the single `StateStore` implementation: `makeDiskBackend` opens a StateStore bucket at the ledger path and serializes effectful read-modify-write through `withAdvisoryLock` from `core/src/state/lock.ts`, so envelope/lock/quarantine semantics exist in exactly one place. `core/src/state-store/json-bucket.ts` (the parallel versioned-envelope + advisory-lock + quarantine implementation) is deleted outright, and its `check:probe-boundary` carve-out is removed so only the documented `core/src/state/lock.ts` loop remains allowlisted.
- Ledger format compatibility is proven, not assumed: the old bucket and StateStore's `json` codec emit byte-identical `{ version, data }` envelopes, and a new characterization test hand-writes the legacy serializer's exact bytes and reads them back through the new path. The old bucket's dead-pid lock-takeover coverage was ported to a new `core/test/state/lock.test.ts` against `withAdvisoryLock` so retiring the module dropped no behavior coverage.
- Verification: typecheck, lint, check:state-store-boundary, check:probe-boundary passed; spot checks green (managed-file service/contract/events, state contract/lock/plugin-state, state-store atomic, boundary-script tests, cli init); manual QA drove the real disk surface end-to-end (apply -> ledger bytes -> legacy-format overwrite -> status -> locked remove) with zero `.tmp-`/`.lock`/`.corrupt-` leftovers.
- **Learnings for future iterations:**
  - `resolveStatePath` rejects a `{ path }` StateStore root that does not exist yet: containment realpath-checks the deepest EXISTING ancestor, which sits above a missing root. `mkdir -p` the root before `stateStore.open` when pinning an explicit directory.
  - `StateBucket` has no `peek`; a side-effect-free read is the same spec opened with `onCorrupt: "discard"` (recovers to `default` without quarantining).
  - `StateBucket.modify` takes a PURE function; effectful read-modify-write composes as `withAdvisoryLock(bucket.path, op, bucket.get -> f -> bucket.set)` over a `lock: "none"` bucket.
  - The retired json-bucket envelope and StateStore's `json` codec are byte-identical (`JSON.stringify({version, data}, null, 2) + "\n"`), so no data migration was needed - pin that with a literal-bytes characterization test whenever swapping serializers.
---

## 2026-07-02 - US-372
- Durable atomic writes now flush to disk before publishing: `writeFileAtomicScoped` (state store / managed-file seam) and `writeFileAtomicViaRename` (cache seam) both fsync the temp file via a file handle before the rename, so power loss can never leave a torn live file. The cwd-app-map cache no longer writes with raw `writeFile`; both its write and delete-rewrite paths route through the atomic cache helper.
- Both helpers gained an injectable `syncFile` seam; unit tests prove syscall ordering (sync fires while the live file does not yet exist, rename after) and that a flush failure rejects, skips the rename, and leaves no temp orphan.
- Verification: typecheck, lint, check:state-store-boundary passed; spot checks green (cache atomic, state-store atomic, binary-encoding, services/cache, json-bucket, state contract, plugin-state, managed-file service, unit state-store, apps-list scenario); manual QA exercised the real default fsync path via writeCwdAppMapEntry/readCwdAppMapEntry/deleteCwdAppMapEntry round-trip with no `.tmp-` leftovers.
- **Learnings for future iterations:**
  - `handle.writeFile` + `handle.sync()` on an `open(path, "w")` FileHandle is the pattern for fsync-before-rename; plain `fs/promises.writeFile` gives no flush guarantee.
  - Injectable `syncFile`/`renameFile` options on the atomic helpers are the test seam for syscall-ordering proofs — no fs module mocking needed.
  - `check-state-store-boundary.ts` carve-outs already cover both atomic helpers; routing callers through them (instead of raw writeFile) is what keeps new files off the offender list.
  - When editing `spec/beta-1/prd.json` with Python, use `json.dumps(..., ensure_ascii=False)` — the file contains literal em dashes and § characters.
---
## [2026-07-02 21:26:27 CDT] - Babysit
- PR: #503 https://github.com/AaronFeledy/core4/pull/503
- CI outcome: pass
- Review feedback: fixed (triage added retrying, fully-qualified CI provider image pre-pulls after linux-x64 provider integration hit registry pull failure)
- Merge: merged (squash)
---

## 2026-07-02 22:44 CDT - US-374 Review
- Review fixed the healthcheck runner's absent-RedactionService path so provider/timeout details use the canonical standalone secrets redactor instead of an identity fallback; tests now prove fallback redacts secret-shaped provider output. Review also split reusable healthcheck test fixtures out of the oversized focused test module while preserving the same behavior coverage.
- Review notes (deferred/rejected): `RetryPolicy.timeout` mapping was rejected because `HealthcheckPlan.timeoutSeconds` is the SDK schema's per-attempt timeout and wiring it as `RetryPolicy.timeout` would turn the shipped default interval/timeout combination into an overall deadline; doctor subsystem wiring and stale healthcheck guide prose are deferred to the US-376/guide-drift owners rather than folded into this story.
- Verification: typecheck, lint, check:probe-boundary, check:redaction-boundary passed; focused healthcheck runner suite passed (12 tests, 47 assertions); adjacent probe suite passed (21 tests, 71 assertions); changed TypeScript files have clean LSP diagnostics and each remains under the 250 pure-LOC ceiling.
- **Learnings for future iterations:** When a service intentionally keeps `R = never` by resolving `RedactionService` optionally, the absent-service branch must use `createStandaloneRedactor("secrets", ...)`, not identity redaction.
---

## [2026-07-03 08:25:16 CDT] - Babysit
- PR: #510 https://github.com/AaronFeledy/core4/pull/510
- CI outcome: pass
- Review feedback: fixed (added Guide-Coverage-Skip PR metadata for additive PathsService.managedFileLedger helper; no code changes)
- Merge: merged (squash)
---

## 2026-07-03 08:37 CDT - US-381
- Decision recorded: **ADOPT** `javascript`/`typescript` into the `FileFormat` contract, not trim. The introducing commit (#416, "migrate recipe scaffolding") deliberately added both values with real, distinct behavior: the codec encodes/decodes them verbatim (like `text`) but the marker layer gives them a `//` ownership-comment prefix instead of `#`, so a scaffolded `.js`/`.ts` source file stays syntactically valid. They are actively used by shipped consumers — `inferRecipeScaffoldFormat` in `lando init` maps `.js/.cjs/.mjs`→`javascript` and `.ts/.cts/.mts`→`typescript`, and the bundled `node-postgres` (server.js) and `node-ts` (.lando.ts) recipes emit code files through that path. Trimming would break init scaffolding and corrupt those files with `#` markers, so trim was rejected.
- Reconciled the only remaining drift, which was documentation-only: the SDK schema, committed schema snapshot, `dist/schemas/managed-file.json`, and `sdk/API_COMPATIBILITY.md` already carried the adopted 9-value surface, but the frozen contract text still listed 7. Updated the `FileFormat` enumeration in `prd-alpha-4-18-managed-files.md` and the normative §10.13.1 `ManagedFile` model comment in `spec/11-subsystems.md` to the 9-value enum (with a note that js/ts are verbatim + `//` marker). PRD text, spec, schema, snapshot, and API_COMPATIBILITY now agree with no silent drift.
- Added a focused marker unit test (`core/test/unit/managed-file-marker.test.ts`): the marker module previously had no direct unit coverage. It pins `commentPrefix` for every format and proves the js/ts `//` ownership round-trip (compose → detect → strip-back-to-body), locking the marker contract the adopt decision preserves.
- Verification: typecheck, lint passed; `codegen:schema-snapshot` regenerated with zero drift (snapshot/dist/docs already at 9 values). Test spot checks green — new marker suite + managed-file codecs + managed-file service (86) and the js/ts-scaffolding init suites `init.canonical-recipes`/`init.landofile-ts` (29). Manual QA drove the real codec+marker surface via a throwaway driver: both formats encode/decode verbatim, compose a `//` marker line, detect it, and strip back to the raw body.
- **Learnings for future iterations:**
  - When an audit flags a "schema ships N, contract says M" mismatch, check the introducing commit and whether the extra values have distinct behavior (codec branch, marker prefix) and real consumers before choosing trim vs adopt — here the schema/snapshot/API_COMPATIBILITY were already adopted and only the two normative enum enumerations (PRD acceptance-criteria list + §10.13.1 code-comment) were stale.
  - `bun run codegen:schema-snapshot` refreshes `sdk/test/fixtures/schema-snapshot.json`, `dist/schemas`, and `docs/reference/schemas` together; for an already-consistent enum it produces zero drift — run it to confirm agreement, don't expect a diff.
  - `commentPrefix(format)` (`core/src/managed-file/marker.ts`) is the single source of per-format ownership-comment syntax: `//` for javascript/typescript, `;` for ini, `null` for json (ledger/`x-lando-generated` key), `#` for everything else.
---

## 2026-07-03 08:47 CDT - US-381 Review
- Review fixed the new marker test's `test.each` rows so TypeScript/LSP diagnostics understand the javascript/typescript parameterized cases while preserving the same marker behavior coverage.
- Review notes (deferred/rejected): archival `spec/alpha-4/prd.json` still mirrors the old 7-value Alpha PRD text, but it is not a named normative US-381 surface and no repo script consumes it; duplicate marker-prefix sentinel test is intentional as a regression pin for the js/ts corruption risk; marker-id control-character hardening is pre-existing trusted-plugin-surface work, not a blocker for this contract reconciliation.
- Verification: typecheck, lint passed; focused marker, managed-file codec, and managed-file service spot checks passed; all five review-work lanes passed with no blocking findings.
---

## 2026-07-03 10:20 CDT - US-382
- The bundled `@lando/renderer-lando` plugin now OWNS the default `lando` TTY renderer instead of being a label over a `Layer.empty` placeholder. The plugin's `renderer` export is a real `RendererContribution` (id `lando`, `makeService`, `makeEventConsumer`), composed in the new `plugins/renderer-lando/src/renderer-runtime.ts` from the renderer implementation that moved into the plugin: the task-tree painter (`task-tree-tail.ts`), keybinding controller (`keybindings.ts`), and line formatters (`format.ts`). Core keeps those three module paths as thin re-export shims (`export * from "@lando/renderer-lando/<name>"` via new plugin subpath exports) so core's plain/json/verbose renderer modes and the ~30 renderer test files import them with zero churn.
- `core/src/cli/renderer/bundled-renderers.ts` no longer constructs the renderer from parts (`factory.make(coreLandoRendererPrimitives)` and the `rendererFactories`/`coreLandoRendererPrimitives` DI seam are deleted). It now RESOLVES the plugin's finished contribution by identity — `landoRenderer === plugin.renderer` — which is the "resolution not construction" proof, structurally impossible under the old factory path. Fallback selection (non-TTY plain, `--renderer=json`) and renderer selection order are unchanged; `renderer-boundary.ts`/`runtime.ts` still own the plain/json/verbose modes.
- Contribution-shape change wired through codegen (AC5): the plugin's `renderer` export is a `RendererContribution`, not a Layer, so `scripts/build-bundled-plugins.ts` now emits `layer: Layer.empty` (value import) for renderer plugins in the generated `core/src/plugins/bundled.ts`, and `core/test/plugins/bundled.test.ts` expects `Layer.empty` for the renderer entry. The renderer's bundled-table `layer` slot was never merged into the runtime graph, so this is behavior-preserving.
- `@lando/sdk/renderer` stays type/contract-only. The now-dead DI-seam types (`RendererRuntimePrimitives`, `RendererContributionFactory`, `TaskTreePainterHandle`, `TaskTreePainterOptions`) — zero remaining consumers — were removed and the module doc + `sdk/API_COMPATIBILITY.md` updated so the subpath exports only `RendererIO` and `RendererContribution`, making the plugin a truthful reference implementation. Cold-start discipline holds: `@opentui/core` stays behind the `loadInteractivePromptDriver` dynamic import; the plugin's static graph is pure.
- Verification: typecheck, lint, check:renderer-boundary passed; import-boundary + opentui-coldstart-canary green; new ownership suite `core/test/cli/renderer/bundled-renderers.test.ts` (identity resolution + non-TTY paint) plus 193 painter/visual-QA/golden tests, 75 boundary/selection/routing tests, the renderer flag source-vs-compiled parity scenario, and a broad `bun test core/test/cli/ plugins/renderer-lando/test/` (1198 pass, 0 fail) all green with goldens unchanged. Codegen re-run produced no further drift; schema snapshot unchanged (TS-interface-only). No CLI output change, so compiled-binary parity is covered by the renderer flag scenario.
- **Learnings for future iterations:**
  - `BUNDLED_PLUGINS[].layer` for a renderer plugin is never merged at runtime — the renderer resolves via a direct plugin import in `bundled-renderers.ts`. When a plugin's `renderer` export changes from a Layer to a `RendererContribution`, update the generator to emit `layer: Layer.empty` (value-import `Layer`) for that entry and set the same in `bundled.test.ts`; do not force a contribution into the `layer` slot.
  - Plugin subpath re-export shims are the cheap way to relocate implementation into a plugin without churning core importers: add `exports` subpaths in the plugin `package.json` (mirror `sdk/package.json`; `moduleResolution: bundler` honors them) and leave the old core file as `export * from "@lando/renderer-lando/<name>"`. All prior import sites keep resolving.
  - The renderer implementation cluster (`format.ts` → `task-tree-tail.ts` → `keybindings.ts`) depends only on `@lando/sdk/*` + effect, so it moves into a plugin cleanly; `console-layout.ts`/`summary.ts` are a separate cluster (used by `summary.ts`, not the event-stream renderer) and stay in core.
---

## 2026-07-03 10:22 CDT - US-382 Review
- Review confirmed the renderer ownership move satisfies US-382 and fixed the new bundled-renderer ownership test to publish a schema-decoded `task.start` event instead of using a type escape.
- Review notes (deferred/rejected): compatibility re-export shims and plugin-owned fallback formatter helpers are accepted as deliberate zero-churn ownership seams documented in `core/AGENTS.md`; duplicated event-consumer/input wiring and the plugin `./runtime` export are non-blocking follow-ups, not story blockers.
- Verification: typecheck, lint, check:renderer-boundary passed; focused bundled renderer/plugin tests passed; edited TypeScript file has clean LSP diagnostics.
---
## [2026-07-03 12:05:09 CDT] - Babysit
- PR: #512 https://github.com/AaronFeledy/core4/pull/512
- CI outcome: pass
- Review feedback: fixed (guide-drift failures resolved with documented Guide-Coverage-Skip for internal-only renderer DI refactor; Bugbot inline comments remained empty)
- Merge: merged (squash)
---
## [2026-07-03 12:35 CDT] - US-383
- Doctor NDJSON now emits every line through the central redaction-aware StreamFrame encode seam. `renderDoctorChecksAsNdjson` (the shared helper behind the provider/subsystem/global-app doctor renderers) and `doctor-report.ts`'s deprecation/app-config `doctor.check` lines both route through `encodeStreamEventFrame` instead of hand-rolled `JSON.stringify(Schema.encodeSync(StreamFrame)(...))`. Output is byte-identical to before because the sync renderers default to `identityRedactor`; the helper now accepts an optional `Redactor` so a caller with secret-bearing check results can mask them through the same seam.
- The machine-output gate needed no change: it already treats `_tag:"event"` frames as compliant and had no allowlist entries for these two files (its only carve-out is `result-encode.ts`).
- Verification: typecheck, lint, check:machine-output passed; doctor + machine-output-conformance test spot checks passed (52 doctor tests, 6 gate tests); new doctor-ndjson RED→GREEN redaction test proves a registered secret in a doctor check result is masked to `[redacted]`; a byte-compat characterization test plus a real-render QA confirmed all event lines stay byte-identical to the pre-change encoder.
- **Learnings for future iterations:**
  - The `secrets` redaction profile applies PATTERN-based masking (assignment/bearer/userinfo/signed-query), not just registered exact values. Wiring a live `forProfile("secrets")` redactor into a byte-frozen NDJSON/snapshot path would rewrite fixture bytes, so redaction-aware sync renderers must default to `identityRedactor` and only redact when a caller injects a real redactor.
  - `encodeStreamEventFrame({ event, payload, redactor: identityRedactor })` is byte-for-byte equal to `JSON.stringify(Schema.encodeSync(StreamFrame)({ _tag:"event", event, payload }))`, so migrating one-off event-line renderers onto the seam is a safe no-diff refactor — verify with a characterization test that pins the first line.
  - The doctor sync `render(value, ctx)` path has no `RedactionService` (RenderContext carries mode/format/columns/isTTY only), which is why doctor NDJSON uses injectable-redactor threading rather than the Effect-resolved redactor the streaming-JSON path uses.
---
## [2026-07-03 13:40 CDT] - US-383 Verify
- Verified the freshly built compiled binary in `lando4-dev`: `setup` completed cleanly, then a throwaway app run of `lando --format=ndjson doctor` emitted StreamFrame NDJSON (`doctor.check` events plus terminal result), with adjacent `--format=json doctor` and `doctor --help` smoke checks green.
- Fixed a compiled-dispatch regression where `runDoctor` ignored `ctx.format === "ndjson"` and rendered text instead of the shared doctor NDJSON StreamFrame renderer.
- **Learnings for future iterations:** Source/OCLIF doctor render tests are not enough for this seam; compiled `core/src/cli/run.ts` has a separate `runDoctor` render branch that must explicitly preserve NDJSON.
---

## [2026-07-03 19:29:09 CDT] - Babysit
- PR: #515 https://github.com/AaronFeledy/core4/pull/515
- CI outcome: pass
- Review feedback: fixed (guide drift skip added; Bugbot findings fixed for setup ownership read failures, legacy machine-state guidance, stdout draining, and Podman exit-125 handling)
- Merge: merged (squash)
---

## 2026-07-03 19:45 CDT - US-386
- `lando uninstall --purge` no longer loses its resumable failure report when the purge removes `userDataRoot` itself. `executeUninstall` now always writes the report on failure: to `<userDataRoot>/uninstall/report.json` when the data root survives, or to a fallback `<os-tmpdir>/lando-uninstall-report.json` when the data root is already gone — it never recreates the just-removed root. The chosen path is returned as `result.reportPath` and printed in the failure output (`Partial failure report: <path>`), so the "unavailable" dead-end is gone.
- The report still enumerates every completed/failed/manual/skipped step with its remediation `detail`, and re-running the same command reconciles from live disk state (already-removed targets re-plan to skipped, still-present targets are retried) until it succeeds. Report writing continues to route through the sanctioned atomic helper `writeFileAtomicViaRename` (US-372), so the fallback write is fsync-before-rename durable too.
- A new `_reportFallbackDir` test seam (mirrors the existing `_userDataRoot`/`_remove` seams) lets tests pin the fallback directory; production defaults to `os.tmpdir()`.
- Verification: typecheck, lint, check:paths-boundary passed; uninstall suite green (20 tests) including the rewritten purge-after-removal test (report at fallback path, root not recreated, path printed) and a new re-run reconciliation test; adjacent uninstall-machines/runtime-service/provider-machines and both atomic-write suites green. Manual QA drove the real `uninstall()` on a real filesystem: purge removed the actual data root, the cache-root step failed, and a resumable report survived at the fallback tmp path with full step enumeration while the data root stayed removed.
- **Learnings for future iterations:**
  - The purge-failure report path is chosen by `existsSync(userDataRoot)`, not the injected `exists` seam, because it must reflect the REAL post-remove filesystem (the plan's `exists` seam is for planning, not the durability decision).
  - `writeFileAtomicViaRename` mkdir-p's the report's parent dir, so the fallback dir need not pre-exist; a purge that removed `userDataRoot` still writes cleanly to `os.tmpdir()`.
  - The compiled binary path (`core/src/cli/run.ts` `runMetaUninstall`) shares `uninstall()`/`renderUninstallResult`, so report-path changes stay in source/compiled parity with no separate wiring.
---

## 2026-07-03 19:51 CDT - US-386 Review
- Review fixed the purge-failure fallback report path so production writes into a private `mkdtemp` directory under the OS temp root instead of a predictable shared temp filename, while preserving the deterministic `_reportFallbackDir` test seam. The uninstall guide now documents both the primary data-root report and the private fallback report path.
- Review notes (deferred/rejected): the existing `reportPath ?? "unavailable"` render fallback is now defensive dead text but harmless; stale-report cleanup after later success is pre-existing hygiene and not required for US-386's resumability contract.
- Verification: typecheck, lint, paths-boundary, guide lint/coverage/focused guide pass, focused uninstall suite, and adjacent uninstall/atomic spot checks passed; guide-drift skipped locally because PR context env vars are absent.
- **Learnings for future iterations:** Fallback artifacts that outlive a purged Lando root should use `mkdtemp(join(tmpdir(), "lando-...-"))` and write inside that private directory, not a fixed filename directly under shared temp.
---

## [2026-07-03 20:31:57 CDT] - Babysit
- PR: #516 https://github.com/AaronFeledy/core4/pull/516
- CI outcome: pass
- Review feedback: fixed (Bugbot temp-dir leak and fallback report write rejection)
- Merge: merged (squash)
---

## 2026-07-03 20:50 CDT - US-387
- macOS/Windows `lando setup` now stands up the managed Podman machine from the *bundled* runtime tooling instead of system Podman on PATH. The provider setup path resolves the machine runner from `${runtimeBinDir}/podman` (Paths-driven), and the "Detect Podman" version step on mac/win probes that same bundled binary rather than PATH `podman`. Removed the production fallbacks the audit flagged: the `makeSystemPodmanMachineRunner(undefined, …)` system-machine default (in both the provider factory's ensureRuntime wiring and `setupProviderLando`) and every `process.env.LANDO_TEST_PODMAN_SOCKET` read in production. Socket precedence now mirrors Linux: explicit option > injected managed paths > managed default; the test env var is honored only in `@lando/core/testing`'s `resolveLiveProviderSocket` and explicit test wiring.
- When the installed bundle lacks machine tooling for the host platform, setup fails inside the "Ensure Podman machine" step with a tagged `ProviderUnavailableError` naming the platform and the missing bundled binary path (plus remediation), instead of silently shelling out to system Podman. Recorded setup state still carries the machine identity (`{name:"lando", createdByLando}`) and the managed socket path for idempotent re-detection and US-385 teardown.
- Verification: typecheck, lint, paths-boundary passed; new bundled-machine-tooling suite (6) covers both platforms — bundle-missing tagged error, bundle-path runner resolution, bundled-binary version detection, injected-runner bypass — plus an env-ignored regression in runtime-path-resolution; full provider-lando unit suite (121) and adjacent core machine/socket suites green. Real-surface driver (no seams, real existsSync) confirmed the darwin tagged error names the bundle path and the resolved machine command is `${runtimeBinDir}/podman`. Live mac/win verification runs in the `provider-integration-darwin-*`/`provider-integration-windows-*` CI jobs.
- **Learnings for future iterations:**
  - The mac/win setup path has THREE system-Podman touch points, not one: the machine runner (setup.ts + index.ts), the socket env fallback, AND the `podman --version` "Detect Podman" step. Route all three through the bundled `managedRuntimePodmanArgv0(runtimeBinDir)` = `${runtimeBinDir}/podman`; the version step is the easy one to miss.
  - `managedRuntimePodmanArgv0` lives in `@lando/core/managed-runtime-service` and is importable from provider-lando (import-boundary allows it).
  - Test-only seams use an underscore prefix (`_machineToolingExists`, `_machineRunnerFactory`) mirroring US-386's `_reportFallbackDir`; they let unit tests prove the bundled command string and the missing-tooling gate without spawning real podman. A bundled-binary version proof can also use a real executable shell script at `${runtimeBinDir}/podman` (host-safe, no container).
  - `LANDO_TEST_PODMAN_SOCKET` must be read only in `core/src/testing/live-provider-socket.ts` and test wiring; `grep -rn LANDO_TEST_PODMAN_SOCKET plugins/provider-lando/src` must return nothing.
---

## [2026-07-03 21:57 CDT] - Babysit
- PR: #517 https://github.com/AaronFeledy/core4/pull/517
- CI outcome: pass
- Review feedback: fixed (Bugbot missing-bundled-tooling error path; guide drift skip added for failure-mode-only changes)
- Merge: merged (squash)
---

## 2026-07-03 22:20 CDT - US-388
- Decided the release-automation posture: full signed release runs **manually until RC**, not from CI. The 13-stage orchestrator (`scripts/release.ts`) is fully implemented and credential-gated, but signing-credential ownership (Apple notarization, Windows certificate, cosign/OIDC identity) is unassigned and no signing secrets are wired into any workflow — so wiring the pipeline into CI now would only produce warning-skipped signing stages, not a trustworthy signed artifact. The generated `release` workflow stays scoped to dev prereleases only.
- Decision recorded in three places: the canonical `docs/beta-1-decisions.md`, the PRD-ALPHA4-08 open-questions section (credential-ownership question resolved-by-decision), and this progress note.
- Added `docs/release-runbook.md`: the exact manual invocation surface (`bun run release -- --all|--binary|--library`, `--through-stage=<id>`, `LANDO_RELEASE_PLATFORM=windows-x64`, `LOCAL_REHEARSAL=1`), a per-stage required-credential table, and concrete verification steps (checksum verify, binary smoke, signing/notarization, SBOM+provenance, npm dist-tag, GitHub release), cross-linked to the existing release guides.
- Stated the dev-prerelease-only scope in the generated workflow header: `scripts/build-release-workflow.ts` now emits a scope comment block (kept below the generated-file header line so the drift/idempotency contract holds) pointing at the runbook and decision doc.
- Verification: typecheck, lint (biome + deprecations) passed; `codegen-ci.test.ts` (14) and adjacent `release.test.ts` (53) green; a RED→GREEN codegen test asserts the scope comment, manual-until-RC statement, runbook pointer, and absence of `win32-x64` in the release workflow/generator. `bun run codegen` is idempotent (no drift on re-run). Manual QA parsed every runbook invocation through `parseReleaseOptions` and confirmed each resolves as documented.
- **Learnings for future iterations:**
  - The generated release workflow drift test (`core/test/scripts/codegen-ci.test.ts`) asserts `release.yml` does NOT contain the substrings `cosign`, `gpg`, `sbom`, `installer`, or `update-manifest`, and that its first line is exactly the generated header. Any scope/comment text added to the release workflow must avoid those substrings and must sit below the header line ("signed release pipeline" / "credential-gated" are safe phrasings).
  - Platform-id vocabulary is two domains: CI/release artifact ids are `windows-x64` (see `scripts/ci-platforms.ts` `CI_PLATFORMS`); the `win32-x64` runtime host key lives only in `scripts/build-runtime-bundle.ts` and `scripts/build-mutagen-versions.ts`. Never introduce `win32-x64` into `.github/workflows/*`.
  - `scripts/release.ts` scopes a run to one platform via `LANDO_RELEASE_PLATFORM` (CI/release id) and warning-skips credential-gated stages under `LOCAL_REHEARSAL=1`; both are the sanctioned manual-invocation levers for the runbook.
---

## 2026-07-03 22:26 CDT - US-388 Review
- Review fixed the remaining decision-record gap: `spec/beta-1/prd.json` now records the accepted manual-until-RC release posture in the US-388 story notes, and the Beta 1 source PRD open question now points at the resolved decision instead of reading as still-gating.
- Review notes (deferred/rejected): workflow `contents: write`/`id-token: write` permissions are unchanged pre-existing dev-prerelease behavior, not a review blocker for this decision story; runbook secret environment variable names are operational documentation, not credential leakage.
- Verification: typecheck, lint, focused release workflow/release orchestrator spot checks, PRD JSON validation, codegen idempotency, and edited-file diagnostics passed.
---

## 2026-07-04 02:30 CDT - US-389
- All three config surfaces (`app config`, `meta config`, `meta global config`) gained working `set`/`unset`/`edit`/`validate` verbs, replacing the previous read-only + deferred-stub state. Writes parse the target file to the encoded (wire) tree, mutate it with a shared pure dot/bracket-path engine, validate the result against the published schema (LandofileShape for app + global-app; GlobalConfig for global config.yml), then emit canonical YAML through `emitLandofileYaml` and land it via the atomic write helper. `set` honors `--type string|number|boolean|json|yaml`; `edit` opens `$VISUAL`/`$EDITOR` (or `--editor`) through the existing injectable editor seam and re-validates the buffer before saving; `--dry-run` reports the change without touching disk.
- Rejections (malformed path or schema violation) fail with a new tagged `LandofileWriteValidationError` carrying the offending path + remediation, exit non-zero, and leave the file byte-for-byte unchanged. App-plan cache correctness is automatic: the plan cache key already hashes `.lando.yml` content, so a write is an implicit cache miss — no explicit invalidation needed.
- The shared write engine lives in `core/src/cli/config-write/` (`dot-path.ts` get/set/unset on the encoded tree, `value-parse.ts` `--type` parsing, `write-core.ts` path/value/validation/result-field helpers) and is consumed identically by all three scopes.
- Machine-output: each verb returns a faithful result through the universal envelope (`--json` emits `apiVersion/command/ok/result`), verified live for every scope. Result schemas for the three command ids plus the four app + four global verb ids are frozen in the schema snapshot.
- Verification: typecheck, lint, renderer/probe/state-store/paths boundary gates, public-transcripts + guide-coverage gates all passed. Dual-dispatch parity, schema-snapshot, command-result-schema, dispatch-unification, and the app/meta/global config scenario suites passed (150+ tests across the config/parity blast radius); new RED→GREEN suites under `core/test/cli/config-write/` cover set/unset/edit/validate/dry-run/reject per scope. Manual CLI QA drove the real source binary end-to-end: `app config set/unset/validate/--dry-run/--json`, `config set/validate` (canonical + alias), and `global:config set/validate/--dry-run` — happy paths write canonical YAML, dry-run leaves files untouched, and schema-violating writes exit 1 with the tagged error while the file stays unchanged.
- **Learnings for future iterations:**
  - OCLIF positional-subcommand dispatch (one command branching on `args.subcommand`) only routes for a *leaf* command. A command whose parent is an OCLIF *topic* (has sibling subcommand files, e.g. `app:config` beside `app:config:translate`/`:lint`) greedily resolves `lando app config set k v` to the id `app:config:set:k:v` and fails — each write verb must be its own canonical id registered in `compiled-commands.ts` (id = map key; filename irrelevant under `strategy:"explicit"`), added to `MVP_COMMAND_IDS`, given a `run.ts` branch, then `codegen:oclif-manifest` + `codegen:schema-snapshot` regenerated. Deep-topic leaves (`meta:global:config`) route via their single-token top-level alias but fail the multi-token canonical path, so verb ids fix that too. `meta:config` stays single-dispatch because it is a true leaf reachable via the `config` alias and the canonical `meta config` path.
  - The canonical serializer round-trips *values*, not comments (`parseLandofile` strips comments; `emitLandofileYaml` re-emits canonical block YAML). `edit` therefore writes the user's raw edited text (preserving their formatting) after validating it, while `set`/`unset` re-emit canonical YAML from the mutated tree.
  - `GlobalConfig` (config.yml) is a deliberately narrow struct — most global fields are "modeled elsewhere" — so config.yml writes must validate with default excess handling, not `onExcessProperty:"error"`, or legitimate keys (pluginConfig/providers/envPrefix) would be rejected. Landofile writes use `onExcessProperty:"error"` since `LandofileShape` is the full contract.
---

## 2026-07-04 03:01 CDT - US-389 Review
- Review fixed two config-write blockers: global `config.yml` can now read back canonical block-list YAML emitted by `meta config set`, so array-valued global config no longer bricks later config loads; all config write surfaces now map canonical serializer failures into `LandofileWriteValidationError` with remediation instead of leaking Effect defects/stack traces, leaving files untouched.
- Review also refreshed stale `meta config` help text and regenerated the OCLIF manifest.
- Review notes (deferred/rejected): `app config edit --target ...` / `meta global config edit --target dist` support remains deferred because it is a broader §8.2 target-selection extension rather than a regression in the shipped default write path; prototype-pollution, editor-injection, dry-run, schema-snapshot, and compiled-dispatch concerns from review-work were rejected as false positives after code/test inspection.
- Verification: typecheck, lint passed; focused config-write and adjacent meta-config scenario spot checks passed; edited files have clean LSP diagnostics.
---
## [2026-07-04 04:28:31 CDT] - Babysit
- PR: #519 https://github.com/AaronFeledy/core4/pull/519
- CI outcome: fail (static-checks-platform (linux-x64) still pending; Cursor Bugbot reported new current-head finding)
- Review feedback: fixed (cwd for global config edit; --path write verbs; library/schema drift; invalid config subcommands; bracket-path get/view; meta global CLI typo forwarding; inline flow YAML scalars)
- Merge: deferred (five triage rounds exhausted; latest Bugbot finding remains: Meta config typos fall through)
---
## 2026-07-04 04:40 CDT - US-389 Build (finish in-flight)
- Closed the last open PR #519 blocker (Cursor Bugbot "Meta config typos fall through"): a mistyped `meta config` verb (e.g. `settt`) silently defaulted to the view path instead of failing, unlike the already-hardened `app config` and `meta global config` scopes. Now all three write surfaces reject unknown subcommands identically.
- Fixed at both layers to match the sibling pattern: `metaConfigOptionsFromInput` no longer drops unrecognized verbs through the `isSubcommand` type-guard — it widens the local subcommand to `string` so an unknown verb reaches `config()`; and `config()` now rejects any subcommand outside `view`/`get`/the handled write+telemetry+translate verbs with a tagged `LandofileWriteValidationError` (message + `meta config` usage remediation), so it exits non-zero and reads nothing.
- Verification: typecheck (exit 0), lint (biome + deprecations) passed; meta-config scenario suite 10 pass incl. RED→GREEN library reject + end-to-end CLI reject tests; adjacent app-config/global-commands/config-write suites 91 pass; dual-dispatch parity 10 pass; no schema-snapshot drift (no verbs/schemas changed); LSP clean on both changed source files. Manual CLI QA drove the real source binary: `meta:config settt` now exits 1 with the tagged error + remediation in both `--format json` envelope and human render, while `view`/`get`/`set`/`unset` still exit 0.
- **Learnings for future iterations:**
  - Config write-surface parity is enforced at TWO layers per scope: the OCLIF `*OptionsFromInput` mapper must WIDEN `subcommand` to `string` (never gate it behind an `isSubcommand` guard, which drops typos to `undefined`→view), and the library command must have an explicit reject branch after its known-verb dispatch. Fixing only one layer leaves the typo falling through.
  - The load-bearing `// Widened to string ...` comment on the mapper's `subcommand` field is intentional and must stay: it stops a future agent from re-narrowing the type and silently reintroducing the fall-through bug. All three scopes (app/meta/meta-global) carry the identical comment.
---
## [2026-07-04 07:15:00 CDT] - Babysit
- PR: #519 https://github.com/AaronFeledy/core4/pull/519
- CI outcome: pass
- Review feedback: fixed (Bugbot template-write findings fixed; guide drift resolved with PR-body skip rationale)
- Merge: merged (squash)
---
## [2026-07-04 07:40:00 CDT] - US-390
- `lando app:config:translate` gained the full detect/list/from/file flow. The command result became a tagged union (`list` / `preview` / `write`) so one command serves three shapes through the machine-output seam: `--list` enumerates registered translators (id, input kinds, summary); autodetection with no `--from` probes each translator's `detect` surface and runs the lone `exact`/`likely` match (zero or multiple matches fail with a `ConfigTranslateError` whose remediation enumerates the `--from` choices); `--from <id>` forces a translator; `--file <path>` (repeatable) scopes translator input.
- Non-write mode now PREVIEWS the canonical Landofile by printing it (through the renderer/JSON seam) instead of writing a `<input>.canonical` file — aligning with the spec's "translate is preview-only unless --write". `--write` still overwrites the input with a `.bak` backup and consumes the same `emitLandofileYaml` serializer. Only the single selected translator runs now (previously all registered translators ran).
- No new registry: translators remain an injected seam (`ConfigTranslatorShape[]`), so the honest production state is an empty list — `--list` shows none and translate fails with the plugin-install remediation, matching "None bundled by default". Both the OCLIF source path and the compiled `run.ts` argv parser were updated in lockstep for dispatch parity; the OCLIF `--format` options moved to `yaml|table|json`.
- Verification: core typecheck (exit 0), lint (biome + deprecations) passed; renderer-boundary gate passed. RED→GREEN unit + contract-fixture spot checks (15 pass) — the compose fixture passes `runConfigTranslatorContractSuite` and then drives list/detect/from/file; regression suites landofile-config-translate (7), sdk config-translator contract (8), dual-dispatch parity (10), oclif-manifest (2), dispatch-parity (66) all green. The OCLIF manifest was regenerated for the new flags with no unexpected drift; no schema-snapshot drift (the result schema is a core CLI schema, not an SDK export). Manual CLI QA drove the real source binary: `--list --format json` emits `{mode:"list",translators:[]}`, `--list` prints "No config translators are installed.", default/`--from`/`--file` reach the NoTranslators remediation without writing a `.canonical` file, and `--from`/`--file` parse cleanly on the source path.
- **Learnings for future iterations:**
  - Preview-by-default means the translated Landofile is the command's stdout payload, not a side-effect file. Diagnostics ride along in the structured result (and JSON envelope); in human/yaml mode they are appended as `#`-prefixed YAML comments so the printed document stays parseable.
  - A command that emits multiple result shapes should use a `Schema.Union` discriminated on a `mode` literal; `render()` branches on `mode` and stays human-only while the central seam JSON-encodes the whole union. Watch `exactOptionalPropertyTypes`: derive the render helpers' parameter types from the schema (`Schema.Schema.Type<typeof XSchema>`), not the SDK interface, or `path?: string` vs `path?: string | undefined` mismatches.
---
