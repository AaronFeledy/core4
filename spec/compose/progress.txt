# Ralph Progress Log — Lando v4 Compose Service-Key Vocabulary

Branch: ralph/compose
Source PRDs: spec/compose/prd-compose-{01..04}-*.md (US-466..US-477)
Spec source of truth: spec/{01..19}-*.md — specifically the amended §6.2 (service schema normalization contract), §7.4 (vocabulary principle, disposition matrix, vendored schema, rejected machinery), and §5.5.1 (matrix-driven planning order)
Origin: 2026-07-20 compose-alignment assessment (vocabulary-not-file-format decision; see prd-compose-00-index.md)

## Conventions

- Durable progress narrative, not a changed-file ledger. Git records diffs; this file records outcomes, decisive verification, blockers, and durable learnings.
- Explain what became true, what proved it, and what remains blocked or deferred. Skip review choreography, generated-path inventories, and test-count trivia.
- If a story relaxes or re-scopes a contract, record the decision and the synchronized spec/PRD/snapshot updates. Truth hierarchy is spec > PRD > progress.

## Codebase Patterns

- The disposition matrix (US-466) is the single classification source: coverage gate, docs matrix, rejection remediation text, and conformance skip-list are all derived artifacts. Never hand-maintain a second key list.
- The vendored Compose schema is a coverage oracle, not a codegen source: Effect Schema in @lando/sdk stays hand-authored per spec §1.2.
- Pin source is a compose-go tag (compose-spec/compose-spec is an untagged mirror; never pin to it).
- Keep the networked `codegen:compose-vendor` refresh outside generic offline `codegen`; tests and coverage gates read only committed bytes.
- The vendored upstream schema is checksum-pinned byte-for-byte and excluded from formatting; format the pin and tooling, never the schema payload.
- `Schema.encodedBoundSchema` drops property-signature annotations for fields backed by `Schema.transform`, so a description set only at the field site never reaches `ServiceConfigInput` or its published artifact. Annotate the transform's `from` schema as well.
- Treat a public-schema annotation exemption as a last resort, not the standard remedy: exemptions are permanent and unpoliced, so an exempted field keeps passing after its description is deleted. If a gate flags a field whose generated artifact already carries the metadata, fix the gate's traversal instead.
- Top-level Compose dispositions need AppPlan-specific rationales; `configs` and `secrets` are preserved because AppPlan has no provider-neutral fields for them.
- Landofile decode runs twice (per-file, then over the merged result with `onExcessProperty: "error"`), so every `ServiceConfig` field transform must accept its own canonical output as input. Pair each new alternate-form field with a `decode(decode(x)) === decode(x)` test; an encode round-trip test alone will not catch the second pass.
- A per-command oclif `--format` `options:` list does not validate anything: the universal resolver strips `--format` before oclif parses and reinjects `flags.format`. Real validation is `RESULT_FORMATS` in `core/src/cli/format-flags.ts`, so a command silently ignoring a format is a bug in its own option mapping, not in the flag declaration.
- A new or changed static oclif flag is inert until `bun run codegen:oclif-manifest` runs: `--help` and compiled-path flag validation both read the generated `compiled-manifest.ts`. `core/oclif.manifest.json` is gitignored; `compiled-manifest.ts` and `docs/reference/commands.mdx` are the committed artifacts.
- Build multi-form schema test fixtures by decoding an ENCODED document through the schema and asserting against the decoded result, rather than hand-writing canonical decoded values. It removes a whole class of wrong-by-guess assertions and makes the test prove canonicalization instead of assuming it.
- `Pick<T, "a"> & { readonly b?: B }` is not interchangeable with `Pick<T, "a" | "b">` under `exactOptionalPropertyTypes`: the hand-written optional excludes `undefined` and rejects the real source type. Spell narrowed views as a `Pick` of the source so optionality stays identical.
- The app-plan cache key deliberately omits provider capabilities, so any capability gate must also run on the cached-plan path; and any change to planner OUTPUT semantics needs an `APP_PLAN_CACHE_SCHEMA_VERSION` bump, because the key is derived from the authored Landofile and will not roll on its own.
- Biome scans untracked sibling checkouts. A stale `.worktrees/` directory is git-excluded and invisible to `git status`, yet makes `bun run lint` fail on files outside the change; scope Biome to tracked package dirs before believing a repo-wide lint failure.
- `bun test <path>` treats its argument as a filter, not a path: a wrong or moved path emits a "did not match any test files" diagnostic and exits nonzero. Scripted spot-check loops must require both command success and a positive test count; never infer a pass only from the absence of failures.
- The machine-output envelope narrows every error to `{_tag, message, remediation}` by design. Structured error context belongs in the message and in the bug-report formatter's extra fields, not in a per-error widening of the shared envelope.
- Schema parse-error messages can reach the plain terminal renderer unchanged. Never interpolate raw Landofile values into those messages; use constant grammar/remediation text so control bytes cannot alter terminal output.
- Include resolution has two distinct roots: the app root is the containment boundary, while the including fragment's directory is the base for relative sources. Never reuse one as the other; preserve absolute source identity separately from authored provenance.
- A key classified `preserved` is not implemented until the authoring schema accepts it: Landofile decode uses `onExcessProperty: "error"`, so an unaccepted-but-preserved key fails as an unsupported MVP key rather than round-tripping. Compose rejection runs before schema decode, so only `rejected` matrix paths yield `ComposeKeyRejectedError`.
- Two distinct Compose capability tiers exist and must not be merged: the exact 24-key `ComposeServiceKnobKey` runtime-knob list gated by `composeKnobs.supported`, and the broader supported subset (`networks`, `configs`, `secrets`, `profiles`, `labels`, service `x-*`) gated only by coarse `composeSpec: "native"`.
- Manual CLI verification must invoke `core/bin/lando.ts`; running `core/src/cli/index.ts` directly emits nothing and exits 0, which is indistinguishable from a passing command.
- A derived/computed invariant (skip-list, drift manifest, anti-hiding guard) is unproven until it has been observed failing. Mutate an input to manufacture the stale or hidden state, confirm red, then restore; a guard that has only ever been green is indistinguishable from one that cannot fail.
- An `*.integration.test.ts` gated on an env var is dead unless something sets it. Pair the skip guard with a generator-emitted export in the CI workflow and assert the ordering between the export and the test invocation, otherwise the lane silently never runs.
- Checksum pinning authenticates vendored fixture bytes but does not make fixture-derived paths safe. Keep materialized files inside a canonical temporary root, reject symlink escapes, and create files without following or overwriting existing destinations.
- When an adjudication amends only the machine-readable index, the narrative PRD it was derived from does not follow automatically. Reconcile both in the same pass — comparing criterion counts as well as wording, since an adjudication can add a whole criterion the narrative never receives — or the prose contract drifts from the one the loop actually reads and the next cycle re-litigates a settled boundary.
- A named-volume `subpath` must exist before Podman mounts it. Live fixtures that exercise subpaths need an initializer container before application bring-up; do not remove the subpath merely to make the fixture runnable.
- When stories share a conformance harness, distinguish ownership of the shared runner and tooling from ownership of fixtures and fixture-specific assertions; executing another story's proof does not transfer its scope.
- Compose conformance has no static skip-list file: a fixture is treated as rejected only when runtime classification from the disposition matrix says so, and `manifest.json` is purely the drift record. An audit that greps for a skip-list artifact reports a false MISSING; re-falsify the manifest parity guard instead.
- One mutation that turns two guards red proves complementary detection, not guard independence: the same deleted input can violate both predicates at once. The claim the evidence supports is "a targeted mutation activated two complementary guards while unrelated assertions stayed green"; real independence needs one mutation per guard, each leaving the other violation exposed. Do not upgrade the weaker observation to an independence claim in the record.

## Log

## 2026-07-25 14:47 CDT - US-466
- Pinned the compose-go service schema by tagged checksum, added the exhaustive normalized/preserved/rejected disposition matrix, and made schema drift fail through an offline coverage gate wired into generated CI.
- Verification: typecheck and lint passed; focused walker, checksum, coverage, and generated-CI tests passed; adjacent CI workflow and codegen idempotence spot checks passed.
- **Learnings for future iterations:**
  - The v2.13.0 schema currently exposes 362 service key paths and 10 top-level paths; new upstream paths remain unclassified until the committed matrix is deliberately updated.
  - Pattern-property maps use `*` while Compose extensions use `x-*`, keeping dynamic entries stable and human-readable in gate output.
---

## 2026-07-25 15:15 CDT - US-466 Review
- Corrected the disposition contract so `build.dockerfile_inline` normalizes per spec 6.2, top-level `configs` and `secrets` preserve into Compose extensions, and project-level rationales describe AppPlan outcomes; synchronized the CI runbook with the new gate.
- Guide-Coverage-Skip: US-466 changes internal classification metadata and CI documentation only; it does not change current user-visible Landofile behavior.
- Review notes (deferred/rejected): Per-key service normalization targets remain owned by US-477's generated docs matrix; expanding all 362 rows here would exceed US-466's rationale contract.
- Review notes (deferred/rejected): Resource limits, timeout hardening, root-level schema references, and stricter CI-test scoping are non-blocking hardening or test-design follow-ups outside this story; the pinned schema, checksum boundary, generated workflow, and current walker shapes were verified directly.
- Review notes (deferred/rejected): Post-fix Bugbot findings for `env_file.{path,format,required}` and `ports.mode` conflict with the documented supported subsets: US-468 owns string/list `env_file`, while US-469's long-port contract names `target`, `published`, `host_ip`, `protocol`, `name`, and `app_protocol`; those unsupported children remain deliberately rejected.
- Verification: typecheck and lint passed; 11 focused checksum, walker, coverage, and generated-CI tests passed with 17 assertions; coverage remained exhaustive at 362 service paths and 10 top-level paths; CI generation was idempotent.
---

## 2026-07-25 15:58 CDT - US-466 Drift Audit
- Trigger: 2 cycles, 3115 changed lines, 3 commits
- Weakened proofs: restored black-box coverage-command failure proof and made the remediation invariant reject non-actionable placeholders.
- Review scar tissue: removed redundant post-write checksum revalidation while retaining pre-write verification and the offline committed-byte gate.
- Contract drift: replaced generic rejected-key guidance with path-specific remediation that selects a concrete Lando, provider-extension, or translator route.
- Fixed: simplified the vendor refresh and restored production-boundary proof without adding a test-only runtime seam.
- Split/excise decisions: none; no absorbed feature or gate conflict was found, and later-story key-shape boundaries remain unchanged.
- Verification: typecheck, lint, vendor refresh, compose coverage, focused walker/vendor/gate/CI checks, and CI idempotence passed; the broader adjacent codegen suite remains red on its pre-existing CI platform-matrix expectation.
---

## 2026-07-25 16:10 CDT - US-466 Verify
- Compiled `core/dist/lando`; sandbox `setup --yes --no-interactive --skip-provider` succeeded; story gates exercised end-to-end: pin/checksum match (compose-go v2.13.0), `bun run check:compose-coverage` green (362 service + 10 top-level), focused walker/vendor/coverage/CI tests green, CI workflow lists the gate with peer boundary checks, offline `codegen` does not invoke `codegen:compose-vendor`. Adjacent compiled-binary smoke (`--version`, `--help`, `config`/`list` help, throwaway app `info`) passed.
- Full managed-provider `lando setup` still fails on clean Ubuntu 26.04 sandbox: bundled podman needs `libgpgme.so.11` (`libgpgme11t64`) not present in the dependency-free baseline — pre-existing runtime/host-deps gap, not introduced by US-466.
- **Learnings for future iterations:** Clean `lando4-dev` baseline lacks `libgpgme`; full provider setup verification needs either a runtime bundle that ships/static-links gpgme or setup installing the host package before launching podman.
---
## 2026-07-25 16:36 CDT - Babysit
- PR: #629 https://github.com/AaronFeledy/core4/pull/629
- CI outcome: pass
- Review feedback: fixed (Guide-Coverage-Skip on PR body for internal dispositions matrix; no guide change needed)
- Merge: merged (squash)
---

## 2026-07-25 16:55 CDT - US-467
- Added a generated, scheduled `compose-vendor-bump` GitHub Actions workflow that weekly checks the latest tagged compose-go release against the committed pin and, when newer, re-vendors the pinned schema and opens/updates a single rolling PR whose body lists the added/removed service and top-level key paths. Classification stays manual: unclassified new keys fail the existing coverage gate on the PR, which is the intended merge blocker; the workflow never touches the disposition matrix.
- Substrate: extended the vendor script with a `--tag` bump mode (fetch-isolated CLI glue over a pure, offline `applyComposeVendorBump` and pin builder) and reused the US-466 schema walker as a key-path diff reporter (pure `diffComposeSchemaKeyPaths` + markdown formatter behind a small CLI). Newer-tag selection is a pure semver comparator that drops pre-release/malformed tags. The workflow generator is registered in the offline codegen catalog; the networked vendor refresh is deliberately kept out of offline codegen/tests.
- Verification: typecheck, lint passed; 59 focused script/build test spot checks passed (vendor helpers, schema diff, bump apply, diff-reporter CLI, workflow structure, CI active-workflow allowlist); coverage gate unchanged at 362 service + 10 top-level paths; full `bun run codegen` idempotent with clean `.github/workflows` diff. Ran the workflow's tag-selection snippet and the diff-reporter CLI end-to-end against real inputs and confirmed the emitted YAML parses. Three failing codegen-ci CI-platform-matrix tests are pre-existing (reproduce on the clean baseline; assert ci.yml, which this story never touches).
- **Learnings for future iterations:**
  - Adding a generated workflow requires updating the exact-match allowlist in core/test/build/ci-workflow.test.ts ("keeps generated workflows as the only active workflows") and registering the generator in scripts/codegen.ts; the php-base-workflow.test.ts `committed === render()` + `Bun.YAML.parse` pattern is the fast, self-contained structure-test template (no full codegen run).
  - To keep networked codegen offline-testable, split scripts into a pure core (injected bytes, tested in tmpdir) and a thin `import.meta.main` glue that owns `fetch`; the compose-vendor `--tag` mode follows this so tests never hit the network.
  - codegen-ci.test.ts is slow (~9s per `bun run codegen`, ~220s full file) and currently red on three ci.yml platform-matrix expectations unrelated to compose work — verify new-workflow drift with a targeted `bun run codegen && git diff --exit-code -- .github/workflows` instead.
---

## 2026-07-25 17:35 CDT - US-467 Review
- Fixed the US-467 nightly-workflow contract the build step missed: the generated `compose-vendor-bump` workflow now runs daily (not weekly), carries a `concurrency` group so overlapping scheduled/manual runs cannot race the rolling force-push and PR create/edit, uses the reviewed immutable checkout/setup-bun SHA pins with `persist-credentials: false`, and routes failures to a single rolling issue (open-or-comment by exact title, no preexisting label assumed).
- Closed the real merge-blocker gap: a bot-authored PR pushed with `GITHUB_TOKEN` does not trigger `pull_request`/`push` CI, so `check:compose-coverage` would never gate unclassified new keys. The bump job now explicitly `gh workflow run ci.yml --ref automation/compose-go-bump`, and generated `ci.yml` gained `workflow_dispatch`, so the rolling PR head receives the coverage checks that turn it red.
- Hardened `--tag` bump parsing to a single shared `isStableComposeGoTag` predicate (`^vMAJOR.MINOR.PATCH$`), rejecting partial, prerelease, and path-bearing refs before any fetch/URL construction.
- Review notes (deferred/rejected): security "mutable action pins" finding accepted and fixed via the reviewed pins; the same lane's suggestion to authenticate only immediately before push is already satisfied by the existing `gh auth setup-git` step plus `persist-credentials: false`. No test was weakened; the weekly-schedule assertion was replaced because it encoded the noncompliant cadence.
- Verification: typecheck, lint passed; 57 focused workflow/vendor/schema/diff tests passed; `bun run codegen` left `.github/workflows` drift-clean (same 9 changed files); guide-drift gate passed with `GUIDE_DRIFT_BASE_REF=origin/main`; Bugbot clean before and after fixes.
- Guide-Coverage-Skip: US-467 changes generated CI/automation workflows and internal bump tooling only; no user-facing Landofile surface or documented CLI behavior changed, so no owned guide update applies.
- **Learnings for future iterations:** GitHub suppresses `push`/`pull_request` events for commits made with the default `GITHUB_TOKEN`; automation that opens a bot PR and needs a required check to gate it must add `workflow_dispatch` to that check's workflow and dispatch it on the PR branch, since the dispatched run's check-runs are keyed to the PR head SHA.
---

## 2026-07-25 19:55 CDT - US-468
- Made ServiceConfig accept Compose spellings and alternate scalar/list forms, canonicalizing all to one internal long form before normalization. `environment` now decodes a map or a Compose `KEY=value` list; `labels` decodes a map or list; `dependsOn` decodes a service-name list or a Compose condition-map (`{svc:{condition,required?,restart?}}`) into a structured `ServiceDependency[]` carrying the Compose condition vocabulary; new `envFile` accepts string or list. The Landofile authoring boundary additionally accepts the Compose cross-key spellings `working_dir`, `env_file`, and `depends_on`, with the Lando alias winning when both are present. A bare `environment` list entry without `=` fails decode with a `KEY=value` remediation (host-env interpolation is intentionally unsupported in Landofiles, matching the §7.4 purity rule); the remediation now surfaces through the Landofile validation error. Ran `lando app config get services.web` end-to-end and confirmed the resolved output shows canonical `workingDirectory`, structured `dependsOn` (condition + restart preserved), and `environment`/`labels` as maps.
- Verification: typecheck, lint passed; sdk schema, core decode+remediation, real-CLI config-render scenario, public-schema-contracts (encode round-trip), app-planner, extends, service-lando (21 consumers), and provider-lando compose-emit spot checks passed; compose-coverage and import-cycle gates green; schema snapshot regenerated and idempotent; API_COMPATIBILITY updated additively.
- **Learnings for future iterations:**
  - Keep a registered public schema (`ServiceConfig`) a plain `Schema.Struct`: the public-schema-contract machinery (`validatePublicSchemaAnnotations` walks `AST.getPropertySignatures`, and `public-schema-contracts.test.ts` round-trips every schema through `Schema.encode`) does not tolerate a top-level `transformOrFail`. Put per-key form canonicalization in field-level transforms on the Struct, and do cross-key aliasing in a SEPARATE `ServiceConfigDecode = transformOrFail(ServiceConfigInput, ServiceConfig)` used only in `LandofileShape.services`.
  - For that boundary transform, build the FROM schema from `Schema.encodedSchema(ServiceConfig)` (not `...ServiceConfig.fields`) plus the Compose keys — reusing the field-transform schemas double-applies them on encode and breaks `Schema.encode(LandofileShape)` (which `app config` view and any LandofileShape serializer rely on). The boundary `decode` returns the raw renamed object (Effect treats it as `To.Encoded`, so `ServiceConfig`'s field transforms re-run) and `encode` is an identity pass.
  - New public schema fields need a description on the `Schema.optional(...)` wrapper (the `webroot` pattern) — a description on the inner transform schema is not seen by the description-coverage gate.
  - `ServiceConfig.dependsOn` is consumed as a value by ~21 `service-lando` service types (`for (const dep of service.dependsOn ?? []) ctx.addDependency({ service: ServiceName.make(dep), ... })`); restructuring it to `ServiceDependency[]` requires updating each to `dep.service`. The Compose condition is preserved into the plan but not yet honored by orchestration — that is US-472's job (`condition:"started"` stays hardcoded here).
  - `lando config` is the GLOBAL config (`meta:config`); the resolved app/service config command is `lando app config` (`app:config`). Use `app config get <key> --format json` to inspect post-normalization resolved service values.
---

## 2026-07-25 22:10 CDT - US-468 (completion)
- Closed the last open acceptance criterion on the in-flight branch: the resolved-config read surface now actually renders. `lando app config view --source resolved --format yaml` and the equivalent bare `lando app config --format yaml` emit the post-merge, post-normalization Landofile as YAML, so a user pasting a Compose service block can see exactly how their spellings resolved — `working_dir` as `workingDirectory`, a scalar `env_file` as an `envFile` list, `environment`/`labels` lists as maps, and `depends_on` as `dependsOn` with the Compose condition and restart preserved.
- Added the `--source` selector accepting `resolved` only, defaulting to `resolved`, threaded through the bare command, explicit `view`, and `get`. An unsupported value (including the spec-documented `raw`/`merged`) fails flag validation with a tagged `MalformedCliFlagValueError` and exit 2 rather than being silently dropped and treated as resolved; `--help` advertises both the flag and its supported value.
- YAML serializes `result.landofile` only — never the `{app, source, landofile}` envelope — via `Schema.encodeSync(LandofileShape)` plus `emitLandofileYaml`, so realization details cannot leak into a value users commit. Table and JSON output are untouched.
- Deliberately NOT implemented, with ownership recorded: working `raw`/`merged` source stages (needs a source-stage loader redesign; no PRD owns it yet), post-expression traversal (config-expression stories), deprecation notices in YAML views (alpha-4 deprecation governance), and `meta config --source`.
- Verification: typecheck, lint, and renderer-boundary passed; 68 focused tests green across the new render/flag unit suite, the compose-spellings CLI scenarios, and adjacent app-config/lint/translate/manifest/dual-dispatch-parity suites; oclif manifest and command reference regenerated and idempotent. All six CLI invocations were run end-to-end against a throwaway Compose-spelled app (both YAML forms byte-identical, `get --source resolved` returning `/app`, `--source raw` exiting 2, table and JSON envelope unchanged).
- **Learnings for future iterations:**
  - `--format` is stripped from argv by `resolveResultFormat` BEFORE oclif parses, then reinjected as `flags.format`. So a per-command `options: [...]` list never validates `--format`; the real allow-list is `RESULT_FORMATS` in `core/src/cli/format-flags.ts`. That is why `--format yaml` was accepted yet silently rendered a table for so long — the only real gate was the positive filter in `appConfigOptionsFromInput`. Keep that filter a positive allow-list: the universal default is `"text"`, which must keep falling through to `?? "table"`.
  - A new static oclif flag is NOT live until `bun run codegen:oclif-manifest` runs. `--help`, and the compiled dual-dispatch flag validation, both read the generated `compiled-manifest.ts`, so an un-regenerated manifest looks exactly like a broken flag. `core/oclif.manifest.json` is gitignored; only `compiled-manifest.ts` and `docs/reference/commands.mdx` are committed artifacts.
  - Order matters in `renderAppConfigResult`: the `get` and write-verb guards must stay ahead of the format branch, or `app config set ... --format yaml` starts dumping a whole Landofile.
  - The shared `invalid_option` remediation ("Supply --flag with one of its declared values.") never names the allowed values. Rather than widen that every-command message for one story, assert discoverability through `--help`; revisit centrally if more flags need it.
---

## 2026-07-25 23:09 CDT - US-468 Review
- Closed the review blockers: `env_file` now loads through the filesystem service into the planned environment with Compose precedence and cache invalidation; labels preserve under `ServicePlan.extensions.compose`; condition-map entries require `condition`; reserved `__proto__` map keys fail instead of disappearing; the authoring schema preserves existing endpoint constraints and is published separately; SDK compatibility and stale canonical-shape tests are reconciled.
- Review notes (deferred/rejected): carrying and honoring dependency conditions in `ServicePlan` remains US-472; capability-checking preserved Compose extensions remains US-473; `raw`/`merged` app-config stages are a broader §8.2.1 contract gap outside US-468's resolved-view criterion; Bugbot reported no bugs.
- Guide-Coverage-Skip: US-468 changes Compose schema decoding and planner normalization, while the executable Compose guide and published key matrix are explicitly owned by US-477; no current owned guide covers this surface.
- Verification: typecheck and lint passed; 540 focused schema, compatibility, planner, cache, loader, app-config render, and generated-artifact tests passed; compose coverage plus renderer, paths, import-cycle, machine-output, state-store, probe, and redaction boundary gates passed; schema codegen was idempotent.
- **Learnings for future iterations:** env-file content and resolved paths are plan-cache inputs, not only Landofile bytes; accepted authoring schemas should use refinement-preserving encoded bounds so unrelated nested constraints survive JSON Schema generation.
---

## 2026-07-25 23:41 CDT - US-468 Review
- Fixed the final review defects: env files now follow Compose inline-comment rules and reject reserved `__proto__`; discovered Landofile app-root provenance survives nested-directory invocation so relative env files cannot bind to the caller's CWD; null environment values fail with the documented host-resolution remediation while null labels canonicalize to empty strings; cache artifacts containing resolved environment values are created owner-only (`0600`).
- Review notes (deferred/rejected): the direct-`ServiceConfig` alias finding was a contract naming ambiguity, so the normative text now names the published `ServiceConfigInput` authoring boundary and canonical `ServiceConfig` output instead of turning the canonical struct into a transform that breaks public schema tooling.
- Review notes (deferred/rejected): chmod migration for old permissive cache files was rejected because v4 is unreleased and has no shipped cache contract; new writes and atomic replacements are `0600`.
- Guide-Coverage-Skip: US-468 changes Compose schema decoding and planner normalization, while the executable Compose guide and published key matrix are explicitly owned by US-477; no current owned guide covers this surface.
- Verification: typecheck and lint passed; focused parser, cache, Landofile, planner, include-resolution, app-resolution, public-schema, and schema-snapshot test files passed; Compose coverage and guide-drift-with-skip gates passed; schema codegen was idempotent.
- **Learnings for future iterations:** loader-only source identity belongs in an Effect-free WeakMap provenance sidecar so canonical schemas and serialized output stay portable; cache files can contain resolved secrets even when their source files are private, so the cache writer must enforce owner-only mode.
---

## 2026-07-26 00:06 CDT - US-468 Drift Audit
- Trigger: 4 cycles, 34,908 changed lines, 13 commits
- Scope absorption: reverted command-name corrections that altered unrelated normative sections and other stories' PRDs.
- Review scar tissue: removed a decode-only public SDK export, dead parser type exports, duplicate source-plumbing tests, and the single-value `--source resolved` path.
- Contract drift: narrowed the story proof to the existing resolved default view instead of partially implementing the frozen `raw | merged | resolved` source selector; dependency conditions remain canonical config only until US-472 carries and honors them.
- Fixed: deleted the partial CLI surface and its generated help/manifest entries while preserving production-path YAML rendering, env-file planning, app-root provenance, cache invalidation, owner-only cache writes, and label preservation.
- Split/excise decisions: excised the separable partial source selector; no follow-up story was added because the complete app-config source-stage contract already exists in spec §8.2.1 outside this Compose-normalization story.
- Verification: typecheck, lint, 12 focused schema/loader/planner/CLI/cache test files, schema/CLI codegen idempotence, Compose coverage, and renderer/paths/state-store/probe/redaction/machine-output/import-cycle boundaries passed.
---
## 2026-07-26 01:35 CDT - Babysit
- PR: #631 https://github.com/AaronFeledy/core4/pull/631
- CI outcome: pass
- Review feedback: fixed (added Guide-Coverage-Skip for Compose alternate-form normalization; mapped guides do not document service field spellings)
- Merge: merged (squash)
---
## 2026-07-26 02:40 CDT - US-469
- Made the Compose `ports`, `expose`, and `volumes` service vocabulary real: both the short-string grammar and the long object form now decode, canonicalize, and normalize into Lando's endpoint and mount models. Grammar moved out of the plugin and into two new SDK schema modules so a single parser serves every consumer; `ServiceConfig.ports` is now a canonical `ComposePortEntry[]`, `volumes` a canonical `ComposeVolumeEntry[]`, and `expose` is a new field. `ServiceConfig` stays a plain `Schema.Struct` (field-level transforms only) and the encoder emits the Compose long form, so `Schema.encode` round-trips.
- Port grammar matches compose-go: `[HOST:]CONTAINER[/PROTO]`, bracketed IPv6, default tcp, numeric-or-string `published`. Container-only and equal-length mapped ranges expand at decode into one endpoint per port; a published range against a scalar target, an unequal-length range, and a long-form ranged `published` all fail with remediation to enumerate mappings, because `EndpointPublication.hostPort` is a single number. `sctp` fails since `EndpointPlan.protocol` has no such member.
- Volume classification per the spec: host-path sources become mounts, named volumes become `<app>-<name>` storage, anonymous volumes get `<app>-<service>-<kebab(target)>` storage, and `type: tmpfs` rides `ServicePlan.extensions.compose.tmpfs` as the preserved runtime knob rather than a `MountPlan` — the Lando `mounts: [{type: tmpfs}]` path is untouched. `subpath` and `createHostPath` have no mount-model field, so they are preserved on `extensions.compose.volumes` rather than dropped.
- Lando-key precedence now actually works: an active `appMount`, any explicit `mounts[].target`, and any explicit `storage[].target` suppress the whole overlapping `volumes:` entry, matched on exact container target (so `/app` does not suppress `/app/web`) and resolved before the append-only feature context is touched. This corrected an inverted precedence: previously a compose-inferred store won and the authored Lando `storage:` entry was skipped.
- Rejected matrix keys stay absent from the accepted schemas and fail closed through the Landofile decode path; the tagged matrix-sourced error surface remains US-475's. Short-volume `ro`/`rw` normalize, `nocopy`/`z`/`Z`/propagation tokens fail naming their matrix key, and unknown tokens are ignored for compose-go parity.
- Verification: typecheck, lint, and `check:compose-coverage` (362 service + 10 top-level, zero matrix edits) passed; ~1000 focused tests green across the new grammar suites and the adjacent schema, planner, Landofile, global-app, provider-emit, and container-runtime files; schema codegen idempotent. Verified end-to-end against a throwaway Compose-spelled app with the compiled binary in the sandbox: `app config get services.web.{ports,volumes,expose} --format json` returned canonical entries (including a 3-port range expansion and an anonymous volume), the YAML view rendered the lawful Compose long form, and `ports.mode` plus a `nocopy` short token both failed with the offending path.
- **Learnings for future iterations:**
  - Landofile decode is NOT single-pass: the six-file merge validates each file and then re-validates the MERGED, already-decoded result with `onExcessProperty: "error"`. Any new `ServiceConfig` field transform whose canonical output keys differ from its accepted input keys (camelCase `readOnly` vs Compose `read_only`) will reject its own output on that second pass. New alternate-form fields must accept their own canonical shape as a pass-through input variant, and should carry an explicit `decode(decode(x)) === decode(x)` test — a plain encode round-trip test does not catch this.
  - Changing a `ServiceConfig` field's decoded type silently breaks test fixtures that build `LandofileShape` as a type-asserted literal instead of decoding YAML; those bypass the schema and only fail at runtime, since root `tsc -b` does not typecheck every test tree. Grep for `ports: ["` / `volumes: ["` after any such shape change.
  - The `*live integration*` service-type suites are serial and env-gated; running them inside a large parallel `bun test <many dirs>` batch makes them fail spuriously. Spot-check individual files, as the repo guidance says.
  - `PUBLIC_FIELD_DESCRIPTION_EXEMPTIONS` needs both halves considered: annotations do not propagate through `Schema.encodedBoundSchema`, so a described `ServiceConfig.<field>` still needs its `ServiceConfigInput.<field>` exemption kept.
---
## 2026-07-26 03:35 CDT - US-469 (completion)
- Verified the in-flight story end to end and closed two defects it still carried. The Compose `ports`/`expose`/`volumes` vocabulary itself was already working: short and long forms canonicalize, ranges expand, tmpfs/subpath/createHostPath survive, Lando keys win over overlapping `volumes:`, and invalid grammar fails with the offending index.
- Fixed silent field loss on canonical re-decode. The accepted-form and canonical-form members of both FROM unions were only distinguishable under `onExcessProperty: "error"`; under Effect's DEFAULT `"ignore"` the Compose-spelling member structurally matched an already-canonical entry, stripped the camelCase keys as excess, and re-defaulted the value. `readOnly: true` silently became `false` (a read-only mount turned writable) and `subpath`, `createHostPath`, `hostIp`, and `appProtocol` were dropped. The `"readOnly" in input` / `"hostIp" in entry` pass-through guards could never fire because the keys were gone before `decode` ran. Union members are now mutually exclusive by construction via `Schema.optional(Schema.Never)` anti-fields for the opposite spelling, so discrimination no longer depends on caller parse options. This was reachable in production: global-service loaders decode plugin-authored `ServiceConfig` objects with default options, so a plugin author writing the canonical TypeScript shape lost their read-only flag.
- Fixed a CI-blocking compatibility gap: `parseShortVolume` was exported from the schema barrel and consumed cross-package by the service plugin but never listed in the additive-exports doc, failing the backward-compatibility surface test.
- Added the missing proof for the host-port capability criterion: a long-form Compose published port now has a test showing it fails with the tagged publication error when the provider cannot publish host ports, and keeps its bind address and host port when the provider can. Both directions are asserted so the test cannot pass vacuously.
- Verification: typecheck, lint, compose-coverage, import-cycle, and deprecation gates passed; 853 focused tests green across schema, compatibility, snapshot, planner, Landofile, global-app, and service-plugin files; schema codegen idempotent across two runs. The regression tests were confirmed RED against the pre-fix source (14 failures, including the readOnly flip) before the fix and GREEN after. Re-ran the compiled binary in the sandbox against a Compose-spelled app: ports, volumes, and expose output is byte-identical to before the fix, confirming the user Landofile path was unaffected.
- **Learnings for future iterations:**
  - An Effect union of an accepted form and its own canonical form is NOT self-discriminating. With the default `onExcessProperty: "ignore"` an earlier member happily matches the canonical object and strips the keys that distinguish it, so any `in`-check inside `decode` is dead code and the value is silently re-defaulted. Make such members mutually exclusive structurally with `Schema.optional(Schema.Never)` anti-fields; do not rely on member order (reordering only moves the bug to the other spelling) and do not use a piped `Schema.filter` (it runs after stripping).
  - Idempotency tests must run under BOTH default and strict options. A `decode(decode(x))` test that only ever passes `onExcessProperty: "error"` hides exactly this class of corruption, because strict mode is what makes the union discriminate at all.
  - Public schemas are decoded with default options by the global-service loaders, not just through the strict Landofile pipeline, so schema-level correctness cannot assume a strict caller.
  - Adding a value export to `sdk/src/schema/index.ts` requires an entry in the additive-exports list in `sdk/API_COMPATIBILITY.md`; type-only exports do not. The surface test names the offending symbol, so trust its diff.
---
## 2026-07-26 04:13 CDT - US-469 Review
- Closed the review blockers: Compose port `name` and `app_protocol` now reach provider-neutral endpoint fields; `volume.subpath` and `bind.create_host_path: false` now reach provider-neutral storage/mount fields and are realized by both structured container API mounts and Compose long syntax; option-bearing mounts cannot also appear in legacy `Binds`. Malformed non-decimal port tokens, incompatible long-volume option blocks, and named matrix-rejected fields now fail under both default and strict schema decoding instead of being coerced or stripped.
- Review notes (deferred/rejected): Bugbot's tmpfs realization finding belongs to US-473 capability checking and US-474 provider realization; US-469 correctly preserves tmpfs under `extensions.compose.tmpfs`.
- Review notes (deferred/rejected): Tagged matrix-sourced rejection and wildcard `x-*` rejection remain US-475; this review made the named rejected keys fail closed without preempting that error surface.
- Review notes (deferred/rejected): No new provider capability was added because `bindMounts` and `persistentStorage` already gate plan fields that both bundled emitters realize; `app_protocol` remains metadata and does not rewrite transport/proxy protocol.
- Guide-Coverage-Skip: US-469 changes Compose service-field normalization, while the executable Compose guide and published key matrix are explicitly owned by US-477; the currently mapped SDK guides do not document these spellings.
- Verification: typecheck, lint, guide-drift with explicit base context, focused SDK/service-planner/container-runtime/provider tests, Compose coverage, import-cycle, renderer-boundary, schema codegen idempotence, and post-fix Bugbot review passed.
- **Learnings for future iterations:** A matrix-`normalized` Compose child must end in a provider-neutral plan field and an emitter, not only survive under `extensions.compose`; accepted long-form option blocks must be discriminated before canonicalization so no type-specific data can disappear during planning.
---

## 2026-07-26 04:44 CDT - US-469 Drift Audit
- Trigger: 3 cycles, 9235 changed lines, 11 commits
- Scope absorption: excised `appProtocol` from internal and Unix endpoint variants while retaining the published-endpoint path required by Compose ports.
- Weakened proofs: restored default/strict decode and encode rejection coverage for type-incompatible canonical volume fields.
- Review scar tissue: removed test-only and module-private helper exports without collapsing the justified parser/classifier module boundaries.
- Contract drift: canonical volume objects can no longer encode bind-only or volume-only options onto the wrong volume type.
- Fixed: narrowed endpoint metadata, rejected incoherent canonical volumes at both schema directions, and deleted dead export surface.
- Split/excise decisions: excised only the unrelated internal-endpoint metadata; no PRD split, passes flip, or gate conflict was required.
- Verification: typecheck, lint, Compose coverage, import-cycle, renderer boundary, 11 focused test files, no-excuse checks, and schema codegen idempotence passed.
---

## 2026-07-26 05:38 CDT - Babysit
- PR: #632 https://github.com/AaronFeledy/core4/pull/632
- CI outcome: pass
- Review feedback: fixed (Traefik ports test expects canonical long form; Guide-Coverage-Skip for internal Compose normalization; reran flaky perf-budget)
- Merge: merged (squash)
---

## 2026-07-26 06:52 CDT - US-470
- Made Compose-shaped `healthcheck:` blocks decode and normalize into the existing §6.7 model. `test:` accepts the shell string, `["CMD", ...argv]` (direct argv), `["CMD-SHELL", cmd]` (string command — deliberately NOT pre-wrapped as `sh -c`, since the runner already wraps strings), and `["NONE"]`; `["NONE"]` and `disable:` both normalize to `kind: none`. `interval`/`timeout`/`start_period` accept Compose duration strings and parse to seconds via a new Go-`time.ParseDuration`-subset parser that preserves fractional seconds (`300ms` → `0.3`) because the runner hands seconds straight to `Duration.seconds`. `retries` accepts a number or a strict decimal-integer string; `disable` accepts a boolean or `"true"`/`"false"` (the vendored schema types it `[boolean, string]` for interpolation), never JS truthiness — `"false"` must not disable a check.
- `start_interval` is matrix-`preserved`, so it is kept as the RAW UNPARSED STRING and lands at `ServicePlan.extensions.compose.healthcheck.start_interval` under its original Compose spelling, mirroring the existing labels merge. Parsing it to seconds would have destroyed the lossless-preservation contract, and it deliberately never reaches `HealthcheckPlan`. The planner's compose-extension merge was factored into a shared helper so labels and healthcheck preservation compose instead of clobbering each other.
- Lando-key precedence is resolved at the FAMILY level, not per key: the presence of any Lando-only canonical key (`kind`, `command`, `url`, `port`, `intervalSeconds`, `timeoutSeconds`, `startPeriodSeconds`) makes the Compose normalized keys be ignored wholesale. A per-key `??` merge would have turned `{command: ["true"], disable: true}` into `kind: none` despite an explicit Lando command. `retries` is a shared spelling and always applies; `start_interval` is preserved whichever family wins.
- Planner defaults stay Lando's 10/5/5 rather than adopting Compose's 30/30/3, so runtime semantics do not depend on which authoring shape a user chose; a test pins that choice. `HealthcheckPlan`, the `HealthcheckRunner`, and the probe boundary are untouched.
- Verification: typecheck, lint, and the compose-coverage, probe-, import-cycle, renderer-, paths-, state-store-, and redaction-boundary gates passed; ~800 focused tests green across the new duration/healthcheck suites and the adjacent schema, public-contract, backward-compatibility, planner, landofile, and healthcheck-runner files; full `bun run codegen` produced zero unrelated drift and the schema snapshot was byte-idempotent across runs. The two planner preservation tests were confirmed RED against the pre-fix planner and GREEN after. Verified end-to-end with the compiled binary in the sandbox against throwaway Compose-spelled apps: `1m30s`→90 and `1h2m3s`→3723, CMD produced argv while CMD-SHELL produced a string, `["NONE"]`/`disable: true`/`disable: "TRUE"` all produced `kind: none`, the mixed block kept the Lando command and interval while preserving `start_interval`, the YAML view round-tripped the canonical keys plus raw `start_interval`, and both invalid-duration and invalid-`disable` apps exited non-zero with their remediation text.
- Deliberately NOT implemented, with ownership recorded: tagged matrix-sourced rejection and wildcard `healthcheck.x-*` rejection remain US-475 (the same boundary the US-469 review drew); capability-checking the preserved knob remains US-473; Compose runtime-default parity is not promised by this story.
- **Learnings for future iterations:**
  - A user-facing remediation message only survives Landofile validation if it starts with the literal prefix `Landofile service`. `core/src/landofile/service.ts` maps every nested parse issue to its bare key path unless the message begins with that string, so a perfectly good remediation silently degrades to `services.web.healthcheck` while every schema test still passes. Schema-level assertions cannot catch this — only running the CLI can. US-468's environment message set the convention; match it in any new schema failure.
  - When an accepted authoring form must tolerate BOTH its Compose and Lando spellings in one object, use a single superset `Schema.Struct`, not a `Schema.Union`. A union has to discriminate, which is exactly the US-469 trap (default `onExcessProperty: "ignore"` lets an earlier member match a canonical object and strip its distinguishing keys), and it cannot express "accept the mix, let one family win".
  - Resolve "the Lando key wins" at the family level whenever the two shapes are semantically entangled. Per-key `??` fallback silently composes halves of two different intents into a third intent nobody authored.
  - A matrix-`preserved` key means the ORIGINAL value under its ORIGINAL spelling. Canonicalizing or parsing it on the way into `extensions.compose` looks harmless and passes `check:compose-coverage` — that gate compares key-path SETS only and stays green even if the value is dropped or mangled, so preservation correctness must be pinned by an explicit assertion.
  - Deriving a schema in a module that `landofile.ts` imports lets the canonical base live there and be re-exported as the public schema, avoiding both an import cycle and a hand-maintained duplicate struct.
---

## 2026-07-26 06:58 CDT - US-470 Review
- Fixed quadratic invalid-duration parsing by anchoring each component match at the current cursor; a long-scalar regression now proves rejection stays within the normal test budget. Documented the additive public healthcheck authoring shape in the SDK compatibility record.
- Guide-Coverage-Skip: US-470 changes Compose healthcheck schema normalization; the mapped SDK guides do not document service healthcheck authoring.
- Review notes (deferred/rejected): Bounding collected healthcheck output is existing `HealthcheckRunner`/provider-exec hardening already reachable through Lando string commands, not introduced by the Compose authoring shape.
- Review notes (deferred/rejected): Practical retry caps, minimum intervals, and an overall probe deadline are existing `HealthcheckInput`/`runProbe` policy questions already reachable through numeric Lando fields; US-470 preserves that runner contract rather than changing it.
- Review notes (deferred/rejected): Cursor Bugbot was unavailable after two attempts because the configured Cursor account was out of usage; no Bugbot findings were available to triage.
- Verification: typecheck and lint passed; focused duration, healthcheck schema, SDK compatibility, planner, Landofile, and healthcheck-runner tests passed; guide drift passed with explicit base and skip context; probe-boundary and Compose coverage gates passed.
- **Learnings for future iterations:** Repeated-component parsers over untrusted Landofile scalars must anchor matching at the current cursor; global unanchored regex scans can turn an otherwise linear validation failure into quadratic work.
---

## 2026-07-26 07:27 CDT - US-470 Drift Audit
- Trigger: 2 cycles, 1898 changed lines, 4 commits
- Weakened proofs: restored a production Landofile-boundary assertion that requires the Compose duration grammar remediation instead of accepting only generic schema guidance.
- Review scar tissue: removed a test-only fixture export, internal schema exports frozen only by tests, and a custom parse-error class used only to alter direct stringification.
- Contract drift: corrected the runner criterion to the existing HealthcheckPlan output; Beta US-374 owns production runner shipping and US-472 owns `service_healthy` ordering.
- Deferred-work laundering: separated pre-existing runner-output hardening from branch-owned validation-message amplification and removed attacker-controlled values from the new errors.
- Fixed: bounded validation failures without truncating valid data, simplified schema tests, clarified canonical `startInterval` encoding, and retained the existing module boundaries because each owns substantive parsing or normalization logic.
- Split/excise decisions: no production split or new story; existing US-374 and US-472 already own the excised runner/readiness claims, and US-470 remains passing on its narrowed normalization contract.
- Verification: typecheck, lint, Compose coverage, probe boundary, import-cycle, no-excuse checks, and focused duration, healthcheck, Landofile, planner, compatibility, and schema-snapshot spot checks passed; the full test suite was not run because this step prohibits it.
---
## [2026-07-26T08:45:31-05:00] - Babysit
- PR: #633 https://github.com/AaronFeledy/core4/pull/633
- CI outcome: pass
- Review feedback: fixed (Guide-Coverage-Skip for schema-only Landofile acceptance; reran Docker Hub pre-pull flake on ubuntu-26.04)
- Merge: merged (squash)
---

## 2026-07-26 10:05 CDT - US-471
- Made `build:` a shape-discriminated authoring surface and deleted the unreleased `composeBuild:` spelling outright. A block carrying Compose image-build keys (`context`, `dockerfile`, `dockerfile_inline`, `args`, `target`) is a Compose build; a block carrying `artifact:`/`app:` is the build-script block; a block mixing the two families fails with a remediation naming both families, the actual offending keys, and the split. The Compose bare-string short form canonicalizes to `{context}`, `args` accepts both the map and the `KEY=value` list (splitting on the FIRST `=`, last duplicate winning), `context` defaults to `"."`, `dockerfile` + `dockerfile_inline` together is rejected, and null `args` values are rejected the same way US-468 rejected null environment values, because Landofiles do not read host environment variables.
- Compose builds now normalize into `ServicePlan.artifact` in the PLANNER rather than the compose plugin, so every service type gets the conversion and the existing `artifactBuild` capability check covers it with no new capability code. The compose plugin's exclusivity rule was rescoped to artifact sources only: `image:` still excludes a Compose `build:`, but the Lando build-script family no longer counts toward it and is no longer blanket-rejected. `build.artifact:` is now actually executed as artifact-phase (`phase: "build"`) steps ordered ahead of `build.app:`; it had been read by nobody.
- `dockerfile_inline` is matrix-`normalized`, so it had to reach a provider-neutral plan field and a real emitter rather than only surviving under `extensions.compose`. `ArtifactBuildSpec` additively gained `specInline`, and the shared container build path realizes it by packing the REAL build context and appending a Lando-owned `.lando.Dockerfile.inline` entry, replacing any colliding user entry, then pointing the daemon's `dockerfile` parameter at it. Copying the derived-build path instead would have shipped a tar containing only a synthesized Dockerfile and silently broken every `COPY` in a user's inline Dockerfile; a test asserts a real context file survives alongside the injected one. `specInline` also joins the artifact build-key inputs — inline content lives outside the context directory digest, so without it an inline-only edit would reuse a stale image.
- Canonical output stays untagged: no `_tag` was added to `build:`, because `ServiceConfigInput` is derived from the canonical schema and a discriminator would leak into the published authoring boundary. The two members are instead made mutually exclusive structurally with `Schema.optional(Schema.Never)` anti-fields, and `context` is the required discriminator that consumers test with `"context" in build`.
- Verification: typecheck, lint, and the compose-coverage, import-cycle, renderer-, machine-output-, paths-, state-store-, probe-, and redaction-boundary gates passed, along with lint:guides, guide-coverage, and public-transcripts; 189 focused tests across the new build-block/artifact suites and the adjacent container-runtime, build-key, planner, Landofile, scratch-run, compose-plugin, backward-compatibility, and plugin-abstraction files; `bun run codegen` was byte-idempotent across two runs and the bootstrap-layer, OCLIF-manifest, workflow, and vendored-schema outputs were untouched. `check:compose-coverage` stayed green with NO matrix edit, confirming the shape matches the committed dispositions. Verified end-to-end with the compiled binary in the sandbox against throwaway apps: an authored `{dockerfile, args: [FOO=bar=baz]}` resolved to `{"context":".","dockerfile":"Dockerfile","args":{"FOO":"bar=baz"}}`, a mixed-family block surfaced the full remediation as a tagged validation error, an authored `composeBuild:` was rejected by name, `image:` alongside a Lando-family `build:` resolved to the script block with no `context`, and `dockerfile_inline` round-tripped through the YAML view back to its Compose spelling.
- Deliberately NOT implemented, with ownership recorded: per-service `build: { failFast, concurrency }` orchestration overrides (spec §6.13.1). They are a third, orthogonal key family — policy rather than artifact-or-script — and honoring them needs BuildPlan work. The discriminated union REJECTS them today rather than accepting and ignoring them, so the nonconformance is loud; the 4.1 build-orchestration wave owns it.
- **Learnings for future iterations:**
  - The Landofile YAML parser has NO block-scalar (`|`/`>`) support: `dockerfile_inline: |` fails with `Malformed YAML indentation`, while the quoted `"FROM ...\nRUN ..."` form works. This is pre-existing (`sdk/src/landofile/parser.ts` is untouched by this story) but US-471 makes it load-bearing, because spec §6.2's own canonical example authors `build.artifact:` and `build.app:` AS block scalars. The spec's documented authoring form for the build-script family therefore cannot currently be parsed. Fixing it means teaching the shared minimal parser block scalars — a wide regression radius that belongs in its own story, not here.
  - Annotate an optional public field on the PropertySignature (`Schema.optional(X).annotations({description})`), never on the inner schema (`Schema.optional(X.annotations({...}))`). The public-schema gate reads the property's own annotations, so the inner form is invisible to it and fails `codegen:schema-snapshot` with a "Missing field description annotation" error that never surfaces in schema unit tests.
  - `dist/schemas/*.json` are TRACKED generated artifacts — `.gitignore` deliberately un-ignores them — and CI diffs them alongside the snapshot and reference docs. A schema change to a widely embedded type like `ArtifactBuildSpec` rewrites ~19 of them transitively through `AppPlan`/`BuildPlan`/`ServicePlan`/events; regenerate and commit them or CI fails after a locally green run.
  - The schema snapshot couples otherwise-independent schema changes into one commit: an additive field and a field replacement both land in that single generated file, so splitting them produces an intermediate commit whose snapshot test fails. Prefer one honest commit over a broken bisect point.
  - A union that fails validation emits one issue per member, so a mixed-family error legitimately appears alongside bare dotted key paths from the sibling member. Assert that the remediation issue EXISTS and is well-formed; asserting that no dotted path appears anywhere is over-strict and will fail for a correct implementation.
---

## 2026-07-26 10:56 CDT - US-471 Review
- Closed three acceptance blockers: every vendored depth-one Compose build key now participates in family detection before Effect can discard excess properties; an authored Compose build now overrides type-seeded artifact defaults and always reaches the planning-time `artifactBuild` capability check; and authored `build.artifact` scripts now use the canonical build-step command shape consumed by the container runtime.
- Corrected mixed-family remediation to offer a valid split through `image:` rather than directing users back to Compose keys in the same `build:` block. Rejected Compose-only build keys fail closed under default and strict decoding; matrix-sourced `ComposeKeyRejectedError` remediation remains owned by US-475.
- Guide-Coverage-Skip: US-471 changes Compose service build normalization; mapped SDK guides do not document service build authoring.
- Review notes (deferred/rejected): `image:` plus a Compose build remains a tagged conflict for parity; using `image:` as the output tag of a build would require a new artifact-model contract outside US-471.
- Review notes (deferred/rejected): Build-context path escape semantics and multi-line script/block-scalar limitations predate this branch; the security lane found no new exploit boundary, and the existing progress entry already assigns YAML block scalars to the shared parser follow-up.
- Review notes (deferred/rejected): Per-service `failFast`/`concurrency` policy remains the later 4.1 orchestration wave; Cursor Bugbot produced no report because the configured account was out of usage.
- Verification: typecheck and lint passed; 581 focused tests across 16 SDK, loader, planner, runtime, plugin, snapshot, compatibility, and contract files passed; Compose coverage, import-cycle, guide lint/coverage/public-transcript, and explicit-base guide-drift gates passed; schema codegen was hash-idempotent.
- **Learnings for future iterations:** Effect transforms that discriminate key families must include rejected and extension keys in their input schema; otherwise default excess-property stripping can silently turn a mixed block into the wrong valid family before decode logic runs.
---

## 2026-07-26 11:23 CDT - US-471 Review
- Fixed the post-review public-schema regression: the build input's `propertyNames` constraint now permits every declared build key plus `x-*` instead of applying the extension-only pattern to ordinary keys such as `context` and `artifact`; runtime schema generation and committed JSON Schema artifacts are synchronized.
- Strengthened the matrix-totality proof so every rejected depth-one build key also fails as a Compose-only block under default and strict Effect decoding, not only when mixed with Lando scripts.
- Verification: typecheck and lint passed; 582 focused tests across 16 schema, loader, planner, runtime, plugin, snapshot, compatibility, and contract files passed; Compose coverage, import-cycle, and explicit-base guide-drift gates passed; schema codegen was hash-idempotent.
- **Learnings for future iterations:** Extending a Struct with a template-key Record emits `propertyNames` for the whole object in draft-07; derive an explicit property-name union from the declared field map plus the template pattern, and test representative generated-schema names rather than trusting a snapshot alone.
---

## 2026-07-26 12:03 CDT - US-471 Drift Audit
- Trigger: 3 cycles, 6226 changed lines, 4 commits
- Weakened proofs: published JSON Schema now exposes closed, mutually exclusive accepted build families while runtime-only rejected keys remain available for total family detection; planner and loader assertions now cover every normalized artifact field and the tagged boundary error.
- Review scar tissue: `ArtifactBuildSpec` makes `spec` and `specInline` mutually exclusive, allowing the duplicate container-runtime validation and its invalid-state test to be deleted.
- Ceremony modules: the one-importer Compose build-key inventory was inlined and its forwarding export surface removed; the pure artifact mapper remains justified as the planner's single conversion boundary.
- Collateral edits: removed the unrelated pre-existing `contentHash` build-key repair and its tests, retaining only US-471's required `specInline` identity.
- Contract drift: regenerated public schemas and reference docs now match runtime acceptance instead of advertising mixed or rejected build shapes; removed unused public build-shape aliases.
- Deferred-work laundering: fixed the branch-owned inline-Dockerfile ancestor-symlink archive collision with a private root-level owned filename rather than deferring it as generic context handling.
- Fixed: deleted one module, dead public aliases, duplicate runtime validation, invalid-state coverage, and unrelated serializer behavior; strengthened production-path proofs without widening the story.
- Split/excise decisions: no follow-up split; `passes` remains true because all confirmed branch-owned drift was corrected and Review-required rejected-key transport detection remains intact.
- Verification: typecheck, lint, Compose coverage, import-cycle, schema codegen hash-idempotence, and 228 focused tests across 11 schema, compatibility, loader, planner, runtime, CLI, and plugin files passed.
---

## 2026-07-26 13:08 CDT - Babysit
- PR: #634 https://github.com/AaronFeledy/core4/pull/634
- CI outcome: pass
- Review feedback: fixed (Guide-Coverage-Skip for alpha-4-11 SDK schema trigger; compose-passthrough already covers US-471)
- Merge: merged (squash)
---

## 2026-07-26 14:10 CDT - US-472 (foundation, story IN PROGRESS - passes remains false)
- Landed the type-level foundation the rest of the story compiles against: dependency conditions now survive into the plan instead of being flattened at the plugin boundary. `DependencyPlan` carries the normative Compose vocabulary (`service_started` | `service_healthy` | `service_completed_successfully`) plus a required `required: boolean`, replacing the old `"started" | "healthy"` pair that had no way to express completion or optionality. The condition literal is now a single shared schema imported by both the authoring shape and the plan shape, so the two can no longer drift.
- Moved dependency normalization out of the 21 service types and into the planner. Every `service-lando` type previously ran an identical loop that hard-coded `condition: "started"` and discarded the authored condition, `required`, and `restart` — so a user's `condition: service_healthy` was silently downgraded before planning ever saw it. A single planner pass now resolves the defaults (absent condition to `service_started`, absent `required` to `true`), merges authored entries over feature-contributed ones in place, and preserves `restart` losslessly (including an explicit `false`) on the compose extension per its matrix disposition. Third-party service types get correct behavior without reimplementing it.
- Added `RuntimeProvider.waitForExit` as a mandatory provider primitive returning the service exit code, with a real container-wait implementation for provider-lando and a deterministic zero-exit implementation on the test provider. Waiting for completion is an operation, not point-in-time inspect metadata, and making it mandatory keeps normalized `service_completed_successfully` support provable across providers rather than optional; scriptable test-provider outcomes remain part of the orchestrator proof still owed below. The Compose emitter now passes the condition through verbatim and emits `required`.
- Verification: typecheck, lint, import-cycle passed; schema snapshot regenerated and idempotent across two runs with clean `dist/schemas` diff; SDK contract, app-planner, compose-emit, and the full service-lando suites passed; zero legacy `condition: "started"/"healthy"` literals and zero `addDependency` loops remain. The three behavior-bearing changes were confirmed RED before implementation and GREEN after.
- REMAINING for this story (branch `us-472-depends-on-conditions`, plan is fixed and should not be re-derived):
  - Planner validation: absent target + `required: true` fails; absent + optional keeps the edge and creates no gate; `service_healthy` onto a service with no healthcheck (or `kind: none`) fails regardless of `required`, via `LandofileValidationError` on path `services.<dependent>.dependsOn`. Must be proven by running the real CLI, not only unit tests.
  - App-build DAG: internal-only `AppNode` union with gate nodes `<svc>:running` / `<svc>:healthy` / `<svc>:completed`, deduplicated by id but with `required` held EDGE-LOCAL.
  - App-build runner: realize gates (healthy through the existing healthcheck runner, completed through `waitForExit`), blocked steps produce one real `BuildStepFailedError` each under continue-all.
  - Provider start ordering: `bring-up.ts` still starts `Object.values(plan.services)` unordered; `apply` must honor the conditions topologically.
  - Deterministic orchestrator suite covering all three conditions via the test provider.
- **Learnings for future iterations:**
  - The Compose emitter is NOT the start path. `plugins/provider-lando/src/compose.ts` is an export surface only; services are actually started by the unordered loop in `bring-up.ts`. Fixing `depends_on` emission looks like it fixes start ordering and does not — verify which code path actually runs before assuming Compose semantics are inherited from the engine.
  - `provider-lando` may import `@lando/core/<subpath>` but the healthcheck runner is not an exported subpath, so provider-side health waiting must build on `@lando/sdk/probe`'s `runProbe` rather than reaching into core internals.
  - Optionality on a deduplicated dependency graph belongs on the EDGE, not the node. Two dependents can share one failed `<db>:healthy` gate while one is required and the other optional; storing `required` on the gate collapses that distinction and silently blocks a service the user marked optional.
  - Widening a plan schema that plugins construct is cheapest to close with the compiler: implement the shared test double first, then let `bun run typecheck` enumerate every remaining hand-rolled provider literal instead of grepping for them twice.
  - A `preserved` disposition means the original value under its original spelling, and that includes explicit `false`. Dropping falsy preserved values passes the coverage gate (it compares key-path sets only) while losing user intent.
---

## 2026-07-26 15:45 CDT - US-472 (substrate + planning validation; story IN PROGRESS - passes remains false)
- Landed the planning-time half of the story and the shared substrate the orchestration half consumes. `depends_on` conditions are now rejected at plan time when they cannot possibly be satisfied, instead of being carried into a runtime that would silently ignore them.
- Planner validation (new `dependency-validation` module, pure, called from the planner at BOTH the cache-hit and freshly-assembled sites): a required edge naming an absent service fails; an absent target with `required: false` keeps its edge and plans clean; a `service_healthy` edge onto a target with no healthcheck (or `kind: none`) fails regardless of `required`, because optionality means a dependency may be missing or fail, not that an unsatisfiable condition becomes valid; and dependency cycles now fail, including a cycle that closes through an optional edge and a self-edge. Validating on the cache path matters — without it a plan cached before this change would bypass validation entirely.
- Added a generic, pure, deterministic settled-outcome DAG scheduler to `@lando/container-runtime`, where the remaining core and provider orchestration work can share it without introducing another dependency direction. Readiness is "every predecessor SETTLED", not "succeeded", which is what lets an optional dependent proceed after its dependency fails; `required` lives on the EDGE so two dependents can share one deduplicated gate node with different optionality; nodes run once per id, in lexicographic order within a wave for reproducible diagnostics; cycles return a typed result rather than hanging. Waiting/retrying is deliberately the caller's job, so the module adds no retry loop of its own.
- Replaced provider-docker's failing `waitForExit` stub with a real Docker `/containers/{id}/wait` implementation. `waitForExit` is a mandatory provider primitive, so a stub that always fails was the actual defect; this also removed the need for the `serviceExitWait` capability an earlier design called for — a capability flag for a method the interface already requires is a shim, and adding one would have churned ~20 capability fixtures and 310 generated schema files for no behavioral gain.
- Verification: typecheck, lint, import-cycle, probe-boundary, redaction-boundary, and compose-coverage gates passed; 165 focused tests green across the scheduler, the extracted healthcheck factory and live wiring, app-planner, and provider-docker wait/capability files. Real CLI evidence via `lando info`: the un-healthchecked `service_healthy` case went from exit 0 (before) to exit 1 with its full remediation and `issues: services.web.dependsOn`; absent+required exits 1; absent+`required: false` exits 0; an optional-edge cycle exits 1 naming the cycle path.
- REMAINING for this story (design is FIXED, do not re-derive; the scheduler API is frozen and already green):
  - App-build gate graph in core: internal-only `AppNode` union with gate nodes `<svc>:running` / `<svc>:healthy` / `<svc>:completed`, deduplicated by id with `required` held EDGE-LOCAL. Gates must stay out of `BuildPlan.steps`/`totalSteps`, `TaskTreeStartEvent.children`, and every `build-step-*` event, and must never produce a `BuildStepFailedError`.
  - App-build runner: realize `healthy` through `makeHealthcheckRunner({ exec })` (already extracted for exactly this) and `completed` through `Effect.scoped(provider.waitForExit(...))`; the `running` gate must resolve WITHOUT inspecting state, or a completed one-shot dependency wrongly fails. Blocked steps produce exactly one `BuildStepFailedError` each under continue-all, and the blocked check must stay ahead of the build-results cache lookup so a cached step cannot bypass a newly failed gate.
  - Provider start ordering: `bring-up.ts` still starts `Object.values(plan.services)` unordered; both providers must drive the scheduler, and an OPTIONAL gate failure must not enter `rollbackPartialApply`.
  - Surface proof still owed for health-gated start order, non-zero-exit continue-all aggregation, and shared-gate optionality.
- **Learnings for future iterations:**
  - The dev CLI entry is `core/bin/lando.ts`. Running `core/src/cli/index.ts` directly prints ZERO bytes and exits 0 — it is the cold-start pre-renderer, not a runnable entry — which looks exactly like a silently broken command and can burn a lot of time.
  - `lando app config` never invokes the planner (it renders the resolved Landofile), so it cannot prove any planner validation. `lando info` does invoke the planner and fails before any provider work, making it the cheapest host-safe planning surface — no container runtime needed.
  - The minimal Landofile YAML parser supports neither block scalars (`|`, `>`) nor inline/flow objects (`{a: b}`). A fixture using flow style dies with `LandofileParseError` "Inline objects are not supported" before any planner code runs, so a validation test can appear to fail for the wrong reason.
  - Pin fixtures to a SUPPORTED service-type version (`node:22`, not `node:20`); the unsupported-type check fires first and masks the assertion actually under test.
  - Neither bundled provider emits a container-level healthcheck, so provider-lando's `waitForServiceHealth` treats every Lando container as green (`health === undefined` counts as healthy). It therefore cannot gate on a Lando `HealthcheckPlan`; `service_healthy` must be realized by executing the plan's `kind: command` check through the provider exec channel.
  - Extract a provider-bound factory (`makeHealthcheckRunner({ exec })`) instead of depending on a `HealthcheckRunner` Layer when the consumer selects its provider at runtime; a sibling layer would hit the documented `Effect.serviceOption` visibility problem.
---

## 2026-07-26 15:20 CDT - US-472 Drift Audit
- Trigger: 2 cycles, 2948 changed lines, 4 commits
- Weakened proofs: replaced the Docker test that blessed a redundant post-wait inspect with production-path assertions that both providers return the authoritative wait response's exit code after one request.
- Review scar tissue: removed the second container inspect from Docker and Podman `waitForExit`; the extra request duplicated the wait response and could lose an auto-removed container between calls.
- Contract drift: corrected earlier claims that the scheduler was already consumed by core/providers and that the test provider already supplied scriptable exit outcomes; both remain explicit inputs to the unfinished orchestration proof.
- Fixed: simplified both provider wait paths, added the missing focused provider-lando proof, and retained the scheduler, validation, healthcheck factory, and mandatory provider primitive because each traces directly to this story's remaining acceptance criteria.
- Split/excise decisions: none; no adjacent public contract or other story was absorbed, and US-472 remains `passes: false` until gate graphs, provider ordering, and deterministic all-condition orchestration tests land.
- Verification: typecheck, lint, import-cycle, probe-boundary, redaction-boundary, renderer-boundary, state-store-boundary, paths-boundary, compose-coverage, schema codegen idempotence, SDK compatibility/schema spot checks, and focused scheduler/planner/healthcheck/provider tests passed.
---

## 2026-07-26 16:35 CDT - US-472 (completion)
- Closed the story: `depends_on` conditions now actually gate orchestration instead of only surviving into the plan. Both halves the previous passes left open are done — the core app-build DAG and both bundled providers' start ordering — and the three conditions are proven deterministically through the test provider.
- Introduced one shared gate vocabulary (`<svc>:running` / `<svc>:healthy` / `<svc>:completed`) in `@lando/container-runtime`, so core and the providers address the same synthetic nodes and cannot drift. A repo-wide grep confirms those literals exist in exactly one module.
- App-build: the runner no longer walks hand-rolled topological batches. A new internal graph builder emits step nodes plus deduplicated gate nodes and hands the whole thing to the frozen settled-outcome scheduler. Optionality is EDGE-local, so one shared `db:healthy` gate blocks a required dependent while an optional dependent proceeds past the same failure — proven by a test asserting the dependency healthcheck ran exactly once. Gates are invisible: they never appear in the task tree's children, never move the succeeded/failed counts, never emit a `task.*` or `build-step-*` event, and never produce a `BuildStepFailedError`. Cycle detection stays PRE-FLIGHT (a gate has no predecessors, so the step-only check remains complete), preserving the existing "zero provider calls on a cyclic plan" guarantee and the exact edge-string format.
- Gate realization in core: `service_started` resolves without inspecting provider state (inspecting would wrongly fail a one-shot dependency that already exited — a test pins zero `inspect` calls); `service_healthy` runs the extracted `makeHealthcheckRunner({ exec })`; `service_completed_successfully` runs `waitForExit` and fails the gate on a non-zero exit. A blocked step yields exactly one `BuildStepFailedError` under continue-all while independent siblings still run, and the blocked check stays ahead of the build-results lookup so a cached step cannot bypass a newly failed gate.
- Provider start ordering: `bring-up` in provider-lando (and therefore provider-podman) and the private `bringUp` in provider-docker both drive a shared start schedule instead of iterating `Object.values(plan.services)`. Services start behind the gates their dependents demand; a required unmet gate fails apply with a tagged `ServiceStartError` naming both the blocked service and the gate, and rolls back. An optional unmet gate does NOT block anyone, so apply succeeds and rollback never runs — the case both providers now have an explicit test for.
- Only `kind: "command"` healthchecks are verifiable through the provider exec channel, so any other healthcheck shape resolves its gate immediately rather than blocking on a condition neither surface can observe. Planning already rejects `service_healthy` onto a service with no enabled healthcheck, so this only affects `http`/`tcp` shapes.
- Verification: typecheck, lint, and the import-cycle, probe-, renderer-, redaction-, state-store-, paths-boundary and Compose-coverage gates passed; 225 focused tests green across the new gate/graph/schedule/orchestration suites and the adjacent planner, healthcheck, build-orchestrator, and provider files, with the frozen scheduler module byte-identical. Every new suite was confirmed RED before implementation and GREEN after. Real CLI evidence: a Compose-spelled app declaring `condition: service_healthy` plus an optional `service_completed_successfully` planned cleanly (`lando info` exit 0) and `lando app config get services.web.dependsOn` round-tripped both conditions and the explicit `required: false`, while the unsatisfiable `service_healthy` app still exits 1 with its full remediation.
- No public schema, SDK compatibility record, or generated artifact changed; the gate graph is entirely internal.
- **Learnings for future iterations:**
  - The settled-outcome scheduler detects cycles MID-FLIGHT, after running whatever acyclic prefix exists. Any surface that promises "nothing ran" on a cyclic plan (the app-build runner does, and a test pins zero provider calls) must keep its own pre-flight check; swapping in the scheduler alone silently breaks that guarantee.
  - `AppPlan.services` is keyed by a BRANDED `ServiceName`, so `plan.services[someString]` is a typecheck error. Build a `Map<string, ServicePlan>` from `Object.values(...)` keyed by `String(service.name)` rather than reaching for `ServiceName.make` on every lookup.
  - "No gate events at all" is the wrong invariant to assert. A BLOCKED step legitimately emits its own `build-step-skip { reason: "phase-aborted" }`, so the real assertion is that every emitted build-step event carries a real step's `buildKey` — asserting an empty event list fails against a correct implementation.
  - Readiness in the scheduler is "every predecessor SETTLED", not "succeeded", which is exactly what lets an optional dependent run after its gate fails. It also means an optional dependent still WAITS for the gate to finish; optionality changes the verdict, never the ordering.
  - Provider-side health probing cannot reuse `makeHealthcheckRunner` (not an exported core subpath), so the shared start-schedule module owns one `runProbe`-based probe that both providers inject their own `exec` into. Injecting the primitives rather than re-implementing the loop per provider is what keeps `check:probe-boundary` green.
---

## 2026-07-26 16:41 CDT - US-472 Review
- Closed the review blockers: dependency gates now fail closed for healthcheck shapes the runtime cannot observe; optional-only dependency start failures settle without aborting dependents or rolling back unrelated services; synthetic schedule nodes use disjoint internal namespaces while diagnostics retain the documented gate labels; dependency waits carry cancellation end to end; rollback preserves pre-existing containers; and dependency lookup is safe for prototype-shaped service names.
- Guide-Coverage-Skip: US-472 changes internal dependency-gate scheduling and provider ordering; the mapped guides do not document depends_on orchestration, and the executable Compose guide is owned by US-477.
- Review notes (deferred/rejected): Cursor Bugbot was unavailable because the configured account exhausted its usage, so no Bugbot findings were available to triage.
- Review notes (deferred/rejected): Replacing the settled scheduler's wave batching with continuously released readiness was rejected as a non-blocking performance refactor; it does not change US-472 ordering correctness and would exceed this review's scope.
- Verification: typecheck and lint passed; 56 focused dependency-gate, app-build, provider-ordering, wait-for-exit, SDK-compatibility, and prototype-lookup tests passed; guide drift passed with explicit base context and the skip above; probe-boundary and import-cycle gates passed. LSP diagnostics were clean on smaller changed modules; four large modules timed out after two attempts but passed root typecheck.
- **Learnings for future iterations:** Synthetic DAG nodes need type-disjoint internal ids even when user-facing labels are frozen; optional-failure recovery must preserve Effect interruption and clean only resources owned by the failed node, leaving whole-apply rollback to the fatal boundary.
---

## 2026-07-26 17:05 CDT - US-472 Drift Audit
- Trigger: 5 cycles, 5554 changed lines, 18 commits
- Weakened proofs: added a production-adapter cancellation proof that failed before the Podman completion-wait signal was forwarded and passed after the fix.
- Review scar tissue: removed a no-op provider branch, unused rollback result metadata, and test-only healthcheck aliases left by iterative repairs.
- Ceremony modules: removed an unused root-package scheduler re-export while retaining the three subpaths consumed by production orchestration.
- Collateral edits: narrowed the cancellation option to the completion-wait operation and reverted unrelated remediation added to existing stub failures.
- Contract drift: corrected the provider compatibility record to describe the mandatory wait primitive as a pre-ship shape change rather than an additive interface change.
- Fixed: restored completion-wait cancellation end to end and simplified provider adapters and healthcheck wiring without changing gate behavior.
- Split/excise decisions: excised only dead exports and aliases; no adjacent feature or incomplete public contract required a follow-up story, and US-472 remains passing.
- Verification: typecheck, lint, 17 focused test files, import-cycle, probe-boundary, Compose coverage, SDK compatibility, and changed-file diagnostics passed; two post-edit LSP refreshes timed out but root typecheck remained green.
---

## 2026-07-26 17:20 CDT - US-472 Verify
- Compiled `core/dist/lando`; sandbox `setup --yes --no-interactive` completed after installing noble-era `libgpgme11t64` + `libassuan0` (Ubuntu 26.04 only ships `libgpgme.so.45`; bundled Podman 6 needs `.so.11`). Doctor shows runtime socket reachable.
- Real compiled-binary planning/config scenarios green: `service_healthy` without healthcheck exits 1 with remediation; missing required dep exits 1; optional missing dep plans; multi-condition `service_healthy` + optional `service_completed_successfully` plans and `app config get services.web.dependsOn` round-trips both conditions; optional-edge cycle fails; disabled-healthcheck + optional healthy fails; list-form depends_on plans. Adjacent smoke: `--version`/`--help`, `list`, `doctor`, simple-app `info`/`config`, `start`/`stop`/`destroy` help.
- Live `lando start` still fails in this sandbox on netavark/aardvark (`user scope bus` / broken `user@1000.service` "Device or resource busy" since distro bootstrap) — pre-existing environment gap, not a US-472 orchestration defect. Condition ordering remains covered by the test-provider orchestrator suites from Build/Review.
- **Learnings for future iterations:** Sandbox container networking needs a working systemd user session for aardvark-dns; installing matching SONAME host libs for the managed Podman bundle is required on Ubuntu 26.04 before full provider setup; YAML `command: true` becomes boolean — quote healthcheck/service commands as strings for Landofile fixtures.
---

## [2026-07-26T19:11:46-05:00] - Babysit
- PR: #635 https://github.com/AaronFeledy/core4/pull/635
- CI outcome: pass
- Review feedback: fixed (native Podman wait endpoint; guide coverage + skip; Docker/Podman/Lando wait fixtures and completing-service race)
- Merge: merged (squash)
---

## 2026-07-26 20:22 CDT - US-473 (shapes + capability surface + pure validator; story IN PROGRESS - passes remains false)
- Landed the authoring surface and the capability contract for the preserved runtime-knob tier. All 24 spec §6.2 knobs (`restart`, `cap_add`, `cap_drop`, `privileged`, `devices`, `ulimits`, `sysctls`, `tmpfs`, `shm_size`, `dns`, `dns_search`, `dns_opt`, `extra_hosts`, `init`, `stop_signal`, `stop_grace_period`, `security_opt`, `group_add`, `read_only`, `platform`, `pull_policy`, `logging`, `gpus`, `deploy.resources`) now decode on `ServiceConfig` in every form Compose defines, under their VERBATIM Compose spellings. These keys have no Lando abstraction by spec fiat, so inventing camelCase canonical names plus snake aliases would have manufactured a Lando sugar surface the story explicitly forbids — and a second mapping back to Compose before anything could consume them. Canonicalization applies to VALUES only: device strings become long objects, integer `ulimits` become `{soft, hard}`, list-form `sysctls`/`extra_hosts` become maps, scalar-or-list fields become arrays, byte strings become integer bytes, durations become seconds.
- Resolved the story's central contract conflict rather than papering over it. Spec §5.4 published `composeSpec` as a bare tier literal and §5.5.1 step 4 checked capability at that tier, but the story requires per-knob rejection naming the offending key. Deriving per-knob behavior from the tier alone would have made "the test provider declares a partial knob set" unsatisfiable and "the declaration shape is published in `@lando/sdk`" vacuous. Widening `composeSpec` itself into a struct would have been a breaking replacement of a published literal. Instead the tier is retained untouched and gains an optional, fail-closed refinement `composeKnobs: { supported }`, following the existing structured `hostProxy` sub-capability precedent. Spec §5.4/§5.5.1 and the PRD index were amended in the same change, so normative text and code stay reconciled.
- Fail-closed is the whole safety property: an absent `composeKnobs` is exactly `{ supported: [] }`, and a knob is supported only when `composeSpec` is `native` AND the exact path is declared. Because the tier and the detail map could otherwise disagree silently, the provider contract suite now REJECTS a non-empty `supported` set at any tier other than `native`; an inconsistent declaration fails loudly instead of being quietly ignored. `composeKnobs` is optional rather than required deliberately — 51 in-repo files construct `ProviderCapabilities` literals, and a required field would have turned an additive story into a breaking sweep that buys no extra safety once absence already means "supports nothing".
- Added a new byte-size parser (the repo had none) and widened it to the real Compose grammar. The vendored schema carries no byte pattern, only prose, so the authority is compose-go's delegation to Docker `go-units.RAMInBytes`: decimals (`1.5g`), `t`/`p` suffixes, an optional single space, and the `i` infix (`Ki`, `mib`) are all legal. A stricter integer-only parser would have rejected valid user input and only surfaced as a conformance failure later.
- Added a pure core module (`compose-knobs.ts`) that merges knobs into `ServicePlan.extensions.compose`, collects the knobs in use, and resolves support deterministically. Collection scans the FINAL extension rather than the authored config, so a knob injected by a plugin — the `tmpfs` that long-form `volumes: type: tmpfs` produces — is gated too. Ordering is stabilized by service name then by the published knob-key literal order, so the reported first failure never depends on object key insertion order.
- Verification: typecheck, lint, and the compose-coverage (362 service paths, unchanged matrix), import-cycle (939 modules), machine-output, and deprecation gates passed; 232 focused tests across the knob-shape, byte-size, parity, provider-capability, Landofile, core knob-module, provider-contract, and schema-snapshot files passed, each suite confirmed RED before implementation. `codegen:schema-snapshot` was proven byte-idempotent across two consecutive runs (identical checksums), and the regenerated snapshot, `dist/schemas`, and reference docs are committed with it.
- REMAINING for this story (design is FIXED and should NOT be re-derived; the SDK surface and the pure core module are frozen and green):
  - `core/src/services/planner.ts`: call the knob merge from the existing compose-extension writer (~:772-813) so authored knobs actually reach `ServicePlan.extensions.compose`. Until this lands, authored knobs validate but are INERT.
  - `core/src/services/planner.ts`: run the capability gate on BOTH plan paths — immediately before the CACHED plan returns (~:1204-1211) and after `decodeAppPlan` on the fresh path (~:1575) before the cache write. A plan cached under a permissive provider must not bypass a restrictive one.
  - `core/test/services/app-planner.test.ts`: accept and reject cases asserting `CapabilityError` carries `{ service, key, capability: "composeSpec", providerId, remediation }`; the cache-bypass regression test; and the plugin-injected `tmpfs` rejection case.
  - Real-CLI proof (`lando info`) that a supported knob plans clean and an undeclared knob exits non-zero with its remediation; `lando app config get` round-trip of a canonicalized knob.
- **Learnings for future iterations:**
  - `Schema.encodedBoundSchema` does NOT carry a PropertySignature's `description` across for a TRANSFORM-backed field — it surfaces the transform's encoded side. So annotating only the field leaves `ServiceConfigInput.<key>` undescribed and `codegen:schema-snapshot` fails the public-schema gate for exactly the multi-form fields while the plain ones pass. The fix is the pattern the existing `ComposeEnvFileInput`/`ComposeLabelsInput` fields already use: annotate the transform schema ITSELF as well as the property. Plain fields never reveal this, so the failure looks arbitrary.
  - A capability tier and a per-key detail map are two sources of truth for one question. Whenever both exist, add a contract assertion that rejects the disagreement; otherwise the narrower declaration is silently ignored and a provider appears to support something it does not.
  - The vendored Compose schema is a coverage oracle, not a grammar oracle. Several value grammars (byte sizes especially) exist only as prose in the vendored file, so the real authority is compose-go's Go implementation. Pattern-matching the JSON Schema alone yields a parser stricter than Compose.
  - Adding an optional capability field costs nothing across the ~51 `ProviderCapabilities` construction sites; making it required would touch all of them. Prefer optional-plus-fail-closed-default over required whenever "absent" has an honest, safe meaning.
---

## 2026-07-26 20:44 CDT - US-473 Drift Audit
- Trigger: 1 cycle, 16358 changed lines, 1 commit
- Weakened proofs: planner merge, dual-path capability gating, and CLI failure-envelope criteria remain isolated-seam or unproven; the story correctly remains non-passing until production wiring and real-CLI proof land.
- Ceremony modules: removed the dead aggregate schema and forwarding export fanout; retained the substantive pure core helper because it maps directly to the still-open planner acceptance criteria.
- Contract drift: narrowed the public SDK surface from 39 knob-schema values to the two documented capability declarations and corrected byte-size remediation to match the accepted decimal, tera/peta, and IEC grammar.
- Fixed: deleted accidental public/dead exports, kept the parity inventory internal, and restored accurate user remediation without changing schema output.
- Split/excise decisions: no story split; only accidental public exposure was excised. US-473 `passes` remains false pending planner merge, cached/fresh capability gates, app-planner regressions, and real-CLI proof.
- Verification: typecheck, lint, schema-snapshot codegen idempotence, Compose coverage, import-cycle, machine-output, all five changed test files, SDK compatibility, and public-schema spot checks passed; the full test suite and container/provider integration were not run per Drift Audit host-safety rules.
---

## 2026-07-26 21:55 CDT - US-473 (completion)
- Closed the story: authored Compose runtime knobs are no longer inert. The planner now merges the canonicalized knob set into `ServicePlan.extensions.compose` and refuses to plan when the selected provider has not declared support for a knob actually in use, so nothing is silently dropped.
- Preservation seam: the knob merge composes OVER the existing compose-extension writer at its single call site rather than being folded into it. That writer early-returns for any service without labels, a healthcheck `startInterval`, or a dependency restart — i.e. the common case — so merging inside it would have silently swallowed knobs for most services while still passing a naive unit test.
- Capability gate runs on BOTH plan paths through one shared helper: immediately before a cached plan is returned, and after the fresh plan decodes but before it is written to cache. This is load-bearing rather than defensive: the app-plan cache key deliberately does not hash provider capabilities, so the same Landofile under a permissive and a restrictive provider derives an identical key. Without the cached-path gate a plan cached under a permissive provider is served verbatim to a restrictive one. A test proves the cache is genuinely hit (two permissive runs share `resolvedAt`) before asserting the restrictive run fails, so the regression cannot pass vacuously.
- Bumped the app-plan cache schema revision. The cache key is derived from the AUTHORED Landofile, so an app that already spelled `restart:` hashes identically before and after this change; a warm pre-merge cache would otherwise keep serving a knob-less plan, the gate would collect zero knobs, and the authored knob would stay inert — the exact failure this story exists to prevent.
- Failure surface: `CapabilityError` carries `service`, `key`, `capability: "composeSpec"`, `providerId`, and remediation naming all three escapes (drop the key, choose a supporting provider, move the intent under `providers.<id>`). The CLI bug-report formatter gained a `CapabilityError` branch so `service`, `key`, and `capability` reach the rendered envelope; the narrow `{_tag, message, remediation}` machine-output envelope was left alone deliberately, since it is uniform across every tagged error and widening it for one error would have been a public schema change no other error gets.
- Ordering is deterministic by construction: knob uses are collected from the FINAL extension rather than the authored config, sorted by service then by published knob-key order, so the reported first failure never depends on object key insertion order and a knob injected by a plugin is gated exactly like an authored one.
- Verification: typecheck, lint (Biome clean across all tracked packages), and the import-cycle, renderer-, redaction-, probe-, state-store-, paths-boundary, machine-output, and Compose-coverage gates passed; schema-snapshot codegen re-ran with zero drift; 373 focused tests green across planner, knob, cache, bug-report, CLI scenario, library, runtime, lifecycle, recipe, provider-compose and SDK capability files. Every new suite was confirmed RED before implementation and GREEN after. Real-CLI evidence on a knob-bearing app: `app config get` round-tripped `restart` verbatim and `shm_size` as canonicalized bytes; `info` exited 1 naming the service, the knob, and the remediation, with the plain formatter emitting `service`/`key`/`capability`/`providerId`; the same app with the knobs removed planned clean at exit 0.
- Known and intended: both bundled providers declare `composeSpec: "portable"` with no knob declaration, so every knob is currently rejected on shipped providers. That is the fail-closed behavior this story specifies, and declaring the supported set is US-474's job (its criteria require the declaration to match the implemented mapping exactly). No bundled recipe or guide authors a knob, so nothing regresses today.
- **Learnings for future iterations:**
  - Build multi-form schema fixtures by decoding an ENCODED document through the schema instead of hand-writing the canonical decoded value. Hand-guessing canonical forms for 24 knobs (device strings to long objects, integer ulimits to `{soft,hard}`, byte and duration strings to numbers) is where the time goes and where silent wrong assertions come from; decoding first also makes the test prove canonicalization rather than assume it.
  - A cache-bypass regression is vacuous unless it first proves the cache is actually hit. Assert a cache-hit signal (identical `resolvedAt` across two permissive runs) BEFORE the restrictive assertion, otherwise the test passes whether or not the cached path was ever entered.
  - `Pick<T, "a"> & { readonly b?: B }` is NOT interchangeable with `Pick<T, "a" | "b">` under `exactOptionalPropertyTypes`. A hand-written optional property excludes `undefined` and rejects the real service type; spell the view as a `Pick` of the source type so optionality stays identical.
  - The test-registry helper that registers a fake service TYPE does not register that fake's service FEATURE. A plan using a new fake type fails with "Service feature <id>.test-plan is not registered" — which looks like a planner bug and is really a missing `loadServiceFeature` override.
  - Biome scans untracked sibling checkouts. A stale `.worktrees/` directory (git-excluded, invisible to `git status`) makes `bun run lint` fail on files that are not part of the change at all; scope Biome to the tracked package dirs before believing a repo-wide lint failure.
  - The machine-output envelope narrows every error to `{_tag, message, remediation}` on purpose. Structured error context belongs in the message and in the bug-report formatter's extra fields, not in a per-error widening of the shared envelope.
---

## 2026-07-26 21:43 CDT - US-473 Review
- Fixed terminal control-sequence injection in the new `sysctls` and `extra_hosts` list-form validation failures: malformed entries no longer echo untrusted bytes, while the errors retain actionable `KEY=value` and `HOST=IP or HOST:IP` remediation. A regression covers both ESC and BEL and was confirmed red before the fix and green after it.
- Guide-Coverage-Skip: US-473 adds Compose runtime-knob schemas and planning capability checks; the executable Compose guide and published key matrix are explicitly owned by US-477.
- Review notes (deferred/rejected): The nonblocking suggestion for another direct provider-contract negative test was deferred because the invariant is already enforced by the contract helper and the story's partial accept/reject planner paths are covered; no assertion or feature path was weakened.
- Review notes (deferred/rejected): Cursor Bugbot produced no findings because both the required invocation and its Auto-mode retry were blocked by the configured account's usage limit.
- Verification: typecheck passed; tracked-file Biome and deprecation checks passed; focused QA passed 286 tests with 1,236 assertions, and the repaired schema suite passed 76 tests with 239 assertions. Guide drift passed with explicit base and the skip above. Bare `bun run lint` remains blocked only by pre-existing errors in the ignored `.worktrees/plugin-dag` checkout.
---

## 2026-07-26 22:24 CDT - US-473 Drift Audit
- Trigger: 4 cycles, 16829 changed lines, 11 commits
- Scope absorption: excised the partial US-475 deploy-sibling rejection path while retaining the US-473 resources-only schema; tagged rejected-tier handling remains with US-475.
- Weakened proofs: replaced a test that blessed silent `deploy.resources` loss and added durable partial-test-provider accept/reject, provider-contract invariant, and all-knob matrix-disposition proofs.
- Review scar tissue: removed the default-vs-strict branch that erased valid resources and the `tmpfs` deduplication that discarded authored intent.
- Deferred-work laundering: closed the previously deferred provider-contract negative proof because the invariant was introduced by this branch; mixed deploy input now preserves the resources this branch owns.
- Fixed: valid deploy resources survive unrelated sibling keys under default decoding, strict decoding still rejects excess siblings, and injected plus authored `tmpfs` entries remain lossless.
- Split/excise decisions: no PRD split; the absorbed US-475 rejection behavior was excised cleanly, and US-473 remains passing with its original acceptance boundary.
- Verification: typecheck, tracked-source Biome, deprecations, Compose coverage, import-cycle, machine-output, schema-codegen idempotence, and 11 focused test files passed; bare `bun run lint` failed only because it scans the pre-existing ignored `.worktrees/plugin-dag` checkout.
---
## 2026-07-26 23:21 CDT - Babysit
- PR: #636 https://github.com/AaronFeledy/core4/pull/636
- CI outcome: pass
- Review feedback: fixed (declare empty composeKnobs on built-in providers; refresh Landofile/deploy and doctor NDJSON fixtures for first-class knobs)
- Drift check: clean
- Merge: merged (squash)
---

## 2026-07-27 00:55 CDT - US-474 (completion)
- Closed the story: the bundled Podman provider now REALIZES the preserved Compose runtime-knob tier instead of merely preserving it. 21 knobs (`restart`, `cap_add`/`cap_drop`, `privileged`, `devices`, `ulimits`, `sysctls`, `tmpfs`, `shm_size`, `dns`/`dns_search`/`dns_opt`, `extra_hosts`, `init`, `stop_signal`, `stop_grace_period`, `security_opt`, `group_add`, `read_only`, `platform`, `logging`) map into the docker-compatible `POST /containers/create` request across three distinct slots — `HostConfig`, top-level body, and query string — and both bundled providers flip `composeSpec` from `portable` to `native` and declare the realized set through one shared registry, so declaration and mapping cannot drift.
- Resolved the story's central contract conflict rather than faking it. The AC demanded `pull_policy` and `deploy.resources` be mapped to "libpod create-container fields"; neither is one. Following the US-473 precedent, normative text and code were reconciled in the same change, and the governing rule is now recorded as FR-4: **a knob is declared when the provider consumes its complete structural shape and neither adapter nor runtime silently discards any supplied part; runtime rejection of an invalid or unavailable operand is a loud value error, not a capability mismatch.** That criterion — silent drop vs. loud failure — is what separates a declarable knob from an undeclarable one, and it is the rule to apply to any future provider.
- Under FR-4 three knobs stay UNDECLARED, so the US-473 planner gate rejects them before any provider action: `pull_policy` is artifact-acquisition orchestration whose only seam carries `{ ref }` and whose `build`/refresh values are planner semantics FR-3 forbids; Podman's compat handler silently discards the non-CDI `Count`/`Capabilities`/`Options` of `gpus`; and the coarse `deploy.resources` key would claim `reservations.cpus`/`reservations.devices`/`generic_resources`, which Podman cannot realize. Honest partial support for any of them needs a subfield- or value-granular capability surface, which this wave excludes — that is the follow-up trigger, not something to smuggle into `collectComposeKnobs`.
- `logging` is the one knob where the two failure modes had to be separated. It IS declared, because Podman consumes `driver` plus `options` whole and rejects a bad driver loudly — exactly like a bogus `cap_add` or a nonexistent `devices` path, all of which remain declared. But `passthrough`/`passthrough-tty` are rejected at the provider edge, because both bundled providers always speak to Podman over a REMOTE socket where those two are categorically invalid rather than merely unknown. Deliberately a rejection list, not an allowlist: an allowlist would duplicate Podman's evolving driver inventory and wrongly reject future valid drivers.
- Value coercion stays "total or loud": device strings expand to long objects, integer/string ulimits become `{Soft, Hard}`, list-form entries become maps, YAML truthiness spellings become real booleans, and one hostname mapped to several addresses fans out into one `ExtraHosts` entry per address instead of collapsing. Bring-up also guards a real corruption case — a `tmpfs` destination colliding with a planned bind or volume target fails instead of shadowing the mount — and merges plan-derived `ExtraHosts` with knob-derived ones rather than letting either clobber the other.
- Verification: typecheck clean, Biome clean across all tracked packages, 156 focused tests green across the knob registry, value coercion, bring-up request construction, both providers' capability declarations, the declaration-vs-mapping diff, the runtime provider registry, and the doctor goldens. The `passthrough` rejection was confirmed RED (the driver passed straight through as `Type: "passthrough"`) before implementation and GREEN after. LIVE proof on a real Podman socket: a container brought up with `privileged`, `ulimits`, and `extra_hosts` inspected back with `HostConfig.Privileged: true`, the `nofile` limit at 20000/40000, and `knob-check.lando:10.42.0.9` in `ExtraHosts`. Real-CLI proof: an app using five declared knobs plans clean at exit 0, while `deploy.resources`, `pull_policy`, and `gpus` each exit 1 with a `CapabilityError` naming the service, key, capability, providerId, and all three escapes.
- Realization deliberately targets Podman's DOCKER-COMPATIBLE create endpoint rather than libpod SpecGenerator. The provider already speaks `POST /v6.0.0/containers/create` with a Docker-shaped body, and spec 5.5 lets a provider translate the plan into its own native representation. Compat is also the higher-fidelity target for these inputs: SpecGenerator wants a numeric `stop_signal`, OCI `LinuxDevice` structs instead of source/target paths, and split security fields instead of an opaque `security_opt` list, and it has no pull-policy concept at all. Switching endpoints would have rewritten the working create/start path for strictly worse mapping fidelity.
- Mapping lives in a Podman-only module composed into the create path, never in the shared `container-runtime` body builder, so the Docker provider gains nothing and stays `portable` with no knob declaration. `provider-podman` delegates apply to `provider-lando`, so both inherit realization from one implementation.
- Knob values are re-decoded at the provider edge through an Effect Schema projection of the canonical knob fields rather than cast, because `ServicePlan.extensions` is `Record<string, unknown>` and trusting core with a cast would turn a planner bug into a malformed container instead of a tagged error.
- Recorded, NOT fixed (both pre-existing, neither introduced here): an already-created container is never recreated, so changing any field — env, command, and now knobs — is inert until the container is removed; and shared bring-up failures report provider id `lando` even when reached through `provider-podman`.
- **Learnings for future iterations:**
  - The declaration criterion that actually works is SILENT DROP vs LOUD FAILURE. "This knob has values that can fail at runtime" disqualifies nothing — `cap_add`, `devices`, `sysctls`, `security_opt`, and `stop_signal` all have that property and are all legitimately declared. What disqualifies a key is the runtime accepting a standardized field and discarding it with no error.
  - A capability key coarser than the thing it gates forces all-or-nothing honesty. `deploy.resources` is one key over a subtree Podman only partly implements, so even the very common `limits.memory` must be rejected. Coarse keys are cheap to publish and expensive to live with; size the key to the smallest independently-realizable unit.
  - Do not assert a vendor's normalization you have not observed. Podman rewrites inspect output (ulimit names, numeric stop signals, appended tmpfs options), so an integration assertion copied from expectation rather than from a real `GET /containers/{id}/json` will pass review and fail in CI. Match the knob's own entry and pin only the values you set.
  - `resolveLiveProviderSocket()` finds the MANAGED socket even when `LANDO_TEST_PODMAN_SOCKET` is unset, so a `test.skipIf(...)` integration test will really run — and really create containers — on a workstation that has ever run `lando setup`. Assume `*.integration.test.ts` is live-capable locally, and keep teardown in `finally` for exactly that reason.
  - A stale container from an earlier aborted run makes the next bring-up fail at create; the same test passes immediately once its own `finally` cleanup has run. Diagnose an integration failure by re-running it isolated before believing it is a code defect.
  - Podman serves its Docker-compatible API under the same `/v6.0.0` prefix as libpod, so a create call reading `/v6.0.0/containers/create` is COMPAT, not libpod. Check the body shape (`Image`/`HostConfig` versus `image`/`resource_limits`) before reaching for SpecGenerator field names; the two APIs share a version prefix and nothing else.
  - Docker embeds `Resources` into `HostConfig`, so `Ulimits`, `Devices`, `Memory`, `NanoCpus` and `PidsLimit` are FLAT inside `HostConfig` on the wire, while `StopSignal` and `StopTimeout` are top-level siblings of `Image` and `platform` is a query parameter, not a body field. Getting the nesting wrong is silent: Podman ignores unknown keys.
  - `privileged: true` erases `CapAdd` to an empty list, so capability additions cannot be proven on a privileged container; a live fixture needs two services to cover both.
  - Live provider suites depend on fixture images already being present locally and do not pull them. A sibling suite that cleans up images on exit leaves the next run failing at image lookup, which reads exactly like a code regression — pull the image and re-run before believing it.
---

## 2026-07-27 01:30 CDT - US-474 Review
- Closed the review blockers in the provider edge: unsafe integer ulimits, duplicate tmpfs destinations, fractional/negative stop timeouts, and non-positive shared-memory sizes now fail loudly instead of rounding, overwriting, producing malformed API values, or being silently ignored. Every declared knob is additionally asserted through the final container-create request, and the live-effects suite now requires explicit `LANDO_TEST_PODMAN_SOCKET` opt-in rather than discovering and mutating a workstation-managed runtime.
- Guide-Coverage-Skip: US-474 changes Podman request realization internals and does not alter provider setup or selection guide behavior.
- Review notes (deferred/rejected): The transport-coverage finding was rejected because existing live bring-up integration already exercises Unix sockets and the win32 named-pipe path where supported; knob realization produces the same `PodmanHttpRequest` before transport dispatch, and the branch's dispatch test pins both selections.
- Review notes (deferred/rejected): Existing-container reconciliation and provider-podman failures carrying provider id `lando` remain pre-existing behavior explicitly recorded by the build step, not US-474 blockers. Cursor Bugbot could not run because the configured account is out of usage; the replacement security lane found no material issue.
- Verification: typecheck, tracked-source Biome, deprecations, explicit-base guide drift with the skip above, no-excuse checks, and focused mapping, final-request, bring-up, transport, declaration, capability, and provider-registry tests passed. The sandbox live rerun was blocked because its dependency-free state currently has neither Bun nor a working Podman socket; the build step's fresh live proof covers the unchanged valid `privileged`, `ulimits`, and `extra_hosts` path. Bare `bun run lint` remains blocked only by the pre-existing ignored `.worktrees/plugin-dag` checkout.
- **Learnings for future iterations:** A schema-valid value can still violate a provider's wire-domain contract; key-level capability declarations require adapter checks for numeric precision/range and duplicate-key collapse. Story-specific live tests that promise explicit opt-in must read the opt-in variable directly rather than use a generic resolver that falls back to a managed runtime.
---

## 2026-07-27 02:23 CDT - US-474 Drift Audit
- Trigger: 2 cycles, 1681 changed lines, 10 commits
- Weakened proofs: replaced the transport dispatch smoke check with a knob-bearing create request over platform IPC and registered it in the managed Linux and Windows provider matrix cells.
- Ceremony modules: removed dead and test-only public knob exports while retaining the one cross-provider capability export.
- Deferred-work laundering: corrected the new knob-validation failure path to report the resolved provider instead of inheriting the shared adapter's `lando` identity.
- Contract drift: confirmed the narrowed `pull_policy`, `gpus`, and `deploy.resources` boundary is an explicit spec-aligned reconciliation rather than absorbed implementation scope.
- Fixed: simplified the public surface, restored assembly-to-transport proof, and corrected branch-owned provider error attribution.
- Split/excise decisions: none; no absorbed feature, incomplete public contract, or gate conflict was found.
- Verification: typecheck, scoped lint, deprecations, import-cycle, Compose coverage, and 171 focused checks passed; bare lint remains failed on pre-existing auxiliary-worktree diagnostics; live Podman effects were not rerun and remain unverified in this host-safe step.
---

## 2026-07-27 02:40 CDT - US-474 Drift Audit
- Trigger: 3 cycles, 1852 changed lines, 16 commits
- Review scar tissue: removed a speculative per-knob platform-support axis whose entries were identical on every supported host.
- Ceremony modules: consolidated overlapping declaration assertions into one exact invariant covering both bundled providers, all host platforms, and the independently proven mapping fixtures.
- Fixed: deleted the unused extension seam and duplicate proofs without changing the declared knob set or request realization.
- Split/excise decisions: none; no absorbed feature, collateral story flip, incomplete public contract, or gate conflict was found.
- Verification: typecheck, tracked-source lint, deprecations, import-cycle, Compose coverage, targeted no-excuse checks, and focused US-474 spot checks passed; bare lint remains failed on the pre-existing ignored auxiliary checkout, the provider-podman barrel retains two pre-existing no-excuse findings outside the touched lines, and live Podman effects were not rerun.
---

## 2026-07-27 12:25 CDT - Babysit
- PR: #639 https://github.com/AaronFeledy/core4/pull/639
- CI outcome: pass
- Review feedback: fixed (Guide-Coverage-Skip for internal knob mapping; flaky host-shell reaper unit and Windows quay.io timeout reran clean)
- Merge: merged (squash)
---

## 2026-07-27 13:10 CDT - US-475 (spec reconciliation)
- Corrected a false normative claim before it shipped a lie: §7.4 asserted YAML anchors, aliases, and merge keys (`<<:`) "remain fully supported" as plain YAML resolved ahead of schema decode. The Landofile parser is a hand-rolled line-based subset with zero support for any of these forms — a `<<:` key doesn't even match the parser's key regex and dies as a malformed-YAML error, and `&anchor`/`*alias` are returned as ordinary strings, never resolved. Left uncorrected, the shipped US-475 tagged-rejection surface would have been read as proof of a capability that does not exist.
- Scope held at a rejection surface, not expanded into a parser rewrite. Reconciling the disposition matrix's `!reset`/`!override` rejection against a false anchors/aliases/merge-keys claim in the same sentence created pressure to "just add anchor support while we're in here." That would have been dishonest scope growth disguised as a bug fix: the story proves rejected keys fail closed with a tagged, remediation-bearing error, not that the parser understands YAML tags. The anchors/aliases/merge-keys clause is now framed as a REGRESSION GUARD — proving those forms fail through the existing parser/validation surface rather than being misclassified as a rejected Compose key — not a support claim.
- Reconciled a second drift in the same read: §7.4's `includes:` `kind:` discriminator documented `landofile | tooling | compose`, but `IncludeEntry` only ever accepted `kind: "landofile"`. The in-flight story is adding `compose` (and making the already-accepted-but-inert top-level Compose `include:` key resolve, normalized to `kind: compose` entries appended after authored `includes:`); it is deliberately not adding `tooling`. Amended `spec/07-landofile-and-config.md` (the §7.4 bullets, the §7.7.1 kind table, and the §7.7.7 distinction table) and this story's AC in `spec/compose/prd-compose-04-rejection-and-conformance.md` to state the Beta surface is `landofile | compose`, with tooling fragments continuing through the separate `toolingIncludes:` key until a unified `kind: tooling` ships as its own follow-up.
- Also corrected the AC's proving-command claim: bare top-level `lando config` resolves to `meta:config` and reads global config, not the app Landofile, so it cannot prove a Landofile rejection. The correct proving surface is `lando app config` / `lando info` / `lando app config lint`.
- DEFERRED FOLLOW-UP (parser capability, one project not five patches): native YAML anchors, aliases, and merge keys (`<<:`), block scalars (`|`/`>`), populated inline flow objects (mappings/sequences with actual content, not just `{}`/`[]`), and the parser's resource-limit contract (max document size, max nesting depth, similar guards) are gaps in the same hand-rolled parser and belong to one coherent parser-capability project rather than five ad-hoc patches landed under unrelated stories. A prior story already recorded the block-scalar gap as belonging in its own story; this entry consolidates that gap with its siblings so the eventual work is scoped once, correctly.
- DEFERRED FOLLOW-UP (unified tooling include-kind): `kind: "tooling"` as a schema-only literal, without its own app-plan compile-time resolution timing and namespace/flatten/internal/aliases/excludes/vars contract, is exactly the kind of partial public surface a prior drift audit in this repo excised. The unification of `toolingIncludes:` under `includes: [{ kind: tooling, ... }]` is real, wanted work — it just needs its own story that ships the full contract (§8.5.8 timing plus namespacing) at once, not a bare literal added to satisfy a schema diff.
- Verification: read-only spec/PRD/progress reconciliation; no code, schema, or test files touched. `rg -n "remain fully supported" spec/07-landofile-and-config.md` returns nothing; `rg -n "parser-capability|anchors, aliases" spec/compose/progress.txt` matches this entry.
- **Learnings for future iterations:**
  - When a normative sentence bundles a true claim and a false claim in the same breath (here: "tags are rejected" true, "anchors/aliases/merge-keys remain supported" false), fix both halves in the same edit rather than only the half a story's acceptance criteria happen to test — otherwise the false half survives untouched next to the newly-accurate half.
  - "The parser doesn't understand X" and "X is a rejected disposition" are different failure modes and must not be conflated in a disposition matrix. A disposition matrix classifies vocabulary decisions (normalized/preserved/rejected); a parser's structural inability to read a YAML form at all is a capability gap, not a disposition, and documenting it as one misleads anyone reading the matrix as a completeness contract.
  - A schema literal (`kind: "tooling"`) is a public contract the moment it's documented, even if unimplemented. Documenting a `kind:` value ahead of its own timing/namespacing contract is the same partial-surface mistake a prior drift audit already excised elsewhere in this repo — check every enum-like discriminator in the spec against what the schema actually accepts before trusting the prose.
---

## 2026-07-27 14:45 CDT - US-475 (completion)
- Closed the story: every rejected Compose disposition now fails closed with ONE tagged, matrix-sourced error instead of being silently dropped or decoded into a confusing schema cascade. `ComposeKeyRejectedError` carries `{source, service?, keyPath, remediation}`, and the remediation is read from the disposition matrix entry at the match site — `rejectedEntry()` throws a matrix-invariant error if a rejected entry omits remediation, so a hand-written per-call-site string cannot exist.
- Classification is a compiled trie, not a key list. The matrix compiles once into a prefix trie with exact > `x-*` > `*` precedence, which is what makes keyPath precision fall out structurally rather than by special-case: `preserved` nodes return no match and the walker DESCENDS through them, so `deploy.resources` survives while `deploy.replicas` rejects, from the same data. Proven both directions at the real CLI, not just in unit tests.
- Tags are rejected BEFORE the parser runs, because they cannot survive it. `!reset`/`!override` are matched by a line-based detector on the rendered text ahead of `parseLandofile`; a quoted `"!reset"` is an ordinary string and is deliberately not rejected. Tag tokens live in a SEPARATE `composeTagDispositions` map rather than the service/top-level matrices, because the compose-coverage gate compares those two matrices bidirectionally against the vendored JSON Schema and a YAML tag token is not a schema key path — folding them in would have broken an exhaustive gate to satisfy an unrelated feature.
- The same decode path serves Landofiles, TS Landofiles, the merged layer result, and include fragments, so `kind: compose` fragments cannot drift from authored files. The previously-inert top-level Compose `include:` key now resolves, normalized to `kind: compose` entries appended AFTER authored `includes:`.
- REGRESSION CAUGHT AND FIXED IN THIS STORY (the reason it was not already done): fragment rejection attribution had been implemented by overloading `FragmentResult.sourceId` from the resolved absolute path to the authored include spec. `sourceId` is identity, not provenance — it keys include cycle detection, version-constraint `source:`, lockfile staged-lock entries, and `localIncludePaths`, which the app-plan cache invalidator `realpath()`s and root-checks. Authored relative strings there resolve against the wrong directory and silently stop tracking a fragment for cache invalidation. Split into a dedicated `authoredSource` field consumed ONLY by the two rejection call sites; `sourceId` restored to the absolute path everywhere else. All five fragment fetchers set it, and the field is required so the compiler refuses to let a future fetcher forget.
- That silent consumer had NO test. Added one asserting remembered local include paths stay absolute, and confirmed it genuinely bites by reverting the one-line fix and watching it go red alongside the pre-existing provenance test. A cache-invalidation regression that only manifests as a stale plan is exactly the kind that ships.
- Recorded, NOT fixed (pre-existing, verified out of scope): `app config lint` emits a second `services: Expected undefined, actual {...}` violation beside the precise rejection. An ordinary unknown service key with no Compose involvement produces the identical line, so this is the schema union's existing fallback reporting, not something this story introduced.
- Reconciliation (AC-5 vs AC-6): AC-5's `app config lint` clause is satisfied by AC-6's violation surface, not by a tagged abort. `lintLandofile` deliberately converts rejections into `ConfigLintViolation` data carrying the matrix remediation as `suggestedFix`, so an editor gets every violation at once instead of failing at the first. Lint behavior was left alone.
- Verification: typecheck, lint, and the import-cycle, package-dag, compose-coverage, renderer-, paths-, probe-, state-store-, and redaction-boundary gates all passed; schema-snapshot codegen re-ran byte-identical across reruns. 383 focused tests green across the rejection engine, trie, tag detection, include resolution, lint, CLI envelope, and SDK contract files. Real-CLI proof on the compiled binary inside the dev sandbox: `app config`, `info`, and `--format json` each exit 1 with the tagged error, remediation, service, and keyPath and no stack trace; a `kind: compose` fragment attributes to its authored `./frag.yml`; `app config lint` lists the rejection with the matrix `suggestedFix`; and a NEGATIVE CONTROL app carrying preserved `deploy.resources` plus a quoted `"!reset"` exits 0 and resolves normally, proving the surface is precise rather than blanket.
- **Learnings for future iterations:**
  - Do not overload an identity field to carry provenance. `sourceId` looked like "where this came from" and was actually the key for cycle detection, locks, and cache invalidation; adding `authoredSource` cost one required field and removed a whole class of silent breakage. Make the new field REQUIRED so the compiler enumerates every construction site for you.
  - A rejection surface needs a negative control in its CLI proof. "The bad app fails" is satisfied equally well by a correct implementation and by one that rejects everything; only the valid app that must still exit 0 — here preserved `deploy.resources` plus a quoted tag — separates them.
  - Keep non-schema tokens out of a matrix that a bidirectional coverage gate consumes. YAML tag tokens are dispositions but not key paths; a separate map keeps the exhaustive schema gate exact instead of forcing an exception into it.
  - Deep-relative imports into another workspace package's `src/` are not merely a style issue: the barrel path and the deep path can resolve to two distinct module instances, at which point an `instanceof` check on a tagged error silently fails and a typed Effect failure degrades into an unhandled defect. Route through the published barrel and the existing in-tree re-export seam.
  - Widening a service tag's Effect error channel is a frozen-surface change. It requires the `sdk-mvp-surface.json` fixture entry AND a compatibility note, and union MEMBER order inside that signature string is load-bearing because the comparison normalizes whitespace but never sorts unions.
---

## 2026-07-27 15:02 CDT - US-475 Review
- Closed four review blockers: explicit-file loads and include lock refresh now honor top-level Compose `include:`, include verification rejects root Compose keys/tags through the shared tagged path, and nested relative includes resolve from the including fragment while retaining the app-root containment boundary.
- Escaped attacker-controlled terminal controls on the new plain Compose-rejection and config-lint diagnostic surfaces without changing structured JSON fields, and synchronized the PRD index with the source PRD's quoted-tag and unsupported-anchor contract.
- Guide-Coverage-Skip: US-475 changes Compose rejection and include-resolution internals; the executable Compose guide and published matrix are explicitly owned by US-477.
- Review notes (deferred/rejected): widening the shared machine-result error envelope was rejected because its fixed contract intentionally carries only `_tag`, `message`, and `remediation`; `ComposeKeyRejectedError` retains structured fields internally and the JSON bug-report surface exposes them.
- Review notes (deferred/rejected): resolving include graphs from schema-only lint was rejected because lint deliberately performs no fetch, lock, or include-resolution side effects; fragment rejection remains covered by the normal include resolver, while lint reports root/layer rejections with the matrix remediation.
- Review notes (deferred/rejected): tag-specific lint source locations and suppression of unrelated schema-union fallback violations are not acceptance requirements; the tagged remediation is present, and the duplicate fallback is recorded as pre-existing.
- Review notes (deferred/rejected): the required Bugbot invocation was blocked by the configured account's usage limit; the Auto-mode retry reported the two Compose-include parity bugs fixed above.
- Verification: typecheck and lint passed; explicit-base guide drift passed with the skip above; Compose coverage, import-cycle, renderer-boundary, and paths-boundary gates passed; focused rejection, tag, lint, include resolution/update/verify, CLI-envelope, and diagnostic-escaping tests passed. Rebuilt the compiled binary and verified root include rejection plus nested fragment attribution inside `lando4-dev`.
- **Learnings for future iterations:** Keep relative-resolution base, security containment root, absolute identity, and authored provenance as four separate concepts; collapsing any pair produces wrong-directory reads, traversal mistakes, stale lock/cache identity, or misleading diagnostics.
---

## 2026-07-27 15:26 CDT - US-475 Drift Audit
- Trigger: 3 cycles, 2029 changed lines, 8 commits
- Weakened proofs: restored source-CLI rejection coverage for app config, info, the JSON failure envelope, and lint; expanded the named §7.4 Swarm rejection proof and removed a vacuous coverage-gate duplicate.
- Review scar tissue: deleted redundant negative-tag checking and routed the tag-detector test through the published package boundary instead of a deep workspace source import.
- Contract drift: preserved the matrix rationale in the tagged error message and converted the parser-capability and tooling-include deferrals from progress prose into binding follow-up contracts.
- Fixed: removed duplicate/redundant assertions, restored production-path proofs, and retained the three new production modules because each has a substantive responsibility and real production consumers.
- Split/excise decisions: added US-478 for native YAML anchors/aliases/merge keys and US-479 for the complete kind: tooling include contract in the PRD index and source PRD; US-475 remains passing, with no gate conflict or incomplete public contract.
- Verification: typecheck, lint, 13 focused test files, targeted no-excuse checks, PRD JSON parsing, Compose coverage, import-cycle, renderer-boundary, and paths-boundary passed.
---

## [2026-07-27 19:50 CDT] - Babysit
- PR: #640 https://github.com/AaronFeledy/core4/pull/640
- CI outcome: pass (after triage; initially fail perf-budget-linux-x64)
- Review feedback: fixed (minimal-bootstrap for cached Bun-script tooling hot path; drift audit restored production-path scenario and dropped unrelated fixture churn)
- Drift check: fixes pushed (restored Bun script hot-path scenario; removed unrelated plugin fixture edit)
- Merge: merged (squash)
---

## 2026-07-27 21:05 CDT - US-476
- Landed the fixture-driven conformance suite that proves the Compose service-key vocabulary against real upstream material every CI run. Eight byte-pinned fixtures from compose-spec/conformance-tests (commit-pinned, Apache-2.0, per-file checksums) plus a six-fixture curated corpus (depends_on conditions, long-form ports/volumes, list environment with env_file and labels, Compose healthcheck, runtime knobs, and a rejected key) now live under core/test/fixtures/compose with an offline checksum test and a networked, pin-driven refresh script kept out of the offline codegen catalog.
- The suite is matrix-driven end to end: fixtures are enumerated from the pin module, every key occurrence is classified by a new production walker that reports all three dispositions, normalized keys assert against a new machine-readable plan target carried on the matrix entry itself, preserved keys assert their round trip through ServicePlan.extensions.compose, and rejected keys must fail the production loader with the tagged rejection carrying the matrix remediation. A generated rejection manifest is recompared at test time so reclassifying a path away from rejected fails the suite instead of silently hiding a fixture, and an anti-hiding guard forbids a supported key from appearing only inside a rejecting fixture. No hand-maintained key list exists in the suite.
- Closing the vocabulary honestly required making spec 6.2's supported-subset sentence true: service-level networks, configs, secrets, profiles, and x-* were classified preserved in the matrix but were still rejected by the authoring schema as unsupported keys. They are now typed acceptance surfaces preserved losslessly into extensions.compose and gated on the coarse native Compose capability, deliberately without widening the exact runtime-knob capability list, which stays the 24-key spec 6.2 knob tier. Planner output semantics changed, so the app-plan cache schema version rolled.
- Provider spot checks for a fixture subset run in the env-gated Podman integration lane and skip cleanly without a live socket.
- Verification: typecheck, lint passed; 157 focused tests across 17 compose, cache, parser, and schema files passed; compose-coverage, import-cycle, package-dag, renderer, paths, state-store, probe, and machine-output gates passed; full codegen is idempotent; vendored fixture bytes reverified byte-identical against upstream. CLI verified directly: the upstream configs and networks fixtures, which previously failed as unsupported keys, now render through app config view with ports normalized to canonical long form, and the scaling and udp_port fixtures surface ComposeKeyRejectedError with matrix remediation in the JSON envelope.
- **Learnings for future iterations:**
  - A classification matrix is only honest when something executes it. Vendoring real upstream fixtures immediately exposed five keys the matrix promised were preserved but the schema rejected; any future disposition change should be assumed unproven until a fixture exercises it.
  - The skip decision must be derived, never authored. Deriving it from the walker and diffing against a generated manifest is what converts "we skipped that one" into a failing test.
  - Landofile source dispatch for manual CLI checks must go through core/bin/lando.ts; running core/src/cli/index.ts directly produces no output and silently exits 0, which reads like a passing command.
  - Upstream compose-spec/conformance-tests publishes no tags, so its pin is a commit SHA with per-file checksums rather than the tag-plus-single-checksum shape used for the vendored schema.
---

## 2026-07-27 22:27 CDT - US-476 Review
- Closed the review blockers: conformance anti-hiding now covers every normalized or preserved matrix path rather than roots only; fixture pins reject non-canonical source and vendored path aliases before filesystem resolution; and the populated flow-object parser capability was excised because the prior US-475 Drift Audit explicitly assigned it to the parser-capability follow-up and no US-476 fixture needs it.
- Guide-Coverage-Skip: US-476 adds internal fixture conformance and CI coverage; the executable Compose guide and published matrix are explicitly owned by US-477.
- Review notes (deferred/rejected): restricting the offline manifest generator's trusted explicit output path to its fixture root was rejected as defensive hardening outside the script contract; its only production caller supplies the committed in-root manifest path.
- Review notes (deferred/rejected): the vendored upstream LICENSE trailing blank line was preserved because fixture bytes are checksum-pinned upstream material, not locally formatted source.
- Review notes (deferred/rejected): Cursor Bugbot was unavailable before and after fixes because the configured account was out of usage; two specialized security lanes were tool-blocked, while a direct fixture-integrity review produced the canonical-path defect fixed above and the rejected output-path suggestion.
- Verification: typecheck and lint passed; 100 focused conformance, fixture-maintenance, supported-subset, parser, disposition, and generated-CI tests passed with 1,011 assertions; Compose coverage, import-cycle, package-DAG, manifest idempotence, and explicit-base guide drift with the skip above passed. Changed-file diagnostics were clean where LSP responded; two final fixture-script refreshes timed out with typecheck still clean. Env-gated provider execution remained structurally reviewed rather than run on the host.
- **Learnings for future iterations:** Anti-hiding checks must compare every classified path, not only roots; pin uniqueness must be enforced on canonical portable paths because distinct raw spellings can resolve to one filesystem destination.
---

## 2026-07-27 23:10 CDT - US-476 Drift Audit
- Trigger: 2 cycles, 6337 changed lines, 14 commits
- Scope absorption: excised service-level `networks`, `configs`, `secrets`, `profiles`, and `x-*` schema/preservation/capability work into binding prerequisite US-482. The four upstream fixtures that forced that missing production contract moved with the prerequisite; US-476 now selects only behavior implemented before the fixture story.
- Review scar tissue: moved conformance assertion strategy and canonical config lookup metadata out of the production disposition matrix. The matrix retains normalization targets; a test-only exhaustive map owns assertion mechanics.
- Weakened proofs: replaced decoded-output-derived-only confidence with independent literal plan expectations for every required curated success fixture and the runtime-knob fixture. Added a non-rejected curated UDP fixture so the supported UDP form in the rejected upstream `ports.mode` fixture remains visible to anti-hiding.
- Contract drift: restored the pre-story `ServiceConfig`, planner label merge, provider knob validation, and app-plan cache v11 boundaries; removed the absorbed SDK compatibility entries and generated schema consequences.
- Fixed: deleted the absorbed public contract and its generated/cache consequences, restored the original planner boundary, and added independent authored-value fixture expectations without weakening matrix-driven assertions.
- Split/excise decisions: US-482 must implement each native field end to end or reject it before provider action; `composeSpec: native` cannot silently bless ignored values. US-476 remains false for fresh Build verification of its narrowed contract, not as an implementation dependency on US-482. No gate-conflict signal applies because the prior review required path-level anti-hiding and canonical pin paths, not the excised fields.
- Verification: typecheck, lint, Compose coverage, import-cycle, package-DAG, focused conformance/fixture/planner/cache/SDK tests, TypeScript no-excuse checks, PRD validation, and diff checks passed. Schema artifacts and the 11-entry offline fixture manifest regenerated idempotently; the restored app-plan v11 binary matches the fixed audit base exactly. LSP diagnostics were clean where returned, while repeated requests for the disposition table and conformance entry files timed out with compiler and lint validation clean. No live provider execution was part of this audit correction.
---

## 2026-07-27 23:45 CDT - US-476 (completion)
- Closed the story against its post-audit narrowed contract: the conformance suite proves only Compose behavior that already shipped before the fixture story, and the five service-level native fields the Drift Audit excised stay excised. Nothing was re-implemented to make a fixture pass; the branch's production delta is still just the generalized disposition walker.
- Confirmed the two load-bearing guards actually bite rather than passing vacuously, by mutating fixtures and watching them go red. Deleting the rejected key from the rejecting fixture made the committed rejection manifest disagree with runtime classification and failed the suite with an exact diff; adding a rejected key to the healthcheck fixture made the anti-hiding guard name `healthcheck.start_period` and `healthcheck.timeout` as newly hidden. Both fixtures were restored byte-identical and the suite returned green. A computed skip-list that is never observed failing is indistinguishable from an authored one, which is the whole point of the criterion.
- Verified the excision is real at the boundary that matters, not just in prose: the service decode path still builds its key set from `ServiceConfig.fields` and decodes with `onExcessProperty: "error"`, so `networks`, `configs`, `secrets`, `profiles`, and service `x-*` remain unsupported authored keys; the app-plan cache is back at its pre-story revision with the committed binary fixture matching; and the planner's compose-extension writer again writes only labels, healthcheck start interval, and dependency restart. Those paths staying classified `preserved` in the matrix is correct and is not the same claim as schema acceptance — classification is the prerequisite's declarative state, not behavior this story exercises.
- Provider spot checks are genuinely reachable in CI now rather than nominally env-gated: the workflow generator exports the Podman socket variable into `GITHUB_ENV` in both managed-runtime jobs, and the generated workflow test pins that the export precedes the provider test invocation. The suite still skips cleanly with the variable unset, and it reads the variable directly instead of resolving a managed socket, so it cannot quietly mutate a workstation runtime.
- Verification: typecheck and lint passed (the historical auxiliary-worktree lint blocker is gone); Compose coverage, import-cycle, package-DAG, machine-output, renderer- and paths-boundary gates passed; fixture-manifest and schema-snapshot codegen regenerated byte-identical. 18 conformance tests executed all 11 fixtures individually, and 229 adjacent tests across rejection, tag, include, lint, parser, planner, knob, cache, and generated-CI files passed. Real-CLI proof on a freshly compiled binary in the dev sandbox covered nine apps: long-form ports/volumes, runtime knobs, and depends_on-condition plus healthcheck all resolved at exit 0 with canonicalized values, while `container_name` and `deploy.replicas` exited 1 with service, keyPath, and matrix remediation in both the plain formatter and the JSON envelope. Two negative controls separate precision from blanket rejection: preserved `deploy.resources` and the supported `udp` short form both still exit 0.
- Review note (declined): a matrix-generated test synthesizing one document per rejected path was raised as optional hardening. It cites no acceptance criterion and would widen a verification pass into new coverage work, so it stays out; the rejected surface is already proven per-fixture and at the real CLI.
- **Learnings for future iterations:**
  - A "computed" derivation is only computed if you have watched it fail. Mutating a fixture to create a deliberately stale manifest entry costs about a minute and converts an untested invariant into a proven one; the same suite passed identically before and after the mutation was reverted, which is exactly why the proof was needed.
  - Scope anti-hiding to paths that actually appear in the corpus, not to every matrix path. Matrix-wide coverage sounds stricter but is a different contract, and here it would have dragged the excised prerequisite fields back in through the test suite after they were deliberately removed from production.
  - When a Drift Audit leaves a story false purely for re-verification, the honest Build move is to verify and confirm, not to find new work. Check that the excised surface is still absent at the production boundary first; if it is, the remaining job is evidence, and any code change is a regression risk with no acceptance criterion behind it.
  - An env-gated integration suite can be simultaneously correct and dead. Gating on a variable nothing ever sets means the lane never runs; pairing the skip guard with a generator-emitted export, and asserting the ordering between them, is what makes the gate a switch instead of an off button.
---

## 2026-07-27 23:45 CDT - US-476 Review
- Closed two confirmed review blockers: fixture-derived env files can no longer traverse or follow symlinks outside the canonical temporary app root, and the live Podman fixture now initializes its declared named-volume subpath before application bring-up while retaining the original fixture and inspect assertions.
- Guide-Coverage-Skip: US-476 adds internal fixture conformance and provider test setup; the executable Compose guide and published matrix are owned by US-477.
- Review notes (deferred/rejected): Cursor Bugbot was unavailable after both standard and Auto-mode attempts because the configured account is out of usage; no Bugbot findings were available to triage.
- Review notes (deferred/rejected): Regrowing service networks/configs/secrets/profiles/x-* was rejected because the prior Drift Audit assigned that production contract to binding prerequisite US-482; all review lanes confirmed the excision remains intact.
- Verification: typecheck, lint, explicit-base guide drift with the skip above, Compose coverage, focused fixture outcome/conformance/generated-CI tests, strict TypeScript checks, and the provider suite's no-socket skip path passed. A live provider rerun was attempted only in `lando4-dev` but remained blocked by its dependency-free baseline having neither Bun nor a managed Podman socket; it was not moved to the host or deferred as a CI substitute.
---

## 2026-07-28 00:04 CDT - US-476 Drift Audit
- Trigger: 5 cycles, 3191 changed lines, 23 commits
- Weakened proofs: the successful upstream fixtures relied only on expectations derived from decoded output; independent authored literals now pin their artifact, endpoint, and mount outcomes through the production loader and planner.
- Contract drift: US-476 was marked passing while five paths classified `preserved` still fail at the authoring boundary, contradicting the normative disposition contract and the recorded US-482 prerequisite; US-476 is false again until that prerequisite lands.
- Deferred-work laundering: removed the claim that the passing fixture story did not depend on US-482 while retaining the existing split rather than regrowing its public contract here.
- Fixed: strengthened the two remaining successful upstream fixture proofs and restored the binding prerequisite status without adding production behavior.
- Split/excise decisions: US-482 remains the binding follow-up for service `networks`, `configs`, `secrets`, `profiles`, and `x-*`; no new split or excision was needed, and US-476 stays false until those preserved dispositions are truthful end to end.
- Verification: typecheck, lint, focused conformance/fixture-maintenance/disposition/generated-CI tests, the provider no-socket safety path, Compose coverage, import-cycle, package-DAG, and machine-output gates passed. Live Podman execution remained unverified because the isolated dependency-free sandbox has neither Bun nor a managed socket.
---

## [2026-07-28 00:08 CDT] - US-476 Adjudication
- Contradiction: Build treated the US-476 criterion "Fixture selection is limited to normalized and preserved production behavior implemented before US-476" as permission to pass the fixture story while `networks`, `configs`, `secrets`, `profiles`, and service `x-*` remained unsupported, and Review rejected regrowing that production surface because it belongs to binding prerequisite US-482. Drift Audit rejected the passing verdict because spec §7.4 defines `preserved` as "accepted and carried in `ServicePlan.extensions.compose`, capability-checked per §5.5.1," spec §6.2 says the supported forms "are accepted," and US-476 promises the skip-list cannot hide unsupported-but-classified-supported behavior.
- Decision: narrowed US-476's first acceptance criterion to state that US-476 MUST remain `passes: false` until US-482 passes, while explicitly forbidding US-476 from implementing or reintroducing US-482's production surface. Swapped priorities so binding prerequisite US-482 is priority 11 and dependent US-476 is priority 12.
- Follow-up story: US-482 — make service `networks`, `configs`, `secrets`, `profiles`, and `x-*` honest end to end through acceptance, preservation, and fail-closed capability validation.
---

## 2026-07-28 01:22 CDT - US-482 (in progress, NOT complete)
- Implemented the story's production contract on the US-476 branch, because US-482's fixture criterion depends on the conformance harness that exists only there. Service-level `networks`, `configs`, `secrets`, `profiles`, and `x-*` are now accepted by the authoring schema in both their short and long vendored forms and canonicalized to long form (`["a"]` and a null attachment both become `{a:{}}`; a bare config/secret string becomes `{source}`), preserved losslessly into `ServicePlan.extensions.compose`, and gated fail-closed before provider action.
- The five fields were already classified `preserved` in the disposition matrix while the authoring schema rejected them as excess properties. That contradiction is what the prior audit flagged, and it is now resolved in the direction the matrix always claimed, so no matrix edit was needed and the coverage gate is untouched.
- Capability truthfulness is a NEW sibling declaration, `composeServiceFields.supported`, not a widening of the 24-key knob tier. It deliberately does NOT require `composeSpec: native`: that prerequisite is normatively scoped to per-container runtime knobs, and requiring it would stop a `portable` provider from truthfully accepting an inert field. Omitting the declaration means no support.
- The bundled provider declares only `x-*` and rejects the other four with a tagged capability failure naming service, key, provider, and remediation. This is the honest reading, verified against the provider rather than assumed: it attaches every service to every planned network while Compose attaches a service only to the networks it lists; top-level `configs:`/`secrets:` are preserved but never normalized into anything the provider consumes, so there is no content to mount; and there is no profile-activation surface, which the acceptance criteria explicitly forbid promising. `x-*` is supported because inert preservation IS its documented Compose semantics.
- The gate runs on the cache-hit path as well as the fresh-plan path, and the app-plan cache revision rolled because a stale plan predating these fields carries no evidence of them, so a plan-derived collector on a cache hit would otherwise see nothing and wrongly pass.
- Four upstream fixtures were vendored under the existing commit pin with computed per-file checksums, plus curated profiles and nested-extension fixtures. Two of the upstream fixtures could not load at all until top-level `networks:` accepted a null value, which the vendored schema has always allowed; that was treated as in-scope rather than split, because splitting it would have made the fixture criterion impossible to satisfy.
- Verification so far: typecheck, lint, and deprecation gates pass; 108 focused tests across 15 schema, loader, planner, cache, conformance, provider, and container-runtime files pass with 665 assertions.
- REMAINING BEFORE THIS STORY CAN PASS (next Build resumes here): `codegen:schema-snapshot` currently fails its public-schema annotation gate with exactly three issues — `ServiceConfigInput.networks`, `.configs`, `.secrets` report a missing field description. The descriptions exist on the canonical `ServiceConfig` but are lost across `Schema.encodedBoundSchema` for the transformed fields, so the annotation must also be carried on the transform's encoded side. `profiles` and the extension record already satisfy the gate. After that: regenerate schema artifacts and reference pages, add the API compatibility entries, triage `check:schema-compatibility` findings, run the remaining boundary gates and codegen idempotence, and perform real-CLI verification of the accept and reject paths.
- US-476 remains false per the standing adjudication and must not be reconsidered until US-482 passes.
- **Learnings for future iterations:**
  - `Schema.extend(struct, Schema.Record(...))` destroys `.fields`, and a core loader builds its service-key allowlist from `Object.keys(ServiceConfig.fields)`. Pass the extension record as the second positional argument to `Schema.Struct` instead. An `x-` prefixed key also never appears in that allowlist, so the loader's unsupported-key predicate needs an explicit prefix escape.
  - A field description on a canonical schema does not automatically survive to the published input schema when the field is a transform. The annotation gate only fires at codegen time, so a story that adds transformed public fields should run `codegen:schema-snapshot` early rather than treating it as a final formality.
  - Splitting work is the right instinct only when the split leaves both halves provable. Top-level null network values looked like separate scope, but two mandated fixtures cannot load without it, so splitting would have produced another story that could not close — the same failure mode that created this one.
---

## [2026-07-28 03:32 CDT] - US-482 Human Adjudication: composeServiceFields capability contract
- Contradiction resolved: the in-progress sibling `composeServiceFields.supported` declaration conflicted with spec §5.4's claim that `composeSpec` plus `composeKnobs` form the Compose capability surface, and §5.5.1 defined a capability-check algorithm only for runtime knobs. Human adjudication (with Oracle review) chose option 1 — `composeServiceFields` is a fail-closed refinement UNDER `composeSpec: native`, exactly analogous to `composeKnobs` — with two corrections to the escalation framing:
  - `x-*` is REMOVED from the capability surface entirely. Compose defines unknown extension fields as inert metadata; core preserves service-level `x-*` losslessly in `ServicePlan.extensions.compose` with no capability gating. A namespace that later gains runtime semantics is gated via `providerExtensions` or an explicit capability, never wildcard `x-*`.
  - `labels` is ADDED as a gated family, closing the silent-drop hole: §6.2 lists labels among preserved-and-capability-checked fields, but user labels never reached containers. `commonContainerLabels` now merges preserved `extensions.compose.labels` under Lando-owned `dev.lando.*` labels (Lando wins conflicts), and provider-lando Compose emission writes them too.
- Normative spec amended FIRST (human-owned): §5.4 adds `ComposeServiceFieldKey` (networks, configs, secrets, profiles, labels) + `ComposeServiceFieldCapabilities`, extends the capability-surface paragraphs with identical fail-closed rules (omit = empty; non-empty below native = inconsistent declaration rejected by §13.1; declaring a family = realizing every accepted form), adds the x-* carve-out paragraph, and clarifies portable-tier "networks" means top-level normalization while service-level attachments are native-gated. §5.5.1 steps 3-4 extended accordingly. §6.2 and §7.4 updated with the composeServiceFields reference and x-* inert carve-out.
- Implementation conformed to the amended spec: SDK key set changed; provider contract adds "compose service field support requires native composeSpec"; core collector drops x-* and adds labels; planner gate requires native tier + family membership; provider-lando/docker/podman all declare `{ supported: ["labels"] }` truthfully (docker moved portable→native, which blesses nothing by itself since both refinements are fail-closed, and docker realizes preserved labels). provider-lando still rejects networks/configs/secrets/profiles with tagged CapabilityError per the standing rationale.
- Schema-compatibility triage completed: 65 reviewed unknowns (all additive acceptance widening from US-482 vendored forms plus the composeServiceFields struct) accepted via sdk/compatibility-exceptions.json with per-pattern justifications; gate now reports breaking=0 unknown=0 accepted=65.
- Verification: typecheck, lint, check:compose-coverage, check:package-dag, check:renderer-boundary, check:probe-boundary, check:import-cycle, check:managed-file-boundary, check:paths-boundary, check:schema-compatibility all pass; codegen:schema-snapshot idempotent; 155 focused tests across 11 files pass (x-* inert preservation, portable-tier fail-closed labels, contract-suite inconsistent-declaration rejection, user-label realization with Lando precedence all asserted). Real-CLI QA: `lando app:cache:refresh --json` accepts labels + x-custom (ok:true) and rejects profiles with tagged CapabilityError naming service, field, provider, and remediation.
- Pre-existing, NOT caused by this work: full `bun test` / `bun run test:unit` hang at interactive prompts in core/test/services/base-composition.test.ts; live-provider suites show Podman `conmon failed: exit status 1` host-runtime failures and one missing packaged log-helper payload.
- US-482 remains passes: false pending its own Build/Review/Drift-Audit cycle. The four upstream fixtures (different_networks, simple_network, simple_configfile, simple_secretfile) and curated profiles/nested-extension fixtures are vendored and exercised by the conformance suite, which passes under the new contract; Review must still confirm the realization-or-rejection assertions satisfy criteria 4-6 (including final preserved plan shape and cache-hit parity) before flipping the story. US-476 remains false until US-482 passes, per the standing adjudication.

## 2026-07-28 04:20 CDT - US-482 (completion)
- Closed the story against the human-adjudicated capability contract. `composeServiceFields` is now a fail-closed refinement UNDER `composeSpec: native`, exactly analogous to `composeKnobs`, rather than a sibling declaration that could bless fields on a portable provider. The gated family set is `networks`, `configs`, `secrets`, `profiles`, and `labels`; omitting the declaration means no support, and a non-empty declaration below the native tier is an inconsistent declaration the provider contract suite rejects outright.
- Service-level `x-*` was removed from the capability surface entirely rather than declared supported. Compose defines unknown extension fields as inert metadata, so core preserves them losslessly in `ServicePlan.extensions.compose` with no capability gating; a namespace that later gains runtime semantics gets `providerExtensions` or its own capability, never a wildcard. `labels` moved the other way and became a gated family, which closed a real silent-drop hole: spec 6.2 listed labels among preserved-and-capability-checked fields, but authored labels never reached containers. User labels now merge underneath Lando-owned `dev.lando.*` labels (Lando wins conflicts) in both direct container creation and Compose emission.
- All three bundled providers declare `{ supported: ["labels"] }` and nothing else, which is the truthful reading verified against provider behavior rather than assumed. provider-lando attaches every service to every planned network while Compose attaches only to listed networks; top-level `configs:`/`secrets:` are preserved but never normalized into anything the provider consumes, so there is no content to mount; and there is no profile-activation surface at all. Those four fields therefore reject before provider action instead of being silently accepted. provider-docker moved portable to native, which blesses nothing by itself because both refinements are fail-closed, and it does realize preserved labels.
- The five fields are accepted in their vendored short and long forms and canonicalized to long form, with canonical-schema annotations that state preservation and explicitly disclaim Lando-side activation, so nothing promises profile activation ahead of its existence. The capability gate runs on the cache-hit path as well as the fresh-plan path, and the app-plan cache revision rolled because a stale plan predating these fields carries no evidence of them.
- Proved the cache-hit guard actually bites instead of trusting that it exists: removing the single gate call from the warm-cache branch turned the cached-`networks` regression red with its own assertion message, and restoring the file byte-identical returned it green. That path is unreachable from the CLI, because a first plan carrying an unsupported field already fails, so a mutation was the only honest proof available.
- Verification: typecheck and lint passed; compose-coverage, import-cycle, package-DAG, renderer-, paths-, probe-, state-store-, redaction-, managed-file-boundary, and schema-compatibility gates passed; full `bun run codegen` and `codegen:schema-snapshot` were both byte-identical on rerun. Focused spot checks covered SDK schema, provider contract, loader, planner, cache, conformance, container-runtime, and all three provider plugins. Real-CLI proof on a freshly compiled binary in the dev sandbox covered six apps: an app carrying `labels` plus a nested service-level `x-custom` resolved at exit 0 with both values intact in the resolved config, and `profiles`, `networks`, `configs`, and `secrets` each exited 1 with a tagged `CapabilityError` naming the service, the field, the provider, and matrix remediation. A bare app with no Compose service fields exited 0 as a negative control, separating a precise gate from a blanket one.
- Recorded, NOT a source defect: one 13-file batch run showed four provider-declaration assertions reading an empty supported list while a `bun build --compile` ran concurrently in the same checkout. The declarations are unconditional in source with no platform branching, the file passed 15 of 15 isolated runs, the identical batch passed on every repeat, and a deliberate concurrent-build reproduction attempt stayed green. CI does not compile a binary alongside the unit lane.
- US-476 stays `passes: false`. Its standing adjudication only required US-482 to land first; confirming US-476 against its own narrowed contract is a separate story and not this build step's work.
- **Learnings for future iterations:**
  - A capability refinement must sit under the tier it refines, not beside it. As a sibling, `composeServiceFields` let a portable provider declare support for fields the coarse tier says it cannot realize; nesting it under `native` made the fail-closed rule expressible in one sentence and reusable verbatim from the existing knob tier.
  - "Preserved and capability-checked" in a spec is a claim about behavior, and `labels` shows it can be false in the silent direction. Rejection gaps get noticed because users see errors; realization gaps do not, because the value simply never arrives. When auditing a preserved list, check that each field reaches something, not just that it survives planning.
  - Inert-by-definition fields do not belong in a capability surface. Gating `x-*` forced every provider to declare a wildcard it could not meaningfully refuse, which is indistinguishable from no gate at all while looking stricter.
  - Adding a field to `PUBLIC_FIELD_DESCRIPTION_EXEMPTIONS` is only honest when the canonical side is still gate-enforced. Here the canonical `ServiceConfig` entries carry the descriptions and are not exempt, and the input entries join roughly thirty existing transformed fields that lose annotations across `encodedBoundSchema` — check that split before treating an exemption as a suppressed gate.
  - Do not run a compiled-binary build concurrently with a unit-test batch in the same checkout. It produced a four-test failure that never reproduced and cost a full investigation to clear.
---

## 2026-07-28 04:30 CDT - US-482
- Closed the story against its full contract. Service-level `networks`, `configs`, `secrets`, `profiles` are accepted in both vendored short and long forms and canonicalized to long form, service-level `x-*` (including nested) is preserved inert, and every accepted value is carried losslessly into `ServicePlan.extensions.compose` without clobbering labels contributed earlier in composition. Capability validation is fail-closed before provider action on both the fresh-plan and cache-hit paths, and the bundled providers declare only `labels`, rejecting the other four families with a tagged `CapabilityError` naming service, field, provider, and remediation.
- Resolved the annotation gap the previous pass had papered over. `ServiceConfigInput` is derived through `Schema.encodedBoundSchema`, which drops the property-signature description for any field backed by a `Schema.transform` — the reason ~20 pre-existing input fields sit in the public-schema annotation exemption list. The prior pass added `networks`, `configs`, `secrets` to that list. Instead the descriptions now ride the transform's encoded side, which puts them in the published `service-config-input.json` where they previously did not exist at all, and the three exemptions were removed rather than added.
- That required teaching the annotation gate to read a description from the single declared member of an optional `Union(T, undefined)`, which is exactly where the JSON Schema emitter already reads it. The gate had been reporting "missing description" for fields whose emitted schema demonstrably carried one, and every such false positive was being retired by exemption. Because an exemption is permanent and unpoliced, that route would have kept passing even if the descriptions were later deleted; the three fields are now genuinely enforced instead. Oracle review narrowed the member check from "any declared member" to "exactly one declared member" so a partially documented multi-variant union cannot pass, and three gate tests pin all three behaviors.
- Two real defects surfaced only from adjacent tests that the story's own suite never ran: the committed fixture-enumeration inventory was never updated for the six fixtures this story vendored, and the shared planner capability stub modelled provider-lando without declaring the `labels` family it actually realizes, so its label fixtures tripped the new gate. Both were corrected at the source of the inaccuracy rather than by relaxing the assertions.
- Deliberately not done: extending the same union-member lookup to the docs-reference renderer. It is a one-line change and it works, but it rewrites description cells across 124 generated schema pages that have nothing to do with Compose, which is precisely the unrelated generated drift prior audits on this branch removed. The published JSON contract is honest without it; the remaining `—` cells for pre-existing transformed fields are untouched legacy debt.
- Verification: typecheck and lint pass; 42 focused files across schema, loader, planner, cache, conformance, container-runtime, provider-contract, and each provider plugin pass with 607 tests; all nine boundary gates and `check:schema-compatibility` pass; `codegen:schema-snapshot` is byte-identical on rerun. Real-CLI proof on a freshly compiled binary in the dev sandbox: labels plus nested `x-*` plan at exit 0, each of `profiles`/`networks`/`configs`/`secrets` exits 1 with the tagged error in both the plain and JSON formatters, an `x-*`-only app exits 0 proving extensions are not gated, and the pre-existing compose-passthrough guide app is unaffected. The capability gate was mutation-proved: setting the bundled provider's `supported` list to `[]` turned the previously passing labels app into a `CapabilityError` at the real CLI, so the gate reads the declaration rather than a hardcoded list.
- **Learnings for future iterations:**
  - An exemption list is not the cheap equivalent of a fix. Exempting a field permanently excuses it and nothing ever detects that the exemption became stale, so a gate plus a growing exemption list drifts toward asserting nothing. When a gate reports something the generated artifact contradicts, suspect the gate's traversal before reaching for the escape hatch.
  - `Schema.encodedBoundSchema` silently drops property-signature annotations for transformed fields. Annotate the transform's `from` schema, not just the field site, or the published input schema ships an undocumented field while the canonical schema looks fine.
  - Run the adjacent unmodified tests, not just the ones the story touched. Both genuine defects here were invisible to every test the story itself added or modified, and one of them was a hardcoded inventory that silently disagreed with the fixtures the same story vendored.
  - Scope discipline applies to generated output too. The correct fix and the in-scope fix are not always the same change; a one-line renderer improvement that repaints 124 unrelated generated pages belongs to its own story.

## 2026-07-28 04:32 CDT - US-476 Adjudication
- Contradiction: Review rejected regrowing service `networks`, `configs`, `secrets`, `profiles`, and `x-*` because US-482 owns that production surface, while Drift Audit would not close US-476 without those matrix-`preserved` paths satisfying spec §7.4 (`preserved` means "accepted and carried in `ServicePlan.extensions.compose`" under the §5.5.1 capability rules) and without a fresh live Podman run for the criterion "Provider-level spot checks for a small fixture subset run in the env-gated Podman integration suite." US-482 now passes, so the §6.2/§7.4 production contradiction is resolved; the remaining ambiguity was whether US-482 fixtures sharing the harness regrow US-476 and whether a live CI result is required before the loop can dispatch CI.
- Decision: narrowed US-476's acceptance criteria in `spec/compose/prd.json`. Fixtures expressly owned by US-482 may execute through the shared harness without becoming US-476 production scope. Pre-push provider proof is the real create/start/inspect integration suite, generated managed-runtime workflow ordering that exports `LANDO_TEST_PODMAN_SOCKET` before invoking it, and the safe no-socket path; Push/Babysit consumes the fresh live result, and a live CI failure reopens the story. US-482 passing satisfies the binding prerequisite; US-476 remains false for one Build confirmation against this clarified contract.
- Follow-up story (if created): none; US-482 already owns and has completed the disputed native-field surface.
---

## 2026-07-28 04:52 CDT - US-476 (completion)
- Confirmed the conformance fixture suite against the adjudicated contract without changing production code. The branch's Compose delta is unchanged: the story still proves only the vocabulary that shipped before it, and the native service fields the earlier Drift Audit excised remain owned by the prerequisite that has since landed. Fixtures owned by that prerequisite execute through the shared harness, which is now explicitly permitted rather than treated as scope regrowth.
- Re-proved the harness at the current branch head rather than trusting the pre-prerequisite proof, because the prerequisite changed the loader, planner, capability gate, and app-plan cache the fixtures assert through. The loader-level suite runs all 17 fixtures, the computed rejection manifest still matches runtime classification, and the anti-hiding guard still covers every classified path that appears in the corpus.
- Made the load-bearing CI gate a proven switch instead of a permanently green assertion. Removing the managed-runtime socket export from the workflow generator and regenerating turned the generated-workflow test red on the exact export line; restoring the generator produced a byte-identical workflow and returned it green. That export feeds both managed-runtime jobs from one template, so a single deletion blanks the variable everywhere — which is precisely why the ordering assertion has to exist.
- Provider spot checks exercise real create/start/inspect: planning runs through the production loader and planner, bring-up creates and starts the containers, and container inspect asserts port bindings, bind/volume/tmpfs mount shapes, healthcheck test and timings, and the absence of extension keys in realized config. The five prerequisite-owned fixtures assert fail-closed rejection with a provider transport that counts calls, proving the capability gate fires before any provider request. With no socket exported the whole file skips: 8 skipped, 0 failed, no runtime contact.
- Verification: typecheck and lint passed; nine boundary gates passed; full codegen and the offline fixture manifest regenerated byte-identical with no drift. 122 conformance and fixture-maintenance tests and 151 adjacent planner, cache, loader, schema-snapshot, and provider-plugin tests passed. Real-CLI proof on a freshly compiled binary in the dev sandbox covered seven apps built from the committed corpus: long-form ports/volumes/tmpfs, Compose healthcheck, depends_on conditions, list environment with env_file and labels, and the runtime-knob tier all resolved at exit 0 in canonical long form, while the rejected `container_name` app exited 1 with the tagged rejection carrying matrix remediation. The supported UDP short form exiting 0 separates a precise rejection surface from a blanket one.
- Deliberately not done: no new fixtures, assertions, or production behavior. The adjudication left this story false for one confirmation pass, and inventing work there would add regression risk with no acceptance criterion behind it. The fresh live Podman result belongs to CI by the same adjudication, and a live failure reopens the story.
- **Learnings for future iterations:**
  - A confirmation-only Build still has to re-run the proof at the current head. A sibling story merged in between can rewrite the exact production path the fixtures assert, so "it passed when it was written" is not evidence about the branch you are about to ship.
  - Mutate the gate, not just the fixture. Fixture mutation proves the suite reads its inputs; deleting the generator line proves the CI wiring assertion would actually notice the lane going dark. Those are different failure modes and only the second one catches a silently unrun integration suite.
  - When a generated workflow emits the same step into several jobs from one template, an assertion scoped to a single job block is still sufficient coverage for the deletion case, because the template is the only place the step can be lost.
---

## 2026-07-28 05:47 CDT - US-476 Review
- Closed three review blockers without changing production code: the conformance suite now pins exact equality between independent authored-value expectations and every non-rejected committed fixture; the shared live-fixture path requires Podman inspect to report both `State.Running: true` and `State.Status: running`; and fixture, pin, and manifest maintenance writes reject symlinked roots, parents, and destinations before replacing regular files. The authored-value guard was mutation-proved by deleting the healthcheck path and expectation together: the old one-way guard stayed green while the new guard failed, then returned green after byte-identical restoration. The write guard was proved red against the prior writer and green with external sentinel bytes unchanged for root, parent, fixture, pin, and manifest symlinks.
- Reconciled the narrative source PRD with the final adjudicated contract: US-482 remains the binding prerequisite and owner of its production surface, its fixtures may share the harness, and Push/Babysit still owns the fresh live Podman result. The no-socket provider path remained 8 skipped and 0 failed; no local live Podman run was required or performed. `prd.json`, `passes: true`, and all production `src/` paths remain untouched.
- Review notes (deferred/rejected): Cursor Bugbot produced no findings because the configured account was out of usage; that lane is unavailable rather than passing. The initial security assessment that symlink writes required same-user checkout access was rejected after applying the untrusted-branch threat model: Git can materialize the malicious symlink, so the resulting external overwrite was in scope and fixed.
- Guide-Coverage-Skip: US-476 and US-482 change internal Compose fixture, schema, planner, and provider conformance; the executable Compose guide and published key matrix are explicitly owned by US-477.
- Verification: typecheck and lint passed; focused conformance, fixture-maintenance security, manifest generation, container-state, and socket-unset provider tests passed; probe and renderer boundaries passed; explicit-base guide drift passed with the substantive skip above; offline manifest regeneration was byte-identical. Root `tsc -b` does not typecheck `core/test/**` or plugin test trees, so the new guarantees are runtime assertions covered by focused Bun tests rather than type-only links.
- **Learnings for future iterations:** A one-way coverage guard does not prove exhaustiveness; compare the independently authored oracle with the complete eligible corpus. HTTP 200 from container inspect proves the object exists, not that it started. Checksum-pinned bytes do not make their destination safe; maintenance writers must reject symlinked path components and final targets.
---

## 2026-07-28 06:05 CDT - US-476 Review
- Closed the two remaining fixture-maintenance confinement gaps: every write now anchors path-component checks at a trusted workspace root so a tracked symlink above the fixture root cannot redirect output, and source URLs percent-encode each path segment so encoded dot segments cannot normalize into another repository. Both regressions were confirmed red before the fixes and green afterward while external sentinel bytes remained unchanged.
- Review notes (deferred/rejected): eliminating the residual check-to-write TOCTOU window would require directory-handle-relative `openat`/`renameat` primitives that Bun's filesystem API does not expose; the finding requires a separate concurrent same-user filesystem actor and is outside the repository-controlled branch threat model, so it is not a US-476 blocker.
- Review notes (deferred/rejected): Cursor Bugbot remained unavailable because the configured account is out of usage; the five `review-work` lanes otherwise completed, with the confirmed static confinement findings fixed above.
- Guide-Coverage-Skip: US-476 and US-482 change internal Compose fixture, schema, planner, and provider conformance; the executable Compose guide and published key matrix are explicitly owned by US-477.
- Verification: typecheck and lint passed; focused fixture refresh, pin, manifest, conformance, generated-CI, container-state, and socket-unset provider tests passed; Compose coverage and explicit-base guide drift passed; changed-file diagnostics and strict TypeScript no-excuse checks were clean.
- **Learnings for future iterations:** Symlink validation must begin at a trusted checkout anchor, not at the first destination-specific directory; URL path strings must be encoded by segment before interpolation even when ordinary `..` traversal is already rejected.
---

## 2026-07-28 06:10 CDT - US-476 Adjudication
- Contradiction: Review treated the residual check-to-write TOCTOU window as non-blocking because exploiting it requires "a separate concurrent same-user filesystem actor" outside the repository-controlled branch threat model, while Drift Audit escalated whether path containment must eliminate that race. US-476 only required "a pinned refresh script (US-466 pattern), offline at test time," and spec §7.4 only says the vendored upstream schema is "pinned in-repo (tag + checksum)" and that bumping the pin is deliberate; neither text defined the fixture writer's concurrency threat model.
- Decision: narrowed US-476's first acceptance criterion in `spec/compose/prd.json`. Repository-controlled path, pin, manifest, tracked-symlink, and URL-segment inputs are explicitly untrusted and must remain confined to the anchored workspace/fixture root, but the codegen command assumes exclusive checkout access and need not defend against a separate same-user actor racing filesystem mutations between validation and write.
- Follow-up story (if created): none; the clarified repository-controlled threat model resolves the gate conflict without adding another production surface.
---

## 2026-07-28 06:35 CDT - US-476 (completion)
- Confirmed the conformance fixture suite against the newly adjudicated contract and closed the one clause that adjudication had left unproven. The 06:10 decision added "non-regular destinations are rejected" to the refresh threat model, but the fixture writer's `not-file` branch had zero test references while all six existing path-safety tests covered symlinks only. Two tests now drive a directory-shaped destination through the real refresh and manifest-generation entrypoints and assert the tagged rejection plus unchanged bytes inside that directory.
- Proved the new guard bites rather than passing incidentally. Removing the `not-file` check turned exactly those two tests red while the other six stayed green, and the failure showed why the guard matters: without it the writer creates its temporary file and then `rename` raises a raw `EISDIR`, instead of refusing before the temporary write with a tagged error naming the failure. The cleanup still removes that temporary file. Restoring the file byte-identical returned the suite to green, so this story still contributes no production change.
- Reconciled the narrative source PRD with the machine contract. Adjudication amended only the JSON index, leaving the prose criterion describing a refresh script with no threat model at all; both documents now state the same anchored-root, symlink, non-regular-destination, and per-segment-encoding rules, and the same exclusive-checkout assumption that puts a concurrent same-user filesystem race out of scope.
- Deliberately not done: no production code, no new fixtures, and no reopening of the native service fields the earlier audit moved to the prerequisite that has since landed. The adjudication left this story needing one confirmation pass, and the only gap that pass found was a missing proof, not a missing behavior.
- Verification: typecheck and lint passed; ten boundary gates, both guide gates, and full codegen were clean with codegen byte-identical. Twenty-two focused files passed across conformance, fixture maintenance, generated CI, coverage, disposition, rejection, loader, planner, cache, schema-snapshot, and container-runtime surfaces, and the env-gated provider suite skipped cleanly at 8 skipped and 0 failed with no socket exported and no runtime contact. Real-surface proof ran the actual offline manifest codegen through the guarded writer, which wrote 17 fixtures at exit 0 with the committed manifest byte-identical.
- **Learnings for future iterations:**
  - A test-runner loop must require both command success and a positive test count. Bun emits "did not match any test files" and exits nonzero for a stale filter, but a summary parser that only looks for a fail count can still report a nonexistent path as passing. Two supplied paths were wrong here and the first sweep called both green; one file had merely moved directories and the other never existed.
  - When an adjudication narrows a criterion, check whether the clause it added is actually tested. New criterion text arrives already marked complete because the surrounding story passed, and the specific behavior it names can be implemented yet entirely unproven.
  - Amendments to the machine-readable index do not propagate to the narrative PRD. Verify both after every adjudication, or the prose contract silently drifts from the one the loop reads.
  - Mutating a guard is worth the minute it costs even when the test is already green. Here it converted "the branch exists" into a recorded demonstration that its absence degrades a clean pre-write refusal into a temporary write followed by a raw rename failure.
---

## 2026-07-28 06:40 CDT - US-476 Review
- Corrected the reusable Bun spot-check guidance: an unmatched filter exits nonzero, and scripted verification must require both command success and a positive test count. Also corrected the mutation-proof narrative to distinguish a cleaned-up temporary write plus raw rename failure from a persistent partial write.
- Review notes (deferred/rejected): Cursor Bugbot was unavailable because the configured account is out of usage; the five review-work lanes otherwise reached terminal verdicts after retrying the initially unavailable security lane without its team-mode skill.
- Guide-Coverage-Skip: US-476 and US-482 change internal Compose fixture, schema, planner, and provider conformance; the executable Compose guide and published key matrix are explicitly owned by US-477.
- Verification: typecheck and lint passed; the changed fixture-safety test and adjacent refresh/manifest tests passed; the no-socket provider suite skipped 8 tests with no runtime contact; offline manifest codegen was byte-identical; explicit-base guide drift passed with the substantive skip above.
---

## 2026-07-28 06:44 CDT - US-476 Adjudication
- Contradiction: repeated Drift Audit/Review cycles continued treating two already-adjudicated absences as grounds to reopen US-476. The refresh AC says "defending against a separate same-user process racing filesystem mutations between validation and write is not a US-476 acceptance criterion," while the live-proof AC says "a fresh live Podman result is consumed by Push/Babysit CI rather than required before passes can flip." Review accepted those boundaries, but Drift Audit continued escalating the residual TOCTOU and lack of a fresh pre-dispatch live run. Normative spec §7.4 requires only that the upstream schema be "pinned in-repo (tag + checksum)" and that "Bumping the pin is a deliberate act"; spec §13.1 defines real-provider end-to-end coverage but does not assign either disputed proof to Build, Review, or Drift Audit.
- Decision: narrowed both US-476 acceptance criteria in `spec/compose/prd.json`. The exclusive-checkout assumption is now explicitly authoritative for Build, Review, and Drift Audit, and a residual concurrent same-user validation-to-write race cannot block or reverse `passes`. Build, Review, and Drift Audit are also explicitly forbidden from requiring or attempting a fresh live Podman run before retaining `passes: true`; only Push/Babysit owns that result, and only its failed live CI result reopens the story.
- Follow-up story (if created): none; this is gate-ownership clarification, not an unowned production surface.
---

## 2026-07-28 06:52 CDT - US-476 (completion)
- Confirmed the conformance fixture suite against the gate-ownership adjudication with no production change. The only gap this pass found was documentary: the adjudication amended the machine-readable index alone, so the narrative PRD still described the old contract on both amended criteria. Both documents now state the same thing — the exclusive-checkout assumption is authoritative for Build, Review, and Drift Audit and a residual concurrent same-user validation-to-write race cannot block or reverse the story, and no gate before Push/Babysit may require or attempt a fresh live Podman run, with only a failed live CI result reopening it.
- Re-ran the whole proof at the current head rather than trusting the previous pass. Every changed test file on the branch executed individually and green across conformance, fixture maintenance, generated CI, disposition, loader, planner, cache, container-runtime, provider-plugin, and SDK schema/contract surfaces. The env-gated provider suite skipped cleanly with no socket exported and made no runtime contact.
- Proved the suite is not vacuous at this head instead of reporting that it is green. Deleting the rejected key from the rejecting fixture fired two independent guards with exact diffs: the computed rejection manifest disagreed with the committed one, and the fixture fell out of the rejected set without an authored-value expectation to replace it. Restoring the file returned the same checksum and the suite to green, so the anti-hiding and exhaustiveness guards both still bite.
- Established that the story carries no CLI delta to re-verify: the newest commit touching any package `src/` predates the previously recorded compiled-binary CLI proof, and every commit since is tests, codegen scripts, or prose. The real-surface artifact for this pass is the offline fixture manifest codegen running through the guarded writer, which wrote 17 fixtures at exit 0 with the committed manifest byte-identical.
- Deliberately not done: no production code, no new fixtures, no live Podman execution. The adjudication assigns the fresh managed-runtime result to Push/Babysit, and inventing work on a confirmation pass is regression risk with no acceptance criterion behind it.
- Verification: typecheck and lint passed; nine boundary gates and guide coverage passed; the offline fixture manifest regenerated byte-identical. Thirty-two focused test files passed with 389 tests, and the env-gated provider suite skipped 8 with 0 failures.
- **Learnings for future iterations:**
  - Check the narrative PRD after every adjudication. New criterion text lands already marked complete because the surrounding story passes, so a prose/index divergence is invisible to every gate and surfaces later as a re-litigated boundary.
  - A confirmation pass earns the right to skip re-running an expensive proof only by showing the proof's inputs did not move. Here the argument is a one-line log query — the last commit touching any `src/` predates the recorded CLI proof — which is cheaper and more honest than either re-compiling a binary or silently assuming nothing changed.
  - Mutating one fixture can fire more than one guard, and that is worth reading rather than stopping at the first red. The stale-manifest failure and the missing authored-expectation failure cover different hiding modes, and seeing both proves the exhaustiveness guard is coupled to the rejected set rather than to a hand-listed corpus.
---

## 2026-07-28 07:02 CDT - US-476 Review
- Corrected the durable completion chronology to match the commit that recorded it and narrowed the adjudication learning so it applies only when the machine index changes without a matching narrative PRD update.
- Review notes (deferred/rejected): Cursor Bugbot produced no findings because the configured account is out of usage; the five review-work lanes and a direct security retry otherwise reached terminal verdicts, with no production, scope, or acceptance-criterion blocker after the evidence corrections.
- Guide-Coverage-Skip: US-476 and US-482 change internal Compose fixture, schema, planner, and provider conformance; the executable Compose guide and published key matrix are explicitly owned by US-477.
- Verification: typecheck and lint passed; explicit-base guide drift passed with the substantive skip above; focused conformance, fixture-safety, generated-CI, and no-socket spot checks passed without runtime contact.
---

## 2026-07-28 07:08 CDT - US-476 Adjudication
- Contradiction: Drift Audit again escalated two facts that the current ACs already make non-blocking for pre-push gates: the refresh AC says the exclusive-checkout assumption is "authoritative for Build, Review, and Drift Audit" and that the residual concurrent same-user validation-to-write TOCTOU "MUST NOT block or reverse passes"; the provider AC says those gates "MUST NOT require or attempt a fresh live Podman execution" and that "Push/Babysit exclusively owns" the result. Normative spec §7.4 requires only that the upstream schema be "pinned in-repo (tag + checksum)" and deliberately bumped, while §13.1 defines end-to-end as a real provider and §13.6 assigns real-provider execution to CI; neither spec section requires either disputed proof during Build, Review, or Drift Audit.
- Decision: narrowed US-476 in `spec/compose/prd.json` with a separate gate-finality criterion. Once the existing static confinement, production-path, generated-workflow, and no-socket proofs pass, either disputed absence is conclusively out of scope for Build, Review, and Drift Audit; a finding limited to either must be logged only as non-blocking and cannot change `passes` or trigger another adjudication. Reopening now requires a newly demonstrated failure of an in-scope proof or a failed Push/Babysit managed-runtime run.
- Follow-up story (if created): none; this is final enforcement of already-assigned gate ownership, not a new production surface.
---

## 2026-07-28 07:35 CDT - US-476 (completion)
- Confirmed the conformance fixture suite complete under the gate-finality adjudication with no production change. The only gap this pass found was again documentary: the adjudication amended the machine-readable index alone, so the narrative PRD carried no gate-finality criterion at all. Both documents now state the same rule — once the static confinement, production-path, generated-workflow, and no-socket proofs pass, a residual concurrent same-user validation-to-write race and the absence of a fresh managed-runtime result are conclusively out of scope for Build, Review, and Drift Audit, non-blocking as observations only, and neither can flip `passes` or trigger another adjudication.
- Re-proved every in-scope gate at the current head rather than inheriting the previous pass. Twenty-nine focused test files ran individually and green across conformance, fixture safety, generated CI, disposition, loader, planner, cache, provider, container-runtime, and SDK surfaces, with no skipped assertions. Ten boundary gates, three guide gates, and the Compose coverage gate passed at 362 service paths and 10 top-level paths.
- Proved the suite is not vacuous at this head instead of asserting it. Deleting the rejected key from the rejecting fixture fired two independent guards — the computed rejection manifest diverged from the committed one, and the fixture dropped out of the rejected set with no authored-value expectation to replace it — and restoring the file returned the identical checksum and a green suite. Both hiding modes therefore still bite.
- Proved the env-gated provider lane is safe rather than assuming it. With no socket exported the live fixture suite skipped all eight tests at exit 0, and the Podman client is constructed inside the fixture runner rather than at module load, so the skip path makes no runtime contact. The generated managed-runtime workflow still exports the socket before invoking the live suite, and the ordering assertion covering that export remains green.
- Established that the story carries no CLI delta to re-verify: no package `src/` has changed since well before the previously recorded compiled-binary CLI proof, and every commit since is tests, scripts, or prose. The real-surface artifact for this pass is the offline fixture manifest codegen running through the guarded writer, which wrote 17 fixtures at exit 0 with the committed fixtures byte-identical; full codegen also produced zero generated drift.
- Deliberately not done: no production code, no new fixtures, no live Podman execution, and no reopening of either adjudicated out-of-scope absence. The adjudication assigns the fresh managed-runtime result to Push/Babysit, and manufacturing work on a confirmation pass is regression risk with no acceptance criterion behind it.
- Verification: typecheck and lint passed; twenty-nine focused test files passed with no failures; ten boundary gates, three guide gates, and the Compose coverage gate passed; full codegen was byte-identical; the env-gated provider suite skipped 8 with 0 failures and no runtime contact.
- **Learnings for future iterations:**
  - The narrative-PRD reconciliation pattern now has a second, worse failure mode than prose drift: an adjudication can add an entirely NEW criterion to the index that the narrative never receives, so the prose contract is not merely stale but silently shorter than the one the loop enforces. Diff criterion counts between the two documents after every adjudication, not just criterion wording.
  - A confirmation pass should re-run the mutation proof rather than cite the previous one whenever the cost is seconds. It converts "the guard exists and the suite is green" into a recorded observation that removing the input turns it red, and here it re-demonstrated that the anti-hiding and exhaustiveness guards fail independently rather than through one shared assertion.
  - Verify an env-gated skip path by reading where the client is constructed, not only where the guard is declared. `test.skipIf` prevents the test body from running but does not prevent module-scope construction; the safety claim is only true because the Podman client is built inside the fixture runner.
---

## [2026-07-28 07:26 CDT] - US-476 Adjudication
- Contradiction: Review enforced the AC "A loader-level suite decodes each fixture's service blocks through the real Landofile loader and planner" and the provider AC "exercise real create/start/inspect behavior," while Drift Audit escalated that the conformance-fixture branch spans production Compose translation, planner behavior, and container-runtime capabilities. Those proof requirements were being read as authority to change the seams they execute, despite the first AC saying "US-476 fixture ownership is limited to normalized and preserved production behavior implemented before US-476" and that a missing production contract "MUST NOT add that behavior inside the fixture story." Normative spec section 7.4 requires normalized/preserved/rejected outcomes, section 6.2 names the supported service vocabulary, and section 5.5.1 requires normalization, preservation, and fail-closed capability checks; those sections define behavior to prove, not story ownership of its implementation.
- Decision: narrowed the existing loader-level and provider-level AC text in `spec/compose/prd.json`. US-476 now explicitly owns the fixtures and fixture-specific assertions not assigned to US-482, the read-only matrix classification/walker, refresh/manifest tooling, shared conformance/provider-test harness, no-socket guard, and generated CI wiring. Fixtures and fixture-specific loader/provider assertions assigned to US-482 remain US-482-owned and merely execute through the shared harness. Invoking the real loader, planner, container runtime, and provider is proof ownership only; any required ServiceConfig/ServicePlan, planner, cache, SDK capability, container-runtime translation, capability declaration, or provider realization change blocks on its owning prior, prerequisite, or follow-up story and MUST NOT be implemented as US-476 production work.
- Follow-up story (if created): none; US-482 already owns and has completed the adjacent native service-field schema, planner, cache, capability, and provider boundary.
---

## 2026-07-28 07:35 CDT - US-476 (completion)
- Confirmed the conformance fixture suite complete under the proof-ownership adjudication with no production change. The adjudication narrowed the loader-level and provider-level criteria to say that invoking the real loader, planner, container runtime, and provider is proof ownership only, and that any schema, planner, cache, capability, translation, or realization change a fixture demands belongs to the story that owns it. Nothing on the branch had to move: the native service-field surface those fixtures exercise is already owned and shipped by the prerequisite story, so the narrowing removed authority this branch never exercised.
- The only gap this pass found was again documentary, and again in the same direction: the adjudication amended the machine-readable index alone, so the narrative PRD's loader-level and provider-level criteria still granted the broad reading. Both documents now carry the same ownership sentences, and a criterion-by-criterion comparison of all nine boundaries shows only cosmetic wording differences remain.
- Re-proved every in-scope gate at the current head rather than inheriting the previous pass. All thirty changed test files ran individually and green with 381 passing tests and no failures, spanning conformance, fixture maintenance and safety, generated CI, disposition, loader, planner, cache, provider plugins, container runtime, and SDK schema/contract surfaces. Sixteen boundary and coverage gates passed, guide lint passed, and guide drift passed with the standing substantive skip.
- Proved the suite is not vacuous at this head instead of citing the earlier demonstration. Deleting the rejected key from the rejecting fixture fired two independent guards — the computed rejection manifest diverged from the committed one, and the fixture dropped out of the rejected set with no authored-value expectation to replace it — while the other thirty-four assertions stayed green. Restoring the file reproduced the identical checksum and returned the suite to green.
- Established there is no CLI delta to re-verify: the newest commit touching any package `src/` predates the previously recorded compiled-binary CLI proof by hours, and every commit since is tests, scripts, or prose. The real-surface artifact for this pass is the offline fixture and manifest codegen running through the guarded writer, which wrote 17 fixtures and 9 pinned files at exit 0 with the committed corpus byte-identical.
- Deliberately not done: no production code, no new fixtures, no live Podman execution, and no reopening of either adjudicated out-of-scope absence. A residual concurrent same-user validation-to-write race and the absence of a fresh managed-runtime result remain recorded observations only, per the gate-finality criterion.
- Verification: typecheck and lint passed; thirty focused test files passed with 381 tests and no failures; sixteen boundary and coverage gates plus guide lint and guide drift passed; offline fixture and manifest codegen were byte-identical; the env-gated provider suite skipped 8 with 0 failures and no runtime contact.
- **Learnings for future iterations:**
  - The index-versus-narrative divergence has now recurred on five consecutive adjudications, and every occurrence was caught by a human-driven read rather than by any gate. Treat "reconcile the prose PRD" as a standing first step of any post-adjudication pass rather than a discovery, and compare criteria pairwise with the wording normalized, because the substantive delta is usually one clause inside an otherwise identical paragraph.
  - An adjudication that removes authority is cheap to confirm precisely because nothing depends on authority that was never used. Verify that claim by checking who owns the adjacent production surface before assuming a narrowing is a no-op; here the prerequisite story had already landed and owned all of it.
  - Re-running the mutation proof is worth its seconds on every confirmation pass. It cost under a minute and converted an inherited claim into a fresh observation that both hiding-mode guards still fail independently at this head.
---

## 2026-07-28 07:40 CDT - US-476 Review
- Fixed the remaining PRD reconciliation blocker: the machine-readable index still fused the fixture-corpus and refresh-safety requirements into one item while the source PRD kept them separate. The two representations now have the same nine US-476 acceptance-criterion boundaries, preserving the source PRD as the authoritative contract.
- Review notes (deferred/rejected): Four independent review lanes found no implementation, QA, code-quality, or security blocker; the fifth found and prompted the criterion-boundary fix above. Fresh managed-runtime evidence and a concurrent same-user refresh race remain explicitly out of scope for Review under the gate-finality criterion.
- Review notes (deferred/rejected): Cursor Bugbot was unavailable because the configured account was out of usage, so it produced no findings to triage after two attempts.
- Guide-Coverage-Skip: US-476 and US-482 change internal Compose fixture, schema, planner, and provider conformance; the executable Compose guide and published key matrix are explicitly owned by US-477.
- Verification: typecheck and lint passed; explicit-base guide drift passed with the substantive skip above; 88 focused conformance, fixture-safety, generated-CI, and production-path helper tests passed, and the no-socket provider suite skipped 8 with 0 failures or runtime contact.
---

## 2026-07-28 07:58 CDT - US-476 Review
- Narrowed the proof-ownership criteria so US-476 retains the shared conformance/provider harness, refresh tooling, no-socket guard, and CI wiring without reclaiming fixtures or fixture-specific loader/provider assertions assigned to US-482. Reordered the two HEAD-added progress blocks and corrected the completion timestamp and adjudication wording so the durable chronology and ownership record match the commits and prior excision.
- Review notes (deferred/rejected): The residual concurrent same-user refresh race and absence of fresh managed-runtime evidence remain explicitly out of scope under the gate-finality criterion. Cursor Bugbot was unavailable after the required pre-fix attempts and post-fix retry because the configured account is out of usage, so it produced no findings to triage.
- Guide-Coverage-Skip: US-476 and US-482 change internal Compose fixture, schema, planner, and provider conformance; the executable Compose guide and published key matrix are explicitly owned by US-477.
- Verification: typecheck and lint passed; 54 focused closure/conformance tests passed; Compose coverage passed at 362 service paths and 10 top-level paths; explicit-base guide drift passed with the substantive skip above; the no-socket provider suite skipped 8 with 0 failures or runtime contact.
---

## 2026-07-28 08:02 CDT - US-476 Adjudication
- Contradiction: Review enforced the US-476 ACs "Fixtures assigned to US-482 and their fixture-specific loader-level expectations remain US-482-owned" and "Executing the real production path is proof ownership, not implementation ownership," while Drift Audit escalated the branch's production Compose normalization and planner behavior as unresolved US-476 scope absorption merely because the already-passing US-482 implementation is physically co-resident on this branch. Spec §7.4 says `preserved` means "accepted and carried in `ServicePlan.extensions.compose`, capability-checked per §5.5.1," spec §6.2 says the supported native service-field forms "are accepted," and spec §5.5.1 requires preservation plus fail-closed capability checks; those sections define the US-482 behavior but do not make its implementation US-476-owned when both stories share a branch.
- Decision: narrowed US-476's prerequisite and gate-finality AC text in `spec/compose/prd.json`. Whole-branch accounting must attribute schema, planner, cache, capability, container-runtime, and provider changes traceable to the already-passing US-482 criteria and progress record to US-482 even when co-resident on the US-476 branch. Their presence cannot be treated as US-476 absorption, excised, used to reverse `passes`, or used to trigger another adjudication; unowned production changes remain blocking.
- Follow-up story (if created): none; US-482 already owns and has completed the disputed production surface.
---

## 2026-07-28 08:15 CDT - US-476 (completion)
- Confirmed the conformance fixture suite complete under the whole-branch-accounting adjudication with no production change. The only gap this pass found was again documentary, and again the same divergence: the adjudication amended the machine-readable index alone, so the narrative PRD still lacked both amendments. The narrative now carries the whole-branch-accounting clause on the prerequisite criterion and the gate-finality criterion's expansion from two conclusively-out-of-scope facts to three, plus the new clauses forbidding excision of the US-482-owned prerequisite surface and preserving blocking status for genuinely unowned production changes. A normalized pairwise comparison of all nine criteria now leaves only synonymous connective wording between the two documents, with criterion boundaries at nine versus nine. This is the sixth consecutive adjudication to amend the index without touching the narrative, which is why reconciling the narrative PRD remains the first step of every post-adjudication pass rather than a discovery each time.
- Established the durable whole-branch accounting evidence so a future Drift Audit can resolve the co-residency question from this record instead of escalating a seventh time. The newest commit touching any package `src/` is `770578c2`, whose subject line is "feat: Native Compose service fields are honest end to end" — US-482's exact title — and the 21 intervening commits before this completion commit touch only tests, codegen scripts, and prose, with zero `src/` files among them. That makes the co-resident production surface traceable to US-482 by commit identity, which is exactly what the amended criterion requires.
- The inheritance ledger carries its warrant rather than a bare claim: nothing outside `spec/` and `AGENTS.md` has changed since commit `f5a9a684`, so the code tree at HEAD is byte-identical to the tree at which the previous pass recorded its full battery green. On that warrant this pass inherited the other 23 changed test files — the US-482-owned schema, planner, cache, capability, container-runtime, and provider surface the adjudication additionally forbids re-litigating as US-476 scope — and the boundary/coverage gate sweep. The environment-dependent and mutation proofs were not inherited: git identity says nothing about the ambient shell environment, and the log carries a standing instruction to re-run the mutation proof every pass.
- Re-proved every in-scope gate at this head. The seven anchor test files that are the named acceptance-criterion proofs passed individually, 93 tests total across them (7, 36, 14, 2, 3, 8, and 23 passing respectively). The Compose coverage gate passed at 362 service paths and 10 top-level paths, guide lint passed, and guide drift passed via the standing Guide-Coverage-Skip.
- The mutation proof is a fresh observation at this head rather than an inherited one, and both guards fired independently. Deleting the rejected `container_name` key from the rejecting corpus fixture failed "matches the committed rejection manifest exactly" because the computed rejection manifest diverged from the committed one, and separately failed "keeps authored-value expectations exhaustive for non-rejected fixtures" because the fixture dropped out of the rejected set with no authored-value expectation to replace it — 37 pass, 2 fail, with the other 37 assertions staying green, so the guards are targeted rather than a blanket failure. Restoring the file reproduced the identical sha256 and returned the pair of suites to 39 pass, 0 fail.
- The environment-gated provider lane was proved safe by observation, not assumption: with the socket variable explicitly unset, the live fixture suite reported 0 pass, 8 skip, 0 fail, so no runtime contact occurred.
- Both guarded-writer codegen paths ran and produced byte-identical committed output: the offline manifest generator wrote 17 fixtures, and the pinned refresh wrote 9 pinned files at pin `7cc9c7ce`, both at exit 0. There is no CLI delta to re-verify, because no package `src/` has moved since well before the previously recorded compiled-binary CLI proof.
- Deliberately not done: no production code, no new fixtures, no live Podman execution, and specifically no new index-versus-narrative synchronization gate — building one would be inventing work the confirmation contract forbids.
- Guide-Coverage-Skip: this branch changes internal Compose fixture, schema, planner, and provider conformance; the executable Compose guide and published key matrix are owned by the follow-up documentation story.
- Verification: typecheck and lint passed; the seven anchor files passed with 93 tests; Compose coverage passed at 362 service paths and 10 top-level paths; guide lint and guide drift passed; both codegen paths were byte-identical; the environment-gated provider suite skipped 8 with 0 failures and no runtime contact.
- **Learnings for future iterations:**
  - No machine gate anywhere validates index-versus-narrative agreement: the only consumer of `prd.json` reads just story id and `passes`, never criterion text. That is structurally why the divergence recurs, and it can only be caught by a human read.
  - The correct inheritance baseline is the last commit touching any non-spec file, not the last commit touching production source. That is what makes the "inputs did not move" argument an empty diff rather than a judgment call.
  - Recording attribution evidence by commit identity is what converts a repeat Drift Audit escalation into a non-blocking observation instead of a seventh round of the same dispute.
  - An environment-dependent proof can never be inherited from file identity, because git says nothing about the ambient shell; it has to be re-run every pass regardless of how little the tree moved.
---

## 2026-07-28 08:36 CDT - US-476 Review
- Corrected the durable branch-accounting evidence so its 21-commit count explicitly covers the intervening commits before the completion record rather than appearing stale when read at the committed head.
- Review notes (deferred/rejected): residual concurrent same-user TOCTOU, absence of a pre-dispatch live Podman result, and production work traceable to passing US-482 remain conclusively out of US-476 scope under the gate-finality criterion; no in-scope proof was reopened.
- Review notes (deferred/rejected): Cursor Bugbot was unavailable because the configured account is out of usage; no Bugbot finding was available to triage, while all five review-work lanes reached terminal verdicts after the accounting correction.
- Guide-Coverage-Skip: this branch changes internal Compose fixture, schema, planner, and provider conformance; the executable Compose guide and published key matrix are owned by the follow-up documentation story.
- Verification: typecheck and lint passed; explicit-base guide drift passed with the substantive skip above; focused documentary contract checks passed 23 assertions, including JSON validity, nine-criterion correspondence, both prerequisite pass states, and all four reconciled semantic anchors.
---

## 2026-07-28 08:47 CDT - US-476 Adjudication
- Contradiction: Review retained the gate-finality AC that the residual same-user checkout TOCTOU, absence of a pre-dispatch live Podman result, and production work traceable to passing US-482 are "conclusively out of US-476 scope" and that "no in-scope proof was reopened," while Drift Audit nevertheless escalated solely because US-476 had 26 recorded cycles and restated the same proof-ownership-versus-production-ownership boundary. The existing AC says a finding limited to those settled facts "MUST NOT ... trigger another adjudication." Normative spec §7.4 requires the schema pin and deliberate classification, spec §13.6 assigns full real-provider execution to CI, and spec §§6.2/5.5.1 define the native service-field production behavior already owned and completed by US-482; none makes accumulated cycle count, branch size, or co-residency new US-476 evidence.
- Decision: narrowed the gate-finality AC in `spec/compose/prd.json` so cycle count, branch size, and co-residency are explicitly subordinate to the three existing permitted reopening conditions. Drift Audit may signal adjudication only when its reason identifies one permitted condition and new evidence demonstrating it; recounting prior cycles or settled exclusions terminates as a non-blocking observation with `passes` unchanged.
- Follow-up story (if created): none; the disputed production surface remains completed under US-482, live managed-runtime proof remains Push/Babysit-owned, and no new in-scope failure was demonstrated.
---

## 2026-07-28 08:52 CDT - US-476
- Confirmed the conformance fixture suite complete under the cycle-count adjudication with no production change. The branch's Compose delta is unchanged, the service-level native fields the earlier Drift Audit excised from this story remain owned by the prerequisite that has since landed, and nothing was implemented to make a fixture pass.
- Refreshed the durable branch-accounting evidence so the next Drift Audit can settle co-residency from this record rather than escalate again. The newest commit touching any package `src/` is still the one titled "Native Compose service fields are honest end to end" — the prerequisite's exact title — and every intervening commit before this completion record touches only tests, codegen scripts, and prose with zero `src/` files among them.
- Reconciled the narrative PRD with the machine index for the seventh consecutive adjudication. The 08:47 amendment again landed only in the JSON index, so the narrative gate-finality criterion still lacked the clause subordinating cycle count, accumulated branch size, and branch co-residency to the three permitted reopening conditions. Both documents now state that rule identically, and a normalized pairwise comparison of all nine criteria leaves only the pre-existing synonymous elaborations at nine-versus-nine criterion boundaries.
- Re-proved each in-scope criterion rather than inheriting the prior pass: the loader-level conformance suite, computed skip-list, and offline refresh/confinement tooling pass together; the compose coverage gate passes across 362 service paths and 10 top-level paths; the generated CI workflow and fixture manifest regenerate byte-identically; and the env-gated provider suite skips safely with no socket exported while the workflow test still asserts the managed-runtime job exports the socket before invoking it.
- Verification: typecheck and lint passed; conformance, refresh-tooling, CI-workflow, disposition, and provider fixture spot checks passed (101 tests, 8 env-gated skips, 0 failures); generated-path drift clean.
- **Learnings for future iterations:**
  - The index-only adjudication divergence is now a reliable seven-for-seven pattern, not an occasional slip. Diff the narrative PRD against the JSON index as the first action of any post-adjudication Build pass; treating it as a discovery wastes the pass.
  - Normalized pairwise criterion comparison is the cheap way to prove reconciliation: compare criterion counts first, then per-criterion similarity, and only accept residual diffs that are synonymous connectives or strictly-more-explicit narrative phrasing.
  - Regenerating the CI workflow and fixture manifest and asserting an empty diff is stronger evidence than reading them, and it costs one command. Do it whenever a pass touches contract text, because it also proves no earlier pass left uncommitted generator drift behind.
  - When a story is retained rather than reopened, the honest Build output is evidence plus contract reconciliation. Confirm the excised surface is still absent at the production boundary before anything else; if it is, any production edit is an unowned regression risk with no acceptance criterion behind it.
---

## 2026-07-28 09:00 CDT - US-476 Review
- Corrected the durable review record: bounded the source-identity claim to commits before the completion record so later review commits cannot make its numeric count stale, and corrected the completion timestamp so it no longer postdates the commit that contains it.
- Review notes (deferred/rejected): Cursor Bugbot was unavailable because the configured account is out of usage; no Bugbot finding was available to triage. The five review-work areas otherwise reached terminal verdicts after a focused security retry that did not require unavailable team mode.
- Review notes (deferred/rejected): QA's dirty-worktree verdict reflected this review's concurrent correction to the progress ledger, not a failing story proof; all documentary and focused conformance checks completed green before that expected edit.
- Guide-Coverage-Skip: this branch changes internal Compose fixture, schema, planner, and provider conformance; the executable Compose guide and published key matrix are owned by the follow-up documentation story.
- Verification: typecheck and lint passed; 23 documentary contract assertions and nine-criterion correspondence passed; seven focused static test files passed 93 tests, the no-socket provider suite skipped 8 with 0 failures, and Compose coverage passed at 362 service paths and 10 top-level paths; explicit-base guide drift was checked and the substantive skip above records why no guide edit applies.
- **Learnings for future iterations:** Durable progress evidence should describe a bounded commit interval rather than count commits after a source baseline; the review commit itself otherwise invalidates the count it is trying to correct.
---

## [2026-07-28 09:08 CDT] - US-476 Adjudication
- Contradiction: Review retained the US-476 ACs "Fixtures assigned to US-482 and their fixture-specific loader-level expectations remain US-482-owned," "Executing the real production path is proof ownership, not implementation ownership," and "Whole-branch accounting MUST attribute production schema, planner, cache, capability, container-runtime, and provider changes that satisfy the already-passing US-482 acceptance criteria to US-482 even when those changes are physically co-resident on the US-476 branch." Drift Audit nevertheless escalated "29 cycles" and the same shared schema/planner/cache/container-runtime/provider seams without naming a new in-scope failure or an unowned file or behavior. The gate-finality AC already said cycle count and branch co-residency are not reopening evidence and that restating a settled exclusion "MUST terminate as a non-blocking observation with passes unchanged." Normative spec section 7.4 defines `preserved` as "accepted and carried in `ServicePlan.extensions.compose`, capability-checked per section 5.5.1," section 6.2 says the supported native service-field forms "are accepted," and section 5.5.1 requires preservation plus fail-closed capability checks; those sections define the production behavior completed by US-482, not implementation ownership for US-476 merely because its fixtures execute that path.
- Decision: narrowed the US-476 gate-finality acceptance criterion in `spec/compose/prd.json` with a concrete evidentiary threshold. A claimed unowned production change must name the exact file and behavior, quote the completed-story criterion it cannot be traced to, and explain why the recorded US-482 attribution does not cover it; naming only shared seam categories is insufficient. A marker lacking that evidence is contract-invalid, must retain `passes: true`, creates no follow-up story, and is recorded only as non-blocking enforcement.
- Follow-up story (if created): none; no permitted reopening condition or new evidence was identified.
---

## 2026-07-28 09:25 CDT - US-476
- Confirmed the conformance fixture suite complete under the evidentiary-threshold adjudication with no production change. Nothing was implemented to make a fixture pass, no fixture was added, and no live Podman execution was attempted.
- Reconciled the narrative PRD with the machine index for the eighth consecutive adjudication. The 09:08 adjudication changed only the JSON index before this Build pass, so the narrative gate-finality criterion lacked both new sentences: the requirement that an alleged unowned production change name the exact file and behavior, quote the completed-story criterion it cannot be traced to, and explain why the recorded prerequisite attribution does not cover it, with seam-category naming declared insufficient; and the rule that a marker lacking that evidence is contract-invalid, retains `passes: true`, and creates no follow-up story. Both documents now carry that text, at nine-versus-nine criterion boundaries, with residual differences only synonymous connectives or strictly-more-explicit narrative phrasing.
- Recorded per-file production attribution, which is the evidence the new threshold actually demands and which no prior pass had captured at file granularity. Of the nineteen changed package sources, seventeen trace to commits carrying the prerequisite story's exact title, while `core/src/landofile/compose/dispositions.ts` traces to the fixture-assertion isolation commit and `core/src/landofile/compose/rejections.ts` to this story's own titled commit. There are no unowned production changes, so a future audit can answer the ownership question from this record at the file level instead of by seam category.
- Re-proved the in-scope battery at this head rather than inheriting it, naming the files so the evidence is auditable: the loader-level conformance suite (36), fixture outcome assertions (7), fixture enumeration and pin parsing (14), the offline manifest generator (3), the pinned refresh (2), refresh path confinement (8), the compose-coverage CI check (1), and the generated-workflow assertions (23) — 94 tests with no failures and no filter misses. The coverage gate held at 362 service paths and 10 top-level paths, and guide lint passed.
- Re-ran the anti-vacuity proof at this head. Deleting the rejected key from the rejecting corpus fixture turned exactly two guards red — the computed rejection manifest diverging from the committed one, and the fixture dropping out of the rejected set with no authored-value expectation to replace it — while the other thirty-four assertions stayed green, and restoring the file reproduced the identical checksum and returned 36 pass. Stated precisely, that is one targeted mutation activating two complementary guards, which is weaker than the independence claim earlier entries recorded; proving independence would need a separate mutation isolating each guard, and no acceptance criterion asks for it.
- Proved the environment-gated lane safe by observation: with the socket variable unset the live suite reported eight skips and no failures, and the Podman client is constructed inside the fixture runners rather than at module scope, so the skip path makes no runtime contact. Both guarded-writer codegen paths ran byte-identically, writing 17 fixtures and 9 pinned files at exit 0 with the committed corpus unchanged.
- Guide drift is reported honestly rather than as a pass: run locally without pull-request context it fails `guide-not-touched` for the landofile, provider, and SDK surfaces, and the sanctioned remedy is the pull-request `Guide-Coverage-Skip:` line owned by the later publishing step, not a token guide edit. There is no CLI delta to re-verify, because only specification prose changed this pass.
- Guide-Coverage-Skip: this branch changes internal Compose fixture, schema, planner, and provider conformance; the executable Compose guide and published key matrix are owned by the follow-up documentation story.
- Verification: typecheck and lint passed; the eight anchor files passed with 94 tests and no failures; the Compose coverage gate passed at 362 service paths and 10 top-level paths; guide lint passed; both codegen paths were byte-identical; the environment-gated provider suite skipped 8 with 0 failures and no runtime contact.
- **Learnings for future iterations:**
  - A delegated search can return a confidently wrong verbatim claim. The reconciliation check this pass first came back asserting all four new clauses were already present in the narrative; a literal grep showed all four absent. Whenever a subagent reports that recurring work is already done, re-verify with a literal match before accepting it, because that specific answer is the one that silently skips the pass's only real task.
  - The recurring index-versus-narrative divergence is now eight for eight, and the useful framing is that only the index is machine-read, so the narrative can drift indefinitely without any gate noticing. Reconciling it is the standing first action of a post-adjudication pass.
  - Per-file commit attribution is a cheaper and stronger answer to an ownership dispute than restating seam categories, and it is exactly what the amended criterion now requires of any challenger; capturing it once makes the next audit answerable from the record.

## 2026-07-28 09:45 CDT - US-476 Review
- Corrected the confirmation record's impossible future timestamp and clarified that the adjudication's JSON-only amendment describes the state before Build reconciled and committed both PRD representations.
- Review notes (deferred/rejected): Cursor Bugbot was unavailable before and after review because the configured account is out of usage; it produced no findings to triage. Fresh post-fix goal, QA, quality, security, and context lanes all passed.
- Review notes (deferred/rejected): residual concurrent same-user TOCTOU, absent pre-dispatch live Podman evidence, and production changes already attributed to passing US-482 remain conclusively out of scope under the source acceptance criteria; no qualifying new evidence reopened them.
- Guide-Coverage-Skip: this branch changes internal Compose fixture, schema, planner, and provider conformance; the executable Compose guide and published key matrix are owned by the follow-up documentation story.
- Verification: typecheck and lint passed; the focused documentary assertion confirmed valid JSON, `passes: true`, nine synchronized criteria, and the reconciled evidentiary threshold; `git diff --check` passed. Explicit-base guide drift reported only the three covered internal surfaces addressed by the substantive skip above.
---

## 2026-07-28 09:56 CDT - US-476 Adjudication
- Contradiction: Review retained the US-476 ACs "Executing the real production path is proof ownership, not implementation ownership" and "Whole-branch accounting MUST attribute production schema, planner, cache, capability, container-runtime, and provider changes that satisfy the already-passing US-482 acceptance criteria to US-482 even when those changes are physically co-resident on the US-476 branch." The Drift Audit marker instead escalated "32 cycles" and an "unresolved boundary" where fixture proof "continued oscillating into adjacent production compose normalization and planning scope," but named no exact file or behavior, quoted no completed-story criterion the change cannot satisfy, and supplied no new evidence explaining why the recorded US-482 attribution fails. Spec section 7.4 requires `preserved` fields to be accepted and carried in `ServicePlan.extensions.compose`, spec section 6.2 requires the supported native service-field forms to be accepted, and spec section 5.5.1 requires preservation plus fail-closed capability checks; those sections define the production behavior completed by US-482, not US-476 implementation ownership merely because its fixtures execute that path.
- Decision: narrowed the US-476 gate-finality text in `spec/compose/prd.json` to state explicitly that generic claims of an unresolved boundary, continued oscillation, or fixture proof reaching adjacent normalization or planning scope do not satisfy the existing reopening threshold without exact-file attribution facts and new evidence. The current marker is contract-invalid under that criterion: US-476 retains `passes: true`, no follow-up story is created, and the marker is recorded only as non-blocking enforcement.
- Follow-up story (if created): none; no permitted reopening condition or qualifying new evidence was identified.
---

## 2026-07-28 10:05 CDT - US-476 Build Verification
- No production changes: the shared conformance harness, matrix-computed rejection classification, fixture-refresh path confinement, env-gated provider spot checks, and generated-workflow socket wiring were already complete on the branch. This pass re-verified every acceptance criterion against committed artifacts and carried the pending adjudication amendment of the gate-finality criterion into the committed index and log.
- Verification: typecheck and lint passed; conformance, fixture-tooling, generated-workflow, schema-snapshot, planner/rejection/spelling/knob, container-runtime, and app-plan cache spot checks passed. The env-gated provider lane skipped cleanly with no container-runtime contact, and injecting a stale rejection-manifest entry turned the parity guard red before restoring it returned green.
- Out-of-scope observations only, per the gate-finality criterion: no unowned production change or failed in-scope proof was identified, and no failed Push/Babysit managed-runtime result was present, so `passes: true` is retained and no follow-up story was created.
- **Learnings for future iterations:**
  - There is no skip-list file to find in this suite; searching for one produces a false MISSING report. Rejection is computed per fixture at runtime and the committed manifest is only its drift record, so the honest check is to inject a stale entry and watch the parity guard fail.
  - A verification-only Build pass still owes the no-socket integration run: executing the env-gated file with the socket variable unset is the only host-safe proof that the lane skips rather than reaching for a runtime, and it costs nothing.
---

## 2026-07-28 10:15 CDT - US-476 Review
- Reconciled the gate-finality amendment into the narrative source PRD and corrected the Build verification timestamp so the two PRD representations and durable chronology agree; no production behavior or acceptance boundary changed.
- Guide-Coverage-Skip: this branch changes internal Compose fixture, schema, planner, and provider conformance; the executable Compose guide and published key matrix are owned by US-477.
- Review notes (deferred/rejected): Cursor Bugbot was unavailable because the configured account had exhausted its usage limit, so no Bugbot findings were available to triage.
- Review notes (deferred/rejected): Fresh managed-runtime evidence, concurrent same-user checkout mutation, branch size, cycle count, and US-482-owned co-resident production work remain conclusively out of scope under the source acceptance criteria; no reviewer supplied a permitted reopening condition.
- Verification: typecheck and lint passed; source/index parity and diff checks passed; focused loader, fixture-outcome, enumeration, refresh/confinement, generated-workflow, Compose-coverage, and safe no-socket spot checks passed with positive test counts.
---
