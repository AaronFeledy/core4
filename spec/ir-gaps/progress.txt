# IR gaps progress

## Codebase Patterns
- A fail-closed planner default lands its blast radius in FIXTURES, not source. Measure before implementing: `bun --no-orphans test --path-ignore-patterns='**/*.integration.test.ts' <pkg>/test` per package. Raw `bun test <dir>` pulls in `*.integration.test.ts`, which fail with `ProviderUnavailableError` on a host with no runtime and mask the real count.
- Adding one member to a plan-carrying error union touches ~16 unions. Insert it ONCE per union: a second copy is type-identical (`A | A = A`) so nothing fails, and it silently pads line counts and the frozen SDK signature string. Many of those files hold TWO unions (an exported `XError` alias plus an inline channel); both need the member, so a blind dedupe breaks the alias.
- An SDK error-union change is a three-file commit: the union, `sdk/test/fixtures/sdk-mvp-surface.json` (frozen service-tag signature), and `sdk/API_COMPATIBILITY.md` (additive exports). New exported primitives must be listed in the latter or `sdk-backward-compatibility.test.ts` fails.
- Build-step identity is resolved ONCE in `servicePlanExtensionsFromDraft` (`engine/src/planner/extensions.ts`), the only point where the final composed `draft.user` and the complete step list (authored plus feature-contributed) meet. Resolve anything that depends on the final planned user there, never in `authored.ts`.
- Widening a public Landofile union needs a `sdk/compatibility-exceptions.json` row per affected surface (keyed surface + changeKind + path); one union change fans out to every schema and command surface that embeds `ServiceConfigInput`.
- Type planner helpers with the exported schema types, not re-spelled structural shapes: `Schema.optional(X)` produces `k?: T | undefined`, which `exactOptionalPropertyTypes` rejects against a hand-written `k?: T`.
- Tooling metadata normalizes ONCE via `normalizeToolingTask` in `@lando/landofile/tooling-normalize`; every consumer (command cache, CLI help, machine index, MCP, execution) reads that IR instead of re-deriving from the authored `ToolingTaskShape`. Argv validation lives beside it in `@lando/landofile/tooling-input` so CLI and MCP share one validation path and emit identical error tags.
- Adding a publicly registered schema needs THREE edits, not one: the schema itself, `JSON_SCHEMA_REGISTRY` in `sdk/src/schema/json-schema.ts`, and a fixture row in `sdk/test/schema/public-schema-contracts.ts`. Missing the third fails `codegen:schema-snapshot` with "Public schema contract fixture list does not match JSON_SCHEMA_NAMES".
- Widening a Landofile value type ripples into expression resolution: `engine/src/tooling/step-runner.ts` walks the same shape and must handle every new union member, and `Effect.forEach` over a heterogeneous union needs an explicit callback return annotation under `exactOptionalPropertyTypes`.
- `check:guide-coverage` only scans PRDs listed in `docs/guides/prd-sources.json`. A PRD outside that list needs no `docs/guides/INDEX.md` row, so do not add one speculatively.
- Guide fixtures resolve from `docs/guides/<frontmatter-id>/fixtures/<name>/`, never from the MDX's own subdirectory.
- A non-zero exit from a tooling command is a RESULT, not an Effect failure: `runTooling` stops at the first non-zero step and returns the accumulated streams carrying that exit code, and the CLI maps it through `successExitCode`. Reserve tagged failures for compile, input, and disabled errors so CLI, MCP, and library surfaces stay uniform.
- Adding a member to a planner error channel is repo-wide: grep `| CommandAliasConflictError` to find every plan-carrying union across `sdk/src/app`, `sdk/src/services`, `engine/src/operations`, and `core/src/cli/commands`, and patch them in one pass.
- `engine` and `core` typecheck against `sdk/dist`, not `sdk/src`. After an SDK type change run `bun run --filter='@lando/sdk' build`, or TS reports "Two different types with this name exist, but they are unrelated".
- Pattern-validated schemas (`ContainerUser`, `AbsoluteContainerPath`) are NOT branded, so their `.Type` is plain `string` and declaration sites need no `.make()`. Brand only when call sites should be forced through a constructor.
- Removing or widening a public schema property needs an entry in `sdk/compatibility-exceptions.json` keyed by surface + changeKind + path with a justification. `check:schema-compatibility` is its own gate; typecheck, lint, and the test suites all stay green while it fails.
- A new `services.<name>.*` key must clear two gates, not one: the Effect schema, and `SERVICE_CONFIG_KEYS` in `landofile/src/service.ts`, which is derived from `Object.keys(ServiceConfig.fields)` and rejects everything else as an "unsupported MVP key". Decoding an unknown service key silently DROPS it and looks like success, so only a real CLI run proves the key is accepted.
- A scoped provider apply must persist the full app plan with the newly built selected service definitions overlaid. Remembering only the subplan loses unrelated services; remembering the original full plan loses newly stamped artifact references.
- Service features are filesystem-free by type: `apply` is `(ctx) => Effect<void, ServiceFeatureError>` with no requirements channel. Anything needing `stat`/`readdir`/realpath belongs in the planner (see certs), leaving the feature the pure `resolveBindSource` join.
- Export container mount destinations as `PortablePath.make(...)` constants. A bare string constant typechecks at the `addMount` call site but fails every whole-`MountPlan` `toEqual`/`toContainEqual` assertion, because `target` is branded.
- The guide harness fakes `lando start`: `runScenarioLayerCommand` in `core/src/testing/scenario-context.ts` returns a canned `ready` / exit 0 before the real CLI runs, and only one hardcoded landofile substring makes it exit non-zero. A guide scenario that must prove a planner or validation rejection has to run a command that falls through to `invokeRealCli`, such as `lando app:cache:refresh`.
- Service-type config rejections belong in `ServiceType.resolve`, not in the feature. `engine/src/services/feature.ts` decodes `feature.definition.schema` against the feature REF config, never against the service config, so a feature schema cannot see `services.<name>.*` keys. `ServiceType.schema` is catalog metadata that nothing decodes. `assemble.ts` maps a resolve failure through `servicePlanError`, so the plugin's `ServiceTypeError` message becomes the user-visible `LandofileValidationError` body: lead that message with the exact key path (`services.<name>.<key>[<i>]`) because `issues` stays at service granularity.
- One resolution prefix, many surfaces: `resolveKnownEventSet` (`engine/src/planner/event-set.ts`) is the provider-free prefix of `planApp` — provider precedence, plugin manifests, per-service seeds through `ServiceType.resolve`, effective tooling, and the valid event set. Lint, doctor, and start all call it, and `planApp` consumes the seeds it returns instead of re-resolving, so a service still resolves exactly once per plan.
- Internal-task exclusion rides a WeakMap keyed on object identity (`tooling-include-provenance`). Nothing between `compileEffectiveTooling` and `validEventNames` may spread, clone, or re-sort the tooling map; `compileEffectiveTooling` already sorts, so `attachEffectiveTooling` must store the object it was handed.
- A `ServiceType.resolve` that reads `input.capabilities` must treat `undefined` as "not asked", not as "unsupported". Provider-free surfaces pass no capabilities, so a bare `input.capabilities?.x !== true` check fails a service that `lando start` accepts.
- One Compose serializer, many providers: `renderCompose`/`emitCompose` live only in `container-runtime/src/podman/compose.ts`; each provider exports a thin adapter that injects its own `ProviderErrorContext` and delegates. Values always go through `scalar` (JSON.stringify); every dynamic mapping key goes through `mappingKey`, which emits a plain key only when an allowlist accepts it and YAML cannot re-resolve it.
- `check:schema-compatibility` reads the WORKING side from the gitignored derived tree (`core/dist/schemas`), which goes stale the moment you merge main. A schema that main added but your local dist predates reports as `BREAKING surface-removed` for a surface your branch never touched. Run `bun run codegen:schema-snapshot` before believing a breaking row, and cross-check the PR's `schema-snapshot` job.

## 2026-09-11 - US-613 (in progress, not complete)

Landed the normalization spine and most of its consumers.

- Landofile tooling authoring now accepts task `user`/`disabled`, flag `alias`/`choices`/`boolean`/`default`/`required`, arg `choices`/`order`, and `cmds` entries that are either a string or an object step carrying `cmd` plus per-step `service`/`dir`/`user`/`env`. The retired flag `type` spelling was replaced outright rather than kept alongside `boolean`.
- A pure normalizer produces one `NormalizedToolingTask` IR (ordered steps, resolved service references, sorted positional args) and a companion validator parses argv against it. Declaring input is opt-in: a task with no `flags`/`args` keeps raw argv passthrough, which is what preserves every pre-existing tooling behavior.
- Execution resolves the task, rejects disabled tasks, validates argv, and resolves `:<flag>` service references all BEFORE provider selection. A task whose every step is `:host` never initializes a provider at all; mixed tasks select once and run steps in authored order with step-level `user`/`dir` overriding task-level values independently.
- The app command index carries normalized flag/arg metadata (schema version 3; version-2 blobs read as a cache miss, never an exception), CLI help renders FLAGS/ARGUMENTS sections from it, and MCP projects the same metadata into tool input schemas, serializing back to canonical argv so both surfaces run one validator.
- New tagged errors `ToolingInputError` and `ToolingDisabledError` carry source path, key path, and remediation.

Verification: typecheck, lint, and check:boundaries passed; 359 targeted tests passed across sdk/landofile/engine/core tooling suites; codegen:schema-snapshot regenerated cleanly.

**Two defects found in manual CLI QA remain open, so the story is NOT marked passing:**
1. `lando <task> --help` on a task that declares flags is consumed by the input parser and fails with `ToolingInputError` instead of printing help. Help must be intercepted before tooling input parsing.
2. A disabled task on a FRESH cache reports the cache-miss `ToolingCompileError` rather than `ToolingDisabledError`, because disabled tasks are excluded from the command index entirely so the router never matches them. Only the stale-cache path reaches the authoritative check. Keep disabled tasks in the index marked hidden plus disabled, and reject them by tag at the router.

- **Learnings for future iterations:**
  - Manual CLI QA caught both defects; the 359 green tests did not. Always run the real binary from a fixture app directory (`bun core/bin/lando.ts <task>` with isolated `LANDO_USER_*` roots), not just the suites.
  - Excluding a task from the command index and relying on a downstream authoritative check gives the wrong error tag on the fresh-cache path. Model "known but refused" distinctly from "unknown".
  - `bun core/src/cli/index.ts` exits 0 silently; only `core/bin/lando.ts` is a faithful manual-verification entry.
---
## 2026-09-11 23:50 - US-613 Drift Audit
- Trigger: 1 cycle, ~2560 changed lines, 2 commits
- Weakened proofs: the command-index suite blessed omitting disabled tasks, so a fresh cache could never emit `ToolingDisabledError`
- Ceremony: unused plan-normalized IR cache existed only for its own tests
- Contract: `lando <task> --help` with declared flags is still unproven and left to Build; story stays `passes: false`
- Fixed: disabled tasks stay in the index as hidden; unused plan cache deleted; retired flag `type` leftover fixture and exemption removed
- Split/excise decisions: none
- Verification: typecheck pass, lint pass, 87 targeted tests pass
---

## 2026-09-12 00:45 - US-613 (complete)

Closed the two defects that held the story open, plus branch-local breakage that only CI would have caught.

- `lando <task> --help` now renders the declared FLAGS/ARGUMENTS help before the task's argv is parsed. The decision reads the normalized flag/arg metadata the command index already carries, so a task with declared input gets Lando's help while a raw-passthrough task still hands `--help` to the tool it wraps. A hidden or disabled task keeps its own tagged refusal, and a task that declares its own `help` flag (or `h` alias) keeps ownership of both spellings.
- Restored the tooling result contract the step-execution rewrite had changed: a non-zero command exit is the task's result, carrying the verbatim exit code, accumulated stdout/stderr, redaction tokens, and progress events, while a multi-step task still stops at the first failing step. Tagged failures stay reserved for compile, input, and disabled errors, which keeps the CLI, MCP, and library surfaces identical for a failing developer tool. Reviewed against the published `ToolingEngine` contract; lifecycle events are unaffected because they abort through their own event-step failure.
- Recorded the story's public schema movement (retired flag spelling, widened flag/arg/step projections) as compatibility acceptances so the schema gate passes on intent rather than silence.
- Repaired the translated-tooling rejection check, whose fixture had been switched to supported metadata while it still asserted a rejection; it now proves invalid tooling metadata from a translator is refused before anything is written.

Verification: typecheck, lint, check:boundaries, lint:guides, check:guide-coverage, check:public-transcripts, check:schema-compatibility, check:telemetry-inventory, and codegen:check pass; 677 targeted tests pass across 34 tooling, help, alias, MCP, cache, and schema suites; the flags-and-args guide plus three adjacent tooling guides pass. Manual CLI QA on the real binary with isolated user roots: help renders for bare name, canonical id, and custom alias; `-h` behaves the same; passthrough and author-declared help flags are byte-identical to the pre-change baseline; a disabled task still fails `ToolingDisabledError` on a fresh cache; a failing single-step task returns exit 7 with its streams; a failing first step stops the chain.

- **Learnings for future iterations:**
  - Story breakage hides OUTSIDE the story's own suites. Three pre-existing CLI contract tests and the schema-compatibility gate had been red for two iterations because only the new suites were run. Sweep by grepping `*.test.ts` for every symbol whose behavior changed, and run the semantic gates (`check:schema-compatibility` above all) whenever `sdk/src/schema/**` moves.
  - Pass the canonical command id (`app:<task>`) into help dispatch, never the typed token: a task named `app`, `global`, or `recipes` would otherwise be captured by the help-topic branch.
  - Route dispatch decisions off cache metadata instead of argv shape. The command index already knows whether a task declared input, so the help gate needs no Landofile load and no provider.
  - Once the unsupported-tooling allowlists matched the schema exactly, no tooling key is both schema-valid and scan-rejected, so translator output carrying bad tooling metadata now surfaces as `ConfigTranslateError` before any write rather than `NotImplementedError`.
---
## 2026-09-12 - US-613 Review
- Fixed in-scope review blockers: cmd/cmds mutual exclusion after schema decode, source-aware missing service-flag errors, declared flags with `arguments: false`, MCP `--` delimiters for hyphen positionals, tagged compile failures from cache refresh, and the plugin-index schema-version assertion.
- Review notes (deferred/rejected): event-command argv order, mixed-step collapse, and host provider selection stay US-614; CommandRegistry swallow-on-discovery is the existing listing contract; Object.keys cmd/cmds walk was replaced by rejecting both rather than inventing a key-order policy; security lane aborted with no confirmed HIGH/CRITICAL issue.
- Verification: typecheck and lint passed; 149 focused tests passed across normalize/input/tooling/compiler/MCP/command-registry suites.
- **Learnings for future iterations:** Schema.Struct rebuilds decoded objects in field-declaration order, so Object.keys authored-order tests that skip decode are false greens. MCP→argv must insert `--` before positionals or leading-hyphen values become flags in the shared parser.
---
## 2026-09-12 01:50 - US-613 Drift Audit
- Trigger: 3 cycles, ~2952 changed lines, 20 commits
- Contract: disabled tasks were documented as unlisted while the index keeps them hidden
- Scar tissue: argv rejection still accepted authored `arguments` after normalize
- Fixed: listing contract text; argv check reads only normalized `acceptsArguments`
- Split/excise decisions: none; event mixed-step collapse stays US-614
- Verification: typecheck pass, lint pass, 257 targeted tests pass
---
## 2026-09-12 04:40 - Babysit
- PR: #964 https://github.com/AaronFeledy/core4/pull/964
- CI outcome: pass
- Review feedback: fixed (Cursor security finding — flag-derived tooling service targets could resolve to the `:host` sentinel and escape the container; colon-prefixed flag values now rejected as ToolingInputError, with regression coverage)
- Drift check: clean
- Merge: merged (squash)
---

## 2026-09-12 06:00 - US-614 (in progress, not complete)

Landed the name-resolution spine, the recursion bound, and the restart brackets. Tooling brackets and the guide remain, so the story stays `passes: false`.

- Landofile event names are no longer a closed literal set. `AppLifecycleEventName` gains `pre-restart`/`post-restart`, and `LandofileEvents` keeps its twelve documented keys alongside a plain-string index signature so `pre-<task>`/`post-<task>` survive decode. The index key is deliberately unconstrained: a malformed name must reach the semantic validator that reports the complete valid set, not die structurally with a useless message.
- Semantic validation moved to the one place that can see the whole namespace. It ran per raw file before layer merge and before tooling includes resolved, so it could never know the task set; it now runs in the planner right after effective tooling is compiled, on both the cache-hit and fresh-plan paths, and before any provider action. The valid set is the twelve lifecycle names plus brackets for every invocable task, including service-type contributions and namespaced include ids, excluding include-only internal tasks. `app:config:lint` and `doctor --app` share the same builder and comparator against the fully resolved Landofile, so there is one definition of validity rather than two.
- Nested event recursion is bounded. The active-frame stack carries app, event, source file, and the invoking command with its authored step index, never argv or expression values. Cycle identity is (app, event), so the same command under two different events stays legal; depth is checked before the cycle, capped at sixteen simultaneously active event frames. Both rejections carry the alternating event/command chain, and neither can be swallowed by a step's `ignoreError`.
- Restart now brackets the pair: `pre-restart`, the untouched stop and start event trees, then `post-restart`, which sits outside route-removal compensation and never runs after a failed pair.
- Post-event failures are fatal. The warn downgrade is deleted outright, so a failing `post-*` step fails its operation with the redacted output tail instead of a message nobody reads, and a fatal post hook never undoes a body that already succeeded.

Verification: typecheck, lint, check:boundaries, and check:schema-compatibility pass; 60 targeted tests pass across event runtime, planner event-name, landofile validation, and restart scenario suites.

Remaining for the next pass: `pre-<task>`/`post-<task>` brackets around top-level tooling runs, and the `docs/guides/events/task-events.mdx` guide.

- **Learnings for future iterations:**
  - The tooling-bracket seam cannot live inside `runTooling` alone. `engine/src/operations/event-tooling-command.ts` `runEventToolingCommand` is a second top-level entry that calls the engine directly, so an event `command: app:<task>` step would bypass brackets added only to `runTooling`. Extract one shared resolved-task executor and route both through it.
  - An event `task:` step is an inline graph invocation and must NOT re-fire brackets; only `command:` re-enters. Blur that line and `pre-foo` calling `task: foo` recurses immediately.
  - A non-zero tooling exit is a result, not a failure, so "a failed body prevents success post-steps" is an exit-code check, not an error branch.
  - Tagged errors leak into unions far from where they are raised. Moving `LandofileUnknownEventError` from discovery to planning touched the SDK service signatures, the frozen mvp-surface fixture, six global CLI commands, and doctor. Follow the compiler outward rather than guessing the blast radius.
  - Pass-through `instanceof` lists are a trap: a new boundary error added to only one of them gets silently rewrapped as a generic step failure. They are now one shared predicate.
---

## 2026-09-12 06:55 - US-614 (complete)

Closed the story by giving top-level tooling runs their event brackets and shipping the guide.

- A tooling task now runs between `pre-<task>` and `post-<task>`. The seam is a shared pair of operations rather than a hook bolted onto one caller: one compiler turns an authored task into one invocation per normalized step, and one executor runs those invocations in authored order. Both top-level entries (the CLI/library `runTooling` path and the event `command: app:<task>` path) compile, then bracket, then execute. An event `task:` step is an inline invocation and calls the executor directly, so it never re-fires the brackets it is already inside.
- Order follows command semantics. A failing pre bracket prevents the body. A body that exits non-zero keeps its exit code as the task result and skips the post bracket, because a non-zero tool exit is a result rather than a Lando failure. A failing post bracket fails the whole run with the tagged step error and its redacted output tail.
- Bracket failures reach callers intact. `ToolingError` now carries the three event runtime errors instead of remapping them, so a bracket failure keeps its event name, step index, exit code, and redacted tail all the way out to the CLI, MCP, and library surfaces. No service requirement widened: the event runtime is read through optional services, and an event with no steps returns before it is consulted, so an unbracketed task needs no event runtime at all. A bracket that IS configured while that runtime is missing fails loudly rather than being silently skipped.
- Retired the invocation builder that collapsed every step of a task into a single invocation. It was reachable from event `task:` steps with arbitrary multi-step tasks, which erased per-step service, dir, and user targeting and defeated stop-at-first-failure. Two event tests that had pinned the collapsed shape now assert one invocation per step, each carrying its own resolved target.
- Provider selection stayed lazy through the move. A task whose steps all target `:host`, brackets included, still initializes no provider.

Verification: typecheck, lint, check:boundaries, check:guide-coverage, check:schema-compatibility, and lint:guides pass; 89 targeted tests pass across tooling, compile, event runtime, init/restart event, progress, planner event-name, and SDK error suites; 150 core CLI tooling/router/event/restart/start scenario tests pass; the new `task-events` guide runs 4/4 scenarios. Manual CLI QA on the real binary with isolated user roots confirmed `before build` and `after build` bracketing a two-step host task at exit 0.

- **Learnings for future iterations:**
  - `events.ts` imports the event step runtime, so anything the step runtime imports cannot import `events.ts` back. Putting the compiler and executor in one module and the bracket wrapper in a second keeps the cycle from forming; the inline event path takes the first module only.
  - The renderer does not echo tooling body stdout in the live progress tree, for bracketed and unbracketed tasks alike. A guide that wants to prove step ordering must write ordered markers to a file and verify the file, not scrape stdout.
  - `ToolingDisabledError` requires `source`, so the shared compiler takes a required source and the event command path synthesizes one from the plan root. Do not make it optional to accommodate a caller that simply has not built one.
  - Deferred and still open, carried from the prior story's review: `toolingArgv` in `core/src/cli/event-command-target.ts` still orders positionals by authored object key instead of the normalized argument `order`. Any test for it must decode the task through the schema first, because `Schema.Struct` rebuilds objects in field-declaration order and a decode-free fixture is a false green.
---

## 2026-09-12 - US-614 Review
- Kept fatal `post-start` outside host-proxy acquisition, and skipped restart/rebuild route-removal compensation for that same post-start failure, so a failing post hook cannot undo a start body that already succeeded.
- Review notes (deferred/rejected): lint/doctor event names stay provider-free layered/included tooling (service-type contributions remain planner-only, as the lint comment documents); QA FAIL was a detached worktree missing generated bundled plugins, not a product bug; `ignoreError` on command steps still ignores target failures by contract while cycle/depth stay unignorable; `toolingArgv` positional order stays deferred from US-613.
- Verification: typecheck and lint passed; restart-events 5/5 including start and restart post-start no-rollback cases.
- **Learnings for future iterations:** Making a previously warn-only post hook fatal changes `acquireUseRelease` keep/drop semantics and any outer `compensateFailure` wrapping `startApp`. Run the post hook after the body has already marked resources retained, and skip compensation for that retained post-start failure.
---
## 2026-09-12 07:00 - US-614 Drift Audit
- Trigger: 3 cycles, ~2363 changed lines, 9 commits
- Ceremony: unused argument-validator and depth-constant re-exports; stacked compile-then-bracket facade
- Weakened proofs: post-task failure claimed a redacted tail but only asserted the field existed
- Deferred work re-examined: toolingArgv positional order stays a US-613 leftover in an untouched file; lint/doctor valid-set stays provider-free layered/included tooling by review contract
- Fixed: deleted the unused re-exports; collapsed the extra bracket compile wrapper; restored secret redaction on post-task failure
- Split/excise decisions: none
- Verification: typecheck pass, lint pass, 65 then 8 focused tests pass
---

## 2026-09-12 07:10 - US-614 Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev, started a lamp app, and proved task/restart event brackets against the binary: pre/body/post order, pre-fail skips body, body-fail keeps exit 3 and skips post, post-fail is `LandofileEventStepFailedError` with `secret=[redacted]` tail, unknown events list the twelve lifecycle names plus `pre-build`/`post-build`, and `command: app:build` from `pre-build` is `LandofileEventLifecycleReentryError`. Adjacent `php` tooling, lint, stop, and destroy stayed green.
- **Out-of-scope finding:** `curl http://us614-verify.lndo.site:8000/` returned Traefik `404 page not found` after setup warned ports 80/443 were in use and fell back to 8000/4443; `lando exec appserver -- php /app/index.php` printed `us614-ok`. Suspected host/WSL mirrored bind on 80/443, not US-614.
- **Out-of-scope finding:** `lando apps:list` still shows ghost `qa950-mysqlnservices-n-database-n-type-mysqln-creds-n-use` (empty services, missing `/tmp/qa950/mysql`) after destroy; leftover managed-runtime network JSON from prior QA, no running containers.
---

## 2026-09-12 08:15 - Babysit
- PR: #965 https://github.com/AaronFeledy/core4/pull/965
- CI outcome: pass (after fixing unit-tests-linux-x64-shard 3)
- Review feedback: fixed (isolated restart event fixtures from the shared topology plan so start-progress-topology tests stop inheriting lifecycle hooks; realigned schema contract fixture order with the public registry). Bugbot could not run — Cursor usage limit.
- Drift check: clean
- Merge: merged (squash)
---

## 2026-09-12 08:35 - US-615 (complete)

Concise routes now lower to explicit, provider-neutral middleware.

- A route may be authored as a shorthand string (`hostname[:port][/path]`, wildcards anywhere in the host, and every combination of the three) or as the object form. One pure normalizer turns both into a single canonical IR before backend resolution, and every consumer reads that IR: the planner, TLS SAN generation, and the router. Shorthand is accepted structurally by the schema rather than rewritten at decode, so an authored Landofile still round-trips its own spelling through `app:config` and friends.
- Five provider-neutral filters ship: strip prefix, add prefix, request header, response header, and redirect. They are a discriminated union on `type`, so an unsupported filter is a decode failure rather than something a proxy discovers at runtime. A filter's optional `name` is its layer-merge identity and never an HTTP header name; header filters carry `header` plus `value`. Across Landofile layers, filters merge by `name`, fall back to type identity when unnamed, and a named filter never collapses into an unnamed one. Merged lists keep first-appearance order, which is the order the router renders them in.
- Layer identity is path-scoped rather than global. Adding `type` to the shared array-identity key list would have collapsed two mounts of the same kind into one, so the filter rule applies only to `filters` arrays and every other array merges exactly as before.
- Nothing strips a path implicitly. A `pathPrefix` route matches on the prefix and forwards the full path; only an explicit strip filter removes it. The Traefik renderer emits a middleware block plus a per-router middleware list in authored order, and a route with no filters renders byte-identically to before.
- A wildcard hostname compiles to an anchored `HostRegexp` with the wildcard expanded and every other metacharacter escaped. A hostname without a wildcard keeps emitting `Host()` verbatim.
- Invalid input fails closed with a new source-aware tagged error naming the authored key (`services.<svc>.routes[2]`, `proxy.<svc>[0]`) and a remediation. It is raised before any provider action and surfaces identically from the planner and from config lint.
- The published route-filter contract suite now runs against the shipped filter implementations instead of hand-written reference transforms, and two dead proxy stubs that duplicated the route and filter schemas were deleted.

Verification: typecheck, lint, boundaries, schema snapshot codegen, schema compatibility, guide lint, and guide coverage pass; focused suites pass across sdk schema, landofile normalize/merge/filters/lint, engine planner/services/lifecycle/proxy contract, proxy-traefik rendering, and core CLI config scenarios; the new `route-shorthand` guide runs 3/3 scenarios. A socket-gated live integration test pins the routing behavior end to end: a filtered route strips its prefix and gains its response header, while the sibling route without a strip filter forwards the prefix and 404s.

- **Learnings for future iterations:**
  - `yield*` on an `Either` inside `Effect.gen` does not unwrap it, so raw authored values sail past the normalizer and blow up downstream. Convert at the boundary with an explicit `Either.isLeft` branch, which is also the house style here.
  - Widening a publicly registered schema is a four-file move, not one: the schema, the JSON-schema registry, the ordered fixture list, and the compatibility exceptions. A union widening produces one exception entry per embedding surface, so expect dozens rather than one.
  - The live provider socket resolver finds a managed socket left on the host from earlier runs, so a `*.integration.test.ts` gated on it will actually try to start containers locally instead of skipping. Those files are excluded from the unit shards; run them only in the sandbox.
  - Guide fixtures key off the frontmatter `id`, not the MDX directory, so a guide at `proxy/route-shorthand.mdx` reads fixtures from `docs/guides/route-shorthand/fixtures/`.
---

## 2026-09-12 - US-615 Review
- Fixed wildcard HostRegexp authorities so proxy status and TLS doctor still see them; named overlays that change filter type now replace instead of keeping stale fields; planner RouteInputError no longer fabricates `.lando.yml`.
- Review notes (deferred/rejected): same-host pathPrefix collapse in `pushRoute` is pre-existing; Traefik HostRegexp vs Host() priority is out of AC; full layered-file provenance is not required because the AC names the authored key path; lint keeps `ConfigLintViolation` rather than returning `RouteInputError`; shorthand route arrays replacing on merge is the existing scalar-array law (converting at merge would break round-trip); extra §10.2 filters stay out of this story; header CRLF and Traefik backtick injection were security PASS / defense-in-depth only.
- Verification: typecheck, lint passed; focused merge/normalize/routing/planner tests passed; `GUIDE_DRIFT_BASE_REF=origin/main bun run check:guide-drift` passed.
- **Learnings for future iterations:** persisted YAML parsers must track renderer grammar changes (`Host` vs `HostRegexp`); discriminated-union merge cannot deep-merge when `type` changes.
---
## 2026-09-12 09:56 - US-615 Drift Audit
- Trigger: 2 cycles, ~2074 net lines, 8 commits
- Ceremony: unused planner route-wrapper param; rewritePath kept as a frozen adjacent filter in the contract suite
- Weakened proofs: live routing test hits renderer+Traefik, not Landofile→planner→ProxyService; HostRegexp recovery is proven only on the pure parser — left in place rather than growing a second live path
- Contract: discover compatibility note implied it raises RouteInputError
- Deferred work re-examined: rewritePath was owned by this branch's contract suite so it was excised; remaining review deferrals still hold
- Fixed: dropped unused planner param; removed rewritePath contract stub; corrected discover error-channel note
- Split/excise decisions: none; extra §10.2 rewritePath harness excised without a PRD split or passes flip
- Verification: typecheck pass, lint pass, 127 targeted tests pass
---
## 2026-09-12 10:07 - US-615 Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev, and proved route shorthand/filters against the binary: lint/config round-trip, invalid shorthand `RouteInputError` on start, layered named-filter merge, then a php:8.3 app whose HTTPS routes stripped `/api`, set request/response headers, forwarded an unfiltered prefix to Apache 404, matched `*.wild.us615.lndo.site`, and 302-redirected. Adjacent lint/exec/destroy stayed green.
- **Out-of-scope finding:** leftover Traefik from 04:35 UTC had `file.Provider` fail with `error adding file watcher: no space left on device`; first curls 404ed until a later `lando start` recreated Traefik. Not caused by US-615. `lando global:restart` then failed `ProviderUnavailableError` because `lando-global` still had v951-* containers.
- **Out-of-scope finding:** host 80/443 in use so proxy published on 8000/4443; left concurrent `/tmp/v951/{lamp,lemp,static}` apps in place (sbx951).
---
## 2026-09-12 11:55 - US-616

Build steps now name the identity they run as, and planning resolves that identity once.

- `build.artifact:` and `build.app:` accept `{ run, user? }` beside the existing string and string-array forms. A new `ContainerUser` primitive (`<name|uid>[:<group|gid>]`, allowlisted so the value is safe to interpolate into a `USER` instruction) validates authored users at the schema boundary, and the hand-written `build` JSON Schema twin mirrors the widened union.
- `servicePlanExtensionsFromDraft` is the single resolution point: every step, authored or feature-contributed, gets `step.user ?? draft.user`, and the key stays absent when neither exists. That funnel is the only place the final composed planned user and the complete step list meet, so resolving in `authored.ts` would have used a pre-composition user.
- The artifact realizer replaced its per-step privilege wrap with active-user tracking: it emits `USER` only when a step's resolved identity differs from the currently active one and restores the service's final identity after the last step. Inherited image identities are now preserved exactly rather than collapsed to nothing when they are root-ish, so a `root:wheel` base is restored as `root:wheel` instead of bare `root`. Inspection and referenced-parent materialization now trigger on any resolved user, not just privilege.
- The `privileged` build-step flag is gone. It only ever meant "run as root", which the resolved identity states directly, and keeping both would have needed a precedence rule the contract does not define. The six bundled system-mutating steps (boot scaffold, CA trust store, PHP prerequisites, Composer, Xdebug, database clients) now declare `user: "root"` so a service authored with a non-root `user:` cannot starve them of privilege.
- Resolved users reach execution and caching: app steps carry their identity on the `ExecTarget`, and both the Docker and Podman exec-create bodies now send `User`. That seam was declared in the provider contract but silently discarded, so Landofile tooling's `user:` had never actually applied either. Per-step identity also enters ordered artifact and app build-key hashing.

Verification: typecheck, lint, check:boundaries, check:schema-compatibility, check:guide-coverage, check:public-transcripts, check:telemetry-inventory, and lint:guides pass; schema snapshot regenerated (383 schemas). 468 targeted tests pass across 32 sdk/engine/container-runtime/plugin suites, plus the new build-steps guide (2 scenarios). Manual CLI QA on the real binary with isolated user roots: a mixed string/object build block lints valid, `app:config --format=json` round-trips object steps verbatim in authored order, and `user: "root wheel"` fails lint with `services.app.build.artifact`.

- **Learnings for future iterations:**
  - Widening a Landofile union costs 17 separate `unknown` findings in `check:schema-compatibility` (ServiceConfig, ServiceConfigInput, LandofileShape, both authoring projections, TemplateRenderContext, and ten `app:config`/`meta:global:config` command surfaces). `breaking` stays 0, but each needs its own `sdk/compatibility-exceptions.json` row keyed by surface + changeKind + path. Budget for it.
  - Deleting a field means grepping `docs/guides/**` for its prose, not just `src`. `lando-boot-scaffold.mdx` asserted that a service `user:` override "is never baked into the derived artifact", which this story inverted; no scanner or gate would have caught that sentence.
  - A test can encode the very gap a later story closes. `image-build-privilege.test.ts` asserted `not.toContain("runtime-only")` to prove the artifact ignored the service user. Treat a suspiciously specific negative assertion as a contract marker and rewrite it deliberately rather than deleting it.
  - `Schema.optional(X)` yields `user?: string | undefined`, which does not satisfy a hand-spelled `user?: string` parameter under `exactOptionalPropertyTypes`. Type planner helpers with the exported schema type (`BuildScript`, `Exclude<BuildScriptStep, string>`) instead of re-spelling the shape.
  - `Array.isArray` does not narrow a `readonly` array out of a union of object-or-array; discriminate with `"run" in value` and let the array fall through.
  - `provider: test` cannot prove planner-resolved users, because no command prints the resolved plan's build steps. The guide proves schema round-trip, lint rejection, and a successful plan refresh, and leaves USER emission to the container-runtime suites.
---

## 2026-09-12 - US-616 Review
- Kept generated log-redirect mkdir/ln steps as root so a non-root service user cannot starve system log-path mutations; formatted the IR-gap PRD index so lint is green.
- Review notes (deferred/rejected): omitting `user` when neither the step nor the service names one is the intended image-default path, not a planning hole; public BuildPlan materialization is a pre-existing orchestrator shape, not this story's surface; feature-intent ContainerUser decoding is defense-in-depth after Landofile schema validation (security PASS); full-suite uninstall failures are unrelated and out of host-safe scope.
- Verification: typecheck, lint passed; 192 focused tests passed across redirect, planner, build-key, image-user, boot/security, and the build-steps guide (2/2).
- **Learnings for future iterations:** System-mutating build steps include generated log redirects, not only bundled feature producers. After replacing `privileged` with inherited planned users, grep every `addBuildStep` / build-step factory for a missing `user: "root"`.
---
## 2026-09-12 12:20 - US-616 Drift Audit
- Trigger: 2 cycles, 1509/499 changed lines, 5 commits
- Scar tissue: intent comment claimed consumers never re-resolve omitted users; artifact realization still maps omitted to the final/inherited identity (two-layer semantics, comment aligned)
- Ceremony: leftover privilege-key names in new build-user tests renamed
- Weakened proofs: none; privilege→user assertions were inverted/expanded; guide stays CLI/schema-only as already documented
- Collateral: none; only this story's `passes` flip
- Deferred work re-examined: image-default omit, feature-intent string typing, and artifact users on extensions rather than public BuildPlan rows still hold
- Fixed: contract comment; privilege leftover test names
- Split/excise decisions: none
- Verification: typecheck pass, lint pass, 326 then 51 focused tests pass
---

## 2026-09-12 13:25 - Babysit
- PR: #968 https://github.com/AaronFeledy/core4/pull/968
- CI outcome: pass (after triage; initially failed library-api-tests-runner and unit-tests-linux-x64-shard 3)
- Review feedback: fixed (documented additive build-user SDK exports; expect `user: "root"` on planned trust-store step)
- Drift check: clean
- Merge: merged (squash)
---

## 2026-09-12 14:05 - US-617A (in progress, not complete)

Landed the mechanism for planned-user home persistence and host reachability. The story is NOT marked passing: the fail-closed default is live and the fixtures it now refuses have not been updated yet.

- A service accepts `home: false | { path? }`, enabled when omitted. A new `AbsoluteContainerPath` primitive validates the authored path the same pattern-only way `ContainerUser` does, so it stays a plain string at every call site.
- A service type now declares the identities inside the image it ships: `ServiceType.identity` is `{ defaultUser, homes }`, where `homes` maps a user principal (the part before any `:group`) to that user's home directory. Every bundled catalog type declares one; `compose` and `lando` deliberately do not, because their image comes from the Landofile.
- Planning resolves the home once, in `finalizeServices`, which is the first point where the composed planned user, the final artifact, the complete storage list, and the provider capabilities all exist. `home.path` wins outright; otherwise the planned user (or the identity's default user) selects the path. An authored storage entry already covering that destination keeps its own store name, scope, and ownership, and nothing is generated on top of it; a trailing slash does not make two destinations different.
- A service whose image is supplied by the Landofile has no trustworthy identity, so it refuses with the new `HomePathCapabilityError` before any provider action, naming the service and both ways out. That error is threaded through every plan-carrying error union up to the public `AppPlanner.plan` signature.
- Host reachability is realized only from declared capability: when a provider reports `hostReachability` other than `none`, each service gets the `host.lando.internal` alias pointing at the runtime-resolved `host-gateway` sentinel plus `LANDO_HOST_IP` naming that alias. When the provider cannot reach the host, neither exists and planning does not fail for it. `ServicePlan.hostAliases` was a dead field; the shared container host-config fragment now projects it into `ExtraHosts`, so both the Podman bring-up path and the Docker provider realize it.
- Build keys already excluded storage; that is now pinned by regression coverage rather than left implicit.

Verification: typecheck and lint pass. 78 focused tests pass across home resolution, host reachability, extends identity merge, catalog identity coverage, container host config, compose-knob ExtraHosts merge, and build-key storage.

**Remaining before this story can pass:**
1. Landofile fixtures that author `image:` on a catalog type, or use `type: compose`, now refuse by design. `engine/test/services/app-planner.test.ts` has 97 such failures, all `HomePathCapabilityError`. They need `home: false`, and the same sweep covers `core/test`, `plugins/*/test`, and `docs/guides/**/fixtures`.
2. `plugins/proxy-traefik/src/global-services/traefik.ts` and `plugins/service-lando/src/global-services/mailpit.ts` are production global services declared as `type: compose`; both need `home: false` or the router itself will not plan.
3. The `docs/guides/services/home-and-host.mdx` executable guide, its fixtures under `docs/guides/home-and-host/fixtures/`, and the `docs/guides/INDEX.md` row.
4. `check:schema-compatibility` and `codegen:schema-snapshot` for the added `home` property, plus `check:boundaries` and real-runtime evidence.

- **Learnings for future iterations:**
  - Adding one member to a planner error channel is a repo-wide edit, not a local one. `HomePathCapabilityError` had to join sixteen separate unions across `sdk/src/app`, `sdk/src/services`, `engine/src/operations`, and seven `core/src/cli/commands` files before the tree typechecked. Grep `| CommandAliasConflictError` to find every plan-carrying union at once.
  - `engine` typechecks against `sdk/dist`, not `sdk/src`. After changing an SDK type, run `bun run --filter='@lando/sdk' build` or the engine reports "Two different types with this name exist, but they are unrelated" and sends you hunting for a phantom mismatch.
  - Pattern-validated schemas like `ContainerUser` are NOT branded, so their `.Type` is plain `string`. Following that precedent for `AbsoluteContainerPath` kept roughly thirty catalog declaration sites free of `.make()` noise.
  - A fail-closed default lands its blast radius in fixtures, not in source. Measure it before implementing: `type: compose` alone appears in 40 files here, including two production global services, so budget the sweep as part of the story rather than discovering it at the end.

## 2026-09-12 15:45 - US-617A (in progress, not complete)

Closed the fail-closed blast radius everywhere except `core/test`, and shipped the guide. The story stays `passes: false`: `core/test` is still red and one production path is genuinely broken.

- The two shipped global services declared `type: compose` now opt out. Traefik and Mailpit keep no per-user state, so `home: false` states that directly rather than inventing a path. Without this the router itself could not plan.
- Swept every fixture that the default-on refusal correctly rejects: engine planner, certs, network-inject, tooling, service-type, and traefik-routing suites; the service-lando compose and catalog scenario suites; and ten executable-guide Landofiles across compose-passthrough, config-lint, external-access, compose-service-block, lamp-stack-variants, and drupal-stack-overrides. The only edit used was adding `home: false`; no assertion was loosened. Two guide fixtures were deliberately left alone because their rejection happens before home planning, and both still reject for their original reason.
- New executable guide `docs/guides/services/home-and-host.mdx` with three scenarios: the default-on catalog path, the capability refusal on a Landofile-supplied image, and the explicit `home.path` escape hatch. The refusal scenario asserts the diagnostic text and both remediations, so the error's usefulness is now a test, not a hope.
- Fixed a defect the mechanism shipped with: `HomePathCapabilityError` had been inserted **twice** into most plan-carrying unions. Harmless to the type system, but it pushed `assemble.ts` to 601 lines and broke the planner bounded-concern ceiling, and it leaked a doubled member into the frozen SDK signature. Deduplicated where a union genuinely had two copies.
- Recorded the story's public surface: the frozen `AppPlanner.plan` signature now carries the new error, and `AbsoluteContainerPath` plus its pattern and predicate are documented as additive exports.

Verification: typecheck, lint, check:boundaries (all 13 rules), check:schema-compatibility (compatible 16, breaking 0, unknown 0, no exception rows needed), codegen:schema-snapshot, lint:guides, check:guide-coverage, and check:public-transcripts all pass. 5699 tests pass with 0 failures across sdk, engine, service-lando, proxy-traefik, container-runtime, and landofile; the new guide runs 3/3. Manual CLI QA on the compiled binary inside the sandbox with isolated user roots: a catalog service lints valid and refreshes its plan, `home: false` and `home.path` both round-trip through `app:config`, and a bare compose service exits 1 with `HomePathCapabilityError` printing both ways out.

**Remaining before this story can pass:**
1. `core/test` is 139 failures red. Most are mechanical: compose planner-cache and capability fixtures, `conformance/compose-fixtures`, and `cli/global-commands` need the same `home: false` opt-out.
2. A real product defect blocks the rest. The bundled `lamp` recipe cannot plan: its `database` service is refused with "its image is supplied by the Landofile", which means `hasCustomImage` sees an `image` on a service the author never gave one to. Recipe-rendered and scratch-forked apps are affected (`services/scratch-*`, `recipes/canonical-recipes.matrix`, `app/resolve`). Find where the recipe path puts `image` on the authored config before adding a single `home: false`; opting recipes out would paper over it.
3. Real-runtime evidence that a home store survives a rebuild and that `host.lando.internal` resolves from inside a container. The Verify step owns this.

**Two contract calls that need adjudication in Review:**
- Three tests asserted that a compose passthrough service receives no `LANDO_*` environment at all. Host reachability now adds `LANDO_HOST_IP` to every service. Those assertions were narrowed to exclude that one key, because their sibling assertions (`LANDO_APP_KIND`, `LANDO_MAIL_HOST`) show they were written to police the app env layer, not capability realization. If the passthrough-fidelity contract is meant literally, the fix belongs in host reachability instead.
- `type: apache` with `user: www-data` is refused, because apache declares a home only for `root`. That is a plausible thing to author. Its fixture got `home: false` rather than a guessed `/var/www`, since inventing a home path for a shipped image is exactly what this story refuses to do. The catalog metadata may still be incomplete.

- **Learnings for future iterations:**
  - Measure per package with the integration tests excluded. `bun test <dir>` runs `*.integration.test.ts`, which fail with `ProviderUnavailableError` on a host with no container runtime and inflate the count with noise you cannot fix.
  - A blind regex dedupe of a repeated union member breaks the files that hold two unions. Twelve files here export an `XError` alias next to an inline channel; removing the member from the alias typechecks nowhere and shows up as an `exactOptionalPropertyTypes` mismatch far from the edit.
  - When a delegated fixture sweep hits a fixture with no authored service config to edit, that is the signal a production path is broken, not a stubborn test. The core sweep stopped on exactly that and found the lamp recipe defect.
  - `provider: test` proves the refusal end to end. `lando app:cache:refresh` resolves the plan, so the capability error and its remediation are assertable in a guide without booting a container.
---

## 2026-09-13 01:45 - US-617A (complete)

Closed the last two blockers on home persistence and host reachability, so the story now passes end to end.

- Fixed the product defect that blocked every recipe-rendered and versioned-catalog app. A service written as `type: mariadb:10.6` has the service type's *own* published artifact tag written onto its config as `image` during assembly, long before home planning runs. The custom-image test read that as "the author supplied an image" and refused the service with `HomePathCapabilityError`, which is why the bundled `lamp` recipe could not plan. The resolved tag now travels to `serviceHomeIntent` as `pinnedArtifactTag` and is excluded from the check: an `image` equal to Lando's own pinned tag still leaves the type's identity describing the container. Any other `image`, and any Compose `build.context`, is still a custom image.
- Three Lando-supplied images now state their home intent instead of leaning on that fix. The `toolbox` and `node-ts` bundled recipes author their own `image:` on a `lando`/`node` service, and the ssh-agent sidecar runs a bare Alpine; none keeps per-user state, so each declares `home: false`. Both recipe snapshots and the canonical `recipes/toolbox/recipe.yml` were regenerated with their new content digests.
- Finished the fail-closed fixture sweep in `core/test`: compose planner-cache and capability fixtures, the compose conformance fixture tables, global-commands, scratch fork/finalizer/run, the library runtime app, and the recipe decomposer suites. Only `home: false` was added; no assertion was weakened.

Verification: typecheck, lint, check:boundaries (all 13 rules), check:schema-compatibility (compatible 16, breaking 0, unknown 0), codegen:check (catalog clean), check:guide-coverage, check:public-transcripts, check:telemetry-inventory, and lint:guides pass. The home-and-host guide runs 3/3. Test spot checks: 1197 core blast-radius tests pass with 0 failures; the wider engine/sdk/container-runtime/plugins/landofile sweep is 6811 pass; the whole `core/test` tree is 5431 pass with 13 failures that reproduce identically on `origin/main` in a clean worktree (renderer-lando transcript-tail-reader, runtime-service uninstall, and the slow scratch tests in `app/resolve` that exceed the 5s default when the machine is loaded).

Manual CLI QA against `core/bin/lando.ts` with isolated user roots and `provider: test`: a `mariadb:10.6` service refreshes its plan (and, with the pinned-tag exclusion temporarily reverted, refuses with `HomePathCapabilityError` — red/green on the real command); a `lando` service on `alpine:3.20` refuses with both remediations printed; adding `home.path: /opt/appuser` makes it plan and round-trip through `app:config --format=json`; `home: false` plans; and `lando init --recipe lamp --full` produces an app that refreshes three commands, which is the defect above proven fixed at the CLI.

- **Learnings for future iterations:**
  - `ServiceConfig.image` is not authored intent once you are inside the planner. `assemble.ts` overwrites it with `resolvePinnedArtifactTag(...)` for any versioned type, so anything downstream that asks "did the author give this an image?" must compare against the resolved tag. Expect the same trap in future capability checks.
  - When a fail-closed default breaks a *recipe*, do not opt the recipe out. The rendered Landofile has no authored `image:`, so an opt-out there hides a planner bug. Recipes that genuinely author their own image (toolbox, node-ts) are a different case and should say `home: false` explicitly.
  - Establish a failure baseline in a detached `git worktree` of `origin/main` before triaging. It needs its own `bun install` and `bun run codegen` — symlinking the branch's `node_modules` silently resolves `@lando/*` back to branch source and produces a useless comparison.
  - `core/test/app/resolve.test.ts` is genuinely slow (several tests exceed the 5s default) and cascades: when one times out, its temp dir is removed and every later test in the file dies on `chdir`. Failure counts from that file swing between 0 and 9 run to run on the same tree. Re-run it alone before blaming a change.
---

## 2026-09-13 - US-617A Review
- Reviewed home persistence and host reachability against the source ACs; no in-scope blockers. Bugbot found none. review-work: goal/QA/code-quality/security/context all PASS.
- Review notes (deferred/rejected): LANDO_HOST_IP on compose passthrough stays — AC requires alias/IP from declared capability, not a passthrough-fidelity hole.
- Review notes (deferred/rejected): apache `user: www-data` fail-closed is correct; identity documents defaultUser `root` only, and guessing `/var/www` would invent a home for a shipped image.
- Review notes (deferred/rejected): extra_hosts vs planned `host.lando.internal` duplicate is defense-in-depth (Traefik already maps the same host-gateway); not an AC miss. AbsoluteContainerPath `..`/colon tightening is out of AC.
- Review notes (deferred/rejected): real-runtime home-survives-rebuild and in-container alias resolution stay Verify-owned.
- Verification: typecheck, lint, and `GUIDE_DRIFT_BASE_REF=origin/main bun run check:guide-drift` passed; focused home/host/identity/plan/build-key/schema tests plus the home-and-host guide (3/3) passed.
---

## 2026-09-13 - US-617A Drift Audit
- Trigger: 4 cycles, 1545 insertions, 9 commits
- Weakened proofs: default-on catalog home, pinned-tag identity, explicit path, apache www-data refusal, and hostReachability none were isolated-unit or opted-out; restored through AppPlanner.plan. Compose LANDO_* filters now require LANDO_HOST_IP instead of only carving it out.
- Dual path: Traefik extra_hosts duplicated planner hostAliases; extra_hosts removed so the alias comes only from declared capability.
- Contract drift: HomePathCapabilityError, ServiceConfig.home, and ServiceType.identity were missing from API_COMPATIBILITY; documented. Real-runtime rebuild/DNS remains Verify-owned.
- Fixed: restored production-path proofs; deleted Traefik extra_hosts dual path; documented the public error/channel.
- Split/excise decisions: none
- Verification: typecheck, lint, and focused AppPlanner/home/host/passthrough/traefik/compat spot checks pass
---

## 2026-09-13 02:40 - US-617A Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev, and proved home persistence plus host reachability against the binary: `node:22` home marker survived `lando rebuild`; `LANDO_HOST_IP=host.lando.internal` resolved to `169.254.1.2` and served a host HTTP response; compose without `home` failed `HomePathCapabilityError` with both remediations; `home.path` planned; lamp `init --full` then `app:cache:refresh` compiled 3 commands. Adjacent redis `PONG`, `node -e`, info/logs/destroy stayed green.
- **Out-of-scope finding:** `lando destroy` on a never-started compose Landofile that refuses `HomePathCapabilityError` also fails with that tag (destroy plans first). No containers existed; dir delete cleaned it. Not an AC miss.
---

## 2026-09-13 04:05 - Babysit
- PR: #970 https://github.com/AaronFeledy/core4/pull/970
- CI outcome: pass (after fixes; initially provider-integration-linux-x64 and drupal-journey-linux-x64 failed)
- Review feedback: fixed (resolved the `sdk/API_COMPATIBILITY.md` conflict by merging main, and declared `home.path: /root` for the live Compose fixture harness's substituted `node:22-alpine` image so the US-617A home refusal no longer pre-empts the unsupported-feature CapabilityError; drupal-journey was a Composer HTTP 504 flake that passed on rerun)
- Merge: deferred (all merge conditions met and branch is MERGEABLE with every check green, but GitHub's merge API returned repeated 502/500 errors across five spaced attempts)
---

## 2026-09-13 04:30 - US-617A (verified complete, no new code)

Build found home persistence and host reachability already finished and published on its branch, with PR #970 open and awaiting a merge that GitHub's API refused with repeated 5xx. Re-verified the story against its acceptance criteria rather than inventing work; no source, test, or fixture changed and no commit was made.

- Re-ran the story's gates on the branch as it stands: typecheck, lint, and all thirteen boundary rules pass; `check:schema-compatibility` reports compatible 16, breaking 0; the home-and-host executable guide runs 3/3.
- Spot checks across the story's blast radius are green: 178 engine tests (planner home, AppPlanner home, host reachability, service-type input, build-key user), 114 core tests (compose planner-cache and capability fixtures, compose conformance, global-commands, both recipe decomposers), and 54 plugin/container-runtime tests (compose passthrough scenario and providers, service identity, Traefik global service, Compose plan and knobs bring-up).
- CLI verification re-run on the real command with isolated user roots and `provider: test`: a `mariadb:10.6` catalog service refreshes its plan, proving the pinned-artifact-tag identity fix still holds; a `type: lando` service on `alpine:3.20` exits 1 with `HomePathCapabilityError` printing both remediations; `home.path: /opt/appuser` plans and round-trips through `app:config --format=json`; `home: false` plans.
- Left the story phase at `published`. The only work left on it is the deferred merge, which Babysit owns; signalling an earlier phase would send an already-reviewed, already-pushed story back through Review and Push for nothing.

Verification: typecheck, lint, boundaries, and schema-compatibility passed; the guide and 346 blast-radius tests passed; four CLI scenarios verified on `core/bin/lando.ts`.

- **Learnings for future iterations:**
  - A `BREAKING ... surface-removed` row from `check:schema-compatibility` is more often a stale local derived tree than a real regression. `core/dist/schemas` is gitignored, so merging main leaves it missing whatever schemas main added since — here `HostTerminal`, which the branch's own registry still exports. `bun run codegen:schema-snapshot` restored it (384 -> 385 artifacts) and the row disappeared. Regenerate before triaging, and check whether the PR's `schema-snapshot` job is green.
  - When Build reaches a story that is already `published`, the completion path is verification, not a phase signal. Phases move forward (`implemented` -> `reviewed` -> `published` -> `merged`); emitting an earlier one rewinds the pipeline over work that already passed it.
---

## 2026-09-13 04:25 - US-617A Drift Audit
- Trigger: 6 cycles, 1761 insertions, 18 commits
- Findings: none; second pass after the prior audit found no compounding scope, dual path, ceremony, or gamed proofs
- Fixed: none
- Split/excise decisions: none
- Verification: typecheck pass, lint pass, 95 focused tests pass
---

## 2026-09-13 07:15 - US-617B (complete)

Router disablement and post-start URL scanning now change real behavior instead of only resolving into config. The branch arrived with the mechanism written but uncommitted and unverified; this pass closed the consumer gaps that made disablement cosmetic, then verified the whole story.

- `router.enabled` resolves through compiled default, global config, then Landofile, and the planner records the answer on the plan as `AppPlan.router`. Every consumer reads that one decision through a single `routerEnabled` predicate rather than re-deriving precedence. The field is optional purely so plans persisted before it existed still decode, and those default to enabled.
- Four consumers were still ignoring the decision, so a disabled router only looked disabled. The app-plan cache re-derived `requires.globalServices: ["traefik"]` from `routes.length > 0` alone, which meant the shared router auto-started on every run after the first; scratch apps published routes straight through their own setup/applyRoutes path; `open` handed back `*.lndo.site` hostnames nobody serves; and `share` defaulted its tunnel to the first route hostname. All four now honor the plan, and `share` falls back to the same app-scoped target it already used for a route-less app.
- `lando open` used to blame the user for the refusal it now produces, printing "the app declares no matching route" for an app that declares one. A disabled router with declared routes gets its own message and remediation naming `router:` `enabled: true` or publishing an http host port.
- A service accepts `scanner: false | {path?, okCodes?, retries?, timeout?}`, overlaid over an identical global key, and the planner resolves it per service onto `ServicePlan.scanner`. Start hands the plan to `UrlScanner`, so each service probes its own path, status set, retry budget, and deadline; a disabled service is skipped without a request. The probe now carries an overall deadline through `runProbe`, and the per-attempt timeout is capped by whatever is left of it, so a slow URL can no longer outlive its budget.
- The scan runs after the app is up and never decides whether start succeeded. Every non-pass verdict, and a scan that fails outright, becomes one warning; bodies are redacted again on the way to the event bus, and a dead event bus cannot fail a start that already worked.

Verification: typecheck, lint, all thirteen boundary rules, `check:schema-compatibility` (compatible 17, breaking 0, unknown 0), `codegen:check`, `check:guide-coverage`, `check:public-transcripts`, `check:telemetry-inventory`, `lint:guides`, and guide-drift against origin/main all pass. The router-and-scanner guide runs 3/3 and the adjacent share-target guide stays 2/2. Spot checks are green across the blast radius: 224 engine tests (cache round-trip, routes lifecycle, router precedence, scan plan, post-start scan, share target, scanner live and probe behavior, AppPlanner), 418 sdk tests, and 206 core tests (open resolve and open app, scratch fork/app/recipe/finalizer, share skeleton, info/start/destroy/restart/rebuild scenarios, bootstrap layers, doctor subsystems).

Manual CLI QA on the real command with isolated user roots and `provider: test`: with the router enabled, `lando open --print` resolves `https://web.router-qa.lndo.site`; a global `router.enabled: false` makes the same app refuse with the new diagnostic and both remediations, and a Landofile `router.enabled: true` opts back in and resolves the hostname again, proving precedence end to end on the binary. `app:config --format=json` round-trips a service `scanner` object and `scanner: false` verbatim, and `retries: -1` fails `app:config:lint` naming `services.app.scanner.retries`.

Real-socket scan evidence is now a permanent test rather than a one-off artifact: `UrlScannerLive` runs against a live `Bun.serve` listener through the real `HttpClientLive`, proving a plan-resolved path and status set score green, an unaccepted real status scores yellow carrying `HTTP 500`, a dead port scores red inside its resolved deadline, and a disabled service is never requested.

**Deferred to Verify (owns real runtime):** a container-backed start proving a published URL is probed and a failing probe warns without failing start, and that a disabled router leaves `lando info` printing host ports only. One host observation worth carrying: the Lando-managed Podman socket left running by the previous story's Verify step un-skips `core/test/cli/start.scenario.test.ts`'s live-provider test on the host, where it times out at 60s. That test is container-runtime scope, not host-safe scope.

- **Learnings for future iterations:**
  - A planner decision is not honored until every consumer asks for it. `AppPlan.router.enabled` was written correctly and still did nothing, because the cache re-derived the requirement it was supposed to suppress. When a story adds a plan field that suppresses behavior, grep for code that *derives* the same behavior from a sibling field (here `routes.length > 0`) and not just for readers of the new field.
  - `engine/src/planner/assemble.ts` sits at exactly 600 lines and `engine/test/planner-module-boundaries.test.ts` fails it at 601. Blank-line trims in that file are load-bearing, not churn; budget a line for a line when adding to the planner, or extract.
  - Put a plan predicate in the module that owns the rule, not the module that first needs it. `routerEnabled` started in `lifecycle/routes.ts`, which is not an exported engine subpath, so core could not reach it. `config/router-config.ts` is exported and already owned precedence, so it houses both.
  - Hand-spelling a structural parameter type defeats `exactOptionalPropertyTypes`: `{ readonly router?: { readonly enabled: boolean } }` rejects the `AppPlan` whose field is `... | undefined`. Type these predicates `Pick<AppPlan, "router">`.
  - Adding one optional property to `ServicePlan` costs twelve `check:schema-compatibility` exception rows, because every plan-carrying surface reports `$.plan.services.additionalProperties` separately. Append them; do not re-serialize `sdk/compatibility-exceptions.json` with a sort, which rewrites 826 unrelated lines.
  - `check:public-transcripts` fails after running a single guide, because transcripts are gitignored and regenerated per run. Run the guides whose surfaces you touched, then the gate, rather than treating the first failure as a missing artifact.
---

## 2026-09-13 - US-617B Review
- Fixed production post-start scanning so start probes the published and routed URLs it already computed instead of listing through the bootstrap stub provider; unpublished `--route` now names router disablement even when a host port exists; disabled plans no longer require shared cross-app networking and withdraw previously published routes.
- Review notes (deferred/rejected): scanner HTTP proxy bypass and unbounded response buffering are pre-existing HttpClient behavior, not this story; timeout warning text still reports per-attempt seconds rather than the capped deadline; scan interruption remains swallowed so a cancelled scan cannot fail start; container-backed start/info proofs stay Verify-owned.
- Verification: typecheck, lint passed; focused open/scanner/routes/planner/post-start tests passed; `GUIDE_DRIFT_BASE_REF=origin/main bun run check:guide-drift` passed.
- **Learnings for future iterations:** `UrlScannerLive` captures `RuntimeProvider` at layer construction, and bootstrap provides the stub whose `list()` is empty. A start-path that calls `scan()` without passing the URLs it already has will probe nothing in production.
---

## 2026-09-13 07:39 - US-617B Drift Audit
- Trigger: 2 cycles, 1961 changed lines, 10 commits
- Weakened proofs: infoForPlan never asserted published-only endpoints; start never asserted UrlScanner.scan(plan, urls); catchAllCause swallowed scan interruption so a cancelled start could continue
- Ceremony: appendScanPath was a test-only export
- Deferred re-examined: interruption swallow was rejected as out of scope but is owned by this branch's new scan wrapper; container-backed start/info remain Verify-owned; guide stays lint/config
- Fixed: unexported appendScanPath; scan interruption now propagates; restored infoForPlan and start→UrlScanner production-path proofs
- Split/excise decisions: none; SDK scan options and open/share consumers stay in-story
- Verification: typecheck, lint, 40 focused tests — pass
---

## 2026-09-13 09:25 - US-618A (complete)

Catalog services can now be configured from a file in the project instead of only through Landofile keys.

- A new `services.<name>.config` block accepts `server` (a single file) and `dir` (a directory), both app-relative. Solr mounts `config.dir` read-only at `/etc/lando/solr/conf` and its startup script copies that directory into `/var/solr/data/<core>/conf` for every declared core, so a config dir and three cores yield three identically configured cores without an init container. PostgreSQL mounts `config.server` at `/etc/lando/postgresql.conf` and starts with `-c config_file=...`; MongoDB mounts at `/etc/lando/mongod.conf` and starts with `--config`; MySQL and MariaDB mount at `/etc/mysql/conf.d/99-lando.cnf`, which the server reads as a drop-in with no command change. An authored `command:`/`entrypoint:` still wins everywhere; the mount is added either way.
- Sources are validated in the planner, before any provider action, by one new module that every service type shares. Absolute paths and `~` paths are refused outright, containment runs through the same realpath guard that governs includes and certs, the declared kind is enforced (a directory handed to `server` or a file handed to `dir` is refused), and a directory walk refuses entries that symlink out of the app root, dangling symlinks, and symlink cycles. Every refusal is a tagged `LandofileValidationError` naming `services.<name>.config.<key>` with remediation.
- Source identity is content, not path. A file hashes its bytes; a directory hashes its sorted relative-path-plus-content list, so adding, renaming, or editing a file inside the config directory changes the identity. That identity is folded into the app-plan cache key and into the per-service build key. The build-key wiring matters because the plan's mount input deliberately drops the host source, so without an explicit digest a changed config file would have been invisible to the up-to-date check even though it changes what the container runs.
- The five service guides document the option, its container destination, the read-only mount, and the pre-start refusal. No recipe README changed: no bundled recipe ships Solr, and recipe READMEs document init answers rather than per-type service options.

Verification: typecheck, lint, all boundary rules, `codegen:schema-snapshot` (387 SDK schemas), `check:schema-compatibility` (compatible 2, breaking 0, unknown 0 — `ServiceFileConfig` reported as `surface-added`), `lint:guides`, and `check:guide-coverage` pass. Spot checks are green across the blast radius: 92 story tests plus 199 service-lando catalog/contract/scenario tests and an 830-test planner/services sweep, with 0 failures. Manual CLI QA on the real binary with isolated user roots and `provider: test`: all four services round-trip their `config` block through `app:config --format=json`; `app:cache:refresh` resolves the plan and the persisted plan carries the mysql bind source and `/etc/mysql/conf.d/99-lando.cnf` target, `config_file=/etc/lando/postgresql.conf`, `--config /etc/lando/mongod.conf`, the Solr per-core copy script, and the `configSources` identities; `../outside.cnf`, `/etc/passwd`, and a directory handed to `server` each fail with the tagged error naming `services.db.config.server` before any provider action.

- **Learnings for future iterations:**
  - A new service key is rejected twice before it works. Beyond the Effect schema, `landofile/src/service.ts` builds `SERVICE_CONFIG_KEYS` from `Object.keys(ServiceConfig.fields)` and refuses anything else as an "unsupported MVP key" — it is derived, so adding the schema field clears it, but a CLI smoke test is the only thing that proves it. Decode alone silently drops an unknown key and looks like success.
  - Export container destinations as `PortablePath.make(...)` constants, not bare strings. A plain string constant typechecks at the `addMount` call but breaks every test that asserts a whole `MountPlan`, because `target` is branded and `toEqual`/`toContainEqual` compare against the branded type.
  - Service features cannot touch the filesystem: `apply` is `(ctx) => Effect<void, ServiceFeatureError>` with no requirements channel. Anything needing `stat`, `readdir`, or realpath belongs in the planner, with the feature left to the pure `resolveBindSource` join — the same split certs already uses.
  - The SDK `FileSystem` service has no directory listing, so a directory-identity walk has to use `node:fs/promises` directly. That is also the only way to see symlinks, since the service's `stat` follows them.
  - `deriveAppPlanCacheKey` runs at the end of assemble and the cached-plan lookup happens after it, so planner-side validation is always reached and a content digest folded into the key busts the cache correctly. A validation placed after the cache read would be skipped on every warm run.
---

## 2026-09-13 - US-618A Review
- Fixed empty `config.server`/`config.dir` at schema and planner, hashed only `{key,digest}` into the service build key, and made Solr's per-core copy fail closed instead of starting after a failed `cp`.
- Review notes (deferred/rejected): post-plan symlink TOCTOU snapshotting matches the existing certs bind-mount contract, not this AC; live daemon config loading stays Verify-owned; recipe READMEs stay init-answer docs (service guides cover the options); ignored extra `config.*` keys on other types are the shared schema, not a silent per-type contract; fabricated `.lando.yml` on `LandofileValidationError` is the planner house style; overlay-replace, empty-dir hashing, core `..`, and app-step keys are out of scope.
- Verification: typecheck, lint, lint:guides, and `GUIDE_DRIFT_BASE_REF=origin/main bun run check:guide-drift` passed; 97 focused tests passed.
- **Learnings for future iterations:** Bind-mount config identity belongs in the build key as `{key,digest}` only. Including the canonical host `source` reintroduces path sensitivity the mount projection already stripped.
---

## 2026-09-13 10:55 - US-618B (complete)

Catalog services can now bake global packages into their image from the Landofile.

- PHP `composer:` additively accepts `{ version, packages }` beside the string and `false` forms it already took. `{ version: "2" }` plans byte-identically to `composer: "2"`, and `packages: {}` is a no-op. Packages install with `composer global require` into `COMPOSER_HOME=/usr/local/composer` with `COMPOSER_BIN_DIR=/usr/local/bin`, so the binary proxies land on the default PATH with no PATH editing and no symlink loop, and the Composer home is left world readable for a non-root service user.
- Node services accept `globals:`, a package-name-to-version-specifier map installed with `npm install -g`. Unlike the PHP prerequisite stack, globals are emitted even when the service brings its own `image:`: dropping authored intent silently is worse than a loud "npm not found" at build time. The PHP package step follows the Composer binary step instead, because it cannot run without one; with a custom image it is emitted without the `dependsOn` edge.
- Both maps normalize through one shared module before anything is generated. Names are validated (npm names including `@scope/`, Composer's own lowercase `vendor/name` grammar) and so are versions, against charsets that exclude quoting, substitution, and expression characters. That is what makes the "secrets redacted" requirement structural rather than a post-hoc pass: a `${secret:...}` constraint is refused at plan time, so no secret can reach a build command or a build-key input. Each entry is single-quoted in the generated command as a second guard.
- Packages sort by name before they reach the build step, so reordering the YAML map cannot invalidate a built image, while adding, removing, or re-pinning one does. The normalized list rides on `buildKeyInputs`, which the artifact key already folds, so the engine needed no package-specific hashing.
- One latent bug fell out: `assertPhpComposerCompatible` compared the raw authored value against `"2.7.7"`, so the object form would have slipped a Composer release past the PHP 8.5 gate. Both it and `resolvePhpComposer` now read the requested version through one accessor.
- The Landofile YAML subset rejected the authored shape outright: mapping keys were `[A-Za-z0-9_.-]+`, so neither `drush/drush:` nor `@angular/cli:` could be written at all. The key charset now also accepts `/` and `@`. Sequence scalars are unaffected because list-item map detection still requires whitespace or end-of-line after the colon, which is pinned by a new test asserting `- ./src:/app` and `- 8080:80` stay strings.

Verification: typecheck, lint, check:boundaries, check:schema-compatibility (compatible 2, breaking 0, unknown 0; one appended exception row for the widened `$.composer.anyOf`), codegen:check, lint:guides, check:guide-coverage, check:guide-drift, and check:public-transcripts pass. 653 focused tests pass across the blast radius (schema, package normalization, PHP, node, engine build key, and the whole landofile and sdk landofile trees). Both executable guides run green.

Real-surface evidence: on the real binary with isolated user roots, `app:config --format=json` round-trips the composer object form and the globals map, and the persisted plan carries `npm install -g --no-fund --no-audit 'gulp-cli@latest' 'yarn@1.22.4'` and `composer global require --no-progress 'phpstan/phpstan:^1.11' 'squizlabs/php_codesniffer:^3.10'` in sorted order. An uppercase npm package name fails `app:cache:refresh` with a tagged `LandofileValidationError` and exit 1 before any provider action. A live Podman socket was present on this host, so the new `package-installs.integration.test.ts` actually built both stock images and ran the installed tools: `cowsay@1.6.0` appears in `npm ls -g` and `cowsay moo` runs, and `phpcs --version` reports PHP_CodeSniffer with `composer global show` listing it.

- **Learnings for future iterations:**
  - Check the Landofile YAML subset before designing a map-keyed option. `sdk/src/landofile/parser.ts` restricts mapping keys to `[A-Za-z0-9_.@/-]+` and supports no quoted keys, so a key with `/`, `@`, `:`, or a space simply cannot be authored. Schema decode never sees it; the file fails with `LandofileParseError` naming the line.
  - Naming a new union member with an `identifier` changes the shape of the schema-compatibility bill. The widened `composer` cost exactly one new exception row (`schema:ServiceConfig` / `unsupported-construct` / `$.composer.anyOf`) because every embedding surface reported `ref-graph-changed` at `$.$defs`, which prior stories had already accepted. An anonymous inline struct would have reported per-path instead.
  - Guide fixtures are parsed by the same YAML subset, so a fixture is the cheapest place to discover an authoring limit. The invalid-input fixture has to fail for the reason under test: `"../escape"` failed as malformed YAML rather than as a rejected package name, which proved nothing.
  - `resolveLiveProviderSocket()` can find a Lando-managed Podman socket left running by an earlier step, so an `*.integration.test.ts` named by path will really build images on the host instead of skipping. Budget ~90s and expect real artifacts, cleaned up by the test's `removeArtifact`.
---

## 2026-09-13 - US-618B Review
- Fixed custom-image Composer package steps so an unused `composer.version` no longer invalidates the artifact key; the managed Composer install step still hashes the pin when Lando actually installs it.
- Review notes (deferred/rejected): charset-only npm/Composer version allowlists stay as designed for secret/injection refusal, not a full specifier grammar; recipe and package READMEs stay init-answer/framework docs (service guides cover the options, matching US-618A); code-quality MINOR on no-op test wording left as-is.
- Verification: typecheck, lint passed; `GUIDE_DRIFT_BASE_REF=origin/main bun run check:guide-drift` passed; 26 focused package-spec/composer-package tests passed.
- **Learnings for future iterations:** Hash only inputs the step actually consumes. A skipped dependency's identity belongs on the step that installs it, not on a later command that merely invokes the binary.
---

## 2026-09-13 12:05 - US-618B Drift Audit
- Trigger: 2 cycles, 1476 changed lines, 8 commits
- Scar tissue: packages step still hashed the Composer pin the install step already owns
- Ceremony: command and version helpers exported with no external importers
- Weakened proofs: secrets stay refuse-at-plan; hasher suite still uses synthetic steps beside production apply
- Fixed: dropped redundant composer identity from the packages step; unexported unused helpers
- Split/excise decisions: none
- Verification: typecheck pass, lint pass, 92 focused tests pass
---

## 2026-09-13 12:20 - US-618B Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev, and proved Node globals plus PHP composer packages against the binary: `yarn --version` is 1.22.4, `gulp --version` is CLI 3.1.0, `phpcs --version` and `phpstan --version` run, Composer 2.10.2 stays on PATH, HTTPS returned `us618b-ok`, and a second start rebuilt both services in ~400ms. Invalid `Gulp-CLI` / `drush` fail plan-time `LandofileValidationError`; `composer: "2"` and `composer: false` still plan.
- Fixed stock `node:22` Corepack `/usr/local/bin/yarn` EEXIST by adding `--force` to `npm install -g`.
- **Out-of-scope finding:** host 80/443 in use so proxy published on 8000/4443; `curl http://appserver.us618b-verify.lndo.site:8000/` returned Traefik `404 page not found` while HTTPS `:4443` served `us618b-ok`. Same bind fallback as prior verifies, not US-618B.
- **Learnings for future iterations:** Stock Node images ship Corepack shims at `/usr/local/bin/yarn`. Live package-install tests that use `cowsay` miss the documented `yarn` example. Verify the guide's packages, not a unique dummy.
---

## 2026-09-13 - US-618C (complete)

Redis authentication and persistence now follow the Landofile, and app-local Mailpit captures mail from exactly the selected PHP services.

- Redis passwords reach startup through an environment-backed hashed ACL, not command arguments. The generated client and healthcheck inherit authentication, resolved credentials expose the same password to expression consumers, and redaction collects both authored passwords before resolution and REDISCLI_AUTH values afterward.
- Omitted persistence retains the durable default. Disabling it removes generated data and home stores and disables AOF plus RDB snapshots. The planner now honors a service type's normalized home default while preserving explicitly authored home intent.
- Mailpit omission selects every resolved PHP service, including a valid no-op when none exist. False and empty lists select none. Lists deduplicate by first occurrence without sorting; unknown and non-PHP names fail planning. Only selected PHP services receive the msmtp build step, sendmail_path, app-local mail environment, and dependency. Mailpit's service-name schema rejects unsafe SMTP host syntax at resolution without changing the general SDK name contract or adding shell escaping.
- Executable Redis and Mailpit guides cover ephemeral configuration, selected senders, and invalid targets. Relevant plugin and Rails README updates are included; generated scaffolding and the plugin-manifest fixture are committed with their sources.

Verification: 254 focused tests pass with 983 assertions. Typecheck, lint, boundaries, codegen:check, schema compatibility (49 compatible, zero breaking or unknown), guide lint, coverage, drift, and public transcripts pass. Sandbox executable guides pass with Redis 2, Mailpit 4, and Rails 1 scenarios. Returned TypeScript LSP diagnostics are clean; the Redis behavior test's diagnostics request repeatedly timed out, while root typecheck and its direct tests pass.

Real-surface evidence: the current checkout's source CLI ran through the working Lando self-Bun launcher inside lando4-dev. Version and config-lint smoke passed. An unauthenticated Redis request failed with NOAUTH; generated authenticated tooling returned PONG. After restart the durable key still returned durable, while the ephemeral key count was zero, appendonly was no, and save was empty. Selected PHP mail returned MAIL_SENT and appeared in the Mailpit API under its expected subject. The unselected service and a separate mailFrom:false app both retained /usr/sbin/sendmail -t -i without injected wiring. Unknown and non-PHP targets produced LandofileValidationError before build/apply phases. The password canary was absent from info, logs, process argv, and tooling output.

Teardown: both source-QA apps and the earlier proof app were destroyed with purge, with all provider, route, host-proxy, and snapshot cleanup steps successful and volumes removed. Task-owned temporary binaries, scripts, fixtures, and logs were removed; the local branch marker remains. No push or PR was performed.

- **Learnings for future iterations:**
  - A service-composition test alone does not cover generated home storage. Assert the complete planner's stores when an option promises ephemeral behavior.
  - Use the supported self-Bun launcher to exercise current source against the sandbox provider when a newly compiled binary fails before application startup; do not substitute an older implementation for runtime proof.
  - The native exec command takes its service positionally. Run the documentation's actual invocation rather than assuming a --service flag selects it.
---

## 2026-09-13 - US-618C Build acceptance follow-up
- Closed an acceptance-check finding: an authored Redis command or entrypoint could bypass managed authentication and persistence flags. Explicit password or persist options now reject that combination during planning with a tagged error and remediation; startup overrides without those options remain supported.
- Verification: typecheck and lint passed; 36 focused tests passed. Test-first rejection cases went from unexpected successful plans to tagged failures, and sandbox CLI refresh exited 1 before app creation. The scoped acceptance recheck approved the fix. LSP requests for the changed Redis files timed out; repository typecheck passed.
- **Learnings for future iterations:** Generated startup defaults are not sufficient proof for managed options: test their interaction with authored command and entrypoint overrides so explicit intent cannot be silently discarded.
---

## 2026-09-13 - US-618C Review
- Fixed the Redis password example so it loads as a literal Landofile value, and collected authored Redis passwords separately from environment secrets so neither token is dropped.
- Review notes (deferred/rejected): skipping custom-image PHP Mailpit wiring would violate omitted-mailFrom=all PHP; AppFeature contract suite is already invoked so the synthetic mailpit fixture is not a gate miss; hashed ACL startup is the shipped auth path, not requirepass; short-password redaction policy is pre-existing; unpinned msmtp and RedisServiceConfig are out of scope.
- Verification: typecheck, lint, lint:guides, and GUIDE_DRIFT_BASE_REF=origin/main check:guide-drift passed; 37 focused Redis/Mailpit/redaction tests passed.
- **Learnings for future iterations:** Landofile load rejects unescaped `${secret:...}` on this path. Guide password examples must use literals, matching mysql/mariadb, not secret-store syntax.
---

## 2026-09-13 14:45 - US-618C Drift Audit
- Trigger: 3 cycles, 721 changed lines, 17 commits
- Scar tissue: mailFrom was ordered-deduped twice; validated PHP targets could be skipped silently
- Weakened proofs: command+password refusal used an empty password as the managed-option stand-in
- Fixed: one owner for mailFrom lists; missing select fails closed; conflict fixture uses a real password
- Split/excise decisions: none; Mailpit name guard and hashed ACL stay in-story; RedisServiceConfig and msmtp pin stay deferred
- Verification: typecheck pass, lint pass, 26 then 37 focused tests pass
---

## 2026-09-13 16:48 - US-618D1 (complete)

- Rebuild and info accept repeatable service selections through CLI, library, and MCP. Names deduplicate and unknown names fail before provider actions. Info returns only selected services in plan order, without dependencies; omitted or empty selections retain app-wide behavior.
- Scoped rebuild stops the selected services and their transitive prerequisites in reverse dependency order, builds and applies that closure in topological order, and forces its app-build steps. Running prerequisites restart, while unrelated services and downstream dependents stay untouched. Providers retain the complete app plan, including updated built artifacts for the selected services, rather than replacing persisted state with a partial or stale plan.
- The executable guide documents both selection semantics. Public SDK compatibility notes cover the plural service options and full-plan recording contract. The acceptance check caught stale artifact references in recorded plans; a failing regression test reproduced it before the fix, and the scoped recheck approved the corrected behavior.
- Verification: typecheck and lint passed; 65 focused CLI/library/MCP tests and 3 SDK compatibility tests passed, followed by 7 rebuild tests for the recorded-artifact fix. The executable guide passed. Boundaries, codegen drift, schema compatibility, guide lint, coverage, public transcripts, and guide drift passed. Changed TypeScript LSP diagnostics are clean. No full suite was run.
- Real-surface verification in lando4-dev: selected info omitted prerequisites and duplicates; scoped rebuild restarted api and database while dependent and unrelated process identities stayed unchanged; unknown selections failed without mutation; app-wide info/rebuild retained all services. The QA app was destroyed and its fixture removed. A separate parent CLI check confirmed selected plan ordering, no dependency expansion for info, help flags, and tagged unknown-service refusal. The parent terminal detached without stopping the persistent sandbox.
- **Learnings for future iterations:**
  - Native string flags carry type `option`; MCP projection must translate that metadata to JSON Schema string items before repeatable selections validate.
  - Partial rebuild must not close the app handle's whole managed scope: that would tear down unrelated file-sync and host-proxy resources.
  - Assert artifact definitions as well as service names when testing persisted scoped plans; a names-only assertion misses a stale post-build artifact.
---

## 2026-09-13 - US-618D1 Review
- Scoped rebuild now recreates selected containers: apply honors `reconcile` in Docker and shared Podman/Lando bring-up, missing containers are skipped on stop, and prototype-chain names such as `constructor` fail closed before provider action.
- Review notes (deferred/rejected): MCP integer `option` projection vs `app:logs` tail is pre-existing and out of story scope; Kahn cycle incomplete-plan is planner-rejected already; filtering top-level stores on a subplan is not required by the ACs.
- Verification: typecheck, lint passed; 86 focused tests passed across rebuild/info scenarios, Podman recreate, Docker bring-up order, argv parser, library handle, and MCP catalog.
- **Learnings for future iterations:** Unscoped rebuild recreates because stopApp destroy-removes containers; scoped stop+apply only restarts existing ones unless `reconcile` removes and recreates. Provider fakes that record apply calls cannot prove runtime recreation.
---

## 2026-09-13 17:30 - US-618D1 Drift Audit
- Trigger: 2 cycles, 721 insertions, 13 commits
- Deferred work re-examined: MCP integer `option` projection, Kahn incomplete-plan, and unfiltered top-level stores still hold; none are regressions this branch owns
- Contract: public `services`/`recordedPlan` surfaces and the scoped vs unscoped rebuild split all trace to this story's ACs; only this story's `passes` flip
- Fixed: none
- Split/excise decisions: none
- Verification: typecheck pass, lint pass, 80 focused tests pass
---

## 2026-09-13 18:54 - Babysit
- PR: #976 https://github.com/AaronFeledy/core4/pull/976
- CI outcome: pass (after triage; initial fails were unit-tests-linux-x64-shard 3, plus one drupal-journey-linux-arm64 infra 504 flake re-run green)
- Review feedback: fixed (decoupled rebuild target assertion from source indentation; synchronized host-proxy startup-interruption test instead of a 25ms sleep)
- Drift check: clean
- Merge: merged (squash)
---

## 2026-09-13 - US-618D3 (complete)
- Global app environment and label defaults now apply below service-authored values to user apps only. Environment overrides replace whole maps; global and scratch apps remain excluded. Bounded schemas enforce entry and byte limits and reserve an exact generated core environment catalog plus the core label namespace.
- Effective environment and secret-bearing label values enter canonical plan redaction. The executable guide covers setting defaults and rejecting reserved keys. Acceptance review caught missing bare-property database keys and label-token collection; both received regression tests and the scoped recheck passed.
- Verification: typecheck, lint, build, boundaries, schema compatibility, guide lint/coverage/public transcripts and focused guide passed; 194 focused tests and 399 public-contract tests passed, then 9 affected regressions passed after the acceptance fixes. Changed-file LSP checks passed; committed codegen catalog drift is clean. The implementation agent incorrectly attempted the prohibited full suite, which reported runtime cleanup and live-provider failures; it was not repeated.
- Sandbox CLI verification confirmed user-service defaults, runtime environment propagation, reserved-key refusal, and global/scratch exclusion. QA apps were destroyed and isolated config roots removed; the persistent sandbox was preserved.
- **Learnings for future iterations:**
  - Generated environment reservations must recognize bare object-property keys as well as quoted setter arguments; database credentials use the former.
  - Effective container labels live under Compose plan extensions and need explicit inclusion in secret-token collection.
---

## 2026-09-13 - US-618D3 Review
- No in-scope code fixes. Goal, QA, quality, and context lanes passed; Bugbot found no bugs; guide-drift passed against origin/main.
- Review notes (deferred/rejected): compose `writeScalarMap` YAML key quoting is pre-existing export-only emission with no apply callers, not this story's AC, and authored service labels already share that renderer; dotted-label secret heuristics stay the existing `collectSecretEnvValues` splitter, which already covers the story's hyphenated label tokens.
- Verification: typecheck and lint trusted from the clean post-build tree; 52 focused tests passed across planner defaults, schema bounds, env overlays, redaction, and catalog generation.
---

## 2026-09-13 21:00 - US-618D3 Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev (exit 0), and proved global `appEnv`/`appLabels` on a php:8.3 app: service env/labels won over defaults, reserved `LANDO_APP_NAME` and `dev.lando.*` writes were refused, `LANDO_APP_ENV` replaced the whole map on restart, and global/scratch containers did not receive the defaults. Adjacent `info`, `logs`, `exec`, HTTPS, and destroy stayed green. Scenario app, scratch fork, temp dirs, and config keys were torn down; only the global app remains.
- **Out-of-scope finding:** setup warned ports 80/443 were in use and fell back to 8000/4443. After restart, `curl http://appserver.us618d3v.lndo.site:8000/` returned nginx `404 page not found` (`Server: nginx/1.26.3`) while `https://appserver.us618d3v.lndo.site:4443/` still printed `us618d3-ok`. Suspected leftover host/WSL bind on 8000, not US-618D3.
- **Out-of-scope finding:** `lando config get appEnv --format=json` and `lando config --format=json` omit the map values (dump shows only `userDataRoot`/`userConfRoot`/`defaultProviderId`); `config set` and `~/.config/lando/config.yml` showed the maps. Suspected meta:config JSON projection, not this story's apply path.
---

## 2026-09-13 22:40 - Babysit
- PR: #977 https://github.com/AaronFeledy/core4/pull/977
- CI outcome: pass (initially failed `guide-scenarios-darwin-arm64`)
- Review feedback: fixed (typecheck OOMed at the default ~2GB V8 heap on darwin-arm64 — a pre-existing borderline failure on main that this branch's added typecheck surface made deterministic; raised the compiler subprocess heap ceiling in `scripts/typecheck.ts` for every `bun run typecheck` consumer, plus a transient Drupal scaffold e2e rerun)
- Merge: merged (squash)
---

## 2026-09-13 23:45 - US-618E
- Apache honors authored `webroot` in generated httpd config plus `APACHE_DOCUMENT_ROOT`/`LANDO_WEBROOT`; Node sets `PORT`, targets authored `port` for endpoints, and only adds a TCP healthcheck when a server `command` is authored (idle default stays unprobed).
- Canonical `ServiceType.versions`/`artifacts` is the single shipped matrix: planner rejects unknown/absent versions (including php8.0/node999/apache9) with tagged remediation before app registration; registered ids must also validate; planner-injected PHP pins no longer suppress FPM/prerequisites; Node idle command has no healthcheck.
- Learnings: exact registered version ids must also validate; planner-injected PHP pin must not suppress FPM/prerequisites; Node idle command no healthcheck.
- Verification: parent root typecheck/lint clean; 274 targeted tests across nine files passed; boundaries, codegen:check, schema compatibility (one accepted PhpServiceConfig widening), guide lint/coverage/public transcripts/drift pass. Sandbox fresh compile proved Apache `/app/public` HTTP, Node PORT5173 HTTP+TCP health, PHP8.3.33; php8.0/node999/apache9 fail LandofileValidationError before app registration. Apache2 Node4 PHPmatrix7 guide scenarios pass. Source LSP clean; scoped Oracle acceptance approved. QA three apps purged, temporary binaries/scripts/terminal removed, shared global remains. No full suite run.
---

## 2026-09-14 - US-618E Review
- Apache generated config now overwrites a dedicated include instead of appending to httpd.conf on every start. Recipe database prompts no longer offer `mariadb:10.11`, which the new catalog matrix rejects.
- Review notes (deferred/rejected): php-db-client 10.6/10.11 stay client-package versions, not service runtimes; php:8.0 still fail-closes via registered family ids rather than a bare `php` matrix; real-runtime HTTP belongs to Verify; full-suite uninstall failures are out of host-safe Review scope.
- Verification: typecheck and lint passed; Apache, planner-home, app-planner, Node, version-metadata, registration, and five recipe decomposer suites passed; guide-drift passed against origin/main. Post-fix Bugbot found no bugs.
- **Learnings for future iterations:** Fail-closed ServiceType version matrices must be checked against recipe prompt/snapshot enums in the same story, or init will write Landofiles that cannot plan.
---

## 2026-09-14 00:37 - US-618E Drift Audit
- Trigger: 2 cycles, 716 changed lines, 9 commits
- Weakened proofs: version-metadata lock only pinned a subset of shipped families
- Ceremony: unused Apache launcher export
- Dual validation: plugin validateVersion and registry-family rejects stay; php:8.0 still fail-closes as deferred
- Fixed: exhaustive shipped-matrix equality; unused Apache launcher unexported
- Split/excise decisions: none
- Verification: typecheck pass, lint pass, 202 focused tests pass
---
## 2026-09-14 01:33 - Babysit
- PR: none (no PR for branch us-618e-catalog-fields-versions; Push was gate-skipped at phase `reviewed`)
- CI outcome: n/a
- Review feedback: none
- Merge: deferred (no PR to watch or merge)
---
## 2026-09-14 21:35 - US-618E Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev, and proved Apache `webroot: /app/public` (HTTPS body `Apache webroot works`, `APACHE_DOCUMENT_ROOT`/`LANDO_WEBROOT`=/app/public, generated `lando-webroot.conf`) and Node `port: 5173` (HTTPS `PORT=5173`, in-container listen on 5173). `php:8.0`/`node:999`/`apache:9` failed `LandofileValidationError` before provider action; php:8.3 refresh and idle `node:22` (PORT=3000, no server) stayed green. Scenario apps torn down; only global remains.
- **Out-of-scope finding:** `curl http://web.us618e-apache.lndo.site:8000/` and the Node HTTP twin returned 404 while HTTPS `:4443` served the apps. Same host/WSL 80/443 fallback as prior verifies, not US-618E.
---
## 2026-09-14 23:20 - Babysit
- PR: #991 https://github.com/AaronFeledy/core4/pull/991
- CI outcome: pass (after two triage rounds; initial push had no Actions runs because the PR was CONFLICTING)
- Review feedback: fixed (merged origin/main and resolved conflicts; replaced a secret canary that collided with hex in a SHA-256 cache key; made the PHP db-client "custom" image genuinely custom and added stock-image coverage; refreshed stale LAMP producer digests in guide fixtures)
- Drift check: clean
- Merge: merged (squash)
---

## 2026-09-15 13:35 - US-623
- Tooling events and MCP now share argv serialization from normalized declarations, preserving declared positional order and hyphen-leading arguments. Decoded cross-surface tests compare argv and exit outcomes directly, while undeclared tasks retain exact raw argv bytes.
- Integer inputs now follow flag metadata rather than the `tail` name. MCP publishes integer schemas and rejects fractional, string, and non-finite values through its existing tagged validator; plugin-owned CLI parsing retains integer metadata instead of truncating fractions. The task-events and MCP guides document the behavior.
- Verification: gate:pr passed, including typecheck, lint, codegen, boundaries, and guide drift; semantic gates passed; 337 focused tests and both guides passed. Regression failures were captured before fixes. Sandbox CLI checks proved argv equality, raw passthrough, exit 7 preservation, and invalid-tail refusal. Final AC-scoped review approved; owned QA resources cleaned up.
- **Learnings for future iterations:**
  - Integer metadata must survive intermediate flag projections, including plugin-owned command definitions; fixing the primary parser alone leaves a second path silently truncating fractional input.
  - CLI parity fixtures must avoid global flag names such as `verbose`: those tokens belong to the outer CLI, not to the task's structured input contract.
---

## 2026-09-15 - US-624
- Routes now retain distinct host, scheme, and path matches instead of silently collapsing same-host paths. Equivalent backends and ordered filters deduplicate; conflicting matches fail with a source-aware RouteInputError before provider action. Shorthand ports continue selecting backend endpoints rather than listeners.
- The planner assigns explicit priorities: exact hosts outrank wildcards, longer paths win next, and lexical hostname/path/scheme ordering resolves ties. Traefik projects those priorities above its diagnostic fallback. Both route guides document identity, ordering, fallback ports, and the expected HTTP 404 for HTTPS-only routes.
- Verification: gate:pr, schema compatibility, guide lint/coverage/transcripts, and changed-file LSP checks passed; 340 focused tests and both guides passed. Native-Bun sandbox CLI checks passed 107 tests with one existing environment-gated skip. The same-host collapse was captured red before implementation; final acceptance review approved unconditionally.
- Live sandbox requests on fallback ports reached the exact, wildcard, longer-path, and scheme-specific responders. Disabled routing published no shared route, returned 404 there, and exposed only a working host endpoint through info. Both QA apps were destroyed with their volumes; shared sandbox resources were preserved.
- **Learnings for future iterations:**
  - Compose services with explicit endpoints use those endpoints instead of deriving publication from ports. To prove host-only publication, declare a published endpoint with its publication mapping rather than mixing internal endpoints and ports.
  - Embedded-Bun CLI test runners can misroute internal worker entrypoints as scripts. Native Bun resolved the non-live harness failures without changing product code or test assertions.
---

## 2026-09-15 - US-624 Review
- Reviewed route identity/specificity against source ACs. No in-scope product defects; no code changes.
- Review notes (deferred/rejected): AC1 "port" in match identity is the scheme's acquired listener, not Landofile `:port`; treating `:port` as a public match would change host-port acquisition, a story non-goal. Security HIGH (per-app ranks letting one app's wildcard outrank another app's exact host) is out of scope: the planner policy is per AppPlan, the guide states "within an app," and Traefik already merged independent configs. QA "diagnostic lowest untested" is false: diagnostics.ts emits priority 1 and proxy.test.ts asserts it. Backend-port conflict already fail-closes; not asserting RouteInputError class is not a PRD miss. Bugbot did not run (Cursor usage limit).
- Verification: lint:guides, check:guide-coverage, codegen:guide-scenarios, check:public-transcripts, and GUIDE_DRIFT_BASE_REF=origin/main check:guide-drift passed; gate:pr passed; 79 focused tests across route-identity, route-normalization, route-urls, route-normalize, and route-filter passed.
- **Learnings for future iterations:** Route-match "port" in this stack means the router listener implied by scheme. Shorthand `:port` is backend selection and must not be re-litigated as a second public listener.
---
## 2026-09-15 - US-626 (implementation verified)

- Config view and get now return one curated effective public config projection, including persisted appEnv and appLabels maps; encoded results retain maps, selected keys, roots, and telemetry while omitting internal bookkeeping.
- Secret-bearing map values and proxy credentials are redacted through the canonical output boundary across renderer/format combinations, including scalar lookups, without changing persisted values. The public schema, result encoding, redaction hook, and regression tests land atomically so no intermediate revision exposes newly visible secrets.
- Verification: gate:pr passed (typecheck, lint, codegen:check, check:boundaries, and guide drift against origin/main); 491 focused tests, 9 adjacent tests (host CLI case filtered), and 3 executable global-config guide tests passed with zero failures. Schema compatibility and public-transcript gates passed; modified TypeScript LSP diagnostics were clean. Evidence was supplied by implementation session ses_f58dd5c42ffeWLagKvHhrBjcMG.
- Sandbox acceptance: a relocated compiled binary in lando4-dev with isolated config/data/cache roots passed all 48 redaction combinations plus encoded-map parity, selected-key preservation, roots, telemetry, validation, internal omission, and invalid-map tagged rejection. QA teardown removed owned temporary roots and the relocated binary; no app/provider resources were created.
- Acceptance approval: reviewer ses_f58c261beffebRyPG4J7ElI3xN approved all criteria unconditionally. This entry records implementation and acceptance evidence only; no completion signal, push, PR, or merge was performed.
- **Learnings for future iterations:** widening a config result requires both a curated schema projection and output-boundary value tokens; scalar lookups lose their enclosing secret-key context unless tokens come from the full config or selected key. Preserve key metadata while redacting values, and test encoded output rather than only the in-memory operation result.
---

## 2026-09-15 - US-626 Review
- Reviewed redacted config view/get against source ACs. No in-scope product defects; no code changes.
- Review notes (deferred/rejected): Security HIGH (GITHUB_PAT, short numeric API_TOKEN=1234, username-only proxy userinfo) is out of scope. AC4 requires the canonical RedactionService path, which view/get already use; GITHUB_PAT is outside collectSecretEnvValues vocabulary, short numerics are the existing short-token policy (US-633 keeps it), and username-only proxy matching belongs to redactor URL patterns / proxyUrls, not this story's projection. Goal/QA/quality/context lanes passed. Bugbot did not run (Cursor usage limit).
- Verification: lint:guides, check:guide-coverage, codegen:guide-scenarios, check:public-transcripts, and GUIDE_DRIFT_BASE_REF=origin/main check:guide-drift passed; gate:pr passed; 94 focused tests passed across config-view schema, operations, CLI redaction, meta-config scenarios, and adjacent write tests.
- **Learnings for future iterations:** Newly exposing maps through a curated schema does not expand the secret-key catalog. Token vocabulary and short-token policy stay in @lando/redaction; do not add a config-specific redactor to satisfy a security lane that wants PAT/short-PIN/username-only coverage.
---
## 2026-09-15 18:12 - US-626 Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev, then proved `lando config set/get/view` against the binary: appEnv/appLabels round-trip, JSON/YAML/table parity, secret `DB_PASSWORD` redacted as `[redacted]` in every mode while `config.yml` kept the real value, invalid map writes fail `LandofileWriteValidationError` without changing the file. Started a php:8.3 app; HTTPS and `lando exec` printed `us626-ok`; `printenv TEAM` was `platform`. Adjacent `config validate`, `config get defaultProviderId`, `global:config`, `doctor`, `app:config`, and `logs` stayed green. App destroyed; maps unset; scratch dirs removed.
- **Out-of-scope finding:** `curl http://appserver.us626-verify.lndo.site:8000/` returned Traefik `404 page not found` after setup warned 80/443 were in use and fell back to 8000/4443; HTTPS `:4443` served the app. Same host/WSL mirrored bind as prior verifies, not US-626.
- **Out-of-scope finding:** `lando config --format=yaml` emits `DB_PASSWORD: [redacted]` unquoted, which YAML parses as a sequence. JSON keeps the string sentinel. Secret is not leaked; quoting belongs with serializer/redaction encoding, not this view story.
---
## 2026-09-15 19:15 - Babysit
- PR: #998 https://github.com/AaronFeledy/core4/pull/998
- CI outcome: fail then pass (schema-snapshot check:schema-compatibility; guide-scenarios-linux-x64 and provider-integration-linux-x64 confirm jobs were downstream skips)
- Review feedback: fixed (recorded the intentional `command:meta:config` -> GlobalConfigView ref change as two justified entries in sdk/compatibility-exceptions.json; local Bugbot found no bugs)
- Merge: merged (squash)
---

## 2026-09-15 - US-628
- Startup URL scans now read only status and headers within a scope, close response streams without buffering bodies, limit concurrent probes to four, and report measured elapsed time for timeout and deadline diagnostics. Failed scans still warn without failing start; interruption propagates.
- The HTTP trust seam explicitly sends loopback and configured NO_PROXY matches directly, without exempting router domains or callers. A narrow transport adapter avoids Bun's ambient-proxy fallback while preserving custom CA verification, redirects, streaming decompression, cancellation, and paired lifecycle events.
- Permanent serial live tests prove the actual start CLI probes its published URL, a failing response warns at exit zero, and interrupting a real start aborts its in-flight scan. Explicit matching managed-runtime socket opt-in is required; missing or mismatched opt-in safely skips.
- Verification: final gate:pr passed, including typecheck, lint, codegen, boundaries, and guide drift; 123 final focused tests passed, the HTTP package's 112 tests passed, and the router-and-scanner guide passed three scenarios. Regression failures were captured before implementation. Changed TypeScript diagnostics were clean; acceptance review approved after fixes and its final gate condition passed.
- Sandbox verification passed all three permanent live scenarios with three scoped app/volume cleanup receipts. Owned temporary binaries and tmux windows were removed; shared sandbox resources were preserved. No full suite was run.
- **Learnings for future iterations:**
  - Bun 1.4 treats empty, null, and false fetch proxy options as ambient-proxy fallback, not explicit direct routing. A direct adapter must also preserve compressed response semantics for non-scanner consumers.
  - Live tests using the bundled managed provider must require an explicit socket override matching that provider's actual socket; socket existence alone is not safe test opt-in.
---
## 2026-09-15 20:51 - US-628 Drift Audit
- Trigger: 1 cycle, 1587 changed lines, 8 commits
- Scope: the direct HTTP adapter is this story's trust-seam policy, not absorbed adjacent work; no public SDK surface and no prd.json edits
- Weakened proofs: fail-first is not in git history (implementation and tests landed together); remaining ACs still have production-path proofs for scoped status reads, concurrency 4, elapsed diagnostics, local-endpoint policy, and env-gated live start
- Ceremony: new http-client modules are single-importer but own redirect/direct transport, not forwarding facades; left uninlined because live.ts is already over the size ceiling
- Split/excise decisions: none
- Verification: typecheck pass, lint pass, 114 focused unit tests pass. Live integration not re-run (host-unsafe).
---
## 2026-09-15 21:10 - US-628 Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev, started a php:8.3 app, and proved post-start scanning against the binary: default `/` scan succeeded with no URL warning; `scanner.path: /nope` warned `URL scan for service appserver at https://appserver.us628-verify.lndo.site:4443/nope answered with an unaccepted status: HTTP 404` while start still exited 0; `scanner: false` emitted no URL-scan warning. HTTPS curl and `lando exec` printed `us628-ok`; `lando info` listed the published HTTPS URL; invalid `retries: -1` failed lint on `services.appserver.scanner.retries`. App destroyed; temp dir removed.
- **Out-of-scope finding:** setup warned ports 80/443 were in use and fell back to 8000/4443; `curl http://appserver.us628-verify.lndo.site:8000/` returned 404. Same host/WSL mirrored bind as prior verifies; HTTPS-only diagnostic is US-624, not US-628.
- **Out-of-scope finding:** `lando destroy --yes` against a Landofile with `scanner.retries: -1` failed `LandofileValidationError` before teardown. Restoring a valid Landofile then destroy succeeded. Belongs to US-629 invalid-config teardown, not this scanner story.
---
## 2026-09-16 - US-629 (in progress, not complete)
- Stop and destroy now recover validated applied state when desired configuration is unavailable or invalid, including fresh library handles. Provider provenance, canonical root, and ownership checks fail closed; resource-confirmed empty apps return an explicit unchanged outcome. Failed provider teardown or applied-record removal no longer silently discards recovery evidence.
- Inventory reports stale roots, and bounded pruning rechecks provider resources under the app mutation lock before removing modern applied records. Stop/destroy output and the lifecycle and everyday-command guides describe the new outcomes.
- Verification: gate:pr, typecheck, lint, boundaries, guide gates, and changed-file diagnostics passed; the final focused safety checks passed 63 tests, and both required executable guides passed one scenario each. Sandbox CLI verification stopped and destroyed an app after invalidating its configuration, removed its volumes, and left only the shared global app in inventory. Owned sandbox fixtures were removed; no full suite was run.
- Remaining acceptance blocker: stale pruning still leaves legacy provider inventory records that discovery continues to read, allowing a pruned entry to reappear. Remove applicable legacy records through advisory-locked StateStore buckets inside the existing app mutation lock, with legacy-only and duplicate-record regression coverage, before marking the story implemented.
- **Learnings for future iterations:**
  - Applied-plan provider identity must be checked against the provider supplying the record, not a provider selected from the record itself.
  - Empty applied state is not proof of absent resources; include stopped containers and owned volumes, and propagate inspection failures.
  - Inventory cleanup must cover every representation that discovery reads, including legacy records, and deletion failures must not produce successful prune receipts.
---
## 2026-09-16 - US-629 (implemented)
- Stale inventory pruning now removes matching legacy provider records as well as modern applied state, so legacy-only and duplicate entries stay gone after rediscovery. App, root, and provider mismatches fail closed; deletion failures cannot produce successful prune receipts.
- Applied ownership discovery now reads persisted provider inventory before initializing a runtime. Invalid-config stop and destroy select the actual owner without failing on an unrelated installed provider's unavailable socket; inventory failures and ownership conflicts still propagate.
- Verification: gate:pr passed, including typecheck, lint, codegen, boundaries, and guide drift; 135 final focused tests passed. Both required executable guides passed with positive scenario counts, guide gates passed, and changed TypeScript diagnostics were clean. Regression failures were captured before both fixes; independent acceptance review approved without blockers. No full local suite was run.
- Sandbox verification: legacy-only and duplicate stale records were pruned with exact receipts and remained absent. A live app stopped after config corruption with all four volumes preserved, then restarted and was destroyed after another corruption; containers, volumes, and inventory were empty afterwards. Owned QA roots, daemons, network, and tmux resources were cleaned without touching shared resources.
- **Learnings for future iterations:**
  - Persisted ownership discovery must not initialize every installed runtime: an unrelated provider's valid availability check can otherwise block recovery of the actual owner's applied state. Keep inventory reading separate from live provider construction.
  - Legacy inventory filenames are not authoritative app identities; decode records and validate app, root, and provider before removing them under the existing mutation lock.
---
## 2026-09-16 - US-629 Review
- Prune now rechecks that the candidate root is still missing under the app mutation lock, and deletes modern/Podman applied records only when the persisted root still matches the stale candidate, so a same-id live app at another root cannot be erased.
- Review notes (deferred/rejected): ownerKey volume-selector dual-path is a pre-ship compatibility shim; onCorrupt discard is the existing applied-state cache-miss contract from #850; live container/network label inspection before name-based bring-down is a pre-existing provider teardown surface, not this story's applied-record checks.
- Verification: typecheck diagnostics clean on changed files; 38 focused prune tests passed; guide gates and gate:pr were already green before the prune fix.
---

## 2026-09-16 - US-629 Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev, started a php:8.3 app, and proved applied-state teardown: invalid-config stop preserved the app, missing Landofile destroy cleared inventory, never-started/repeat destroy returned `unchanged`, a deleted root listed as stale and `--prune` kept it while resources remained, then empty-root destroy removed it.
- Blocker fixed: empty applied teardown initialized bundled docker/podman daemons and failed `ProviderUnavailableError` instead of `unchanged`; skip runtime evidence when `isAvailable` is false.
- **Out-of-scope finding:** `curl http://appserver.us629-verify.lndo.site:8000/` was not used; setup warned 80/443 in use and fell back to 8000/4443. HTTPS `:4443` served `us629-ok`. Same host/WSL mirrored bind as prior verifies.
- **Out-of-scope finding:** after a volumes-preserving destroy, a later `destroy --volumes` without restart fails `AppResolveError` (`provider-resources`) because applied state is gone while volumes remain. Start then `destroy --volumes` cleans them. Fail-closed by design for this story.
- **Learnings for future iterations:** bundled unused providers must not be initialized to confirm absence; `isAvailable` false (or make() failing) is not evidence of leftover resources.
---

## 2026-09-16 15:15 - Babysit
- PR: #1003 https://github.com/AaronFeledy/core4/pull/1003
- CI outcome: fail (unit-tests-linux-x64-shard 1 and 2, guide-scenarios-windows-x64) — residual failures are `lando stop`/`lando destroy` "preserves the missing Landofile error outside an app directory", the three `destroy --purge/--purge-caches/--volumes` flag-parsing scenarios, and compiled app command aliases; all exercise the compiled CLI binary, which the focused source-level spot checks did not cover.
- Review feedback: fixed (optional identity honored on resolved teardown plans; guide fixture registry no longer reports a false empty applied state; desired-resolution errors now propagate when applied recovery finds no plan; `apps:list` MCP opt-in restored; container label goldens updated for the consumed `dev.lando.app-root` label). First triage round wrongly rejected the MCP, container-label, and missing-Landofile failures as stale expectations — all three were real and were fixed in round two.
- Drift check: clean (no drift in the five babysit commits; emission goldens remain as strict as at the audit base)
- Merge: deferred (CI still red after two triage rounds; next Build must fix compiled-CLI teardown so a directory with no Landofile still fails with the `lando init` remediation instead of an idempotent `unchanged`)
---
## 2026-09-16 15:13 - Babysit
- [looper] Babysit blocked: CI still red: compiled-CLI teardown must preserve the missing-Landofile error (stop/destroy scenarios + compiled aliases) and guide-scenarios-windows-x64 fails; babysit budget exhausted after two triage rounds
---
## 2026-09-16 - US-629 Drift Audit
- Trigger: 4 cycles, 2668/241 changed lines, 37 commits
- Dual path: compiled `apps:list --prune` called read-only list and emitted empty prune receipts
- Weakened proofs: `listServices({ prune: true })` blessed a fake receipt; source missing-Landofile stop/destroy still fail-closed
- Ceremony: new modules are real logic, not forwarders; left in place to avoid blowing 250 LOC
- Collateral: `prd.json` unchanged; no other-story phases
- Deferred re-exam: ownerKey volume-selector, leftover volumes, onCorrupt — no in-place regression to fix
- Fixed: compiled list prune uses the mutating path; prune receipts emit only when prune actually ran
- Split/excise decisions: none
- Verification: typecheck pass, lint pass, 110 focused tests pass; compiled-binary missing-Landofile CI residual not reproduced on source CLI
---
## 2026-09-16 17:55 - Babysit
- PR: #1003 https://github.com/AaronFeledy/core4/pull/1003
- CI outcome: pass
- Review feedback: fixed (provider-lando lifecycle/cross-process fixtures realigned to the four-part ownership-scoped volume selector; windows-arm64 private-file ACL failure was an unrelated runner flake cleared by rerun)
- Drift check: clean
- Merge: merged (squash)
---

## 2026-09-16 18:40 - US-631

Solr core names can no longer address anything but their own directory, and the config overlay rules are written down instead of implied.

- Core names now go through a `SolrCoreName` schema in the Solr plugin that keeps the existing character set and additionally refuses the reserved `.` and `..`. The check moved out of command generation and into `ServiceType.resolve`, so a bad name is refused before the startup command exists, before any provider is selected, and even when the author supplied their own `command:` (authored `cores` is configuration, so its validity does not depend on whether the built-in command consumes it). The refusal is a tagged `ServiceTypeError` whose message names `services.<name>.cores[<index>]`, the rejected value, and the fix; the planner turns that into the user-visible `LandofileValidationError` against the Landofile. Dotted names such as `a.b` and `a...b` stay accepted and keep byte-identical generated shell.
- The feature-level guard stayed, rewritten to consume the same schema, so composing `solrServiceFeature` directly without a resolver still cannot emit a traversing core. The schema is the single rule source; the guard is a sink.
- The Solr guide now states the core naming rule with its error text, and describes the config overlay as intentional: `cp -a` lays the mounted directory over what `precreate-core` wrote for each core, it reruns every start, and a failed copy exits instead of serving a half-configured core. It also documents that overlay identity is content, not path: relocating a directory keeps the build key, changing a file inside it rebuilds, and an empty directory is a legal overlay with one fixed identity.
- Locked the empty-overlay contract with tests rather than leaving it to inference: an empty `config.dir` hashes the empty file list, two empty directories at different paths share that identity, the resulting service build key survives relocation, and the first file added changes it.

Verification: gate:pr (typecheck, lint, codegen:check, all 13 boundary rules, guide-drift against origin/main) passed, plus check:guide-coverage, check:public-transcripts, check:schema-compatibility (0 breaking), and lint:guides. Spot checks: 977 service-lando tests, 922 engine planner/services tests, and the Solr guide (2 scenarios) all pass with 0 failures. Manual CLI QA on the real binary with isolated user roots: `.`, `..`, `a/b`, `a\b`, and `../escape` each fail `lando info` with exit 1, `code: LandofileValidationError`, `issues: services.search`, and a message naming `services.search.cores[0]`; the same rejection fires with an authored `command:`; `a.b`, `a...b`, `core_1`, and `core-2` plan through validation and stop only at the absent provider.

- **Learnings for future iterations:**
  - The RED test earned its keep twice. It proved `.` and `..` were accepted, and it also exposed that every other rejection was happening inside `defaultCommand`, which the authored-`command:` path skips entirely.
  - Copying an existing guide's rejection scenario is a trap: the `invalid-service-type` scenario passes because its fixture contains the one string the scenario-layer fake looks for, not because `lando start` validates anything. Check which commands the harness actually forwards before writing a failure scenario.
  - A plugin cannot widen the error surface it reports. `ServiceType.resolve` is typed to `ServiceTypeError` and `servicePlanError` fixes `issues` at `services.<name>`, so precision has to live in the message. That is enough for a source-aware refusal and avoids an SDK error-shape change for one service type.
---
## 2026-09-16 19:12 - Babysit
- PR: #1004 https://github.com/AaronFeledy/core4/pull/1004
- Merge: merged (squash) as 9ae63749 on main
- Reconciled by hand, not written by the step. Babysit merged this PR and switched back to
  `main`, then was killed ~33 minutes later by a permission-gate timeout (external_directory)
  before it ran `looper signal story-phase merged`. US-631 therefore sat at `published` while
  every story-gated step skipped on `main`, until the stall detector stopped the run.
---
## 2026-09-16 19:45 - Babysit
- [looper] Babysit blocked: permission gate timed out: external_directory
---

## 2026-09-17 - US-632 (in progress, not complete)

Landed the fail-first regression proof and the user-facing contract; the shared resolver itself is
mid-flight in the working tree and uncommitted.

- The defect is now pinned by a cross-surface regression test: one temp app carrying an authored task,
  a public tooling include, an `internal: true` tooling include, and a service type that contributes
  its own task. Both `lando app:config:lint` and the planner are asserted against ONE explicit
  `validEvents` literal. RED captured: lint reported only the lifecycle names plus the authored and
  public-include brackets, so `pre-inspect` (the service-contributed task) came back as an unknown
  event while the planner accepted it. Internal-include brackets were already excluded on both
  surfaces, so include provenance was never the bug.
- The lint and task-events guides now describe the single resolution rule: lint, doctor, and start
  build the known set from the Landofile, every config layer, every include, and every service type,
  without touching a container runtime, and a resolution failure is reported as itself rather than as
  a false unknown-event. A new guide scenario lints an app whose only `pre-mysql` producer is the
  `mysql:8.0` service type.

Verification so far: the regression test fails 2/2 for the intended reason, and `lint:guides` passes.
Repo gates were not run because the implementation is incomplete.

Remaining work, decided and ready to execute:
- Extract the provider-free per-service resolution prefix of `planApp` (through `ServiceType.resolve`)
  plus the effective-tooling and event-name block into one shared resolver that `planApp` and
  `appConfigLint` both call, so start still resolves each service exactly once.
- `appConfigLint` takes `PluginRegistry` in its requirements channel; the lint command spec moves from
  the `minimal` bootstrap tier to `plugins`, because `minimal` does not merge the registry.
- Doctor injects the app-config section from its already-built provider context, keeps only its three
  existing tagged catches, and lets resolution failures reach the section isolator.
- Guard the MSSQL architecture-emulation check on capabilities being present, so provider-free lint
  does not fail a service that start accepts.
- `attachEffectiveTooling` re-sorts into a new object and drops the WeakMap internal-task provenance;
  attach the already-sorted object instead.

- **Learnings for future iterations:**
  - The surfaces did not disagree about includes, only about service types. `appConfigLint` passed
    `services: []` into `compileEffectiveTooling`, so every task a service type contributes was
    invisible to lint and doctor while `lando start` honored it.
  - A cross-surface regression test must hand BOTH surfaces the same `PluginRegistry`. Providing the
    test registry only to the planner makes lint fail for a wiring reason that looks like the defect.
  - Internal-task exclusion rides a WeakMap keyed on object identity, so nothing between
    `compileEffectiveTooling` and `validEventNames` may spread, clone, or re-sort the tooling map.
---
## 2026-09-17 16:01 - Review
- [looper] gate skipped Review: gate: phase merged is at or past reviewed (nothing to do)
---
## 2026-09-17 16:01 - Drift Audit
- [looper] gate skipped Drift Audit: gate: script exited with code 1
---
## 2026-09-17 16:01 - Verify
- [looper] gate skipped Verify: gate: phase merged is at or past verified (nothing to do)
---
## 2026-09-17 16:01 - Cleanup
- [looper] gate skipped Cleanup: gate: phase merged is at or past published (nothing to do)
---
## 2026-09-17 16:01 - Push
- [looper] gate skipped Push: gate: phase merged is at or past published (nothing to do)
---

## 2026-09-17 16:05 - Babysit
- PR: none — branch us-632-shared-event-resolution has no commits ahead of origin/main and no upstream; US-632 changes are uncommitted in the working tree
- CI outcome: n/a (no PR to watch)
- Review feedback: none
- Merge: deferred (missing PR; story work never committed or pushed)
---
## 2026-09-17 16:03 - Build
- [looper] gate skipped Build: gate: phase merged is at or past implemented (nothing to do)
---
## 2026-09-17 16:03 - Review
- [looper] gate skipped Review: gate: phase merged is at or past reviewed (nothing to do)
---
## 2026-09-17 16:03 - Drift Audit
- [looper] gate skipped Drift Audit: gate: script exited with code 1
---
## 2026-09-17 16:03 - Verify
- [looper] gate skipped Verify: gate: phase merged is at or past verified (nothing to do)
---
## 2026-09-17 16:03 - Cleanup
- [looper] gate skipped Cleanup: gate: phase merged is at or past published (nothing to do)
---
## 2026-09-17 16:03 - Push
- [looper] gate skipped Push: gate: phase merged is at or past published (nothing to do)
---

## 2026-09-17 16:10 - Babysit
- PR: none — no PR exists for branch us-632-shared-event-resolution
- CI outcome: n/a (branch tip equals origin/main, no upstream, US-632 work still uncommitted in the working tree)
- Review feedback: none
- Merge: deferred (nothing published to merge; work must be committed and pushed first)
---
## 2026-09-17 16:04 - Build
- [looper] gate skipped Build: gate: phase merged is at or past implemented (nothing to do)
---
## 2026-09-17 16:04 - Review
- [looper] gate skipped Review: gate: phase merged is at or past reviewed (nothing to do)
---
## 2026-09-17 16:04 - Drift Audit
- [looper] gate skipped Drift Audit: gate: script exited with code 1
---
## 2026-09-17 16:04 - Verify
- [looper] gate skipped Verify: gate: phase merged is at or past verified (nothing to do)
---
## 2026-09-17 16:04 - Cleanup
- [looper] gate skipped Cleanup: gate: phase merged is at or past published (nothing to do)
---
## 2026-09-17 16:04 - Push
- [looper] gate skipped Push: gate: phase merged is at or past published (nothing to do)
---

## 2026-09-17 16:06 - Babysit
- PR: none — no PR exists for branch us-632-shared-event-resolution
- CI outcome: n/a (branch tip equals origin/main; US-632 work still uncommitted, no local commits to push)
- Review feedback: none
- Merge: deferred (nothing published to merge; PRD phase says merged but no commit or PR exists)
---
## 2026-09-17 16:05 - Build
- [looper] gate skipped Build: gate: phase merged is at or past implemented (nothing to do)
---
## 2026-09-17 16:05 - Review
- [looper] gate skipped Review: gate: phase merged is at or past reviewed (nothing to do)
---
## 2026-09-17 16:05 - Drift Audit
- [looper] gate skipped Drift Audit: gate: script exited with code 1
---
## 2026-09-17 16:05 - Verify
- [looper] gate skipped Verify: gate: phase merged is at or past verified (nothing to do)
---
## 2026-09-17 16:05 - Cleanup
- [looper] gate skipped Cleanup: gate: phase merged is at or past published (nothing to do)
---
## 2026-09-17 16:05 - Push
- [looper] gate skipped Push: gate: phase merged is at or past published (nothing to do)
---

## 2026-09-17 16:10 - Babysit
- PR: none — no PR exists for us-632-shared-event-resolution
- CI outcome: n/a (no PR)
- Review feedback: none
- Merge: deferred (branch has no commits; HEAD == origin/main and all US-632 changes remain uncommitted in the working tree, so nothing was pushed or merged)
---

## 2026-09-17 17:30 - US-632 (complete)

`lando app:config:lint`, `lando doctor --app`, and `lando start` now build the known event set from one shared,
provider-free resolver.

- Extracted the provider-free prefix of `planApp` into a shared resolver: provider precedence, plugin manifests,
  per-service seeds resolved through `ServiceType.resolve`, effective tooling, the reserved-name check, and the valid
  event set. `planApp` consumes the seeds that resolver returns rather than resolving services a second time, and a new
  test pins each service to exactly one `resolve` call.
- Lint and doctor call the same resolver, so a task contributed by a service type (`mysql` on a `mysql:8.0` service) makes
  `pre-mysql`/`post-mysql` valid on every surface, while an `internal: true` include task still contributes no brackets
  anywhere. Doctor injects the app-config section from the provider context it already built, and a resolution failure
  reaches the section isolator as its own tagged error instead of a false unknown-event diagnostic.
- `app:config:lint` moved to the `plugins` bootstrap tier (both the spec and the compiled CLI adapter) because the shared
  resolver needs `PluginRegistry`; `App.config.lint()` widened its error channel accordingly.
- The Landofile parser now accepts namespaced keys, which authored event keys need: an include contributes `docs:generate`,
  so `pre-docs:generate` is a legal event key and previously failed to parse.
- MSSQL's architecture-emulation refusal now fires only when capabilities were actually supplied, so provider-free lint no
  longer rejects a service `lando start` accepts.

Verification: gate:pr (typecheck, lint, codegen:check, all boundary rules, guide-drift against origin/main) passed, plus
check:guide-coverage, check:public-transcripts, check:schema-compatibility (0 breaking), and lint:guides. Spot checks:
371 engine planner/operations tests, 582 landofile/parser tests, 2631 core CLI tests, and the task-events and config-lint
guides all pass; the two failures in `core/test/cli/uninstall-runtime-service.test.ts` reproduce identically on a stashed
tree and are unrelated. Manual CLI QA on the real binary with isolated user roots: an app hooking `pre-mysql` on a
`mysql:8.0` service lints clean (exit 0) where the pre-change tree rejected it as unknown; a typo'd key now lists
`pre-mysql`/`post-mysql` in its valid set; `doctor --app` reports that same app-config section; an unresolvable service type
surfaces `LandofileValidationError` from both lint and the doctor section isolator; `doctor` without `--app` still omits the
section.

- **Learnings for future iterations:**
  - Raising a command's bootstrap tier is two edits, not one. The `LandoCommandSpec` `bootstrap` field and the
    `cliRuntimeOptions({ bootstrap })` call in the compiled CLI adapter must agree; only the second one governs the real
    binary, and a mismatch typechecks.
  - `check:codegen-drift` is git-status based, so a story that changes a command summary reports drift until the
    regenerated `docs/reference/commands.mdx` is committed. Run `gate:pr` again after committing before believing a
    drift failure.
  - Prove a cross-surface parity fix at the binary, not only in tests: stashing the branch and re-running the same
    `lando app:config:lint` against the same fixture is the cheapest RED/GREEN evidence for this class of defect.
---

## 2026-09-17 17:41 - US-632 Review
- Confirmed lint, doctor, and start share one provider-free resolver. Fixed namespaced mapping-entry splitting so comment stripping and `!reset`/`!override` detection use the same colon-aware key grammar as parse.
- Review notes (deferred/rejected): capability-dependent tooling divergence is out of scope (same resolver; start may pass capabilities it already has; bundled types follow the MSSQL undefined-means-not-asked pattern). Doctor skipping app lint when provider bootstrap fails is pre-existing doctor resilience, not an AC miss. Resolution failures reaching the doctor section isolator (instead of app-config-lint) is the story's designed tagged-error path, not a false unknown-event. `LandofileFormConflictError` is already in the public lint channel via the `LandofileNotFoundError` alias. Extra three-surface/MySQL/provider-init tripwire tests were rejected as coverage preference; lint/planner parity, doctor→lint delegation, and the two guides already satisfy the ACs.
- Verification: typecheck, lint passed; 31 namespaced-key and tag-detection tests passed, plus 96 parser/round-trip/tag tests; guide gates passed (`lint:guides`, `check:guide-coverage`, `codegen:guide-scenarios`, `check:public-transcripts`, `GUIDE_DRIFT_BASE_REF=origin/main`).
- **Learnings for future iterations:** Widening YAML mapping keys is not done when only `parseMap` changes. `stripComment` and `detectLandofileTags` still split on the first colon unless they share the same splitter.
---
## 2026-09-17 17:46 - Drift Audit
- [looper] gate skipped Drift Audit: gate: script exited with code 1
---

## 2026-09-17 19:16 - Babysit
- PR: #1008 https://github.com/AaronFeledy/core4/pull/1008
- CI outcome: pass (after triage; `guide-scenarios-windows-arm64` needed one rerun after hitting the 30m cap — it passed in 8m37s on retry)
- Review feedback: fixed (comment stripping now opts into the compact-mapping splitter so `key:#comment` stays an empty value; lockfile-only devalue 5.9.0→5.9.1 to clear the new GHSA advisory failing `bun audit` in every static-checks job)
- Drift check: clean
- Merge: merged (squash)
---

## 2026-09-17 19:45 - US-633

Secrets in label and environment values now redact regardless of key separator, and the Compose export is
parseable YAML for every key shape a service can carry.

- Secret-key tokenization in `@lando/redaction` treats dots the same as hyphens and underscores, so
  `com.example.password`, `dev.example.db-password`, and `DB_PASSWORD` all match one canonical rule. That is the
  only tokenizer in the repo, and no label- or env-specific redactor was added: label values still reach the
  canonical `RedactionService` through the existing app-plan token collector and the global-config token walker.
  The short-token policy is unchanged and now asserted under dotted keys too, so cursor-parameter noise like `32`
  and `38;2;255;0;0` still never becomes a redaction token.
- The Compose export serializer routes every dynamic mapping key through one formatter that emits a plain key only
  when YAML cannot re-resolve it, and double-quotes everything else. Before this, a label or environment key
  containing a colon, a leading indicator, whitespace, or a bare scalar word produced a document that would not
  parse at all. Values were already unconditionally quoted; that is now locked by a test rather than assumed.
- `provider-docker` no longer carries its own hand-written Compose renderer and `Bun.write` exporter. Both providers
  are thin adapters that inject their own error context into the single container-runtime serializer, so Docker's
  export gains environment, labels, volumes, tmpfs, and dependency output it never had, writes through the
  `FileSystem` service under the app-ID path, and fails closed with a tagged provider error instead of emitting an
  empty image reference. Runtime container creation still goes through the provider HTTP API and is proven to never
  touch YAML.

Verification: gate:pr passed (typecheck, lint, codegen:check, all thirteen boundary rules, guide-drift against
origin/main), plus lint:guides and check:guide-coverage. Spot checks: compose emission and redaction suites, the
provider-docker contract suite, service-lando Compose passthrough, container-runtime plan/config/service-field
suites, and the CLI config-view redaction matrix all pass with positive counts; the `global-config` guide passes.
The one failure in `plugins/provider-lando/test/logs-live-container-files.integration.test.ts` needs a real
container runtime and is unrelated. Manual CLI QA on the real binary with isolated user roots: with a dotted secret
in `appLabels`, `lando config get appLabels --format=json` emitted the secret verbatim before the change and
`[redacted]` after, while the non-secret dotted label stayed visible.

- **Learnings for future iterations:**
  - A redaction canary can pass for the wrong reason. The SDK's key-name pattern masks some structured output
    independently of tokenization, so prove a tokenization fix by reverting the one-line change and re-running the
    same test: if it stays green, the canary is measuring the other layer.
  - Reuse of a canary value across two keys hides a gap. An early label test used one value under both a dotted and
    a hyphenated key and passed before the fix, because the hyphenated key already matched.
  - A whole-document YAML round-trip is the only assertion that catches an unparseable export. Substring goldens
    over `toContain` stayed green while the emitted document could not be parsed at all. Pair the structural
    round-trip with lexical assertions, because JavaScript object-key coercion makes `true`, `null`, and `123`
    compare equal after parsing whether or not they were quoted.
  - YAML caps simple keys at 1024 code points, but the parser accepts longer quoted keys and no Lando key
    approaches that, so the explicit-key (`? key`) form was deliberately left unimplemented rather than carried as
    untested branch depth at six call sites.
  - Prose-only guides have no `<Guide>` block, so `dev:guides` reports no scenario output and exits non-zero for
    them. That is not a failure; their gates are `lint:guides` and `check:guide-coverage`.
---

## 2026-09-17 19:55 - US-633 Review
- Confirmed dotted/hyphen/underscore secret-key tokenization, one Compose serializer, YAML round-trip of odd keys, and runtime apply still bypassing YAML. No in-scope product defects.
- Review notes (deferred/rejected): token guide edits rejected — check:guide-drift vs origin/main passed with no covered-source hit; global-config already documents secret redaction; compose-compatibility is the Landofile key-disposition page, not export quoting. review-work 5-lane harness could not spawn (continuation task_id injection); equivalent AC/QA/quality/security/context review plus Bugbot (no bugs) substituted.
- Verification: typecheck, lint, codegen:check, boundaries passed; guide gates passed (lint:guides, check:guide-coverage, codegen:guide-scenarios, check:public-transcripts, GUIDE_DRIFT_BASE_REF=origin/main); spot checks 13+17+60+5+33 (3 skip) plus global-config guide 3/3.
- Guide-Coverage-Skip: US-633 tokenizer and Compose export quoting are internal serializer/redaction fixes; owned guides already cover secret redaction and Landofile Compose dispositions, not export YAML quoting.
---
## 2026-09-17 20:03 - Drift Audit
- [looper] gate skipped Drift Audit: gate: script exited with code 1
---

## 2026-09-17 20:16 - US-633 Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev, and proved the story against the binary: `lando config get appLabels`/`appEnv` redact dotted, hyphenated, and underscored secrets while keeping `com.example.team`/`TEAM` visible; a compose alpine app started via the provider API (no compose.yml written), `exec` printed `us633-ok` and applied `NODE_ENV=development`; `app:config` redacts service `DB_PASSWORD` and, after the verify fix, dotted/hyphenated service labels. Adjacent lint/info/apps:list stayed green; app destroyed.
- Fixed: `collectLandofileRedactionTokens` now walks first-class service `labels` so `lando app:config` redacts dotted/hyphenated label secrets (plan collector already did).
- **Out-of-scope finding:** setup warned ports 80/443 in use and fell back to 8000/4443. Pre-existing host/WSL bind, not US-633.
- **Learnings for future iterations:** `app:config` uses the landofile token collector, not the plan collector. Walking `extensions.compose.labels` on the plan does not redact authored `services.<name>.labels` in config output.
---

## 2026-09-18 02:45 - Babysit
- PR: #1009 https://github.com/AaronFeledy/core4/pull/1009
- CI outcome: pass (after fix; `guide-scenarios-windows-arm64` hung with zero output and was cancelled at the 30m cap, then passed on rerun of the identical commit — runner flake, not a code defect)
- Review feedback: fixed — Windows provider-integration failed on a mixed-separator Compose export path; replaced the hand-rolled slash-joining helper in `container-runtime/src/podman/compose.ts` with native `node:path.join` and tightened the shared exact-path tests. Bugbot reported no bugs.
- Merge: merged (squash)
---
