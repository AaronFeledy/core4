# Config translation progress

## Codebase Patterns
- Spec-class stories edit `spec/01..19-*.md` + `spec/README.md`; cross-refs use `§N.N` numbers (README topic lookup maps `§N` to file). Core spec text must state durable detail itself and never cite story ids or the PRD subdirectories.
- Gates for spec-only work: `bun run spec/config-translation/check-coordinated-plan.mjs` (omit `--initial` once any story passes), `bun run check:boundaries`, `bun run lint`, plus a grep scan for forbidden tokens (`ConfigFormat`, `LANDO_MOUNT`, `!lando3`, `LANDO_INFO`, `US-6xx`) and no new em-dashes in added lines.
- The plan checker only scans the three PRD directories for stale story refs and forbidden strings; the core spec files are covered by the grep scan, not the script.
- Parallel spec editing works well when partitioned by file (no two editors on one file); README topic rows go last because they cite final headings.
- Frozen naming contract for later stories: translators `lando4` / `recipe` / `lando3` (decode-only); inputs `landofile-document-set` / `recipe-request`; diagnostics `generated | dropped | rewritten | unsupported | non-portable | needs-review`; transaction states `prepared | committing | committed | blocked`; artifacts `<file>.bak.<full-before-sha256>` and `<target>.lando-stage.<transaction-id>`; secret dispositions `secret-store` or one `postInit.stdin` / `postInit.secretEnv.<name>` sink; commands `app:config:translate --to`, `app:config:explain`, `app:config:migrate`; events `pre-restart` / `post-restart`; doctor checks `lando3-leftovers` / `lando3-shadow`; binary `lando4` / `lando4.exe` during Alpha/Beta.
- Managed-file transactions keep the StateStore journal bucket unlocked (`lock: "none"`) while one scoped canonical-root lock covers every journal transition and filesystem mutation; transaction locks must not expire a valid live owner, but old malformed owner-owned locks remain reclaimable.

## 2026-09-07 01:22 - US-608C
- Added the private managed-file transaction coordinator with canonical-root locking, complete ordered journals, symlink and containment checks, raw-byte digests, immutable full-digest backups, exclusive owner-only stages, exact output modes, and write/removal commits through durable `prepared` → `committing` → `committed` transitions.
- Strengthened StateStore durability with parent-directory fsync after atomic rename and hardened advisory locks so transaction locks preserve live owners while reclaiming old malformed owner-owned records safely.
- Verification: typecheck, lint, and all boundary gates passed; 67 focused transaction/StateStore tests passed with one root-only ownership test skipped, 11 adjacent managed-file tests passed, LSP diagnostics were clean, and a real-disk driver confirmed written targets, mode `0600`, immutable backup retention, stage cleanup, and journal removal.
- **Learnings for future iterations:**
  - Hold one external lock around a `lock: "none"` transaction journal bucket; nesting the bucket's advisory lock with the same coarse lock self-contends.
  - Revalidate every backup globally before entering `committing` and again immediately before its target mutation; preparation alone does not protect immutable artifacts from noncooperative edits.
  - Physical stage files remain owner-only while the journal records the intended final target mode; target and parent-directory fsyncs establish the durable ordering recovery relies on.
---

## 2026-09-06 14:55 - US-607
- Amended the durable core spec so it owns the config-translation, IR-gap, and Lando 3 coexistence contracts: two-way `ConfigTranslator` with the authoring IR (`LandofileAuthoringShape` / `LandofileAuthoringFragment`), set-based inputs, encode round-trip law, six diagnostic kinds, lazy registration with collision failure, and target/write policy (§4.2, §7.4.1, §9.5, §13.1).
- Un-forbade `recipe:` and defined the inert object provenance form with mandatory producer agreement, the `recipe.<option>` expression scope, expression-aware tag-free `emitLandofileYaml` with a provenance comment block, and the recipe-as-decomposer model with declarative `migrations:` snapshots, the pure helper allowlist, secret dispositions, and post-commit-only auxiliary actions (§7.3.1, §7.4, §7.8.1, §8.8.x).
- Cataloged the managed-file transaction lifecycle and guard under §12.4.1 and the loader failure path (`Lando3LandofileDetected`, `LandofileDialectMixError`) under §7.1; seven-layer set conversion and recipe-layer folding under §7.2.
- Froze IR-gap semantics in their owning sections: restart brackets (§3.5/§11.4/§8.2), normalized tooling metadata and dynamic events (§8.5.1/§8.5.7), route shorthand and filters (§6.6/§10.2), planned build users (§6.2/§6.3/§6.13), default `home:` storage (§6.5), scanner union and router disablement (§7.5/§10.5), catalog options and the single shipped version matrix (§6.11.2/§6.12), scoped `rebuild`/`info --service` (§8.2), bounded `appEnv`/`appLabels` with reserved generated `LANDO_*` keys and `dev.lando.*` (§7.5/§6.9/§6.5); removed every `LANDO_INFO` mention and added no mount alias.
- Softened the v3 boundary (core runs nothing v3; the bundled `@lando/lando3` plugin translates on explicit request), added the bundle row, `lando4` side-by-side install/update/uninstall/shellenv ownership, bounded `DoctorCheckContext` ports, and the `lando3-leftovers` / `lando3-shadow` checks with `LegacyProxyContainerDetected` as single legacy-proxy owner (§1.x, §14.1, §10.9, §17.6.2, §17.7, §20.10.3); exclusions for upgrade/adoption, legacy state import, image manufacture, and emulation recorded as non-goals.
- Verification: plan checker PASS (42 stories), check:boundaries and lint passed; forbidden-token scan empty; no dangling `§` references; no new em-dashes. Typecheck ran as a no-code sanity gate.
- **Learnings for future iterations:**
  - Later implementation stories should cite the core sections above rather than the PRD directory; the core spec now states every contract they need.
  - The §6.3 artifact example now uses `run:` steps with optional `user:`; `instructions:` is gone from the spec and implementation should follow the `string | { run, user? }` union.
  - §20.10.3 no longer describes a `meta:setup --migrate-proxy` upgrade path; legacy proxy handling is diagnostic plus conflict error only.
---

## 2026-09-06 15:20 - US-607 Review
- Review-work failed on durable-contract contradictions; Bugbot found no bugs. Fixed: secret argv/file/default wording now rejects raw secrets and treats secret-store as resolve-only references; `meta:uninstall` matches side-by-side §17.7 ownership; `non-portable` keeps the lossless target-policy exception; conversion/init commands request BootstrapLevel `plugins`; include-level `dir:` no longer claims task-level `dir` is rejected; README added-line em-dashes removed; `node-ts` is not claimed as a current bundle member.
- Review notes (deferred/rejected): SecretWriter / owner-only backup creation / debug-argv full-value logging — not required by US-607 ACs and not newly introduced. 24-recipe catalog expansion — owned by US-609E staged growth. Appendix §15.C / §16.2 SDK export inventory — out of this story's amendment table (US-608A).
- Verification: coordinated-plan, check:boundaries, lint, and GUIDE_DRIFT_BASE_REF=origin/main check:guide-drift passed; forbidden-token and added-line em-dash scans empty. Typecheck not rerun (markdown-only). Guide-Coverage-Skip: specification-only durable core amendment with no runtime CLI or guide-covered source surface change.
- **Learnings for future iterations:** Command-table `minimal` is not translator-capable; `ConfigTranslatorRegistry` lives at `plugins`. When freezing IR-gap task/step `dir` precedence, do not leave include-level `dir:` claiming the task-level field is rejected.
---

## 2026-09-06 15:35 - US-607 Review
- Post-commit Bugbot: moved `LANDO_HOST_IP` into the conditional env catalog so the host-reachability omission rule is the one implementers follow.
- Review notes (deferred/rejected): none new.
- Verification: markdown-only follow-up; previous gates unchanged.
---

## 2026-09-06 15:45 - US-607 Review
- Post-commit Bugbot: restart payloads match start (not stop) payload shape.
- Review notes (deferred/rejected): none new.
- Verification: markdown-only follow-up.
---

## 2026-09-06 16:10 - US-607 Drift Audit
- Trigger: 4 cycles, 538 changed lines, 6 commits
- Findings by category (only categories with findings; one line each, outcome-focused)
- Contract drift: §3.2 still listed `apps:init` at `minimal` after conversion/init moved to BootstrapLevel `plugins`; §1.2 called the lando3 path two-way; §1.3 still described fragment application instead of authoring/transaction writes
- Fixed: conversion/init now appear on the `plugins` used-by list; lando3 conversion is named decode-only; the core-owns pipeline bullet names authoring validation, encoding, and managed-file writes
- Split/excise decisions: none
- Verification: typecheck, lint, check:boundaries, coordinated-plan, GUIDE_DRIFT_BASE_REF=origin/main check:guide-drift, forbidden-token and added-line em-dash scans passed
---

## 2026-09-06 17:30 - US-608A
- Added expression-aware authoring schemas to `@lando/sdk/schema`: `LandofileAuthoringShape` (complete) and `LandofileAuthoringFragment` (recursively partial) are derived at runtime by walking `LandofileShape.ast` with `SchemaAST` (`deriveAuthoringAst`), unioning a typed expression slot at every value site; `Wire` twins publish the encoded string-bearing form. Types come from generic mappers over the runtime Encoded type, not handwritten trees.
- Expression slots (`authoringExpressionSlot`, `AuthoringExpression`) parse with the SDK expression parser and check whole-vs-composite form (composite only at string sites), the closed scope list, known helper names (`EXPRESSION_HELPER_NAMES` now exported from `@lando/sdk/expressions`), and a static return-type inference; they never evaluate.
- Replaced the one-way `ConfigTranslator` port: schema-backed `ConfigTranslateInput` (`landofile-document-set` | `recipe-request`), bounded `ConfigTranslateDocument` snapshots, `ConfigTranslateDetectInput { documents }`, set outputs by target layer with wire fragments, six diagnostic kinds, deletion intents, optional `encode`, and explicit `never` requirements. `validateConfigTranslateInput` / `validateConfigTranslateResult` (`@lando/sdk/landofile`) enforce source ids by variant, unique allowlisted target layers, no deletions for recipe requests, diagnostic ordering, and full authoring-fragment decoding.
- Contract suite rewritten around the new harness with the encoder round-trip law (expression AST preserved) and cumulative-prefix/final-complete validation; coverage row stays `none-bundled`. `app:config:translate` now builds bounded document sets in core, validates merges with the authoring shape, reports deletions, and fails closed on `--write` with deletions.
- Verification: typecheck, lint, check:boundaries, codegen:schema-snapshot, check:schema-compatibility (one accepted pre-release finding for the translate command result), lint:guides passed; 216 spot-check tests across 13 files passed; CLI `app:config:translate --list` and the no-translator failure path verified.
- **Learnings for future iterations:**
  - Document roots currently derive as `Union(object, expression-slot)` like every other object site; making the root object-only breaks the public-field-description gate path (it resolves descriptions through the union branch) and the `Wire` schemas lose field descriptions under `encodedSchema`. Fixing that needs the annotation gate to follow the derived root TypeLiteral first; do it together with the `lando4` codec story.
  - Registering a schema also requires a fixture entry in `sdk/test/schema/public-schema-contracts.ts`; `AuthoringExpression` must publish through the expression-definition emission path or JSON Schema generation overflows the stack.
  - `expect<unknown>(...)` is the repo idiom when `toEqual` compares branded/readonly schema values against literals.
  - Deprecated helper aliases (`pathJoin`, ...) are not registered in the evaluator, so `EXPRESSION_HELPER_NAMES` does not include them yet.
---

## 2026-09-06 17:05 - Babysit
- PR: none — branch us-608a-authoring-translation-contracts not pushed to origin, no PR exists
- CI outcome: n/a
- Review feedback: none
- Merge: deferred (no PR to watch or merge; push step must create it)
---

## 2026-09-06 17:15 - US-608A
- Story verified complete on branch (no new code): both acceptance criteria hold against the committed authoring schemas, set-based `ConfigTranslator` port, validators, and rewritten contract suite.
- Verification: typecheck, lint, check:boundaries, check:schema-compatibility (27 compatible, 0 breaking, 1 accepted) passed; 1,095 spot-check tests across story and adjacent SDK schema, landofile, contract, and CLI files passed, including the `app:config:translate` command harness.
---

## 2026-09-06 17:20 - US-608A
- Story verified complete on branch (no new code); acceptance criteria already satisfied by the committed authoring schemas, set-based `ConfigTranslator` port, validators, and contract suite. Branch awaits the Push step (no PR yet).
- Verification: typecheck, lint, check:boundaries passed; 165 spot-check tests across 10 SDK schema, landofile, contract, exports, and CLI harness files passed.
---

## 2026-09-06 17:15 - Babysit
- PR: none (branch `us-608a-authoring-translation-contracts` not pushed to origin)
- CI outcome: not run (no PR)
- Review feedback: none
- Merge: deferred (no PR to watch or merge; push step must create it)
---

## 2026-09-06 17:25 - US-608A
- Story verified complete on branch (no new code); acceptance criteria already satisfied by the committed authoring schemas, set-based `ConfigTranslator` port, validators, and contract suite. Branch still awaits the Push step (no PR yet).
- Verification: typecheck, lint passed; 160 spot-check tests across 9 SDK schema, landofile, contract, exports, and CLI harness files passed.
---

## 2026-09-06 17:18 - Babysit
- PR: none (branch us-608a-authoring-translation-contracts not pushed to origin)
- CI outcome: n/a
- Review feedback: none
- Merge: deferred (no PR to watch or merge)
---

## 2026-09-06 19:25 - US-608A Review
- Review-work failed on path containment and helper typing; Bugbot found no bugs. Fixed: translation sources now realpath-check against the app root (autodetect skips escapes; `--file` fails closed), and `and`/`or`/`regexMatch` no longer pretend to be booleans.
- Review notes (deferred/rejected): Authoring derivation dropping object refinements/transforms — unioning expression slots makes naive predicate wrap reject expressions; recorded for the codec story. Document `layerId` as `LandofileLayer` — foreign source layers are not v4 layers. Required diagnostic remediation — optional matches tagged-error convention. Recipe writable-layer field — §1.1 recipe-request has none; outputs already use `LandofileLayer`. Fail-closed `{{ null }}` / `and(true,"x")` at boolean sites — conservative `unknown` is the documented static policy. Schema-level 1 MiB cap — core already bounds reads.
- Verification: typecheck, lint passed; GUIDE_DRIFT_BASE_REF=origin/main check:guide-drift passed; 120 spot-check tests across 7 authoring/translate files passed.
- **Learnings for future iterations:** Autodetect may skip escaped symlinks, but `--file` must fail on the same realpath check or explicit paths can still leak.
---

## 2026-09-06 19:35 - US-608A Review
- Post-commit Bugbot: annotated translate output comments deletion intents so preview YAML stays valid.
- Review notes (deferred/rejected): none new.
- Verification: lint passed; CLI translate harness passed.
---

## 2026-09-06 19:53 - US-608A Drift Audit
- Trigger: 6 cycles, 3490 changed lines, 8 commits
- Weakened proofs: detect/translate fixtures attributed no source ids; contract harness could skip the complete-shape law
- Ceremony: CLI helper splits kept — inlining would push the command past 250 LOC
- Scope absorption: CLI rewrite is required port wiring; US-609C already owns preview/write/encode orchestration
- Fixed: empty source identities now fail closed; fixtures attribute real snapshot ids; unused completeMerge escape removed
- Split/excise decisions: none; passes remains true
- Verification: typecheck pass, lint pass, 80 spot-check tests pass
---

## 2026-09-06 20:00 - US-608A Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev (exit 0), then a throwaway php:8.3 app: `app:config:translate --list` reports no translators; translate/detect/write fail closed with `ConfigTranslateNoTranslatorsError` and no file writes; start/info/curl/exec succeeded (HTTP 200 `us608a-ok`, PHP 8.3.33); adjacent `app:config` and `app:config:lint` stayed green. Destroyed the scenario app.
- **Out-of-scope finding:** `lando setup`/`lando start` warn that port 80 is in use (fallback 8080) and port 443 is held by leftover `lando-proxy-https` systemd-socket-proxyd (fallback 8443). Story AC not blocked; suspected prior-verify host-integration remnant plus host `rootlessport` on 8080/8443 via WSL mirrored networking.
---

## 2026-09-06 22:19 - Babysit
- PR: #916 https://github.com/AaronFeledy/core4/pull/916
- CI outcome: pass (after unit-test shards, test-isolation, and guide-scenarios-darwin failures on the earlier pushed SHA)
- Review feedback: fixed (verified and pushed the pending typecheck-split, deprecation-decode, and serializer-inventory commits; Bugbot clean)
- Merge: merged (squash)
---

## 2026-09-06 22:55 - US-608B
- Added `configTranslators` to both plugin seams: the manifest `PluginContribution` (new `ConfigTranslatorContribution` with required `id`/`module`/`inputKinds` and advisory `detects`/`optionsSchema`/`summary`/`deprecated`) and `LandoPluginModule` (a `ConfigTranslatorLoader` map of lazy `() => Promise<ConfigTranslatorShape>` factories, the same shape bundled plugins already use for commands, so generated bundling stays static-import-of-descriptors with lazy implementations).
- The module set now validates translator ids against the manifest, indexes loaders, and rejects duplicates with `ConfigTranslatorConflictError` naming both producing plugins (`makePluginCapabilityIndex` now fails with `PluginCapabilityIndexError`); no first-or-last wins.
- New `ConfigTranslatorRegistry` SDK tag with an engine Live installed in the `plugins` bootstrap tier over the contribution graph, so bundled, system, user, app, and host-injected plugins all flow through one candidate list. Candidates are read and loaders invoked only inside `list` (memoized), cross-source duplicates fail `list` with both producer names, and a loader returning a foreign id is a `PluginLoadError` attributed to its plugin.
- `app:config:translate` (native spec and legacy adapter) bootstraps at `plugins`, resolves translators from the registry when none are injected, and is exported from `@lando/core/cli/operations` so library callers share the same operation and tier.
- Verification: typecheck, lint, check:boundaries, codegen:check, check:schema-compatibility (2 compatible additive fields), check:public-transcripts passed; spot checks across SDK schema/compat, engine module-set/registry, core translate/lifecycle/manifest, cold-start (fast-path, opentui canary, import boundary), generated-layer, and contract-coverage files passed. Compiled `core/dist/lando` in lando4-dev: `app:config:translate --list` reports no installed translators (exit 0, JSON `{"mode":"list","translators":[]}`), translate/detect fail closed with `ConfigTranslateNoTranslatorsError`, and `--version` stays on the 12 ms cold path.
- **Learnings for future iterations:**
  - A plugins-tier Live that reads `PluginContributionGraph` through `Effect.serviceOption` must be `Layer.provide`d with the graph inside `renderPlugins()`; a bare `Layer.mergeAll` sibling sees `None`.
  - Widening `makePluginCapabilityIndex`'s error type only touched `validateResolvedPlugin`'s annotation; every other caller already treats the Left generically. Pre-existing module-set tests that read `PluginDescriptorMismatchError` fields need an `instanceof` narrow.
  - `/tmp` is not shared with the lando4-dev distro; put throwaway CLI apps under the gitignored `.local/` tree.
  - Bundled translators (`lando4`, `recipe`) should ship as `configTranslators` map entries wrapping `import()` in their plugin descriptor; the registry verifies the loaded `id` matches the contribution id.
---

## 2026-09-06 23:26 - US-608B Review
- Review-work failed on graph-level manifest/descriptor agreement; Bugbot found no bugs. Fixed: registry `list` now fail-closes with `PluginDescriptorMismatchError` when a plugin's `configTranslators` loader ids disagree with its manifest, before any factory runs.
- Review notes (deferred/rejected): Manifest-only dynamic import of `configTranslators[].module` — command-shaped lazy Maps are the AC loading path; undeclared or missing loaders now fail closed instead of inventing a CA-style module import. Full-suite uninstall timeouts — loop forbids `bun test` with no path and those failures are unrelated. Loading every factory on first `list` / structural runtime-shape checks — MINOR, not AC-blocking.
- Verification: typecheck, lint passed; GUIDE_DRIFT_BASE_REF=origin/main check:guide-drift passed; 74 spot-check tests across registry, module-set, translate, schema, and generated-layer files passed.
- **Learnings for future iterations:** Graph-level translator collection must apply the same exact-id check as the module set. Copying `commandLoaders` without declared-vs-provided validation lets undeclared loaders activate and declared ones vanish.
---

## 2026-09-06 23:42 - US-608B Drift Audit
- Trigger: 2 cycles, 799 changed lines, 8 commits
- Ceremony: candidate collector and its type were exported with no production importer
- Weakened proofs: new `ConfigTranslatorRegistry` tag and library `appConfigTranslate` export were not pinned on the existing public-export tests
- Fixed: unexported the unused collector; pinned both public seams on the sibling export tests
- Split/excise decisions: none; passes remains true
- Verification: typecheck pass, lint pass, 127 spot-check tests pass
---

## 2026-09-07 09:40 - US-608C
- Verified the committed managed-file transaction coordinator against both acceptance criteria; no implementation work remained. The private module in `@lando/managed-file` locks the realpath'd app root through the state-store advisory lock, rejects symlinked targets/parents and any parent outside that root, stages `<target>.lando-stage.<id>` beside each target, and reuses only hash-verified `<file>.bak.<full-before-sha256>` backups created exclusively at `0o600`.
- Confirmed the durability order first-hand: backups, then stages, then an owner-only fsynced `prepared` journal carrying the complete ordered before/after plan (presence, full digests, modes, backup identity, stage dev/ino), and only afterwards `committing`, per-file renames with digest rechecks, fsynced `committed`, stage cleanup, and a removal that fails unless the journal is already `committed`. Journals, tagged errors, and commit receipts carry digests, modes, and paths only, so no user bytes reach an output channel. No CLI, renderer, or prompt surface was added.
- Documented the coordinator in the root `AGENTS.md` package-ownership bullet so later stories consume the `@lando/managed-file/transaction` seam instead of hand-rolling staging.
- Verification: typecheck, lint, check:boundaries, coordinated-plan passed; 57 focused transaction tests (including crash-injection at every checkpoint, lock contention, symlink/hardlink attacks, and backup corruption) and 24 adjacent state-store lock/atomic tests passed. No CLI surface changed, so no CLI run applied.
- **Learnings for future iterations:**
  - The transaction journal is the one artifact allowed outside app-root containment: `journalDirectory` derives it from `makeLandoPaths({ userDataRoot }).managedFileLedger(...)`, keyed by app-root basename plus root digest, so two apps with the same basename never collide.
  - Journal writes are a state machine, not a blind set: `write` re-reads, requires the exact next state in `prepared -> committing -> committed`, and rejects any payload whose non-state fields drift from what was journaled. Recovery work in the next story must transition through it rather than writing terminal states directly.
  - `prepare` hands back an opaque handle and keeps the journal/store in a `WeakMap` lease, so `commit` cannot be replayed or driven from a foreign journal; the scoped finalizer deletes only stages whose recorded dev/ino still match, never foreign or unjournaled ones.
  - Cross-package consumers must import through the `@lando/managed-file/transaction` subpath (package-dag rule); the package stays `private: true`, and the module is deliberately absent from the package root export.
---

## 2026-09-07 09:49 - US-608C Review
- Reviewed the managed-file transaction coordinator against both US-608C ACs and in-scope §12.4.1 coordinator sentences. Bugbot found no bugs. No in-scope defects required a code change; prepare/commit durability, locking, containment, backups/stages, and committed-only cleanup already match the story.
- Review notes (deferred/rejected): FileSystem/`ManagedFileService` wrapping of coordinator IO — AC text does not require it; §12.4.1 forbids commands/translators bypassing the coordinator, and exclusive-open/fsync/O_NOFOLLOW primitives are not a FileSystem surface this story owns. Pathname TOCTOU / fd-relative mutation — §12.4.1 states cooperative locking is not CAS against noncooperative editors; recovery preflight is US-608D. Split-scope commit-without-lock — scope finalizer drops the lease, so commit fail-closes. Unreadable-lock EACCES takeover after stale mtime — intentional reclaim of crashed exclusive-create leftovers; live readable owners stay protected with `expireLiveOwner: false`. Windows owner-only DACL — matches existing state-store/managed-file 0o600 skip; not a new 608C contract. QA standard-invocation FAIL — empty worktree missing generated preload; main-tree focused tests passed.
- Verification: typecheck, lint, GUIDE_DRIFT_BASE_REF=origin/main check:guide-drift passed; 78 focused tests passed (1 root-only skip) across 12 transaction/lock/atomic/adjacent managed-file files.
- Guide-Coverage-Skip: private transaction coordinator adds no user-facing CLI or guide-covered public contract.
---

## 2026-09-07 09:56 - US-608C Drift Audit
- Trigger: 3 cycles, 1942 changed lines, 3 commits
- Weakened proofs: path-policy test claimed own-stage cleanup but failed in planning before stages existed; restored the claim to unjournaled foreign-stage preservation, which is the path it actually runs
- Ceremony: unexported unused PlannedEntry and file-local verifyPrivateFile
- Split/excise decisions: none; FileSystem wrapping re-examined and left deferred; package export stays unwired for US-609C; passes remains true
- Verification: typecheck pass, lint pass, 67 focused tests pass (1 root-only skip)
---

## 2026-09-07 10:07 - US-608C Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev (exit 0), then a throwaway php:8.3 app: start/info/curl/exec succeeded (HTTP 200 `us608c-ok`, PHP 8.3.33); `app:config` and `app:config:lint` stayed green; `app:config:translate --list` reports no translators and translate fails closed; help `--all` exposes no transaction or migrate UI. Destroyed the scenario app.
- **Out-of-scope finding:** `lando config:lint --format json` exits unknown-command (`cli:unknown-command`) while `lando help --all` lists `config:lint` and canonical `lando app:config:lint` succeeds. Story AC not blocked; suspected compiled-binary short-name dispatch, untouched by this diff.
---

## 2026-09-07 11:35 - US-608D
- Added journal recovery to `@lando/managed-file`: a new `blocked` terminal journal state, a preflight that classifies every entry against the complete recorded before/after plan (containment, symlink, link count, absence, digest, mode, immutable backup, and exclusive owner-only stage), and a roll-forward recovery that mutates only unapplied entries. A conflict marks the journal `blocked`, preserves every user file byte for byte, and demands manual resolution instead of overwriting forward or rolling back over an edit.
- Handled the crash window between a stage rename and its chmod as its own `applied-needs-mode` disposition, so a target that already carries the recorded after digest at the stage's owner-only mode gets its intended output mode completed rather than being misread as a conflict. Applied writes must additionally still be the renamed stage inode with no stage left behind; originally absent creations and removals classify from presence.
- Recovery of a `committed` journal performs cleanup only: it never inspects or restores targets or backups, removes stages only when identity *and* digest still prove ownership (`removeRecordedStage` replaces the identity-only removal used everywhere), and then removes the journal. Immutable backups stay until the user deletes them.
- Added the read-only dry run: it opens the journal without creating its directory, takes no lock, mutates nothing, and reports id, state, `recover | cleanup | manual-resolution`, and target paths only.
- Published the thin non-translator guard as an SDK tag (`ManagedFileTransactionGuard`) implemented in `@lando/managed-file`, consulted first by native multi-layer Landofile loading and as the first effect of the bound start path. `ManagedFileTransactionError` moved to the SDK so the tagged failure survives into the Landofile discover, user-load, and start error unions instead of being disguised as a parse failure; its TSDoc carries the durable cooperative-lock/not-CAS statement. The guard peeks before it locks, so an app with no journal costs two stats.
- Verification: typecheck, lint (incl. deprecations), and check:boundaries passed; 391 managed-file + landofile tests and 206 engine-operation + core managed-file tests passed, including a new 21-case recovery suite that went 0/21 red before the implementation. CLI verified end-to-end: with a real blocked journal planted for an app root, `lando app:config` exits 1 with `ManagedFileTransactionError` and its manual-resolution remediation while the concurrent edit survives untouched; the same command succeeds normally once no journal is pending.
- **Learnings for future iterations:**
  - Recovery emits its own checkpoint names (`recovering`, `after-recovery-mutation`, `recovered`). Reusing the commit-phase names makes a crash-injection fixture fire again during recovery and hides real behavior behind a fake second crash.
  - Roll forward, never back. The journal state machine is forward-only, backups plus stages make forward completion safe, and a rollback path would be a second mutation strategy with its own overwrite risk.
  - `verifyPrivateFile` must treat a missing artifact as a `conflict`, not let `ENOENT` escape as `io`; a deleted backup is a recovery decision, not a filesystem surprise.
  - The guard must peek with `openJournal(..., { createDirectory: false })` before locking. Creating the ledger directory on every native load would write to disk on the §12.5 hot path for every app that never ran a transaction.
  - An Effect layer only sees what is provided *to it*: the Landofile service layer takes the guard through `Layer.provide`, and `discover` stays `R = never` by closing over the resolved guard at layer construction.
---

## 2026-09-07 12:06 - US-608D Review
- Guarded production native loads: context-provided `ManagedFileTransactionGuard` is consulted when composition inputs omit it, single-file reads consult it before parse, and discovery recovers the cwd before walking to a parent Landofile. Windows recovery no longer treats POSIX-only `0o600` stage bits as a conflict.
- Review notes (deferred/rejected): Hold the canonical-root lock through layer parse — AC cooperative-lock/no-CAS and §12.4.1 do not require CAS across the read. Windows owner-only DACL — deferred in US-608C, not introduced here. Pathname TOCTOU on rename/chmod — spec says the cooperative lock is not CAS.
- Verification: typecheck, lint passed; GUIDE_DRIFT_BASE_REF=origin/main check:guide-drift passed; 29 focused tests passed (7 landofile guard, 21 recovery, 1 start guard).
- Guide-Coverage-Skip: internal transaction load guard with no new CLI or guide-covered public contract.
---
## 2026-09-07 12:10 - Drift Audit
- [looper] gate skipped Drift Audit: gate: script exited with code 1
---

## 2026-09-07 13:55 - US-609A
- Added the bundled `@lando/lando4` plugin: a leaf workspace package depending only on `@lando/sdk` that contributes `configTranslators: [lando4]` through both the manifest and a lazy literal-dynamic-import loader, so ordinary bootstrap, help, version, loading, and tooling paths never construct the codec.
- Implemented the two-way canonical v4 codec. Decode reads bounded raw bytes as UTF-8, parses with the dependency-free canonical parser, strict-decodes as an authoring fragment with excess properties rejected, and emits one output per source layer. Encode always validates the complete authoring context, then emits only the requested fragment through the canonical serializer with sorted keys, leaving expressions and secret references as verbatim source text. Nothing in either direction resolves environment, secret, file, provider, include, `.lando.ts`, or command values.
- Made every omission explicit instead of silent: a layer the request cannot write becomes a remediated `dropped` diagnostic, while a non-YAML document (TypeScript Landofiles included, never executed), a non-v4 layer id, two documents claiming one layer, and content that is not valid canonical v4 authoring data each become `unsupported`. Non-selected single-layer documents are context and produce neither output nor diagnostic. A recipe request fails closed.
- Made detection conservative and deterministic: it matches only when every YAML candidate strict-decodes as v4 authoring data, reports `exact` when the set carries the `runtime: 4` marker a Lando 3 document cannot have and `likely` otherwise, and returns one set-level match because core invokes a frontend once per ordered set.
- Flipped the ConfigTranslator plugin-abstraction coverage row from `none-bundled` to `built-in` behind a real core contract-suite invocation that resolves the translator through the plugin's own lazy loader, and recorded the plugin in the reference bundle table.
- Verification: typecheck, lint, check:boundaries, codegen:check (clean after commit), and check:schema-compatibility (0 breaking) passed; 44 spot-check tests across the codec, the core contract invocation, the coverage gate, the translator registry, and the plugin module set passed, plus 44 adjacent translate-command and module-set tests. CLI verified from source: `app:config:translate --list --format json` now reports the bundled `lando4` translator, and a real `app:config:translate --from lando4` on a throwaway app round-tripped a Landofile with `{{ env.PORT }}` intact and no provenance header.
- **Learnings for future iterations:**
  - The `contributes` block in `core/build.config.ts` is a fixed documentation summary with no `configTranslators` field; omit it for a translator plugin the way `@lando/sql` omits it rather than widening that type.
  - A new `plugins/*` package needs four registrations beyond its own files: the root `tsconfig.json` reference, `WORKSPACE_EDGE_TABLE` in `scripts/boundary/rules/package-dag-policy.ts`, the `core/build.config.ts` ship list, and `bun install` before workspace imports resolve.
  - `sdk/test/fixtures/bundled-plugin-manifests.json` is a committed codegen output keyed off the ship list, so `codegen:check` stays red until the regenerated fixture is committed with the plugin.
  - Contract-suite and detect fixtures must be built through `Schema.decodeUnknownSync(ConfigTranslate*Input)`; `ConfigTranslateDocument.bytes` decodes base64 to a `Uint8Array`, and handing the raw base64 string to a translator makes detection silently return no match.
  - The encoder is not yet on the CLI preview path. `app:config:translate` still emits merged outputs through `emitLandofileYaml` directly, so the sorted-key formatting this codec guarantees reaches users only when US-609C routes preview and write through the selected target encoder.
---

## 2026-09-07 14:20 - US-609A Review
- Review-work failed on diagnostic order, non-mapping encode context, and stale bundle inventories; Bugbot flagged conservative detect. Fixed: mixed dropped/unsupported diagnostics now sort by input-document order, encode fail-closes unless the complete context is a Landofile mapping even when emitting a fragment, and the bundled-plugin inventories count `@lando/lando4`.
- Review notes (deferred/rejected): Detect returning no match when any YAML candidate fails v4 decode — intentional set-level conservative detect; core passes Landofile snapshots, not arbitrary app-tree YAML. Authoring-root Union→TypeLiteral plus annotation-gate rewrite — US-608A warned that making the root object-only breaks the public-field-description gate; codec fail-closed on non-mapping context instead of changing the shipped schema. Diagnostic value leakage — translate redaction is owned by US-609C. Partial-prefix contract fixture — MINOR; the shared suite still runs prefix/complete laws. Duplicate translator-id constants — nitpick, importing translator.ts from index.ts would break lazy load.
- Verification: typecheck, lint passed; GUIDE_DRIFT_BASE_REF=origin/main check:guide-drift passed; 97 focused tests passed across codec, contract, bundled descriptor, generated-table, entry-points, plugin-registry, and packed-consumer files.
- Guide-Coverage-Skip: codec plugin has no owned guide; US-609C owns config-translate.mdx and CLI preview/write.
- **Learnings for future iterations:** Adding a bundled plugin requires updating every hardcoded ship-list inventory (`bundled.test.ts`, `generated-table.test.ts`, `plugin-registry.test.ts`, `bundled-plugin-loading.test.ts`, `entry-points-export.test.ts`), not only `build.config.ts` and codegen fixtures. `validateConfigTranslateResult` orders diagnostics by the full input document list, so two-pass translate must re-sort before return.
---
## 2026-09-07 14:14 - Drift Audit
- [looper] gate skipped Drift Audit: gate: script exited with code 1
---

## 2026-09-07 14:21 - US-609A Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev (exit 0), then a throwaway php:8.3 app: `app:config:translate --list` reports bundled `lando4`; `--detect` is `exact` with `runtime: 4`; `--from lando4` round-trips YAML with `{{ env.PORT }}` intact and no provenance header; non-YAML `index.php` is `unsupported` and never executed. Start/info/curl/exec succeeded (HTTPS 200 `us609a-ok`, PHP 8.3.33); adjacent `app:config` and `app:config:lint` stayed green. Destroyed the scenario app.
- **Out-of-scope finding:** `lando app:config` on a Landofile with `{{ env.PORT }}` exits 1 (`NotImplementedError`: template expressions not supported) while `app:config:lint` reports valid and `app:config:translate --from lando4` preserves the expression. Story AC not blocked; suspected unimplemented expression evaluation on the config load path.
- **Out-of-scope finding:** `app:config:translate` autodetect includes `index.php` from the app root and emits `unsupported`; `--file .lando.yml` is clean. Story AC not blocked; suspected CLI source discovery (US-609C orchestration).
---

## 2026-09-07 16:55 - US-609B
- Published the SDK recipe-decomposition contract: a schema-backed `RecipeDecomposer` port whose `decompose` takes already-merged nonsecret options plus approved secret references and returns an authoring fragment with inert provenance, with `R = never` and no app-root detection, provider action, write, post-init work, planning, or programmatic recipe execution. Factories close over an injected `Redactor` so a sink failure reports through central redaction without the SDK reaching for an ambient service.
- Split recipe identity into its own module: `RecipeSourceKind`/`RecipePackageName`/`RecipeContentDigest`/`RecipeProducer` plus family and versioned key helpers, so bundled and local producers sharing a package and recipe name can never collide. Added the object-form `recipe:` Landofile value (`LandofileRecipeProvenance`, `LandofileRecipeField`) with schema-enforced id/version agreement and an injective service map; `LandofileShape.recipe` widened from a bare id to that id-or-provenance union and the bare string stays valid and inert.
- Added declarative migration data: `RecipeSnapshot` (identity, serializable option descriptors, defaults, one serialized expression tree, asset digests), `RecipeMigration` edges, and the five hunk kinds with stable ids and the four classifications. `RecipeManifest` gained optional `snapshot`/`migrations`; secret prompts now must declare exactly one disposition (stored-secret reference field, or one named `postInit.stdin` / `postInit.secretEnv.<name>` sink) and may not default a raw value, and post-init actions gained those named sink bindings.
- Implemented the pure logic behind `@lando/sdk/recipes`: provenance validation, an options-only snapshot template walker over a closed 47-name pure-helper allowlist that rejects every IO helper and decoder, budgeted snapshot rendering that treats quoted authoring expressions in its output as data, chain validation covering twelve named failure reasons, option-descriptor checking, migratability verdicts, and secret-disposition/sink validation whose failure type carries structural identifiers only.
- Gave the canonical emitter one fixed named leading comment block for the provenance header, gave the expression evaluator an opt-in step/depth/output/collection budget and an `options` scope, and published `runRecipeDecomposerContractSuite` with a `none-bundled` coverage row (no bundled decomposer ships until recipe init integration).
- Verification: typecheck, lint, boundaries, codegen:check, and schema compatibility (0 breaking) passed; 2,376 focused tests across SDK schema/recipes/contract/library/errors/landofile/expressions and core contract/recipes files passed, plus core library and build suites. A real driver confirmed the emitted header text, an intact `{{ recipe.php }}` site, byte-exact parse round-trip, and fail-closed identity mismatch, forbidden `load()` helper, and chain gap.
- **Learnings for future iterations:**
  - Public JSON-schema fixture keys in `sdk/test/schema/public-schema-contracts.ts` must appear in the SAME ORDER as the registry keys; the coverage assertion compares arrays, not sets.
  - Embedding the recursive expression AST directly in a registered schema overflowed the deprecation-annotation traversal. That traversal now carries a visited guard, and a snapshot template is wrapped in a named carrier so the recursive grammar is referenced once instead of inlined.
  - The annotation gate resolves a union's shared discriminator only through direct struct members; nesting a sub-union as the first member hides the field. Flatten the union rather than adding an exemption.
  - Widening a `LandofileShape` field changes `anyOf` in every schema and command result that embeds it, so expect a batch of `unsupported-construct` findings needing recorded justifications in `sdk/compatibility-exceptions.json`.
  - `Schema.TaggedError.fields` includes `_tag`; assert it when pinning an exact field set.
  - Adding an export to `@lando/sdk/landofile` also needs the core seam list in `core/test/build/landofile-package-seam.test.ts` updated.
---

## 2026-09-07 17:25 - US-609B Review
- Review-work failed on digest derivation, exclusive secret sinks, and nested callbacks; Bugbot found no bugs. Fixed: canonical content-digest projection and hash, exclusive secret-store versus init-only sink ownership, recursive callable rejection, and migratability identity plus default-type checks.
- Review notes (deferred/rejected): ConfigTranslateSecretReference as an unrestricted string — pre-existing US-608A envelope with a trusted-caller precondition. Evaluator join/replace allocation and regexMatch CPU — general evaluator hardening, not this story's ACs. Optional contract mutationProbe — the side-effect law exists and is self-tested. classifyHunk rename path comparison — owned by US-611B analysis. Secret defaults in Effect ParseError trees — filter diagnostics are value-free and sink-failure redaction already holds. node:crypto hashing — existing SDK pattern.
- Verification: typecheck, lint passed; GUIDE_DRIFT_BASE_REF=origin/main check:guide-drift passed; 97 focused tests passed across digest, secret, migration, option, and export files.
- Guide-Coverage-Skip: SDK recipe contract helpers add no user-facing CLI or guide-covered public command surface.
- **Learnings for future iterations:** Digest projections must omit `identity.contentDigest` and migrations; `exactOptionalPropertyTypes` forbids assigning `undefined` into optional keys.
---

## 2026-09-07 21:56 - US-609B Drift Audit
- Trigger: 2 cycles, 3637 changed lines, 3 commits
- Findings: provenance-header test used a fake option map instead of object-form provenance and `{{ recipe.* }}` sites; versioned identity had no behavioral test; secret disposition re-decoded an already-typed union; classifyHunk (US-611B analysis) was on the public recipes barrel
- Fixed: restored object-form header plus intact expression-site round-trip; added digest-keyed identity agreement; removed dead secret re-validation; unexported classifyHunk from the public recipes barrel
- Split/excise decisions: none; classifyHunk stays in-module for US-611B; no passes flip
- Verification: typecheck, lint, 89 spot checks — pass
---
