Branch: docs/core-seams-prd
Source PRDs: spec/core-seams/prd-core-seams-*.md
Spec source of truth: spec/02,03,07,09,13,15 (§2.7, §3.1–§3.4, §7, §16.2, §13.8, §17.2) + ROADMAP (deferred-extraction supersession)
Origin: architecture review (2026-08-06) — @lando/core god package; app/services→cli layering inversion (core/src/app/operations.ts imports cli/commands/*); ~24 behavioral scanners simulate seams the package graph should carry

## Conventions

- Story IDs US-535..US-548; priority order is execution order.
- Internal/infra only — Guide Coverage None on every PRD.
- Do not touch unrelated dirty working-tree files from other waves.
- Behavior-preserving moves: no command semantics, event sequences, cache formats, or public entry-point changes.
- US-541 hoists into core-internal staging (engine package cannot host logic before US-542 moves the planner); US-542 lands with its codegen retarget as one change — expect the wave's largest diff there.

## Codebase Patterns

- Preserve superseded ROADMAP decisions as struck historical text followed by an explicit **Superseded** authority and the still-deferred remainder.
- Discover workspace manifests by expanding root `package.json#workspaces`; package-boundary rules must not maintain a second fixed package-root list.
- When a boundary rule has manifest and source halves, both must consume one exported policy table; a private source allowlist can silently reopen edges the manifest policy forbids.

## 2026-08-06 01:03 CDT - US-535
- Authorized the private Landofile and engine seams while preserving `@lando/core` as the single public runtime + CLI package; defined shell/runtime ownership, the complete workspace-DAG contract, and the superseded Phase 5 extraction deferral.
- Verification: typecheck, lint/deprecations, diff validation, direct acceptance inspection, and high-rigor review passed; no executable test surface changed. The optional spec-reference gate retains only pre-existing `.omo/**` violations outside this story.
- **Learnings for future iterations:**
  - Keep post-wave source-layout diagrams ownership-focused so later moves can preserve the shell/composition versus runtime seam without re-documenting every file.
---

## 2026-08-06 01:16 CDT - US-535 Review
- Corrected the private-seam contract so plugin dependencies remain limited to DAG-declared seams and runtime dependencies on `@lando/engine` or `@lando/core` stay forbidden.
- Review notes (deferred/rejected): rejected restoring the removed file-by-file source tree because US-535 requires the post-wave ownership split, not a fixed exhaustive layout; deferred the full edge-table implementation to US-536.
- Guide-Coverage-Skip: US-535 changes internal architecture contracts only; no user-visible guide surface changed.
- Verification: explicit-base guide drift, typecheck, lint/deprecations, diff validation, acceptance text checks, and post-fix Bugbot passed; no executable test surface changed.
---

## 2026-08-06 02:14 CDT - US-536
- Replaced plugin-only import scanning with one full-workspace runtime/dev dependency policy sourced from root workspace manifests; tagged failures now explain undeclared and forbidden runtime edges, and future Landofile/engine roots participate in cycle scanning.
- Verification: typecheck and lint/deprecations passed; package-DAG, import-cycle, workspace-root, and boundary-CLI spot checks passed with positive counts; both real boundary commands passed; high-rigor review approved. The full boundary runner retained only pre-existing `.omo/**` spec-reference violations outside this story.
- **Learnings for future iterations:**
  - Keep runtime and dev/test edge permissions distinct; plugin test-harness dependencies on core are legal without permitting runtime plugin-to-core edges.
  - Reserve future package roots in the shared all-package scan tier before scaffolding so import-cycle coverage activates as soon as their source trees appear.
---

## 2026-08-06 03:56 CDT - US-536 Review
- Restored the source-edge enforcement dropped by the manifest-DAG rewrite, moved manifest and source decisions onto one policy table, and closed plugin imports of the reserved `@lando/engine` seam before that package exists.
- Review notes (deferred/rejected): rejected canonical relative-import, dynamic-loader, alias, symlink, optional/peer dependency, and locator-value enforcement because those pre-existing gaps exceed US-536's manifest-edge acceptance criteria; the historical locator-alias gap remains explicitly deferred.
- Guide-Coverage-Skip: US-536 changes an internal CI boundary policy only; no user-visible command or guide-owned contract changed.
- Verification: explicit-base guide drift, typecheck, lint/deprecations, 38 focused and adjacent tests, package-DAG, and import-cycle passed; the full boundary runner retained only the pre-existing `.omo/**` spec-reference violations outside this story.
---

## 2026-08-06 04:51 CDT - US-537
- Added the private `@lando/landofile` workspace scaffold with isolated package scripts, exact sdk/paths/state-store dependencies, root project wiring, and synchronized package-DAG fixtures while leaving the public core Landofile implementation unchanged.
- Verification: typecheck and lint/deprecations passed; 20 focused and adjacent tests passed with positive counts; package build, package-DAG, import-cycle, and direct Bun package-resolution checks passed.
- **Learnings for future iterations:**
  - Keep the scaffold entry point empty until the domain move so package creation cannot accidentally change the existing `@lando/core/landofile` runtime surface.
  - Update synthetic workspace fixtures alongside root workspace membership; the real DAG gate and its fixture should describe the same package set.
---

## 2026-08-06 05:15 CDT - US-537 Review
- Added the Landofile package test to CI unit-shard discovery and locked the coverage with a regression assertion so the scaffold cannot pass PR checks without exercising its seam contract.
- Review notes (deferred/rejected): deferred release-package inventory and workflow wiring to US-538, when published core first consumes the private package; rejected a generated CI workflow edit because the existing workflow resolves shard contents dynamically from `scripts/test-shards.ts`.
- Guide-Coverage-Skip: US-537 changes private workspace and CI test plumbing only; no user-visible command, Landofile behavior, or guide-owned contract changed.
- Verification: explicit-base guide drift, typecheck, lint/deprecations, package build, 30 focused and adjacent tests, package-DAG, import-cycle, codegen drift, and direct shard discovery passed.
---

## 2026-08-06 10:31 CDT - US-538
- Moved Landofile discovery, parsing, merge, includes, lint, template, tooling, and version-constraint implementation into `@lando/landofile`; core now retains the public serializer shim and the Live layer that supplies host capabilities.
- Inverted cache, downloader, package-resolution, and environment reaches behind package ports; moved the unit suite with the domain and wired the private package into release packaging and generated workflows.
- Verification: 268 package tests, 86 adjacent tests, 106 public/bootstrap tests, 46 policy/release tests, 67 release tests, 16 codegen-CI tests, and 3 guide tests passed with positive counts; typecheck, lint/deprecations, build, package-DAG, import-cycle, schema compatibility, relevant boundary and guide gates, packed-manifest smoke, diagnostics, and post-commit `codegen:check` passed. The optional spec-reference gate retains only pre-existing `.omo/**` violations outside this story.
- **Learnings for future iterations:**
  - Remove source-path aliases when extracting a workspace package so production and test graphs resolve one canonical module identity; duplicate service tags can otherwise make valid Effect layers appear to lack their own services.
  - Keep host capabilities in the core Live layer and expose narrow pure ports from the extracted package, allowing package tests to supply deterministic implementations without recreating core services.
---

## 2026-08-06 11:49 CDT - US-538 Verify
- Compiled `core/dist/lando`, ran `setup --yes --no-interactive --skip-file-sync` in lando4-dev, and exercised app Landofile discovery/config/lint/validate, local includes update+verify (merged redis), version-constraint reject/accept, `.lando.local.yml` overlay, nested-cwd discovery, plus adjacent cache:refresh/doctor/version/meta:config/apps:list/config:translate --list — all green against the compiled binary.
- Committed Review leftovers as verify fixes: npm include port inversion completed in core, frozen-landofile version-constraint entries retained, package-seam checks hosted in core, release/docs references aligned. Bugbot found no bugs.
- **Learnings for future iterations:**
  - Real compiled-binary Landofiles must use shipped service shapes (`type: compose` + image); invalid MVP keys surface as `LandofileValidationError` and can look like a package-move regression.
  - Dev binary reports version `0.0.0`, so `lando: ">=4.0.0-alpha"` correctly fails with `LandofileVersionConstraintError`; use `>=0.0.0` for positive constraint smoke.
---
