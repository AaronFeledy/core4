Branch: feat/lando3-parity
Source PRDs: spec/lando3-parity/prd-lando3-parity-*.md
Spec source of truth: spec/06 (§6.12.1, §6.12.5), spec/07 (§7.5–§7.6, unchanged), spec/08 (§8.1, §8.5, §8.8.10, §8.8.15, §8.8.16), spec/11 (§10.7, §10.11), spec/16 (§18.4), ROADMAP Phase 5 concurrent-wave note
Origin: Lando 3 → 4 parity audit (.local/LANDO3-PARITY.md, 2026-08-09 snapshot) + plugin-parity scout; spec-coverage audit 2026-08-13

## Conventions

- Story IDs US-561..US-591; prd.json priority order is execution order (strict). Ordering guarantees no story depends on a later story.
- PRD-02 (keys), PRD-03 (services), PRD-04 (php) are mutually independent after US-561 and may run in parallel; PRD-05 requires PRD-04 + its own US-575 first; US-579 requires US-569 only for mssql coverage; US-580..581 closed the original wave; US-584..US-591 are the residual tranche (shipped-feature holes) and may run in parallel except US-585 before anything that assumes ServiceInfo.creds is populated.
- Feature wave, not infra: every user-visible surface ships its executable guide/README in the same story. Load `lando-write-docs` before authoring guides; guides are prose-first MDX with real `<Run>`/`<Verify>`.
- New service types must satisfy the full §6.12.1 per-service-type implementation checklist (resolution, contract suites, tooling, creds, schema publication) in the same change.
- SDK schema changes follow sdk/AGENTS.md: API_COMPATIBILITY.md where required + `bun run codegen:schema-snapshot`.
- Landofile un-gating removes keys from BETA_TOP_LEVEL_KEYS in landofile/src/service.ts one story at a time; never remove the whole gate in one story.
- Plugins may not depend on @lando/core; @lando/sql contributes through the generated composition root; all byte movement rides DataMover (§10.11); probe/paths/state-store boundary rules apply.
- Full-suite lock is always `bun test` (or explicit shard --run commands) with positive test counts. Never `bun run test` alone.
- After adding plugins/sql, run `bun install` and include the package in test shards.
- Run touched semantic gates per story: check:boundaries, check:guide-coverage, check:guide-drift, lint:guides, codegen:check when generated outputs move.
- Do not touch unrelated dirty working-tree files from other waves.
- Out of scope (do not implement): hoster connectors/RemoteSource impls, custom SSH-key mounts (sshAgent.sidecar:false stays rejected), staged §8.8.10 recipes (node-api, astro, sveltekit, nextjs, django, fastapi, rails, jekyll, hugo, eleventy, empty), image save/load.

## Codebase Patterns (carry forward)

- Official Tomcat empty ROOT returns HTTP 404; healthchecks must not use `curl -f`.
- Official `varnish:6` ignores `VARNISH_BACKEND_*` and needs generated VCL; `varnish:7` honors host+port env vars. Prefer one generated-VCL path for both majors.
- Official Varnish jails away from root even when the container user is root; catalog Varnish must exec `varnishd -F ... -j none` to bind :80 under rootless Podman.
- New catalog service types publish `@lando/sdk/schema/services/<type>` with `ServiceConfig.pick` plus a narrowed `type` literal; inherited picked fields need descriptions or schema-snapshot fails.
- `app:config` does not plan. Planner-derived deprecation warnings need `app:cache:refresh`; `lando doctor --deprecations` must scan the authored Landofile because doctor bootstraps at `none`.
- Service types live in plugins/service-lando/src/services/*.ts with _schema-helpers/_port-helpers/_volume-helpers; php build steps in php-prerequisites.ts (checksum-pinned downloads).
- Landofile service keys that are not Compose aliases must be added to `ServiceConfig` fields; the unknown-key allowlist is `Object.keys(ServiceConfig.fields)`, so a PHP-only schema does not make `composer:` authorable.
- The GHCR PHP base-image workflow matrix is hardcoded in `scripts/build-php-base-workflow.ts`, not derived from `SUPPORTED_PHP_VERSIONS`.
- Deferred commands: ids live on `LandoCommandSpec.deferred` plus `core/test/cli/fixtures/deferred-commands.json`; `deferred-commands.ts` is only `notImplementedErrorForSpec`. Remaining trio: `meta:events:follow`, `meta:plugin:login`, `meta:plugin:logout` (4.1).
- `recipes/` is the v4.0 shipped recipe set. `core/build.config.ts` `bundledRecipes` is a superset (staged ids plus `node-postgres`/`node-ts`); do not treat binary embed as the public table.
- Recipe manifests: recipes/<id>/recipe.yml + README.mdx (executable, feeds scaffold README generation); bundled registration via scripts/build-bundled-recipes.ts.
- Guide INDEX at docs/guides/INDEX.md; coverage enforced by check:guide-coverage.
- Planner-read file content outside Landofile expression provenance must contribute source/hash metadata to `deriveAppPlanCacheKey`; embedding parsed values in a service type's normalized config is not a reliable cache identity.
- Widening `AppPlanner.plan`'s error union requires adding the same tagged error to every App-handle and global-lifecycle error union that plans (`StartAppError`, `StopAppError`, `InfoAppError`, `ExecAppError`, `LogsAppError`, `LoadGlobalPlanError`, and the `meta:global:*` / `EnsureGlobalServices` unions).
- App-scoped command-routing policy belongs in the fresh app-command cache; native dispatch and help consume that cache without bootstrapping, while canonical command ids bypass alias policy.
- Dynamic and template-rendered Landofile command caches use file-based freshness only and serve the last full decode when non-file inputs change; do not fingerprint environment, host, or template-render inputs until a dedicated provenance contract authorizes them.
- Registry-backed services in generated bootstrap layers must receive registry entries through one-way injection; importing the built-in command registry back from a bootstrap service creates a command-spec/runtime-layer cycle.
- `ProviderCapabilities` fields added with `optionalWith` defaults become required on the decoded Type; every capability fixture must set the new field, and adding a property re-diffs sibling `allOf` optional structs in schema-compatibility.
- Cross-service catalog wiring that needs sibling discovery belongs in an `AppFeature` (`activatedBy` + `selectors`), not in `ServiceType.resolve`; resolve cannot see other services.
- nginx PHP FastCGI preset activates on `backend: <service>` and fastcgi_passes to that service on port 9000; omit `backend:` for stock static nginx.
- PHP `xdebug:` mode lives in `zz-lando-xdebug.ini`, not `XDEBUG_MODE` env, so `lando xdebug on|off` can reload Apache/FPM without a rebuild; env would win over the ini rewrite.
- An AppFeature with no selectors selects every service and will cycle with nginx PHP-FPM wiring; select PHP via `hasFeature` plus database `types:` when sibling discovery must not include nginx.
- File-backed Compose `configs:` hash into the app-plan cache like `env_file`. Bundled providers declare `composeProjectFields`/`composeServiceFields` `configs` and realize grants as read-only binds; `external: true` fails closed.
- Recipe `extends:` flattens before RecipeManifest validate. `flatten.ts` must not import `service.ts` (cycle). Bare `.` is not a local recipe path; use `./` or a `recipe.yml` path.
- Remote `extends` hops are YAML-only and jailed to the published parent root; `recipe.ts` is not executed from a remote parent tree.
- Bundled PHP recipes emit versioned database types (`mariadb:11.4`) that plan through `loadServiceTypeWithVersion`. `allowOverride` is rejected on `via: fpm`; nginx+FPM recipes must omit it and add an `edge` service with `backend:`.
- Laravel/symfony reuse php-stack like lamp; optional queue workers are a hand-written `via: cli` service, not a php-stack helper.
- A bundled child that `extends:` a parent still needs its own renderer registered under the child id. Flatten merges the manifest only; init looks up `lookupRecipeRenderer(manifest.id)` and never calls the parent renderer.
- Plugin-owned app commands that select among Landofile services should resolve the target from authored service types before `AppPlanner.plan`. Planner capability failures otherwise swallow tagged selection errors.
- Exec `AttachStdin` must be true when `stdinStream` is set, not only for `stdin: inherit`. After a finite stdin stream, half-close the write side (`socket.end()`) once headers are flushed; do not destroy the hijack or mysql hangs or never sees the dump.
- Native volume restore must stop the database container first. `RuntimeProvider.start/stop` are the per-service hooks; app CLI start/stop stay apply/destroy. Do not force `volumeSnapshot: copy` for InnoDB.
- `app:cache:refresh` has no top-level alias; teach `lando app:cache:refresh`. `--renderer=verbose` dumps every event payload and is not the OpenTUI task tree.
- DNS rebinding for custom loopback names is environmental. There is no Lando flag or doctor check; document remediations on the external-access guide.
- Topic-first space verbs rewrite to canonical ids in `normalizeCompiledCommandArgv`. Alias-token rewrites lose to `run.ts` early rawHead alias resolution of the bare topic.
- `LANDO_DB_*` is emitted with `ctx.addEnv` in the catalog type feature (priority 600). Never put reserved `LANDO_*` keys in `normalizedConfig.environment`; `lando.env` (700) rejects them and would otherwise overwrite resolved family env.
- Bundled providers persist applied plans under plugin state (`applied-plans/<appId>.json` or `applied-plans.json`). `apps:list` reads those paths. `resolvePlan` must reload from `PluginStateStore` after process restart; an in-memory Map is not enough. Do not attach `AppPlan` onto DataMover.
- App-plan cache identity includes `planningRuntime`: source-mode digest of engine planner/cache plus service-lando `src`, compiled-mode exec size+mtime. `CORE_VERSION` / plugin semver at `0.0.0` is not enough.
- Uninstall of a managed runtime must `podman unshare rm` `runtime/storage` while `runtime/bin/podman` still exists. Unsharing the whole `runtimeDir` first can delete the binary before subuid-owned volume files are gone.
- JSON/MCP envelopes redact env_file secrets only when the command spec supplies `redactionTokens`. Harvest with `collectAppPlanRedactionTokens` (planned service env, including env_file) or `collectLandofileRedactionTokens` (authored Landofile env/tooling env). Extractors must accept `unknown`.
- PHP × Composer incompatibilities are a duplicated table: plan-time `assertPhpComposerCompatible` in service-lando, recipe-prompt `composerChoicesForPhp` in core. Core cannot import the plugin. Keep the locked 8.5 / 2.7.7 sentence identical on both sides.

## 2026-08-14 .. 2026-08-21 — US-562..US-575 (condensed)

Original wave through recipe `extends:` shipped and merged: top-level `env_file`, `toolingDefaults`, `commandAliases`, events-as-tasks, the embedding built-in catalog (US-583), catalog types (rabbitmq/minio/localstack, mailpit/mailhog, tomcat/varnish, dotnet/mssql/phpmyadmin), PHP 8.5 + `composer:` / `via:` / `xdebug:` / `db_client:`, Compose `configs:` realization, and recipe `extends:`.

Adjudications that still constrain later work:
- Dynamic/templated Landofile command caches are file-fingerprint-only. Do not hash env, host, or template-render inputs (§7.1.1 / US-564).
- Landofile `events:` are resolved-Landofile data only. No service-type or runtime recipe event-contribution surface (US-565). Host steps run through Bun.$; no synthesized `sh -c`.
- Embedding self-registration of the full built-in catalog is US-583, not an events shim. No CLI-registry import through the default library entry.
- §6.11.3 tooling is two-rank (Landofile > service-type). The stale recipe-tooling middle rank was a spec error; US-582 owns plan-time reserved-name enforcement.

Verify findings that were out of those stories and are now residual work (US-584..US-591):
- `lando apps:list` empty while `lando info` shows the app running.
- Authored mysql `creds:` ignored; `lando mysql` tooling missing.
- `lando scratch list` parsed as `apps:scratch:start`.
- `lando uninstall --purge` EACCES on root-owned MariaDB volume files.
- Orphaned host `rootlessport` on `127.0.0.1:38080` blocks global/app start.
- Stale `plan.bin` survives a binary/plugin rebuild.
- JSON/MCP envelopes do not redact Landofile env-file secrets (events do).

Also fixed along the way (not residual): managed-runtime aardvark/user-bus on hosts without a systemd user session; official MinIO/Varnish/Xdebug image quirks; MySQL apt GPG key expiry.

## 2026-08-21 22:05 CDT - US-576

Drupal, Drupal CMS, and LAMP init now offer real stack choices (PHP 8.1–8.5, Composer, webroot, versioned databases; Drupal/CMS also Apache vs nginx+FPM). Drupal `composer: false` is rejected because scaffold still runs `create-project`. Verify started a non-default lamp and a Drupal nginx+postgres+8.5+Drupal 10 app; nginx served `/app/web`.

**Out-of-scope then, residual now (US-590):** `lando exec -- composer -V` on php:8.5 + composer 2.7.7 prints `E_STRICT` / React deprecations. Composer 2.7.7 predates PHP 8.5.

## 2026-08-21 23:20 CDT - US-577

Laravel and Symfony init offer PHP 8.1–8.5, Composer 2/2.7.7, versioned MariaDB or PostgreSQL, Redis, and `/app/public`. Laravel can add a `via: cli` queue worker. Upgrade the existing builtin stub in place; do not add a second `recipes/<id>/recipe.yml` runtime source.

## 2026-08-22 01:10 CDT - US-578

Backdrop and Joomla init as bundled lamp children (`extends: lamp`). MEAN is standalone Node + MongoDB with optional Redis. Review aligned Backdrop/MEAN settings with catalog `lando`/`lando` creds, collapsed Joomla CLI tooling into one `cmds` string, and dropped MEAN `command: npm start` so start uses the Node idle default.

- Tooling `cmds:` items are sequential steps, not argv. Multi-word CLIs belong in one string (`php artisan`, `php cli/joomla.php`).
- Bundled MariaDB/MySQL/MongoDB default to user/password `lando`/`lando` and database=`appName`.

## 2026-08-22 05:43 CDT - US-579

Bundled `@lando/sql` imports, exports, snapshots, restores, and resets creds-bearing database services. Family comes from the service type; a single database needs no `--service`; dump bytes ride DataMover with gzip on `.gz` paths.

Verify blocked twice, then fixed:
- Plugin commands were resolving DataMover against the bootstrap stub provider. Resolve the selected provider at call time.
- `stdinStream` is not `stdin: inherit`. Attach stdin for the stream, then half-close the write side after headers flush or mysql never sees the dump.
- Native volume restore onto a running mysqld is a no-op for InnoDB. `RuntimeProvider.start`/`stop` must bounce the target container; app CLI start/stop stay apply/destroy.

**Out-of-scope then, residual now (US-584):** `lando scratch list` is parsed as `apps:scratch:start` with `InvalidCliInvocationError`. `lando apps:scratch:list` works.

Rejected and still out: restore confirmation, live dump `<Run>`s under `provider: test`, attaching AppPlan onto DataMover.

## 2026-08-22 12:27 CDT - US-580

`lando app:cache:refresh` walkthrough, renderer-mode guidance (`lando`/`plain`/`json`/`verbose` + `LANDO_RENDERER`), and DNS-rebinding remediations on the external-access guide.

- There is no `lando cache:refresh` alias and no `--debug` flag.
- `output-streaming.mdx` still groups verbose with the task tree. Verbose is a payload dump (US-589).
- Guide fixtures resolve from `docs/guides/<frontmatter id>/fixtures/`, not the MDX directory.

## 2026-08-22 13:51 CDT - US-582

Service-type tooling that contributes reserved top-level names (`run`, `scratch`, `scratch:*`) now fails at plan time with `CommandAliasConflictError`. Landofile overlays still replace the contribution. Service-type `topLevelAlias:` enforcement waits for the 4.1 tooling schema graduation.

Keep reserved-name validation as a sibling of `compileEffectiveTooling`, not inside it.

## 2026-08-22 15:10 CDT - US-581

Parity audit refreshed: former functionality gaps are marked shipped. Hosters, sidecar-only SSH, staged recipes, plugin login/logout, and events:follow stay 4.1 or rejected. Shipped `recipes/` ids and catalog families are locked by tests. Deferred map remains the three 4.1 specs.

## 2026-08-22 16:25 CDT - Residual tranche (US-584..US-591)

PRD-08 added for holes in shipped functionality. `passes: false`, priorities 24–31.

- US-584 space-separated `scratch` / `recipes` / `share` verbs
- US-585 catalog connect contract: authored `creds:`, `LANDO_DB_*`, client tooling (including `redis-cli`), mysql/mariadb/postgres healthchecks, phpMyAdmin `hosts:` creds
- US-586 applied-plan inventory + docker `resolvePlan` reload
- US-587 plan-cache identity includes planning runtime
- US-588 uninstall purge of root-owned managed volumes
- US-589 docs (real command ids, verbose ≠ task tree) + JSON/MCP env-file redaction
- US-590 PHP × Composer compatibility — plan-time fail-closed + recipe prompt filter for `php:8.5` + `composer: "2.7.7"`; do not silently bump the exact pin
- US-591 doctor/start remediation for leftover 38080/TLS `rootlessport`

Still out: hosters, SSH `sidecar: false`, staged recipes, image save/load, 4.1 deferred commands, varnishadm (catalog row does not promise it), omitted-`creds:` default rewrite to `{{ service.name }}`.

## 2026-08-22 19:15 CDT - US-584
- Space-separated `scratch <verb>`, `recipes <verb>`, and `share <verb>` now rewrite to the canonical command. Bare `scratch` / `recipes` / `share` still start, list, and share. Colon aliases added for recipes describe/validate/list and share list/stop so all three forms share JSON command identity.
- Verification: typecheck, lint passed; relevant test spot checks passed (native-argv-parser, scratch-namespace, recipes-commands, aliases, share-skeleton). CLI smoke: `scratch list` and `recipes describe toolbox` report the canonical ids; `share list` is `app:share:list` not an unexpected-argument error; bare `scratch --detach` stays start; bare `recipes` still lists.
- **Learnings for future iterations:**
  - Topic-first space verbs must rewrite to canonical ids in `normalizeCompiledCommandArgv`. Rewriting to an alias token (`scratch:list`) loses to `run.ts` early `rawHead` alias resolution of the bare topic (`scratch` → start).
  - `normalizeCompiledCommandArgv` runs twice (before and after universal flag stripping) so `scratch --format json list` still becomes list.
---

## 2026-08-22 19:35 CDT - US-584 Review
- Review-work (5/5 PASS) and Bugbot (no bugs) confirmed space verbs rewrite to canonical ids; bare shortcuts and three-form identity hold. No in-scope code fix.
- Review notes (deferred/rejected): interleaved `--format` unit test — QA already proved `scratch --format json list` → `apps:scratch:list`; not a bug. §8.2 alias-table refresh — AC required the §8.4 taxonomy sentence only.
- Verification: guide-drift (`GUIDE_DRIFT_BASE_REF=origin/main`) passed with no guide edit; typecheck/lint/spot checks reused from Build (no source change).
- Guide-Coverage-Skip: US-584 is dispatcher routing only; PRD says existing pages stay unless a sentence becomes false, and colon/canonical forms remain true.
---

## 2026-08-22 19:46 CDT - US-584 Drift Audit
- Trigger: 3 cycles, 415 insertions / 76 deletions, 2 commits
- Re-examined Review deferrals: interleaved `--format` still QA-proven on the existing double-normalize path; §8.2 alias table still outside this AC.
- Fixed: none
- Split/excise decisions: none
- Verification: typecheck, lint, spot checks (native-argv-parser, aliases, recipes-commands, scratch-namespace, share-skeleton) — pass
---

## 2026-08-22 20:54 CDT - Babysit
- PR: #841 https://github.com/AaronFeledy/core4/pull/841
- CI outcome: pass
- Review feedback: fixed (regenerated command reference for space-separated verb aliases)
- Merge: merged (squash)
---

## 2026-08-22 22:15 CDT - US-585
- Authored `creds:` now provisions mysql, mariadb, postgres, mongodb, and mssql accounts (family env + `LANDO_DB_*`). Container `user:` is no longer a database username. Omitted creds keep existing defaults (`lando`/`lando`/appName + generated root; postgres generated password; mssql `sa` + `SA_PASSWORD`). Resolved creds land on `normalizedConfig.creds` and redacted `lando info` creds. Catalog client tooling ships (`mysql`, `mariadb`, `psql`, `mongosh`, `sqlcmd`, `redis-cli`) with secrets in task env. mysql/mariadb/postgres gained real healthchecks. phpMyAdmin `hosts:` uses matching sibling creds or fails closed instead of silent `lando`/`lando`.
- Verification: typecheck, lint passed; relevant test spot checks passed (creds-helpers, mysql, mariadb, postgres, mongodb, mssql, redis, phpmyadmin, catalog-service-configs, info-creds, info-no-secret, sql creds, php-db-client). Guide lint and coverage passed.
- **Learnings for future iterations:**
  - `LANDO_*` keys are reserved. `lando.env` (priority 700) rejects them in `normalizedConfig.environment`. Emit `LANDO_DB_*` only via `ctx.addEnv` in the type feature (priority 600). Write resolved family env (`MYSQL_*`, etc.) into `normalizedConfig.environment` so 700 cannot clobber authored `creds:` with stale env.
  - Keep `ServiceCreds` fields required. Partial means no `creds:` block plus family env / top-level `database:`.
  - phpMyAdmin `hosts:` that do not match a mysql/mariadb sibling need explicit `creds:` on the phpMyAdmin service (`PhpMyAdminHostsCredsError`).
---

## 2026-08-22 22:12 CDT - US-585 Review
- Review-work and Bugbot confirmed the connect contract; fixed postgres/mongodb `resolve()` omitting `normalizedConfig.creds` (AC for expressions/phpMyAdmin/@lando/sql) and mongosh `MONGO_URI` ignoring `service.port`.
- Review notes (deferred/rejected): mssql default `creds.user=lando` — AC keeps existing defaults; `sa`+`SA_PASSWORD` is the root login via `rootPassword`, not a rewrite of the app user. mongosh `--uri` on argv — AC compile is `--version`; password-on-argv is worse than env-only `MONGO_URI`.
- Verification: typecheck, lint passed; guide-drift (`GUIDE_DRIFT_BASE_REF=origin/main`) passed; spot checks postgres/mongodb passed.
---

## 2026-08-22 22:25 CDT - US-585 Drift Audit
- Trigger: 2 cycles, 1856 insertions / 370 deletions, 2 commits
- Scar tissue: mysql apply trusted a complete-looking authored creds object instead of the shared resolver
- Ceremony: unused phpMyAdmin host helper exports dropped
- Re-examined Review deferrals: mssql default user stays lando; mongosh URI stays env-only
- Fixed: mysql apply always resolves through the shared helper; dead host-helper exports removed
- Split/excise decisions: none
- Verification: typecheck, lint, spot checks (creds-helpers, mysql, mariadb, postgres, mongodb, mssql, redis, phpmyadmin, catalog-service-configs, info-creds, info-no-secret) — pass
---

## 2026-08-22 22:34 CDT - US-585 Verify
- Compiled `core/dist/lando`, ran `lando setup` in lando4-dev, started a mysql+postgres+redis+phpMyAdmin app with authored creds and a second mysql+redis app with omitted creds. Authored accounts provisioned (`appuser`/`pguser`), defaults stayed `lando`/`lando`/appName, `LANDO_DB_*` and family env matched, `lando mysql --version` / `psql --version` / `redis-cli ping` compiled, phpMyAdmin reused sibling creds (`PMA_USER=appuser`) and served HTTP 200, unknown `hosts:` failed closed. Adjacent `db:import --help` still works. Teardown destroyed both apps.
- **Out-of-scope finding:** `lando info` (plain TSV) prints `password=[redacted]]` / `rootPassword=[redacted]]` (extra `]`). JSON info is `[redacted]` and does not leak. Suspected renderer/TSV wrapping of the redaction sentinel, not the creds contract.
---

## 2026-08-23 01:28 CDT - Babysit
- PR: #844 https://github.com/AaronFeledy/core4/pull/844
- CI outcome: pass
- Review feedback: fixed (phpMyAdmin unmatched hosts stay fail-closed; leftover registration test now authors creds; accepted ServiceCreds $defs)
- Drift check: fixes pushed (restored fail-closed for unmatched phpMyAdmin hosts; dropped silent default creds)
- Merge: merged (squash)
---

## 2026-08-23 02:03 CDT - US-586
- After start, `apps:list` finds applied apps from the existing plugin-state inventory. `provider-docker` now persists sanitized plans to `plugins/@lando/provider-docker/applied-plans/<id>.json` and reloads them in `resolvePlan` after a new process, so exec / DataMover `serviceCmd` work without attaching `AppPlan` onto DataMover. Destroy forgets the file.
- Verification: typecheck, lint passed; relevant test spot checks passed (apps-list-discovery, docker applied-state, docker cross-process, service-lifecycle, bring-up-order, exec-stdin, plugin-descriptor).
- **Learnings for future iterations:**
  - `apps:list` already scans every `plugins/<id>/applied-plans/` directory. New providers must write there via `ctx.stateStore`, not invent a second inventory or the legacy `providers/*/apps` path.
  - CLI `exec` re-plans and passes `target.plan`, which hides a missing disk reload. DataMover calls `provider.exec({ app, service })` with no plan — that is the seam that proves cross-process reload.
---

## 2026-08-23 02:18 CDT - US-586 Review
- Docker `destroy` ignored `removeState`, so engine `lando stop` deleted the applied-plan file this story added. Now matches lando/podman: forget only when `removeState !== false`.
- Review notes (deferred/rejected): AC4 start→list e2e — focused seam tests are what the AC asked. Unused docker `appliedPlanStateDir`/`listAppliedPlans` — `apps:list` FS-scans the files; not a second inventory. Lando-style inspect-after-stop — file-exists lock is enough. Security LOW notes (identity sanitizer default, onCorrupt leaves file) match provider-lando.
- Verification: typecheck, lint passed; spot checks apps-list-discovery, docker applied-state, docker cross-process passed.
- Guide-Coverage-Skip: US-586 is applied-plan inventory/reload only; PRD says existing pages stay unless a sentence becomes false.
---

## 2026-08-23 02:35 CDT - US-586 Verify
- Compiled `core/dist/lando`, ran `lando setup` in lando4-dev, then started a default-provider nginx app and a `provider: docker` nginx app (Docker CE 29.7.2 staged for the system-runtime path). After start, `apps:list` / `--format=json` included both apps with matching `appRoot` while `info` reported running. Docker wrote `plugins/@lando/provider-docker/applied-plans/<id>.json`; a fresh `lando exec --service web -- hostname` succeeded (new process reload). Stop kept the docker file; destroy forgot it. Default-provider start/list/exec/stop/start also held. Adjacent `lando logs` worked. Teardown destroyed both apps; sandbox `--reset` after staging Docker.
- **Out-of-scope finding:** `curl https://web.us586-verify-docker.lndo.site:38443/` returned HTTP 502 while `info` said running and `exec` succeeded. Default-provider sibling returned HTTP 200. Suspected: docker app routed through lando-managed Traefik while another lando-provider app was also up.
- **Out-of-scope finding:** `lando destroy --yes` on the default `lando` provider left `plugins/@lando/provider-lando/applied-plans/us586-verify-lando.json`, so `apps:list` still listed the destroyed app after the project dir was gone. Docker destroy correctly forgot its file. Suspected: provider-lando destroy/removeState, not this story's docker persist path.
- **Learnings for future iterations:** Staging system Docker for a provider-docker story dirties the golden sandbox; `--reset` from the host afterward. `uidmap` leftover from prior managed-runtime setup already makes `--check` fail before Docker is added.
---

## 2026-08-23 03:43 CDT - US-587
- App-plan cache keys now include a non-secret planning-runtime fingerprint. Same Landofile + same fingerprint stays a hit; a rebuilt binary (exec size/mtime) or edited bundled planner/service-lando source misses stale `plan.bin`. Env, host, and template inputs stay out of the key.
- Verification: typecheck, lint passed; relevant test spot checks passed (app-plan-revision, planning-runtime, cache).
- **Learnings for future iterations:**
  - Default identity is content/stat, not plugin version. Workspace `CORE_VERSION` and bundled plugin semver stay `0.0.0`, so they cannot invalidate `plan.bin` after a local rebuild or service-type source edit.
---

## 2026-08-23 05:22 CDT - Babysit
- PR: #851 https://github.com/AaronFeledy/core4/pull/851
- CI outcome: pass
- Review feedback: fixed (dropped import.meta.url from planning-runtime so engine closure passes)
- Merge: merged (squash)
---

## 2026-08-23 06:20 CDT - US-588
- `lando uninstall --purge` now unshares `runtime/storage` before the rest of the managed runtime tree, so `bin/podman` is still there when root/subuid-owned volume files need `podman unshare rm`. If leftovers remain, uninstall fail-closes with `UninstallRuntimeDirError` naming the leftover path (preferring `storage/volumes`) and a remediation (`podman unshare rm` or `sudo rm -rf`, then rerun `--purge --yes`).
- Verification: typecheck, lint passed; relevant test spot checks passed (engine uninstall, leftover error unit, core uninstall + runtime-service). Surface: defaultRemoveRuntimeDir deleted a planted `storage/volumes/.../ibdata1` tree; `lando uninstall --help` still lists `--purge`.
- **Learnings for future iterations:**
  - Do not `unshare rm -rf` the entire `runtimeDir` first. Storage (volumes/overlay) must go while the managed podman binary still exists.
  - Host uninstall has no Effect error channel; tagged leftovers flatten into `step.error` as `message (path). remediation`.
---

## 2026-08-23 06:34 CDT - US-588 Review
- Review-work (5/5 PASS) and Bugbot (no bugs) confirmed storage-first unshare plus fail-closed `UninstallRuntimeDirError`. No in-scope code fix.
- Review notes (deferred/rejected): unquoted leftover path in remediation — Lando argv-spawns delete and does not execute the copy-run string; volume names are Lando-owned; quoting is polish. Weak `||` leftover assertion and double-await of one rejected promise — test smell, exact formatter is unit-tested. Real uid-0 `chown` fixture — host-altering; EACCES + fake unshare is the AC stand-in.
- Verification: guide-drift (`GUIDE_DRIFT_BASE_REF=origin/main`) passed with no guide edit; typecheck/lint/spot checks reused from Build (no source change). QA re-ran uninstall + leftover-error tests: 30 pass / 0 fail.
- Guide-Coverage-Skip: US-588 is an uninstall purge fix; PRD says existing pages stay unless a sentence becomes false, and published --purge prose still holds.
---

## 2026-08-23 06:56 CDT - US-588 Verify
- Compiled `core/dist/lando`, ran `lando setup` in lando4-dev, started nginx+mariadb. Live: HTTP 200, `exec` SELECT 1, `ibdata1` uid 100998 (host `rm` EACCES). First `--purge` fail-closed: bare `podman unshare` could not find bundled conmon. Fixed by passing `--config` + `CONTAINERS_CONF`. Re-setup, re-started the app, `--purge` deleted `runtime/storage/volumes` including the MariaDB tree. Adjacent `logs` and `--keep-data --dry-run` held.
- Blocker fixed: managed `podman unshare rm` now uses the runtime config so bundled conmon is found.
- **Out-of-scope finding:** after a successful purge, a short-lived `runtime/bin/podman` process can remain with a deleted exe. Uninstall already finished; not AC-blocking.
- **Learnings for future iterations:** Managed Podman needs `--config <runtime>/config` and `CONTAINERS_CONF=<runtime>/config/containers.conf`. `--config` alone does not load `containers.conf`.
---

## 2026-08-23 08:15 CDT - US-589
- README CLI sample now names `app:cache:refresh`, `app:includes:update`, and `app:includes:verify`. `output-streaming.mdx` treats `--renderer=verbose` as a payload dump, not the task tree. Secret-shaped values from planned service env (including env_file) and authored Landofile environment/tooling env are redacted in JSON command results and MCP envelopes via the existing `spec.redactionTokens` → `RedactionService` seam. `app:exec` attaches plan tokens; `app:config` harvests Landofile env maps.
- Verification: typecheck, lint passed; relevant test spot checks passed (env-file machine-output, MCP dispatch, renderer-boundary redaction, info-no-secret, events). Guide lint, coverage, and `dev:guides output-streaming.mdx --once` passed (1 test). Surface: `lando app:config --format=json` printed `PASSWORD: [redacted]`.
- **Learnings for future iterations:**
  - `LandoCommandSpec.redactionTokens` is `(result: unknown) => string[]`. Token extractors must accept `unknown` and narrow; do not type them against the result interface.
  - Do not put `redactionTokens` on public result schemas. Attach them off-schema on the value (tooling/exec) or harvest from `result.landofile` (config).
  - `output-streaming.mdx` lives under `docs/guides/tooling/`, not `docs/guides/cli/`.
---

## 2026-08-23 08:59 CDT - US-589 Review
- Review-work (5/5 PASS) and Bugbot (no bugs) confirmed README real command ids, verbose as a payload dump, and JSON/MCP env-file redaction via the existing `redactionTokens` seam. No in-scope code fix.
- Review notes (deferred/rejected): tests inject planned `environment` maps rather than real env_file I/O — same harvest events already use after assemble merges env files; not an AC miss. Empty-array `redactionTokens` skip of landofile harvest — production view/get always attach computed tokens. Duplicate token-filter helpers — existing pattern, not a leak.
- Verification: guide-drift (`GUIDE_DRIFT_BASE_REF=origin/main`) passed; typecheck/lint/spot checks reused from Build (no source change). QA re-ran env-file machine-output, MCP dispatch, events, info-no-secret, renderer-boundary redaction: all pass.
---

## 2026-08-23 11:09 CDT - Babysit
- PR: #853 https://github.com/AaronFeledy/core4/pull/853
- CI outcome: pass
- Review feedback: fixed (moved engine-owned redaction collector tests into engine to satisfy package-dag; restored core env-file/MCP tests to call execSpec.redactionTokens)
- Drift check: fixes pushed (restored env-file and MCP tests to call the production extractor instead of only asserting .toBeFunction())
- Merge: merged (squash)
---

## 2026-08-23 12:01 CDT - US-590
- Plan time fails closed for `php:8.5` + `composer: "2.7.7"` with remediation to `composer: "2"` (2.10.2). Channel `"2"` stays 2.10.2 on PHP 8.1–8.5; authored exact pins are never rewritten. Recipe prompts hide `2.7.7` once PHP is ≥ 8.5, and `--answer` of that pair is rejected; hand-written Landofiles still hit the plan-time gate. php-composer-workflows and drupal/drupal-cms/symfony READMEs no longer present the illegal pair.
- Verification: typecheck, lint passed; relevant test spot checks passed (php-prerequisites, php, php-composer-compat, php-stack, prompts runtime, init option-parity). Guide lint, coverage, and `dev:guides php-composer-workflows.mdx --once` passed (5 tests). Surface: `lando app:cache:refresh` on php:8.5 + 2.7.7 exits 1 with LandofileValidationError; php:8.3 + 2.7.7 still refreshes.
- **Learnings for future iterations:**
  - Keep `resolvePhpComposer` version-agnostic. Compatibility is a second assert on the authored pin, or `"2"` would inherit the 2.7.7 ban after resolving to 2.10.2.
  - `collectPrompts` still does not evaluate `when:`. Filter composer choices in the collect loop from already-resolved `php`.
---
