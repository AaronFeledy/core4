Branch: feat/lando3-parity
Source PRDs: spec/lando3-parity/prd-lando3-parity-*.md
Spec source of truth: spec/06 (§6.12.1, §6.12.5), spec/07 (§7.5–§7.6, unchanged), spec/08 (§8.1, §8.5, §8.8.10, §8.8.15, §8.8.16), spec/11 (§10.7, §10.11), spec/16 (§18.4), ROADMAP Phase 5 concurrent-wave note
Origin: Lando 3 → 4 parity audit (.local/LANDO3-PARITY.md, 2026-08-09 snapshot) + plugin-parity scout; spec-coverage audit 2026-08-13

## Conventions

- Story IDs US-561..US-581; prd.json priority order is execution order (strict). Ordering guarantees no story depends on a later story.
- PRD-02 (keys), PRD-03 (services), PRD-04 (php) are mutually independent after US-561 and may run in parallel; PRD-05 requires PRD-04 + its own US-575 first; US-579 requires US-569 only for mssql coverage; US-580..581 last.
- Feature wave, not infra: every user-visible surface ships its executable guide/README in the same story. Load `lando-write-docs` before authoring guides; guides are prose-first MDX with real `<Run>`/`<Verify>`.
- New service types must satisfy the full §6.12.1 per-service-type implementation checklist (resolution, contract suites, tooling, creds, schema publication) in the same change.
- SDK schema changes follow sdk/AGENTS.md: API_COMPATIBILITY.md where required + `bun run codegen:schema-snapshot`.
- Landofile un-gating removes keys from BETA_TOP_LEVEL_KEYS in landofile/src/service.ts one story at a time; never remove the whole gate in one story.
- Plugins may not depend on @lando/core; @lando/sql contributes through the generated composition root; all byte movement rides DataMover (§10.11); probe/paths/state-store boundary rules apply.
- Full-suite lock is always `bun test` (or explicit shard --run commands) with positive test counts. Never `bun run test` alone.
- After adding plugins/sql, run `bun install` and include the package in test shards.
- Run touched semantic gates per story: check:boundaries, check:guide-coverage, check:guide-drift, lint:guides, codegen:check when generated outputs move.
- Do not touch unrelated dirty working-tree files from other waves.
- Out of scope (do not implement): hoster connectors/RemoteSource impls, custom SSH-key mounts (sshAgent.sidecar:false stays rejected), staged §8.8.10 recipes (node-api, astro, sveltekit, nextjs, django, fastapi, rails, jekyll, hugo, eleventy, empty), image save/load.

## Codebase Patterns (carry forward)

- Service types live in plugins/service-lando/src/services/*.ts with _schema-helpers/_port-helpers/_volume-helpers; php build steps in php-prerequisites.ts (checksum-pinned downloads).
- Deferred commands: core/src/cli/deferred-commands.ts + per-spec `deferred:` blocks.
- Recipe manifests: recipes/<id>/recipe.yml + README.mdx (executable, feeds scaffold README generation); bundled registration via scripts/build-bundled-recipes.ts.
- Guide INDEX at docs/guides/INDEX.md; coverage enforced by check:guide-coverage.
- Planner-read file content outside Landofile expression provenance must contribute source/hash metadata to `deriveAppPlanCacheKey`; embedding parsed values in a service type's normalized config is not a reliable cache identity.
- App-scoped command-routing policy belongs in the fresh app-command cache; native dispatch and help consume that cache without bootstrapping, while canonical command ids bypass alias policy.

## 2026-08-14 10:04 CDT - US-562
- Top-level `env_file` now accepts scalar or ordered-list authoring, applies app-root-relative files to every service below service-level env files and direct environment values, fails closed with tagged remediation, and invalidates cached plans when file content changes.
- The env-overrides guide now exercises the host-safe planner refresh path and renders the shared-file declaration plus service override without claiming that `app:config` exposes planner-derived values.
- Verification: typecheck, lint, codegen, schema compatibility, boundaries, guide gates, focused planner/schema/Landofile/cache/redaction tests, and manual CLI spot checks passed.
- **Learnings for future iterations:**
  - `app:cache:refresh` is the host-safe CLI surface for planner-derived behavior; `app:config` reports the resolved authored Landofile and should only assert authored fields.
---

## 2026-08-14 10:43 CDT - US-562 Review
- Fixed the confirmed redaction blocker by registering secret-shaped resolved service-environment values with the standard tooling/build event redactors; bare env-file secrets are now masked in task-detail output while ordinary values remain visible. Restored persisted-cache regression coverage for the refactored service-level env-file path.
- Review notes (deferred/rejected): rejected the tooling-cache finding because dynamic tooling replans through the content-sensitive app-plan path; rejected redacting local per-step build transcripts or their TTY full-tail view because §6.13.6/§8.9.2 explicitly define that owner-only diagnostic surface as unredacted and excluded from telemetry, guide transcripts, and NDJSON; rejected extending the fix to tooling JSON/MCP results because machine envelopes are a separate §8.11 contract and the same pre-existing gap already applies to service-level env files; rejected a token guide assertion because the host-safe guide surface exposes authored config while focused planner tests prove derived environment precedence.
- Verification: typecheck, lint, boundaries, explicit-base guide drift, the focused env-overrides guide, and focused planner/Landofile/schema/tooling/build/redaction test files passed with positive counts.
- **Learnings for future iterations:**
  - Event publishers that can expose provider output must seed the canonical redactor from secret-shaped values in the resolved app plan; process environment and SecretStore values alone do not cover Landofile env files.
---

## 2026-08-14 11:10 CDT - US-562 Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev, started a two-service app with top-level `env_file`, and confirmed shared values, service-level overrides, missing-file fail-closed remediation, plan-cache refresh after env-file edits, and no secret leak in `info`/`logs`/`config`. Adjacent service-level `env_file` still applied after start. Destroyed both apps.
- **Out-of-scope finding:** `lando apps:list --format=json` returned `{"apps":[]}` while `lando info` in `/tmp/us562-verify` and `/tmp/us562-service-env` showed both apps running. Suspected apps-list/applied-app registry, not this story's planner/env_file path.
---

## 2026-08-14 17:18 CDT - Babysit
- PR: #739 https://github.com/AaronFeledy/core4/pull/739
- CI outcome: pass
- Review feedback: fixed (bumped stock PHP libpq-dev pin to 15.19 after Debian security's libpq5=15.19 broke the image build and Drupal reader-path e2e)
- Drift check: clean
- Merge: merged (squash)
---

## 2026-08-14 19:32 CDT - US-563
- App-wide tooling defaults now fill service, working directory, environment, and variables beneath authored, included, recipe-materialized, and service-type-contributed tasks. Same-tier service contributions resolve deterministically, and the contributing service is the default target.
- Effective tooling is attached privately to fresh and cached app plans, participates in cache identity, feeds command-index refresh, and drives runtime invocation without widening the public AppPlan contract. The executable guide shows inherited defaults and task-local overrides.
- Verification: typecheck, lint, boundaries, codegen drift, schema compatibility, guide coverage/drift/lint/public transcripts passed; 606 focused and adjacent tests plus the generated guide scenario passed; compiled CLI verification passed in lando4-dev and its app-specific caches were removed.
- **Learnings for future iterations:**
  - Resolve service-type tooling before applying app defaults; folding defaults into authored tasks earlier can accidentally outrank values contributed later.
  - Keep effective tooling as engine-private plan provenance until the compiled task graph needs its own persisted payload; public AppPlan remains provider-realization data.
---

## 2026-08-14 21:16 CDT - US-563 Review
- Fixed service-derived command-cache invalidation, fail-closed cache-miss discovery for service apps, locale-independent command compilation, and script fallback without changing effective-tooling precedence. Tooling env secrets now redact from task events, CLI plain/JSON results, and MCP envelopes, including numeric and boolean values after normalization.
- Review notes (deferred/rejected): rejected requiring effective tooling on arbitrary externally constructed AppPlan objects because the canonical fresh/cache-hit planner path owns and restores the private sidecar; rejected fingerprinting live linked-plugin source edits because managed plugin mutations invalidate app tooling caches and direct edits already require `app:cache:refresh`; rejected expanding the guide to assert every inherited field because the executable scenario proves applied/overridden defaults while focused compiler/runtime tests cover `dir`, `env`, and `vars`.
- Verification: typecheck, lint, boundaries, explicit-base guide drift, public transcripts, the focused executable guide, and 163 focused schema/Landofile/planner/cache/tooling/renderer/MCP tests passed.
- **Learnings for future iterations:**
  - Result-scoped redaction tokens must cross every imperative shell, including MCP progress and result envelopes; event-only redaction is not enough.
  - Script fallbacks must preserve the same effective-tooling-over-script precedence as command-index compilation, even when planning fails.
---

## 2026-08-14 21:48 CDT - US-563 Drift Audit
- Trigger: 2 cycles, 1811 changed lines, 9 commits
- Category 4: One internal planner type remained exported without a production or test consumer.
- Category 5: Retained the US-562 pass-state carry-forward because `origin/main` already contains the merged US-562 implementation.
- Category 6: The historical compiled-CLI claim was not rerun under this audit's host-safety constraints and remains unverified here.
- Fixed: Removed the dead type export; retained the package seams, fail-closed cache-miss path, and boundary-specific redaction after importer and call-path review.
- Split/excise decisions: None; every added public schema and effective-tooling surface traces to US-563 acceptance criteria.
- Verification: typecheck, lint, boundaries, codegen drift, schema compatibility, guide coverage/drift/lint/public transcripts, 256 changed-test spot checks, the focused 4-test post-fix rerun, and the generated guide scenario passed.
---

## 2026-08-14 23:42 CDT - Babysit
- PR: #741 https://github.com/AaronFeledy/core4/pull/741
- CI outcome: pass
- Review feedback: fixed (refresh tooling caches before start/restart/rebuild so planner-derived commands like drupal-scaffold exist after start; redact app-plan secrets from tooling results)
- Drift check: clean
- Merge: merged (squash)
---

## 2026-08-15 01:10 CDT - US-564
- Per-app `commandAliases` now remap or disable top-level aliases through the shared native dispatcher while preserving canonical command ids, canonical JSON identity, source/compiled parity, and active app-context help.
- Alias policy is normalized into the fresh app-command cache and its semantic fingerprint; canonical/reserved collisions and unknown targets fail with tagged remediation. The executable defaults-and-aliases guide now exercises a custom alias and its canonical form.
- Verification: typecheck, lint, boundaries, codegen drift, schema compatibility, guide coverage/drift/lint/public transcripts, 502 focused and adjacent tests, and both generated guide scenarios passed; automated source/compiled dispatch and relocated-binary CLI checks passed in `lando4-dev`, with QA resources removed.
- **Learnings for future iterations:**
  - Bare aliases handled by entry-point fast paths must flow through the shared dispatcher or app remapping and disablement are bypassed; keep fast paths only for reserved switches and canonical forms.
  - Validate reserved namespace heads without blocking explicit app remaps of already-registered colon aliases such as `scratch:gc`.
---

## 2026-08-15 11:30 CDT - US-564 Review
- Fixed import-time dispatcher crashes, refresh-time alias validation, app-aware preservation of level-none cold paths, target-aware Bun/x passthrough, pre-command alias failures, flag-shaped aliases, bounded fuzzy suggestions, terminal-control escaping, and load-expression provenance for fresh command caches. Added real compiled-binary coverage for app overrides.
- Review notes (deferred/rejected): rejected graduating tooling-task top-level aliases and executable plugin-command aliases because prior Beta decisions defer those surfaces; rejected global-config alias behavior because US-564 is explicitly per-app. Cursor Bugbot was unavailable because the Cursor account has an unpaid invoice.
- Verification: typecheck, lint, boundaries, explicit-base guide drift, the focused executable guide, 44 cache/router tests, and sandboxed source dispatch/cold-path checks passed. A host-built current compiled binary run inside `lando4-dev` resolved the custom `start` alias to canonical `app:greet` in its JSON envelope; the sandbox's Bun 1.3.14 test-runner path crashed before the compiled test body, so that runner invocation remains blocked by the runtime rather than deferred as product coverage.
- **Learnings for future iterations:**
  - Alias-sensitive level-none forms need an Effect-free app-context probe; importing Landofile discovery violates the cold-path boundary even when behavior is correct.
  - Fresh app-command caches must fingerprint Landofile `load()`/`import()` provenance whenever policy or tooling can be expression-derived.
---

## 2026-08-15 14:24 CDT - US-564 Drift Audit
- Trigger: 2 cycles, 1555 changed lines, 6 commits
- Category 2: Cache and help proofs were relocated without weakening, but the interrupted route-state simplification left resolver call sites uncompilable until the migration was completed.
- Category 3: Native dispatch retained a duplicate late tooling retry and carried argv inside already-resolved route values.
- Category 4: Internal alias-policy and tooling helpers remained exported beyond their production consumers; the pre-existing cache-writer size was retained after importer review.
- Category 6: Historical compiled-binary, focused-guide, and sandbox claims were not rerun under this audit's host-safety constraints and remain unverified here.
- Fixed: Removed the duplicate dispatch path and route-owned argv, narrowed internal exports, completed the resolver migration, and preserved relocated cache coverage in its focused suite.
- Split/excise decisions: None; every added public schema, error, cache, dispatcher, help, and guide surface traces to US-564 acceptance criteria.
- Verification: typecheck, lint, boundaries, codegen drift, guide coverage/drift/lint/public transcripts, and 186 changed-test spot checks passed; schema compatibility still fails on the unrelated pre-existing `HostPlatformFamily` removal while every US-564 addition is compatible.
---
