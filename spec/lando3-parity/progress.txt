Branch: feat/lando3-parity
Source PRDs: spec/lando3-parity/prd-lando3-parity-*.md
Spec source of truth: spec/06 (§6.12.1, §6.12.5), spec/07 (§7.5–§7.6, unchanged), spec/08 (§8.1, §8.5, §8.8.10, §8.8.15, §8.8.16), spec/11 (§10.7, §10.11), spec/16 (§18.4), ROADMAP Phase 5 concurrent-wave note
Origin: Lando 3 → 4 parity audit (.local/LANDO3-PARITY.md, 2026-08-09 snapshot) + plugin-parity scout; spec-coverage audit 2026-08-13

## Conventions

- Story IDs US-561..US-581; prd.json priority order is execution order (strict). Ordering guarantees no story depends on a later story.
- PRD-02 (keys), PRD-03 (services), PRD-04 (php) are mutually independent after US-561 and may run in parallel; PRD-05 requires PRD-04 + its own US-575 first; US-579 requires US-569 only for mssql coverage; US-580..581 last.
- Feature wave, not infra: every user-visible surface ships its executable guide/README in the same story. Load `lando-write-docs` before authoring guides; guides are prose-first MDX with real `<Run>`/`<Verify>`.
- New service types must satisfy the full §6.12.1 per-service-type implementation checklist (resolution, contract suites, tooling, creds, schema publication) in the same change.
- SDK schema changes follow sdk/AGENTS.md: API_COMPATIBILITY.md where required + `bun run codegen:schema-snapshot`.
- Landofile un-gating removes keys from BETA_TOP_LEVEL_KEYS in landofile/src/service.ts one story at a time; never remove the whole gate in one story.
- Plugins may not depend on @lando/core; @lando/sql contributes through the generated composition root; all byte movement rides DataMover (§10.11); probe/paths/state-store boundary rules apply.
- Full-suite lock is always `bun test` (or explicit shard --run commands) with positive test counts. Never `bun run test` alone.
- After adding plugins/sql, run `bun install` and include the package in test shards.
- Run touched semantic gates per story: check:boundaries, check:guide-coverage, check:guide-drift, lint:guides, codegen:check when generated outputs move.
- Do not touch unrelated dirty working-tree files from other waves.
- Out of scope (do not implement): hoster connectors/RemoteSource impls, custom SSH-key mounts (sshAgent.sidecar:false stays rejected), staged §8.8.10 recipes (node-api, astro, sveltekit, nextjs, django, fastapi, rails, jekyll, hugo, eleventy, empty), image save/load.

## Codebase Patterns (carry forward)

- New catalog service types publish `@lando/sdk/schema/services/<type>` with `ServiceConfig.pick` plus a narrowed `type` literal; inherited picked fields need descriptions or schema-snapshot fails.
- Service types live in plugins/service-lando/src/services/*.ts with _schema-helpers/_port-helpers/_volume-helpers; php build steps in php-prerequisites.ts (checksum-pinned downloads).
- Deferred commands: core/src/cli/deferred-commands.ts + per-spec `deferred:` blocks.
- Recipe manifests: recipes/<id>/recipe.yml + README.mdx (executable, feeds scaffold README generation); bundled registration via scripts/build-bundled-recipes.ts.
- Guide INDEX at docs/guides/INDEX.md; coverage enforced by check:guide-coverage.
- Planner-read file content outside Landofile expression provenance must contribute source/hash metadata to `deriveAppPlanCacheKey`; embedding parsed values in a service type's normalized config is not a reliable cache identity.
- App-scoped command-routing policy belongs in the fresh app-command cache; native dispatch and help consume that cache without bootstrapping, while canonical command ids bypass alias policy.
- Dynamic and template-rendered Landofile command caches use file-based freshness only and serve the last full decode when non-file inputs change; do not fingerprint environment, host, or template-render inputs until a dedicated provenance contract authorizes them.
- Registry-backed services in generated bootstrap layers must receive registry entries through one-way injection; importing the built-in command registry back from a bootstrap service creates a command-spec/runtime-layer cycle.

## 2026-08-14 10:04 CDT - US-562
- Top-level `env_file` now accepts scalar or ordered-list authoring, applies app-root-relative files to every service below service-level env files and direct environment values, fails closed with tagged remediation, and invalidates cached plans when file content changes.
- The env-overrides guide now exercises the host-safe planner refresh path and renders the shared-file declaration plus service override without claiming that `app:config` exposes planner-derived values.
- Verification: typecheck, lint, codegen, schema compatibility, boundaries, guide gates, focused planner/schema/Landofile/cache/redaction tests, and manual CLI spot checks passed.
- **Learnings for future iterations:**
  - `app:cache:refresh` is the host-safe CLI surface for planner-derived behavior; `app:config` reports the resolved authored Landofile and should only assert authored fields.
---

## 2026-08-14 10:43 CDT - US-562 Review
- Fixed the confirmed redaction blocker by registering secret-shaped resolved service-environment values with the standard tooling/build event redactors; bare env-file secrets are now masked in task-detail output while ordinary values remain visible. Restored persisted-cache regression coverage for the refactored service-level env-file path.
- Review notes (deferred/rejected): rejected the tooling-cache finding because dynamic tooling replans through the content-sensitive app-plan path; rejected redacting local per-step build transcripts or their TTY full-tail view because §6.13.6/§8.9.2 explicitly define that owner-only diagnostic surface as unredacted and excluded from telemetry, guide transcripts, and NDJSON; rejected extending the fix to tooling JSON/MCP results because machine envelopes are a separate §8.11 contract and the same pre-existing gap already applies to service-level env files; rejected a token guide assertion because the host-safe guide surface exposes authored config while focused planner tests prove derived environment precedence.
- Verification: typecheck, lint, boundaries, explicit-base guide drift, the focused env-overrides guide, and focused planner/Landofile/schema/tooling/build/redaction test files passed with positive counts.
- **Learnings for future iterations:**
  - Event publishers that can expose provider output must seed the canonical redactor from secret-shaped values in the resolved app plan; process environment and SecretStore values alone do not cover Landofile env files.
---

## 2026-08-14 11:10 CDT - US-562 Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev, started a two-service app with top-level `env_file`, and confirmed shared values, service-level overrides, missing-file fail-closed remediation, plan-cache refresh after env-file edits, and no secret leak in `info`/`logs`/`config`. Adjacent service-level `env_file` still applied after start. Destroyed both apps.
- **Out-of-scope finding:** `lando apps:list --format=json` returned `{"apps":[]}` while `lando info` in `/tmp/us562-verify` and `/tmp/us562-service-env` showed both apps running. Suspected apps-list/applied-app registry, not this story's planner/env_file path.
---

## 2026-08-14 17:18 CDT - Babysit
- PR: #739 https://github.com/AaronFeledy/core4/pull/739
- CI outcome: pass
- Review feedback: fixed (bumped stock PHP libpq-dev pin to 15.19 after Debian security's libpq5=15.19 broke the image build and Drupal reader-path e2e)
- Drift check: clean
- Merge: merged (squash)
---

## 2026-08-14 19:32 CDT - US-563
- App-wide tooling defaults now fill service, working directory, environment, and variables beneath authored, included, recipe-materialized, and service-type-contributed tasks. Same-tier service contributions resolve deterministically, and the contributing service is the default target.
- Effective tooling is attached privately to fresh and cached app plans, participates in cache identity, feeds command-index refresh, and drives runtime invocation without widening the public AppPlan contract. The executable guide shows inherited defaults and task-local overrides.
- Verification: typecheck, lint, boundaries, codegen drift, schema compatibility, guide coverage/drift/lint/public transcripts passed; 606 focused and adjacent tests plus the generated guide scenario passed; compiled CLI verification passed in lando4-dev and its app-specific caches were removed.
- **Learnings for future iterations:**
  - Resolve service-type tooling before applying app defaults; folding defaults into authored tasks earlier can accidentally outrank values contributed later.
  - Keep effective tooling as engine-private plan provenance until the compiled task graph needs its own persisted payload; public AppPlan remains provider-realization data.
---

## 2026-08-14 21:16 CDT - US-563 Review
- Fixed service-derived command-cache invalidation, fail-closed cache-miss discovery for service apps, locale-independent command compilation, and script fallback without changing effective-tooling precedence. Tooling env secrets now redact from task events, CLI plain/JSON results, and MCP envelopes, including numeric and boolean values after normalization.
- Review notes (deferred/rejected): rejected requiring effective tooling on arbitrary externally constructed AppPlan objects because the canonical fresh/cache-hit planner path owns and restores the private sidecar; rejected fingerprinting live linked-plugin source edits because managed plugin mutations invalidate app tooling caches and direct edits already require `app:cache:refresh`; rejected expanding the guide to assert every inherited field because the executable scenario proves applied/overridden defaults while focused compiler/runtime tests cover `dir`, `env`, and `vars`.
- Verification: typecheck, lint, boundaries, explicit-base guide drift, public transcripts, the focused executable guide, and 163 focused schema/Landofile/planner/cache/tooling/renderer/MCP tests passed.
- **Learnings for future iterations:**
  - Result-scoped redaction tokens must cross every imperative shell, including MCP progress and result envelopes; event-only redaction is not enough.
  - Script fallbacks must preserve the same effective-tooling-over-script precedence as command-index compilation, even when planning fails.
---

## 2026-08-14 21:48 CDT - US-563 Drift Audit
- Trigger: 2 cycles, 1811 changed lines, 9 commits
- Category 4: One internal planner type remained exported without a production or test consumer.
- Category 5: Retained the US-562 pass-state carry-forward because `origin/main` already contains the merged US-562 implementation.
- Category 6: The historical compiled-CLI claim was not rerun under this audit's host-safety constraints and remains unverified here.
- Fixed: Removed the dead type export; retained the package seams, fail-closed cache-miss path, and boundary-specific redaction after importer and call-path review.
- Split/excise decisions: None; every added public schema and effective-tooling surface traces to US-563 acceptance criteria.
- Verification: typecheck, lint, boundaries, codegen drift, schema compatibility, guide coverage/drift/lint/public transcripts, 256 changed-test spot checks, the focused 4-test post-fix rerun, and the generated guide scenario passed.
---

## 2026-08-14 23:42 CDT - Babysit
- PR: #741 https://github.com/AaronFeledy/core4/pull/741
- CI outcome: pass
- Review feedback: fixed (refresh tooling caches before start/restart/rebuild so planner-derived commands like drupal-scaffold exist after start; redact app-plan secrets from tooling results)
- Drift check: clean
- Merge: merged (squash)
---

## 2026-08-15 01:10 CDT - US-564
- Per-app `commandAliases` now remap or disable top-level aliases through the shared native dispatcher while preserving canonical command ids, canonical JSON identity, source/compiled parity, and active app-context help.
- Alias policy is normalized into the fresh app-command cache and its semantic fingerprint; canonical/reserved collisions and unknown targets fail with tagged remediation. The executable defaults-and-aliases guide now exercises a custom alias and its canonical form.
- Verification: typecheck, lint, boundaries, codegen drift, schema compatibility, guide coverage/drift/lint/public transcripts, 502 focused and adjacent tests, and both generated guide scenarios passed; automated source/compiled dispatch and relocated-binary CLI checks passed in `lando4-dev`, with QA resources removed.
- **Learnings for future iterations:**
  - Bare aliases handled by entry-point fast paths must flow through the shared dispatcher or app remapping and disablement are bypassed; keep fast paths only for reserved switches and canonical forms.
  - Validate reserved namespace heads without blocking explicit app remaps of already-registered colon aliases such as `scratch:gc`.
---

## 2026-08-15 11:30 CDT - US-564 Review
- Fixed import-time dispatcher crashes, refresh-time alias validation, app-aware preservation of level-none cold paths, target-aware Bun/x passthrough, pre-command alias failures, flag-shaped aliases, bounded fuzzy suggestions, terminal-control escaping, and load-expression provenance for fresh command caches. Added real compiled-binary coverage for app overrides.
- Review notes (deferred/rejected): rejected graduating tooling-task top-level aliases and executable plugin-command aliases because prior Beta decisions defer those surfaces; rejected global-config alias behavior because US-564 is explicitly per-app. Cursor Bugbot was unavailable because the Cursor account has an unpaid invoice.
- Verification: typecheck, lint, boundaries, explicit-base guide drift, the focused executable guide, 44 cache/router tests, and sandboxed source dispatch/cold-path checks passed. A host-built current compiled binary run inside `lando4-dev` resolved the custom `start` alias to canonical `app:greet` in its JSON envelope; the sandbox's Bun 1.3.14 test-runner path crashed before the compiled test body, so that runner invocation remains blocked by the runtime rather than deferred as product coverage.
- **Learnings for future iterations:**
  - Alias-sensitive level-none forms need an Effect-free app-context probe; importing Landofile discovery violates the cold-path boundary even when behavior is correct.
  - Fresh app-command caches must fingerprint Landofile `load()`/`import()` provenance whenever policy or tooling can be expression-derived.
---

## 2026-08-15 14:24 CDT - US-564 Drift Audit
- Trigger: 2 cycles, 1555 changed lines, 6 commits
- Category 2: Cache and help proofs were relocated without weakening, but the interrupted route-state simplification left resolver call sites uncompilable until the migration was completed.
- Category 3: Native dispatch retained a duplicate late tooling retry and carried argv inside already-resolved route values.
- Category 4: Internal alias-policy and tooling helpers remained exported beyond their production consumers; the pre-existing cache-writer size was retained after importer review.
- Category 6: Historical compiled-binary, focused-guide, and sandbox claims were not rerun under this audit's host-safety constraints and remain unverified here.
- Fixed: Removed the duplicate dispatch path and route-owned argv, narrowed internal exports, completed the resolver migration, and preserved relocated cache coverage in its focused suite.
- Split/excise decisions: None; every added public schema, error, cache, dispatcher, help, and guide surface traces to US-564 acceptance criteria.
- Verification: typecheck, lint, boundaries, codegen drift, guide coverage/drift/lint/public transcripts, and 186 changed-test spot checks passed; schema compatibility still fails on the unrelated pre-existing `HostPlatformFamily` removal while every US-564 addition is compatible.
---

## 2026-08-15 15:12 CDT - US-564 Drift Audit (re-run)
- Trigger: 2 cycles, 1736 changed lines, 12 commits
- Category 3: Native dispatch still probed the app-command cache twice for the same alias head after the first audit's routing simplification.
- Category 6: Historical compiled-binary and focused-guide claims were not rerun under host-safety constraints; guide drift skipped without pull-request context.
- Fixed: Reused the preflight alias resolution during dispatch, removing the duplicate cache probe without adding another routing path.
- Split/excise decisions: None; no deferred surface was absorbed and only US-564's pass state changed.
- Verification: typecheck, lint, boundaries, codegen drift, guide coverage/lint/public transcripts, 177 changed-test spot checks, and 58 post-fix routing checks passed; schema compatibility still fails only on the pre-existing `HostPlatformFamily` removal.
---

## 2026-08-15 15:30 CDT - US-564 Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev, started a node:lts app with `commandAliases.custom.hi: app:greet` and `disabled: [rebuild]`. `lando hi --format=json` and `lando app:greet --format=json` both reported `command: app:greet` and printed `hello-from-alias`; app-context help listed `hi -> app:greet` and omitted `rebuild`; `lando rebuild` failed with `UnknownCommandError`; `lando app:cache:refresh` on a reserved `app:*` alias failed with `CommandAliasConflictError`. Adjacent `info`/`exec`/`cache:refresh`/`version`/`app:config`/`destroy` worked. Destroyed the app and removed `/tmp/us564-*`.
- **Out-of-scope finding:** `lando start` first failed with `GlobalAutoStartError`, then `lando global:start` failed with `rootlessport listen tcp 127.0.0.1:38080: bind: address already in use`. The listener was a leftover host `rootlessport` (pid 1127198) from a deleted `/tmp/lando-start-scenario-*/test-start` cwd, not this story. Killing that orphan unblocked global+app start.
- **Out-of-scope finding:** `lando apps:list` still printed `No Lando apps applied on this host` while `lando info` showed `us564-verify` running. Same applied-app registry gap recorded in US-562 Verify.
- **Learnings for future iterations:**
  - Before sandbox `lando start`, check host `127.0.0.1:38080` for orphaned Lando `rootlessport` from deleted temp apps; WSL localhost sharing makes host leftovers look like in-sandbox binds.
---

## 2026-08-15 16:01 CDT - Push
- PR: none — skipped; uncommitted product changes in `core/test/testing/events.test.ts` (test case swap for `expectEvent` timeout options) plus uncommitted Verify progress entry. Resolve or commit those before push.
---

## 2026-08-15 16:02 CDT - Babysit
- PR: none
- CI outcome: fail (no PR; push skipped over uncommitted `core/test/testing/events.test.ts`)
- Review feedback: none
- Merge: deferred (missing PR)
---

## 2026-08-15 16:10 CDT - US-564 Build completion
- Story verified complete on the existing branch with no new product code; removed a temporary test-fixture mutation that inverted two equivalent timeout cases and did not belong to command-alias behavior.
- Verification: typecheck and lint passed; 97 focused command-alias, router, help, cache, and event-helper tests passed; compiled CLI acceptance verification remains recorded above.
- **Learnings for future iterations:**
  - Keep temporary test mutations out of the working tree after verification; equivalent fixture swaps can pass while leaving test names misleading and blocking publication.
---

## 2026-08-15 16:56 CDT - US-564 Review
- Fixed registered bare aliases such as `scratch` bypassing app remapping and disablement, and escaped terminal controls in unknown-target close-match remediation without changing the raw tagged-error data.
- Blocking: returned the story to Build because `.lando.ts` and templated Landofiles are accepted alias sources but the cache-only router unconditionally rejects their command caches, violating "Alias resolution works identically in source and compiled dispatch and on the tooling hot path (aliases resolve from the cached app plan without a full bootstrap)."
- Review notes (deferred/rejected): rejected fuzzy-match limits and bounded referenced-file hashing as broader local-project cache hardening outside the command-alias acceptance criteria; rejected weakening cache freshness by trusting dynamic source bytes because environment- or template-derived output would become stale. Cursor Bugbot remained unavailable because the Cursor account has an unpaid invoice.
- Guide-Coverage-Skip: The existing alias guide already documents registered-alias overrides; focused dispatch tests cover the `scratch` regression.
- Verification: typecheck, lint, explicit-base guide drift, and 255 focused alias, cache, router, help, schema, and Landofile tests passed with positive counts.
- **Learnings for future iterations:**
  - Cache-only command routing for dynamic Landofile forms needs a non-secret fingerprint of every permitted evaluation and render input; source-byte hashes alone cannot prove the resolved alias policy is fresh.
---

## 2026-08-15 17:18 CDT - US-564 Drift Audit
- Trigger: 5 cycles, 1816 changed lines, 21 commits
- Category 2: YAML-backed source, compiled, cache, JSON, and help proofs remain production-backed; dynamic and templated Landofile hot-path parity remains the recorded blocker.
- Category 3: A defensive fuzzy-match cutoff and its test blessed empty close-match remediation for long unknown targets.
- Category 6: Container-backed verification was not rerun under this audit's host-safety constraints and remains unverified here.
- Fixed: Deleted the fuzzy-match cutoff and restored the oversized-target proof to require close-match remediation.
- Split/excise decisions: None; public surfaces remain within US-564, and the story stays false for the recorded dynamic-Landofile cache boundary.
- Verification: typecheck, lint, boundaries, codegen drift, guide coverage/drift/lint, public transcripts, and 180 baseline plus 93 post-fix spot checks passed; schema compatibility still fails only on unrelated pre-existing schema removals and reference drift.
---

## 2026-08-15 18:18 CDT - US-564 Build completion
- Dynamic and templated Landofile command caches now carry a non-secret fingerprint of their permitted runtime inputs, allowing cache-only alias routing without a full bootstrap while invalidating when a real environment or render input changes. Static YAML and explicit `template: none` remain source-hash-only.
- Source and compiled dispatch now tolerate shell/Bun invocation-control differences while preserving canonical JSON command identity, alias disablement/remapping, and fail-closed refresh remediation.
- Verification: typecheck, lint, boundaries, codegen drift, guide coverage/drift/lint/public transcripts, the focused executable guide, 101 focused cache/router tests, 19 compiled-binary tests, compiled build, and persistent-sandbox source/compiled/template CLI checks passed; reviewer gate approved and QA resources were removed.
- **Learnings for future iterations:**
  - Hash dynamic configuration inputs at the cache boundary, but distinguish semantic user inputs from wrapper controls such as `_`, `SHLVL`, and `BUN_BE_BUN`; command substitution and source-mode self-exec legitimately change those controls.
---

## 2026-08-15 19:10 CDT - US-564 Adjudication (human)
- Contradiction: Review demanded "Alias resolution works identically in source and compiled dispatch and on the tooling hot path (aliases resolve from the cached app plan without a full bootstrap)" for every accepted Landofile form, including `.lando.ts` and template-rendered Landofiles whose output depends on environment/host/render inputs. §7.1.1's "only" clause (re-decoding only on file changes) and §8.7/§12.2's cache-only router rules exclude those non-file inputs as invalidation triggers, so Build's env/host/template-input fingerprints were unauthorized by the normative spec.
- Decision (option 3 of the .looper-stop brief, chosen by human adjudicator): §7.1.1 amended to make explicit that non-file evaluation inputs are NOT re-decoding triggers and caches MUST NOT fingerprint them; dynamic forms serve the last full decode and are excluded from cache-only freshness parity until a dedicated deterministic-evaluation/provenance spec story lands. US-564 AC 3 narrowed accordingly in prd.json and prd-lando3-parity-02-landofile-keys.md.
- Consequence: US-564 `passes` set to false; Build must excise the environment/host/template-input fingerprint machinery added in "feat: commandAliases" (dynamic-form caches stay file-fingerprint-only per §7.1.1) while keeping the proven static YAML source/compiled/hot-path alias behavior.
- Follow-up story: none in this wave; deterministic programmatic-Landofile evaluation and import provenance is deferred to a spec-owned story outside the Lando 3 parity PRD.
---

## 2026-08-15 20:50 CDT - US-564 Build completion
- Removed environment, host, and template-render input fingerprints from app-command cache payloads and freshness checks. Dynamic and template-rendered Landofiles now serve aliases from the last full decode, while declarative YAML, explicit `template: none`, referenced-file invalidation, canonical ids, JSON identity, help, and alias validation remain intact.
- Verification: typecheck, lint, boundaries, codegen drift, guide coverage/drift/lint/public transcripts, schema compatibility for every commandAliases addition, the focused executable guide, 160 focused and adjacent tests, compiled build, and persistent-sandbox compiled CLI verification passed; reviewer gate approved and QA resources were removed. The schema compatibility gate still reports only unrelated pre-existing surface removals and reference drift.
- **Learnings for future iterations:**
  - Cache-only routing may deliberately serve stale dynamic output when the contract defines only file-based invalidation; freshness inputs must come from an explicit provenance contract, not inferred host state.
---

## 2026-08-15 21:19 CDT - US-564 Review
- Reviewed the committed per-app command-alias work against the source acceptance criteria; no in-scope defect required a code change and the story keeps `passes: true`.
- Review notes (deferred/rejected): rejected Bugbot's finding that registered top-level aliases dispatch to their canonical built-in when the app command cache is missing or file-stale. The acceptance criterion scopes app alias resolution to a fresh cached app plan; an absent or stale cache carries no trusted alias policy, while stale-payload or bootstrap-time enforcement would invent an unowned freshness contract and break first-run command behavior. Rejected preserving pre-field app-command caches without `sourceReferencedFiles`; v4 is unreleased, the cache is versioned and rebuildable, and treating incomplete payloads as stale is the intended fail-closed behavior rather than a migration defect.
- Verification: typecheck, lint, explicit-base guide drift, and 87 focused dynamic-cache, command-registry, tooling-router, and app-alias tests passed with positive counts; the build step's broader recorded gates remain unchanged.
- **Learnings for future iterations:**
  - Cache-only alias routing falls back to the canonical built-in when no fresh app command cache exists; route any change to that behavior through a spec-owned story rather than a reviewer-authored regression contract.
---

## 2026-08-15 21:32 CDT - US-564 Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev, started a node:lts app with `commandAliases.custom.hi: app:greet` and `disabled: [rebuild]`. `lando hi --format=json` and `lando app:greet --format=json` both reported `command: app:greet` and printed `hello-from-alias`; app-context help listed `hi -> app:greet` and omitted `rebuild`; `lando rebuild` failed with `UnknownCommandError`; reserved `app:*` alias refresh failed with `CommandAliasConflictError`; unknown target failed with `CommandAliasTargetError`. Adjacent `info`/`exec`/`cache:refresh`/`version`/`app:config`/`scratch --help`/`destroy` worked. Destroyed the app and removed `/tmp/us564-*`.
- **Out-of-scope finding:** `lando apps:list --format=json` returned `{"apps":[]}` while `lando info` showed `us564-verify` running. Same applied-app registry gap recorded in US-562 and earlier US-564 Verify.
---

## 2026-08-16 02:59 CDT - US-565
- Landofile lifecycle events now run ordered shell, tooling-task, and canonical-command steps across start, stop, restart, rebuild, and destroy. User entries override deterministic service-type contributions; unsupported future step fields fail closed instead of being ignored.
- Pre-event failures abort with tagged step identity and redacted output tails, while post-event failures publish warnings after completed operations. Task detail output and event-step environment secrets use the canonical redaction path.
- The executable events guide covers lifecycle configuration, ordered stop hooks, and a hidden tagged failure path.
- Verification: typecheck, lint, codegen drift, boundaries, schema compatibility, guide lint/coverage/public transcripts, 41 focused lifecycle and contract tests, compiled build, and sandboxed compiled CLI verification passed. The aggregate package build reached every non-docs package but the docs package could not invoke missing local `astro`; dedicated guide gates passed.
- **Learnings for future iterations:**
  - Keep canonical-command event dispatch behind a one-way registry injection seam so generated runtime layers do not import the command registry through their own service graph.
  - `pre-start` runs before services exist; service-targeted work belongs at a later lifecycle point such as `post-start` or `pre-stop`.
---

## 2026-08-16 04:26 CDT - US-565 Review
- Returned the story to Build because the source criteria remain unmet. "`events` is removed from `BETA_TOP_LEVEL_KEYS`; schema accepts the §8.5 events map (event name → steps accepting the same step types as `cmds:`, incl. service targeting and canonical-command calls)" is incomplete for deferred/conditional/variable-bearing step forms. "Subscribers execute at the correct points in the §3 standard sequences (start/stop/restart/rebuild/destroy), in declaration order, through the tooling engine (provider exec default, host via ShellRunner) — no new execution path" is blocked by a direct canonical-command executor that bypasses shared command lifecycle semantics, is uninitialized for embedding runtimes, can deadlock through nested cwd resolution, and detects reentry only after preceding steps repeat. The failure/output and contribution-precedence criteria are also blocked by deferred-command defects, discarded `successExitCode`, object-shaped failures becoming `[object Object]`, named-task environment secrets missing from event redaction, and service contributions replacing rather than merging ordered steps. These surfaces are explicitly inside US-565; the progress log contains no prior Drift Audit excision for them, so this is not a gate conflict or contract gap.
- Review notes (deferred/rejected): rejected treating empty captured stdout/stderr from a successful canonical command as independently defective because command output belongs to the shared Renderer boundary; the real defects are bypassing that boundary and discarding its exit semantics. Rejected loosening the stale Beta-era test that still expects top-level `events:` rejection; Build must update it to assert the graduated contract instead.
- Verification: typecheck, lint, explicit-base guide drift, the 2-scenario executable events guide, and 13 lifecycle-focused scenario tests passed. A broader explicit-file spot check passed 96 host-safe tests but accidentally selected one env-enabled live-Podman case on the host; that case failed during start and was not rerun. Cursor Bugbot and all five review-work lanes failed the implementation on confirmed in-scope blockers.
- **Learnings for future iterations:**
  - Canonical event commands must reuse the shared command lifecycle/renderer seam and remain available to embedding runtimes without a reverse registry import cycle.
  - Event redaction must seed secrets from referenced effective tooling tasks, not only inline event-step environment values.
---

## 2026-08-16 05:16 CDT - US-565 Drift Audit
- Trigger: 2 cycles, 1572 changed lines, 3 commits
- Category 1: The public event schema partially freezes the larger canonical-command contract and still omits the full app lifecycle taxonomy; the story remains incomplete rather than absorbing the rest during audit.
- Category 2: Canonical-command and guide execution proofs remain weaker than the production wiring claimed, while a stale Beta test contradicted the graduated key.
- Category 3: Removed a test-facing forwarding export; the mutable command-registry injection, nested cwd deadlock, and late reentry guard remain Build blockers.
- Category 4: Event errors pushed the Landofile error module past the 250-line ceiling, and an internal command-executor result type remained exported without an external consumer.
- Category 6: Service contributions were discarded instead of merging below user steps, and plain-object failures plus named-task env redaction drifted from the tagged-output contract.
- Category 7: Re-examination confirmed the stale rejection test, contribution loss, object-shaped failure output, and named-task redaction as branch-owned fixes; empty successful canonical-command output remains correctly tied to the shared Renderer boundary.
- Fixed: Preserved all deterministically ordered contributed steps after user steps, restored typed failure messages and named-task secret redaction, corrected the graduated-key proof, guide precedence, and SDK compatibility note, split event errors by responsibility, and removed dead exports.
- Split/excise decisions: None; `passes` stays false for the unresolved canonical-command lifecycle/embedding/deadlock contract and missing `pre-init`/`post-init` execution.
- Verification: typecheck, lint, boundaries, codegen drift, schema compatibility, guide lint/coverage/drift/public transcripts, 183 focused tests, and both guide scenarios passed; container-backed verification was not run.
---

## 2026-08-16 12:55 CDT - US-565 Build completion
- Landofile `events:` steps now execute through a shared step compiler/runner kernel instead of a bespoke inline loop, so the full §8.5.2 step grammar works at lifecycle points: string, `cmd:`, `task:`, `command:`, `defer:` (LIFO finalization, including on failure), `for:` (list, variable, and matrix selectors), plus `if:`, `vars:`, `silent:`, and `ignoreError:`. Event expressions resolve `.event` from the decoded payload and `.item`/`.key` inside loops.
- Canonical `command:` steps now run through the shared command lifecycle seam rather than a private execution path, so they honor the target's `successExitCode`, renderer suppression under `silent:`, and interrupt propagation. Nested invocation resolves cwd explicitly instead of deadlocking beneath an outer resolved cwd, and lifecycle reentry is rejected at event entry before earlier steps can replay.
- `pre-init` and `post-init` now have real execution points: initialization runs exactly once per top-level lifecycle sequence, immediately after plan resolution and before the operation's own `pre-*` event, for start, stop, rebuild, destroy, and restart. `pre-init` fails closed; `post-init` warns. Read-only plan-resolving commands do not run event steps. Restart now resolves a single effective target and reuses it across its stop and start phases instead of resolving twice.
- Failure tails, task-detail progress, and event-bus publication stay redacted, with secret seeding reaching env on every step form.
- Verification: typecheck, lint, boundaries, codegen drift, guide coverage/lint/public transcripts passed; 117 focused and adjacent tests passed across the event kernel, init ordering, canonical executor, Landofile event validation, schema, effective tooling, and the start/stop/restart/rebuild scenarios. CLI check: a Landofile declaring `events.pre-boom` fails `lando app:config --format=json` with `LandofileUnknownEventError` listing all ten valid names.
- **Learnings for future iterations:**
  - The canonical-command executor must keep the one-way registry injection seam (`built-in-command-registry` pushes entries into the executor). Replacing it with a dynamic `import()` of the registry re-creates the generated-app-layer import cycle and fails `check:boundaries`, because the cycle rule counts dynamic imports too.
  - Broad multi-file `bun test` selections can pick up an env-enabled live-Podman case when a managed socket exists on the host; run focused sweeps under an isolated `LANDO_USER_DATA_ROOT` to keep them host-safe.
  - Event steps read off the resolved `AppPlan`, so `pre-init` cannot fire before planning; anchoring it to top-level lifecycle entrypoints (not the shared resolution helper) is what keeps it exactly-once and keeps read-only commands inert.
---

## 2026-08-16 15:39 CDT - US-565 Review
- Fixed failure-tail redaction across the truncation boundary, preserved authored failure identity, kept silent failures out of task-detail progress, redacted event-scoped secrets from ShellRunner lifecycle events, made resolved-cwd nesting fiber-reentrant, preserved canonical raw argv and result rendering, rejected deferred canonical commands with a tagged error, graduated event-step working directories, and corrected lifecycle-safe host examples.
- Review notes (deferred/rejected): rejected making bare event commands host-default because the contract requires provider exec by default and explicit `service: ":host"`; rejected republishing canonical-command stdout/stderr because the target Renderer already owns that output and duplicate capture was previously adjudicated; rejected pulling the executable CLI registry into the embedding entry because it violates the OCLIF-free library boundary and requires a separate native-registry extraction rather than a US-565 shim; rejected expanding root event programs to tooling-context-only selectors or plugin/tooling command registries because those broader surfaces are not owned by this story.
- Verification: typecheck, lint, boundaries, codegen drift, explicit-base guide drift, the two-scenario executable events guide, and 110 focused schema, Landofile, tooling-kernel, event/init, canonical-executor, ShellRunner, embedding-handle, and library import-boundary tests passed with positive counts.
- **Learnings for future iterations:**
  - Redact complete streams before taking a bounded failure tail; truncation can split an exact secret and defeat value-based replacement.
  - Process-wide cwd locking must track the owning Effect fiber so nested app commands can reuse the lock without allowing concurrent fibers through.
---

## 2026-08-16 17:29 CDT - US-565 Review
- Preserved the confirmed Renderer and ShellRunner redaction fixes, and changed the executable guide from config inspection to an actual start lifecycle run. Returned the story to Build because "`events` is removed from `BETA_TOP_LEVEL_KEYS`; schema accepts the §8.5 events map (event name → steps accepting the same step types as `cmds:`, incl. service targeting and canonical-command calls)" remains unmet: `command:` resolves only built-ins and validates only unknown keys instead of target-required fields, value types/options, plugin commands, and tooling commands. "Subscribers execute at the correct points in the §3 standard sequences (start/stop/restart/rebuild/destroy), in declaration order, through the tooling engine (provider exec default, host via ShellRunner) — no new execution path" is also unmet because host string steps now serialize `sh -c` into `ShellRunner`, requiring an external `sh` and failing on Windows/no-`sh` hosts.
- Review notes (deferred/rejected): rejected Bugbot's request to capture canonical-command stdout/stderr because the corrected executor now invokes the target render callback through Renderer while retaining empty capture fields; rejected the attempted textual POSIX-parameter expander because it lost empty/missing arguments, corrupted embedded `"$@"`, inherited predictable environment variables, and changed redirection semantics; rejected the packed-root import failure as unrelated to this branch. The prior Drift Audit recorded no split or excision for either remaining demanded surface, so both fixes remain inside US-565 rather than requiring adjudication.
- Verification: typecheck, lint, boundaries, explicit-base guide drift, guide lint/coverage/public transcripts, the two-scenario executable events guide, and 47 focused canonical-executor, ShellRunner, event-kernel, and init-event tests passed with positive counts.
- **Learnings for future iterations:**
  - Do not emulate POSIX positional parameters by textual substitution; preserve structured shell source and argv until the execution adapter can bind them without an external interpreter.
---

## 2026-08-16 17:42 CDT - US-565 Review
- Fixed the final Bugbot regression by self-providing `EventCommandExecutor` inside its captured runtime context, so a canonical event command may enter a different lifecycle event containing another `command:` step without losing nested execution or correlation. The recorded Build blockers and `passes: false` decision remain unchanged.
- Verification: the 15-test canonical executor file, 14-test adjacent event-kernel file, and focused `@lando/core` typecheck passed; changed-file diagnostics were clean.
---

## 2026-08-16 18:38 CDT - US-565 Adjudication
- Contradiction: US-565 AC 1 said the schema accepts "the §8.5 events map (same step types as cmds:, service targeting, canonical-command calls)," and §8.5.2/§8.5.2.1 requires `command:` to resolve "built-in, plugin-contributed, or tooling" canonical ids and validate `flags`, `args`, and `raw` against the target `LandoCommandSpec`. The 15:39 Review nevertheless rejected plugin/tooling command registries as outside US-565, while the 17:29 Review demanded that exact normative surface; the 05:16 Drift Audit had classified the larger canonical-command contract as incomplete rather than split or excised. Independently, AC 3 required host execution "through the tooling engine ... host via ShellRunner — no new execution path," while §3.4 requires `ShellRunnerBunLive` to use `Bun.$` with no system shell and §8.6 defines the host engine as cross-platform Bun Shell; the 17:29 Review found the current `sh -c` serialization violated that contract.
- Decision: clarified US-565 AC 1 in `prd.json` to name built-in, plugin-contributed, and tooling canonical-id resolution plus target-spec validation at the §8.5.2.1 compile/invocation boundaries. Clarified AC 3 to require the shared tooling graph/ToolingEngine path, providerExec for service targets, and `ShellRunnerBunLive`/`Bun.$` for `service: :host`, explicitly forbidding synthesized `sh -c`, external system shells, and argv re-encoding as a literal shell string.
- Follow-up story (if created): none; both disputed surfaces are already mandatory parts of the normative §8.5/§8.6 events-as-tasks contract.
---

## 2026-08-17 14:35 CDT - US-565 Build completion
- Canonical `command:` event steps now resolve built-in, plugin-contributed, and tooling canonical ids through one target resolver: plugin manifests gain lazy executable-command loaders keyed by canonical id, tooling targets route through the shared tooling invocation path, and unresolved targets fail with a tagged lookup error carrying close-match remediation. Literal `flags`, `args`, and `raw` values validate against the target command spec at compile time, and expression-resolved values re-validate at invocation.
- Host event steps and `:host` tooling tasks now execute through Bun Shell with structural argv binding: authored shell source and forwarded arguments stay separate end to end, legacy provider-form `sh -c` command arrays are decoded structurally instead of spawned, and unsupported positional references fail closed at compile with tagged remediation.
- Event step failures now surface their identity and underlying detail on the CLI failure report: the failure footer renders event, step number, kind, service, and the redacted output tail, and compile-time canonical-command failures preserve the cause message and remediation (for example the close-match suggestion or the allowed flag values) instead of a generic validate message.
- Verification: typecheck, lint, boundaries, codegen drift, guide lint/coverage/public transcripts, and the two-scenario executable events guide passed; 300+ focused event/tooling/executor/scenario/bug-report tests passed with positive counts. Compiled CLI verification in lando4-dev proved the destroy lifecycle running host steps, a canonical built-in command, and a tooling-target command in order with task-detail output, plus fail-closed unknown-event, unknown-target, and invalid-flag paths with actionable detail. Schema compatibility reports only the pre-existing main-side sshServices/ProviderSetupPlan drift from commits merged after this branch diverged.
- **Learnings for future iterations:**
  - Container-network-dependent verification is currently blocked in lando4-dev: the host session's `user@1000.service` occupies the shared WSL2 kernel cgroup path, so the sandbox user manager cannot start and rootless netavark/aardvark-dns cannot reach a user bus. `lando destroy` lifecycles exercise real event execution (host steps, canonical commands, ordering, failure paths) without container networking; use them when start-path verification is blocked.
  - Tagged error values carrying rich fields are not user-visible by themselves; `core/src/cli/bug-report.ts` per-tag extra-field extraction is where step identity and output tails become part of the CLI failure footer.
  - When wrapping a validation cause in a broader tagged error, fold the cause message and remediation into the wrapper message; downstream failure rendering only reads the outermost message.
---

## 2026-08-17 14:28 CDT - US-565 Adjudication
- Contradiction: US-565 AC 6 said "Recipe- and service-type-contributed event tasks merge below user entries per §6.11.3 precedence." The 2026-08-16 04:26 Review and 05:16 Drift Audit treated service-derived event merging as in scope, and Build added a plan-time service-event merge. The adjudication trigger rejected the same AC as requiring an out-of-scope runtime recipe contribution surface. Normative §6.11.3 defines only `ServiceTypeResolution.tooling` and its user > recipe > service-type tooling precedence; the normative interface has no `events` field. §8.8 removes runtime recipe expansion and makes recipes one-shot scaffolds inert after init, while §8.5.7 scopes events-as-tasks to the resolved Landofile `events:` map. The AC had generalized a tooling-only precedence rule to an event-contribution surface the normative contract does not define.
- Decision: narrowed US-565 AC 6 in `prd.json` to event steps present in the resolved Landofile top-level `events:` map, including ordinary §7.2/§7.7 merged fragments, with authored declaration order preserved. It now explicitly excludes runtime recipe and service-type event contributions, an `events` field on `ServiceTypeResolution`, and a planner-side contribution merge. The branch's service-event merge, SDK event-contribution field and compatibility note, and guide promise of lower-precedence service-type contributions are excision targets for the next Build/Review cycle. `passes` is unchanged.
- Follow-up story (if created): none; no normative runtime event-contribution surface exists to own — §6.11.3 contributes tooling only and §8.8 makes recipes inert after init.
---

## 2026-08-17 15:05 CDT - US-565 Build (adjudication excision)
- Excised the service-type event-contribution surface per the same-day adjudication: effective events now compile solely from the resolved Landofile top-level `events:` map, `ServiceTypeResolution` no longer carries an `events` field (SDK compatibility note updated to match), the planner performs no service-event merge and no longer keys the plan cache on per-service events, and the events guide no longer promises lower-precedence service-type contributions.
- The restart scenario that exercised the merge now proves authored events run in lifecycle order exactly once; the planner test proves compilation is authored-only.
- Verification: typecheck, lint, boundaries, guide lint/coverage/public transcripts, and the two-scenario executable events guide passed; 138 focused planner/event/init/restart/landofile/start/stop tests passed with positive counts. Schema compatibility reports only the pre-existing main-side sshServices/ProviderSetupPlan drift.
- **Learnings for future iterations:**
  - When an adjudication names excision targets, grep for every consumer of the excised field first (planner wiring, cache-key inputs, tests, guide prose, API_COMPATIBILITY notes) — the cache-key serviceInputs entry was easy to miss and would have silently kept stale keys.
---

## 2026-08-17 14:54 CDT - US-565 Adjudication (human, source-PRD reconciliation)

- Contradiction: the still-human-owned source PRD `prd-lando3-parity-02-landofile-keys.md` AC 6 required "Recipe-contributed and service-type-contributed event tasks merge below user entries with the §6.11.3 precedence," while the 2026-08-17 14:28 adjudication narrowed `prd.json` AC 6 to Landofile-only events and the 15:05 Build excised `ServiceTypeResolution.events` plus the planner merge. `prd.json` was amended; the source markdown was not, so the loop re-detected the same divergence.
- Decision (option 1 of the .looper-stop brief, chosen by human adjudicator): amend the source PRD to match `prd.json`. Events are resolved-Landofile data only. Recipe scaffolds may author `events:` into the generated Landofile at init time, but no distinct runtime recipe or service-type event layer exists. The 14:28 narrowing and the 15:05 excision stand; no code, guide, or SDK change is implied by this entry.
- Grounds: §6.11.0's canonical `ServiceTypeResolution` interface is `{ base, normalizedConfig, features, tooling?, metadata? }` with no `events` field; §6.11.3 is explicitly tooling-only; §8.5.7 defines event entries from the Landofile `events:` map; §8.8 removes runtime recipe expansion and makes recipes inert after init. Additionally, service types already inject lifecycle work through the §6.11.0 stage-3 `ServiceFeature`/`AppFeature` phase (`addBuildStep`, healthchecks, run hooks, §6.13 `app` build steps), so denying them an events surface costs no v3-parity capability. The source AC had generalized a tooling-only precedence rule by analogy.
- Options 2 and 3 rejected: both require inventing a public runtime contribution surface (and, for option 2, reversing §8.8's deliberate inert-recipe model) that an adjudication step cannot normatively authorize.
- Follow-up story (if created): none required for US-565. Two observations recorded for separate spec stories, neither blocking: (1) §6.11.3's tooling precedence line still reads "user `tooling:` > recipe `tooling:` > service-type `tooling:`", but §8.8 leaves no runtime recipe layer to occupy that middle rank — that stale rank is what the source AC generalized from, and it will keep generating this contradiction until reconciled; (2) if direct service-type event contributions are genuinely wanted, authorize them through a dedicated story that defines inheritance, precedence, plan-cache identity, ordering, and SDK compatibility.
- `passes` unchanged. US-565 is unblocked; resume the loop at Review.
---

## 2026-08-17 15:12 CDT - US-565 Adjudication follow-up (spec correction + US-582 split)

- **Authorized collateral edit — do not revert.** This entry exists so the Drift Audit's category-5 check ("unrelated doc/spec edits") can classify the change as a deliberate, defensible, human-authorized finding rather than smuggled scope. Human adjudicator directed the change on 2026-08-17 while unblocking US-565.
- Root cause: the 14:54 adjudication recorded that §6.11.3's tooling precedence still read `user tooling: > recipe tooling: > service-type tooling:` even though §8.8 leaves no runtime recipe layer to occupy the middle rank. That stale rank is what US-565's source AC generalized from, so leaving it in place would keep regenerating the same contradiction for any later story that cites §6.11.3 by analogy.
- Landed (prose only, no production code): `spec/06-services.md` §6.11.3 and `spec/10-plugins.md` service-type `tooling:` bullet now state the shipped two-rank model — resolved Landofile `tooling:` > service-type `tooling:` — plus whole-task replacement keyed on task name and the ordinal service-name tie-break, and state that recipe-authored tooling has no rank of its own because a scaffold writes `tooling:` into the generated Landofile at init and is inert thereafter (§8.8).
- Grounds: `compileEffectiveTooling` (`engine/src/planner/effective-tooling.ts`) merges `{ ...contributed, ...landofile.tooling }` — exactly two ranks, no recipe tier — and `engine/test/planner/effective-tooling.test.ts` already locks wholesale replacement, the ordinal tie-break (`äther` vs `zeta`, permutation-stable), and defaults-after-merge. US-563 owns that behavior and passes. The spec prose was wrong; the code was not. No code, schema, SDK, or guide change was made or is implied.
- Split recorded in the PRD index (not prose-only, per the Drift Audit's step-6 requirement): **US-582 "Service-type tooling precedence reconciliation + reserved-name enforcement"** added to `spec/lando3-parity/prd.json` and `prd-lando3-parity-02-landofile-keys.md` at priority 21, `passes: false`. `US-581` (wave closure) moved priority 21 -> 22 so closure still runs last. No other story's `passes` or priority changed.
- US-582 carries the one genuine code finding this sweep surfaced, deliberately NOT fixed here: `assertToolingNameClaimable` (`engine/src/operations/reserved-aliases.ts`) has zero production callers and is covered by a green unit test that proves nothing shipped. Reserved top-level names are guarded only at invocation time (`engine/src/operations/tooling.ts`, `tooling-bun-script.ts`), while §6.11.3 says service-type tooling merges "at plan time" and `assemble.ts` validates no names during that merge. Whether to reject at plan time or keep the invocation-time guard is a behavioral decision US-582 owns.
- Drift Audit note: the audit was already past its churn threshold before this change (delta ~2788 changed lines against a 1500 trigger, 39 commits, 8 Build/Review cycles). This entry does not suppress it, and the `.drift-gate-US-565` counter was deliberately left untouched. When the audit runs, its escalation-tier suppression check should apply: a prior adjudication ruled the boundary, and every commit since touches only spec/PRD/progress prose, so no new production commits have landed.
- No `passes` flip for US-565; story state remains `implemented`. Resume at Review.
---

## 2026-08-17 19:04 CDT - US-565 Review
- Fixed complete `cmd` step parity for deferred/looped working directories, invocation-time validation for expression-bearing repeated canonical inputs, nested tooling command ids, canonical tooling output propagation, raw/tooling-environment/result redaction, and plural or hyphenated secret-field matching. Restored canonical built-in spec identity and manifest-only plugin discovery without weakening executable-loader validation.
- Review notes (deferred/rejected): rejected Bugbot's blanket claim that every successful canonical command must return captured stdout/stderr because built-in and plugin output remains Renderer-owned and duplicate capture was previously adjudicated; the confirmed tooling-target loss was fixed. The remaining request for embedding runtimes to self-register every built-in command requires a class-free executable registry across the full command catalog, a §16 surface not explicitly owned by the source US-565 criteria, so Review routed that contract gap to adjudication instead of importing the CLI registry or creating a second dispatcher.
- Guide-Coverage-Skip: The existing events guide already covers lifecycle execution; these fixes close validation, redaction, and registry regressions without changing its user workflow.
- Verification: explicit-base guide drift, typecheck, lint, boundaries, schema snapshot codegen, 223 focused event/registry/discovery/lifecycle tests, and a 64-test post-security redaction/event rerun passed. Schema compatibility reports only the pre-existing `sshServices` removals and `ProviderSetupPlan` unknown drift.
- **Learnings for future iterations:**
  - Canonical command redaction must cover lifecycle snapshots and target-owned tooling environment values, not only returned streams; secret-key matching must recognize plural and hyphenated forms.
  - Keep canonical command specs identical to their registry source objects; transport-only base flags belong in a separate input-validation projection.
---

## 2026-08-17 19:15 CDT - US-565 Review follow-up
- Fixed the post-commit Bugbot finding by rejecting deferred built-in `command:` steps during static event-program validation, before any earlier event step can execute; runtime rejection remains as a second boundary.
- Review notes (deferred/rejected): the embedding built-in registry finding remains the contract gap recorded in the preceding Review entry and is routed to adjudication rather than fixed through a forbidden CLI-registry import.
- Verification: focused executor tests, typecheck, and lint passed.
---

## 2026-08-17 22:14 CDT - US-565 Adjudication
- Contradiction: US-565 AC 1 required that "command: steps resolve built-in, plugin-contributed, and tooling canonical ids" under the full §8.5.2 contract. Normative §8.5.2.1 says the step calls the same Effect program backing `@lando/core/cli` "so wrap behavior in the CLI and in an embedding host is identical," while §8.5.7 says lifecycle events include embedding-host invocations. Review therefore asked an ordinary `makeLandoRuntime`/`openLandoRuntime` to self-register every built-in target, but the same Review rejected importing the CLI registry or creating a second dispatcher because §16.7 separately owns the class-free command-operation surface: "Every built-in command in §8.2 has a corresponding exported Effect-returning command operation" and command effects do not require OCLIF. Meeting that embedding proof requires extracting the full built-in executable catalog, not another US-565 event shim.
- Decision: split the disputed surface into follow-up US-583 and narrowed US-565 AC 1 in `prd.json`. US-565 still owns the full events map, all §8.5.2 step forms, built-in/plugin/tooling target resolution and target-spec validation through the shared native CLI/runtime production path. It explicitly does not own extracting or self-registering the complete class-free built-in executable catalog for embedding, may not import the CLI-only registry through the default library entry, and may not create a second dispatcher. US-583 was inserted at priority 6, and the former priorities 6-22 moved to 7-23 so the direct follow-up runs next and wave closure remains last.
- Follow-up story (if created): US-583 — extract one class-free, completeness-checked built-in executable command catalog shared by the native CLI, `@lando/core/cli`, and embedding EventCommandExecutor registration, with App-handle lifecycle parity and import-cycle/boundary proof.
---

## 2026-08-17 22:29 CDT - US-565 Build completion
- Story verified complete on the existing branch with no new product code after the embedding-only executable-catalog surface moved to US-583; the remaining events-as-tasks criteria are implemented.
- Verification: typecheck and lint passed; 168 focused event/compiler/registry tests and 83 lifecycle scenario tests passed with one intentional skip; the compiled binary built successfully and compiled CLI checks in `lando4-dev` proved ordered host, built-in, and tooling event steps plus the tagged unknown-target failure path.
- **Learnings for future iterations:**
  - A `pre-destroy` program is the host-safe compiled-CLI surface for event ordering and command-target checks when container networking is unavailable.
---

## 2026-08-17 22:58 CDT - US-565 Review
- Reconciled the authoritative source PRD with the adjudicated US-565/US-583 split: US-565 now explicitly retains native CLI/runtime canonical-command proof while the new US-583 source story owns complete class-free embedding registration. This prevents Review from re-demanding a surface already split by adjudication.
- Review notes (deferred/rejected): rejected reopening embedding-wide built-in registration under US-565 because the 22:14 adjudication assigns it to US-583 and forbids a CLI-registry import or second dispatcher; Bugbot and the goal, QA, quality, and security lanes found no product defect.
- Verification: typecheck, lint, explicit-base guide drift, source/index consistency, and 30 focused event/compiler/registry/lifecycle/guide spot checks passed with positive counts.
- **Learnings for future iterations:**
  - When adjudication splits or narrows a story in `prd.json`, update the matching source PRD section in the same change; the source Markdown is Review's authoritative acceptance contract.
---

## 2026-08-17 23:15 CDT - US-565 Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev, and proved events-as-tasks on the compiled binary: `events:` accepted by `app:config`; unknown event fail-closed with the ten valid names; pre-init/post-init/pre-start host steps ran on `lando start`; destroy ran host, `command: app:mark-destroy`, `task: mark-destroy`, and `command: app:config` in authored order; include-only fragment events executed; pre-destroy abort kept later steps from running and redacted env secrets; post-destroy failure warned and still completed. Adjacent `info`, `app:cache:refresh`, `app:config`, `version`, and `destroy` worked. Removed `/tmp/us565-*`.
- **Out-of-scope finding:** `lando start` then `lando global:start` failed with `ServiceStartError` / `netavark: aardvark-dns failed to start: Failed to connect to user scope bus via local transport`. Same sandbox user-bus/cgroup limitation recorded in the 14:35 Build entry; not caused by this story. Service-targeted post-start/pre-stop steps therefore could not run against a live container.
- **Learnings for future iterations:**
  - Tooling `command:` targets use the namespaced canonical id (`app:<task>`), not the bare task name; `task:` still takes the bare name.
---

## 2026-08-18 02:10 CDT - Babysit
- PR: #787 https://github.com/AaronFeledy/core4/pull/787
- CI outcome: pass
- Review feedback: fixed (package-dag test seams, app-plan revision 15 fixture, and tooling flags no longer treated as positionals; rejected include-merge as generic §7.2 array rules)
- Merge: merged (squash)
---

## 2026-08-18 10:10 CDT - US-583 Build completion
- Extracted one completeness-checked class-free executable catalog for all 85 built-in command ids, with explicit deferred and four native-only embedding exemptions. Native dispatch, MCP projection, and embedding EventCommandExecutor now consume the same canonical spec entries.
- Preserved validation, lifecycle correlation, rendering, redaction, live streaming, interruption, and success-exit behavior. Generated bootstrap composition self-registers the catalog without import cycles, and shared operations/renderers no longer mutate embedding-host exit state.
- Verification: codegen drift, full boundaries, typecheck, lint, workspace build, and independent reviewer passed; 325 broad focused tests, 23 doctor/seam tests, 56 streaming/argument regressions, and 96 renderer/exit regressions passed. Compiled sandbox checks covered version, MCP catalog listing, native-only shellenv, and exit-2 invalid input.
- **Learnings for future iterations:**
  - Embedding parity includes process ownership: executable specs and shared renderers return exit semantics through `successExitCode`; only the native shell may mutate its process exit status.
  - An Effect service supplied to generated bootstrap layers must be introduced through the generator and the hand-maintained runtime service union, not merged as a sibling after consumers are built.
---

## 2026-08-18 10:58 CDT - US-583 Review
- Reviewed the embedding catalog against the source acceptance criteria. Updated stale `core/AGENTS.md` class-layer guidance to the class-free catalog, one-way `BuiltInCommandCatalogLive` injection, and the four native-only embedding exemptions. Fixed native `lando mcp --list` so it uses the same dispatcher as serve and therefore merges tooling tools.
- Review notes (deferred/rejected): rejected treating `lando mcp --allow` of embedding-exempt ids as a US-583 privilege bug because embedding-exempt is the EventCommandExecutor/library boundary while MCP is the native CLI with its own `mcpAllowed` default allowlist; rejected the `meta:mcp` serve-mode adapter as a second dispatcher because it is native stdio transport under `run.ts`; rejected treating static catalog/spec imports through the default `@lando/core` entry as a leak because import-boundary tests require that graph and the specs are the class-free operations; rejected demanding a complete namespace-first `@lando/core/cli/operations` export surface because US-583 owns catalog extraction and EventCommandExecutor registration, not a §16.7 named-export rewrite; rejected requiring a second fresh-runtime failure matrix because App-handle lifecycle tests already run representative app/apps/meta `command:` steps and executor tests cover unknown/deferred/exempt/invalid tagged failures.
- Verification: QA typecheck, lint, boundaries, and 219 focused tests passed before this docs-only fix; guide-drift vs origin/main passed; Bugbot found no bugs on the build commit.
- **Learnings for future iterations:**
  - `embedding-exempt` is not an MCP allowlist bit; default MCP exposure stays `mcpAllowed: true` only.
  - `Layer.suspend` does not defer static imports; keep CLI-only native adapters out of `BuiltInCommandCatalogLive` and let the generated bootstrap inject the catalog.
---

## 2026-08-18 11:40 CDT - US-583 Verify
- Compiled `core/dist/lando`, ran `lando setup --yes --no-interactive` in lando4-dev (provider+CA satisfied; managed Podman 6.0.1 running). Compiled CLI accepted `events.command:` for `app:config`, `apps:list`, and `meta:version`; `lando destroy --yes` ran those steps between host bookends. Unknown/deferred/invalid targets failed closed with tagged remediation. `lando mcp --list` used the shared dispatcher. An embedding `openLandoRuntime` App handle ran the same three `command:` steps (`cli-app:config-*`, `cli-apps:list-*`, `cli-meta:version-*`). Adjacent `app:config`, `app:cache:refresh`, `info`, `version`, `help`, `doctor`, and `shellenv` worked.
- Fixed shared-catalog `strict: false` argv rejection so `lando bun <script>` and other passthrough commands keep extra arguments (`c95797694`).
- **Out-of-scope finding:** `lando setup` failed the proxy step and `lando start` failed with `ServiceStartError` / `netavark: aardvark-dns failed to start: Failed to connect to user scope bus via local transport`. Same sandbox user-bus/cgroup limitation recorded in US-565 Verify; not caused by this story. Pre-init/post-init/pre-start host steps still ran.
- **Learnings for future iterations:**
  - `runBuiltInCommand` must honor `LandoCommandSpec.strict === false`; flag/positional parity is for strict commands only. `lando bun`/`lando x` extra argv is the canary.
  - Library `openLandoRuntime()` needs `plugins: { policy: "bundled-only" }` after `import "@lando/core/bundled-plugins"`; the default library policy does not discover CLI-installed providers.
---

## 2026-08-18 15:58 CDT - Babysit
- PR: #788 https://github.com/AaronFeledy/core4/pull/788
- CI outcome: pass
- Review feedback: fixed (passthrough argv no longer steals declared flags from the inner command tail)
- Merge: merged (squash)
---

## 2026-08-18 18:20 CDT - US-566
- RabbitMQ 3/4, MinIO, and LocalStack now plan as catalog service types: management/console routes, persistent volumes, healthchecks, and service-type tooling (rabbitmqctl/rabbitmqadmin, mc, awslocal). MinIO bucket init is `database:` mapped to `MINIO_DEFAULT_BUCKETS`.
- Each type publishes a config schema under `@lando/sdk/schema/services/<type>` and ships an executable guide that inspects resolved config.
- Verification: typecheck, lint passed; 115 focused service/contract/registration tests plus guide lint/coverage/public transcripts passed; `lando app:config --format=json` accepted a three-service Landofile.
- **Learnings for future iterations:**
  - New catalog types should publish `@lando/sdk/schema/services/<type>` via `ServiceConfig.pick` plus a narrowed `type` literal, then register the schema in `json-schema.ts`. Missing inherited field descriptions fail `codegen:schema-snapshot`.
  - `ServiceType.schema` is `Schema.Schema.AnyNoContext` so concrete catalog schemas assign without assertions.
---

## 2026-08-18 19:13 CDT - US-566 Review
- Confirmed Bugbot findings and fixed MinIO/LocalStack planner contracts: official MinIO no longer ships curl or honors MINIO_DEFAULT_BUCKETS, so readiness now uses `mc ready local` and bucket init creates `/data/$MINIO_BUCKET` before `minio server` with `--address` bound to `port:`. LocalStack `port:` now sets GATEWAY_LISTEN so the gateway and healthcheck share the same port.
- Review notes (deferred/rejected): rejected switching MinIO to `kind: http` healthchecks because the planner only realizes `kind: command`; rejected treating RabbitMQ `port:` as a listen-port rewrite because AMQP health uses `rabbitmq-diagnostics ping` and matches existing redis-style advertised-port behavior; rejected Bitnami/MINIO_DEFAULT_BUCKETS because the catalog image is official `minio/minio`. Rejected post-fix Bugbot note that authored `entrypoint:` should still wrap bucket mkdir: a custom entrypoint owns process startup, and wrapping it would break the user's command contract.
- Verification: typecheck, lint passed; 115 focused service/contract/registration tests plus the minio guide scenario passed; explicit-base guide drift passed.
- **Learnings for future iterations:**
  - Official `minio/minio` is UBI micro: no curl, no MINIO_DEFAULT_BUCKETS. Use `mc ready local` plus a pre-start directory for bucket init, and bind `--address` when `port:` is authored.
  - LocalStack listen port is GATEWAY_LISTEN, not just the planned endpoint.
---
