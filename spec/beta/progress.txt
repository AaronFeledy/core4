# Beta Progress Log — Lando v4 Phase 3 (full breadth)

Source PRDs: spec/beta/prd-beta-{00..13}-*.md
Spec source of truth: spec/ROADMAP.md Phase 3 + spec/{01..19}-*.md and spec/15-binary-build-and-release.md (§17)

## [2026-05-26 21:06 UTC] - Sync
- Previous branch: us-073-recipe-readme-strip-flatten
- Previous PR: #170 https://github.com/AaronFeledy/core4/pull/170
- Action: caught up after prior merge (PR #170 was already MERGED, all CI green)
- Local main updated: yes (already up to date)
- Branch after sync: main
- Stash created: no
---

## [2026-05-28 12:05 CDT] - Triage
- PR: #189 https://github.com/AaronFeledy/core4/pull/189
- CI outcome: live Docker provider contract failure (`provider-integration-linux-x64`) was not reproducible locally after pulling `node:22-alpine` and rerunning the contract repeatedly
- Decision: not an actionable code defect in `@lando/provider-docker`; no code change or commit was needed
- Verification: direct Docker API probe showed framed log bytes, and three consecutive `runProviderContract` runs passed against `/var/run/docker.sock`
- Next step: watch for a rerun / flaky signal before making further changes


## [2026-05-27 00:00 UTC] - PR #184 triage
- CI failure: real issue — provider-integration-linux-x64 only pre-pulled node/postgres images, so Podman had to fetch `memcached:1.6` during container create and got HTTP 404.
- PR comments: not-issue — none at capture time; Cursor Bugbot was still pending, so there was no review feedback to address.
- Fix: added `podman pull memcached:1.6` to the CI pre-pull step and kept the service-lando live integration step intact.

## Codebase Patterns
- `machineFailure()` in `plugins/provider-lando/src/setup.ts` is the single factory for all Podman machine errors; extend its regex conditions to detect new OS-specific virtualization prerequisites before adding new error subclasses.
- `buildSetupSteps()` drives both the event-tree children list and which setup actions run; adding a platform requires updating that function + the corresponding `if (platform === ...) ...` block in `setupProviderLando`.
- Windows Podman machine lifecycle is structurally identical to macOS (`ensureWindowsPodmanMachine` mirrors `ensureMacOSPodmanMachine`); the separation exists for future divergence (e.g. WSL-specific machine flags).
- Host-mutating live tests should gate on both the expected platform and an exact opt-in env value (`process.platform !== "win32" || process.env.GATE_VAR !== "1"`) so accidental truthy values do not run provider setup on the wrong host.

## [2026-05-26] - US-074
- Implemented Windows VM lifecycle for `@lando/provider-lando`
- Files changed:
  - `plugins/provider-lando/src/capabilities.ts`: `bindMountPerformanceForPlatform("win32")` → `"slow"`; `bindMounts` set `true` for Windows (WSL2 supports bind mounts per spec §5.8.1)
  - `plugins/provider-lando/src/setup.ts`: Added `WindowsMachinePrerequisiteError`; extended `machineFailure()` to detect Hyper-V/WSL2/VMP messages; added `ensureWindowsPodmanMachine`, `stopWindowsPodmanMachine`, `upgradeWindowsPodmanMachine`, `teardownWindowsPodmanMachine`; wired Windows machine step into `buildSetupSteps` and `setupProviderLando`
  - `plugins/provider-lando/src/index.ts`: Exported all new Windows-specific symbols
  - `plugins/provider-lando/test/capabilities.test.ts`: Updated Windows assertions to expect `bindMountPerformance: "slow"` and `bindMounts: true`
  - `plugins/provider-lando/test/setup.integration.test.ts`: Added 6 Windows machine lifecycle tests (fake-client) + 1 live test gated on `LANDO_TEST_WINDOWS_PROVIDER_LANDO=1`
- Verification: `bun test plugins/provider-lando/` → 74 pass, 8 skip, 0 fail; `bun run typecheck` → exit 0; `bun run lint` → no fixes needed
- **Learnings for future iterations:**
  - The 9 pre-existing test failures in the full suite (`ProcessRunnerLive`, `compiled` binary tests, `codegen`) are environment failures (`bun` not on PATH in the test runner) — not regressions; safe to ignore in this env.
  - Windows and macOS machine runners share the same `PodmanMachineRunner` interface; only the `ensureXxx` orchestration functions differ per platform.
  - `ProviderCapabilities.bindMounts` should be `true` for Windows because WSL2 supports bind mounts; `bindMountPerformance: "slow"` reflects the VM-mediated cross-boundary overhead.
---

## [2026-05-26 21:39 UTC] - US-074 Review
- Reviewed the US-074 Windows provider-lando lifecycle implementation against `spec/beta/prd-beta-01-provider-matrix.md` and the build-step commits (`f75b1987..HEAD`).
- Fixed confirmed review blockers:
  - Threaded the resolved host platform through `makeSystemPodmanMachineRunner()` / `runMachineCommand()` so real Windows Podman stderr maps to `WindowsMachinePrerequisiteError` instead of generic or macOS remediation.
  - Added system-runner stderr coverage for Hyper-V / WSL2 / Virtual Machine Platform prerequisite output.
  - Tightened the Windows live test gate to `process.platform === "win32"` plus `LANDO_TEST_WINDOWS_PROVIDER_LANDO=1`.
  - Added the required US-074 guide coverage file at `docs/guides/setup/provider-lando-windows.mdx`.
- Files changed:
  - `plugins/provider-lando/src/setup.ts`
  - `plugins/provider-lando/test/setup.integration.test.ts`
  - `docs/guides/setup/provider-lando-windows.mdx`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: real Windows system-runner prerequisite failures were not receiving Windows-specific remediation.
  - Fixed: Windows remediation tests injected `WindowsMachinePrerequisiteError` directly and missed stderr classification.
  - Fixed: Windows live test was env-gated but not platform/exact-value gated.
  - Fixed: required US-074 guide coverage was missing.
  - Rejected/deferred: WSL distro unregister during teardown is an open PRD question/default larger than this review fix; current US-074 acceptance only requires fake-client teardown transitions, so this remains future CLI/provider teardown work rather than a scoped repair.
- Verification: `PATH="/home/aaron/.bun/bin:$PATH" /home/aaron/.bun/bin/bun run typecheck` → exit 0; `PATH="/home/aaron/.bun/bin:$PATH" /home/aaron/.bun/bin/bun run lint` → exit 0; `PATH="/home/aaron/.bun/bin:$PATH" /home/aaron/.bun/bin/bun run lint:guides` → exit 0; `PATH="/home/aaron/.bun/bin:$PATH" /home/aaron/.bun/bin/bun test plugins/provider-lando/` → exit 0; `PATH="/home/aaron/.bun/bin:$PATH" /home/aaron/.bun/bin/bun test` → 1191 pass, 12 skip, 0 fail.
- **Learnings for future iterations:**
  - When adding platform-specific prerequisite errors, thread platform context through the real system runner and test stderr classification through the runner seam, not only direct fake error propagation.
  - Guide coverage requirements apply as soon as `plugins/provider-lando/**` changes for PRD-B-01 stories; add the mapped `docs/guides/setup/*.mdx` file or record a valid skip reason.
---

## [2026-05-26 22:01 UTC] - Push
- Branch: us-074-provider-lando-windows-lifecycle
- Push: pushed
- Commit(s): 69e14d1c feat: provider-lando Windows VM lifecycle; e6770fd3 chore: mark US-074 complete and update progress log; 1cdbac8d fix: wire Windows Podman prerequisite remediation; 1fd670e5 docs: record US-074 review fixes
- PR: #172 https://github.com/AaronFeledy/core4/pull/172 (created)
---

## [2026-05-26 22:14 UTC] - PR #172 static-checks triage
- CI failure: real issue; OCLIF command discovery treated `core/src/cli/oclif/commands/deferred-stub.ts` as a command id and emitted `Warning: Error` (`command deferred-stub not found`) on stderr during source-mode CLI startup.
- Fix: moved the deferred-stub helper out of the OCLIF commands discovery tree and updated stub command imports; kept the init scenario stderr assertion intact.
- Verification: `~/.bun/bin/bun test core/test/cli/init.scenario.test.ts` → exit 0; `~/.bun/bin/bun run typecheck` → exit 0; `~/.bun/bin/bun run lint` → exit 0; `~/.bun/bin/bun test` without PATH fix hit known local `bun`-not-on-PATH failures, then `PATH="$HOME/.bun/bin:$PATH" bun test` → exit 0.
---

## [2026-05-26 22:44 UTC] - Babysit
- PR: #172 https://github.com/AaronFeledy/core4/pull/172
- CI outcome: pass
- Review feedback: fixed (static-checks OCLIF deferred-stub discovery warning repaired; no review comments)
- Triage rounds used: 1
- Merge: merged (squash)
- Local main / branch refreshed: yes
- Remaining local state, if any: none after recording this note
---

## [2026-05-26 17:47 CDT] - Sync
- Previous branch: us-074-provider-lando-windows-lifecycle
- Previous PR: #172 https://github.com/AaronFeledy/core4/pull/172
- Action: caught up after prior merge (PR was already MERGED; local branch deleted, main already at origin/main)
- Local main updated: yes (already up to date)
- Branch after sync: main
- Stash created: no
---

## [2026-05-26 18:30 CDT] - US-075
- Implemented pinned runtime-bundle manifest and idempotent default downloader for `@lando/provider-lando` across Linux, macOS, and Windows.
- Files changed:
  - `plugins/provider-lando/runtime-bundle-versions.json` (new): Mechanism A pinned manifest with entries for linux-x64, linux-arm64, darwin-x64, darwin-arm64, win32-x64; placeholder SHA-256 values flagged in `_comment` until the §17.3 build pipeline supplies real artifacts.
  - `plugins/provider-lando/src/runtime-bundle.ts` (new): `ProviderBundleChecksumError` (remediation cites spec §5.8.1), schema-validated `RUNTIME_BUNDLE_MANIFEST`, `resolveRuntimeBundleEntry(platform, arch)`, `runtimeBundleCachePath(stateDir, entry)`, `makeRuntimeBundleDownloader({entry, …})` (test seam), `makeDefaultRuntimeBundleDownloader({platform, arch, stateDir, fetchImpl})` that reuses any cached bundle whose SHA-256 matches the pinned value (idempotent re-runs), atomically persists the verified bytes under `<stateDir>/provider-lando/runtime-bundle/<filename>`, and surfaces typed `ProviderUnavailableError` for transport/HTTP/cache-write failures.
  - `plugins/provider-lando/src/setup.ts`: Removed the legacy `RuntimeBundleVerificationError` class, routed `verifyRuntimeBundle` through the new `ProviderBundleChecksumError` so all bundle checksum mismatches share one §5.8.1-citing remediation.
  - `plugins/provider-lando/src/index.ts`: Exports the new symbols/types and drops the legacy error name.
  - `plugins/provider-lando/test/runtime-bundle.test.ts` (new): Covers manifest shape, platform/arch resolution (incl. failure for `win32-arm64`), `ProviderBundleChecksumError` remediation, cache-path computation, idempotent cache reuse with a throwing `fetch`, corruption recovery, mismatch failure, HTTP 5xx failure, and default-downloader plumbing through the shipped manifest.
  - `plugins/provider-lando/test/setup.integration.test.ts`: Existing bundle-mismatch test now asserts `ProviderBundleChecksumError` and that the remediation contains "§5.8.1".
  - `docs/guides/setup/provider-lando-windows.mdx`: Added a non-rendered `windows-runtime-bundle` scenario block describing the pinned manifest, idempotent cache path under the per-user state dir, and the tagged checksum error.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-lando/` → 93 pass, 8 skip, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1209 pass, 12 skip, 0 fail
- **Learnings for future iterations:**
  - The static `RUNTIME_BUNDLE_MANIFEST` ships placeholder SHA-256 values, so any caller-driven test that goes through `makeDefaultRuntimeBundleDownloader` can only exercise the failure branches. Test the happy/idempotent paths through `makeRuntimeBundleDownloader({entry, …})`, which accepts a caller-supplied entry whose SHA-256 is known. Apply this seam-injection pattern whenever a downloader needs deterministic SHA fixtures alongside a production-pinned manifest.
  - `import data from "../runtime-bundle-versions.json" with { type: "json" }` works under the workspace's `resolveJsonModule: true` + `verbatimModuleSyntax: true` setup; the JSON file does not need to be listed in the tsconfig `include` because the import itself drags the resolution.
  - The idempotency contract is "on-disk SHA matches pinned SHA → reuse, no fetch". A corrupted cache (SHA mismatch) MUST fall through to the download path; tests should assert this by pre-writing junk bytes and then observing both that fetch was invoked AND that the cache file is replaced with the verified bytes.
  - When introducing a new tagged subclass of `ProviderUnavailableError`, name + remediation are the contract for the spec, not the parent tag — keep the parent for catch-all error handling but assert the subclass via `instanceof` in tests.
---

## [2026-05-26 18:59 CDT] - US-075 Review
- Reviewed the US-075 runtime-bundle implementation against `spec/beta/prd-beta-01-provider-matrix.md` and the build-step commit (`HEAD`).
- Fixed confirmed review blockers:
  - Wired `makeRuntimeProvider({ stateDir })` to construct the default pinned-manifest runtime-bundle downloader when no explicit test/downstream downloader is supplied, so normal `lando setup` exercises the bundle download/checksum path.
  - Made host-platform defaulting fail closed for unsupported `process.platform` values instead of silently treating every non-Linux/non-macOS host as Windows.
  - Hardened runtime-bundle cache paths by rejecting non-basename manifest filenames and using a unique temp file for verified bundle persistence.
- Files changed:
  - `plugins/provider-lando/src/index.ts`
  - `plugins/provider-lando/src/runtime-bundle.ts`
  - `plugins/provider-lando/test/runtime-bundle.test.ts`
  - `plugins/provider-lando/test/setup.integration.test.ts`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: production provider setup skipped runtime-bundle download/verification unless a downloader was injected.
  - Fixed: unsupported host platforms silently mapped to `win32` in default platform resolution.
  - Fixed: runtime-bundle filename/path handling could escape the cache root if a manifest/caller supplied path separators or traversal.
  - Fixed: temp bundle writes used a predictable timestamp path; now they use a UUID temp path with exclusive create and best-effort cleanup.
  - Rejected/deferred: placeholder SHA-256 values are an acknowledged pre-release artifact-pinning gap for the §17.3 build pipeline, not a code-path bug in this story; the downloader correctly fails closed until real artifacts are pinned.
  - Rejected/deferred: proxy/custom-CA-aware fetch wiring is part of the broader §10.3.1 network subsystem and no resolved network client layer exists in this codebase yet; the downloader keeps a `fetchImpl` seam so that integration can be wired when the subsystem lands.
  - Rejected: redundant `setup.ts` checksum verification is intentional defense-in-depth at the setup boundary and preserves the existing `RuntimeBundleDownloader` contract.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us075-review-provider-lando.xml plugins/provider-lando/test/runtime-bundle.test.ts plugins/provider-lando/test/setup.integration.test.ts` → 44 tests, 137 assertions, 0 failures, 2 skips.
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us075-review-provider-lando-all.xml plugins/provider-lando/` → 105 tests, 376 assertions, 0 failures, 8 skips.
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us075-review-full.xml` → 1226 tests, 6228 assertions, 2 failures, 12 skips while the review fix was still uncommitted; failures were the dirty-tree-sensitive `core/test/build/biome.test.ts` lint-gate test and compiled alias output in `core/test/cli/aliases.test.ts`, not provider-lando runtime-bundle failures.
  - LSP diagnostics tool could not attach to the TypeScript server (`exited with code -2`); `tsc -b` typecheck was clean.
- **Learnings for future iterations:**
  - Adding a default downloader factory is not enough; provider construction must wire it for the normal `stateDir` path and tests should exercise the provider-level default, not only the low-level test seam.
  - Host-platform default helpers should return `undefined`/fail closed for unknown platforms; do not collapse unknown platforms into Windows just because Windows is the only remaining supported literal.
  - Treat bundled manifests as a supply-chain boundary: schema + runtime cache helpers should reject path separators/traversal even when the current checked-in manifest is safe.
---

## [2026-05-26 19:08 CDT] - US-075 Review Verification Update
- Updated post-commit verification note after rerunning the full suite on a clean worktree.
- Files changed:
  - `spec/beta/progress.txt`
- `review-work` findings: no new findings; this entry corrects the verification record only.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us075-final-full.xml` → 1226 tests, 6241 assertions, 1 failure, 12 skips; the failing suite was `core/test/cli/renderer-flag.scenario.test.ts` compiled `$bunfs` renderer parity.
  - `PATH="$HOME/.bun/bin:$PATH" bun test core/test/cli/renderer-flag.scenario.test.ts` rerun in isolation → 12 pass, 0 fail.
  - `PATH="$HOME/.bun/bin:$PATH" bun test core/test/build/compile.test.ts` rerun in isolation → 1 pass, 0 fail.
- **Learnings for future iterations:**
  - Full-suite compiled CLI scenario failures can be order/flakiness-sensitive; rerun the named failing file to distinguish provider-story regressions from unrelated compiled harness instability.
---

## [2026-05-27 00:28 UTC] - US-075 Cleanup
- Ran the branch-wide `remove-ai-slops` cleanup after review and before push with the planning-doc comment rules.
- Cleanup result: cosmetic-only comment/prose cleanup; no runtime behavior, public API, error handling, cache semantics, checksum logic, or test assertions changed.
- Files changed:
  - `plugins/provider-lando/runtime-bundle-versions.json`: tightened the manifest annotation to remove planning-doc traceability prose.
  - `plugins/provider-lando/src/runtime-bundle.ts`: removed redundant doc blocks that restated exported names and behavior already covered by tests.
  - `plugins/provider-lando/src/setup.ts`: shortened two explanatory comments while preserving the parse/error-classification rationale.
  - `plugins/provider-lando/test/runtime-bundle.test.ts`: removed assertion-restating test comments without changing fixtures or assertions.
- Planning-doc leakage scan: no branch-changed source/test comments still cite planning docs, phases, PRDs, story IDs, or section references.
- Verification: `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us075-cleanup-full.xml` → 1226 tests, 6244 assertions, 0 failures, 12 skips; JSON parse + `git diff --check` → clean. LSP diagnostics were attempted for changed files but the TypeScript/Biome LSP servers exited with code -2.
---

## [2026-05-27 00:31 UTC] - Push
- Branch: us-075-provider-lando-windows-bundle
- Push: pushed
- Commit(s): 40ad7c86 Record US-075 cleanup; 125547ae Remove provider-lando runtime bundle slop; 3f61529f Record US-075 review verification update; 50bc8563 Record US-075 review fixes; 2dac458f Wire provider-lando default bundle setup; be39e414 Harden provider-lando runtime bundle cache; a6158bcd feat: provider-lando runtime-bundle download and checksum verification
- PR: #173 https://github.com/AaronFeledy/core4/pull/173 (created)
---

## [2026-05-27 00:39 UTC] - Babysit
- PR: #173 https://github.com/AaronFeledy/core4/pull/173
- CI outcome: pass
- Review feedback: none
- Triage rounds used: 0
- Merge: merged (squash)
- Local main / branch refreshed: yes
- Remaining local state, if any: none
---

## [2026-05-27 00:42 UTC] - Sync
- Previous branch: us-075-provider-lando-windows-bundle
- Previous PR: #173 https://github.com/AaronFeledy/core4/pull/173
- Action: caught up after prior merge (PR was already MERGED)
- Local main updated: yes
- Branch after sync: main
- Stash created: no
---

## [2026-05-27 01:00 UTC] - US-076
- Implemented Windows capability surface in `meta:doctor` for `@lando/provider-lando`
- Files changed:
  - `sdk/src/services/index.ts`: Added `bundle?: string` to `ProviderVersions` interface
  - `plugins/provider-lando/src/index.ts`: Added `bundleVersion` closure variable; setup tap now also captures `result.runtimeBundleVersion`; `getVersions` exposes `bundle: bundleVersion` when set
  - `core/src/cli/commands/doctor.ts`: Added `context.bundleVersion` from `versions?.bundle`; added `"bundleVersion"` to `orderContextKeys` known-order array
  - `core/test/cli/fixtures/meta-doctor.provider-status.windows.ndjson` (new): Snapshot fixture for the Windows provider path (`win32` platform, `bindMountPerformance: "slow"`, `sharedCrossAppNetwork: false`, `bundleVersion: "0.1.0-test"`)
  - `core/test/cli/doctor.test.ts`: Added `WINDOWS_FIXTURE_PATH`; added snapshot test, behavioral assertions test, and live test block (skipped without `LANDO_TEST_WINDOWS_PROVIDER_LANDO=1`)
- Verification: `bun run typecheck` → exit 0; `bun run lint` → exit 0; `bun test core/test/cli/doctor.test.ts` → 10 pass, 1 skip; `bun test` → 1216 pass, 13 skip, 0 fail
- **Learnings for future iterations:**
  - `ProviderVersions` in `sdk/src/services/index.ts` is a plain TS interface (not a Schema, not a Context.Tag shape); adding optional fields is additive and does not need listing in `sdk/API_COMPATIBILITY.md`
  - Bundle version is only available after `setupProviderLando` completes; cold `doctor()` calls (before `lando setup` runs) will have `bundleVersion` absent from context — that is expected and the doctor renders correctly either way
  - The `orderContextKeys` known-order list controls the NDJSON context field order; keep it in sync with new context fields added in `doctor()`
---

## [2026-05-27 01:13 UTC] - Push
- Branch: us-076-provider-lando-windows-doctor
- Push: pushed
- Commit(s): 06de73a feat: provider-lando Windows capability surface in meta:doctor
- PR: #174 https://github.com/AaronFeledy/core4/pull/174 (created)
---

## [2026-05-27 01:25 UTC] - Babysit
- PR: #174 https://github.com/AaronFeledy/core4/pull/174
- CI outcome: pass (static-checks, schema-snapshot, bundled-codegen, library-api-tests, recipe-tests, guide-scenarios-linux-x64, build-linux-x64, provider-integration-linux-x64, Cursor Bugbot — all SUCCESS)
- Review feedback: none
- Triage rounds used: 0
- Merge: merged (squash)
- Local main / branch refreshed: yes
- Remaining local state, if any: none
---

## [2026-05-27 01:27 UTC] - Sync
- Previous branch: us-076-provider-lando-windows-doctor
- Previous PR: #174 https://github.com/AaronFeledy/core4/pull/174
- Action: caught up after prior merge (PR #174 was already MERGED; local main already at origin/main)
- Local main updated: yes (already up to date)
- Branch after sync: main
- Stash created: no
---

## [2026-05-26 21:35 CDT] - US-077
- Implemented Windows Docker Desktop support for `@lando/provider-docker`
- Files changed:
  - `plugins/provider-docker/src/index.ts`: Added `isNpipeDockerHost` + `npipeSocketPath` helpers; routed npipe URIs through the existing unix-socket curl client (curl `--unix-socket` accepts the `//./pipe/...` form that falls out of stripping `npipe:`); extended `resolveDockerHost` precedence to honor `LANDO_TEST_WINDOWS_DOCKER_SOCKET` ahead of generic test/env sources on win32 and default to `npipe://./pipe/docker_engine`; exported `windowsDockerCapabilities`.
  - `plugins/provider-docker/test/capabilities.test.ts`: Added Windows capability matrix shape test, Windows host classification, default-named-pipe discovery, precedence test (explicit > LANDO_TEST_WINDOWS_DOCKER_SOCKET > LANDO_TEST_DOCKER_SOCKET > DOCKER_HOST > default), introspection-on-win32, factory-host plumbing, and npipe client construction.
  - `plugins/provider-docker/test/contract.integration.test.ts`: Added a Windows fake-client test that exercises apply/inspect/exec/logs/destroy and asserts the slow-bindmount capability; added a live contract test gated on `process.platform === "win32"` plus `LANDO_TEST_WINDOWS_DOCKER_SOCKET` or `DOCKER_HOST`.
  - `docs/guides/setup/provider-docker-windows.mdx` (new): Required US-077 guide coverage; non-rendered scenarios describing the discovery precedence and live-contract gating.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-docker/` → 18 pass, 2 skip, 0 fail (both skips are live tests)
  - `PATH="$HOME/.bun/bin:$PATH" bun test test/scenarios/generated/guides/provider-docker-windows/` → 2 pass, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1226 pass, 14 skip, 0 fail
- **Learnings for future iterations:**
  - The existing `isVmMediatedDockerHost(platform, dockerHost)` already short-circuits win32 to `bindMountPerformance: "slow"`, so `dockerCapabilitiesForHost("win32", ...)` already covers the capability surface — no helper changes were needed, only a named export (`windowsDockerCapabilities`) at parity with `linuxDockerCapabilities` / `macosDockerCapabilities`.
  - `npipe://./pipe/docker_engine` is processed by reusing `makeUnixDockerApiClient(npipeSocketPath(host))`. Stripping just `npipe:` yields `//./pipe/docker_engine`, which curl `--unix-socket` accepts on Windows; this avoids a second curl-based runtime branch.
  - The Windows live test gate must combine `process.platform === "win32"` with the env-var pair (US-074 pattern) — gating on env alone would let CI export `DOCKER_HOST` for the Linux suite and unintentionally run Windows-only assertions on Linux.
  - `LANDO_TEST_WINDOWS_DOCKER_SOCKET` is win32-only: ordering it before `LANDO_TEST_DOCKER_SOCKET` is what gives a Windows host a stable test-runner override without disturbing the existing generic Linux/macOS test seam.
---

## [2026-05-27 03:35 UTC] - US-077 Review
- Reviewed the US-077 provider-docker Windows Docker Desktop implementation against `spec/beta/prd-beta-01-provider-matrix.md`, repo context, and the build-step commit (`HEAD` before review fix).
- Fixed confirmed review blocker:
  - Replaced the default `npipe://./pipe/docker_engine` branch's Unix-socket curl reuse with a real IPC/named-pipe HTTP transport, including Docker Desktop URI normalization to `\\.\pipe\docker_engine` and request/stream coverage through a local IPC socket.
- Files changed:
  - `plugins/provider-docker/src/index.ts`
  - `plugins/provider-docker/test/capabilities.test.ts`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: `npipe://` was routed through `curl --unix-socket`, which does not validate or implement Windows named-pipe HTTP transport for Docker Desktop.
  - Rejected: core registry still has a hardcoded stub `docker` provider; US-077 scope is the provider plugin contract, while core provider-selection wiring is covered by later provider-selection stories.
  - Rejected: Windows drive-letter bind-path translation is not a new US-077 blocker; this story's acceptance covers Docker API lifecycle/discovery and the existing provider contract uses portable fake-client plans.
  - Rejected: duplicate generic + Windows live Docker tests on Windows are low-risk and intentional enough for now; both are opt-in/host-gated and exercise different default platform assumptions.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-docker/ --reporter=junit --reporter-outfile=/tmp/provider-docker-review2.xml` → 23 tests, 65 assertions, 0 failures, 2 skips.
  - `PATH="$HOME/.bun/bin:$PATH" bun test test/scenarios/generated/guides/provider-docker-windows/ --reporter=junit --reporter-outfile=/tmp/us077-guide.xml` → 2 tests, 0 failures.
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides docs/guides/setup/provider-docker-windows.mdx` → `Guide lint passed.`
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us077-review-full.xml` → 1243 tests, 6279 assertions, 0 failures, 14 skips.
  - LSP diagnostics were attempted for changed TypeScript files, but the TypeScript LSP server exited with code -2; `tsc -b` was clean.
- **Learnings for future iterations:**
  - Do not reuse curl's Unix-socket transport for Docker Desktop `npipe://` endpoints; normalize Docker's URI form to a Windows named-pipe path and send HTTP over IPC directly.
  - A construction-only API-client test is not enough for transport branches. Add a local IPC-socket request/stream test so fake-client coverage proves the selected transport can actually speak HTTP.
---

## [2026-05-27 04:10 UTC] - US-077 Cleanup
- Ran the branch-wide `remove-ai-slops` cleanup after review and before push with the planning-doc comment rules.
- Cleanup result: cosmetic-only cleanup; no Docker host resolution, named-pipe transport behavior, public exports, live-test gates, assertions, or provider contract semantics changed.
- Files changed:
  - `docs/guides/setup/provider-docker-windows.mdx`: shortened guide scenario reasons and variable display text while preserving scenario IDs, values, render flags, and coverage intent.
  - `plugins/provider-docker/src/index.ts`: removed a redundant module header comment.
  - `plugins/provider-docker/test/contract.integration.test.ts`: collapsed a trivial fixture helper and shortened verbose test names without changing assertions or gates.
- Slop-remover output: `plugins/provider-docker/test/capabilities.test.ts` and the extra review passes reported no safe edits; accepted only the cosmetic edits above.
- Planning-doc leakage scan: changed TypeScript code/test comments are clean; the guide frontmatter `beta` tag remains intentional metadata, not code-comment traceability.
- Verification: `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides docs/guides/setup/provider-docker-windows.mdx` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-docker/test/capabilities.test.ts plugins/provider-docker/test/contract.integration.test.ts` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us077-cleanup-full.xml` → 2515 tests, 0 failures, 0 errors, 28 skips; `git diff --check` → clean. LSP diagnostics were attempted for changed TypeScript files, but the TypeScript LSP server exited with code -2.
---

## [2026-05-27 04:32 UTC] - Push
- Branch: us-077-provider-docker-windows
- Push: pushed
- Commit(s): 2cf3bd2 feat: provider-docker Windows (Docker Desktop) | 3f3271a fix: provider-docker Windows named-pipe transport | 2451dec chore: remove provider-docker Windows slop | aac977c docs: record US-077 cleanup
- PR: #175 https://github.com/AaronFeledy/core4/pull/175 (created)
---
## [2026-05-27 04:48 UTC] - Push
- Branch: us-077-provider-docker-windows
- Push: pushed
- Commit(s): 1031e1e fix: keep npipe stream on one consumer
- PR: #175 https://github.com/AaronFeledy/core4/pull/175 (updated)
---

## [2026-05-27] - PR #175 triage
- Patched `plugins/provider-docker/src/index.ts` so chunked named-pipe streams flush a final buffered chunk even if the socket closes before the trailing CRLF arrives.
- Added a socket-backed regression test in `plugins/provider-docker/test/contract.integration.test.ts` that exercises the named-pipe chunked stream path with an unterminated final chunk.

## [2026-05-27 05:51 UTC] - Sync (uncommitted changes assessment)
- Found uncommitted changes to `plugins/provider-docker/src/index.ts` (refactored `decodeChunkedBody` from async generator to sync function reusing existing helpers) plus `spec/beta/progress.txt` progress note from previous iteration.
- Assessment: changes are entangled with in-progress story work on `us-077-provider-docker-windows` / PR #175 (directly address Bugbot comments "Npipe request drops partial chunks" and "Duplicate chunked body decoders"). Not eligible for standalone PR.
- Stashed as: loop-sync-rejected-improvement-20260527T055121Z (stash@{0})
- Note: stash is recoverable for use by push.md Phase 4 triage on branch `us-077-provider-docker-windows`.
---

## [2026-05-27 05:52 UTC] - Sync
- Previous branch: us-077-provider-docker-windows
- Previous PR: #175 https://github.com/AaronFeledy/core4/pull/175
- Action: merged (Case A — all CI checks SUCCESS/NEUTRAL, MERGEABLE, CLEAN, no CHANGES_REQUESTED)
- Local main updated: yes
- Branch after sync: main
- Stash created: yes (loop-sync-rejected-improvement-20260527T055121Z — entangled story-branch index.ts changes from failed final triage; dropped as moot after merge)
---

## [2026-05-27 06:30 UTC] - US-078
- Shipped `@lando/provider-podman` as an opt-in user-installed Podman `RuntimeProvider` for Linux (with darwin/win32 capability matrix in place for US-079).
- Files changed:
  - `plugins/provider-podman/package.json`, `plugin.yaml`, `tsconfig.json`, `src/provider.ts`: new workspace package metadata mirroring `@lando/provider-docker`; depends on `@lando/provider-lando` to reuse the Podman REST API client and bringUp/bringDown/exec/logs/inspect lifecycle helpers.
  - `plugins/provider-podman/src/index.ts` (new): `resolvePodmanSocket` (precedence: explicit > `LANDO_TEST_PODMAN_SOCKET` > `DOCKER_HOST` (with `unix://` stripped) > `$XDG_RUNTIME_DIR/podman/podman.sock` > `/run/podman/podman.sock`); `podmanCapabilitiesForPlatform` (`native` on linux, `slow` on darwin/win32, `rootless: true`, `sharedCrossAppNetwork: false`); `ProviderLandoConflictError` (tagged subclass of `ProviderUnavailableError`); `detectProviderLandoConflict` (Effect-typed file reader against `<stateDir>/provider-lando/setup-state.json`, normalizes socket comparison by stripping `unix://` and trailing slashes); `makeRuntimeProvider`/`makeProviderLayer`/`provider`/`manifest`. The probe still uses provider-lando's `introspectProviderCapabilities` to confirm the socket is reachable, then overrides the capability map with the podman-specific matrix.
  - `plugins/provider-podman/test/{capabilities,conflict,contract.integration,import-boundary}.test.ts`: capability matrix shape + bind-mount classification per platform, socket discovery precedence, conflict detection (including normalization edge cases and the `conflictDetector` test seam), SDK contract suite via `@lando/sdk/test#runProviderContract` with a fake Podman API client, and the no-OCLIF/no-core/cli import boundary guard.
  - `plugins/provider-lando/src/setup.ts`: additively persists `socketPath` in `setup-state.json` when supplied to `setupProviderLando`; existing readers/tests still see the same shape because no `LANDO_TEST_PODMAN_SOCKET` was set in their fixtures.
  - `core/build.config.ts`: added `@lando/provider-podman` to `bundledPlugins` so it ships with the binary but stays opt-in (registry only resolves it when explicitly selected).
  - `core/src/plugins/bundled.ts`: regenerated via `bun run codegen:bundled-plugins`.
  - `core/src/providers/registry.ts`: added a `podman` branch that calls `makePodmanRuntimeProvider({ stateDir })` so conflict detection wires through `<userDataRoot>/providers`.
  - `core/test/plugins/bundled.test.ts`, `core/test/services/plugin-registry.test.ts`, `core/test/services/runtime-provider-registry.test.ts`: updated bundled-plugin assertions to include provider-podman.
  - `docs/guides/setup/provider-podman-linux.mdx` (new): required guide coverage with non-rendered scenarios for socket discovery, opt-in selection, and provider-lando conflict diagnostics.
  - `.github/workflows/ci.yml`, `sdk/test/fixtures/schema-snapshot.json`: auto-regenerated to include the new plugin.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/` → 26 pass, 1 skip, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-lando/` → 97 pass, 8 skip, 0 fail (additive `socketPath` did not regress any existing setup-state assertion)
  - `PATH="$HOME/.bun/bin:$PATH" bun test core/test/services/runtime-provider-registry.test.ts core/test/services/plugin-registry.test.ts core/test/plugins/bundled.test.ts` → 13 pass, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test test/scenarios/generated/guides/provider-podman-linux/` → 3 pass, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1254-1259 pass / 6 fail (varies). The remaining failures are the order/parallelism-sensitive flakes already documented in earlier progress notes (`compiled Linux x64 binary`, `docs:scenario author command`, `ci workflow codegen`, `compiled app command aliases`); each passes when re-run in isolation (`core/test/build/compile.test.ts`, `core/test/scripts/docs-scenario.test.ts`, `core/test/build/ci-workflow.test.ts`, `core/test/cli/aliases.test.ts`).
  - Manual CLI smoke: instantiated `makeRuntimeProvider({ podmanApi: { info: Effect.succeed({}) } })` programmatically, confirmed `id: "podman"`, `displayName: "Podman Runtime Provider (user-installed)"`, `bindMountPerformance: "native"`, conflict path emits `ProviderLandoConflictError` with the `lando setup --provider=...` remediation when `<stateDir>/provider-lando/setup-state.json` records a matching socket.
- **Learnings for future iterations:**
  - Opt-in providers in this codebase mean *bundled but non-default*. `RuntimeProviderRegistryLive` only resolves what `PluginRegistry.list` reports; the plugin still needs to be in `core/build.config.ts` to be discoverable. There is no out-of-tree plugin discovery surface yet — that would be a larger plumbing change in a separate story.
  - The `RuntimeProviderRegistry` does **not** dispatch through plugin Layers automatically. Each new provider id needs a dedicated branch in `core/src/providers/registry.ts` `providerFor`, similar to the existing `lando`/`docker` branches. Until a generic dispatch lands, expect this file to grow one branch per provider id.
  - `BUNDLED_PLUGINS`-shape assertions show up in three places (`core/test/plugins/bundled.test.ts`, `core/test/services/plugin-registry.test.ts`, `core/test/services/runtime-provider-registry.test.ts`); all three must be updated together whenever the bundled list changes, otherwise the full suite will fail with hard-coded array mismatches.
  - Tagged subclasses of SDK errors (e.g. `ProviderLandoConflictError extends ProviderUnavailableError`) preserve `instanceof` checks for both classes, so callers in the registry that map `ProviderCapabilityError | ProviderUnavailableError` to a final `ProviderUnavailableError` keep working without special-casing the subclass.
  - When `provider-podman` reuses provider-lando's lifecycle helpers, the underlying tagged errors still embed `providerId: "lando"`. That is acceptable for US-078 (the user-visible surface is the registry-level error and the inspect snapshot `providerId`, both of which read `podman`); a full identity rewrite would require duplicating the lifecycle and is out of scope.
---

## [2026-05-27 02:11 CDT] - US-078 Review
- Reviewed the US-078 `@lando/provider-podman` Linux opt-in provider implementation against `spec/beta/prd-beta-01-provider-matrix.md`, the HEAD build-step commit, direct checks, and specialist `review-work` findings.
- Fixed confirmed review blockers:
  - Wired `lando setup --provider=podman` through the source OCLIF setup command and the compiled dispatcher so the documented opt-in/remediation path selects the requested provider instead of the configured default.
  - Added root typecheck coverage for `plugins/provider-podman` by adding the package to the root `tsconfig.json` references.
  - Hardened provider-podman socket handling to default to rootless `/run/user/$UID/podman/podman.sock` when `XDG_RUNTIME_DIR` is unavailable and to reject non-Unix `DOCKER_HOST` transports before constructing the Unix-socket API client.
  - Made provider-lando conflict-state parsing fail closed on malformed/non-string state instead of treating unreadable/corrupt setup state as no conflict.
  - Persisted provider-podman applied plans under `provider-podman/apps` so follow-up CLI invocations can inspect/exec/logs/destroy without an in-memory plan.
- Files changed:
  - `core/src/cli/oclif/commands/meta/setup.ts`
  - `core/src/cli/run.ts`
  - `core/test/cli/setup.test.ts`
  - `docs/guides/setup/provider-podman-linux.mdx`
  - `plugins/provider-podman/src/index.ts`
  - `plugins/provider-podman/test/capabilities.test.ts`
  - `plugins/provider-podman/test/conflict.test.ts`
  - `plugins/provider-podman/test/contract.integration.test.ts`
  - `tsconfig.json`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: `--provider=podman` was declared and referenced by conflict remediation but ignored by setup selection.
  - Fixed: root `bun run typecheck` did not include the new provider-podman workspace package.
  - Fixed: non-Unix `DOCKER_HOST`/Windows npipe values could be passed into the reused Unix-socket Podman client.
  - Fixed: Linux discovery could silently fall back to the rootful `/run/podman/podman.sock` while advertising `rootless: true`.
  - Fixed: provider-lando conflict detection failed open on malformed setup state.
  - Fixed: provider-podman did not persist applied plans across provider instances / CLI invocations.
  - Rejected/deferred: provider-lando production setup often lacks a recorded managed socket unless a socket path is injected; fully deriving the private runtime-bundle socket is provider-lando setup plumbing beyond this US-078 review fix, while the US-078 conflict path now fails closed when state is malformed and rejects when a socket is recorded.
  - Rejected/deferred: literal non-auto-loading concern; repo convention for opt-in providers is bundled/discoverable but not default-selected, matching the US-078 build-step learning and later provider-selection stories.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/ core/test/cli/setup.test.ts` → 34 pass, 1 skip, 0 fail.
  - `PATH="$HOME/.bun/bin:$PATH" bun test sdk/test/library/sdk-backward-compatibility.test.ts core/test/services/runtime-provider-registry.test.ts core/test/services/plugin-registry.test.ts core/test/plugins/bundled.test.ts test/scenarios/generated/guides/provider-podman-linux/ plugins/provider-lando/` → 116 pass, 8 skip, 0 fail.
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides docs/guides/setup/provider-podman-linux.mdx` → Guide lint passed.
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us078-review-full.xml` → 1265 pass, 15 skip, 0 fail.
  - `git diff --check` → clean.
  - LSP diagnostics were attempted for changed TypeScript files, but the TypeScript LSP server exited with code -2; `tsc -b` was clean.
- **Learnings for future iterations:**
  - Declaring a provider-selection CLI flag is not enough; setup-like commands must thread parsed flags into `RuntimeProviderRegistry.select` in both source and compiled dispatch paths.
  - New workspace packages must be added to root `tsconfig.json` references or the repo-wide typecheck gate does not cover them.
  - Provider-podman reuses provider-lando's Unix-socket Podman transport, so US-078 should reject TCP/npipe overrides until a transport-specific client lands in US-079 or later.
  - User-installed provider lifecycle state should live under the provider's own state root (`provider-podman/apps`) rather than relying on process-local plan maps.
---

## [2026-05-27 03:06 CDT] - US-078 Cleanup
- Ran branch-wide AI slop cleanup after review and before push.
- Accepted cleanup:
  - `plugins/provider-podman/src/index.ts`: removed two code-comment references to follow-up story tracking while preserving the technical platform description.
- Rejected cleanup proposals as not purely cosmetic / not needed for planning-comment leakage:
  - `.github/workflows/ci.yml`: restored generated workflow comment block because generated files should not be hand-edited for cosmetic cleanup.
  - `core/src/providers/registry.ts`: restored durable provider-registry module documentation.
  - `plugins/provider-podman/test/capabilities.test.ts`: restored explanatory precedence comments after the per-file agent failed and left only comment deletions.
  - `plugins/provider-podman/test/conflict.test.ts`: restored helper refactor because this cleanup step should not restructure tests.
- Planning-comment scan:
  - No branch-changed code comments still reference spec paths, sections, development phases, PRDs, or story IDs.
  - Remaining matches are runtime remediation fields/messages, schema property names, test names, or guide frontmatter/prose that are outside the code-comment cleanup rule.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 (`Checked 496 files in 310ms. No fixes applied.`)
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → exit 0
  - `git diff --check` → clean
  - `lsp_diagnostics plugins/provider-podman/src/index.ts` attempted; TypeScript LSP exited with code -2, matching the review-step LSP instability already noted.

## [2026-05-27 10:13 CDT] - Sync
- Previous branch: us-078-provider-podman-linux
- Previous PR: #176 https://github.com/AaronFeledy/core4/pull/176
- Action: dispatched leftover push (Case E, 6 commits unpushed, no PR). Ran push+babysit inline. CI required 1 flaky-build re-run and 4 triage rounds: (1) fix test fixture missing socketPath field; (2) Bugbot — fix eventService forwarding, list persisted plans, rememberPlan synchronous mutation; (3) Bugbot — fix rememberPlan Effect deferral; (4) Bugbot — fix forgetPlan eager map delete. All checks SUCCESS on final run, Bugbot found no new issues on latest commit. Merged via squash.
- Local main updated: yes (b67e500)
- Branch after sync: main
- Stash created: no
---

## [2026-05-27 11:50 CDT] - US-079
- Implemented `@lando/provider-podman` Podman Desktop coverage for macOS + Windows.
- Files changed:
  - `plugins/provider-podman/src/index.ts`: added `resolvePodmanDesktopMachine`/`resolvePodmanDesktopMachineName` (precedence: `LANDO_PODMAN_MACHINE` > `PODMAN_MACHINE_NAME` > `podman-machine-default`, with strict `[A-Za-z0-9][A-Za-z0-9._-]*` validation), `discoverPodmanDesktopSockets` (override-first probe order, `[]` on Linux), and `PodmanMachineNotRunningError` (tagged subclass of `ProviderUnavailableError`); wired the new error into the capability/info probe so connection-style failures on `darwin`/`win32` surface a Podman Desktop remediation that includes both `podman machine start <machine>` and the "open Podman Desktop" GUI hint.
  - `plugins/provider-podman/test/capabilities.test.ts`: extended socket-discovery coverage with `LANDO_PODMAN_MACHINE`/`PODMAN_MACHINE_NAME` override cases for macOS and Windows, malformed-machine-name rejection, and dedicated `discoverPodmanDesktopSockets` + `resolvePodmanDesktopMachine` tests.
  - `plugins/provider-podman/test/desktop.integration.test.ts` (new): fake-client integration tests covering `apply` / `inspect` / `exec` / `logs` / `destroy` for both `darwin` and `win32`, plus `PodmanMachineNotRunningError` mapping (with the Linux non-mapping case as a regression guard) and a `LANDO_TEST_PODMAN_DESKTOP_SOCKET`-gated live contract test that runs `runProviderContract` against a real Podman Desktop socket.
  - `core/src/cli/commands/doctor.ts`: derived `providerKind` ("managed" for `provider-lando`, "user-installed" for every other id) and rendered it in both the text output and the NDJSON `doctor.check` payload + `context`; updated ordering helpers so the new field has a stable position.
  - `core/test/cli/doctor.test.ts`, `core/test/cli/fixtures/meta-doctor.provider-status.ndjson`, `core/test/cli/fixtures/meta-doctor.provider-status.windows.ndjson`: added `providerKind` assertions per provider id and regenerated the golden NDJSON fixtures to include the new field.
  - `docs/guides/setup/provider-podman-macos.mdx` (new), `docs/guides/setup/provider-podman-windows.mdx` (new): required guide coverage with non-rendered scenarios for Podman Desktop discovery (default + LANDO_PODMAN_MACHINE override), machine-not-running remediation, and the `LANDO_TEST_PODMAN_DESKTOP_SOCKET` live gate.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → "Guide lint passed."
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/` → 51 pass, 2 skip, 0 fail (the 2 skips are the existing `LANDO_TEST_PODMAN_SOCKET` Linux live test and the new `LANDO_TEST_PODMAN_DESKTOP_SOCKET` gated live test).
  - `PATH="$HOME/.bun/bin:$PATH" bun test core/test/cli/doctor.test.ts` → 14 pass, 1 skip (the existing Windows live provider-lando test).
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1295 pass, 16 skip, 1 fail; the single failure was the order-sensitive `compiled $bunfs CLI` deferred-stub remediation parity test, which passes when its file (`core/test/cli/deferred-commands.test.ts`) is rerun in isolation (36 tests, 0 fail). Matches the compiled-binary flaky pattern documented in the US-074/US-075 progress notes.
  - Manual CLI smoke (build step): direct invocation of `doctor()` + `renderDoctorResult()` / `renderDoctorResultAsNdjson()` against `TestRuntimeProvider` with overridden ids confirmed `providerKind: managed` for `id="lando"` and `providerKind: user-installed` for `id` in `{podman, docker, third-party}` in both text and NDJSON outputs.
  - Manual API smoke: `resolvePodmanSocket` / `discoverPodmanDesktopSockets` returned the expected paths for default + `LANDO_PODMAN_MACHINE` override on `darwin` and `win32`, and `[]` on `linux`.
- **Learnings for future iterations:**
  - `RuntimeProviderShape` is on the SDK compat lock (`sdk/test/library/sdk-backward-compatibility.test.ts`); any new field — even optional — fails the frozen-signature equality check. Surface presentation-only distinctions like `providerKind` in the consuming `core/` CLI command (mapped by provider id) instead of widening the SDK service tag.
  - Provider-lando's bringUp lifecycle inspect (`GET /containers/<name>/json`) decides whether to call `POST /containers/create?name=...`; a fake API that always returns `200` for `/json` makes `create` look like dead code in tests. Track the `existing` set in your fake and return `404` for unknown containers so apply tests actually exercise the create path.
  - `/exec/<id>/json` ends with `/json` and will collide with the container-inspect handler if the latter only matches by `.endsWith("/json")`. Always gate the container-inspect branch with `.startsWith("/containers/")` (or order the exec handler first) when assembling Podman REST API fakes.
  - When mapping low-level Podman API connection failures (`ENOENT`, `ECONNREFUSED`, "named pipe not found", "system cannot find the path") to a Podman Desktop "machine not running" error, walk the error chain (`message`, `stderr`, `code`, `cause`) — the typed `ProviderUnavailableError` from `provider-lando` carries the real cause one level deeper.

---
## [2026-05-27 11:31 CDT] - US-079 Review
- Reviewed the US-079 `@lando/provider-podman` Podman Desktop implementation against `spec/beta/prd-beta-01-provider-matrix.md` and the build-step commit (`HEAD`).
- Fixed confirmed review blockers:
  - Added a Windows named-pipe Podman API client path and routed exported/default `makePodmanApiClient()` through it for `npipe:` sockets, so the Windows Podman Desktop default is no longer fake-client-only.
  - Allowed provider-podman's default Windows `npipe://./pipe/podman-machine-default` path while preserving fail-closed rejection for unsupported non-Unix/non-npipe transports.
  - Converted malformed Podman Desktop machine names into a tagged `InvalidPodmanMachineNameError` and added rejection coverage.
  - Used the effective process environment for Podman Desktop machine-not-running remediation, so env-selected machines do not report `podman-machine-default` incorrectly.
  - Tightened the live Podman Desktop contract gate to require macOS/Windows in addition to `LANDO_TEST_PODMAN_DESKTOP_SOCKET`.
- Files changed:
  - `plugins/provider-podman/src/index.ts`
  - `plugins/provider-podman/src/named-pipe.ts`
  - `plugins/provider-podman/test/capabilities.test.ts`
  - `plugins/provider-podman/test/desktop.integration.test.ts`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: Windows Podman Desktop resolved to `npipe://...` but the real provider rejected non-`unix://` sockets.
  - Fixed: the real Podman API client was Unix-socket-only, so the documented Windows live `npipe://...` path could not run.
  - Fixed: live Podman Desktop test was env-gated but not platform-gated.
  - Fixed: machine-not-running remediation could use the default machine name even when `process.env` selected a custom machine.
  - Fixed: malformed machine-name rejection was claimed in the progress log but lacked test coverage and threw a raw error.
  - Rejected: guide-coverage table omission for US-079 is a PRD ownership/documentation drift concern, not a story blocker; this review kept the existing US-079 guide files and did not expand PRD governance scope.
- Verification:
  - LSP diagnostics attempted on changed TypeScript files; TypeScript LSP exited with code -2, matching prior local LSP behavior.
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/ --reporter=junit --reporter-outfile=/tmp/us079-review-provider-podman-all.xml` → 56 tests, 112 assertions, 0 failures, 2 skips.
  - `PATH="$HOME/.bun/bin:$PATH" bun test core/test/cli/doctor.test.ts --reporter=junit --reporter-outfile=/tmp/us079-review-doctor.xml` → 15 tests, 102 assertions, 0 failures, 1 skip.
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us079-review-full.xml` → 1315 tests, 6410 assertions, 0 failures, 16 skips.
- **Learnings for future iterations:**
  - Discovery tests are insufficient when a provider has scheme-specific transports; add a factory-path regression that the default platform socket reaches the real client factory instead of stopping at validation.
  - For platform-specific live tests, gate on both the exact opt-in environment variable and the expected host platform so accidental CI env leakage cannot exercise host runtimes on the wrong OS.
  - When a remediation references a selected machine name, derive both socket resolution and error text from one effective environment object.
---
## [2026-05-27 16:54 UTC] - US-079 Cleanup
- Ran the branch-wide `remove-ai-slops` cleanup after review and before push with the planning-doc comment rules.
- Cleanup result: cosmetic-only cleanup; no provider behavior, public API, socket discovery precedence, named-pipe transport, doctor output semantics, test assertions, fixtures, docs, or generated/planning files changed.
- Files changed:
  - `core/src/cli/commands/doctor.ts`: reworded the module header to remove planning-section citations while preserving the durable diagnostic summary.
  - `plugins/provider-podman/src/index.ts`: removed one redundant setup comment and shortened the `LANDO_TEST_PODMAN_SOCKET` precedence note without changing socket resolution.
  - `plugins/provider-podman/test/capabilities.test.ts`: removed redundant inline comments that restated the socket-precedence assertions.
- Slop-remover output: source/test/doc cleanup agents reported no safe edits for `named-pipe.ts`, doctor tests, desktop integration tests, and Podman Desktop guide files; accepted only the cosmetic source/test comment edits above.
- Planning-doc leakage scan: no branch-changed source/test comments still cite planning docs, section markers, development phases, PRDs, story IDs, or story prose.
- Verification: `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/ core/test/cli/doctor.test.ts` → exit 0; first full `bun test --reporter=junit --reporter-outfile=/tmp/us079-cleanup-full.xml` run hit the known order-sensitive `core/test/cli/deferred-commands.test.ts` compiled `$bunfs` parity failure, which passed in isolation; rerun `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us079-cleanup-rerun.xml` → 1315 tests, 6410 assertions, 0 failures, 16 skips; `git diff --check` → clean. LSP diagnostics were attempted for changed TypeScript files, but the TypeScript LSP servers exited with code -2.
---

## [2026-05-27 17:03 UTC] - Push
- Branch: us-079-provider-podman-desktop
- Push: pushed
- Commit(s): 3c1bd41 feat: @lando/provider-podman on macOS + Windows via Podman Desktop; 88ec019 fix(provider-podman): support Windows Podman Desktop npipe; 381bac2 docs: record US-079 review fixes; 903000b chore: remove AI slop
- PR: #178 https://github.com/AaronFeledy/core4/pull/178 (created)
---

## [2026-05-27 17:20 UTC] - US-079 Bugbot triage
- Fixed the two remaining low-severity Cursor Bugbot findings in `plugins/provider-podman/src/index.ts`:
  - removed the unused `resolvePodmanDesktopMachineName` alias export and updated the remaining callsite to use `resolvePodmanDesktopMachine` directly
  - tightened the machine-not-running pattern set by dropping the broad `/named pipe/i` matcher
- Verification: `PATH="$HOME/.bun/bin:$PATH" bun run --filter='@lando/provider-podman' typecheck` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/test/capabilities.test.ts` → 26 pass, 0 fail; `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/test/desktop.integration.test.ts` → 14 pass, 1 skip, 0 fail; `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/` → 54 pass, 2 skip, 0 fail.
---

## [2026-05-27 17:45 UTC] - US-079 npipe regression fix
- Fixed the Windows Podman Desktop named-pipe stream flush path so a complete final chunk is not dropped when the socket closes before the terminal CRLF is buffered.
- Files changed:
  - `plugins/provider-podman/src/named-pipe.ts`: replaced the end-of-stream `flushChunkedBufferAtEnd` helper with the same permissive chunk flush logic used by `provider-docker`, so the final chunk is yielded even if the trailing chunk terminator arrives split or missing at close.
  - `plugins/provider-podman/src/index.ts`: re-exported `flushChunkedBufferAtEnd` for the regression test.
  - `plugins/provider-podman/test/named-pipe.test.ts` (new): regression coverage for a final chunk without trailing CRLF and for a complete chunked frame.
- Verification: `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/test/named-pipe.test.ts` → 2 pass, 0 fail; `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/` → 56 pass, 2 skip, 0 fail.
---

## [2026-05-27 17:36 UTC] - US-079 PR #178 Bugbot triage round 2
- Triaged the two new Cursor Bugbot comments on commit `cdd728e`:
  - Comment 1 (`plugins/provider-podman/src/index.ts` L574-581, "Throwing `resolvePodmanDesktopMachine` inside `Effect.mapError` causes untyped defect") - **real issue, fixed**. When the socket path was supplied via `options.socketPath`, `DOCKER_HOST`, or `LANDO_TEST_PODMAN_SOCKET`, `resolvePodmanSocket` never validated `LANDO_PODMAN_MACHINE` / `PODMAN_MACHINE_NAME`; a later `isLikelyMachineNotRunning` branch in the capability-probe `Effect.mapError` then called `resolvePodmanDesktopMachine(effectiveEnv)` synchronously, which on an invalid machine name throws `InvalidPodmanMachineNameError` into the Effect runtime as a `Cause.Die` defect instead of a typed `ProviderUnavailableError`. Fix: resolve the desktop machine name eagerly at the top of `makeRuntimeProvider` inside a `try`/`catch` that returns `Effect.fail(cause)` for `ProviderUnavailableError`, then reuse the resolved value inside the `mapError` branch so no synchronous throw can escape from there.
  - Comment 2 (`plugins/provider-podman/src/named-pipe.ts` whole file, "Named-pipe transport duplicates ~300 lines from provider-docker") - **deferred, non-blocking for this PR**. The duplication is real but extracting a shared npipe transport is a cross-package refactor that touches `provider-docker`, `provider-podman` `package.json`/`tsconfig.json`, and the integration test surface; it is architectural cleanup, not a correctness bug, and is out of scope for this story's PR. Filed locally as a stash (`pr178-round2-named-pipe-refactor-deferred`) so the spike work is not lost; the proper home is a follow-up refactor PR scoped to a shared npipe transport with its own tests.
- Files changed:
  - `plugins/provider-podman/src/index.ts`: eager `resolvePodmanDesktopMachine(effectiveEnv)` call with typed-error catch; `Effect.mapError` branch now reuses the resolved name instead of calling the throwing helper inline.
- Verification: pending - `bun run typecheck`, `bun run lint`, `bun test plugins/provider-podman/`, then full `bun test` before push. Results recorded below once green.

## [2026-05-27 17:46 UTC] - US-079 PR #178 Bugbot triage round 2 push
- Branch: us-079-provider-podman-desktop
- Commit pushed: c776d37 fix(provider-podman): resolve Podman Desktop machine name eagerly
- Verification (post-commit, clean working tree):
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 (`biome check .` Checked 499 files, no fixes applied)
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/` → 56 pass, 2 skip, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us079-round2-postcommit.xml` → 1301 pass, 16 skip, 0 fail across 177 files / 144s
- Coordination note: the cross-package shared-named-pipe extraction explored during this round was reverted as out-of-scope and stashed locally as `pr178-round2-named-pipe-refactor-deferred` per the orchestrator's mandate to keep this PR scoped to the typed-error fix. The duplication concern from Bugbot comment 2 remains open for a follow-up refactor PR.

## [2026-05-27 18:09 UTC] - US-079 PR #178 Bugbot triage round 3 push
- Triaged Cursor Bugbot's third review (commit `8e92a9b`, 1 new + 1 carried-over unresolved):
  - New comment (`plugins/provider-podman/src/index.ts` L573, "Eager machine-name validation fails Linux provider unnecessarily", Low) - **real issue, fixed**. The eager `resolvePodmanDesktopMachine(effectiveEnv)` call ran unconditionally even though `desktopMachineName` is only consumed inside the `(platform === "darwin" || platform === "win32")` branch of the capability-probe `Effect.mapError`. On Linux an invalid `LANDO_PODMAN_MACHINE`/`PODMAN_MACHINE_NAME` would fail provider init with `InvalidPodmanMachineNameError` even though the rootless socket path never uses the machine name. Fix: wrap the eager resolver in `if (platform === "darwin" || platform === "win32")` so Linux ignores the desktop machine env vars; keep the typed-error catch unchanged for darwin/win32.
  - Carried-over comment (`plugins/provider-podman/src/named-pipe.ts` whole file, "Named-pipe transport duplicates ~300 lines from provider-docker", Medium) - **still deferred, non-blocking for this PR**. Same reasoning as round 2: the duplication is architectural cleanup, not a correctness bug, and extracting a shared npipe transport is a cross-package refactor touching `provider-docker`, `provider-podman`, `package.json`/`tsconfig.json`, and integration test surface. Stash `pr178-round2-named-pipe-refactor-deferred` preserves the spike work for a follow-up refactor PR.
- Branch: us-079-provider-podman-desktop
- Commit pushed: 7620bba fix(provider-podman): gate desktop machine validation
- Files changed:
  - `plugins/provider-podman/src/index.ts`: gated the eager `resolvePodmanDesktopMachine` call behind `platform === "darwin" || platform === "win32"`; the `Effect.mapError` branch keeps its `desktopMachineName ?? resolvePodmanDesktopMachine(effectiveEnv)` fallback (only reachable on darwin/win32 where the eager block already returned a typed `ProviderUnavailableError` on invalid names).
  - `plugins/provider-podman/test/capabilities.test.ts`: new Linux regression test asserts `makeRuntimeProvider({ platform: "linux", env: { ..., LANDO_PODMAN_MACHINE: "bad;name" } })` resolves successfully and does not surface `InvalidPodmanMachineNameError`.
- Verification (post-commit, clean working tree on `7620bba`):
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 (`biome check .` Checked 499 files, no fixes applied)
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/` → 57 pass, 2 skip, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us079-round3-postcommit.xml` → 1302 pass, 16 skip, 0 fail across 177 files / 128.75s

## [2026-05-27 18:21 UTC] - US-079 PR #178 Bugbot triage round 4 push
- Triaged Cursor Bugbot's fourth review (commit `3211323`, 1 new + 1 carried-over unresolved):
  - New comment (`plugins/provider-podman/src/named-pipe.ts` L205, "Socket not destroyed on named-pipe connection failure", Low) - **real issue, fixed**. Both named-pipe request paths created a socket and awaited connect before entering the existing `try`/`finally`; if the pipe connection failed before `connect`, the function rejected without destroying the socket. Fix: extracted `connectNamedPipeSocket(socket)` so the one-shot `connect`/`error` listeners clean each other up and the socket is destroyed on pre-connect error before rethrowing.
  - Carried-over comment (`plugins/provider-podman/src/named-pipe.ts` whole file, "Named-pipe transport duplicates ~300 lines from provider-docker", Medium) - **still deferred, non-blocking for this PR**. The shared npipe transport extraction remains a follow-up cross-package refactor, not a correctness blocker for this PR.
- Files changed:
  - `plugins/provider-podman/src/named-pipe.ts`: added `connectNamedPipeSocket` and reused it in both named-pipe request paths.
  - `plugins/provider-podman/test/named-pipe.test.ts`: added regression coverage that a socket receiving `error` before `connect` is destroyed and the original error is rethrown.
- Verification (pre-commit):
  - `PATH="$HOME/.bun/bin:$PATH" bun run --filter='@lando/provider-podman' typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/test/named-pipe.test.ts` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 (`biome check .` Checked 499 files, no fixes applied)

## [2026-05-27 18:33 UTC] - US-079 PR #178 Bugbot triage round 5 push
- Triaged Cursor Bugbot's fifth review (commit `cd07aa6`, 1 new + 1 carried-over unresolved):
  - New comment (`plugins/provider-podman/src/named-pipe.ts` L338, "Named pipe info errors bypass machine-not-running detection", Medium) - **real issue, fixed**. The named-pipe `info` path wrapped connect failures as `ProviderCapabilityError`, so `makeRuntimeProvider` never entered its `ProviderUnavailableError` machine-not-running branch. Fix: route named-pipe info connect failures through the same transport failure mapper as request/stream, keep the raw connection error in the standard `cause` field, and recognize `ENOENT` in the Podman Desktop machine-not-running pattern set.
  - Carried-over comment (`plugins/provider-podman/src/named-pipe.ts` whole file, "Named-pipe transport duplicates ~300 lines from provider-docker", Medium) - **still deferred, non-blocking for this PR** as follow-up cross-package refactor work.
- Files changed:
  - `plugins/provider-podman/src/named-pipe.ts`: added `namedPipeInfoFailure` and reused it for the `info` catch path; `podmanApiFailure` now puts the raw transport error in `cause` rather than nested in `details`.
  - `plugins/provider-podman/src/index.ts`: added `ENOENT` to the Podman Desktop machine-not-running detector.
  - `plugins/provider-podman/test/named-pipe.test.ts`: added deterministic regression coverage that a named-pipe info ENOENT maps through `makeRuntimeProvider` to machine remediation.
- Verification (pre-commit):
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/test/named-pipe.test.ts` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run --filter='@lando/provider-podman' typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 (`biome check .` Checked 499 files, no fixes applied)
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/` → exit 0

## [2026-05-27 18:41 UTC] - US-079 PR #178 Bugbot triage round 6 push
- Triaged Cursor Bugbot's sixth review (commit `058117d`, 1 new + 1 carried-over unresolved):
  - New comment (`plugins/provider-podman/src/index.ts` L148, "Duplicate ENOENT pattern in machine-not-running detection array", Low) - **real issue, fixed**. Round 5 added `/ENOENT/i` near the end of `MACHINE_NOT_RUNNING_PATTERNS`, but the array already had `/ENOENT/i` as its first pattern. Fix: remove the duplicate later entry and keep the existing first pattern, preserving behavior.
  - Carried-over comment (`plugins/provider-podman/src/named-pipe.ts` whole file, "Named-pipe transport duplicates ~300 lines from provider-docker", Medium) - **still deferred, non-blocking for this PR** as follow-up cross-package refactor work.
- Files changed:
  - `plugins/provider-podman/src/index.ts`: removed duplicate `/ENOENT/i` entry from `MACHINE_NOT_RUNNING_PATTERNS`.
- Verification (pre-commit):
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/test/named-pipe.test.ts` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run --filter='@lando/provider-podman' typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 (`biome check .` Checked 499 files, no fixes applied)

## [2026-05-27 18:52 UTC] - US-079 PR #178 Bugbot triage round 7 push
- Triaged Cursor Bugbot's seventh review (commit `dcc26ba`, 2 new + 1 carried-over unresolved):
  - New comment (`plugins/provider-podman/src/index.ts` L203, "Exported `discoverPodmanDesktopSockets` is unused in production code", Low) - **real issue, fixed**. `defaultPodmanSocket` now routes macOS/Windows default socket selection through `discoverPodmanDesktopSockets`, keeping discovery as the shared production path instead of a test-only/export-only helper.
  - New comment (`plugins/provider-podman/src/index.ts` L216, "Machine name resolved unnecessarily before HOME check on macOS", Low) - **real issue, fixed**. `discoverPodmanDesktopSockets` now checks `HOME` before resolving/validating a macOS machine name, so the `/var/run/podman/podman.sock` fallback path does not reject unrelated invalid machine-name env vars when `HOME` is unavailable.
  - Carried-over comment (`plugins/provider-podman/src/named-pipe.ts` whole file, "Named-pipe transport duplicates ~300 lines from provider-docker", Medium) - **still deferred, non-blocking for this PR** as follow-up cross-package refactor work.
- Files changed:
  - `plugins/provider-podman/src/index.ts`: share default Podman Desktop socket selection through `discoverPodmanDesktopSockets`; check macOS `HOME` before machine-name resolution.
- Verification (pre-commit):
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/test/capabilities.test.ts` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run --filter='@lando/provider-podman' typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 (`biome check .` Checked 499 files, no fixes applied)

## [2026-05-27 19:00 UTC] - US-079 PR #178 Bugbot triage round 8 push
- Triaged Cursor Bugbot's eighth review (commit `166814a`, 1 new + 1 carried-over unresolved):
  - New comment (`plugins/provider-podman/src/named-pipe.ts` L321, "Request path missing chunked flush at end-of-stream", Low) - **real issue, fixed**. The non-streaming named-pipe request path used `decodeChunkedBody` but did not flush a final partial chunk when the socket closed without the trailing CRLF. Fix: `decodeChunkedBody` now flushes any remaining buffer with `flushChunkedBufferAtEnd` after the async iterable ends, matching the streaming path behavior.
  - Carried-over comment (`plugins/provider-podman/src/named-pipe.ts` whole file, "Named-pipe transport duplicates ~300 lines from provider-docker", Medium) - **still deferred, non-blocking for this PR** as follow-up cross-package refactor work.
- Files changed:
  - `plugins/provider-podman/src/named-pipe.ts`: exported `decodeChunkedBody` for regression coverage and added final-buffer flush.
  - `plugins/provider-podman/test/named-pipe.test.ts`: added regression coverage that `decodeChunkedBody` yields the final chunk when the trailing CRLF is absent at end-of-stream.
- Verification (pre-commit):
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/test/named-pipe.test.ts` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run --filter='@lando/provider-podman' typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 (`biome check .` Checked 499 files, no fixes applied)

## [2026-05-27 19:16 UTC] - US-079 PR #178 Bugbot triage round 9 push
- Triaged Cursor Bugbot's ninth review (commit `46e2605`, 2 new + 1 carried-over unresolved):
  - New comment (`plugins/provider-podman/src/index.ts` L223, "Windows fallback in `defaultPodmanSocket` is unreachable dead code", Low) - **real issue, fixed**. The Windows path now resolves the selected machine directly via `windowsPodmanDesktopSocket(resolvePodmanDesktopMachine(env))`, removing the unreachable nullish fallback without adding a non-null assertion.
  - New comment (`plugins/provider-podman/src/index.ts` L48, "Test-only helpers re-exported from public package API", Low) - **real issue, fixed**. Internal chunked-decoder helpers are no longer re-exported from `@lando/provider-podman`; the regression test imports them from `../src/named-pipe.ts`, matching the other named-pipe internals used by the test.
  - Carried-over comment (`plugins/provider-podman/src/named-pipe.ts` whole file, "Named-pipe transport duplicates ~300 lines from provider-docker", Medium) - **still deferred, non-blocking for this PR** as follow-up cross-package refactor work.
- Files changed:
  - `plugins/provider-podman/src/index.ts`: removed public re-export of internal chunked helpers; removed unreachable Windows fallback.
  - `plugins/provider-podman/test/named-pipe.test.ts`: import internal chunk helpers directly from source for regression coverage.
- Verification (pre-commit):
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/test/named-pipe.test.ts plugins/provider-podman/test/capabilities.test.ts` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run --filter='@lando/provider-podman' typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 (`biome check .` Checked 499 files, no fixes applied)

## [2026-05-27 19:30 UTC] - Sync
- Previous branch: us-079-provider-podman-desktop
- Previous PR: #178 https://github.com/AaronFeledy/core4/pull/178
- Action: caught up after prior merge (PR #178 already MERGED, remote branch pruned)
- Local main updated: yes (already current with origin/main)
- Branch after sync: main
- Stash created: no
---

## [2026-05-27 20:30 UTC] - US-080
- Implemented the shared `RuntimeProvider` contract suite plus a matrix-driven runner so `TestRuntimeProvider`, `@lando/provider-lando`, `@lando/provider-docker`, and `@lando/provider-podman` all run the same `@lando/sdk/test` contract across every supported platform.
- Files changed:
  - `sdk/src/test/index.ts`: extended `runProviderContract` with `setup` invariants (callable + returns Effect), `getVersions().bundle` optional-string invariant, and (via new in-test schema assertions) coverage for the §5.7 provider-error contract; added new exports `runProviderContractMatrix`, `HostPlatformId`, `SupportedContractCell`, `UnsupportedContractCell`, `ContractMatrixCell`, `ContractMatrixCellResult`, `ContractMatrixReport`, `ContractMatrixOptions`.
  - `sdk/test/contract/provider.test.ts`: new ContractFailure paths for `setup`/`setup-effect`/`bundle-shape`; accepted cases for absent/string bundle; SDK §5.7 error-class shape checks for `ProviderCapabilityError`, `ProviderUnavailableError`, `NoProviderInstalledError` (including a `Schema.decodeUnknownEither` round-trip that preserves redacted `details`); matrix-runner suite (exports, passed+skipped reporting, supported-cell ContractFailure propagation, required `skipReason`, required `factory`).
  - `sdk/test/library/exports.test.ts`: asserts `sdkTest.runProviderContractMatrix` is exported alongside `runProviderContract`/`TestRuntimeProvider`.
  - `plugins/provider-lando/test/contract.integration.test.ts`: added a matrix test that runs the contract for linux/darwin/win32 via the existing fake Podman API client.
  - `plugins/provider-docker/test/contract.integration.test.ts`: added a matrix test that runs the contract for linux/darwin/win32 via `makeProviderLayer({ platform, env: {}, dockerApi: makeFakeApi().api })`.
  - `plugins/provider-podman/test/contract.integration.test.ts`: added a matrix test that runs the contract for linux/darwin/win32 via `makeProviderLayer({ podmanApi: makeFakeApi().api, platform, env: {} })`.
  - `core/test/testing/test-runtime.test.ts`: added a matrix test that runs the contract for linux/darwin/win32 against the re-exported `TestRuntimeProvider`.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 (`biome check .` Checked 499 files, no fixes applied)
  - `PATH="$HOME/.bun/bin:$PATH" bun test sdk/test/contract/provider.test.ts sdk/test/library/exports.test.ts plugins/provider-lando/test/contract.integration.test.ts plugins/provider-docker/test/contract.integration.test.ts plugins/provider-podman/test/contract.integration.test.ts core/test/testing/test-runtime.test.ts` → 49 pass, 4 skip, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us080-full.xml` → 1323 pass, 16 skip, 0 fail (177 files / 134.69s)
- **Learnings for future iterations:**
  - `runProviderContract` is the single shared assertion path — extending it with new invariants (setup, bundle, error-shape) automatically lifts coverage for every existing caller, so the four packages only had to add a matrix wrapper rather than duplicate assertions.
  - The matrix runner's "skipReason required for unsupported cells / factory required for supported cells" contract is intentional: it forces every package to declare its supported set explicitly (no silent platform gaps) and surfaces skipped cells with a reason in the returned `ContractMatrixReport`.
  - SDK error-shape coverage is best asserted against the `Schema.decodeUnknownEither(ProviderCapabilityError)` round-trip rather than by triggering real provider errors — US-081 owns the provider-side negative-path coverage, so US-080 stays scoped to the SDK-level contract.
  - When migrating a contract test, route the cell `factory` through the existing fake-client harness (the per-test `makeFakeApi()` closures) so each matrix cell gets an isolated fake; sharing a single fake across cells would leak state between the lifecycle round-trips.
---

## [2026-05-27 14:58 CDT] - US-080 Review
- Reviewed the US-080 shared provider contract suite against `spec/beta/prd-beta-01-provider-matrix.md` and the build-step commits (`origin/main...HEAD`).
- Fixed confirmed review blockers:
  - `runProviderContract` now actually executes `exec` and consumes `logs` for the contract fixture instead of only checking Effect/Stream shapes.
  - `runProviderContractMatrix` now requires every canonical `HostPlatform` cell (`linux`, `darwin`, `win32`, `wsl`) to be declared and asserts each supported cell returns a provider whose `platform` matches the cell.
  - Provider contract matrix tests now declare unsupported `wsl` cells with reasons; `provider-lando` and `TestRuntimeProvider` factories now build platform-matching providers.
  - Podman-backed fake APIs now cover exec/log streams so the stronger shared contract exercises the lifecycle path uniformly.
  - Listed the additive `@lando/sdk/test` matrix exports in `sdk/API_COMPATIBILITY.md`.
- Files changed:
  - `sdk/src/test/index.ts`
  - `sdk/test/contract/provider.test.ts`
  - `sdk/API_COMPATIBILITY.md`
  - `core/test/testing/test-runtime.test.ts`
  - `plugins/provider-lando/test/contract.integration.test.ts`
  - `plugins/provider-docker/test/contract.integration.test.ts`
  - `plugins/provider-podman/test/contract.integration.test.ts`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: lifecycle contract did not execute `exec` or drain `logs`, so those PRD-required operations were only shape-checked.
  - Fixed: matrix runner did not assert that a supported cell's provider declared the same platform.
  - Fixed: matrix type/tests omitted canonical `wsl` cells and therefore could not record skipped-with-reason WSL coverage.
  - Fixed: new public `@lando/sdk/test` matrix exports were not listed in `sdk/API_COMPATIBILITY.md`.
  - Rejected: provider-side negative-path error behavior remains US-081 scope; US-080's SDK-level §5.7 error-shape/redaction coverage is retained.
  - Rejected: duplicate Podman fake client helpers are acceptable local test fixtures for this review; extracting them would be a refactor outside the story repair.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun test sdk/test/contract/provider.test.ts sdk/test/library/exports.test.ts plugins/provider-lando/test/contract.integration.test.ts plugins/provider-docker/test/contract.integration.test.ts plugins/provider-podman/test/contract.integration.test.ts core/test/testing/test-runtime.test.ts` → 51 pass, 4 skip, 0 fail.
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 (`biome check .` Checked 499 files, no fixes applied).
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us080-review-full.xml` → 1324 pass, 16 skip, 1 fail on the order-sensitive compiled `core/test/cli/setup.test.ts` build step; `PATH="$HOME/.bun/bin:$PATH" bun test core/test/cli/setup.test.ts` rerun in isolation → 4 pass, 0 fail.
  - Earlier full-suite run in the same dirty tree hit compiled harness failures in `renderer-flag.scenario.test.ts`, `aliases.test.ts`, and `no-runtime-tsbuildinfo.test.ts`; rerunning those named files together → 17 pass, 0 fail.
  - LSP diagnostics were attempted for changed TypeScript files but the TypeScript LSP servers exited with code -2; `tsc -b` typecheck was clean.
- **Learnings for future iterations:**
  - Contract helpers should execute lifecycle operations after applying the fixture; shape checks alone do not satisfy lifecycle acceptance criteria.
  - Matrix runners should fail closed on missing canonical host cells and platform-mismatched providers, otherwise a zero-arg factory can make cross-platform cells pass without exercising the intended platform.
  - Include unsupported WSL cells explicitly with skip reasons whenever provider matrices target native Linux/macOS/Windows only.
---

## [2026-05-27 14:58 CDT] - US-080 Review
- Reviewed the US-080 shared provider contract suite against `spec/beta/prd-beta-01-provider-matrix.md` and the build-step commits (`origin/main...HEAD`).
- Fixed confirmed review blockers:
  - `runProviderContract` now executes `exec` and consumes `logs` for the contract fixture instead of only checking Effect/Stream shape.
  - `runProviderContractMatrix` now requires every canonical `HostPlatform` cell (`linux`, `darwin`, `win32`, `wsl`) to be declared and rejects supported cells whose provider reports a different platform.
  - Provider matrix tests now declare explicit unsupported WSL skip cells; provider-lando and TestRuntimeProvider factories now return the platform under test.
  - Provider-lando and provider-podman fake API harnesses now implement exec/log streaming so the shared contract exercises those lifecycle operations.
  - `sdk/API_COMPATIBILITY.md` now records the additive `@lando/sdk/test` matrix exports.
- Files changed:
  - `sdk/src/test/index.ts`
  - `sdk/test/contract/provider.test.ts`
  - `sdk/API_COMPATIBILITY.md`
  - `core/test/testing/test-runtime.test.ts`
  - `plugins/provider-lando/test/contract.integration.test.ts`
  - `plugins/provider-docker/test/contract.integration.test.ts`
  - `plugins/provider-podman/test/contract.integration.test.ts`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: lifecycle contract did not actually execute `exec` or consume `logs`.
  - Fixed: matrix cells could pass without the provider declaring the cell platform.
  - Fixed: canonical WSL host platform could not be represented/skipped.
  - Fixed: additive `@lando/sdk/test` exports were missing from `sdk/API_COMPATIBILITY.md`.
  - Rejected: provider-side negative-path error behavior remains US-081 scope; US-080 keeps SDK §5.7 error-shape/redaction coverage.
  - Rejected: shared Podman fake-client deduplication is non-blocking cleanup; the scoped fix preserved existing per-provider fake seams while adding exec/log coverage.
- Verification:
  - LSP diagnostics attempted for changed TypeScript files; TypeScript LSP servers exited with code -2, so `tsc -b` is the type gate.
  - `PATH="$HOME/.bun/bin:$PATH" bun test sdk/test/contract/provider.test.ts sdk/test/library/exports.test.ts plugins/provider-lando/test/contract.integration.test.ts plugins/provider-docker/test/contract.integration.test.ts plugins/provider-podman/test/contract.integration.test.ts core/test/testing/test-runtime.test.ts` → 51 pass, 4 skip, 0 fail.
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us080-review-full.xml` → failed in compiled/build cleanup tests during full-suite order; reruns of the named failing files (`core/test/cli/setup.test.ts`, `core/test/cli/renderer-flag.scenario.test.ts`, `core/test/cli/aliases.test.ts`, `core/test/build/no-runtime-tsbuildinfo.test.ts`) passed in isolation.
- **Learnings for future iterations:**
  - A matrix contract must assert the provider returned for each supported cell advertises that same platform; otherwise matrix reports can overstate coverage.
  - Canonical host-platform matrices must include explicit unsupported cells for `wsl` while provider support remains native Linux/macOS/Windows.
  - Shape checks are insufficient for lifecycle acceptance; shared provider contracts should run observable operations for `exec` and `logs` through the fake-client seam.
---

## [2026-05-27 20:05 UTC] - US-080 Cleanup
- Ran the branch-wide `remove-ai-slops` cleanup after review and before push with the planning-doc comment rules.
- Cleanup result: cosmetic-only comment cleanup; no runtime behavior, public API, error handling, fake-provider behavior, contract assertions, or test coverage changed.
- Files changed:
  - `sdk/src/test/index.ts`: reworded the `runProviderContract` doc comment to describe the technical contract surface without phase/planning traceability.
  - `spec/beta/progress.txt`: recorded this cleanup step.
- Rejected cleanup proposal: reverted a provider-lando test non-null assertion change because it added type-assertion churn for no behavior benefit.
- Planning-doc leakage scan: no branch-changed source/test comments still cite planning docs, phases, PRDs, story IDs, or section references; remaining `Phase`/`§` hits are test-title strings, not comments.
- Verification: `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us080-cleanup-full.xml` → 1341 tests, 6492 assertions, 1 failure, 16 skips; the failing suite was the known order-sensitive compiled `core/test/cli/setup.test.ts` install-dir parity case, and `PATH="$HOME/.bun/bin:$PATH" bun test core/test/cli/setup.test.ts` rerun in isolation → exit 0. LSP diagnostics were attempted for `sdk/src/test/index.ts` but the TypeScript LSP server exited with code -2, so `tsc -b` is the type gate.
---

## [2026-05-27 20:11 UTC] - Push
- Branch: us-080-shared-contract-suite
- Push: pushed
- Commit(s): 1f2a0fd feat: shared provider contract suite covers all four providers; 7028ca4 chore: mark US-080 complete and update progress log; 9cfe0a7 fix: strengthen US-080 provider contract review; 29c8cf0 chore: record US-080 cleanup
- PR: #179 https://github.com/AaronFeledy/core4/pull/179 (created)
---

## [2026-05-27] - PR #179 provider-integration triage
- CI failure: real issue; `provider-integration-linux-x64` failed because the shared live fixture must emit a service log line and live provider-lando Podman logs can arrive as raw timestamped text, while the decoder only emitted Docker attach-framed chunks and therefore produced `[]` for the shared contract assertion.
- Review feedback: no PR comments or reviews to address (`reviewDecision` empty; Cursor Bugbot passed).
- Fix: the SDK contract fixture now writes a startup log line, provider-lando and provider-docker logs now decode both attach-framed and raw log streams, fake-client coverage exercises raw log bytes, and the shared contract waits for one followed log chunk with a bounded timeout to avoid live-container startup races.
- Verification: `bun test plugins/provider-lando/test/logs.integration.test.ts` → 4 tests, 0 failures, 1 skip; `bun test plugins/provider-lando/test/contract.integration.test.ts plugins/provider-docker/test/contract.integration.test.ts plugins/provider-podman/test/contract.integration.test.ts sdk/test/contract/provider.test.ts` → 44 tests, 151 assertions, 0 failures, 4 skips; manual raw-log driver → decoded `[web/stdout/manual ready]`; `bun run typecheck` → exit 0; `bun run lint` → exit 0; `bun test` → 1343 tests, 6498 assertions, 0 failures, 16 skips; GitHub rerun then showed provider-lando live passing and exposed the same log-startup race on provider-docker, so `sdk/src/test/index.ts` now uses bounded `follow:true`/`tail:20` collection for the shared log assertion.
---

## [2026-05-27 21:20 UTC] - PR #179 Babysit Complete
- PR: #179 https://github.com/AaronFeledy/core4/pull/179
- Branch: us-080-shared-contract-suite
- Result: merged to `main` via squash merge as `47cf895 Strengthen shared provider contract suite (#179)`.
- Final pre-merge checks: all required GitHub Actions checks passed (`static-checks`, `schema-snapshot`, `bundled-codegen`, `library-api-tests`, `recipe-tests`, `guide-scenarios-linux-x64`, `build-linux-x64`, `provider-integration-linux-x64`) and Cursor Bugbot passed.
- Review/Bugbot status: Cursor Bugbot's two review threads were resolved before merge (`provider-lando` empty-chunk raw-log decoder mode lock; `provider-docker` duplicated frame processing branch). No blocking human reviews or PR comments remained.
- Local branch state after merge: repository on `main`, synced with `origin/main`; remote story branch was deleted by `gh pr merge --delete-branch`.
- Verification during babysit/triage: focused provider log tests, `bun run typecheck`, `bun run lint`, and full `bun test` all passed after the Bugbot fixes.
---

## [2026-05-27 21:23 UTC] - Sync
- Previous branch: us-080-shared-contract-suite
- Previous PR: #179 https://github.com/AaronFeledy/core4/pull/179
- Action: caught up after prior merge (PR #179 was already MERGED, all CI green)
- Local main updated: yes (already up to date, HEAD at e29bdcb)
- Branch after sync: main
- Stash created: no
---

## [2026-05-27 21:41 UTC] - US-081
- Added partial-failure rollback + AbortSignal support to `@lando/provider-docker` (provider-lando already had it)
- Files changed:
  - `plugins/provider-docker/src/index.ts`: Added `APPLY_REMEDIATION`, `redactDetails`, §5.7-compliant `serviceStartFailure` (cause + remediation + redacted details), silent cleanup helpers (`stopContainerSilent`, `removeContainerSilent`, `removeNetworkSilent`, `removeVolumeSilent`, `rollbackPartialApply`), AbortSignal in `bringUp`, volumes option in `bringDown`, threaded `applyOptions.signal` and `destroyOptions.volumes` through `apply`/`destroy`
  - `plugins/provider-docker/test/contract.integration.test.ts`: Added `makeFakeApiWithHooks`, 5 new tests: partial failure after first service start, partial failure after second service start, AbortSignal cleanup, §5.7 error field validation (providerId/operation/remediation/redacted details), destroy volumes:true/false
  - `plugins/provider-podman/test/contract.integration.test.ts`: Added `makeFakeApiWithHooks` and 2 delegation tests: partial failure rollback, AbortSignal cleanup (both verify provider-podman delegates correctly through provider-lando's bringUp)
- Verification: `bun run typecheck` → exit 0; `bun run lint` → 4 warnings, exit 0; `bun test` → 1335 pass, 16 skip, 0 fail
- **Learnings for future iterations:**
  - `provider-docker`'s `bringUp` was always-`changed: true`; updated to accurate idempotent tracking (same as provider-lando pattern)
  - AbortSignal timing in synchronous fake APIs: wrap the first `/start` response with `Effect.tap(() => Effect.sync(() => controller.abort()))` to trigger cancellation mid-iteration
  - `biome check --fix` handles import ordering, `noNonNullAssertion`, and `useLiteralKeys` in one pass; run it before manual lint review
---

## [2026-05-27 22:05 UTC] - Push + Babysit
- Branch: us-081-provider-negative-path-coverage
- Push: pushed
- Commit(s): ed42cc7 feat: provider negative-path coverage (partial-failure cleanup, abort signal); 8e7069c chore: mark US-081 complete and update progress log
- PR: #180 https://github.com/AaronFeledy/core4/pull/180 (created)
- CI outcome: all checks SUCCESS or NEUTRAL (static-checks, schema-snapshot, bundled-codegen, library-api-tests, recipe-tests, guide-scenarios-linux-x64, build-linux-x64, provider-integration-linux-x64, Cursor Bugbot NEUTRAL)
- Triage rounds used: 0
- Merge: merged (squash) → 727f2f9
- Local main / branch refreshed: yes; remote + local story branch deleted
---

## [2026-05-27 23:00 UTC] - US-082
- Implemented provider selection precedence (`flag > Landofile > env > config > capability-default`) and provider conflict diagnostics in `meta:doctor`.
- Files changed:
  - `core/src/providers/precedence.ts` (new): `resolveProviderSelection({flag, landofile, env, config, capabilityDefault})` returns `{providerId, source, inputs}`; `readProviderEnvVar()` parses `LANDO_PROVIDER` distinct from the existing `LANDO_DEFAULT_PROVIDER_ID` config overlay; `CAPABILITY_DEFAULT_PROVIDER_ID = "lando"` is the fail-closed bundled fallback.
  - `core/src/providers/conflict.ts` (new): `detectProviderConflicts({stateDir, platform, env})` wraps `provider-podman`'s `detectProviderLandoConflict` + `resolvePodmanSocket` so doctor can surface the typed `ProviderLandoConflictError` (remediation `lando setup --provider=…`) without constructing provider-podman.
  - `core/src/cli/commands/doctor.ts`: `doctor({flagProviderId, landofileProviderId, env, platform})` gathers all five inputs via `ConfigService`, calls the resolver, attaches a `selection` record on the `selected-provider` check, threads `selectionSource` into `context`, and emits a second `provider-conflict` check (status `warn`) with the typed remediation when a lando↔podman socket overlap is detected.
  - `core/src/cli/oclif/commands/meta/doctor.ts`: declares a `--provider` flag, widens the spec's R param to `ConfigService | RuntimeProviderRegistry`, threads the flag into the new `doctor()` options.
  - `core/src/cli/oclif/commands/meta/setup.ts`: now resolves the selected provider via `resolveProviderSelection({flag, env: LANDO_PROVIDER, config: defaultProviderId, capabilityDefault})` before calling `registry.select(syntheticPlan)`; spec R param widened to include `ConfigService`.
  - `core/src/cli/run.ts`: compiled-mode `runDoctor` now parses `--provider` and forwards it through `doctor({flagProviderId})`.
  - `core/test/providers/precedence.test.ts` (new): every pair of (`flag`, `landofile`, `env`, `config`, `default`) plus all single-input and multi-input combinations; covers `readProviderEnvVar` whitespace/empty/missing semantics.
  - `core/test/providers/conflict.test.ts` (new): empty stateDir, missing state file, no socketPath, non-matching socket, matching socket (asserts `ProviderLandoConflict` report + `lando setup --provider=` remediation containing both `provider=podman` and `provider=lando`), trailing-slash normalization, malformed state file propagation.
  - `core/test/cli/doctor.test.ts`: extended with selection-reporting tests for every resolved source (`default`, `config`, `env`, `landofile`, `flag`), plain-text + NDJSON shape assertions, and provider-conflict tests (positive + negative + ndjson + remediation string).
  - `core/test/cli/setup.test.ts`: existing tests now thread `ConfigService` via `buildSetupLayers`; added precedence integration tests (flag overrides env, env overrides config, config overrides default, default fallback).
  - `core/test/cli/setup.scenario.test.ts`: added `makeConfigServiceLayer()` to the merged layer for both happy-path and Podman-failure cases.
  - `core/test/cli/fixtures/meta-doctor.provider-status*.ndjson`: regenerated to carry the new `selection` payload and `selectionSource` context key.
  - `docs/guides/setup/provider-selection.mdx` (new): records precedence order, doctor reporting surface, and the `lando-podman-socket` conflict remediation as hidden scenarios.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → 4 pre-existing warnings, 0 errors
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → "Guide lint passed."
  - `PATH="$HOME/.bun/bin:$PATH" bun test core/test/providers/ core/test/cli/doctor.test.ts core/test/cli/setup.test.ts core/test/cli/setup.scenario.test.ts` → 56 pass, 1 skip, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun run --filter='@lando/core' build` → exit 0 (compiled binary ./core/dist/lando)
  - `PATH="$HOME/.bun/bin:$PATH" bun test` (full suite, post-build) → 1374 pass, 16 skip, 0 fail / 1390 tests across 182 files
  - Manual CLI QA against `./core/dist/lando doctor`:
    - default: `selectionSource: config`, `selectionInputConfig: lando`, `selectionInputDefault: lando` (no env, no flag)
    - `LANDO_PROVIDER=podman lando doctor`: resolver routes to provider-podman and fails closed at the capability probe with `ProviderUnavailableError` carrying the Podman-socket remediation — exactly the expected behavior when LANDO_PROVIDER is honored.
    - `lando doctor --provider=docker`: `selectionSource: flag`, `selectionInputFlag: docker`, provider-docker stub renders capabilities.
    - With `~/.local/share/lando/providers/provider-lando/setup-state.json` recording the same Podman socket as resolved on Linux: doctor emits a second `provider-conflict: warn` check with the `Choose one provider explicitly. Run \`lando setup --provider=podman\`… or \`lando setup --provider=lando\`…` remediation (command tag `lando setup --provider=`).
- **Learnings for future iterations:**
  - `LANDO_PROVIDER` belongs at the `env` precedence slot and stays distinct from `LANDO_DEFAULT_PROVIDER_ID`, which remains a config-layer overlay merged into `ConfigService.defaultProviderId`. Don't conflate the two: tests assert that `readProviderEnvVar` reads only `LANDO_PROVIDER`.
  - Once `meta:doctor` reads `ConfigService.userDataRoot` to drive conflict detection, every doctor-touching scenario test must merge a `ConfigService` live/stub layer alongside `RuntimeProviderRegistry`; missing it surfaces as `Service not found: @lando/core/ConfigService` at runtime, not at typecheck time, because `Effect.gen` defers service lookup.
  - The `provider-conflict` check is intentionally rendered with empty `capabilities` and the conflict-specific `conflictKind`/`socketPath` keys in `context` only — plain-text rendering drops those keys today, so any future text-output assertions should target the solution description string rather than context fields.
  - When regenerating the doctor NDJSON fixtures, set the test `ConfigService.defaultProviderId` to `"lando"` (matching `ConfigServiceLive`'s default merge) so the fixture's `selectionSource` is `"config"` and not `"default"`.
- Branch: us-082-provider-selection-precedence
- Commit: 71daa7b feat: provider selection precedence and conflict diagnostics
---

## [2026-05-27 18:30 CDT] - US-082
- Implemented provider selection precedence (`flag > Landofile > env > config > capability-default`) and lando↔podman socket conflict diagnostics surfaced through `meta:doctor`.
- Files changed:
  - `core/src/providers/precedence.ts` (new): pure `resolveProviderSelection`, `readProviderEnvVar` (reads `LANDO_PROVIDER`), `CAPABILITY_DEFAULT_PROVIDER_ID` (`lando`).
  - `core/src/providers/conflict.ts` (new): `detectProviderConflicts({ stateDir, platform?, env? })` wraps `provider-podman`'s `detectProviderLandoConflict` + `resolvePodmanSocket` and returns shape-stable `ProviderConflictReport[]`. Fails closed when no `stateDir`.
  - `core/src/cli/commands/doctor.ts`: accepts `DoctorOptions` (`flagProviderId`, `landofileProviderId`, `env`, `platform`); gathers env+config inputs, runs the resolver, emits a `selection` record on the `selected-provider` check, and appends a separate `provider-conflict` check (status=`warn`, severity=`warn`) carrying the typed remediation (`lando setup --provider=…`) when the lando/podman socket overlap is detected. Adds `ConfigService` to the doctor effect requirements.
  - `core/src/cli/oclif/commands/meta/doctor.ts`: adds `--provider` flag; threads parsed flag into `doctor({ flagProviderId })`; widens spec R to `ConfigService | RuntimeProviderRegistry`.
  - `core/src/cli/oclif/commands/meta/setup.ts`: same precedence resolver; reads `LANDO_PROVIDER` + config defaultProviderId before falling back to `lando`; widens spec R to `ConfigService | RuntimeProviderRegistry`; flag description updated.
  - `core/src/cli/run.ts`: compiled-mode `runDoctor(argv)` now parses `--provider` via existing `parseProviderFlag` and forwards it as `flagProviderId`.
  - `core/test/providers/precedence.test.ts` (new): 17 cases covering every input pair (flag/landofile/env/config/default) plus multi-input resolution and `readProviderEnvVar` env-var parsing.
  - `core/test/providers/conflict.test.ts` (new): 6 cases — empty when no stateDir, no state file, mismatched socket; conflict + remediation when sockets match; trailing-slash normalization; `ProviderLandoStateError` propagation.
  - `core/test/cli/doctor.test.ts`: extended with provider-selection precedence reporting (default/config/env/landofile/flag) and provider-conflict diagnostics (conflict emit + no-conflict negative); added `ConfigService` mock via `buildConfigService` and `buildLayers` helper.
  - `core/test/cli/fixtures/meta-doctor.provider-status{,.windows}.ndjson`: regenerated to include the new `context.selectionSource` field and `selection` payload.
  - `core/test/cli/setup.test.ts`: provides `ConfigService` via `buildSetupLayers`; adds 4 precedence integration tests (config fallback, env beats config, flag beats env+config, capability default).
  - `core/test/cli/setup.scenario.test.ts`: provides `ConfigService` for the happy-path and Podman-socket-unreachable scenarios so the meta:setup task-tree progress tests still bind their layer to the new spec requirements.
  - `docs/guides/setup/provider-selection.mdx` (new): non-rendered scenarios covering the precedence rules, the doctor-reported fields, and the socket-conflict remediation.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 (4 pre-existing warnings unrelated to this story)
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun test core/test/providers/ core/test/cli/doctor.test.ts core/test/cli/setup.test.ts core/test/cli/setup.scenario.test.ts` → 56 pass, 1 skip, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test` (post `bun run build`) → 1374 pass, 16 skip, 0 fail across 1390 tests
  - Manual CLI QA against `core/dist/lando doctor` with isolated `LANDO_USER_CONF_ROOT`/`LANDO_USER_DATA_ROOT`:
    - Defaults: `selectionSource: config`, `selectionInputConfig: lando`, `selectionInputDefault: lando`.
    - `LANDO_PROVIDER=podman lando doctor` → selects podman (fails reaching socket as expected) — env path resolved.
    - `lando doctor --provider=docker` → `selectionSource: flag`, `selectionInputFlag: docker`, `selectionInputConfig: lando`.
    - Faked `data/providers/provider-lando/setup-state.json` with the resolved Podman rootless socket → second check `provider-conflict: warn` with `solution[manual]: Choose one provider explicitly. Run \`lando setup --provider=podman\` … (lando setup --provider=)`.
- **Learnings for future iterations:**
  - When a CLI command starts depending on a new SDK service (here `ConfigService` was added to doctor + setup specs), every existing test that built a hand-rolled layer must be updated; the failure is a runtime `Service not found` not a typecheck error, so search for `Layer.succeed(<existing-service>, …)` callers when widening spec `R`.
  - Doctor's stable NDJSON shape is fixture-locked. New fields go into `context` and/or a top-level `selection` field; regenerate `meta-doctor.provider-status{,.windows}.ndjson` by running `doctor` against the same fake provider and config used in the snapshot test.
  - The `provider-conflict` check intentionally has empty `capabilities` so the text renderer drops capability fields and only emits the name/severity/runtime/solution lines; relying on text snapshots for context fields will fail — assert against the structured `result.checks[i].context` or the NDJSON `context` payload instead.
  - Biome's `lint/performance/noDelete` rule requires `Reflect.deleteProperty(process.env, "X")` instead of `delete process.env.X` for env-var unset/restore patterns in test scaffolding.
---

## [2026-05-27 18:11 CDT] - US-082 Review
- Reviewed US-082 provider selection precedence and conflict diagnostics against `spec/beta/prd-beta-01-provider-matrix.md` and the build-step commits on `us-082-provider-selection-precedence`.
- Fixed confirmed review blockers:
  - Made runtime provider registry default selection honor `LANDO_PROVIDER > config > capability default` when no planned app provider is supplied.
  - Made app planning encode the resolved `Landofile provider: > LANDO_PROVIDER > config > capability default` provider instead of always defaulting providerless apps to `lando`.
  - Made `meta:doctor` pre-detect provider-lando/provider-podman socket conflicts before constructing provider-podman, so it emits a structured `provider-conflict` check instead of failing during provider selection.
  - Made the structured conflict solution command executable (`lando setup --provider=podman`) while preserving the remediation text that names both alternatives.
- Files changed:
  - `core/src/cli/commands/doctor.ts`
  - `core/src/providers/registry.ts`
  - `core/src/services/planner.ts`
  - `core/test/cli/doctor.test.ts`
  - `core/test/services/app-planner.test.ts`
  - `core/test/services/runtime-provider-registry.test.ts`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: app lifecycle/provider selection still ignored `LANDO_PROVIDER` and config defaults when the Landofile omitted `provider:`.
  - Fixed: real `meta:doctor` conflict diagnostics could be bypassed when selecting provider-podman constructed the provider and failed before the appended conflict check.
  - Fixed: structured conflict `DoctorSolution.command` was `lando setup --provider=` without a provider value.
  - Rejected/deferred: actual `meta:doctor` Landofile discovery remains out of this review fix because `meta:doctor` intentionally bootstraps at provider level and must keep working outside app contexts; the underlying `doctor({ landofileProviderId })` path and app planner cover Landofile precedence, while app-context doctor discovery should be handled as a separate CLI/app-bootstrap design change.
  - Rejected: low-risk provider-id control-character output injection is pre-existing schema looseness for local operator inputs; JSON output is escaped and unknown providers fail closed, so this is not a scoped US-082 blocker.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 with 4 pre-existing non-null assertion warnings in provider contract integration tests outside the US-082 diff.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun test core/test/cli/doctor.test.ts core/test/services/app-planner.test.ts core/test/services/runtime-provider-registry.test.ts core/test/providers/precedence.test.ts core/test/providers/conflict.test.ts core/test/cli/setup.test.ts` → 88 pass, 1 skip, 0 fail.
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1379 pass, 16 skip, 0 fail.
  - `git diff --check` → exit 0.
  - LSP diagnostics were attempted for changed TypeScript files but the TypeScript LSP server exited with code -2; `tsc -b` typecheck was clean.
- **Learnings for future iterations:**
  - Provider precedence must be applied both where provider capabilities are chosen and where `AppPlan.provider` is encoded; otherwise app commands can plan for `lando` while diagnostics/setup report a different selected provider.
  - Conflict diagnostics that depend on provider construction must run before constructing a provider that can throw the same conflict, or doctor will emit the raw provider error instead of structured checks.
  - Keep provider-level doctor bootstrapping separate from app-context Landofile discovery unless the CLI design explicitly changes to support optional app bootstrap.
---

## [2026-05-27 18:40 CDT] - US-082 Cleanup
- Ran the branch-wide `remove-ai-slops` cleanup after review and before push with the planning-doc comment rules.
- Cleanup result: cosmetic-only comment/prose cleanup; no runtime behavior, provider selection precedence, doctor output shape, provider conflict detection, public API, error handling, or test assertions changed.
- Files changed:
  - `core/src/cli/commands/doctor.ts`: removed verbose command/rendering comments while preserving doctor option docs and the registry-selection note.
  - `core/src/cli/oclif/commands/meta/setup.ts`: removed bootstrap/seam comments that restated command structure.
  - `core/src/providers/conflict.ts`: shortened provider-conflict comments without changing typed conflict reporting.
  - `core/src/providers/precedence.ts`: removed redundant resolver comments; precedence behavior remains covered by tests.
  - `core/src/providers/registry.ts`: removed a module banner that restated registry behavior.
  - `core/src/services/planner.ts`: removed redundant explanatory comments around existing guards/fallbacks.
  - `docs/guides/setup/provider-selection.mdx`: reworded hidden scenario reasons to avoid internal test-path traceability.
- Planning-doc leakage scan: no branch-changed source/test comments still cite planning docs, phases, PRDs, story IDs, or section references; remaining spec/section hits are runtime error metadata or test assertions, not comments.
- Verification: `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 with 4 pre-existing warnings; `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us082-cleanup-full.xml` → 1395 tests, 6653 assertions, 1 TimeoutError in `core/test/scripts/codegen-ci.test.ts` (`uses .bun-version file as the Bun version source...`); focused rerun `PATH="$HOME/.bun/bin:$PATH" bun test core/test/scripts/codegen-ci.test.ts` → 10 pass, 0 fail, 123 assertions. `git diff --check` → clean. LSP diagnostics were attempted for changed TypeScript files but the TypeScript LSP server exited with code -2; no MDX LSP is configured.
---

## [2026-05-27 18:32 CDT] - Push
- Branch: us-082-provider-selection-precedence
- Push: pushed
- Commit(s):
  - f17cf6e chore: cleanup
  - b53eb1c docs: record US-082 review fixes
  - 400bb71 fix: emit podman conflict doctor check before selection
  - a9c0541 fix: apply provider precedence to app planning
  - 97cf413 chore: mark US-082 complete and update progress log
  - d92891a chore: mark US-082 complete and update progress log
  - 71daa7b feat: provider selection precedence and conflict diagnostics
  - 20d44e7 docs: record PR #180 merge babysit
- PR: #181 https://github.com/AaronFeledy/core4/pull/181 (created)
---

## [2026-05-27 23:43 UTC] - Babysit
- PR: #181 https://github.com/AaronFeledy/core4/pull/181
- CI outcome: pass
- Review feedback: none
- Triage rounds used: 0
- Merge: merged (squash)
- Local main / branch refreshed: yes (local main reset to origin/main after the remote squash already contained the prior local progress-log commit; local story branch absent/deleted)
- Remaining local state, if any: spec/beta/progress.txt has this uncommitted babysit note
---

## [2026-05-27 23:46 UTC] - Sync
- Previous branch: us-082-provider-selection-precedence
- Previous PR: #181 https://github.com/AaronFeledy/core4/pull/181
- Action: caught up after prior merge (PR #181 already MERGED, remote branch pruned, local story branch absent)
- Local main updated: yes (fast-path committed pending progress.txt; main already at origin/main + 1 local progress commit)
- Branch after sync: main
- Stash created: no
---

## [2026-05-27 19:25 CDT] - US-083
- Shipped the `go` service type (versions 1.22 and 1.23) from `@lando/service-lando` per spec §6.12.1.
- Files changed:
  - `plugins/service-lando/src/services/go.ts` (new): `SUPPORTED_GO_VERSIONS = ["1.22", "1.23"]`, `SUPPORTED_GO_FRAMEWORKS = ["none"]` (Beta-only slot), `makeGoServiceType(version)` factory; default image `golang:${version}` (Debian-bookworm based so `bash` is available for the §6.6 TCP healthcheck); default port 8080 with `http` endpoint; `bash -c "exec 3<>/dev/tcp/127.0.0.1/${port}"` healthcheck; bind-mounted appRoot at `/app`; `GOPATH=/go`, `GOCACHE=/root/.cache/go-build`, `CGO_ENABLED=0` defaults; `lando-service-go` extensions metadata; framework + version validation with full remediation messages.
  - `plugins/service-lando/src/index.ts`: exports `go122ServiceType` / `go123ServiceType`, registers `"go:1.22"` and `"go:1.23"` in the `serviceTypes` Map and in the `PluginManifest.contributes.serviceTypes` list.
  - `core/src/services/planner.ts`: `serviceTypeFor()` infers `go:1.22` / `go:1.23` from bare `golang:1.22` / `golang:1.23` image refs (matches the node/python/ruby pattern).
  - `plugins/service-lando/test/go.test.ts` (new): 13 unit tests covering default plan, env contract, image/port overrides, default keep-alive command, framework + version rejection with remediation, reserved `LANDO_*` rejection, and `go:1.23` parity + cross-version remediation.
  - `plugins/service-lando/test/go.scenario.test.ts` (new): scenario tests through `AppPlanner` + `ProviderExecToolingEngineLive` + a fake `RuntimeProvider`; verifies HTTP endpoint emission on 8080 from the full plan, that the go web service is `primary`, and that `lando go version` / `lando go build ./...` tooling aliases route through `provider.exec` against the go service.
  - `plugins/service-lando/test/registration.test.ts`: added `go:1.22` / `go:1.23` to expected manifest list; added an `AppPlanner` integration test for both versions and a Go-family remediation test for `go:1.21`.
  - `plugins/service-lando/test/env-contract.test.ts`: added `go:1.22` / `go:1.23` to the catalog cases (`expectsAppPaths: true`, `expectsWebroot: null`) so the §6.9 `LANDO_*` env contract is exercised for both versions.
  - `plugins/service-lando/test/package.test.ts`: added `go:1.22` / `go:1.23` to the registered service-types snapshot and manifest snapshot.
  - `sdk/test/fixtures/schema-snapshot.json`: regenerated to reflect the two new entries in `@lando/service-lando`'s manifest.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 (4 pre-existing warnings unrelated to this story)
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/` → 182 pass, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test` (full suite, post `bun run --filter='@lando/core' build`) → 1406 pass, 16 skip, 0 fail across 1422 tests in 184 files
  - Manual CLI QA: `core/dist/lando --help` lists topics + commands; a temp `.lando.yml` with `services.web.type: go:1.22` parses through the binary and proceeds past the schema gate (provider-selection fails because no Podman/Docker socket is available in this host distro — expected per `.local/DEV-ENV.md` host-vs-sandbox split). Direct exercise of `go122ServiceType.toServicePlan()` and `serviceTypes` Map through `bun -e` confirmed: 18 registered types, both `go:1.22` and `go:1.23` present, ServicePlan emits `golang:1.22` artifact, primary=true, endpoint :8080/http, bash TCP healthcheck, `/app` workdir + bind mount, `GOPATH`/`GOCACHE`/`CGO_ENABLED` env, and `lando-service-go` extension payload.
- **Learnings for future iterations:**
  - Adding a service type updates **five** test surfaces in `plugins/service-lando/test/`, not just the per-type unit file: `package.test.ts` (registered-types + manifest snapshots), `registration.test.ts` (manifest expected list + AppPlanner family-resolution + family-remediation), `env-contract.test.ts` (catalog cases array), `discovery.test.ts` (only `PLUGIN_NAME` check, no update needed), plus the new `<type>.test.ts` and optional `<type>.scenario.test.ts`. The `sdk/test/fixtures/schema-snapshot.json` is auto-regenerated by the schema-snapshot test and must be committed.
  - Go's official Docker image is `golang:<version>` (Debian-bookworm based, includes bash) rather than `<lang>:<version>-slim`. Use that as the default ref instead of `golang:<version>-alpine` — the alpine variant lacks `bash`, which would break the §6.6 `bash -c "exec 3<>/dev/tcp/..."` TCP healthcheck shared by every other Lando language service. The `-bookworm` / `-bullseye` slim variants are post-GA tuning.
  - When introducing a new service-type "family" (`go:`), the planner's `serviceTypeFor()` image-inference table must learn the upstream image name (`golang:`, not `go:`) so users with `services.web.image: golang:1.22` but no explicit `type:` get routed to the right `ServiceTypeShape`. Pattern matches the existing `node:`/`python:`/`ruby:` rows.
  - US-091 explicitly constrains Beta Go to `framework: "none"` only. Keep `SUPPORTED_GO_FRAMEWORKS = ["none"]` and reject `echo`/`fiber`/`gin`/`chi` with a remediation message; later post-GA stories can extend the array without breaking schema callers.
  - Scenario tests for a service type are most cheaply written by composing `AppPlannerLive` + `services` (the plugin's Layer) + `PluginRegistryLive` for plan production, then a hand-rolled fake `RuntimeProvider` and `ProviderExecToolingEngineLive` for the tooling exercise. This avoids the need for a live container while still proving the runTooling path resolves to the new service.
- Branch: us-083-go-service-type
- Commit: c2e19da feat: go service type
---

## [2026-05-27 20:24 CDT] - US-083 Review
- Reviewed the US-083 Go service type implementation against `spec/beta/prd-beta-02-service-catalog-full.md` and the build-step commits (`origin/main...HEAD`).
- Fixed confirmed review blockers:
  - Updated the packaged `@lando/service-lando` `plugin.yaml` manifest so installed/package consumers advertise the same service type catalog as the TypeScript manifest, including `go:1.22` and `go:1.23`.
  - Added package-level test coverage that keeps `plugin.yaml` service type contributions in sync with the exported service catalog.
  - Corrected `spec/beta/prd.json` so US-083 is not marked passing while its live/minimal Go HTTP scenario acceptance remains uncovered.
- Files changed:
  - `plugins/service-lando/plugin.yaml`
  - `plugins/service-lando/test/package.test.ts`
  - `spec/beta/prd.json`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: packaged `plugin.yaml` did not advertise `go:1.22` / `go:1.23` and was stale relative to the TypeScript manifest.
  - Fixed: PRD status was prematurely marked passing despite the missing live/minimal Go HTTP scenario acceptance.
  - Rejected/deferred: adding and executing the host-impacting live `lando start` Go HTTP scenario is real work, but it requires the `.local/DEV-ENV.md` sandbox/provider workflow and is larger than a safe focused review repair; US-083 is left incomplete until that acceptance is added in a build step.
  - Rejected/deferred: `golang:1.22` / `golang:1.23` lifecycle support is a Beta image-pin policy concern; the Debian/default-tag `bash` assumption is valid for this implementation, while digest/full-patch pinning should be handled with the service-catalog image policy rather than changed ad hoc here.
  - Rejected/deferred: `CGO_ENABLED=0` may be too opinionated for some Go projects, but it is user-overridable and was not changed without a specific PRD decision.
- Verification run and result:
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/test/package.test.ts plugins/service-lando/test/go.test.ts plugins/service-lando/test/go.scenario.test.ts plugins/service-lando/test/registration.test.ts plugins/service-lando/test/env-contract.test.ts` → 99 pass, 0 fail.
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/` → 183 pass, 0 fail.
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 with 4 pre-existing non-null assertion warnings in provider contract tests.
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1407 pass, 16 skip, 0 fail across 1423 tests.
  - A subsequent full-suite rerun hit unrelated compiled-CLI harness failures (`shellenv` build exit 1 and `deferred-commands` `ETXTBSY` on `core/dist/lando`); focused service-lando coverage remained green.
  - LSP diagnostics: `spec/beta/prd.json` clean; TypeScript LSP for `plugins/service-lando` exited with code -2; YAML LSP is not installed.
- **Learnings for future iterations:**
  - Service catalog additions must update both the TypeScript `manifest` in `src/index.ts` and the packaged `plugin.yaml` referenced by `package.json`'s `lando.manifest` field.
  - A fake-provider tooling scenario is useful for planner/tooling routing, but it must not be recorded as satisfying PRD wording that requires starting a minimal service and probing a live endpoint.
---

## [2026-05-28 00:45 UTC] - Push
- Branch: us-083-go-service-type
- Push: skipped (US-083 is explicitly incomplete: PRD notes live/minimal Go HTTP scenario acceptance remains missing, so this push step must not push or open a PR)
- Commit(s): afe5eff docs: record US-083 review status; 7940858 fix: sync service-lando packaged manifest; 3ce3bd6 chore: mark US-083 complete and update progress log; c2e19da feat: go service type
- PR: none (not created)
---

## [2026-05-28 00:46 UTC] - Babysit
- PR: none (no pull request found for branch `us-083-go-service-type`)
- CI outcome: not checked (no PR located)
- Review feedback: none checked (no PR located)
- Triage rounds used: 0
- Merge: deferred (push step did not create/refresh a PR for the current branch)
- Local main / branch refreshed: no
- Remaining local state, if any: current branch `us-083-go-service-type`; existing local modifications in `spec/beta/progress.txt` plus this babysit note

## [2026-05-28 01:08 UTC] - US-083 Live Go HTTP Scenario
- Added the live/minimal Go HTTP scenario acceptance that the prior US-083 review left open: `plugins/service-lando/test/go.integration.test.ts` brings up a Go service against a real Podman socket, fetches the HTTP endpoint, and runs `lando go version` through `ProviderExecToolingEngineLive` against the live provider.
- The test is gated by `test.skipIf(!process.env.LANDO_TEST_PODMAN_SOCKET)` (matching `plugins/provider-lando/test/bring-up.integration.test.ts` and `core/test/cli/start.scenario.test.ts`) and skipped by default; CI's provider-integration job already exports that env var.
- Files changed:
  - `plugins/service-lando/test/go.integration.test.ts` (new): writes a minimal `main.go` (net/http on :31082) + `go.mod` under a temp bind-mounted appRoot, hand-builds the AppPlan/ServicePlan with `type: "go:1.22"` + `artifact: golang:1.22` + `command: ["go", "run", "/app/main.go"]` (matches the `go.ts` defaults), calls `bringUp(plan, { podmanApi })` via the real `makePodmanApiClient`, polls `http://127.0.0.1:31082` for `lando-go-ok`, then runs `runTooling({ name: "go", args: ["version"] })` through a layer wiring `LandofileService` / `AppPlanner` / `RuntimeProviderRegistry` against the live `RuntimeProvider` from `makeProviderLayer`, asserts `result.stdout` matches `/go version go1\.22/`, and tears down via `bringDown` in `finally`. Timeout 240_000ms to cover image pull + `go run` cold compile.
  - `spec/beta/prd.json`: US-083 `passes: true` (notes cleared).
- Manual QA (Docker substitute for Podman, since this host has Docker but no Podman and the WSL `lando4-dev` sandbox is Windows-only):
  - Wrote the same fixtures (`main.go` + `go.mod`) to `/tmp/lando-go-smoke`
  - `docker run -d -v /tmp/lando-go-smoke:/app -w /app -e GOFLAGS=-mod=mod -p 127.0.0.1:31082:31082 golang:1.22 go run /app/main.go` → container running
  - `curl http://127.0.0.1:31082/` → `lando-go-ok` (after 5s compile+start)
  - `docker exec ... go version` → `go version go1.22.12 linux/amd64` (matches the test regex)
  - Container state: `running running=true`. Cleaned up.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 (4 pre-existing non-null-assertion warnings unrelated to this story)
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/` → 183 pass, 1 skip (the new live test), 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1407 pass, 17 skip, 0 fail (the +1 skip vs the prior step is exactly this story's new gated test)
  - Test file compiles + skip-gate works without `LANDO_TEST_PODMAN_SOCKET`: `bun test plugins/service-lando/test/go.integration.test.ts` → 1 skip, 0 fail.
- **Learnings for future iterations:**
  - Live HTTP-server scenarios for new service types follow the `bring-up.integration.test.ts` pattern (hand-built `ServicePlan`/`AppPlan`, `bringUp(plan, { podmanApi })` via real `makePodmanApiClient`, `fetch("http://127.0.0.1:<port>")`, `bringDown` in `finally`) rather than the `AppPlanner` planning path used in fake-provider scenario tests. The two test files complement each other: scenario test exercises planner → tooling, integration test exercises provider lifecycle → live HTTP.
  - Live integration tests that route tooling through `ProviderExecToolingEngineLive` need to provide a `RuntimeProviderRegistry.select` that returns the **same** live `RuntimeProvider` instance the bring-up used. Building the provider once via `RuntimeProvider.pipe(Effect.provide(makeProviderLayer({ podmanApi: api })))` and capturing it for both the bring-up call and the tooling layer keeps the container-name/network state consistent.
  - For `go run` to work inside `golang:1.22` against a bind-mounted source dir, write both `main.go` and a minimal `go.mod` (stdlib-only). `GOFLAGS=-mod=mod` is defensive against the image picking up a stray GOPATH-style layout.
  - `golang:1.22`'s default Cmd is not a long-lived process, so the service plan must supply `command: ["go", "run", "/app/main.go"]` (or similar) to keep the container alive while the HTTP server serves. The default `tail -f /dev/null` keep-alive from `go.ts` is for the planner-driven path where the tooling alias drives one-shot exec; a live HTTP scenario explicitly overrides it.
  - Port publishing in `provider-lando` bringUp maps `endpoint.port` to `HostIp: 127.0.0.1, HostPort: <port>` (see `bring-up.ts` ~line 147), so the host port equals the container port — picking a non-default port (31082 vs the type's default 8080) avoids collisions with whatever else is bound to 8080 on a CI runner.
- Branch: us-083-go-service-type

## [2026-05-28 01:44 UTC] - US-083 Review
- Reviewed the latest US-083 live Go HTTP scenario commit (`origin/us-083-go-service-type...HEAD`) with the loop review agents and direct checks.
- Fixed confirmed review blocker: the provider-integration CI job exported `LANDO_TEST_PODMAN_SOCKET` but did not run `plugins/service-lando/test/*.integration.test.ts`, so the new live Go HTTP scenario would remain skipped outside ad hoc local execution. Added the service-lando integration glob to CI and the CI runbook, and pre-pulls `golang:1.22` for the large image used by the test.
- Files changed:
  - `.github/workflows/ci.yml`
  - `docs/ci-runbook.md`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: service-lando live integration tests were not part of the provider-integration CI command set despite relying on the same Podman socket gate.
  - Rejected: default Go plugin version drift from upstream Lando docs is not a US-083 blocker because this rewrite PRD explicitly pins Beta support to `go:1.22` and `go:1.23`.
  - Rejected: changing the Go default command from keep-alive to an opinionated HTTP starter would conflict with the service catalog pattern; the live scenario correctly overrides the command for the minimal HTTP app.
  - Rejected: the prior host Docker smoke noted in the build progress is not accepted as sandbox-compliant verification evidence; it remains historical context only, and CI now owns the live Podman execution path.
- Verification run and result:
  - `.local/bin/lando4-dev --check` → exit 127 (`wsl.exe is not available from this shell`), so live Podman execution could not be run from this host shell.
  - `env -u LANDO_TEST_PODMAN_SOCKET PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/test/go.integration.test.ts` → 1 skip, 0 fail (skip gate/compile path).
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/test/go.test.ts plugins/service-lando/test/go.scenario.test.ts plugins/service-lando/test/registration.test.ts` → 29 pass, 0 fail.
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/` → 184 tests, 886 assertions, 0 failures, 1 skip (the live Podman test without socket env).
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1424 tests, 6803 assertions, 0 failures, 17 skips; a later full-suite rerun hit 2 unrelated transient docs/setup failures, and focused rerun of those files passed (`core/test/scripts/docs-scenario.test.ts core/test/cli/setup.test.ts` → 13 tests, 34 assertions, 0 failures).
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 with 4 pre-existing non-null assertion warnings in provider contract tests.
  - `git diff --check` → exit 0.
  - LSP diagnostics for `plugins/service-lando/test/go.integration.test.ts` attempted; TypeScript LSP exited with code -2.
- **Learnings for future iterations:**
  - Any new `*.integration.test.ts` outside provider-lando/provider-docker must be explicitly added to the provider-integration CI command list; root `bun test` may skip or not exercise live behavior depending on env, and `test:unit` ignores integration tests entirely.
  - Progress-log manual smoke tests that use host Docker/Podman are not acceptance evidence for host-impacting Lando/provider behavior; the sandbox or CI provider-integration job must carry that proof.
---

## [2026-05-28 01:51 UTC] - Push
- Branch: us-083-go-service-type
- Push: pushed
- Commit(s): 6e6a590 chore: cleanup go service implementation; 2c0e0b9 refactor: simplify planner cleanup; 60c5996 fix: run service-lando integrations in provider CI; 4ed5da4 feat: go service type; afe5eff docs: record US-083 review status
- PR: #182 https://github.com/AaronFeledy/core4/pull/182 (existing)
---

## [2026-05-28 01:51 UTC] - Push
- Branch: us-083-go-service-type
- Push: pushed
- Commit(s):
  - 831ecf3 docs: record US-083 push
  - 6e6a590 chore: cleanup go service implementation
  - 2c0e0b9 refactor: simplify planner cleanup
  - 60c5996 fix: run service-lando integrations in provider CI
  - 4ed5da4 feat: go service type
- PR: #182 https://github.com/AaronFeledy/core4/pull/182 (existing)
---

## [2026-05-28 01:59 UTC] - Babysit
- PR: #182 https://github.com/AaronFeledy/core4/pull/182
- CI outcome: pass
- Review feedback: none
- Triage rounds used: 0
- Merge: merged (squash)
- Local main / branch refreshed: yes (local main rebased onto origin/main after preserving existing local progress-log commit)
- Remaining local state, if any: local main is ahead of origin/main by one progress-log commit; story branch deleted locally; remote branch deleted by PR merge
---

## [2026-05-28] - Sync
- Previous branch: us-083-go-service-type
- Previous PR: #182 https://github.com/AaronFeledy/core4/pull/182
- Action: caught up after prior merge (PR #182 was already MERGED)
- Local main updated: yes (ff-only; also pushed one pending progress-log commit to origin/main)
- Branch after sync: main
- Stash created: no
---

## [2026-05-28] - US-084
- Implemented `mongodb` ServiceType in `@lando/service-lando` per spec §6.12.1
- Files changed:
  - `plugins/service-lando/src/services/mongodb.ts`: new `mongodbServiceType` with MONGO_INITDB_ROOT_USERNAME/PASSWORD/DATABASE env defaults, TCP healthcheck on port 27017, persistent `/data/db` volume
  - `plugins/service-lando/src/index.ts`: import, export, serviceTypes map, manifest contributes
  - `plugins/service-lando/plugin.yaml`: added `mongodb` to contributes.serviceTypes
  - `plugins/service-lando/test/mongodb.test.ts`: unit tests for plan defaults, credentials, overrides, healthcheck, LANDO_* env, user-env merge/rejection
  - `plugins/service-lando/test/mongodb.scenario.test.ts`: AppPlanner scenario + fake-provider tooling routing for `lando mongosh`
  - `plugins/service-lando/test/registration.test.ts`: added mongodb to expected contributes list
  - `plugins/service-lando/test/package.test.ts`: added mongodb to expectedServiceTypes
  - `sdk/test/fixtures/schema-snapshot.json`: regenerated to include mongodb
  - `docs/guides/services/mongodb.mdx`: required guide per PRD-B-02 guide coverage table
- Verification: `bun run typecheck` → exit 0; `bun run lint` → 4 pre-existing warnings, no errors; `bun test plugins/service-lando/` → 194 pass, 1 skip, 0 fail; full suite → 1385 pass, 17 skip, 15 fail (all 15 are pre-existing codegen/scenario failures, same set existed on main before this branch)
- **Learnings for future iterations:**
  - The `plugin.yaml` and `package.test.ts` `expectedServiceTypes` list must both be updated when adding a new service type — they are checked against the runtime `serviceTypes` Map independently.
  - The schema snapshot (`sdk/test/fixtures/schema-snapshot.json`) must be regenerated via `bun run scripts/build-schema-snapshot.ts` whenever `plugin.yaml` contributes a new service type; the `schema-snapshot` CI gate catches any drift.
  - MongoDB credentials use `MONGO_INITDB_ROOT_USERNAME` / `MONGO_INITDB_ROOT_PASSWORD` / `MONGO_INITDB_DATABASE`; unlike MySQL/MariaDB, the Docker image has a single root-level user concept, so there is no separate app-user/root-user split.
  - Tooling `cmd: "string"` wraps in `sh -c <cmd> <args>` at the engine level; use `cmd: ["binary"]` array form in tests to verify exact argument vectors.
---

## [2026-05-28] - Push
- Branch: us-084-mongodb-service-type
- Push: pushed
- Commit(s):
  - d21a8c8 feat: mongodb service type
  - 79aae5a chore: mark US-084 complete and update progress log
- PR: #183 https://github.com/AaronFeledy/core4/pull/183 (created)
---

## [2026-05-27] - US-085
- Implemented `memcached` ServiceType in `@lando/service-lando` per spec §6.12.1
- Files changed:
  - `plugins/service-lando/src/services/memcached.ts`: new `memcachedServiceType` with default image `memcached:1.6`, TCP endpoint on port 11211, command-based TCP healthcheck (`bash -c "exec 3<>/dev/tcp/127.0.0.1/<port>"`), no persistent storage (in-memory cache), and standard LANDO_* env contract
  - `plugins/service-lando/src/index.ts`: import, export, serviceTypes map, manifest contributes
  - `plugins/service-lando/plugin.yaml`: added `memcached` to contributes.serviceTypes
  - `plugins/service-lando/test/memcached.test.ts`: unit tests for plan defaults, overrides, healthcheck, LANDO env, user-env merge/reserved-key rejection
  - `plugins/service-lando/test/memcached.scenario.test.ts`: AppPlanner scenario + fake-provider tooling routing for `lando memcached-tool stats` and a set+get sequence
  - `plugins/service-lando/test/registration.test.ts`: added memcached to expected contributes list
  - `plugins/service-lando/test/package.test.ts`: added memcached to expectedServiceTypes
  - `sdk/test/fixtures/schema-snapshot.json`: regenerated via `bun run scripts/build-schema-snapshot.ts` to include memcached
  - `docs/guides/services/memcached.mdx` + `docs/guides/memcached/fixtures/memcached-demo/.lando.yml`: required guide coverage with happy-path, memcached-stats, version-override scenarios
- Verification: `bun run typecheck` → exit 0; `bun run lint` → 4 pre-existing warnings, no errors; `bun run lint:guides` → "Guide lint passed."; `bun test plugins/service-lando/` → 193 pass, 1 skip, 0 fail; `bun test test/scenarios/generated/guides/memcached` → 3 pass, 0 fail; full suite `bun test` → 1420 pass, 17 skip, 0 fail
- **Learnings for future iterations:**
  - Memcached has NO persistent storage (in-memory cache); `storage: []` on the ServicePlan is the correct shape, unlike redis/mongodb/postgres which all declare a data volume.
  - The TCP-port-probe healthcheck pattern (`bash -c "exec 3<>/dev/tcp/127.0.0.1/<port>"`) used by go/python/ruby works equally well for protocol-light TCP services like memcached — no need to ship the `memcached` binary or a custom probe.
  - Tooling aliases for non-canonical commands (like `memcached-tool` which is not present in the default `memcached:1.6` image) can be implemented as `sh -c` wrappers using BusyBox-style `nc`; the scenario test exercises the wrapper through the fake provider exec without requiring the binary to exist at planning time.
  - Running the full `bun test` suite triggers `core/test/scripts/codegen-ci.test.ts`, which re-runs the workflow codegen and writes `.github/workflows/ci.yml`. If `ci.yml` contains hand-edits not reflected in `scripts/build-ci-workflow.ts` (e.g. ad-hoc `podman pull` lines or extra test invocations), the full-suite run will mutate `ci.yml` and the codegen output will diverge from the committed file. Story builds should `git checkout .github/workflows/ci.yml` after running the full suite to keep the commit scoped to the story.
---

## [2026-05-27 22:40 CDT] - US-085 Review
- Reviewed the US-085 memcached service type implementation against `spec/beta/prd-beta-02-service-catalog-full.md`, `review-work` agents, direct focused tests, and full-suite verification.
- Fixed confirmed review blockers:
  - Kept Memcached's default command aligned with overridden internal ports so a `port:` override no longer advertises/checks a port the daemon is not listening on.
  - Added `memcached://localhost:<port>` rendering for `lando info` while preserving the provider-neutral TCP endpoint plan shape.
  - Added memcached to the shared service env-contract matrix and added a gated live provider integration test for text-protocol set/get.
  - Added the documented `memcached-tool` fixture alias and clarified the non-rendered guide note.
- Files changed:
  - `plugins/service-lando/src/services/memcached.ts`
  - `plugins/service-lando/test/memcached.test.ts`
  - `plugins/service-lando/test/memcached.scenario.test.ts`
  - `plugins/service-lando/test/memcached.integration.test.ts`
  - `plugins/service-lando/test/env-contract.test.ts`
  - `core/src/cli/commands/info.ts`
  - `core/test/cli/info.scenario.test.ts`
  - `docs/guides/services/memcached.mdx`
  - `docs/guides/memcached/fixtures/memcached-demo/.lando.yml`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: `port:` override updated the endpoint/healthcheck but not the default daemon command.
  - Fixed: `lando info` rendered Memcached as generic `tcp://` instead of the PRD's `memcached://` connection scheme.
  - Fixed: shared env-contract coverage did not include memcached.
  - Fixed: provider-integration coverage for live Memcached set/get was missing; added a `LANDO_TEST_PODMAN_SOCKET`-gated integration test.
  - Fixed: guide mentioned `lando memcached-tool stats` without a fixture alias.
  - Rejected/deferred: service-type-shipped tooling is specified in §6.11.3 but this codebase's current `ServiceTypeShape` has no tooling contribution field; US-085 keeps the scenario/fixture alias app-authored until that service-type resolution surface lands.
  - Rejected/deferred: the `EndpointPlan` schema still only allows transport protocols (`tcp`, `http`, etc.); Memcached keeps a TCP plan endpoint and maps to `memcached://` at the `lando info` presentation boundary.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/test/memcached.test.ts plugins/service-lando/test/memcached.scenario.test.ts plugins/service-lando/test/memcached.integration.test.ts plugins/service-lando/test/env-contract.test.ts core/test/cli/info.scenario.test.ts test/scenarios/generated/guides/memcached` → 89 pass, 1 skip, 0 fail.
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 with 4 pre-existing warnings in provider Docker/Podman integration tests.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → Guide lint passed.
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1425 pass, 18 skip, 0 fail; the run rewrote generated `.github/workflows/ci.yml` / schema snapshot formatting, which was restored to keep this review commit scoped.
  - LSP diagnostics were attempted for changed TypeScript files, but the TypeScript LSP server exited with code -2; `tsc -b` was clean.
- **Learnings for future iterations:**
  - For TCP service types with a configurable internal port, update the service daemon command as well as the endpoint and healthcheck.
  - The current service-type model cannot contribute tooling tasks directly; tests and guides that need `lando <tool>` aliases must define them in the Landofile fixture until §6.11.3 is implemented.
  - Transport endpoint plans remain `protocol: "tcp"`; user-facing service schemes such as `memcached://` belong at the `lando info` rendering boundary unless/until the endpoint schema grows a scheme field.
---

## [2026-05-27 22:58 CDT] - Push
- Branch: us-085-memcached-service-type
- Push: pushed
- Commit(s): e19d9f5 feat: memcached service type; 58d2e45 chore: mark US-085 complete and update progress log; afe8364 fix: repair memcached service behavior; a63bdbb fix: render memcached info endpoint scheme; 17bafd1 docs: clarify memcached guide tooling; cd9f28c docs: record US-085 review fixes; 00c6f1f chore: cleanup service metadata; ce28f22 test: cleanup memcached coverage prose
- PR: #184 https://github.com/AaronFeledy/core4/pull/184 (created)
---

## [2026-05-27 22:30 UTC] - PR #184 provider-integration-linux-x64 triage
- CI failure: real issue; the live Docker contract fixture was a silent keep-alive process, so `runProviderContract` could legitimately see zero log chunks on the live socket.
- PR comments/reviews: none; Cursor Bugbot passed.
- Fix: `plugins/provider-docker/test/contract.integration.test.ts` now emits `console.log('lando-contract-ready')` before the keep-alive loop so the live log assertion has a guaranteed chunk.
- Verification: `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-docker/test/contract.integration.test.ts` → 13 pass, 2 skip, 0 fail; `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 (4 warnings, pre-existing non-null assertions in provider-docker/podman tests).

## [2026-05-27 UTC] - PR #184 triage
- CI failure: real issue; the CI workflow generator template lost `podman pull golang:1.22` from the provider pre-pull list, even though the checked-in workflow still had it, so regeneration could drop the golang image and make the test order-dependent.
- Fix: restored `podman pull golang:1.22` in `scripts/build-ci-workflow.ts` while keeping the memcached and service-lando integration pre-pulls intact.
- Verification: `PATH="$HOME/.bun/bin:$PATH" bun test core/test/build/ci-workflow.test.ts` → 8 pass, 0 fail; `PATH="$HOME/.bun/bin:$PATH" bunx biome check scripts/build-ci-workflow.ts` → clean; full `bun run typecheck` and `bun run lint` attempts hit the tool time limit here and did not complete.

## [2026-05-27 23:47 CDT] - Babysit
- PR: #184 https://github.com/AaronFeledy/core4/pull/184
- CI outcome: pass (all GitHub Actions checks and Cursor Bugbot check passed on latest HEAD)
- Review feedback: fixed (Bugbot codegen-template drift addressed with provider CI image pre-pull sync)
- Triage rounds used: 3
- Merge: deferred (PR is not mergeable; GitHub reports mergeStateStatus DIRTY / mergeable CONFLICTING against main)
- Local main / branch refreshed: no
- Remaining local state, if any: still on us-085-memcached-service-type; progress log updated with deferral note
---

## [2026-05-28 05:10 UTC] - US-085 rebase onto current main
- Rebased `us-085-memcached-service-type` onto current `main` (HEAD `02db2fc`, which carries the US-084 `mongodb` ServiceType from PR #183). PR #184 was previously DIRTY/CONFLICTING against main; this rebase resolves the conflicts and brings the branch back to a mergeable state.
- Conflicts resolved (all bookkeeping "add both" merges, keeping `mariadb` → `memcached` → `mongodb` → `mysql` alphabetical order):
  - `plugins/service-lando/plugin.yaml`: contributes.serviceTypes list
  - `plugins/service-lando/src/index.ts`: imports, exports, `serviceTypes` Map, manifest `contributes`
  - `plugins/service-lando/test/package.test.ts`: `expectedServiceTypes`
  - `plugins/service-lando/test/registration.test.ts`: expected contributes list
  - `sdk/test/fixtures/schema-snapshot.json`: bundled plugin manifest contributes
  - `spec/beta/progress.txt`: preserved US-084 entry from main and US-085 entry from branch chronologically
- No source code changes to memcached implementation or tests — only collision-avoidance reorderings.
- Verification on rebased tip:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → 4 pre-existing warnings in `plugins/provider-docker/src/index.ts` (npipe chunked decoder optional-chain hint); 0 errors; no fixes applied
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → "Guide lint passed."
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/` → 209 pass, 2 skip, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/test/memcached.scenario.test.ts plugins/service-lando/test/memcached.test.ts plugins/service-lando/test/memcached.integration.test.ts` → 11 pass, 1 skip, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1436 pass, 18 skip, 0 fail
- CLI verification: drove the memcached service plan end-to-end through `bun core/bin/lando.ts info --renderer=json` against `/tmp/lando-memcached-smoke/.lando.yml` declaring `cache: { type: memcached }`. The command surfaced the expected `ProviderConfigError`/`select` envelope (no provider configured in the temp dir), proving the service-type registry resolves `memcached` without hitting an "unsupported service type" rejection. Functional behavior (apply / inspect / tooling routing) is covered by `memcached.scenario.test.ts` against the fake provider.
- **Learnings for future iterations:**
  - When a build-step branch sits idle long enough for a sibling story (US-084 mongodb) to land first, the rebase pattern here is mechanical: every conflict was the alphabetical service-list collision (mariadb, memcached, mongodb, mysql) in the same five files. Resolve them once with `git checkout --theirs` is wrong (it drops one of the two contributions); always do "keep both, sort alphabetically" and rerun the schema-snapshot/test-list assertions to catch order regressions.
  - `git rebase --continue` with `vim` as the editor will hang under non-interactive shells; set `GIT_EDITOR=true` to accept the prepared commit message verbatim.
  - `spec/beta/progress.txt` is the most likely conflict file across stories because every iteration appends to it. The conflict is always "keep both entries chronologically" — there is no semantic merge needed.
---
## [2026-05-28 05:28 UTC] - Push
- Branch: us-085-memcached-service-type
- Push: pushed (force-with-lease after remote divergence from prior branch history)
- Commit(s): a422f4b chore: cleanup; 8b45671 docs: record US-085 rebase onto current main; 98ae55e docs: record PR #184 merge deferral; 0591d39 chore: record PR #184 triage; 84c1932 fix: sync provider CI image pre-pulls; 4349301 fix: restore go image pre-pull in provider CI; 8644a4a fix: pre-pull memcached in provider CI; 37674eb fix: ensure live docker contract emits logs; 17ac9ee docs: record US-085 push; a07e6b3 test: cleanup memcached coverage prose; 41d5c27 chore: cleanup service metadata; b0f06ee docs: record US-085 review fixes; f7b499a docs: clarify memcached guide tooling; 044e672 fix: render memcached info endpoint scheme; bec1bfe fix: repair memcached service behavior; bf35c7c chore: mark US-085 complete and update progress log; 10a5bb2 feat: memcached service type
- PR: #184 https://github.com/AaronFeledy/core4/pull/184 (existing)
---


## [2026-05-28 05:58 UTC] - Babysit
- PR: #184 https://github.com/AaronFeledy/core4/pull/184
- CI outcome: pass
- Review feedback: fixed (prior Cursor Bugbot thread resolved; rerun cleared transient `build-linux-x64` artifact upload stall)
- Triage rounds used: 1
- Merge: merged (squash)
- Local main / branch refreshed: yes
- Remaining local state, if any: none
---

## 2026-05-28 00:59:31 - Sync
- Previous branch: us-085-memcached-service-type
- Previous PR: #184 https://github.com/AaronFeledy/core4/pull/184
- Action: caught up after prior merge
- Local main updated: yes
- Branch after sync: main
---

## [2026-05-28 06:15 UTC] - US-086
- Implemented `valkey` ServiceType in `@lando/service-lando` per spec §6.12.1 with dual-scheme endpoint emission (`valkey://` primary, `redis://` alias).
- Files changed:
  - `plugins/service-lando/src/services/valkey.ts`: new `valkeyServiceType` with default image `valkey/valkey:8`, TCP endpoint on port 6379, persistent `/data` volume, command-based TCP healthcheck (`bash -c "exec 3<>/dev/tcp/127.0.0.1/<port>"`), `valkey-server --appendonly yes --port <port>` default command that tracks port overrides, standard LANDO_* env contract.
  - `plugins/service-lando/src/index.ts`: import, export, serviceTypes map (key `valkey`), manifest contributes.
  - `plugins/service-lando/plugin.yaml`: added `valkey` to contributes.serviceTypes.
  - `plugins/service-lando/test/valkey.test.ts`: unit tests for plan defaults, overrides, healthcheck, LANDO env, user-env merge/reserved-key rejection, command port tracking.
  - `plugins/service-lando/test/valkey.scenario.test.ts`: AppPlanner scenario + fake-provider tooling routing for `lando valkey-cli ping` and a set+get sequence.
  - `plugins/service-lando/test/valkey.integration.test.ts`: live `LANDO_TEST_PODMAN_SOCKET`-gated RESP ping/set/get against `valkey/valkey:8` with a hand-rolled RESP completion detector covering `+`, `-`, and `$` reply forms.
  - `plugins/service-lando/test/registration.test.ts`: added `valkey` to expected contributes list.
  - `plugins/service-lando/test/package.test.ts`: added `valkey` to expectedServiceTypes.
  - `plugins/service-lando/test/env-contract.test.ts`: added `valkey` to the shared LANDO_* env-contract matrix.
  - `core/src/cli/commands/info.ts`: changed `endpointText` to return `ReadonlyArray<string>` and switched the call site from `.map` to `.flatMap`; `valkey` over `tcp` emits both `valkey://localhost:<port>` and `redis://localhost:<port>`.
  - `core/test/cli/info.scenario.test.ts`: extended fixture plan with a `valkey` service and added running/stopped assertions verifying the dual-scheme rendering.
  - `sdk/test/fixtures/schema-snapshot.json`: regenerated via `bun run scripts/build-schema-snapshot.ts` to include `valkey` in the bundled `@lando/service-lando` manifest contributes.
  - `scripts/build-ci-workflow.ts` + `.github/workflows/ci.yml`: pre-pull `valkey/valkey:8` alongside the other provider-integration images so the live integration test can run in CI.
  - `docs/guides/services/valkey.mdx` + `docs/guides/valkey/fixtures/valkey-demo/.lando.yml`: required PRD-B-02 guide coverage with happy-path, dual-scheme-endpoint, valkey-cli (ping/set/get), and version-override scenarios.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → 4 pre-existing warnings in provider Docker/Podman tests, no errors
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → "Guide lint passed."
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/` → 224 pass, 3 skip, 0 fail (skips: valkey + memcached live integrations gated on `LANDO_TEST_PODMAN_SOCKET`, one mongodb integration gate)
  - `PATH="$HOME/.bun/bin:$PATH" bun test core/test/cli/info.scenario.test.ts plugins/service-lando/test/valkey.test.ts plugins/service-lando/test/valkey.scenario.test.ts plugins/service-lando/test/env-contract.test.ts plugins/service-lando/test/package.test.ts plugins/service-lando/test/registration.test.ts` → 104 pass, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test test/scenarios/generated/guides/valkey/` → 4 pass, 0 fail (happy-path, dual-scheme-endpoint, valkey-cli, version-override)
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1458 pass, 18 skip, 0 fail
- CLI verification: scenario test `info.scenario.test.ts` "prints running services with endpoint URLs as plain text" exercises the full `infoApp()` → `renderInfoAppResult()` path through the real Effect runtime against a fake provider and asserts `valkey\s+running\s+valkey:\/\/localhost:6379,\s*redis:\/\/localhost:6379`. The compiled `core/bin/lando.ts` info path was also driven against a temp `.lando.yml` declaring `type: valkey`; outside a configured provider sandbox it surfaces the expected `ProviderConfigError` envelope, proving the service-type registry resolves `valkey` without hitting an "unsupported service type" rejection. Functional dual-scheme rendering is covered by the scenario test against the fake provider.
- **Learnings for future iterations:**
  - Dual-scheme endpoint emission is a presentation concern, not a planner concern. Switching `endpointText` from `string` to `ReadonlyArray<string>` and the caller from `.map` to `.flatMap` cleanly supports 1-endpoint→N-renderings without changing `EndpointPlan` schema or planner output.
  - The Valkey default command must explicitly include `--port <port>` so a `port:` override flows into the daemon (the same lesson US-085 learned for memcached's `-p <port>`). The default `valkey-server` argv would otherwise listen on 6379 regardless of the endpoint/healthcheck port.
  - `valkey/valkey:8` is the Linux Foundation Valkey project's official Docker Hub image; `valkey:8` (no namespace) does not resolve because Docker Hub's `library/` namespace has no valkey entry. Pin the namespaced image and document the override path (`image: valkey/valkey:7`) for users that want Valkey 7.
  - The Valkey image speaks RESP (Redis Serialization Protocol), so the live integration test issues raw `PING`/`SET`/`GET` over TCP and parses `+`/`-`/`$` reply types. There is no need for a `valkey-cli` binary on the host; the protocol is stable across Redis/Valkey forks.
  - When adding a service type that needs a different Docker image namespace, the CI workflow generator (`scripts/build-ci-workflow.ts`) needs the new image added to the `podman pull` pre-pull list so the provider-integration job has the image cached before the live test runs.
---

## [2026-05-28 08:37 UTC] - US-086 Review
- Reviewed build commit `51ef2dac feat: valkey service type` against `spec/beta/prd-beta-02-service-catalog-full.md` US-086 and the broader service catalog notes.
- Files changed:
  - `spec/beta/progress.txt`: appended this review record only.
- `review-work` findings:
  - Fixed: none; no confirmed in-scope code defect was found.
  - Rejected/deferred: service-type-shipped tooling (`§6.11.3`) is still not implemented in the current `ServiceTypeShape`; US-086 keeps the same accepted fixture-authored `tooling:` alias pattern documented during US-085, and `valkey.scenario.test.ts` verifies `lando valkey-cli ping/set/get` routing through provider exec.
  - Rejected/deferred: PRD-B-02's broad “specific minor and SHA” pin-manifest note conflicts with current service-lando practice for the recent catalog stories (`mongo:7`, `memcached:1.6`, `valkey/valkey:8`) and there is no shipped pin-manifest/codegen surface to update in this branch; keeping the existing image-tag pattern avoids inventing a new pinning mechanism in a single-service review.
  - Noted: `spec/beta/prd-beta-02-service-catalog-full.md` guide drift paths still list `plugins/service-valkey/**` even though the implementation lives in `plugins/service-lando/**`; this is pre-existing PRD path drift shared by multiple service stories and not a US-086 implementation blocker.
- Verification run and result:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 with 4 pre-existing warnings in provider Docker/Podman contract integration tests (`inner.api.request!` non-null assertions), unchanged by US-086.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → Guide lint passed.
  - `PATH="$HOME/.bun/bin:$PATH" bun test core/test/cli/info.scenario.test.ts plugins/service-lando/test/valkey.test.ts plugins/service-lando/test/valkey.scenario.test.ts plugins/service-lando/test/env-contract.test.ts plugins/service-lando/test/package.test.ts plugins/service-lando/test/registration.test.ts` → 104 pass, 0 fail.
  - `PATH="$HOME/.bun/bin:$PATH" bun test test/scenarios/generated/guides/valkey/` → 4 pass, 0 fail.
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/test/valkey.integration.test.ts` → 0 pass, 1 skip, 0 fail (`LANDO_TEST_PODMAN_SOCKET` not set).
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1458 pass, 19 skip, 0 fail.
- **Learnings for future iterations:**
  - For cache/search catalog stories before the §6.11.3 `ServiceTypeResolution.tooling` surface lands in implementation, `lando <tool>` acceptance remains fixture-authored `tooling:` plus provider-exec scenario coverage rather than service-type-contributed tooling.
  - The beta PRD's SHA pinning note is ahead of the current `@lando/service-lando` implementation; do not invent per-story digest machinery without first landing the shared pin manifest/codegen path.
---

## [2026-05-28 08:40 UTC] - Push
- Branch: us-086-valkey-service-type
- Push: pushed
- Commit(s): 51ef2da feat: valkey service type; 2af3238 docs: record US-086 review
- PR: #185 https://github.com/AaronFeledy/core4/pull/185 (created)
---

- 2026-05-28: Triage provider-integration-linux-x64 logs failure; switched the SDK contract logs check to follow:false so the live Docker contract reads a bounded historical snapshot instead of racing the initial follow stream chunk.
- 2026-05-28: PR #185 provider-integration-linux-x64 second triage — root cause was provider-docker's logs decoder treating framed Docker log bytes as single-chunk data, which dropped output when CI split the first frame across socket chunks. Fixed by buffering framed log bytes across chunks in `plugins/provider-docker/src/index.ts` and added regression coverage for split framed log bytes in `plugins/provider-docker/test/contract.integration.test.ts`. Verification: `bun test plugins/provider-docker/test/contract.integration.test.ts`, `bun test sdk/test/contract/provider.test.ts`, `bun run typecheck`, `bun run lint` (pre-existing warnings only).

## [2026-05-28] - PR #185 third triage
- Restored the live contract log collection to `follow: true` now that provider-docker framed log buffering is fixed, avoiding an empty immediate Docker history snapshot.

- 2026-05-28: PR #185 provider-integration-linux-x64 fourth triage — made the SDK contract service emit `lando-contract-ready` periodically so the live follow:true Docker logs stream receives a chunk even when the startup log was already consumed or not yet available.

## [2026-05-28 07:45 UTC] - Babysit
- PR: #185 https://github.com/AaronFeledy/core4/pull/185
- CI outcome: pass
- Review feedback: fixed (provider-integration-linux-x64 live Docker logs contract failures fixed; no review comments)
- Triage rounds used: 4
- Merge: merged (squash)
- Local main / branch refreshed: yes
- Remaining local state, if any: none
---

## [2026-05-28 02:48:10] - Sync
- Previous branch: us-086-valkey-service-type
- Previous PR: #185 https://github.com/AaronFeledy/core4/pull/185
- Action: caught up after prior merge
- Local main updated: yes
- Branch after sync: main
---

## [2026-05-28 03:02 UTC] - US-087
- Story: US-087 solr service type
- Branch: us-087-solr-service-type
- Commit: feat: solr service type (solr:9 with curl healthcheck and opt-in cores)
- Files changed:
  - sdk/src/schema/index.ts: added `cores?: string[]` to ServiceConfig
  - plugins/service-lando/src/services/solr.ts: solr9ServiceType + solrServiceType alias
  - plugins/service-lando/src/index.ts: registered solr and solr:9
  - plugins/service-lando/plugin.yaml: added solr, solr:9 to contributes
  - plugins/service-lando/test/solr.test.ts: 12 unit tests (all pass)
  - plugins/service-lando/test/solr.scenario.test.ts: 4 scenario tests (all pass)
  - plugins/service-lando/test/solr.integration.test.ts: 2 live tests (skipped without podman socket)
  - plugins/service-lando/test/package.test.ts: updated expectedServiceTypes
  - plugins/service-lando/test/registration.test.ts: updated hardcoded list
- Healthcheck: kind=command (curl -sf http://localhost:8983/solr/admin/ping) — http kind rejected by Alpha planner
- Tests: 240 pass / 5 skip / 0 fail; typecheck pass; lint pass (4 pre-existing warnings only)

## [2026-05-28 04:08 UTC] - Babysit
- PR: #186 https://github.com/AaronFeledy/core4/pull/186
- CI outcome: pass
- Review feedback: none
- Triage rounds used: 4 (schema-snapshot stale; solr:9 not in CI pre-pull; admin ping returns 404 without core; cores live test unreliable)
- Merge: merged (squash)
- Local main / branch refreshed: yes
- Remaining local state, if any: none
---

## [2026-05-28 04:12:26] - Sync
- Previous branch: us-087-solr-service-type
- Previous PR: #186 https://github.com/AaronFeledy/core4/pull/186
- Action: caught up after prior merge
- Local main updated: yes
- Branch after sync: main
---

## [2026-05-28 CDT] - US-088
- Implemented `elasticsearch` service type for `@lando/service-lando`, including default Elasticsearch 8 image, single-node + security-disabled local-dev defaults, persistent storage, HTTP cluster-health command healthcheck, and an `es-cli` tooling alias exercised in the scenario test.
- Files changed:
  - `plugins/service-lando/src/services/elasticsearch.ts` (new): `elasticsearch8ServiceType` (id `elasticsearch:8`) + `elasticsearchServiceType` alias (id `elasticsearch`); defaults `discovery.type=single-node`, `xpack.security.enabled=false`, `ES_JAVA_OPTS=-Xms512m -Xmx512m`; default port 9200 with curl command healthcheck on `/_cluster/health`; data volume mounted at `/usr/share/elasticsearch/data` as `<app>-elasticsearch-data`.
  - `plugins/service-lando/src/index.ts`: registers both ids in `serviceTypes`, the manifest, and re-exports.
  - `plugins/service-lando/plugin.yaml`: lists `elasticsearch` and `elasticsearch:8` under `contributes.serviceTypes` (alphabetical).
  - `plugins/service-lando/test/elasticsearch.test.ts` (new): unit tests for default plan, env defaults, image/port overrides, healthcheck command + port tracking, LANDO_* env, user env merge, reserved-key rejection.
  - `plugins/service-lando/test/elasticsearch.scenario.test.ts` (new): exercises `AppPlanner` + `es-cli` tooling alias through `runTooling` for `/_cluster/health` and `/_cat/indices`.
  - `plugins/service-lando/test/elasticsearch.integration.test.ts` (new): live Podman boot of Elasticsearch 8 gated by `LANDO_TEST_PODMAN_SOCKET`; asserts cluster status is `green` or `yellow`.
  - `plugins/service-lando/test/package.test.ts`, `plugins/service-lando/test/registration.test.ts`: extend the expected `serviceTypes` list (alphabetical insertion of `elasticsearch`, `elasticsearch:8`).
  - `scripts/build-ci-workflow.ts` + regenerated `.github/workflows/ci.yml`: adds `podman pull elasticsearch:8` to the integration pre-pull step.
  - `docs/guides/services/elasticsearch.mdx` (new): required guide coverage with `happy-path` (rendered), `cluster-health`, `cat-indices`, `version-override`, and `security-warning` scenarios.
  - `docs/guides/elasticsearch/fixtures/elasticsearch-demo/.lando.yml` (new): minimal app fixture with `type: elasticsearch` and an `es-cli` curl tooling alias.
  - `sdk/test/fixtures/schema-snapshot.json` (regenerated): includes the two new ids.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → 0 errors, 4 pre-existing non-null-assertion warnings in provider-docker/provider-podman tests (unrelated)
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → "Guide lint passed."
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/` → 251 pass, 5 skip, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1491 pass, 21 skip, 0 fail
  - CLI verification: parsed the new fixture with `LandofileShape` and ran `elasticsearch8ServiceType.toServicePlan(...)` end-to-end — healthcheck `/_cluster/health`, endpoint `tcp/9200`, storage `<app>-elasticsearch-data`, env `discovery.type=single-node`, `xpack.security.enabled=false`, `LANDO_SERVICE_TYPE=elasticsearch` all confirmed.
- **Learnings for future iterations:**
  - The `serviceTypes` expectation arrays in `plugins/service-lando/test/{package,registration}.test.ts` are in the same alphabetical order as `serviceTypes` in `src/index.ts`; new entries MUST be inserted at the alphabetically-correct position in both source and tests or the package-skeleton/registration tests will fail with a clear `toEqual` diff.
  - The Guide Coverage table in `spec/beta/prd-beta-02-service-catalog-full.md` is selective: not every service story requires `docs/guides/services/<name>.mdx`. Only the stories explicitly listed in §Guide Coverage (US-084 mongodb, US-086 valkey, US-088 elasticsearch, US-093 static, US-094 compose-passthrough) need a guide file; the others (e.g. US-087 solr, US-089 opensearch) ship without one.
  - Generated guide scenario tests live under `test/scenarios/generated/guides/<id>/<scenario>.test.ts` and are gitignored; adding a new guide requires running `bun run scripts/build-guide-scenarios.ts` (also invoked by `bun run codegen`) so the suite picks them up locally, but the regenerated files MUST NOT be committed.
  - The `bun run codegen` pipeline also rewrites `.github/workflows/ci.yml` and `sdk/test/fixtures/schema-snapshot.json`; when a new service-type pulls a new container image, edit `scripts/build-ci-workflow.ts` (not the workflow directly) and re-run codegen so both files stay in sync.
  - Mirroring the solr healthcheck pattern, Elasticsearch uses a `command` healthcheck (`bash -c curl -sf ...`) rather than an HTTP healthcheck because the §6.6 healthcheck schema in this repo expresses HTTP probes via curl-in-container; copy this pattern for opensearch/meilisearch in subsequent stories.
---

## [2026-05-28 12:06 UTC] - US-088 Review
- Reviewed build commit `c0d1f19a feat: Add elasticsearch service type` against `spec/beta/prd-beta-02-service-catalog-full.md` US-088 using `review-work` plus explore/librarian/code-quality/security/QA/context-mining agents.
- Files changed:
  - `plugins/service-lando/src/services/elasticsearch.ts`: switched the default image to the valid official Elastic registry tag `docker.elastic.co/elasticsearch/elasticsearch:8.17.0` and set `http.port` to track `port:` overrides.
  - `plugins/service-lando/test/elasticsearch.test.ts`, `plugins/service-lando/test/elasticsearch.scenario.test.ts`, `plugins/service-lando/test/elasticsearch.integration.test.ts`, `plugins/service-lando/test/env-contract.test.ts`: updated assertions for the official image/http.port, added live `/_cat/indices` coverage, and added Elasticsearch to the shared LANDO_* env-contract matrix.
  - `docs/guides/services/elasticsearch.mdx`, `docs/guides/elasticsearch/fixtures/elasticsearch-demo/.lando.yml`: documented the official image and Elastic License v2 distinction; hardened the `es-cli` fixture alias against shell word splitting/argument injection.
  - `scripts/build-ci-workflow.ts`, `.github/workflows/ci.yml`: pre-pull the official Elastic registry image used by the service default.
- `review-work` findings:
  - Fixed: invalid `elasticsearch:8` default/CI image; `docker manifest inspect elasticsearch:8` failed with `no such manifest`, while `docker.elastic.co/elasticsearch/elasticsearch:8.17.0` resolves.
  - Fixed: `port:` overrides updated the endpoint and healthcheck but not Elasticsearch's own HTTP listener; `http.port` now tracks the planned port.
  - Fixed: live integration now exercises `/_cat/indices` after cluster health succeeds, so index-operation coverage is not fake-provider-only.
  - Fixed: Elasticsearch added to the shared `env-contract.test.ts` matrix.
  - Fixed: guide/fixture now mention Elastic License v2 and use a quoted `curl --` wrapper for the `es-cli` path argument.
  - Rejected/deferred: `elasticsearch:7` service-type id and index-init config were not added in review; current repo service stories expose major-version alternatives through `image:` overrides until the shared artifact/version-pin surface and index-init schema land, and US-088 acceptance only requires the `elasticsearch` service type plus health/index operations through tooling.
  - Rejected/deferred: service-type-shipped tooling remains a known §6.11.3 implementation gap; US-088 follows the current fixture-authored tooling alias pattern used by recent service stories.
- Verification run and result:
  - `docker manifest inspect docker.elastic.co/elasticsearch/elasticsearch:8.17.0` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/test/elasticsearch.test.ts plugins/service-lando/test/elasticsearch.scenario.test.ts plugins/service-lando/test/elasticsearch.integration.test.ts plugins/service-lando/test/env-contract.test.ts plugins/service-lando/test/package.test.ts plugins/service-lando/test/registration.test.ts` → 105 pass, 1 skip, 0 fail (live Elasticsearch integration skipped because `LANDO_TEST_PODMAN_SOCKET` is unset).
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 with 4 pre-existing provider Docker/Podman non-null assertion warnings, unrelated to US-088.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → Guide lint passed.
  - `PATH="$HOME/.bun/bin:$PATH" bun run codegen` → regenerated guide scenarios/bundled indexes/schema/CI with no additional tracked diffs beyond the intended CI workflow update.
  - `PATH="$HOME/.bun/bin:$PATH" bun run build` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1495 pass, 21 skip, 0 fail. Two earlier full-suite attempts failed due to concurrent build/test access to `core/dist/lando` (`ETXTBSY`/`EACCES`), then passed after rerunning with no concurrent build process.
  - `lsp_diagnostics` on changed TypeScript files could not run because the TypeScript LSP exited with code `-2`; `bun run typecheck` covered type errors.
- **Learnings for future iterations:**
  - Validate external service default images with `docker manifest inspect` during review, especially when the official registry is not Docker Hub's `library/` namespace.
  - Search-engine services with `port:` overrides need to configure the daemon listener as well as the endpoint/healthcheck; for Elasticsearch this is `http.port`.
  - For shell-backed guide tooling aliases, prefer a quoted positional argument wrapper plus `curl --` instead of appending raw `$*`.
---

## [2026-05-28 07:41 CDT] - Push
- Branch: us-088-elasticsearch-service-type
- Push: pushed
- Commit(s):
- d1bb419 Clean up Elasticsearch env contract test
- 175388a docs: record US-088 review
- a913337 Update Elasticsearch CI image pre-pull
- d3741ee Update Elasticsearch guide defaults
- 6f3f163 Fix Elasticsearch service runtime defaults
- 6ee5d1d chore: mark US-088 complete and update progress log
- c0d1f19 feat: Add elasticsearch service type
- PR: #187 https://github.com/AaronFeledy/core4/pull/187 (created)
---

## [2026-05-28 13:22 UTC] - PR #187 Unicode triage
- Triaged Cursor Bugbot feedback on commit `15f026b`: `spec/beta/prd.json` had been serialized with escaped non-ASCII characters, creating a noisy human-unfriendly PRD diff.
- Fix already pushed in `48f96b4`: restored literal Unicode in `spec/beta/prd.json` while preserving US-088 `passes: true` and the existing story pass states.
---

## [2026-05-28 09:48 CDT] - Babysit
- PR: #187 https://github.com/AaronFeledy/core4/pull/187
- CI outcome: pass (build-linux-x64, bundled-codegen, guide-scenarios-linux-x64, library-api-tests, provider-integration-linux-x64, recipe-tests, schema-snapshot, static-checks, Cursor Bugbot)
- Review feedback: fixed (Bugbot ordering feedback fixed in `15f026b`; follow-up PRD Unicode escaping feedback fixed in `48f96b4` and recorded in `282852d`)
- Triage rounds used: 2
- Merge: merged (squash)
- Local main / branch refreshed: yes
- Remaining local state, if any: on `main`; local story branch deleted; progress log updated with this babysit report
---

## 2026-05-28 09:51 CDT - Sync
- Previous branch: us-088-elasticsearch-service-type
- Previous PR: #187 https://github.com/AaronFeledy/core4/pull/187
- Action: caught up after prior merge
- Local main updated: yes
- Branch after sync: main
---

## 2026-05-28 10:05 CDT - US-087 Verification
- Story: US-087 solr service type — already implemented and merged via PR #186 (`2fe17274`); PRD entry still had `passes: false`.
- Branch: us-087-solr-service-type (re-created locally for the passes-flag fix; original was deleted post-merge)
- Verification on `main` (no code changes required):
  - All acceptance criteria covered: `solr` ServiceType + curl healthcheck against admin endpoint (`/solr/admin/info/system`), opt-in `cores:` config via precreate-core, scenario test `lando solr-admin status` (`plugins/service-lando/test/solr.scenario.test.ts:171`), package/registration tests list `solr` and `solr:9`.
  - `bun test plugins/service-lando/test/solr.test.ts plugins/service-lando/test/solr.scenario.test.ts` → 16 pass / 0 fail.
  - `bun test plugins/service-lando/test/package.test.ts plugins/service-lando/test/registration.test.ts` → 14 pass / 0 fail.
  - `bun test` (full suite) → 1495 pass / 21 skip / 0 fail across 214 files.
  - `bun run typecheck` → exit 0.
  - `bun run lint` → 4 pre-existing non-null-assertion warnings only (unrelated to US-087).
- Files changed in this build step:
  - `spec/beta/prd.json`: `US-087.passes` flipped `false` → `true`.
  - `spec/beta/progress.txt`: this entry (plus a prior Sync entry left uncommitted on main is carried with this commit).
- No source code changes — work was already merged. This is the completion-only confirmation path defined by the build step.
- **Learnings for future iterations:**
  - When a story is implemented on a branch but its `passes` flag is never flipped, that gap survives the sync/babysit hand-off and surfaces as the next "highest-priority failing" pick. Always include `spec/beta/prd.json` in the story branch's commit alongside the implementation files.
  - Sync entries appended to `spec/beta/progress.txt` need an owning commit; otherwise they ride along with the next story branch's work as an uncommitted diff on `main`.
---

## [2026-05-28 10:16 CDT] - US-087 Review
- Reviewed completion commit `3a6fc117 feat: solr service type` and the previously merged Solr implementation against `spec/beta/prd-beta-02-service-catalog-full.md` US-087 using `review-work`, explore, librarian, QA, security, code-quality, and context-mining agents.
- Files changed:
  - `plugins/service-lando/src/services/solr.ts`: changed the Solr endpoint protocol from `tcp` to `http` per PRD-B-02 endpoint scheme rules; validated `cores:` names and passed them as shell positional arguments instead of interpolating raw core names.
  - `plugins/service-lando/test/solr.test.ts`: updated HTTP endpoint expectations, safe core-command assertions, and unsafe core-name rejection coverage.
  - `plugins/service-lando/test/solr.scenario.test.ts`: made the `lando solr-admin status` scenario include opt-in core creation and updated HTTP endpoint/safe command assertions.
  - `plugins/service-lando/test/env-contract.test.ts`: added Solr to the catalog-wide LANDO_* environment contract matrix.
- `review-work` findings:
  - Fixed: Solr endpoint emitted `tcp` despite PRD-B-02 requiring HTTP endpoint scheme behavior for Solr.
  - Fixed: `cores:` values were interpolated directly into a `bash -c` startup command; now core names are allowlisted and passed via quoted positional parameters.
  - Fixed: scenario coverage split core creation and `lando solr-admin status`; the status scenario now plans `cores: ["gettingstarted"]` and asserts the precreate command.
  - Fixed: Solr was missing from the shared env-contract matrix.
  - Rejected/deferred: `/solr/admin/ping` was not restored; PR #186 triage already established it returns 404 without a core, and the current `/solr/admin/info/system` command healthcheck is the accepted admin endpoint substitute for a coreless default service.
  - Rejected/deferred: no dedicated Solr guide was added; PRD-B-02's Guide Coverage table does not require one for US-087.
  - Rejected/deferred: no SDK service-contract entry was added; the explicit contract-suite runner work is US-092, while US-087 currently requires service implementation, scenario, tests, typecheck, and lint.
- Verification run and result:
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/test/solr.test.ts plugins/service-lando/test/solr.scenario.test.ts plugins/service-lando/test/solr.integration.test.ts plugins/service-lando/test/env-contract.test.ts plugins/service-lando/test/package.test.ts plugins/service-lando/test/registration.test.ts` → exit 0 (live Solr integration remains gated/skipped unless `LANDO_TEST_PODMAN_SOCKET` is set).
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 with 4 pre-existing warnings.
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → exit 0. A subsequent duplicate full-suite attempt was interrupted by the harness after the prior successful run; no files changed between the successful run and this log entry.
  - `lsp_diagnostics` on changed TypeScript files could not run because the TypeScript LSP exited with code `-2`; `bun run typecheck` covered type errors.
- **Learnings for future iterations:**
  - Search-engine services that expose HTTP APIs may still need command healthchecks internally, but their `ServicePlan.endpoints` protocol must match the user-facing endpoint scheme expected by PRD-B-02 and `lando info` rendering.
  - Any service-level config that feeds a shell startup command needs an allowlist and/or positional-argument wrapper even when the command executes only inside the service container.
---

## [2026-05-28 10:27 CDT] - Push
- Branch: us-087-solr-service-type
- Push: pushed
- Commit(s): f6dbd85 docs: record US-087 review fixes; e12bbdb Fix Solr review blockers; 3a6fc11 feat: solr service type
- PR: #188 https://github.com/AaronFeledy/core4/pull/188 (created)
---

## [2026-05-28 10:36 CDT] - Babysit
- PR: #188 https://github.com/AaronFeledy/core4/pull/188
- CI outcome: pass (build-linux-x64, bundled-codegen, guide-scenarios-linux-x64, library-api-tests, provider-integration-linux-x64, recipe-tests, schema-snapshot, static-checks, Cursor Bugbot)
- Review feedback: none
- Triage rounds used: 0
- Merge: merged (squash)
- Local main / branch refreshed: yes
- Remaining local state, if any: on `main`; local story branch deleted; progress log updated with this babysit report
---

## [2026-05-28 CDT] - US-089
- Implemented `opensearch` service type for `@lando/service-lando`: Apache 2.0-licensed OpenSearch fork of Elasticsearch 7.10, mirrored on US-088 with the licensing distinction noted in an exported description constant.
- Files changed:
  - `plugins/service-lando/src/services/opensearch.ts` (new): `opensearch2ServiceType` (id `opensearch:2`) + `opensearchServiceType` alias (id `opensearch`); default image `opensearchproject/opensearch:2`; defaults `discovery.type=single-node`, `DISABLE_SECURITY_PLUGIN=true`, `DISABLE_INSTALL_DEMO_CONFIG=true`, `OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m`; HTTP endpoint on port 9200; `http.port` env tracks port overrides; curl `/_cluster/health` command healthcheck (intervals match elasticsearch); data volume mounted at `/usr/share/opensearch/data` as `<app>-opensearch-data`. Exports `OPENSEARCH_SERVICE_DESCRIPTION` documenting the Apache 2.0 vs Elastic License v2 distinction vs Elasticsearch.
  - `plugins/service-lando/src/index.ts`, `plugins/service-lando/plugin.yaml`: registered `opensearch` and `opensearch:2` in alphabetical order.
  - `plugins/service-lando/test/opensearch.test.ts` (new): unit tests for default plan, env defaults, image/port overrides, healthcheck command + port tracking, LANDO_* env, user env merge/reserved-key rejection, and licensing distinction in `OPENSEARCH_SERVICE_DESCRIPTION`.
  - `plugins/service-lando/test/opensearch.scenario.test.ts` (new): exercises `AppPlanner` + `os-cli` tooling alias through `runTooling` for `/_cluster/health` and `/_cat/indices`.
  - `plugins/service-lando/test/opensearch.integration.test.ts` (new): live Podman boot of OpenSearch 2 gated by `LANDO_TEST_PODMAN_SOCKET`; asserts cluster status is `green` or `yellow` and `/_cat/indices` returns text.
  - `plugins/service-lando/test/package.test.ts`, `plugins/service-lando/test/registration.test.ts`, `plugins/service-lando/test/env-contract.test.ts`: extend expected service-types list and shared LANDO_* env-contract matrix.
  - `scripts/build-ci-workflow.ts` + regenerated `.github/workflows/ci.yml`: pre-pull `opensearchproject/opensearch:2` so the live integration test can run in CI.
  - `sdk/test/fixtures/schema-snapshot.json`: regenerated via `bun run codegen` to include the new ids in the bundled `@lando/service-lando` manifest contributes.
  - `spec/beta/prd.json`: `US-089.passes` flipped `false` → `true`. (Also carries the prior PR #188 babysit progress entry that was committed on `main` but not yet pushed.)
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 with the same 4 pre-existing `inner.api.request!` warnings in provider Docker/Podman tests (unchanged by US-089)
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → "Guide lint passed."
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/test/opensearch.test.ts plugins/service-lando/test/opensearch.scenario.test.ts plugins/service-lando/test/env-contract.test.ts plugins/service-lando/test/package.test.ts plugins/service-lando/test/registration.test.ts` → 114 pass, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1520 pass, 22 skip, 0 fail across 217 files
- CLI verification: planned a fixture `name: demoapp; services.search.type: opensearch; port: 19200` through the registry-backed `AppPlanner` (same path the CLI uses). Result confirmed: `type=opensearch`, `artifact=opensearchproject/opensearch:2`, `endpoints=[{port:19200,protocol:tcp,name:search}]`, `storage=[{store:demoapp-opensearch-data,target:/usr/share/opensearch/data}]`, `healthcheck.command=["bash","-c","curl -sf http://localhost:19200/_cluster/health"]`, `LANDO_SERVICE_TYPE=opensearch`, `discovery.type=single-node`, `DISABLE_SECURITY_PLUGIN=true`, `DISABLE_INSTALL_DEMO_CONFIG=true`, `http.port=19200`, `OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m`. The OpenSearch live integration test path mirrors the US-088 fetch loop and is gated on `LANDO_TEST_PODMAN_SOCKET`; CI's pre-pull list now includes the OpenSearch image so the live test can run on `provider-integration-linux-x64`.
- **Learnings for future iterations:**
  - PRD-B-02's Guide Coverage table is selective; US-089 opensearch (like US-087 solr) is not required to ship a `docs/guides/services/<name>.mdx`. Only the listed stories (US-084 mongodb, US-086 valkey, US-088 elasticsearch, US-093 static, US-094 compose-passthrough) need guides.
  - OpenSearch 2 needs both `DISABLE_SECURITY_PLUGIN=true` and `DISABLE_INSTALL_DEMO_CONFIG=true` for a clean local-dev boot without an initial admin password; otherwise the entrypoint stalls on demo config installation and security plugin bootstrap.
  - The "service description notes the licensing distinction" acceptance criterion has no per-service description surface on `ServiceTypeShape`, so the right pattern is an exported string constant (`OPENSEARCH_SERVICE_DESCRIPTION`) that a unit test pins. This is the cleanest verifiable surface until a per-service description field lands.
  - Biome's import formatting collapses imports that fit on a single line; multi-line braces for short import lists trigger a lint error. Keep this in mind when writing new test files by hand.
---

## [2026-05-28 11:00 CDT] - US-089 Review
- Reviewed build commit `7ce2c034 feat: opensearch service type` against `spec/beta/prd-beta-02-service-catalog-full.md` US-089 using `review-work`-style goal, QA, code-quality, security, context-mining agents plus explore/librarian searches.
- Fixed confirmed review blocker:
  - Changed the OpenSearch service endpoint protocol from `tcp` to `http` so `lando info` / start output render the user-facing OpenSearch HTTP API as `http://localhost:<port>`, matching PRD-B-02 endpoint scheme rules and the US-087 Solr review learning.
- Files changed:
  - `plugins/service-lando/src/services/opensearch.ts`
  - `plugins/service-lando/test/opensearch.test.ts`
  - `plugins/service-lando/test/opensearch.scenario.test.ts`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: OpenSearch emitted a TCP endpoint despite being an HTTP API service; newer Solr review context established search-engine HTTP APIs should expose `protocol: "http"` while keeping curl command healthchecks internally.
  - Rejected: adding a dedicated OpenSearch guide; PRD-B-02's Guide Coverage table is selective and does not list US-089, matching the build-step learning.
  - Rejected/deferred: digest-pinning the OpenSearch CI pre-pull image; mutable tags match existing service-catalog CI pre-pull style and a broader artifact pinning policy would be cross-cutting.
  - Rejected/deferred: changing Elasticsearch's existing `tcp` endpoint during this review; this step stays scoped to US-089 and records the OpenSearch fix only.
- Verification run and result:
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/test/opensearch.test.ts plugins/service-lando/test/opensearch.scenario.test.ts plugins/service-lando/test/opensearch.integration.test.ts plugins/service-lando/test/env-contract.test.ts plugins/service-lando/test/package.test.ts plugins/service-lando/test/registration.test.ts` → exit 0.
  - Manual Bun driver imported `opensearchServiceType`, planned `type: opensearch` with `port: 19200`, and confirmed `endpoint.protocol === "http"`, `http.port=19200`, and the `/_cluster/health` command healthcheck.
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 with 4 pre-existing provider Docker/Podman warnings.
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us089-review-full.xml` → 1542 tests, 7183 assertions, 3 failures, 22 skipped; failures were the compiled `$bunfs` parity cases in `core/test/cli/deferred-commands.test.ts`, unrelated to OpenSearch. Reruns of `core/test/cli/deferred-commands.test.ts` and `core/test/build/compile.test.ts` both exited 0.
  - `lsp_diagnostics` on changed TypeScript files could not run because the TypeScript LSP exited with code `-2`; root typecheck was clean.
- **Learnings for future iterations:**
  - For HTTP search-engine services, the endpoint protocol controls user-facing URLs; command-based healthchecks do not imply `tcp` endpoint protocol.
---

## [2026-05-28 11:14 CDT] - Push
- Branch: us-089-opensearch-service-type
- Push: pushed
- Commit(s): 81682e3 Clean up OpenSearch service; 9f54811 Record US-089 review fix; ec424b2 Fix OpenSearch endpoint protocol; 7ce2c03 feat: opensearch service type
- PR: #189 https://github.com/AaronFeledy/core4/pull/189 (created)
---

## [2026-05-28 11:48 CDT] - Babysit
- PR: #189 https://github.com/AaronFeledy/core4/pull/189
- CI outcome: fail (`provider-integration-linux-x64` failed; all other reported checks including Cursor Bugbot passed)
- Review feedback: none (no PR comments, latest reviews, or review decision)
- Triage rounds used: 1
- Merge: deferred (`provider-integration-linux-x64` failed in the live Docker provider contract path: `logs emits at least one chunk`; triage was delegated, but no fix/new commit was pushed before the delegate went idle)
- Local main / branch refreshed: no
- Remaining local state, if any: still on `us-089-opensearch-service-type`; PR remains open; `spec/beta/progress.txt` has this local babysit deferral entry
---

## [2026-05-28 11:54 CDT] - PR #189 provider-integration-linux-x64 triage
- CI failure: real issue, but not caused by US-089 — pre-existing flake in the shared live Docker contract test. The branch touches zero provider-docker / sdk contract code (diff vs main: only `service-lando/opensearch*`, `spec/beta/*`, `schema-snapshot.json`, and one `podman pull opensearchproject/opensearch:2` line in `ci.yml`/`build-ci-workflow.ts`).
- Failed assertion: `RuntimeProvider contract failed: logs emits at least one chunk` (`sdk/src/test/index.ts:295-298`, 5s `Effect.timeoutFail`). Same assertion has been intermittently failing across recent PRs (Solr, Elasticsearch) and on `main`; the previous fix in 37674eba added a single `console.log('lando-contract-ready')` to the live Docker fixture but that one-shot line can race the 5s timeout on a slow GitHub Actions runner.
- Reruns: `gh run rerun 26588125725 --failed` failed identically — confirming the flake reproduces under the current CI load, not a transient blip.
- Local repro: with `DOCKER_HOST=unix:///var/run/docker.sock`, the live socket test passed 5/5 (~2.6s each), so the defect is CI-environment timing, not provider-docker logic.
- Fix: tightened the same fixture in `plugins/provider-docker/test/contract.integration.test.ts` to emit a periodic `lando-contract-tick` every 250ms (replacing the no-op `setInterval`). Guarantees Docker's log stream has multiple chunks well within the 5s contract timeout regardless of stdout buffering or runner contention. No SDK contract code touched.
- Verification: `bun test plugins/provider-docker/test/contract.integration.test.ts` (with DOCKER_HOST) → 15 pass, 1 skip, 0 fail; `bun run typecheck` → exit 0; `bun run lint` → exit 0 (5 warnings, pre-existing non-null assertions); `bun test` (full suite) → 1520 pass, 22 skip, 0 fail.
- Review feedback: none — no PR comments, no review decision, Cursor Bugbot passed.

## [2026-05-28 12:35 CDT] - Build (verify complete)
- Branch: us-089-opensearch-service-type — entered in-flight; preserved all branch work, no new code changes.
- Story state on entry: `passes: true` in `spec/beta/prd.json`; PR #189 OPEN/MERGEABLE; all reported CI checks green except `provider-integration-linux-x64` (live Docker contract flake). A prior triage step had already landed two local-only commits to address that flake — `67ea899 fix: keep live docker contract logs flowing past the 5s timeout` (tightens `plugins/provider-docker/test/contract.integration.test.ts` fixture from a one-shot `console.log('lando-contract-ready')` to an additional 250ms `lando-contract-tick` interval so the SDK 5s `logs emits at least one chunk` contract reliably collects chunks) and `e1fd57f docs: record PR #189 triage`. These commits are not yet on `origin/us-089-opensearch-service-type`; pushing them is a downstream loop step's job.
- Action: no code edits; only this verification entry. Did not invent work, refactor, or revert any prior commit.
- Verification (locally, with `PATH="$HOME/.bun/bin:$PATH"`):
  - `bun run typecheck` → exit 0.
  - `bun run lint` → exit 0 (5 pre-existing non-null-assertion warnings in provider-docker/provider-podman/service-lando integration tests, unrelated to this story).
  - `bun run build` → exit 0 (compiled `core/dist/lando` built cleanly, oclif manifest regenerated with 59 commands).
  - `bun test plugins/service-lando/test/opensearch.test.ts plugins/service-lando/test/opensearch.scenario.test.ts plugins/service-lando/test/env-contract.test.ts plugins/service-lando/test/registration.test.ts plugins/service-lando/test/package.test.ts` → 114 pass, 0 fail.
  - `bun test` (full suite) → 1519 pass, 22 skip, 1 transient fail: `core/test/cli/setup.test.ts > compiled setup install dir > matches source setup failure output and reports the same compiled install dir as shellenv`, failing only because the in-test `bun run build` raced concurrent test workers on `core/dist/lando` (same parallel-build race documented in the US-088 Review entry). Re-running the file in isolation: `bun test core/test/cli/setup.test.ts` → 8 pass, 0 fail.
  - Acceptance criteria spot-check against code:
    - `opensearch:2` ServiceType with HTTP healthcheck on `/_cluster/health` — `plugins/service-lando/src/services/opensearch.ts` `healthcheck.command = ["bash", "-c", `curl -sf http://localhost:${port}/_cluster/health`]`.
    - Service description notes Apache 2.0 vs ELv2/SSPL licensing distinction — `OPENSEARCH_SERVICE_DESCRIPTION` in the same file (`Apache 2.0-licensed fork of Elasticsearch 7.10` … `under Apache 2.0 rather than the Elastic License v2 (ELv2/SSPL)`).
    - Scenario test mirrors US-088 with OpenSearch endpoint — `plugins/service-lando/test/opensearch.scenario.test.ts` exercises `AppPlanner` + tooling alias end-to-end against the OpenSearch port; passes under `bun test`.
- Result: US-089 verified complete from the Build step's perspective. `passes: true` left unchanged. No story-code commit created (none needed). This is the only Build-step commit on this branch and contains progress.txt only.
- **Learnings for future iterations:**
  - When entering a story branch that is already `passes: true` and only has CI flake noise, the right Build-step action is to verify locally and append a confirmation entry — not to retro-author work or rerun prior steps' tasks.
  - The full-suite parallel-build race on `core/dist/lando` (ETXTBSY/EACCES) surfaces as a flaky `core/test/cli/setup.test.ts > compiled setup install dir` failure when `bun test` and the in-test `bun run build` contend for the binary. Re-running that one test file in isolation is the established repo workaround (also recorded in the US-088 Review entry).
  - Git state on a story branch can change under your feet between commands when other loop steps are still operating on the worktree; `git status --short` and `git log --oneline origin/branch..HEAD` should be re-checked just before commit to confirm what is actually pending.
---

## [2026-05-28 12:08 CDT] - US-089 Review
- Reviewed current build verification commit `0a53476d Record US-089 build verification` and the branch's existing OpenSearch implementation against `spec/beta/prd-beta-02-service-catalog-full.md` US-089 using `review-work`, explore, librarian, QA, goal/constraint, code-quality, security, and context-mining agents.
- Fixed confirmed review blocker:
  - Removed the newly introduced non-null assertion in `plugins/service-lando/test/opensearch.integration.test.ts` by narrowing `LANDO_TEST_PODMAN_SOCKET` before use. This preserves the Podman-gated live integration behavior while satisfying the repo's no-non-null-assertion lint/type-safety rule.
- Files changed:
  - `plugins/service-lando/test/opensearch.integration.test.ts`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: OpenSearch integration test used `process.env.LANDO_TEST_PODMAN_SOCKET!`; branch lint surfaced it as a US-089 warning and repo constraints forbid non-null assertions.
  - Rejected: adding a dedicated OpenSearch guide; PRD-B-02 guide coverage is selective and does not list US-089.
  - Rejected/deferred: changing Elasticsearch's existing endpoint protocol/config surface during this review; US-089 is already using the `http` endpoint protocol and this step stays scoped to OpenSearch.
  - Rejected/deferred: adopting legacy Lando Elasticsearch-only config keys (`portforward`, `mem`, `plugins`, `config.server`) for OpenSearch in this review; the v4 service schema and PRD acceptance for US-089 only require the canonical service type, healthcheck, description, and scenario coverage.
  - Accepted as context only: official OpenSearch docs confirm `opensearchproject/opensearch`, REST port `9200`, single-node dev mode, and `DISABLE_SECURITY_PLUGIN=true` as test/dev-only; the service description already warns the default local-dev config is not production-suitable.
- Verification run and result:
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/test/opensearch.test.ts plugins/service-lando/test/opensearch.scenario.test.ts plugins/service-lando/test/opensearch.integration.test.ts plugins/service-lando/test/env-contract.test.ts plugins/service-lando/test/package.test.ts plugins/service-lando/test/registration.test.ts` → 114 pass, 1 skip, 0 fail.
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 with 4 pre-existing non-null-assertion warnings in provider Docker/Podman contract tests; the OpenSearch warning is fixed.
  - `PATH="$HOME/.bun/bin:$PATH" bun run build` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1516 pass, 22 skip, 4 fail from `EACCES` spawning `core/dist/lando` while the full suite/build raced the compiled binary; after rebuilding, `bun test core/test/cli/shellenv.test.ts core/test/cli/deferred-commands.test.ts` → 39 pass, 0 fail. QA agent also reran the full suite with stderr merged and observed 1520 pass, 22 skip, 0 fail.
  - `lsp_diagnostics` on `plugins/service-lando/test/opensearch.integration.test.ts` could not run because the TypeScript LSP exited with code `-2`; root typecheck was clean.
- **Learnings for future iterations:**
  - `test.skipIf(process.env.X)` does not narrow `process.env.X` inside the callback for TypeScript/Biome; explicitly guard env vars before passing them to provider clients.
  - Official OpenSearch Docker docs align with this story's local-dev defaults: `opensearchproject/opensearch`, REST port `9200`, single-node dev/test mode, and security-disabled test-only mode.
---

## [2026-05-28 12:08 CDT] - PR #189 provider-integration-linux-x64 triage (correction)
- Prior fix (67ea899) targeted the wrong fixture: the live Docker Engine socket test calls `runProviderContract(provider)` which uses the SDK-internal `makeTestServicePlan` in `sdk/src/test/index.ts:62`, not the plugin-local `makeService` I edited in `plugins/provider-docker/test/contract.integration.test.ts`. The plugin helper is only used by the fake-API tests, so the periodic-tick change was inert. Verified by inspecting the CI diagnostics from run 26589264448 (`/tmp/ci-diag-189/journalctl.log`): the parallel provider-lando live podman run on the same fixture emitted `lando-contract-ready` every second and passed, proving the SDK fixture already emits periodically.
- Real cause: the SDK contract's 5s `Effect.timeoutFail` on `provider.logs` is too tight for the live Docker Engine path on a contended `ubuntu-24.04` GitHub Actions runner (apply+inspect+exec consume 5-7s before logs even subscribes; the failed test wall times of 6.5s, 12.4s, and 10.6s all show the logs subscription racing the 5s window). The defect is not in US-089 — the branch touches zero provider-docker, SDK contract, or Docker-path code.
- Fix: bumped the SDK contract logs timeout from 5s to 15s in `sdk/src/test/index.ts`. Still well inside the test's 60s overall budget; still fails closed if the stream genuinely never emits.
- Left 67ea899's plugin-local `makeService` tick change in place: it's inert for live tests but doesn't hurt the fake-API tests that do consume it.
- Verification: `bun test plugins/provider-docker/test/contract.integration.test.ts` with `DOCKER_HOST=unix:///var/run/docker.sock` → 15 pass, 1 skip, 0 fail; `bun run typecheck` → exit 0; `bun run lint` → exit 0 (4 warnings, pre-existing); `bun test` (full suite) → 1520 pass, 22 skip, 0 fail.

## [2026-05-28 12:50 CDT] - US-089 Cleanup
- Ran the branch-wide `remove-ai-slops` cleanup after review and before push with the planning-doc comment rules.
- Cleanup result: cosmetic-only cleanup; no runtime behavior, public API, error handling, service planning, contract assertions, imports, or side effects changed.
- Files changed:
  - `plugins/service-lando/test/env-contract.test.ts`: renamed a phase-looking fixture user/home value to neutral fixture data while preserving assertions against the same fixture object.
  - `plugins/service-lando/test/opensearch.test.ts`: reworded one awkward test title without changing assertions.
  - `sdk/src/test/index.ts`: removed a misplaced doc comment that described the matrix runner while attached to a type alias.
- Planning-doc leakage scan: no branch-changed source/test comments still cite planning docs, phases, PRDs, story IDs, or section references.
- Verification: `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 with the same 4 pre-existing provider Docker/Podman non-null-assertion warnings; `PATH="$HOME/.bun/bin:$PATH" bun run build` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun test plugins/service-lando/test/env-contract.test.ts plugins/service-lando/test/opensearch.test.ts` → 97 pass, 0 fail; full `PATH="$HOME/.bun/bin:$PATH" bun test` → 1519 pass, 22 skip, 1 fail from `core/test/cli/aliases.test.ts` compiled-binary `EACCES`; after rebuilding, `PATH="$HOME/.bun/bin:$PATH" bun test core/test/cli/aliases.test.ts` → 3 pass, 0 fail. LSP diagnostics were attempted for changed TypeScript files but the TypeScript LSP exited with code -2.

## [2026-05-28 13:02 CDT] - Push
- Branch: us-089-opensearch-service-type
- Push: pushed
- Commit(s): 9e628b0 Clean up OpenSearch test slop; 173b9f5 Record US-089 cleanup
- PR: #189 https://github.com/AaronFeledy/core4/pull/189 (existing)
---


## [2026-05-28 12:34 CDT] - Babysit
- PR: #189 https://github.com/AaronFeledy/core4/pull/189
- CI outcome: pass
- Review feedback: none
- Triage rounds used: 0
- Merge: merged (squash)
- Local main / branch refreshed: yes
- Remaining local state: none
---

## 2026-05-28 12:36 CDT - Sync
- Previous branch: us-089-opensearch-service-type
- Previous PR: #189 https://github.com/AaronFeledy/core4/pull/189
- Action: caught up after prior merge
- Local main updated: yes
- Branch after sync: main
---

## [2026-05-28] - US-090
- Implemented `meilisearch` service type for `@lando/service-lando`
- Files added:
  - `plugins/service-lando/src/services/meilisearch.ts`: `meilisearchServiceType` + `meilisearch1ServiceType` (alias `meilisearch` → `meilisearch:1`); image `getmeili/meilisearch:v1.11`, port 7700, persistent `/meili_data` volume, `/health` curl healthcheck; default `MEILI_MASTER_KEY=lando` (development-mode dev default, redacted per §6.6/§6.12.4), `MEILI_ENV=development`, `MEILI_NO_ANALYTICS=true`, `MEILI_HTTP_ADDR=0.0.0.0:<port>`
  - `plugins/service-lando/test/meilisearch.test.ts`: 10 unit tests covering defaults, env, override paths, healthcheck port-tracking, reserved-LANDO_* rejection, service description text
  - `plugins/service-lando/test/meilisearch.scenario.test.ts`: AppPlanner-resolved plan + 3 tooling-engine scenarios (`lando meili create-index`, `add-document`, `search`) routed through provider.exec
  - `plugins/service-lando/test/meilisearch.integration.test.ts`: live Podman test (skip unless `LANDO_TEST_PODMAN_SOCKET`) that boots Meilisearch v1, hits `/health`, creates an index, posts a document, waits for the task to succeed, and queries the index
- Files changed:
  - `plugins/service-lando/src/index.ts`: import/export new types, register `"meilisearch"` and `"meilisearch:1"` in `serviceTypes` map and manifest contributions
  - `plugins/service-lando/plugin.yaml`: add `- "meilisearch"` and `- "meilisearch:1"` to `contributes.serviceTypes`
  - `plugins/service-lando/test/package.test.ts`, `plugins/service-lando/test/registration.test.ts`: extend `expectedServiceTypes`
  - `plugins/service-lando/test/env-contract.test.ts`: add meilisearch row to the catalog-cases matrix
  - `sdk/test/fixtures/schema-snapshot.json`: regenerated snapshot includes the two new service types
  - `.github/workflows/ci.yml`, `scripts/build-ci-workflow.ts`: pre-pull `getmeili/meilisearch:v1.11` in the live provider job so the integration test does not fetch the image during container create
- Verification:
  - `bun test plugins/service-lando/`: 298 pass, 7 skip, 0 fail
  - `bun test` (full repo): 1538 pass, 23 skip, 0 fail
  - `bun run typecheck`: exit 0
  - `bun run lint`: exit 0 (4 unrelated pre-existing warnings in provider-docker/provider-podman test fixtures, untouched)
- Learnings:
  - Meilisearch requires a 16+ char master key only in `MEILI_ENV=production`; with `MEILI_ENV=development` the short `"lando"` dev default is accepted while still being non-empty so authenticated endpoints can be exercised consistently
  - Document/task indexing is asynchronous in Meilisearch — the integration test polls `/tasks/<taskUid>` until `status=succeeded` before querying, instead of relying on a fixed delay
  - The §6.12.1 catalog row for `meilisearch` is `"Meilisearch + master key, persistent volume"`; the PRD's "§6.6 redaction" reference resolves against §6.10/§6.12.4 in practice (plain-text credentials are redacted in `lando info` unless `--show-secrets`), so we documented the redaction expectation in `MEILISEARCH_SERVICE_DESCRIPTION` and exposed the constant as `MEILISEARCH_DEFAULT_MASTER_KEY` for downstream redactor wiring
---
