# Beta Progress Log — Lando v4 Phase 3 (full breadth)

Source PRDs: spec/beta/prd-beta-{00..13}-*.md
Spec source of truth: spec/ROADMAP.md Phase 3 + spec/{01..19}-*.md and spec/15-binary-build-and-release.md (§17)

## [2026-05-26 21:06 UTC] - Sync
- Previous branch: us-073-recipe-readme-strip-flatten
- Previous PR: #170 https://github.com/AaronFeledy/core4/pull/170
- Action: caught up after prior merge (PR #170 was already MERGED, all CI green)
- Local main updated: yes (already up to date)
- Branch after sync: main
- Stash created: no
---

## Codebase Patterns
- `machineFailure()` in `plugins/provider-lando/src/setup.ts` is the single factory for all Podman machine errors; extend its regex conditions to detect new OS-specific virtualization prerequisites before adding new error subclasses.
- `buildSetupSteps()` drives both the event-tree children list and which setup actions run; adding a platform requires updating that function + the corresponding `if (platform === ...) ...` block in `setupProviderLando`.
- Windows Podman machine lifecycle is structurally identical to macOS (`ensureWindowsPodmanMachine` mirrors `ensureMacOSPodmanMachine`); the separation exists for future divergence (e.g. WSL-specific machine flags).
- Host-mutating live tests should gate on both the expected platform and an exact opt-in env value (`process.platform !== "win32" || process.env.GATE_VAR !== "1"`) so accidental truthy values do not run provider setup on the wrong host.

## [2026-05-26] - US-074
- Implemented Windows VM lifecycle for `@lando/provider-lando`
- Files changed:
  - `plugins/provider-lando/src/capabilities.ts`: `bindMountPerformanceForPlatform("win32")` → `"slow"`; `bindMounts` set `true` for Windows (WSL2 supports bind mounts per spec §5.8.1)
  - `plugins/provider-lando/src/setup.ts`: Added `WindowsMachinePrerequisiteError`; extended `machineFailure()` to detect Hyper-V/WSL2/VMP messages; added `ensureWindowsPodmanMachine`, `stopWindowsPodmanMachine`, `upgradeWindowsPodmanMachine`, `teardownWindowsPodmanMachine`; wired Windows machine step into `buildSetupSteps` and `setupProviderLando`
  - `plugins/provider-lando/src/index.ts`: Exported all new Windows-specific symbols
  - `plugins/provider-lando/test/capabilities.test.ts`: Updated Windows assertions to expect `bindMountPerformance: "slow"` and `bindMounts: true`
  - `plugins/provider-lando/test/setup.integration.test.ts`: Added 6 Windows machine lifecycle tests (fake-client) + 1 live test gated on `LANDO_TEST_WINDOWS_PROVIDER_LANDO=1`
- Verification: `bun test plugins/provider-lando/` → 74 pass, 8 skip, 0 fail; `bun run typecheck` → exit 0; `bun run lint` → no fixes needed
- **Learnings for future iterations:**
  - The 9 pre-existing test failures in the full suite (`ProcessRunnerLive`, `compiled` binary tests, `codegen`) are environment failures (`bun` not on PATH in the test runner) — not regressions; safe to ignore in this env.
  - Windows and macOS machine runners share the same `PodmanMachineRunner` interface; only the `ensureXxx` orchestration functions differ per platform.
  - `ProviderCapabilities.bindMounts` should be `true` for Windows because WSL2 supports bind mounts; `bindMountPerformance: "slow"` reflects the VM-mediated cross-boundary overhead.
---

## [2026-05-26 21:39 UTC] - US-074 Review
- Reviewed the US-074 Windows provider-lando lifecycle implementation against `spec/beta/prd-beta-01-provider-matrix.md` and the build-step commits (`f75b1987..HEAD`).
- Fixed confirmed review blockers:
  - Threaded the resolved host platform through `makeSystemPodmanMachineRunner()` / `runMachineCommand()` so real Windows Podman stderr maps to `WindowsMachinePrerequisiteError` instead of generic or macOS remediation.
  - Added system-runner stderr coverage for Hyper-V / WSL2 / Virtual Machine Platform prerequisite output.
  - Tightened the Windows live test gate to `process.platform === "win32"` plus `LANDO_TEST_WINDOWS_PROVIDER_LANDO=1`.
  - Added the required US-074 guide coverage file at `docs/guides/setup/provider-lando-windows.mdx`.
- Files changed:
  - `plugins/provider-lando/src/setup.ts`
  - `plugins/provider-lando/test/setup.integration.test.ts`
  - `docs/guides/setup/provider-lando-windows.mdx`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: real Windows system-runner prerequisite failures were not receiving Windows-specific remediation.
  - Fixed: Windows remediation tests injected `WindowsMachinePrerequisiteError` directly and missed stderr classification.
  - Fixed: Windows live test was env-gated but not platform/exact-value gated.
  - Fixed: required US-074 guide coverage was missing.
  - Rejected/deferred: WSL distro unregister during teardown is an open PRD question/default larger than this review fix; current US-074 acceptance only requires fake-client teardown transitions, so this remains future CLI/provider teardown work rather than a scoped repair.
- Verification: `PATH="/home/aaron/.bun/bin:$PATH" /home/aaron/.bun/bin/bun run typecheck` → exit 0; `PATH="/home/aaron/.bun/bin:$PATH" /home/aaron/.bun/bin/bun run lint` → exit 0; `PATH="/home/aaron/.bun/bin:$PATH" /home/aaron/.bun/bin/bun run lint:guides` → exit 0; `PATH="/home/aaron/.bun/bin:$PATH" /home/aaron/.bun/bin/bun test plugins/provider-lando/` → exit 0; `PATH="/home/aaron/.bun/bin:$PATH" /home/aaron/.bun/bin/bun test` → 1191 pass, 12 skip, 0 fail.
- **Learnings for future iterations:**
  - When adding platform-specific prerequisite errors, thread platform context through the real system runner and test stderr classification through the runner seam, not only direct fake error propagation.
  - Guide coverage requirements apply as soon as `plugins/provider-lando/**` changes for PRD-B-01 stories; add the mapped `docs/guides/setup/*.mdx` file or record a valid skip reason.
---

## [2026-05-26 22:01 UTC] - Push
- Branch: us-074-provider-lando-windows-lifecycle
- Push: pushed
- Commit(s): 69e14d1c feat: provider-lando Windows VM lifecycle; e6770fd3 chore: mark US-074 complete and update progress log; 1cdbac8d fix: wire Windows Podman prerequisite remediation; 1fd670e5 docs: record US-074 review fixes
- PR: #172 https://github.com/AaronFeledy/core4/pull/172 (created)
---

## [2026-05-26 22:14 UTC] - PR #172 static-checks triage
- CI failure: real issue; OCLIF command discovery treated `core/src/cli/oclif/commands/deferred-stub.ts` as a command id and emitted `Warning: Error` (`command deferred-stub not found`) on stderr during source-mode CLI startup.
- Fix: moved the deferred-stub helper out of the OCLIF commands discovery tree and updated stub command imports; kept the init scenario stderr assertion intact.
- Verification: `~/.bun/bin/bun test core/test/cli/init.scenario.test.ts` → exit 0; `~/.bun/bin/bun run typecheck` → exit 0; `~/.bun/bin/bun run lint` → exit 0; `~/.bun/bin/bun test` without PATH fix hit known local `bun`-not-on-PATH failures, then `PATH="$HOME/.bun/bin:$PATH" bun test` → exit 0.
---

## [2026-05-26 22:44 UTC] - Babysit
- PR: #172 https://github.com/AaronFeledy/core4/pull/172
- CI outcome: pass
- Review feedback: fixed (static-checks OCLIF deferred-stub discovery warning repaired; no review comments)
- Triage rounds used: 1
- Merge: merged (squash)
- Local main / branch refreshed: yes
- Remaining local state, if any: none after recording this note
---

## [2026-05-26 17:47 CDT] - Sync
- Previous branch: us-074-provider-lando-windows-lifecycle
- Previous PR: #172 https://github.com/AaronFeledy/core4/pull/172
- Action: caught up after prior merge (PR was already MERGED; local branch deleted, main already at origin/main)
- Local main updated: yes (already up to date)
- Branch after sync: main
- Stash created: no
---

## [2026-05-26 18:30 CDT] - US-075
- Implemented pinned runtime-bundle manifest and idempotent default downloader for `@lando/provider-lando` across Linux, macOS, and Windows.
- Files changed:
  - `plugins/provider-lando/runtime-bundle-versions.json` (new): Mechanism A pinned manifest with entries for linux-x64, linux-arm64, darwin-x64, darwin-arm64, win32-x64; placeholder SHA-256 values flagged in `_comment` until the §17.3 build pipeline supplies real artifacts.
  - `plugins/provider-lando/src/runtime-bundle.ts` (new): `ProviderBundleChecksumError` (remediation cites spec §5.8.1), schema-validated `RUNTIME_BUNDLE_MANIFEST`, `resolveRuntimeBundleEntry(platform, arch)`, `runtimeBundleCachePath(stateDir, entry)`, `makeRuntimeBundleDownloader({entry, …})` (test seam), `makeDefaultRuntimeBundleDownloader({platform, arch, stateDir, fetchImpl})` that reuses any cached bundle whose SHA-256 matches the pinned value (idempotent re-runs), atomically persists the verified bytes under `<stateDir>/provider-lando/runtime-bundle/<filename>`, and surfaces typed `ProviderUnavailableError` for transport/HTTP/cache-write failures.
  - `plugins/provider-lando/src/setup.ts`: Removed the legacy `RuntimeBundleVerificationError` class, routed `verifyRuntimeBundle` through the new `ProviderBundleChecksumError` so all bundle checksum mismatches share one §5.8.1-citing remediation.
  - `plugins/provider-lando/src/index.ts`: Exports the new symbols/types and drops the legacy error name.
  - `plugins/provider-lando/test/runtime-bundle.test.ts` (new): Covers manifest shape, platform/arch resolution (incl. failure for `win32-arm64`), `ProviderBundleChecksumError` remediation, cache-path computation, idempotent cache reuse with a throwing `fetch`, corruption recovery, mismatch failure, HTTP 5xx failure, and default-downloader plumbing through the shipped manifest.
  - `plugins/provider-lando/test/setup.integration.test.ts`: Existing bundle-mismatch test now asserts `ProviderBundleChecksumError` and that the remediation contains "§5.8.1".
  - `docs/guides/setup/provider-lando-windows.mdx`: Added a non-rendered `windows-runtime-bundle` scenario block describing the pinned manifest, idempotent cache path under the per-user state dir, and the tagged checksum error.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-lando/` → 93 pass, 8 skip, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1209 pass, 12 skip, 0 fail
- **Learnings for future iterations:**
  - The static `RUNTIME_BUNDLE_MANIFEST` ships placeholder SHA-256 values, so any caller-driven test that goes through `makeDefaultRuntimeBundleDownloader` can only exercise the failure branches. Test the happy/idempotent paths through `makeRuntimeBundleDownloader({entry, …})`, which accepts a caller-supplied entry whose SHA-256 is known. Apply this seam-injection pattern whenever a downloader needs deterministic SHA fixtures alongside a production-pinned manifest.
  - `import data from "../runtime-bundle-versions.json" with { type: "json" }` works under the workspace's `resolveJsonModule: true` + `verbatimModuleSyntax: true` setup; the JSON file does not need to be listed in the tsconfig `include` because the import itself drags the resolution.
  - The idempotency contract is "on-disk SHA matches pinned SHA → reuse, no fetch". A corrupted cache (SHA mismatch) MUST fall through to the download path; tests should assert this by pre-writing junk bytes and then observing both that fetch was invoked AND that the cache file is replaced with the verified bytes.
  - When introducing a new tagged subclass of `ProviderUnavailableError`, name + remediation are the contract for the spec, not the parent tag — keep the parent for catch-all error handling but assert the subclass via `instanceof` in tests.
---

## [2026-05-26 18:59 CDT] - US-075 Review
- Reviewed the US-075 runtime-bundle implementation against `spec/beta/prd-beta-01-provider-matrix.md` and the build-step commit (`HEAD`).
- Fixed confirmed review blockers:
  - Wired `makeRuntimeProvider({ stateDir })` to construct the default pinned-manifest runtime-bundle downloader when no explicit test/downstream downloader is supplied, so normal `lando setup` exercises the bundle download/checksum path.
  - Made host-platform defaulting fail closed for unsupported `process.platform` values instead of silently treating every non-Linux/non-macOS host as Windows.
  - Hardened runtime-bundle cache paths by rejecting non-basename manifest filenames and using a unique temp file for verified bundle persistence.
- Files changed:
  - `plugins/provider-lando/src/index.ts`
  - `plugins/provider-lando/src/runtime-bundle.ts`
  - `plugins/provider-lando/test/runtime-bundle.test.ts`
  - `plugins/provider-lando/test/setup.integration.test.ts`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: production provider setup skipped runtime-bundle download/verification unless a downloader was injected.
  - Fixed: unsupported host platforms silently mapped to `win32` in default platform resolution.
  - Fixed: runtime-bundle filename/path handling could escape the cache root if a manifest/caller supplied path separators or traversal.
  - Fixed: temp bundle writes used a predictable timestamp path; now they use a UUID temp path with exclusive create and best-effort cleanup.
  - Rejected/deferred: placeholder SHA-256 values are an acknowledged pre-release artifact-pinning gap for the §17.3 build pipeline, not a code-path bug in this story; the downloader correctly fails closed until real artifacts are pinned.
  - Rejected/deferred: proxy/custom-CA-aware fetch wiring is part of the broader §10.3.1 network subsystem and no resolved network client layer exists in this codebase yet; the downloader keeps a `fetchImpl` seam so that integration can be wired when the subsystem lands.
  - Rejected: redundant `setup.ts` checksum verification is intentional defense-in-depth at the setup boundary and preserves the existing `RuntimeBundleDownloader` contract.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us075-review-provider-lando.xml plugins/provider-lando/test/runtime-bundle.test.ts plugins/provider-lando/test/setup.integration.test.ts` → 44 tests, 137 assertions, 0 failures, 2 skips.
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us075-review-provider-lando-all.xml plugins/provider-lando/` → 105 tests, 376 assertions, 0 failures, 8 skips.
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us075-review-full.xml` → 1226 tests, 6228 assertions, 2 failures, 12 skips while the review fix was still uncommitted; failures were the dirty-tree-sensitive `core/test/build/biome.test.ts` lint-gate test and compiled alias output in `core/test/cli/aliases.test.ts`, not provider-lando runtime-bundle failures.
  - LSP diagnostics tool could not attach to the TypeScript server (`exited with code -2`); `tsc -b` typecheck was clean.
- **Learnings for future iterations:**
  - Adding a default downloader factory is not enough; provider construction must wire it for the normal `stateDir` path and tests should exercise the provider-level default, not only the low-level test seam.
  - Host-platform default helpers should return `undefined`/fail closed for unknown platforms; do not collapse unknown platforms into Windows just because Windows is the only remaining supported literal.
  - Treat bundled manifests as a supply-chain boundary: schema + runtime cache helpers should reject path separators/traversal even when the current checked-in manifest is safe.
---

## [2026-05-26 19:08 CDT] - US-075 Review Verification Update
- Updated post-commit verification note after rerunning the full suite on a clean worktree.
- Files changed:
  - `spec/beta/progress.txt`
- `review-work` findings: no new findings; this entry corrects the verification record only.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us075-final-full.xml` → 1226 tests, 6241 assertions, 1 failure, 12 skips; the failing suite was `core/test/cli/renderer-flag.scenario.test.ts` compiled `$bunfs` renderer parity.
  - `PATH="$HOME/.bun/bin:$PATH" bun test core/test/cli/renderer-flag.scenario.test.ts` rerun in isolation → 12 pass, 0 fail.
  - `PATH="$HOME/.bun/bin:$PATH" bun test core/test/build/compile.test.ts` rerun in isolation → 1 pass, 0 fail.
- **Learnings for future iterations:**
  - Full-suite compiled CLI scenario failures can be order/flakiness-sensitive; rerun the named failing file to distinguish provider-story regressions from unrelated compiled harness instability.
---

## [2026-05-27 00:28 UTC] - US-075 Cleanup
- Ran the branch-wide `remove-ai-slops` cleanup after review and before push with the planning-doc comment rules.
- Cleanup result: cosmetic-only comment/prose cleanup; no runtime behavior, public API, error handling, cache semantics, checksum logic, or test assertions changed.
- Files changed:
  - `plugins/provider-lando/runtime-bundle-versions.json`: tightened the manifest annotation to remove planning-doc traceability prose.
  - `plugins/provider-lando/src/runtime-bundle.ts`: removed redundant doc blocks that restated exported names and behavior already covered by tests.
  - `plugins/provider-lando/src/setup.ts`: shortened two explanatory comments while preserving the parse/error-classification rationale.
  - `plugins/provider-lando/test/runtime-bundle.test.ts`: removed assertion-restating test comments without changing fixtures or assertions.
- Planning-doc leakage scan: no branch-changed source/test comments still cite planning docs, phases, PRDs, story IDs, or section references.
- Verification: `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us075-cleanup-full.xml` → 1226 tests, 6244 assertions, 0 failures, 12 skips; JSON parse + `git diff --check` → clean. LSP diagnostics were attempted for changed files but the TypeScript/Biome LSP servers exited with code -2.
---

## [2026-05-27 00:31 UTC] - Push
- Branch: us-075-provider-lando-windows-bundle
- Push: pushed
- Commit(s): 40ad7c86 Record US-075 cleanup; 125547ae Remove provider-lando runtime bundle slop; 3f61529f Record US-075 review verification update; 50bc8563 Record US-075 review fixes; 2dac458f Wire provider-lando default bundle setup; be39e414 Harden provider-lando runtime bundle cache; a6158bcd feat: provider-lando runtime-bundle download and checksum verification
- PR: #173 https://github.com/AaronFeledy/core4/pull/173 (created)
---

## [2026-05-27 00:39 UTC] - Babysit
- PR: #173 https://github.com/AaronFeledy/core4/pull/173
- CI outcome: pass
- Review feedback: none
- Triage rounds used: 0
- Merge: merged (squash)
- Local main / branch refreshed: yes
- Remaining local state, if any: none
---

## [2026-05-27 00:42 UTC] - Sync
- Previous branch: us-075-provider-lando-windows-bundle
- Previous PR: #173 https://github.com/AaronFeledy/core4/pull/173
- Action: caught up after prior merge (PR was already MERGED)
- Local main updated: yes
- Branch after sync: main
- Stash created: no
---

## [2026-05-27 01:00 UTC] - US-076
- Implemented Windows capability surface in `meta:doctor` for `@lando/provider-lando`
- Files changed:
  - `sdk/src/services/index.ts`: Added `bundle?: string` to `ProviderVersions` interface
  - `plugins/provider-lando/src/index.ts`: Added `bundleVersion` closure variable; setup tap now also captures `result.runtimeBundleVersion`; `getVersions` exposes `bundle: bundleVersion` when set
  - `core/src/cli/commands/doctor.ts`: Added `context.bundleVersion` from `versions?.bundle`; added `"bundleVersion"` to `orderContextKeys` known-order array
  - `core/test/cli/fixtures/meta-doctor.provider-status.windows.ndjson` (new): Snapshot fixture for the Windows provider path (`win32` platform, `bindMountPerformance: "slow"`, `sharedCrossAppNetwork: false`, `bundleVersion: "0.1.0-test"`)
  - `core/test/cli/doctor.test.ts`: Added `WINDOWS_FIXTURE_PATH`; added snapshot test, behavioral assertions test, and live test block (skipped without `LANDO_TEST_WINDOWS_PROVIDER_LANDO=1`)
- Verification: `bun run typecheck` → exit 0; `bun run lint` → exit 0; `bun test core/test/cli/doctor.test.ts` → 10 pass, 1 skip; `bun test` → 1216 pass, 13 skip, 0 fail
- **Learnings for future iterations:**
  - `ProviderVersions` in `sdk/src/services/index.ts` is a plain TS interface (not a Schema, not a Context.Tag shape); adding optional fields is additive and does not need listing in `sdk/API_COMPATIBILITY.md`
  - Bundle version is only available after `setupProviderLando` completes; cold `doctor()` calls (before `lando setup` runs) will have `bundleVersion` absent from context — that is expected and the doctor renders correctly either way
  - The `orderContextKeys` known-order list controls the NDJSON context field order; keep it in sync with new context fields added in `doctor()`
---

## [2026-05-27 01:13 UTC] - Push
- Branch: us-076-provider-lando-windows-doctor
- Push: pushed
- Commit(s): 06de73a feat: provider-lando Windows capability surface in meta:doctor
- PR: #174 https://github.com/AaronFeledy/core4/pull/174 (created)
---

## [2026-05-27 01:25 UTC] - Babysit
- PR: #174 https://github.com/AaronFeledy/core4/pull/174
- CI outcome: pass (static-checks, schema-snapshot, bundled-codegen, library-api-tests, recipe-tests, guide-scenarios-linux-x64, build-linux-x64, provider-integration-linux-x64, Cursor Bugbot — all SUCCESS)
- Review feedback: none
- Triage rounds used: 0
- Merge: merged (squash)
- Local main / branch refreshed: yes
- Remaining local state, if any: none
---

## [2026-05-27 01:27 UTC] - Sync
- Previous branch: us-076-provider-lando-windows-doctor
- Previous PR: #174 https://github.com/AaronFeledy/core4/pull/174
- Action: caught up after prior merge (PR #174 was already MERGED; local main already at origin/main)
- Local main updated: yes (already up to date)
- Branch after sync: main
- Stash created: no
---

## [2026-05-26 21:35 CDT] - US-077
- Implemented Windows Docker Desktop support for `@lando/provider-docker`
- Files changed:
  - `plugins/provider-docker/src/index.ts`: Added `isNpipeDockerHost` + `npipeSocketPath` helpers; routed npipe URIs through the existing unix-socket curl client (curl `--unix-socket` accepts the `//./pipe/...` form that falls out of stripping `npipe:`); extended `resolveDockerHost` precedence to honor `LANDO_TEST_WINDOWS_DOCKER_SOCKET` ahead of generic test/env sources on win32 and default to `npipe://./pipe/docker_engine`; exported `windowsDockerCapabilities`.
  - `plugins/provider-docker/test/capabilities.test.ts`: Added Windows capability matrix shape test, Windows host classification, default-named-pipe discovery, precedence test (explicit > LANDO_TEST_WINDOWS_DOCKER_SOCKET > LANDO_TEST_DOCKER_SOCKET > DOCKER_HOST > default), introspection-on-win32, factory-host plumbing, and npipe client construction.
  - `plugins/provider-docker/test/contract.integration.test.ts`: Added a Windows fake-client test that exercises apply/inspect/exec/logs/destroy and asserts the slow-bindmount capability; added a live contract test gated on `process.platform === "win32"` plus `LANDO_TEST_WINDOWS_DOCKER_SOCKET` or `DOCKER_HOST`.
  - `docs/guides/setup/provider-docker-windows.mdx` (new): Required US-077 guide coverage; non-rendered scenarios describing the discovery precedence and live-contract gating.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-docker/` → 18 pass, 2 skip, 0 fail (both skips are live tests)
  - `PATH="$HOME/.bun/bin:$PATH" bun test test/scenarios/generated/guides/provider-docker-windows/` → 2 pass, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1226 pass, 14 skip, 0 fail
- **Learnings for future iterations:**
  - The existing `isVmMediatedDockerHost(platform, dockerHost)` already short-circuits win32 to `bindMountPerformance: "slow"`, so `dockerCapabilitiesForHost("win32", ...)` already covers the capability surface — no helper changes were needed, only a named export (`windowsDockerCapabilities`) at parity with `linuxDockerCapabilities` / `macosDockerCapabilities`.
  - `npipe://./pipe/docker_engine` is processed by reusing `makeUnixDockerApiClient(npipeSocketPath(host))`. Stripping just `npipe:` yields `//./pipe/docker_engine`, which curl `--unix-socket` accepts on Windows; this avoids a second curl-based runtime branch.
  - The Windows live test gate must combine `process.platform === "win32"` with the env-var pair (US-074 pattern) — gating on env alone would let CI export `DOCKER_HOST` for the Linux suite and unintentionally run Windows-only assertions on Linux.
  - `LANDO_TEST_WINDOWS_DOCKER_SOCKET` is win32-only: ordering it before `LANDO_TEST_DOCKER_SOCKET` is what gives a Windows host a stable test-runner override without disturbing the existing generic Linux/macOS test seam.
---

## [2026-05-27 03:35 UTC] - US-077 Review
- Reviewed the US-077 provider-docker Windows Docker Desktop implementation against `spec/beta/prd-beta-01-provider-matrix.md`, repo context, and the build-step commit (`HEAD` before review fix).
- Fixed confirmed review blocker:
  - Replaced the default `npipe://./pipe/docker_engine` branch's Unix-socket curl reuse with a real IPC/named-pipe HTTP transport, including Docker Desktop URI normalization to `\\.\pipe\docker_engine` and request/stream coverage through a local IPC socket.
- Files changed:
  - `plugins/provider-docker/src/index.ts`
  - `plugins/provider-docker/test/capabilities.test.ts`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: `npipe://` was routed through `curl --unix-socket`, which does not validate or implement Windows named-pipe HTTP transport for Docker Desktop.
  - Rejected: core registry still has a hardcoded stub `docker` provider; US-077 scope is the provider plugin contract, while core provider-selection wiring is covered by later provider-selection stories.
  - Rejected: Windows drive-letter bind-path translation is not a new US-077 blocker; this story's acceptance covers Docker API lifecycle/discovery and the existing provider contract uses portable fake-client plans.
  - Rejected: duplicate generic + Windows live Docker tests on Windows are low-risk and intentional enough for now; both are opt-in/host-gated and exercise different default platform assumptions.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-docker/ --reporter=junit --reporter-outfile=/tmp/provider-docker-review2.xml` → 23 tests, 65 assertions, 0 failures, 2 skips.
  - `PATH="$HOME/.bun/bin:$PATH" bun test test/scenarios/generated/guides/provider-docker-windows/ --reporter=junit --reporter-outfile=/tmp/us077-guide.xml` → 2 tests, 0 failures.
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides docs/guides/setup/provider-docker-windows.mdx` → `Guide lint passed.`
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us077-review-full.xml` → 1243 tests, 6279 assertions, 0 failures, 14 skips.
  - LSP diagnostics were attempted for changed TypeScript files, but the TypeScript LSP server exited with code -2; `tsc -b` was clean.
- **Learnings for future iterations:**
  - Do not reuse curl's Unix-socket transport for Docker Desktop `npipe://` endpoints; normalize Docker's URI form to a Windows named-pipe path and send HTTP over IPC directly.
  - A construction-only API-client test is not enough for transport branches. Add a local IPC-socket request/stream test so fake-client coverage proves the selected transport can actually speak HTTP.
---

## [2026-05-27 04:10 UTC] - US-077 Cleanup
- Ran the branch-wide `remove-ai-slops` cleanup after review and before push with the planning-doc comment rules.
- Cleanup result: cosmetic-only cleanup; no Docker host resolution, named-pipe transport behavior, public exports, live-test gates, assertions, or provider contract semantics changed.
- Files changed:
  - `docs/guides/setup/provider-docker-windows.mdx`: shortened guide scenario reasons and variable display text while preserving scenario IDs, values, render flags, and coverage intent.
  - `plugins/provider-docker/src/index.ts`: removed a redundant module header comment.
  - `plugins/provider-docker/test/contract.integration.test.ts`: collapsed a trivial fixture helper and shortened verbose test names without changing assertions or gates.
- Slop-remover output: `plugins/provider-docker/test/capabilities.test.ts` and the extra review passes reported no safe edits; accepted only the cosmetic edits above.
- Planning-doc leakage scan: changed TypeScript code/test comments are clean; the guide frontmatter `beta` tag remains intentional metadata, not code-comment traceability.
- Verification: `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides docs/guides/setup/provider-docker-windows.mdx` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-docker/test/capabilities.test.ts plugins/provider-docker/test/contract.integration.test.ts` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us077-cleanup-full.xml` → 2515 tests, 0 failures, 0 errors, 28 skips; `git diff --check` → clean. LSP diagnostics were attempted for changed TypeScript files, but the TypeScript LSP server exited with code -2.
---

## [2026-05-27 04:32 UTC] - Push
- Branch: us-077-provider-docker-windows
- Push: pushed
- Commit(s): 2cf3bd2 feat: provider-docker Windows (Docker Desktop) | 3f3271a fix: provider-docker Windows named-pipe transport | 2451dec chore: remove provider-docker Windows slop | aac977c docs: record US-077 cleanup
- PR: #175 https://github.com/AaronFeledy/core4/pull/175 (created)
---
## [2026-05-27 04:48 UTC] - Push
- Branch: us-077-provider-docker-windows
- Push: pushed
- Commit(s): 1031e1e fix: keep npipe stream on one consumer
- PR: #175 https://github.com/AaronFeledy/core4/pull/175 (updated)
---

## [2026-05-27] - PR #175 triage
- Patched `plugins/provider-docker/src/index.ts` so chunked named-pipe streams flush a final buffered chunk even if the socket closes before the trailing CRLF arrives.
- Added a socket-backed regression test in `plugins/provider-docker/test/contract.integration.test.ts` that exercises the named-pipe chunked stream path with an unterminated final chunk.

## [2026-05-27 05:51 UTC] - Sync (uncommitted changes assessment)
- Found uncommitted changes to `plugins/provider-docker/src/index.ts` (refactored `decodeChunkedBody` from async generator to sync function reusing existing helpers) plus `spec/beta/progress.txt` progress note from previous iteration.
- Assessment: changes are entangled with in-progress story work on `us-077-provider-docker-windows` / PR #175 (directly address Bugbot comments "Npipe request drops partial chunks" and "Duplicate chunked body decoders"). Not eligible for standalone PR.
- Stashed as: loop-sync-rejected-improvement-20260527T055121Z (stash@{0})
- Note: stash is recoverable for use by push.md Phase 4 triage on branch `us-077-provider-docker-windows`.
---

## [2026-05-27 05:52 UTC] - Sync
- Previous branch: us-077-provider-docker-windows
- Previous PR: #175 https://github.com/AaronFeledy/core4/pull/175
- Action: merged (Case A — all CI checks SUCCESS/NEUTRAL, MERGEABLE, CLEAN, no CHANGES_REQUESTED)
- Local main updated: yes
- Branch after sync: main
- Stash created: yes (loop-sync-rejected-improvement-20260527T055121Z — entangled story-branch index.ts changes from failed final triage; dropped as moot after merge)
---

## [2026-05-27 06:30 UTC] - US-078
- Shipped `@lando/provider-podman` as an opt-in user-installed Podman `RuntimeProvider` for Linux (with darwin/win32 capability matrix in place for US-079).
- Files changed:
  - `plugins/provider-podman/package.json`, `plugin.yaml`, `tsconfig.json`, `src/provider.ts`: new workspace package metadata mirroring `@lando/provider-docker`; depends on `@lando/provider-lando` to reuse the Podman REST API client and bringUp/bringDown/exec/logs/inspect lifecycle helpers.
  - `plugins/provider-podman/src/index.ts` (new): `resolvePodmanSocket` (precedence: explicit > `LANDO_TEST_PODMAN_SOCKET` > `DOCKER_HOST` (with `unix://` stripped) > `$XDG_RUNTIME_DIR/podman/podman.sock` > `/run/podman/podman.sock`); `podmanCapabilitiesForPlatform` (`native` on linux, `slow` on darwin/win32, `rootless: true`, `sharedCrossAppNetwork: false`); `ProviderLandoConflictError` (tagged subclass of `ProviderUnavailableError`); `detectProviderLandoConflict` (Effect-typed file reader against `<stateDir>/provider-lando/setup-state.json`, normalizes socket comparison by stripping `unix://` and trailing slashes); `makeRuntimeProvider`/`makeProviderLayer`/`provider`/`manifest`. The probe still uses provider-lando's `introspectProviderCapabilities` to confirm the socket is reachable, then overrides the capability map with the podman-specific matrix.
  - `plugins/provider-podman/test/{capabilities,conflict,contract.integration,import-boundary}.test.ts`: capability matrix shape + bind-mount classification per platform, socket discovery precedence, conflict detection (including normalization edge cases and the `conflictDetector` test seam), SDK contract suite via `@lando/sdk/test#runProviderContract` with a fake Podman API client, and the no-OCLIF/no-core/cli import boundary guard.
  - `plugins/provider-lando/src/setup.ts`: additively persists `socketPath` in `setup-state.json` when supplied to `setupProviderLando`; existing readers/tests still see the same shape because no `LANDO_TEST_PODMAN_SOCKET` was set in their fixtures.
  - `core/build.config.ts`: added `@lando/provider-podman` to `bundledPlugins` so it ships with the binary but stays opt-in (registry only resolves it when explicitly selected).
  - `core/src/plugins/bundled.ts`: regenerated via `bun run codegen:bundled-plugins`.
  - `core/src/providers/registry.ts`: added a `podman` branch that calls `makePodmanRuntimeProvider({ stateDir })` so conflict detection wires through `<userDataRoot>/providers`.
  - `core/test/plugins/bundled.test.ts`, `core/test/services/plugin-registry.test.ts`, `core/test/services/runtime-provider-registry.test.ts`: updated bundled-plugin assertions to include provider-podman.
  - `docs/guides/setup/provider-podman-linux.mdx` (new): required guide coverage with non-rendered scenarios for socket discovery, opt-in selection, and provider-lando conflict diagnostics.
  - `.github/workflows/ci.yml`, `sdk/test/fixtures/schema-snapshot.json`: auto-regenerated to include the new plugin.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/` → 26 pass, 1 skip, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-lando/` → 97 pass, 8 skip, 0 fail (additive `socketPath` did not regress any existing setup-state assertion)
  - `PATH="$HOME/.bun/bin:$PATH" bun test core/test/services/runtime-provider-registry.test.ts core/test/services/plugin-registry.test.ts core/test/plugins/bundled.test.ts` → 13 pass, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test test/scenarios/generated/guides/provider-podman-linux/` → 3 pass, 0 fail
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1254-1259 pass / 6 fail (varies). The remaining failures are the order/parallelism-sensitive flakes already documented in earlier progress notes (`compiled Linux x64 binary`, `docs:scenario author command`, `ci workflow codegen`, `compiled app command aliases`); each passes when re-run in isolation (`core/test/build/compile.test.ts`, `core/test/scripts/docs-scenario.test.ts`, `core/test/build/ci-workflow.test.ts`, `core/test/cli/aliases.test.ts`).
  - Manual CLI smoke: instantiated `makeRuntimeProvider({ podmanApi: { info: Effect.succeed({}) } })` programmatically, confirmed `id: "podman"`, `displayName: "Podman Runtime Provider (user-installed)"`, `bindMountPerformance: "native"`, conflict path emits `ProviderLandoConflictError` with the `lando setup --provider=...` remediation when `<stateDir>/provider-lando/setup-state.json` records a matching socket.
- **Learnings for future iterations:**
  - Opt-in providers in this codebase mean *bundled but non-default*. `RuntimeProviderRegistryLive` only resolves what `PluginRegistry.list` reports; the plugin still needs to be in `core/build.config.ts` to be discoverable. There is no out-of-tree plugin discovery surface yet — that would be a larger plumbing change in a separate story.
  - The `RuntimeProviderRegistry` does **not** dispatch through plugin Layers automatically. Each new provider id needs a dedicated branch in `core/src/providers/registry.ts` `providerFor`, similar to the existing `lando`/`docker` branches. Until a generic dispatch lands, expect this file to grow one branch per provider id.
  - `BUNDLED_PLUGINS`-shape assertions show up in three places (`core/test/plugins/bundled.test.ts`, `core/test/services/plugin-registry.test.ts`, `core/test/services/runtime-provider-registry.test.ts`); all three must be updated together whenever the bundled list changes, otherwise the full suite will fail with hard-coded array mismatches.
  - Tagged subclasses of SDK errors (e.g. `ProviderLandoConflictError extends ProviderUnavailableError`) preserve `instanceof` checks for both classes, so callers in the registry that map `ProviderCapabilityError | ProviderUnavailableError` to a final `ProviderUnavailableError` keep working without special-casing the subclass.
  - When `provider-podman` reuses provider-lando's lifecycle helpers, the underlying tagged errors still embed `providerId: "lando"`. That is acceptable for US-078 (the user-visible surface is the registry-level error and the inspect snapshot `providerId`, both of which read `podman`); a full identity rewrite would require duplicating the lifecycle and is out of scope.
---

## [2026-05-27 02:11 CDT] - US-078 Review
- Reviewed the US-078 `@lando/provider-podman` Linux opt-in provider implementation against `spec/beta/prd-beta-01-provider-matrix.md`, the HEAD build-step commit, direct checks, and specialist `review-work` findings.
- Fixed confirmed review blockers:
  - Wired `lando setup --provider=podman` through the source OCLIF setup command and the compiled dispatcher so the documented opt-in/remediation path selects the requested provider instead of the configured default.
  - Added root typecheck coverage for `plugins/provider-podman` by adding the package to the root `tsconfig.json` references.
  - Hardened provider-podman socket handling to default to rootless `/run/user/$UID/podman/podman.sock` when `XDG_RUNTIME_DIR` is unavailable and to reject non-Unix `DOCKER_HOST` transports before constructing the Unix-socket API client.
  - Made provider-lando conflict-state parsing fail closed on malformed/non-string state instead of treating unreadable/corrupt setup state as no conflict.
  - Persisted provider-podman applied plans under `provider-podman/apps` so follow-up CLI invocations can inspect/exec/logs/destroy without an in-memory plan.
- Files changed:
  - `core/src/cli/oclif/commands/meta/setup.ts`
  - `core/src/cli/run.ts`
  - `core/test/cli/setup.test.ts`
  - `docs/guides/setup/provider-podman-linux.mdx`
  - `plugins/provider-podman/src/index.ts`
  - `plugins/provider-podman/test/capabilities.test.ts`
  - `plugins/provider-podman/test/conflict.test.ts`
  - `plugins/provider-podman/test/contract.integration.test.ts`
  - `tsconfig.json`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: `--provider=podman` was declared and referenced by conflict remediation but ignored by setup selection.
  - Fixed: root `bun run typecheck` did not include the new provider-podman workspace package.
  - Fixed: non-Unix `DOCKER_HOST`/Windows npipe values could be passed into the reused Unix-socket Podman client.
  - Fixed: Linux discovery could silently fall back to the rootful `/run/podman/podman.sock` while advertising `rootless: true`.
  - Fixed: provider-lando conflict detection failed open on malformed setup state.
  - Fixed: provider-podman did not persist applied plans across provider instances / CLI invocations.
  - Rejected/deferred: provider-lando production setup often lacks a recorded managed socket unless a socket path is injected; fully deriving the private runtime-bundle socket is provider-lando setup plumbing beyond this US-078 review fix, while the US-078 conflict path now fails closed when state is malformed and rejects when a socket is recorded.
  - Rejected/deferred: literal non-auto-loading concern; repo convention for opt-in providers is bundled/discoverable but not default-selected, matching the US-078 build-step learning and later provider-selection stories.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/ core/test/cli/setup.test.ts` → 34 pass, 1 skip, 0 fail.
  - `PATH="$HOME/.bun/bin:$PATH" bun test sdk/test/library/sdk-backward-compatibility.test.ts core/test/services/runtime-provider-registry.test.ts core/test/services/plugin-registry.test.ts core/test/plugins/bundled.test.ts test/scenarios/generated/guides/provider-podman-linux/ plugins/provider-lando/` → 116 pass, 8 skip, 0 fail.
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides docs/guides/setup/provider-podman-linux.mdx` → Guide lint passed.
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us078-review-full.xml` → 1265 pass, 15 skip, 0 fail.
  - `git diff --check` → clean.
  - LSP diagnostics were attempted for changed TypeScript files, but the TypeScript LSP server exited with code -2; `tsc -b` was clean.
- **Learnings for future iterations:**
  - Declaring a provider-selection CLI flag is not enough; setup-like commands must thread parsed flags into `RuntimeProviderRegistry.select` in both source and compiled dispatch paths.
  - New workspace packages must be added to root `tsconfig.json` references or the repo-wide typecheck gate does not cover them.
  - Provider-podman reuses provider-lando's Unix-socket Podman transport, so US-078 should reject TCP/npipe overrides until a transport-specific client lands in US-079 or later.
  - User-installed provider lifecycle state should live under the provider's own state root (`provider-podman/apps`) rather than relying on process-local plan maps.
---

## [2026-05-27 03:06 CDT] - US-078 Cleanup
- Ran branch-wide AI slop cleanup after review and before push.
- Accepted cleanup:
  - `plugins/provider-podman/src/index.ts`: removed two code-comment references to follow-up story tracking while preserving the technical platform description.
- Rejected cleanup proposals as not purely cosmetic / not needed for planning-comment leakage:
  - `.github/workflows/ci.yml`: restored generated workflow comment block because generated files should not be hand-edited for cosmetic cleanup.
  - `core/src/providers/registry.ts`: restored durable provider-registry module documentation.
  - `plugins/provider-podman/test/capabilities.test.ts`: restored explanatory precedence comments after the per-file agent failed and left only comment deletions.
  - `plugins/provider-podman/test/conflict.test.ts`: restored helper refactor because this cleanup step should not restructure tests.
- Planning-comment scan:
  - No branch-changed code comments still reference spec paths, sections, development phases, PRDs, or story IDs.
  - Remaining matches are runtime remediation fields/messages, schema property names, test names, or guide frontmatter/prose that are outside the code-comment cleanup rule.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0 (`Checked 496 files in 310ms. No fixes applied.`)
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → exit 0
  - `git diff --check` → clean
  - `lsp_diagnostics plugins/provider-podman/src/index.ts` attempted; TypeScript LSP exited with code -2, matching the review-step LSP instability already noted.

## [2026-05-27 10:13 CDT] - Sync
- Previous branch: us-078-provider-podman-linux
- Previous PR: #176 https://github.com/AaronFeledy/core4/pull/176
- Action: dispatched leftover push (Case E, 6 commits unpushed, no PR). Ran push+babysit inline. CI required 1 flaky-build re-run and 4 triage rounds: (1) fix test fixture missing socketPath field; (2) Bugbot — fix eventService forwarding, list persisted plans, rememberPlan synchronous mutation; (3) Bugbot — fix rememberPlan Effect deferral; (4) Bugbot — fix forgetPlan eager map delete. All checks SUCCESS on final run, Bugbot found no new issues on latest commit. Merged via squash.
- Local main updated: yes (b67e500)
- Branch after sync: main
- Stash created: no
---

## [2026-05-27 11:50 CDT] - US-079
- Implemented `@lando/provider-podman` Podman Desktop coverage for macOS + Windows.
- Files changed:
  - `plugins/provider-podman/src/index.ts`: added `resolvePodmanDesktopMachine`/`resolvePodmanDesktopMachineName` (precedence: `LANDO_PODMAN_MACHINE` > `PODMAN_MACHINE_NAME` > `podman-machine-default`, with strict `[A-Za-z0-9][A-Za-z0-9._-]*` validation), `discoverPodmanDesktopSockets` (override-first probe order, `[]` on Linux), and `PodmanMachineNotRunningError` (tagged subclass of `ProviderUnavailableError`); wired the new error into the capability/info probe so connection-style failures on `darwin`/`win32` surface a Podman Desktop remediation that includes both `podman machine start <machine>` and the "open Podman Desktop" GUI hint.
  - `plugins/provider-podman/test/capabilities.test.ts`: extended socket-discovery coverage with `LANDO_PODMAN_MACHINE`/`PODMAN_MACHINE_NAME` override cases for macOS and Windows, malformed-machine-name rejection, and dedicated `discoverPodmanDesktopSockets` + `resolvePodmanDesktopMachine` tests.
  - `plugins/provider-podman/test/desktop.integration.test.ts` (new): fake-client integration tests covering `apply` / `inspect` / `exec` / `logs` / `destroy` for both `darwin` and `win32`, plus `PodmanMachineNotRunningError` mapping (with the Linux non-mapping case as a regression guard) and a `LANDO_TEST_PODMAN_DESKTOP_SOCKET`-gated live contract test that runs `runProviderContract` against a real Podman Desktop socket.
  - `core/src/cli/commands/doctor.ts`: derived `providerKind` ("managed" for `provider-lando`, "user-installed" for every other id) and rendered it in both the text output and the NDJSON `doctor.check` payload + `context`; updated ordering helpers so the new field has a stable position.
  - `core/test/cli/doctor.test.ts`, `core/test/cli/fixtures/meta-doctor.provider-status.ndjson`, `core/test/cli/fixtures/meta-doctor.provider-status.windows.ndjson`: added `providerKind` assertions per provider id and regenerated the golden NDJSON fixtures to include the new field.
  - `docs/guides/setup/provider-podman-macos.mdx` (new), `docs/guides/setup/provider-podman-windows.mdx` (new): required guide coverage with non-rendered scenarios for Podman Desktop discovery (default + LANDO_PODMAN_MACHINE override), machine-not-running remediation, and the `LANDO_TEST_PODMAN_DESKTOP_SOCKET` live gate.
- Verification:
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → "Guide lint passed."
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/` → 51 pass, 2 skip, 0 fail (the 2 skips are the existing `LANDO_TEST_PODMAN_SOCKET` Linux live test and the new `LANDO_TEST_PODMAN_DESKTOP_SOCKET` gated live test).
  - `PATH="$HOME/.bun/bin:$PATH" bun test core/test/cli/doctor.test.ts` → 14 pass, 1 skip (the existing Windows live provider-lando test).
  - `PATH="$HOME/.bun/bin:$PATH" bun test` → 1295 pass, 16 skip, 1 fail; the single failure was the order-sensitive `compiled $bunfs CLI` deferred-stub remediation parity test, which passes when its file (`core/test/cli/deferred-commands.test.ts`) is rerun in isolation (36 tests, 0 fail). Matches the compiled-binary flaky pattern documented in the US-074/US-075 progress notes.
  - Manual CLI smoke (build step): direct invocation of `doctor()` + `renderDoctorResult()` / `renderDoctorResultAsNdjson()` against `TestRuntimeProvider` with overridden ids confirmed `providerKind: managed` for `id="lando"` and `providerKind: user-installed` for `id` in `{podman, docker, third-party}` in both text and NDJSON outputs.
  - Manual API smoke: `resolvePodmanSocket` / `discoverPodmanDesktopSockets` returned the expected paths for default + `LANDO_PODMAN_MACHINE` override on `darwin` and `win32`, and `[]` on `linux`.
- **Learnings for future iterations:**
  - `RuntimeProviderShape` is on the SDK compat lock (`sdk/test/library/sdk-backward-compatibility.test.ts`); any new field — even optional — fails the frozen-signature equality check. Surface presentation-only distinctions like `providerKind` in the consuming `core/` CLI command (mapped by provider id) instead of widening the SDK service tag.
  - Provider-lando's bringUp lifecycle inspect (`GET /containers/<name>/json`) decides whether to call `POST /containers/create?name=...`; a fake API that always returns `200` for `/json` makes `create` look like dead code in tests. Track the `existing` set in your fake and return `404` for unknown containers so apply tests actually exercise the create path.
  - `/exec/<id>/json` ends with `/json` and will collide with the container-inspect handler if the latter only matches by `.endsWith("/json")`. Always gate the container-inspect branch with `.startsWith("/containers/")` (or order the exec handler first) when assembling Podman REST API fakes.
  - When mapping low-level Podman API connection failures (`ENOENT`, `ECONNREFUSED`, "named pipe not found", "system cannot find the path") to a Podman Desktop "machine not running" error, walk the error chain (`message`, `stderr`, `code`, `cause`) — the typed `ProviderUnavailableError` from `provider-lando` carries the real cause one level deeper.

---
## [2026-05-27 11:31 CDT] - US-079 Review
- Reviewed the US-079 `@lando/provider-podman` Podman Desktop implementation against `spec/beta/prd-beta-01-provider-matrix.md` and the build-step commit (`HEAD`).
- Fixed confirmed review blockers:
  - Added a Windows named-pipe Podman API client path and routed exported/default `makePodmanApiClient()` through it for `npipe:` sockets, so the Windows Podman Desktop default is no longer fake-client-only.
  - Allowed provider-podman's default Windows `npipe://./pipe/podman-machine-default` path while preserving fail-closed rejection for unsupported non-Unix/non-npipe transports.
  - Converted malformed Podman Desktop machine names into a tagged `InvalidPodmanMachineNameError` and added rejection coverage.
  - Used the effective process environment for Podman Desktop machine-not-running remediation, so env-selected machines do not report `podman-machine-default` incorrectly.
  - Tightened the live Podman Desktop contract gate to require macOS/Windows in addition to `LANDO_TEST_PODMAN_DESKTOP_SOCKET`.
- Files changed:
  - `plugins/provider-podman/src/index.ts`
  - `plugins/provider-podman/src/named-pipe.ts`
  - `plugins/provider-podman/test/capabilities.test.ts`
  - `plugins/provider-podman/test/desktop.integration.test.ts`
  - `spec/beta/progress.txt`
- `review-work` findings:
  - Fixed: Windows Podman Desktop resolved to `npipe://...` but the real provider rejected non-`unix://` sockets.
  - Fixed: the real Podman API client was Unix-socket-only, so the documented Windows live `npipe://...` path could not run.
  - Fixed: live Podman Desktop test was env-gated but not platform-gated.
  - Fixed: machine-not-running remediation could use the default machine name even when `process.env` selected a custom machine.
  - Fixed: malformed machine-name rejection was claimed in the progress log but lacked test coverage and threw a raw error.
  - Rejected: guide-coverage table omission for US-079 is a PRD ownership/documentation drift concern, not a story blocker; this review kept the existing US-079 guide files and did not expand PRD governance scope.
- Verification:
  - LSP diagnostics attempted on changed TypeScript files; TypeScript LSP exited with code -2, matching prior local LSP behavior.
  - `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun run lint:guides` → exit 0.
  - `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/ --reporter=junit --reporter-outfile=/tmp/us079-review-provider-podman-all.xml` → 56 tests, 112 assertions, 0 failures, 2 skips.
  - `PATH="$HOME/.bun/bin:$PATH" bun test core/test/cli/doctor.test.ts --reporter=junit --reporter-outfile=/tmp/us079-review-doctor.xml` → 15 tests, 102 assertions, 0 failures, 1 skip.
  - `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us079-review-full.xml` → 1315 tests, 6410 assertions, 0 failures, 16 skips.
- **Learnings for future iterations:**
  - Discovery tests are insufficient when a provider has scheme-specific transports; add a factory-path regression that the default platform socket reaches the real client factory instead of stopping at validation.
  - For platform-specific live tests, gate on both the exact opt-in environment variable and the expected host platform so accidental CI env leakage cannot exercise host runtimes on the wrong OS.
  - When a remediation references a selected machine name, derive both socket resolution and error text from one effective environment object.
---
## [2026-05-27 16:54 UTC] - US-079 Cleanup
- Ran the branch-wide `remove-ai-slops` cleanup after review and before push with the planning-doc comment rules.
- Cleanup result: cosmetic-only cleanup; no provider behavior, public API, socket discovery precedence, named-pipe transport, doctor output semantics, test assertions, fixtures, docs, or generated/planning files changed.
- Files changed:
  - `core/src/cli/commands/doctor.ts`: reworded the module header to remove planning-section citations while preserving the durable diagnostic summary.
  - `plugins/provider-podman/src/index.ts`: removed one redundant setup comment and shortened the `LANDO_TEST_PODMAN_SOCKET` precedence note without changing socket resolution.
  - `plugins/provider-podman/test/capabilities.test.ts`: removed redundant inline comments that restated the socket-precedence assertions.
- Slop-remover output: source/test/doc cleanup agents reported no safe edits for `named-pipe.ts`, doctor tests, desktop integration tests, and Podman Desktop guide files; accepted only the cosmetic source/test comment edits above.
- Planning-doc leakage scan: no branch-changed source/test comments still cite planning docs, section markers, development phases, PRDs, story IDs, or story prose.
- Verification: `PATH="$HOME/.bun/bin:$PATH" bun run typecheck` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun run lint` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/ core/test/cli/doctor.test.ts` → exit 0; first full `bun test --reporter=junit --reporter-outfile=/tmp/us079-cleanup-full.xml` run hit the known order-sensitive `core/test/cli/deferred-commands.test.ts` compiled `$bunfs` parity failure, which passed in isolation; rerun `PATH="$HOME/.bun/bin:$PATH" bun test --reporter=junit --reporter-outfile=/tmp/us079-cleanup-rerun.xml` → 1315 tests, 6410 assertions, 0 failures, 16 skips; `git diff --check` → clean. LSP diagnostics were attempted for changed TypeScript files, but the TypeScript LSP servers exited with code -2.
---

## [2026-05-27 17:03 UTC] - Push
- Branch: us-079-provider-podman-desktop
- Push: pushed
- Commit(s): 3c1bd41 feat: @lando/provider-podman on macOS + Windows via Podman Desktop; 88ec019 fix(provider-podman): support Windows Podman Desktop npipe; 381bac2 docs: record US-079 review fixes; 903000b chore: remove AI slop
- PR: #178 https://github.com/AaronFeledy/core4/pull/178 (created)
---

## [2026-05-27 17:20 UTC] - US-079 Bugbot triage
- Fixed the two remaining low-severity Cursor Bugbot findings in `plugins/provider-podman/src/index.ts`:
  - removed the unused `resolvePodmanDesktopMachineName` alias export and updated the remaining callsite to use `resolvePodmanDesktopMachine` directly
  - tightened the machine-not-running pattern set by dropping the broad `/named pipe/i` matcher
- Verification: `PATH="$HOME/.bun/bin:$PATH" bun run --filter='@lando/provider-podman' typecheck` → exit 0; `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/test/capabilities.test.ts` → 26 pass, 0 fail; `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/test/desktop.integration.test.ts` → 14 pass, 1 skip, 0 fail; `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/` → 54 pass, 2 skip, 0 fail.
---

## [2026-05-27 17:45 UTC] - US-079 npipe regression fix
- Fixed the Windows Podman Desktop named-pipe stream flush path so a complete final chunk is not dropped when the socket closes before the terminal CRLF is buffered.
- Files changed:
  - `plugins/provider-podman/src/named-pipe.ts`: replaced the end-of-stream `flushChunkedBufferAtEnd` helper with the same permissive chunk flush logic used by `provider-docker`, so the final chunk is yielded even if the trailing chunk terminator arrives split or missing at close.
  - `plugins/provider-podman/src/index.ts`: re-exported `flushChunkedBufferAtEnd` for the regression test.
  - `plugins/provider-podman/test/named-pipe.test.ts` (new): regression coverage for a final chunk without trailing CRLF and for a complete chunked frame.
- Verification: `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/test/named-pipe.test.ts` → 2 pass, 0 fail; `PATH="$HOME/.bun/bin:$PATH" bun test plugins/provider-podman/` → 56 pass, 2 skip, 0 fail.
---

## [2026-05-27 17:36 UTC] - US-079 PR #178 Bugbot triage round 2
- Triaged the two new Cursor Bugbot comments on commit `cdd728e`:
  - Comment 1 (`plugins/provider-podman/src/index.ts` L574-581, "Throwing `resolvePodmanDesktopMachine` inside `Effect.mapError` causes untyped defect") - **real issue, fixed**. When the socket path was supplied via `options.socketPath`, `DOCKER_HOST`, or `LANDO_TEST_PODMAN_SOCKET`, `resolvePodmanSocket` never validated `LANDO_PODMAN_MACHINE` / `PODMAN_MACHINE_NAME`; a later `isLikelyMachineNotRunning` branch in the capability-probe `Effect.mapError` then called `resolvePodmanDesktopMachine(effectiveEnv)` synchronously, which on an invalid machine name throws `InvalidPodmanMachineNameError` into the Effect runtime as a `Cause.Die` defect instead of a typed `ProviderUnavailableError`. Fix: resolve the desktop machine name eagerly at the top of `makeRuntimeProvider` inside a `try`/`catch` that returns `Effect.fail(cause)` for `ProviderUnavailableError`, then reuse the resolved value inside the `mapError` branch so no synchronous throw can escape from there.
  - Comment 2 (`plugins/provider-podman/src/named-pipe.ts` whole file, "Named-pipe transport duplicates ~300 lines from provider-docker") - **deferred, non-blocking for this PR**. The duplication is real but extracting a shared npipe transport is a cross-package refactor that touches `provider-docker`, `provider-podman` `package.json`/`tsconfig.json`, and the integration test surface; it is architectural cleanup, not a correctness bug, and is out of scope for this story's PR. Filed locally as a stash (`pr178-round2-named-pipe-refactor-deferred`) so the spike work is not lost; the proper home is a follow-up refactor PR scoped to a shared npipe transport with its own tests.
- Files changed:
  - `plugins/provider-podman/src/index.ts`: eager `resolvePodmanDesktopMachine(effectiveEnv)` call with typed-error catch; `Effect.mapError` branch now reuses the resolved name instead of calling the throwing helper inline.
- Verification: pending - `bun run typecheck`, `bun run lint`, `bun test plugins/provider-podman/`, then full `bun test` before push. Results recorded below once green.
