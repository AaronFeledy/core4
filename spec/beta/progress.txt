# Beta Progress Log — Lando v4 Phase 3 (full breadth)

Source PRDs: spec/beta/prd-beta-{00..13}-*.md
Spec source of truth: spec/ROADMAP.md Phase 3 + spec/{01..19}-*.md and spec/15-binary-build-and-release.md (§17)

## Codebase Patterns
- Plugin contribution surfaces that must become part of a materialized artifact (e.g. `globalServices:` → global `.lando.dist.yml`) follow this seam: a pure resolver in `core/src/services/*` collects across `PluginRegistry.list` manifests (deterministic, id-sorted), detects cross-plugin id collisions as a tagged error, then a materializer turns each accepted contribution into the artifact shape via an INJECTABLE loader (default = dynamic `import()` of the contribution `module`; tests inject a fake or point at a temp module). The low-level writer service (e.g. `GlobalAppService.regenerateDist`) stays frozen and dumb; resolution lives in core helpers the command composes. For a `bootstrap: "global"` command to `yield* PluginRegistry`, `PluginRegistryLive` must be in the TOP-LEVEL `Layer.mergeAll` of `makeProviderRuntimeLive` (not only `Layer.provide`'d into `RuntimeProviderRegistryLive`), and the compiled-dispatcher cast in `core/src/cli/run.ts` must widen to the command's full required-service set.
- User-app CLI commands resolve the current project via `loadUserLandofile(landofileService)` (`core/src/cli/app-resolution.ts`), never `landofileService.discover` directly — a canary test (`core/test/cli/reserved-app-id-canary.test.ts`) forbids `.discover` in `core/src/cli/commands/**`. The helper rejects the reserved `global` app id with `AppIdReservedError` before any plan is built. Reason: `LandofileService.discover` / `AppPlanner.plan` error unions are frozen by the SDK backward-compat fixture, so new resolution errors must ride a new seam, and each command's hand-maintained error union (plus composing commands like `rebuild`/`restart`) gains the new error.
- `lando doctor` subsystem recovery is classified per-subsystem in `core/src/cli/commands/doctor-subsystems.ts` via a `SubsystemSpec.recovery` of `automatic` (non-privileged, exposes a re-runnable `setup()` — proxy, ssh) or `manual` (privileged setup like CA trust-store / host DNS, or no `setup()` at all — certs, host-proxy, healthcheck, scanner). `--fix` only re-runs `setup()` for `automatic` degraded subsystems; `manual` ones report `fixOutcome: skipped-manual`. Doctor never auto-elevates. Fix outcomes ride in the check `context` (`fixOutcome`/`fixCommand`/`fixExitCode`/`fixError`) so both the plain-text and NDJSON renderers surface them with no renderer changes.
- New doctor CLI flags must be wired in BOTH dispatch paths to keep source/compiled parity: the OCLIF `static flags` + `inputDoctorOptions` in `core/src/cli/oclif/commands/meta/doctor.ts`, and a hand-rolled `parse*Flag` in `core/src/cli/run.ts` `runDoctor`. Both funnel into the same `doctorReport(options)`.
- Running `@lando/core/cli` in-process (source mode → OCLIF `execute()`) calls `process.exit` on any erroring command, which silently kills the surrounding test runner. Scenario/test harnesses that invoke the CLI in-process must only drive commands that succeed; faked/canned runners exist precisely to avoid this for `start`/`destroy`/`curl`-style flows. Spawn the compiled binary (`core/dist/lando`, overridable via `LANDO_SCENARIO_E2E_BINARY`) when you need to run an erroring command without taking down the runner.
- `machineFailure()` in `plugins/provider-lando/src/setup.ts` is the single factory for all Podman machine errors; extend its regex conditions to detect new OS-specific virtualization prerequisites before adding new error subclasses.
- `buildSetupSteps()` drives both the event-tree children list and which setup actions run; adding a platform requires updating that function + the corresponding `if (platform === ...) ...` block in `setupProviderLando`.
- Windows Podman machine lifecycle is structurally identical to macOS (`ensureWindowsPodmanMachine` mirrors `ensureMacOSPodmanMachine`); the separation exists for future divergence (e.g. WSL-specific machine flags).
- Host-mutating live tests should gate on both the expected platform and an exact opt-in env value (`process.platform !== "win32" || process.env.GATE_VAR !== "1"`) so accidental truthy values do not run provider setup on the wrong host.
- All three providers (docker/lando/podman) realize networking through the shared `@lando/sdk/schema` helpers (`landoNetworkNames`, `landoServiceNetworkAliases`, `landoAppNetworkName`, `landoSharedNetworkName`), not from `plan.networks` directly. To make every provider consume a new per-app networking field, widen those helpers to read it (with a slug-derived fallback for legacy/hand-built plans) instead of editing each provider — provider behavior stays identical when the plan reproduces the slug derivation.
- Adding any new export to `@lando/sdk/schema` (schemas AND helper functions) requires adding it to the `Additive Alpha schema exports` list in `sdk/API_COMPATIBILITY.md`; the `sdk-backward-compatibility` test diffs live exports vs that list. Adding a field to a snapshotted schema (e.g. `AppPlan`) also requires `bun run codegen:schema-snapshot`.
- Adding a new executable-guide component touches five registration points: SDK `sdk/src/docs/components/props.ts` (prop schema + `decode*Either` helper + the `BETA_COMPONENTS` allowlist consulted by `assertAlpha2Component`), `scripts/build-guide-scenarios.ts` (the `GuideStepComponent` union + `parseStepComponent` + `renderStepComponent`), the `scripts/lint-guides.ts` component allowlist, the SDK schema registry in `sdk/src/schema/index.ts` (export/import/`JSON_SCHEMA_REGISTRY`/`getJsonSchema`), and `SDK_SCHEMA_NAMES` in `scripts/build-schema-snapshot.ts`; finish with `bun run codegen:schema-snapshot` and add the new export name to `sdk/API_COMPATIBILITY.md`. (A component can be schema-registered long before it is code-emitting — e.g. `<Hidden>` only needed the decode helper, allowlist flip, codegen, lint, and runtime seam.)
- Any new scenario transcript frame kind must be added to the `TranscriptFrame` union in `sdk/src/docs/transcript.ts`; `persistTranscript` runs `Schema.encodeSync(Transcript)` at scenario teardown and throws on an unknown frame kind.
- Executable-guide scenarios fan out into one generated test file per axis variant. `tabs:` frontmatter declares the single implicit `default` axis's ordered values; `variantsOf()` in `scripts/build-guide-scenarios.ts` turns those into per-scenario variant files named `<scenario-id>.<value>.test.ts` with a `// @variant: default=<value>` header. A scenario body is an ordered `Step | TabsBlock`; `<Tabs>`/`<Tab name>` live inside `<Scenario>` and each tab carries its own `<Step>`s. `resolveVariantSteps()` computes the block step-name union in source order (first occurrence wins) and emits `test.skip(<step>, …)` for union steps a variant's tab omits. A guide with `tabs:` but no `<Tabs>` block still fans every scenario into N identical variants. Multi-axis `axes:` (a record of axis-name → ordered values) fans scenarios into the Cartesian product of all axes in declaration order; a variant is now an ordered list of `{axis,value}` pairs, the filename suffix is the dot-joined values, and the header is space-joined `// @variant: axis=value` pairs. `<Tabs axis="A">` resolves only against that variant's value for axis A (axis prop required when a guide declares >1 axis, inferred for a single axis). Per-cell `variants:` overrides are keyed by the dot-joined cell key and refine `skip` (guide-wide `skip:` ?? per-cell → emits a whole-variant `test.skip`), `tags` (guide ∪ cell → `// @tags:`), and `platforms` (cell wins → `// @platforms:`); only per-cell overrides emit those header lines so guides without `variants:` stay byte-identical. `tabs:`/`axes:` are mutually exclusive and `variants:` keys must resolve to real cells — both enforced on decode by `GuideFrontmatterChecked` (a decode-only refinement; the exported `GuideFrontmatter` stays a plain struct so its JSON snapshot keeps a single named definition). `findDeferredGuideFrontmatter` now only defers `defaultLayer: e2e` (§19.11). NOTE: guide frontmatter is parsed by the Landofile YAML subset (`core/src/landofile/parser.ts`), which only supports block-style maps (no inline `{ }` flow maps) and whose map-key charset now includes `.` so dot-joined `variants:` keys parse.
- Transcript suppression has a single runtime seam: `ScenarioContext.hidden(effect)` (depth-counted, restored on failure) funnels all frame writes through an `appendFrame` choke point in `makeScenarioContext`. Any "emit-but-don't-render" guide construct (`<Hidden>`) should reuse `context.hidden` rather than re-plumbing the frame array.
- `scripts/lint-guides.ts` separates pure AST rules from filesystem-aware fixture rules. `lintGuideContent(sourcePath, content, options?)` stays pure and deterministic — fixture rules read an injected `options.fixtures` inventory (`GuideFixtureInventoryEntry[]` = `{ name, sourcePath, scope: "local"|"shared", kind: "directory"|"symlink"|"other", symlinkPaths? }`), never the disk — so they are unit-testable by passing synthetic inventories (no fixture dirs on disk). Only `lintGuides(root)` walks the disk: it derives each guide's `id` from frontmatter (NOT the MDX path — fixtures live at `docs/guides/<id>/fixtures/<name>` even when the MDX is nested like `docs/guides/services/mongodb.mdx`), inventories local + shared (`docs/guides/fixtures/`) fixture roots, and flags symlinks at the fixture root or any descendant. Add new guide-content lint rules as a pure `lint*` helper called from `lintGuideContent`; add new fixture/disk rules behind the injected inventory. Component-prop validation reuses the SDK `decode*PropsEither` helpers (e.g. `decodeVerifyPropsEither` for `<Verify>` matcher shape) so lint mirrors codegen exactly and needs no new SDK exports.
- Lifecycle event scope: events in `sdk/src/events/index.ts` carry a `scope` discriminator — `scope: "app"` on the canonical per-app analogs (`pre-start`/`post-start`/`pre-stop`/`post-stop`, which are SCHEMA-ONLY, never emitted) and `scope: "global"` on the `pre-/post-global-start|stop` family. The actually-emitted per-app path uses a SEPARATE family (`pre-app-start`/`pre-app-stop`/`pre-service-*`, keyed by `eventName`+`appRef`+`providerId`) that carries no `scope` — leave it alone when adding scope-tagged work. Event payload schemas are NOT frozen (not in `sdk-mvp-surface.json`, not snapshotted, not in `API_COMPATIBILITY.md`); adding a new event schema or a field only needs a presence entry in `sdk/test/library/exports.test.ts`. A command that publishes events must add `EventService` to its required-service union, `EventError` to its failure union, and (for `bootstrap: "global"` commands) the `run.ts` `globalRuntimeLayer` cast must be widened to include `EventService` (the global runtime tier already provides `EventServiceLive`).

## Summary — US-074 through US-110 + guide-engine work (PRD-01..04 + early PRD-05)

This condenses the provider-matrix, service-catalog, file-sync, and subsystem stories (plus the agent2 guide-engine workstream). All landed on `main` via squash-merged PRs (#172–#221); per-story commit lists, PR-created markers, and merge-only babysit notes are intentionally dropped (they live in git history).

**PRD-01 Provider Matrix (US-074–082).** Cross-platform Podman-machine lifecycle for `@lando/provider-lando` (Windows mirrors macOS; prerequisite errors map to Hyper-V/WSL2/VM-Platform remediation). Pinned, SHA-256-verified, idempotent runtime-bundle downloader (Linux/macOS/Windows) with a caller-injectable entry seam for deterministic fixtures. `@lando/provider-docker` Windows Docker Desktop support via a real named-pipe (npipe) IPC HTTP transport. `@lando/provider-podman` shipped as a bundled-but-non-default opt-in provider for Linux + Podman Desktop (macOS/Windows). Shared `RuntimeProvider` contract suite + matrix runner (`runProviderContract` / `runProviderContractMatrix`) covering every supported platform across all four providers. Partial-failure rollback + `AbortSignal` for provider-docker. Provider selection precedence `flag > Landofile > env (LANDO_PROVIDER) > config > capability-default` with `meta:doctor` conflict diagnostics (`provider-conflict` check + executable remediation).

**PRD-02 Service Catalog (US-083–094).** Added service types to `@lando/service-lando`: `go` (1.22/1.23, framework `none` only), `mongodb`, `memcached`, `valkey` (dual-scheme `valkey://`+`redis://`), `solr`, `elasticsearch`, `opensearch`, `meilisearch`, `static`/`static:caddy`, and raw `compose` passthrough (default `/app` bind + `appMount:false` opt-out). Framework-preset coverage documented in the package README. Matrix-driven Beta service contract suite in `@lando/sdk/test` wired across all new types. Recurring rules: a new service type touches five test surfaces (`package`/`registration`/`env-contract` tests + `plugin.yaml` + the per-type test) and needs `bun run codegen:schema-snapshot`; healthchecks express HTTP probes as in-container `curl` `command` healthchecks (Alpha planner rejects `http` kind); pick the base image's actual shell (`golang:`→bash, alpine images→`sh`).

**PRD-03 File Sync / Mutagen (US-095–098).** Published the `FileSyncEngine` SDK contract (tag, schemas, `FileSyncStartError`/`FileSyncDriftError`/`FileSyncStopError`) + contract suite. Shipped the bundled `@lando/file-sync-mutagen` Live Layer (fixed §10.6.2 capability matrix, deterministic kebab-case session names, injectable `MutagenClient` seam, fail-closed `unavailable` default) via a new `fileSyncEngines` contribution kind. Added pinned, SHA-256-verified Mutagen host-CLI + agent download to `lando setup` (real v0.18.1 pins; per-target Linux agents extracted from the nested `mutagen-agents.tar.gz`), the `--skip-file-sync` flag, and a `file-sync` doctor check for slow-bind-mount providers. Wired auto-selected file sync into the lifecycle: `AppPlanner` emits a per-service `FileSyncPlan`; `lando start` creates one session per entry, `stop`/`destroy` terminate them; accelerated mounts use the planned named volumes.

**PRD-04 Subsystems (US-101–110).** Subsystem contracts published with the precedent pattern (SDK contract + error + test helpers + contract test + core fail-closed `Unavailable`/`Disabled` Live Layer + docs guide + API_COMPATIBILITY entry; per-platform Live Layers deferred to later stories) — covering `HostProxyService` (`--host-proxy=auto|none` opt-out) and the shared cross-app network capability (`sharedCrossAppNetwork: true` flipped on all providers; providers create/join `lando_bridge_network` with `<svc>.<app>.internal` aliases; app destroy preserves the shared network). Added the `globalServices:` plugin-contribution surface + `GlobalServiceContribution` schema + `validateGlobalServiceContributions` planner gate. `lando doctor` gained per-subsystem diagnostics (`subsystemDoctor`/`doctorReport` as a separate result section to keep provider-status fixtures byte-stable) and failure-recovery: degraded subsystems report `automatic` (`lando doctor --fix` re-runs `setup()`) or `manual` (`lando setup`) recovery, with secret-redacted fix outcomes. A typed per-app `NetworkingPlan` (`perAppBridge` + optional `sharedNetworkMembership`) now flows through the shared schema helpers (slug-derived fallback for legacy plans).

**Guide-engine workstream (US-175–179, agent2).** Split the scenario `runCli` into three runners on `@lando/core/testing` (`scenario` real in-process seam, `e2e` compiled-binary spawn, `testOnlyFake` canned shim; only `testOnlyFake` populates `ScenarioContext.events`). Added the read-only `<Inspect>` component (+ `inspect` transcript frame). Generalized the variant engine from single-axis `tabs:` to multi-axis `axes:` with Cartesian fan-out and per-cell `variants:` overrides. Made `<Hidden>` a code-emitting block: its `<Step>`s emit real scenario code inside a `context.hidden(...)` scope that suppresses their transcript frames while keeping the steps runnable (details in Codebase Patterns above).

**Recurring CI / environment themes (not regressions).**
- `provider-integration-linux-x64` live Docker/Podman "logs emits at least one chunk" was repeatedly flaky: root causes were the SDK contract's tight `logs` timeout (bumped 5s→15s) and provider-docker treating framed Docker log bytes as single-chunk (fixed by buffering framed bytes across socket chunks); the live fixture now emits a periodic log line. Live scenarios must publish backends on unprivileged host ports (e.g. 31081), not port 80, or rootless Podman fails container start.
- `core/test/scripts/codegen-ci.test.ts` and `core/test/build/schema-snapshot.test.ts` flake under arbitrary parallel subsets (shared codegen/biome writes race); both are byte-idempotent and pass in isolation and in the full run. Compiled-binary tests (`compile.test.ts`, `aliases.test.ts`) flake on `ETXTBSY` under concurrent `bun run build`; rebuild and re-run in isolation before treating as a regression.

**Cross-cutting learnings retained.**
- The SDK backward-compat fixture (`sdk/test/fixtures/sdk-mvp-surface.json`) freezes existing service-tag method signatures by exact match: you cannot widen a frozen tag's error union — surface new errors through a new non-frozen seam. Additive *new* tags only need a `sdk/API_COMPATIBILITY.md` entry; new tagged *errors* are tracked by `sdk/test/library/exports.test.ts` presence only (not API_COMPATIBILITY, not the schema snapshot).
- Adding a required field to `AppPlan` breaks provider plugins, SDK schema tests, and the binary-cache fixture at once — prefer `Schema.optional` with a planner that always populates it + helper fallbacks (≈47 hand-built fixtures otherwise need editing).
- `Layer.mergeAll(a, b)` gives the LAST layer precedence on a duplicate tag; `Effect.serviceOption`/dependencies resolve against what the sub-layer provides, so `Layer.provide` deps onto the sub-layer that needs them.
- Bundled `globalServices:` resolve from a static plugin export captured in `core/src/plugins/bundled.ts`, never the manifest `module:` path (relative imports can't resolve and `import()` is impossible in a `bun build --compile` binary); the `module:` field is docs + the non-bundled fallback only.

## Summary — US-111 through US-118 + guide-engine US-180/181/182 (PRD-B-05 global app + agent2)

This condenses the global-app lifecycle workstream and the interleaved agent2 guide-engine work. All landed on `main` via squash-merged PRs; per-story commit lists, "verified complete" re-checks, and babysit/merge notes are intentionally dropped (they live in git history). The recurring seams (dist/user file split, frozen-tag-vs-new-seam, bundled `globalServices:` static map, layer-wiring) are captured in **Codebase Patterns** above and in AGENTS.md.

**PRD-B-05 Global App (US-111–118).** Shipped `GlobalAppService` (`id`/`root`/`ensureRoot`, idempotently materializes `<userDataRoot>/global`) and reserved the `global` app id — any user Landofile resolving to `name: global` is rejected with `AppIdReservedError` before a plan is built, enforced by the `loadUserLandofile` seam (`core/src/cli/app-resolution.ts`) + a canary test. `meta:global:install` materializes the global stack via spec §20's dist/user file split rather than an in-file merge: `regenerateDist` is the single idempotent writer of the generated `.lando.dist.yml` (Lando header + body sha256), and `ensureUserLandofile` writes a comment-only `.lando.yml` overlay exactly once so user edits survive by construction (foreign/edited dist → `GlobalDistConflictError`). Added the `globalServices:` plugin-contribution surface — `resolveGlobalServiceContributions` (deterministic, id-sorted, cross-plugin collision → `GlobalServiceCollisionError`) + capability-filtered `materializeGlobalServices` — then shipped two bundled global services: `traefik` (from `@lando/proxy-traefik`, `compose` on `traefik:v3.3` with a provider-neutral file-provider dashboard) and Mailpit (from `@lando/service-lando`, `compose` on `axllent/mailpit`). Both resolve from a static `globalServices` map captured in the generated `core/src/plugins/bundled.ts` (compiled-binary-safe) via `bundledFirstGlobalServiceLoader`, never the manifest `module:` path. Mailpit added the per-app `LANDO_MAIL_HOST`/`LANDO_MAIL_PORT` env contract, gated `appKind !== "global"` (inferred from the reserved `global` app name so the global services themselves skip the self-referential projection). Completed the host-level `meta:global:*` namespace — all seven commands (`install`/`start`/`stop`/`status`/`config`/`uninstall`/`destroy`) + `global:*` aliases on a dedicated `makeGlobalRuntimeLive` tier (provider runtime + `AppPlanner`), with `destroy` gated behind `--yes`/`GlobalDestroyConfirmationError` and `--service` genuinely subsetting the applied plan. Published the `Global` lifecycle event scope (`pre-/post-global-start|stop`, `scope: "global"`; the schema-only per-app analogs now carry `scope: "app"`) and emit them around the global apply/teardown. Finally, wired user-app `lando start` to auto-start the global app when `AppPlan.requires.globalServices` is non-empty (planner-populated `["traefik"]` for any routed app, also re-derived on cached-plan read): `ensureGlobalServicesRunning` runs before `pre-app-start`, failures chain into `GlobalAutoStartError`/`GlobalServiceMissingError`, and `stop` never auto-stops the global app.

**Guide-engine (US-180/181/182, agent2).** Added the `<Inline>` (verbatim, non-executed code sample → `inline` transcript frame) and `<Skip>` (scenario-body block emitting module-level `test.skip` placeholders, `reason` ≥ 8 chars) executable-guide components, completing the Beta component vocabulary. Extended the `bun run lint:guides` gate with the remaining §19.10 rules: `<Verify>` matcher-shape validation via `decodeVerifyPropsEither`, and inventory-driven fixture discipline (`guide.fixture.missing`/`.symlink`/`.unused`), plus honoring `GUIDES_DIR_OVERRIDE` so the depth test exercises fixtures. Activated the recipe README strip/flatten pipeline (`scripts/build-recipe-readmes.ts`): an MDX-authored `recipes/<id>/README.mdx` doubles as an executable guide and strips to committed `recipes/<id>/.scaffold/<axis-value...>.md` artifacts (`recipes/lamp` is the canonical example).

---

## [2026-05-31 09:46 CDT] - US-119
- Added a `global-app` section to `lando doctor` (`meta:doctor`) per §10.9. A new read-only `globalAppDoctor()` check (`core/src/cli/commands/doctor-global-app.ts`) reports whether the global app is installed and, when it is, surfaces the materialized dist/user Landofile paths, the last `meta:global:install` timestamp (derived from the dist file mtime), the per-service list parsed from the generated dist file, and the sorted list of plugins contributing `globalServices:`. When not installed it reports `warn` with a `lando global:install` manual solution. The check requires only `GlobalAppService | PluginRegistry | FileSystem` (no provider/app bootstrap) via `DefaultGlobalAppDoctorLayer`, and is merged into `DoctorReport` (`doctor-report.ts`) as a third section after provider + subsystems, with both plain-text and NDJSON renderers. Owned guide `docs/guides/global/doctor-walkthrough.mdx` covers the not-installed, installed, and `--renderer=json` paths.
- Verification: typecheck, lint, lint:guides all green; full `bun test` 2088 pass / 25 skip / 0 real fail (the single `compiled setup install dir` failure is the documented compiled-binary build-pressure flake — `bun run build` succeeds standalone and the test passes 9/9 in isolation after a fresh build; it does not touch the doctor path). CLI QA against an isolated `LANDO_USER_DATA_ROOT`: `lando doctor` before install shows `global-app: warn / installed: false` + remediation; after `lando global:install` shows `global-app: pass` with dist/user paths, `lastInstallTimestamp`, `services: mailpit, traefik`, and `contributingPlugins`; `lando doctor --renderer=json` emits a well-formed `{"_tag":"doctor.check","name":"global-app",...}` event carrying every service id.
- **Learnings for future iterations:**
  - The global-app doctor check parses service ids out of the *generated* dist Landofile text (two-space-indented keys under `services:`), not by re-resolving plugin contributions — the dist file is the materialized source of truth and is deterministically formatted by `GlobalAppService.regenerateDist`, so a lightweight line scan is sufficient and avoids re-running the contribution resolver inside a read-only diagnostic.
  - A new doctor *section* (vs. a flag) is wired only in `doctor-report.ts` (the combined report both dispatch paths share) — there is no per-path flag plumbing like the `--fix`/renderer flags need, because the section always renders. Adding one means: new `*Doctor()` check + `DefaultXDoctorLayer`, a field on `DoctorReport`, and appending to both `renderDoctorReport` (filter empty parts, join) and `renderDoctorReportAsNdjson` (push `checkLinesFromNdjson(...)`). The report's check-count/stream-order assertions in `doctor-report.test.ts` must bump accordingly.
  - When a prior loop run commits a story directly to `main` (unpushed) instead of a story branch, relocate it cleanly: `git switch -c <story-branch>` at the commit, `git branch -f main origin/main`, update `.local/looper/.last-branch`. This preserves the work, keeps `main` == `origin/main` for the Push/PR flow, and avoids a direct-to-main push that bypasses review.
---

## [2026-05-31 10:50 CDT] - US-120
- Shipped the foundational scratch-app runtime seam. Published the additive `ScratchAppService` SDK service tag + `ScratchAppPaths` contract (`root`, `ensureRoot`, `synthesizeId`, `paths`) and the umbrella `ScratchAppError` tagged error from `@lando/sdk`, following the `GlobalAppService` precedent (minimal contract now, grown by the later scratch-lifecycle stories). Added the core `ScratchAppServiceLive` (`core/src/scratch-app/service.ts`) — a `Layer.effect` over `FileSystem` that resolves the scratch base at `<userCacheRoot>/scratch/` via `resolveUserCacheRoot()`, idempotently materializes it, synthesizes `scratch-<base>-<6hex>` ids, and computes the §21.3 per-instance layout (`root/`, `plan.bin`, `info.json`, `build-results.bin`).
- Gave the `scratch` bootstrap level its own runtime tier. `scratch` already existed in `BootstrapLevel` (rank 7, between `global` and `app` per spec §3.2) but the runtime switch aliased it to the provider tier; `runtimeLayerFor` now routes `scratch` to a new `makeScratchRuntimeLive` (= provider tier + `ScratchAppServiceLive`), with a matching `ScratchRuntimeServices` union, `RuntimeLayer` arm, and `makeLandoRuntime({ bootstrap: "scratch" })` overload for library-mode hosts. The provider-tier base carries no `LandofileService` cwd discovery, so the scratch tier "takes no Landofile from CWD" as required.
- Verification: typecheck and lint clean; full `bun test` 2095 pass / 25 skip / 0 fail. Manual QA exercised the shipped `makeLandoRuntime({ bootstrap: "scratch" })` tier end-to-end against an isolated `LANDO_USER_CACHE_ROOT` — `root` → `<cache>/scratch`, `ensureRoot` created the dir on disk, `synthesizeId("My Drupal App")` → `scratch-my-drupal-app-63e2af`, and `paths` laid out the instance dirs.
- **Learnings for future iterations:**
  - Scratch state lives under `<userCacheRoot>` (cache root, `resolveUserCacheRoot()`), NOT `<userDataRoot>` like the global app — the spec (§21.3/§3.4) is explicit. `ScratchAppServiceLive` therefore depends only on `FileSystem` (the cache root is a pure env-resolved path), unlike `GlobalAppServiceLive` which reads `ConfigService.userDataRoot`.
  - `BootstrapLevel` (and `BOOTSTRAP_RANK`) already shipped `scratch` at rank 7 with frozen ordering tests (`sdk/test/schema/bootstrap.test.ts`, `core/test/unit/bootstrap.test.ts`) and a frozen-surface entry — do NOT move it (the PRD's "between commands and provider" is superseded by spec §3.2's `…provider → global → scratch → app`). `AppRef.kind` likewise already includes `"scratch"`. The only runtime gap was the missing dedicated tier + the `ScratchAppService` tag itself.
  - The `ScratchAppService` contract is intentionally minimal (root/ensureRoot/synthesizeId/paths) — the spec §21.5 `acquire`/`start`/`stop`/`destroy`/`gc` surface belongs to US-121–128, which grow this additive (non-frozen) tag the same way US-112+ grew `GlobalAppService`.
---

## [2026-05-31 11:30 CDT] - US-120 Review
- Reviewed `ScratchAppService` + scratch bootstrap tier against PRD-B-06 / spec §21.3. Fixed one real bug (`review-work`/Oracle MAJOR): `ScratchAppService.paths(id)` did `join(base, id)` on an unvalidated id, so a `..`/separator-bearing id could escape `<userCacheRoot>/scratch/<id>/` (spec §21.3 invariant). `paths` is a published, compat-locked SDK seam that downstream stories feed ids from non-`synthesizeId` sources (fork-mode `<source>-scratch-<rand>`, `apps:scratch:destroy <id>`, the gc/registry reaper), so the guard matters before those land. Added an `isUnsafeScratchId` reject-guard (rejects empty, path separators, NUL, pure-dot segments) that fails with `ScratchAppError`, mirroring the existing `sanitizeAppName` all-dot guard in the same `core/src/cache/paths.ts` neighborhood — reject (not sanitize) because `paths` must round-trip the exact id for lookups. Added a regression test covering `..`, `.`, `../../etc`, `a/b`, `a\b`, `""`.
- Review notes (rejected): entropy bump (24-bit→more hex) — spec §21.2 mandates `scratch-<base>-<6-hex>` and relies on the §21.11 registry + per-id lock for uniqueness; changing it contradicts the spec and frozen test. `ensureRoot` declaring `Scope.Scope` and the wide `ScratchAppError` channel on pure methods — intentional forward-looking design for the §21.6 scope-bound finalizer and SDK evolution; no defect (no finalizer leak).
- Verification: typecheck clean, lint clean (614 files), scratch suite 6/6, full `bun test` 2096 pass / 25 skip / 0 fail on a clean run (intermittent 1–2 failures are the documented parallel-load flakes — the `biome.test.ts` lint-gate meta-test, which also failed pre-edit, and the compiled-binary build-pressure flake; neither touches scratch).
---

## [2026-05-31 11:00 CDT] - US-196 (agent2)
- Shipped `bun run dev:guides`, the watch-mode TDD driver for executable guides (`scripts/dev-guides.ts`). Each iteration regenerates affected scenarios via `build-guide-scenarios.ts`, typechecks the generated path with an ephemeral `tsc --noEmit`, then re-runs only the affected guide's scenario tests through the existing source-map rewriter so PRD-A2-03 failures surface unchanged. Full mode watches `docs/guides/**/*.mdx`, `core/src/**`, `sdk/src/**`, `plugins/*/src/**`, and the scenario generator; an MDX change re-runs that one guide while a production-source change re-runs all. `bun run dev:guides docs/guides/<path>.mdx` pins the loop to a single guide for focused TDD, and `--once` runs one non-watching pass (used by the test suite). Watch mode survives a failing iteration and exits cleanly on SIGINT.
- Verification: typecheck, lint, tests passed. Manual CLI QA: green guide one-shot (`1 pass`, exit 0); red guide one-shot (source-mapped `at docs/guides/<f>.mdx:LINE` + `Re-run:` hint, exit 1); watch mode re-ran on a live edit and recovered after an invalid-MDX edit; Ctrl-C (SIGINT to the process group) exited 0 with no leftover processes or temp tsconfig dirs. (3 pre-existing `@lando/core/testing`/`operations` packed-package-export failures reproduce on clean main and are unrelated.)
- **Learnings for future iterations:**
  - Generated guide scenario tests (`test/scenarios/generated/guides/**`, gitignored) are not in any committed tsconfig — `tsc -b` skips them and `core/test/tsconfig.json` is a standalone project that isn't built. To typecheck one generated path standalone, write an ephemeral tsconfig that `extends` `tsconfig.base.json`, sets `typeRoots: ["<repoRoot>/node_modules/@types"]` (otherwise `types: ["bun"]` can't resolve from a tmpdir config), maps `@lando/core`/`@lando/sdk` `paths` to workspace sources, and `include`s the dir. Plain `tsc --noEmit <file>` ignores tsconfig entirely (loses `paths`/bundler resolution) and won't work.
  - A `scripts/*.ts` file that is *imported* by a `core/test/**` test (for pure-function unit tests) gets pulled into `core/test/tsconfig.json`'s type graph, unlike scripts that are only spawned as subprocesses (e.g. `docs-scenario.ts`). That extra coverage caught a real `Bun.spawn({ cmd: ReadonlyArray<string> })` overload error — copy to a mutable array (`cmd: [...cmd]`). The harmless `TS6059` rootDir notice for the cross-dir import matches existing precedent (`core/test/codegen/guide-scenarios.test.ts`).
---

## [2026-05-31 12:15 CDT] - US-196 Review
- Fixed the real review gap from the PRD-B-12 success metric by adding core-scoped `dev:guides` TDD-loop guidance in `core/AGENTS.md`; root `AGENTS.md` already documented the command.
- Review notes (rejected): single-guide/red-guide failure reports were false positives from review-run pollution or stale focused output — `bun test core/test/scripts/dev-guides.test.ts` is green locally. The docs watcher being broader than MDX-only was accepted as non-blocking because guide fixture changes can affect executable-guide runs, and the PRD requires watching MDX, not ignoring all other guide-adjacent inputs.
- Verification: typecheck and lint passed; focused `dev-guides` tests passed; full `bun test` still fails only in the pre-existing package-export/packed-install cases (`@lando/core/cli/operations`, `@lando/core/testing`, packed `fast-check`) documented by prior progress entries and unrelated to US-196.
---

## [2026-05-31 12:30 CDT] - US-121
- Shipped the `apps:scratch:*` CLI namespace: all seven commands (`start`, `stop`, `destroy`, `list`, `info`, `logs`, `gc`) implemented with their §8.2 aliases (`scratch:*` plus the bare `scratch` alias for start), routed through both dispatch paths (OCLIF spec/class commands + compiled `run.ts` hand-rolled dispatcher + canonical-id map) and promoted out of the deferred-command list/fixture. Each command runs on the `bootstrap: "scratch"` tier and renders honest plain/JSON output (`list` table/JSON, `gc` orphan-reap summary, id-addressed `info`/`logs`/`stop`/`destroy`).
- Widened the additive `ScratchAppService` tag with the lifecycle/registry seam (`acquire`, `resolveById`, `list`, `start`, `stop`, `destroy`, `gc`) plus its input/output types (`ScratchSource`, `ScratchAcquireInput`, `ScratchHandle`, `ScratchSummary`, `ScratchGcReport`, …). The Live Layer returns honest interim outcomes — `acquire` fails `ScratchSourceUnresolvedError` (source resolution is US-122/123), id-addressed ops fail `ScratchAppNotFoundError` (registry is US-126), `list`/`gc` return empty — so the namespace is fully wired ahead of those stories. `gc` invokes the real `service.gc()` orphan-reap seam on demand; `--detach` is parsed and threaded on `start`.
- Published the `ScratchSourceUnresolvedError`, `ScratchAppNotFoundError`, and `ScratchAppIdInvalidError` tagged errors from `@lando/sdk/errors`.
- Verification: typecheck, lint, and full `bun test` (2102 pass / 25 skip / 0 fail) passed. CLI manual QA across canonical + alias routes confirmed: empty `list`/`gc`, clean `ScratchSourceUnresolvedError` on `start --detach`, and clean `ScratchAppNotFoundError` (with `commandId`, no raw JSON dump) on `info`/`logs`/`stop`/`destroy <id>`.
- **Learnings for future iterations:**
  - US-121 is the namespace-only story: the ScratchAppService Live Layer intentionally returns "not yet available" tagged errors for `acquire`/`resolveById`/`start`/`stop`/`destroy` because source resolution (US-122 fork / US-123 recipe), the registry + provider-label reap (US-126), and the scope finalizer (US-125) are separate later stories. The seven commands + aliases + `--detach` flag surface are the deliverable; the contract tests (`core/test/cli/scratch-namespace.test.ts`) pin exactly these interim outcomes, so later stories flip the seam impls without touching the CLI wiring.
  - Promoting deferred scratch commands to real touched the usual surfaces: removed ids from `DEFERRED_COMMAND_PLANS` (`deferred-commands.ts`) + the fixture, added them to `MVP_COMMAND_IDS` (`command-base.ts`), replaced the OCLIF stubs with real spec/class commands, and added the canonical-id map entries + dispatch branches in `run.ts`. The scratch error/render helpers live in one shared `core/src/cli/commands/scratch.ts` module both dispatch paths import (single source of truth per spec §8.4.1).
  - Scratch tagged errors follow the message-ful pattern (explicit `message` + `remediation`), so `bug-report.ts` renders them directly with no `_tag` special-case needed (unlike the message-less `AppIdReservedError`). New SDK tagged errors only need a presence assertion in `sdk/test/library/exports.test.ts` — not API_COMPATIBILITY.md or the schema snapshot.
---

## [2026-05-31 12:45 CDT] - US-121 Review
- Reviewed the `apps:scratch:*` namespace against PRD-B-06 US-121 / spec §8.2: all seven commands + §8.2 aliases (bare `scratch` → start) are wired through both dispatch paths, promoted out of the deferred list/fixture (parity probe re-pointed to the still-deferred `meta:global:list`), and pinned by contract tests for the honest interim outcomes. Goal/constraint, code-quality, and security review all pass; typecheck, lint, and full `bun test` green.
- Fixed one in-scope regression: the build commit deleted the security comment documenting the `isUnsafeScratchId` path-traversal guard (added by the US-120 review) while leaving the guard itself. Restored the rationale so the §21.3 escape-prevention invariant is not silently removable.
- Review notes (rejected/deferred): the `scratchSourceLabel` `input.source === undefined` branch is unreachable (`ScratchAcquireInput.source` is required) — harmless defensive guard at the `unknown`-fed boundary, not worth churn. `--detach` cannot yet return a real id because `acquire` honestly fails `ScratchSourceUnresolvedError` until source resolution lands (US-122/123) — accepted as the namespace-only deliverable, not a review blocker.
- Verification: typecheck clean, lint clean (617 files), scratch suites green; full suite previously 2102 pass / 25 skip / 0 fail.
---

## [2026-05-31 13:30 CDT] - US-122
- Implemented scratch-app fork mode (`apps:scratch:start --fork`). `ScratchAppService.acquire`, for a `{ kind: "fork" }` source, discovers the cwd Landofile, re-derives its plan under a freshly synthesized `scratch-<base>-<6hex>` identity, materializes the scratch instance directories, selects the active provider, and applies the fork plan so the source app and the fork run simultaneously with separate containers, networks, DNS aliases, and volume names. Recipe-source acquire still returns the interim `ScratchSourceUnresolvedError` (US-123 owns recipe mode). The CLI command + both dispatch paths were already wired in US-121; only `acquire` and its runtime dependencies changed.
- Forking re-plans with a renamed Landofile (`{ ...landofile, name: scratchId }`) rather than deep-copying the source `AppPlan`: service-type plugins bake the app name into each `ServicePlan` (env `LANDO_PROJECT`/`LANDO_APP_NAME`, derived volume names, DNS), so re-planning is the only way to guarantee every slug-keyed resource name becomes the scratch id and the source's resources stay untouched. The fork's app root stays the source root (shared mount) by design; content-copy isolation, the scope finalizer, the registry/orphan-reap, and `--mount-cwd`/`--share-global-storage` remain their own later stories.
- `makeScratchRuntimeLive` now provides `LandofileService` + `AppPlanner` alongside the provider tier and feeds `FileSystem`/`LandofileService`/`AppPlanner`/`RuntimeProviderRegistry` into `ScratchAppServiceLive` (single shared `providerBase` reference so Effect memoizes it). `acquire`'s declared error union (`ScratchSourceUnresolvedError | ScratchAppError`) and its `R` (`Scope.Scope`) are unchanged — all dependencies live in the Live Layer closure, so no SDK contract widened and the compiled-dispatcher cast needed no change. Added the owned guide `docs/guides/scratch/fork-existing-app.mdx`.
- Verification: typecheck, lint, lint:guides, and full `bun test` (2108 pass / 26 skip / 0 fail) all green. A host-safe unit test drives the real `acquire` against a recording fake provider and asserts the applied fork plan's id/name/slug equal the scratch id, its per-app network differs from the source's, and the source plan object is not mutated; an unresolved-cwd case maps to `ScratchSourceUnresolvedError`. A podman-gated live scenario forks a Node+Postgres app and asserts both run simultaneously (skips without `LANDO_TEST_PODMAN_SOCKET`). CLI QA: `lando scratch start --fork` outside a project renders the `ScratchSourceUnresolvedError` + remediation and exits 1, proving the command routes through the new fork acquire.
- **Learnings for future iterations:**
  - Fork the plan by RE-PLANNING the source Landofile under the scratch identity, never by cloning the resolved `AppPlan` — `appName` is threaded into `serviceType.toServicePlan(...)` so per-service env/volume/DNS strings are already materialized with the source name in a clone.
  - To give a `bootstrap: "scratch"` core service the planner + landofile + provider registry, widen `makeScratchRuntimeLive` (and the `ScratchRuntimeServices` union) and provide those deps to the `ScratchAppServiceLive` sub-layer; capture them in the service closure so the SDK method signatures (`acquire` R/error union) stay frozen-compatible and the compiled-path cast in `run.ts` needs no change.
  - The `scratch` runtime tier intentionally roots fork plans at `process.cwd()` (shared source root) for now; the later `--isolate=full` story is the seam that swaps the fork's app root to a content copy under `<userCacheRoot>/scratch/<id>/root/`.
---

## [2026-05-31 CDT] - US-122 (verified complete)
- Story verified complete on branch `us-122-scratch-fork-mode` (no new code). `ScratchAppService.acquire` resolves `{ kind: "fork" }` sources by re-planning the cwd Landofile under a fresh `scratch-<base>-<6hex>` identity (separate containers, networks, DNS aliases, volume names; source plan untouched); recipe-source acquire still returns the interim `ScratchSourceUnresolvedError`. `apps:scratch:start --fork` is wired through both dispatch paths and the `makeScratchRuntimeLive` tier carries the planner/Landofile/provider deps. `passes: true` was already set.
- Verification: typecheck clean, lint clean (619 files), full `bun test` 2108 pass / 26 skip / 0 fail. CLI QA: `lando scratch start --fork` outside a project renders the `ScratchSourceUnresolvedError` + remediation and exits 1. The podman-gated Node+Postgres simultaneous-run scenario skips without `LANDO_TEST_PODMAN_SOCKET` (host-safe).
---

## 2026-05-31 14:45 CDT - Babysit
- PR: #234 https://github.com/AaronFeledy/core4/pull/234
- CI outcome: pass (initially provider-integration-linux-x64 failed)
- Review feedback: fixed (scratch-fork-live integration test used unresolvable short image names → Podman create 404; updated fixture to fully-qualified refs node:22-alpine/postgres:16-alpine)
- Merge: merged (squash)
---

## [2026-05-31 15:40 CDT] - US-123
- Implemented scratch mode: `apps:scratch:start --from <recipe-ref>` now renders a bundled recipe into the scratch root (`<userCacheRoot>/scratch/<id>/root/`, per spec §19.3 — the PRD's "<userDataRoot>" wording is superseded by spec), plans it under a fresh `scratch-<base>-<6hex>` identity, and starts it via `provider.apply`, mirroring fork mode's materialize→plan→apply shape. `ScratchAppService.acquire` gained a `kind: "recipe"` branch that reuses `initApp` (extended with additive `destination`/`runPostInit` options so the recipe pipeline scaffolds into an exact directory without running recipe `postInit:` actions), reads the rendered `.lando.yml` directly (discovery excludes the scratch dir), decodes `LandofileShape`, forces `name: scratchId`, and re-plans under a `withProcessCwd` scope because the planner stamps `root = process.cwd()`. Recipe-resolution failures map to `ScratchSourceUnresolvedError`; render/IO/parse failures map to `ScratchAppError`.
- Recipe prompts auto-answer through repeatable `--answer key=value` and synonym `--option key=value` (option wins on key collision), with `--yes` (accept defaults) and `--no-interactive`; these thread through the additive `ScratchAcquireInput` fields and are parsed identically in both the OCLIF command and the compiled `run.ts` dispatcher.
- Verification: `bun run typecheck`, `bun run lint`, and full `bun test` (2133 pass / 26 skip / 0 fail) all passed. CLI QA (source-mode subprocess under a temp cache root): `lando scratch start --from empty` renders `name: scratch-empty-<hex>` into the scratch root and only stops at provider-capability resolution (no container runtime on host); `lando scratch start --from lamp --option php=8.2` renders `type: php:8.2` — proving the CLI flag → answers → recipe-render pipeline end to end. A full container start belongs in the sandbox and is out of scope here.
- **Learnings for future iterations:**
  - The scratch app root is `<userCacheRoot>/scratch/<id>/root/` (spec §19.3/§21.3 + the US-120 AGENTS note), NOT `<userDataRoot>`; the US-123 PRD AC wording is loose and the spec wins. Recipe scratch reuses the exact same `paths(id)` layout fork mode does — do not introduce a second base.
  - `LandofileService.discover` and `AppPlanner.plan` are both cwd-bound, and discovery explicitly excludes the scratch dir, so a scratch-root plan must (a) parse the rendered Landofile directly like `loadGlobalPlan` and (b) wrap `planner.plan` in a `process.chdir(<root>)` scope. There is no non-cwd planning seam yet.
  - `initApp` is the reusable recipe-scaffold entry point. It self-provides `RecipeManifestServiceLive`, so calling it from a service via `Effect.tryPromise` adds NO new `R` dependency to `acquire` and needs no scratch-tier layer change. The `destination`/`runPostInit` options are the seam US-124 (`--isolate`) and a future `--run-post-init` flag will build on.
  - `ScratchAcquireInput`/`ScratchAppService` are additive (absent from `sdk/test/fixtures/sdk-mvp-surface.json`), so optional fields can be added freely — no `API_COMPATIBILITY.md` or backward-compat fixture edit; `exports.test.ts` only asserts tag presence.
---

## [2026-05-31 16:05 CDT] - US-123 Review
- Reviewed scratch recipe mode against PRD-B-06 US-123 + spec §21.10 via review-work (5 agents): QA and security PASS; goal/code-quality/context-mining flagged blockers. Fixed three in-scope: (1) created the PRD-required executable guide `docs/guides/scratch/scratch-from-recipe.mdx` — it is "Required at story acceptance" in the PRD Guide Coverage table and the build omitted it; written as a `render={false}` documentation guide mirroring `fork-existing-app.mdx` (recipe mode needs a real provider, so it documents the command surface without booting containers) and generates 4 passing scenarios. (2) CLI dual-dispatch parity — the OCLIF `apps:scratch:start` command now declares `char: "y"` for `--yes` and a `non-interactive` alias for `--no-interactive`, so source mode accepts the same `-y`/`--non-interactive` synonyms the compiled `run.ts` parser already did. (3) `mapInitError` now maps `RecipeManifestParseError` → `ScratchSourceUnresolvedError`, consistent with its sibling `RecipeManifestValidationError` (a malformed recipe manifest is a source-resolution failure, not an internal render error).
- Review notes (rejected/deferred): `withProcessCwd` `process.chdir` concurrency — rejected; the cwd-bound planner is a documented frozen interim seam, `loadGlobalPlan` uses the identical pattern without a mutex, and the CLI performs one scratch acquire per process so interleaving cannot occur in shipped code (a process-wide semaphore would be out-of-scope refactoring). "Any recipe (canonical/remote)" breadth — deferred to PRD-07 recipe-source work; non-bundled refs already fail honestly via `NotImplementedError` → `ScratchSourceUnresolvedError`, and the AC does not require remote rendering. Symlink hardening + local-recipe-ref metadata read — security LOW, same-user-only, pre-existing filesystem behavior, non-blocking.
- Repaired progress.txt: a stray process truncated 259 lines (the US-111…US-120 history) from the working-tree copy during review; restored from HEAD before appending so no prior entries were lost.
- Verification: typecheck clean, lint clean (624 files), lint:guides passed, full `bun test` 2137 pass / 26 skip / 0 fail (the +4 over the build's 2133 are the new guide's generated scenarios).
- **Learnings for future iterations:** Every `apps:scratch:*` story must ship its PRD-B-06 "Guide Coverage" executable guide — the US-199 drift gate is NOT enforced yet AND its declared surface paths (`core/src/scratch/**`) are stale vs the real layout (`core/src/scratch-app/**`, `core/src/cli/commands/scratch.ts`), so a missing guide passes CI silently; verify the guide exists by hand. Scratch guides are `render={false}` (no container boot) since both fork and recipe modes need a real provider.
---
