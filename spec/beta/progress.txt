# Beta Progress Log — Lando v4 Phase 3 (full breadth)

Source PRDs: spec/beta/prd-beta-{00..13}-*.md
Spec source of truth: spec/ROADMAP.md Phase 3 + spec/{01..19}-*.md and spec/15-binary-build-and-release.md (§17)

## Codebase Patterns
- Plugin contribution surfaces that must become part of a materialized artifact (e.g. `globalServices:` → global `.lando.dist.yml`) follow this seam: a pure resolver in `core/src/services/*` collects across `PluginRegistry.list` manifests (deterministic, id-sorted), detects cross-plugin id collisions as a tagged error, then a materializer turns each accepted contribution into the artifact shape via an INJECTABLE loader (default = dynamic `import()` of the contribution `module`; tests inject a fake or point at a temp module). The low-level writer service (e.g. `GlobalAppService.regenerateDist`) stays frozen and dumb; resolution lives in core helpers the command composes. For a `bootstrap: "global"` command to `yield* PluginRegistry`, `PluginRegistryLive` must be in the TOP-LEVEL `Layer.mergeAll` of `makeProviderRuntimeLive` (not only `Layer.provide`'d into `RuntimeProviderRegistryLive`), and the compiled-dispatcher cast in `core/src/cli/run.ts` must widen to the command's full required-service set.
- User-app CLI commands resolve the current project via `loadUserLandofile(landofileService)` (`core/src/cli/app-resolution.ts`), never `landofileService.discover` directly — a canary test (`core/test/cli/reserved-app-id-canary.test.ts`) forbids `.discover` in `core/src/cli/commands/**`. The helper rejects the reserved `global` app id with `AppIdReservedError` before any plan is built. Reason: `LandofileService.discover` / `AppPlanner.plan` error unions are frozen by the SDK backward-compat fixture, so new resolution errors must ride a new seam, and each command's hand-maintained error union (plus composing commands like `rebuild`/`restart`) gains the new error.
- `lando doctor` subsystem recovery is classified per-subsystem in `core/src/cli/commands/doctor-subsystems.ts` via a `SubsystemSpec.recovery` of `automatic` (non-privileged, exposes a re-runnable `setup()` — proxy, ssh) or `manual` (privileged setup like CA trust-store / host DNS, or no `setup()` at all — certs, host-proxy, healthcheck, scanner). `--fix` only re-runs `setup()` for `automatic` degraded subsystems; `manual` ones report `fixOutcome: skipped-manual`. Doctor never auto-elevates. Fix outcomes ride in the check `context` (`fixOutcome`/`fixCommand`/`fixExitCode`/`fixError`) so both the plain-text and NDJSON renderers surface them with no renderer changes.
- New doctor CLI flags must be wired in BOTH dispatch paths to keep source/compiled parity: the OCLIF `static flags` + `inputDoctorOptions` in `core/src/cli/oclif/commands/meta/doctor.ts`, and a hand-rolled `parse*Flag` in `core/src/cli/run.ts` `runDoctor`. Both funnel into the same `doctorReport(options)`.
- Running `@lando/core/cli` in-process (source mode → OCLIF `execute()`) calls `process.exit` on any erroring command, which silently kills the surrounding test runner. Scenario/test harnesses that invoke the CLI in-process must only drive commands that succeed; faked/canned runners exist precisely to avoid this for `start`/`destroy`/`curl`-style flows. Spawn the compiled binary (`core/dist/lando`, overridable via `LANDO_SCENARIO_E2E_BINARY`) when you need to run an erroring command without taking down the runner.
- `machineFailure()` in `plugins/provider-lando/src/setup.ts` is the single factory for all Podman machine errors; extend its regex conditions to detect new OS-specific virtualization prerequisites before adding new error subclasses.
- `buildSetupSteps()` drives both the event-tree children list and which setup actions run; adding a platform requires updating that function + the corresponding `if (platform === ...) ...` block in `setupProviderLando`.
- Windows Podman machine lifecycle is structurally identical to macOS (`ensureWindowsPodmanMachine` mirrors `ensureMacOSPodmanMachine`); the separation exists for future divergence (e.g. WSL-specific machine flags).
- Host-mutating live tests should gate on both the expected platform and an exact opt-in env value (`process.platform !== "win32" || process.env.GATE_VAR !== "1"`) so accidental truthy values do not run provider setup on the wrong host.
- All three providers (docker/lando/podman) realize networking through the shared `@lando/sdk/schema` helpers (`landoNetworkNames`, `landoServiceNetworkAliases`, `landoAppNetworkName`, `landoSharedNetworkName`), not from `plan.networks` directly. To make every provider consume a new per-app networking field, widen those helpers to read it (with a slug-derived fallback for legacy/hand-built plans) instead of editing each provider — provider behavior stays identical when the plan reproduces the slug derivation.
- Adding any new export to `@lando/sdk/schema` (schemas AND helper functions) requires adding it to the `Additive Alpha schema exports` list in `sdk/API_COMPATIBILITY.md`; the `sdk-backward-compatibility` test diffs live exports vs that list. Adding a field to a snapshotted schema (e.g. `AppPlan`) also requires `bun run codegen:schema-snapshot`.
- Adding a new executable-guide component touches five registration points: SDK `sdk/src/docs/components/props.ts` (prop schema + `decode*Either` helper + the `BETA_COMPONENTS` allowlist consulted by `assertAlpha2Component`), `scripts/build-guide-scenarios.ts` (the `GuideStepComponent` union + `parseStepComponent` + `renderStepComponent`), the `scripts/lint-guides.ts` component allowlist, the SDK schema registry in `sdk/src/schema/index.ts` (export/import/`JSON_SCHEMA_REGISTRY`/`getJsonSchema`), and `SDK_SCHEMA_NAMES` in `scripts/build-schema-snapshot.ts`; finish with `bun run codegen:schema-snapshot` and add the new export name to `sdk/API_COMPATIBILITY.md`. (A component can be schema-registered long before it is code-emitting — e.g. `<Hidden>` only needed the decode helper, allowlist flip, codegen, lint, and runtime seam.)
- Any new scenario transcript frame kind must be added to the `TranscriptFrame` union in `sdk/src/docs/transcript.ts`; `persistTranscript` runs `Schema.encodeSync(Transcript)` at scenario teardown and throws on an unknown frame kind.
- Executable-guide scenarios fan out into one generated test file per axis variant. `tabs:` frontmatter declares the single implicit `default` axis's ordered values; `variantsOf()` in `scripts/build-guide-scenarios.ts` turns those into per-scenario variant files named `<scenario-id>.<value>.test.ts` with a `// @variant: default=<value>` header. A scenario body is an ordered `Step | TabsBlock`; `<Tabs>`/`<Tab name>` live inside `<Scenario>` and each tab carries its own `<Step>`s. `resolveVariantSteps()` computes the block step-name union in source order (first occurrence wins) and emits `test.skip(<step>, …)` for union steps a variant's tab omits. A guide with `tabs:` but no `<Tabs>` block still fans every scenario into N identical variants. Multi-axis `axes:` (a record of axis-name → ordered values) fans scenarios into the Cartesian product of all axes in declaration order; a variant is now an ordered list of `{axis,value}` pairs, the filename suffix is the dot-joined values, and the header is space-joined `// @variant: axis=value` pairs. `<Tabs axis="A">` resolves only against that variant's value for axis A (axis prop required when a guide declares >1 axis, inferred for a single axis). Per-cell `variants:` overrides are keyed by the dot-joined cell key and refine `skip` (guide-wide `skip:` ?? per-cell → emits a whole-variant `test.skip`), `tags` (guide ∪ cell → `// @tags:`), and `platforms` (cell wins → `// @platforms:`); only per-cell overrides emit those header lines so guides without `variants:` stay byte-identical. `tabs:`/`axes:` are mutually exclusive and `variants:` keys must resolve to real cells — both enforced on decode by `GuideFrontmatterChecked` (a decode-only refinement; the exported `GuideFrontmatter` stays a plain struct so its JSON snapshot keeps a single named definition). `findDeferredGuideFrontmatter` now only defers `defaultLayer: e2e` (§19.11). NOTE: guide frontmatter is parsed by the Landofile YAML subset (`core/src/landofile/parser.ts`), which only supports block-style maps (no inline `{ }` flow maps) and whose map-key charset now includes `.` so dot-joined `variants:` keys parse.
- Transcript suppression has a single runtime seam: `ScenarioContext.hidden(effect)` (depth-counted, restored on failure) funnels all frame writes through an `appendFrame` choke point in `makeScenarioContext`. Any "emit-but-don't-render" guide construct (`<Hidden>`) should reuse `context.hidden` rather than re-plumbing the frame array.

## Summary — US-074 through US-110 + guide-engine work (PRD-01..04 + early PRD-05)

This condenses the provider-matrix, service-catalog, file-sync, and subsystem stories (plus the agent2 guide-engine workstream). All landed on `main` via squash-merged PRs (#172–#221); per-story commit lists, PR-created markers, and merge-only babysit notes are intentionally dropped (they live in git history).

**PRD-01 Provider Matrix (US-074–082).** Cross-platform Podman-machine lifecycle for `@lando/provider-lando` (Windows mirrors macOS; prerequisite errors map to Hyper-V/WSL2/VM-Platform remediation). Pinned, SHA-256-verified, idempotent runtime-bundle downloader (Linux/macOS/Windows) with a caller-injectable entry seam for deterministic fixtures. `@lando/provider-docker` Windows Docker Desktop support via a real named-pipe (npipe) IPC HTTP transport. `@lando/provider-podman` shipped as a bundled-but-non-default opt-in provider for Linux + Podman Desktop (macOS/Windows). Shared `RuntimeProvider` contract suite + matrix runner (`runProviderContract` / `runProviderContractMatrix`) covering every supported platform across all four providers. Partial-failure rollback + `AbortSignal` for provider-docker. Provider selection precedence `flag > Landofile > env (LANDO_PROVIDER) > config > capability-default` with `meta:doctor` conflict diagnostics (`provider-conflict` check + executable remediation).

**PRD-02 Service Catalog (US-083–094).** Added service types to `@lando/service-lando`: `go` (1.22/1.23, framework `none` only), `mongodb`, `memcached`, `valkey` (dual-scheme `valkey://`+`redis://`), `solr`, `elasticsearch`, `opensearch`, `meilisearch`, `static`/`static:caddy`, and raw `compose` passthrough (default `/app` bind + `appMount:false` opt-out). Framework-preset coverage documented in the package README. Matrix-driven Beta service contract suite in `@lando/sdk/test` wired across all new types. Recurring rules: a new service type touches five test surfaces (`package`/`registration`/`env-contract` tests + `plugin.yaml` + the per-type test) and needs `bun run codegen:schema-snapshot`; healthchecks express HTTP probes as in-container `curl` `command` healthchecks (Alpha planner rejects `http` kind); pick the base image's actual shell (`golang:`→bash, alpine images→`sh`).

**PRD-03 File Sync / Mutagen (US-095–098).** Published the `FileSyncEngine` SDK contract (tag, schemas, `FileSyncStartError`/`FileSyncDriftError`/`FileSyncStopError`) + contract suite. Shipped the bundled `@lando/file-sync-mutagen` Live Layer (fixed §10.6.2 capability matrix, deterministic kebab-case session names, injectable `MutagenClient` seam, fail-closed `unavailable` default) via a new `fileSyncEngines` contribution kind. Added pinned, SHA-256-verified Mutagen host-CLI + agent download to `lando setup` (real v0.18.1 pins; per-target Linux agents extracted from the nested `mutagen-agents.tar.gz`), the `--skip-file-sync` flag, and a `file-sync` doctor check for slow-bind-mount providers. Wired auto-selected file sync into the lifecycle: `AppPlanner` emits a per-service `FileSyncPlan`; `lando start` creates one session per entry, `stop`/`destroy` terminate them; accelerated mounts use the planned named volumes.

**PRD-04 Subsystems (US-101–110).** Subsystem contracts published with the precedent pattern (SDK contract + error + test helpers + contract test + core fail-closed `Unavailable`/`Disabled` Live Layer + docs guide + API_COMPATIBILITY entry; per-platform Live Layers deferred to later stories) — covering `HostProxyService` (`--host-proxy=auto|none` opt-out) and the shared cross-app network capability (`sharedCrossAppNetwork: true` flipped on all providers; providers create/join `lando_bridge_network` with `<svc>.<app>.internal` aliases; app destroy preserves the shared network). Added the `globalServices:` plugin-contribution surface + `GlobalServiceContribution` schema + `validateGlobalServiceContributions` planner gate. `lando doctor` gained per-subsystem diagnostics (`subsystemDoctor`/`doctorReport` as a separate result section to keep provider-status fixtures byte-stable) and failure-recovery: degraded subsystems report `automatic` (`lando doctor --fix` re-runs `setup()`) or `manual` (`lando setup`) recovery, with secret-redacted fix outcomes. A typed per-app `NetworkingPlan` (`perAppBridge` + optional `sharedNetworkMembership`) now flows through the shared schema helpers (slug-derived fallback for legacy plans).

**Guide-engine workstream (US-175–179, agent2).** Split the scenario `runCli` into three runners on `@lando/core/testing` (`scenario` real in-process seam, `e2e` compiled-binary spawn, `testOnlyFake` canned shim; only `testOnlyFake` populates `ScenarioContext.events`). Added the read-only `<Inspect>` component (+ `inspect` transcript frame). Generalized the variant engine from single-axis `tabs:` to multi-axis `axes:` with Cartesian fan-out and per-cell `variants:` overrides. Made `<Hidden>` a code-emitting block: its `<Step>`s emit real scenario code inside a `context.hidden(...)` scope that suppresses their transcript frames while keeping the steps runnable (details in Codebase Patterns above).

**Recurring CI / environment themes (not regressions).**
- `provider-integration-linux-x64` live Docker/Podman "logs emits at least one chunk" was repeatedly flaky: root causes were the SDK contract's tight `logs` timeout (bumped 5s→15s) and provider-docker treating framed Docker log bytes as single-chunk (fixed by buffering framed bytes across socket chunks); the live fixture now emits a periodic log line. Live scenarios must publish backends on unprivileged host ports (e.g. 31081), not port 80, or rootless Podman fails container start.
- `core/test/scripts/codegen-ci.test.ts` and `core/test/build/schema-snapshot.test.ts` flake under arbitrary parallel subsets (shared codegen/biome writes race); both are byte-idempotent and pass in isolation and in the full run. Compiled-binary tests (`compile.test.ts`, `aliases.test.ts`) flake on `ETXTBSY` under concurrent `bun run build`; rebuild and re-run in isolation before treating as a regression.

**Cross-cutting learnings retained.**
- The SDK backward-compat fixture (`sdk/test/fixtures/sdk-mvp-surface.json`) freezes existing service-tag method signatures by exact match: you cannot widen a frozen tag's error union — surface new errors through a new non-frozen seam. Additive *new* tags only need a `sdk/API_COMPATIBILITY.md` entry; new tagged *errors* are tracked by `sdk/test/library/exports.test.ts` presence only (not API_COMPATIBILITY, not the schema snapshot).
- Adding a required field to `AppPlan` breaks provider plugins, SDK schema tests, and the binary-cache fixture at once — prefer `Schema.optional` with a planner that always populates it + helper fallbacks (≈47 hand-built fixtures otherwise need editing).
- `Layer.mergeAll(a, b)` gives the LAST layer precedence on a duplicate tag; `Effect.serviceOption`/dependencies resolve against what the sub-layer provides, so `Layer.provide` deps onto the sub-layer that needs them.
- Bundled `globalServices:` resolve from a static plugin export captured in `core/src/plugins/bundled.ts`, never the manifest `module:` path (relative imports can't resolve and `import()` is impossible in a `bun build --compile` binary); the `module:` field is docs + the non-bundled fallback only.

## [2026-05-30 00:30 CDT] - US-111
- Shipped `GlobalAppService` (tag/contract/Live Layer: `id`, `root`, `ensureRoot`) in `@lando/core`, wired into the provider/`global` runtime tier; it resolves and idempotently materializes `<userDataRoot>/global`.
- Published `AppIdReservedError` and the umbrella `GlobalAppError` tagged errors from `@lando/sdk`.
- Reserved the `global` app id: any user Landofile resolving to `name: global` is now rejected with `AppIdReservedError` before any plan is built. Enforcement lives in a new `core/src/cli/app-resolution.ts` `loadUserLandofile()` seam that every cwd user-app command routes through (start/stop/restart/rebuild/info/destroy/logs/exec/shell/tooling/app-config/app-cache-refresh), guarded by a canary test so future commands can't bypass it.
- Taught `bug-report.ts` to render the message-less `AppIdReservedError` (derived message + remediation + `reserved` diagnostic) and added the owned guide `docs/guides/global/install-and-bundled-services.mdx`.
- Verification: typecheck, lint, and tests passed. CLI-verified end-to-end: `lando info` in a `name: global` app prints the reserved-id error + remediation and exits non-zero.
- **Learnings for future iterations:**
  - The SDK backward-compat fixture (`sdk/test/fixtures/sdk-mvp-surface.json`) freezes existing service-tag method signatures by exact match; you cannot widen `LandofileService.discover` / `AppPlanner.plan` error unions to carry a new error. Surface new errors through a *new* non-frozen seam (here, the `loadUserLandofile` helper) and keep the new tagged error out of the frozen tags. Adding a NEW `Context.Tag` is fine — list it under "Additive Alpha service tags" in `sdk/API_COMPATIBILITY.md`.
  - An Effect `TaggedError` with no `message` field still exposes a non-empty `.message` (a JSON dump of its fields), so a renderer that prefers `error.message` prints raw JSON. `bug-report.ts` `extractMessage` must branch on `_tag` *before* reading `.message`. The unit test passed while the real CLI output was wrong — manual `lando` QA caught it.
  - `GlobalAppService.ensureRoot` keeps the spec's `Scope.Scope` requirement even though the current mkdir impl needs no finalizer; an effect with `R = never` is assignable to a contract declaring `R = Scope.Scope`, so the Live Layer satisfies the frozen-forward contract without a real scope yet.
---

## [2026-05-30 04:55 UTC] - US-112
- Implemented `meta:global:install` (alias `global:install`) to materialize the global app's Landofile stack under `<userDataRoot>/global/`, following spec §20's dist/user file split rather than an in-file 3-way merge. `GlobalAppService.regenerateDist` is the single writer of the generated `.lando.dist.yml` (Lando-owned header + embedded body sha256, idempotent: created → unchanged); `GlobalAppService.ensureUserLandofile` creates a comment-only `.lando.yml` overlay once and never rewrites it, so user edits are preserved by construction. A foreign or manually-edited `.lando.dist.yml` fails with the new tagged `GlobalDistConflictError` (foreign-file / manual-edit) carrying remediation; a directory at the user-file path fails with `GlobalLandofilePathConflictError`. Command wired through both dispatch paths (OCLIF spec/class + compiled `run.ts` branch + canonical-id map) and removed from the deferred-command list/fixture. A `[plugin]` positional is accepted but fails with a `GlobalAppError` remediation pointing at the no-arg form, keeping the surface forward-compatible with the spec's later plugin-enablement model.
- Verification: typecheck, lint, full test suite (1934 pass / 23 skip / 0 fail), hot-path canaries, and manual CLI QA (create → idempotent rerun with user-edit preserved → tampered-dist conflict → plugin-arg remediation) all passed.
- **Learnings for future iterations:**
  - The PRD's literal "`.lando.yml` is generated by `meta:global:install`" is superseded by spec §20.3/§20.5 (the PRD defers design to §20): the *generated* layer is `.lando.dist.yml` (single writer = `regenerateDist`), while `.lando.yml` is the never-overwritten user overlay. "Preserve user edits" is structural (separate files merged at load order), not an in-file diff3 — there is no YAML serializer or merge engine in the repo and none was added.
  - The Landofile parser (`core/src/landofile/parser.ts`) rejects inline `{}` flow maps. Emit an empty services map as a block-style `services:` with no children (round-trips to `{ services: {} }`), and emit comment-only files for empty user overlays (parse to `{}`). Verify any generated Landofile round-trips through `parseLandofile` + validates against `LandofileShape` in a test.
  - `GlobalAppService` is an *additive* (non-frozen) SDK service tag — listed in `sdk/API_COMPATIBILITY.md` "Additive Alpha service tags" but NOT in `sdk/test/fixtures/sdk-mvp-surface.json`. The backward-compat test only equality-freezes tags present in that fixture, so additive tags can grow method signatures freely; only the tag *name* must stay documented. New tagged errors just need to be exported (the exports test asserts presence, not a frozen set).
  - The minimal runtime previously wired a no-op `FileSystem` stub (`exists→false`, `writeAtomic→void`); any command that genuinely touches disk through the runtime needs the real `FileSystemLive`. `makeMinimalRuntimeLive` now uses `FileSystemLive`, and `bootstrap: "global"` (→ `makeProviderRuntimeLive`) resolves both `GlobalAppService` and `FileSystem`. This swap has repo-wide blast radius for runtime-built commands; covered by the full suite.
  - AC "references plugin-contributed globalServices entries" is wired (`regenerateDist({services})` emits a sorted `services:` map) but the live contributed set is empty until US-113 (contribution surface) and US-114/115 (bundled Traefik/Mailpit) land — `regenerateDist` is the stable extension point those stories feed.
---

## [2026-05-30 11:36 CDT] - US-112 Review
- Reviewed the US-112 global Landofile materialization against PRD-B-05 and spec §20 (dist/user file split). All acceptance criteria met; no code changes required.
- Review notes (rejected): two review-agent FAIL verdicts were false positives. (1) "New SDK errors GlobalDistConflictError/GlobalLandofilePathConflictError must be documented in sdk/API_COMPATIBILITY.md" — rejected: the backward-compat test only freezes the "Additive Alpha schema exports"/"Additive Alpha service tags" headings (the frozen fixture has no errors key); error exports are governed solely by exports.test.ts presence assertions, which were updated. Precedent: US-111's GlobalAppError/AppIdReservedError are also absent from that doc and the suite is green. (2) "Hand-rolled YAML emitter is unsound for ServiceConfig (colon-space scalars, empty maps emit null)" — rejected: verified empirically that `command: echo: hello world` round-trips correctly through Lando's own subset parser and empty maps round-trip to `{}`; the emitter+parser are a matched pair. Exotic-value hardening is deferred to US-114/115.
---

## [2026-05-30 13:46 CDT] - Babysit (PR #218)
- Merge deferred: triage hardened the global-app YAML emitter/parser round-trip (quoting strings that resemble already-quoted scalars; quoted-comma/nested-bracket-aware inline-array parsing for nested ServiceConfig arrays) with focused materialize coverage, but Cursor Bugbot kept surfacing fresh low-severity YAML edge cases on each new HEAD. Five-round triage cap reached with a sixth finding ("empty record in array emits unparseable YAML") still open — closed by the follow-up hardening entry below.
---

## [2026-05-30 CDT] - US-112 (YAML round-trip hardening)
- Closed the remaining global-app Landofile emitter/parser soundness gap that blocked PR #218: an empty record nested in an array (`[{}]`) was emitted as a bare `-`, which the Landofile YAML subset parser rejected as "Malformed YAML" — an unparseable round-trip. The emitter now writes empty records as the flow-empty map `- {}`, and `parseScalar` round-trips the flow-empty `{}` back to `{}` (block list items, nested inline arrays, and bare map values all converge through that one parser branch) while populated inline objects stay rejected. Added parser unit coverage and three complementary materialize round-trip tests (block, single-element, nested-inline).
- Verification: typecheck, lint, full `bun test` (1948 pass / 23 skip / 0 fail) passed; `meta:global:install` create/idempotent-rerun QA in an isolated `LANDO_USER_DATA_ROOT` confirmed well-formed output.
- **Learnings for future iterations:**
  - An empty mapping has NO block-style representation as a YAML sequence item — `- ` alone parses as null/throws. The only portable form is the flow-empty map `- {}`. Any emitter that serializes arbitrary nested structures (here `emitArray`) must use `{}` for empty records, and the matched parser must accept flow-empty `{}` (the single allowed inline object) even though it rejects populated inline objects. This is the natural extension point US-114/115 will hit when emitting real `globalServices:` arrays.
  - When hardening the global-app YAML emitter, drive both halves of the matched pair from ONE parser branch (`parseScalar`'s `{}` check) rather than special-casing each call site (`parseList`, `parseInlineArray`) — duplicate special-cases are exactly what review bots flag, and the block-list / inline-array paths already funnel array-item scalars through `parseScalar`.
---

## [2026-05-30] - US-113
- Shipped the `globalServices:` plugin contribution resolver + materialization wiring so a plugin that declares `contributes.globalServices` has its service appear in the materialized global Landofile (`<userDataRoot>/global/.lando.dist.yml`). Added `resolveGlobalServiceContributions` (deterministic, id-sorted; same-plugin re-declare = last-wins; two different plugins claiming the same id fail with the new `GlobalServiceCollisionError`) and `materializeGlobalServices` (drops `enabledByDefault:false`, drops capability-rejected contributions, loads each contribution's `module` default-exported Effect via an injectable `GlobalServiceModuleLoader`, validates the result against `ServiceConfig`) in `@lando/core`'s global-services helper. Wired `meta:global:install` (no-arg path) to pull plugin manifests + the active provider's capabilities and feed the resolved services into the existing `GlobalAppService.regenerateDist({ services })`; the `<plugin>` arg still rejects (US-116 owns enablement). Published `GlobalServiceCollisionError` from `@lando/sdk/errors`.
- Verification: typecheck, lint, full `bun test` (1954 pass / 23 skip / 0 fail) passed. CLI manual QA: `lando global:install` with no contributing plugin materializes `name: global` + empty `services:` ("Global services: none"), matching §20.3; the plugin-contribution path is proven by a contract test that points a fake plugin's contribution at a real temp module exporting `Effect.succeed({ api: 4, type: "lando" })` and asserts `services.fakegs` lands in the generated dist file.
- **Learnings for future iterations:**
  - `GlobalAppService.regenerateDist({ services })` is a frozen SDK Context.Tag signature and the single low-level writer of `.lando.dist.yml`. Do NOT move contribution collection/conflict/capability/module logic into the tag (would widen the frozen signature). Keep that logic in `core/src/services/global-services.ts` helpers and feed the resulting `Record<string, ServiceConfig>` into `regenerateDist`. US-114/115 (real Traefik/Mailpit services) only add manifest entries + service modules; the generic machinery already exists.
  - For a `bootstrap: "global"` command to `yield* PluginRegistry`, `PluginRegistryLive` must be in the TOP-LEVEL `Layer.mergeAll` of `makeProviderRuntimeLive` — it was previously only `Layer.provide`'d INTO `RuntimeProviderRegistryLive`, which does not expose it to the command's environment. Provider capabilities at the global level come from `RuntimeProviderRegistry.select(undefined)` → `provider.capabilities`/`provider.id`.
  - The compiled-dispatcher cast in `core/src/cli/run.ts` (`runMetaGlobalInstall`) narrows the global runtime layer to a fixed service set; widening `globalInstall`'s required services means widening that `as Layer.Layer<...>` cast too, or the compiled path fails to typecheck.
---

## [2026-05-30] - US-113 Review
- Reviewed the `globalServices:` contribution surface against PRD-B-05 US-113 and spec §18/§10. Resolver/materialization, SDK error export, runtime + compiled-dispatch wiring, and the contract test all hold. No code fixes were required.
- Review notes (rejected/deferred): (1) Collision detected at consumption (`meta:global:install`) rather than literal "at plugin load" per AC#2 — accepted as interim (Oracle-confirmed): bundled-only discovery + zero `globalServices` contributions make collision currently untriggerable, and `PluginRegistry.list`'s error union is frozen; recorded as an AGENTS.md interim note to close when dynamic plugin loading (US-116) lands. (2) Capability-rejected contributions dropped silently — out of scope (surfacing belongs to US-119 doctor). (3) `conflicts:` not enforced — that is the distinct `GlobalServiceConflictError` at dist-regen, a deferred feature. (4) Raw `import(module)` loader abstraction — injectable loader is intentional and unexercised until real bundled services land (US-114/115).
- **Learnings for future iterations:** `globalServices` collision validation timing is an explicit interim gap (now in AGENTS.md). When US-116 adds non-bundled plugin loading, move the `resolveGlobalServiceContributions` collision check onto a non-hot-path plugin-load validation seam (without widening the frozen `PluginRegistry.list` error union) and add capability-rejection surfacing via the US-119 doctor `global-app` section.
---

## [2026-05-30] - US-114
- Shipped the bundled `traefik` global service from `@lando/proxy-traefik`. The plugin now exports a `manifest` (`contributes.globalServices: [traefik]` + `proxies: [traefik]`), a `proxy` Layer slot, and a static `globalServices` map (`id` → `Effect<ServiceConfig>`). The traefik `ServiceConfig` is a `compose`-type service on `traefik:v3.3` whose start command materializes a file-provider dashboard router on `traefik.lndo.site` → `api@internal` and execs Traefik with the file provider + dashboard enabled (no Docker-provider socket dependency, so it stays provider-neutral across docker/podman/lando).
- Solved bundled global-service module resolution with a static, compiled-binary-safe seam: the bundled-plugins generator now emits each plugin's `globalServices` map into `core/src/plugins/bundled.ts`, and a new `bundledFirstGlobalServiceLoader` resolves bundled contributions from that static map (failing loudly if a bundled plugin omits a declared id) while leaving raw dynamic `import(module)` only as the non-bundled/future fallback. `meta:global:install` now uses this loader, so `lando global:install` materializes traefik into `<userDataRoot>/global/.lando.dist.yml` by default.
- Added a socket-gated live routing scenario (`core/test/scenario/global-traefik-live.integration.test.ts`) that brings up nginx + traefik on the shared cross-app network and asserts a `Host: web.shop.lndo.site` request reaches nginx through Traefik; it runs in CI's provider-integration job and skips without `LANDO_TEST_PODMAN_SOCKET`.
- Verification: typecheck, lint, full `bun test` (1968 pass / 0 fail) passed; bundled.ts, ci.yml, and schema-snapshot codegen are idempotent; the compiled-binary test confirms the binary ships `@lando/proxy-traefik`. Manual QA: ran the real `lando global:install` CLI (stdout "Global services: traefik"; dist file carries the traefik service + dashboard router).
- **Learnings for future iterations:**
  - Bundled `globalServices:` MUST resolve from a static plugin export, never the manifest `module:` path: a relative `module` can't resolve from `core/src/services/global-services.ts` and dynamic `import()` is impossible in a `bun build --compile` binary. The bundled-first loader reads `BUNDLED_PLUGINS[*].globalServices`; the `module:` field is documentation + the non-bundled dynamic fallback only. US-115 (Mailpit) follows the identical pattern — add a `globalServices` map export + a `module` (for docs) and declare it in `core/build.config.ts`.
  - Bundling a plugin that contributes only `proxies`/`globalServices` (no provider/serviceType/logger/engine) required widening `BundledPluginEntry.contributes` and the generator's `layerExportFor` (proxies → the plugin's `proxy` Layer export). The generated `bundled.ts`, the schema snapshot, AND the CI workflow header all list bundled plugins, so adding one touches three generated artifacts — regenerate all three and they each have an idempotency/drift test.
  - Adding a bundled plugin to the ship list is a 4-test-surface change: the BUNDLED_PLUGINS trio (`bundled.test.ts` length+list, `plugin-registry.test.ts` name list, `runtime-provider-registry.test.ts` only if it asserts membership) PLUS `compile.test.ts`'s `expectedBundledPluginNames` (proves the compiled binary statically imports the plugin).
  - Shipping a default global service changes `meta:global:install`'s observable output: the global app now materializes `traefik` by default (when the provider advertises `sharedCrossAppNetwork`), so the prior "Global services: none" assertion in `global-install.test.ts` becomes "Global services: traefik". This is the expected new default, not a regression.
  - A Traefik global proxy should use the FILE provider (dynamic config written by the container start command via a heredoc), not the Docker provider: the Docker provider needs socket access and is provider-specific, which breaks Podman/lando parity. The `compose` service type (`type: "compose"` + `image:`) is the right shape for a raw upstream image like Traefik.
---

## [2026-05-30] - US-114 Review
- 5-agent `review-work` review (goal, code-quality, security, context-mining) plus independent verification. No in-scope code fix was warranted: every FAIL/BLOCKING finding traces to a separate story, pre-existing repo state, a non-blocker, or a false positive. Gate reconfirmed green (typecheck, lint, `bun test` 1968 pass / 0 fail; bundled.ts/ci.yml/schema-snapshot drift-free; 21 US-114-specific tests pass).
- Review notes (rejected/deferred):
  - AC#3 "scenario doesn't execute the shipped `TRAEFIK_START_SCRIPT`/`meta:global:install` path" — rejected as blocker: the socket-gated live scenario brings up a `global`-named traefik service + nginx over the shared cross-app network and proves Host-routed reachability through Traefik (satisfies AC#3's text); the shipped command body is unit-asserted. The fuller materialize→provider-start→shipped-command E2E is owned by US-101 (ProxyService Live Layer, §20.10.1).
  - `proxy = Layer.empty` / ProxyService not realized — out of scope: §11.57/§20.10.1 + PRD-B-04 US-101 own the default `ProxyServiceTraefikGlobalAppLive` realization.
  - Dashboard base-domain hardcoded `traefik.lndo.site` — AC#2 explicitly permits the default ("or configured base domain").
  - Security `--api.insecure=true` + published `:8080` unauthenticated dashboard — deferred, not a US-114 blocker: conventional for a local-dev proxy; revisit when US-101 wires real proxy exposure.
  - Stale `plugins/proxy-traefik/plugin.yaml` (old `provides.proxyServices` shape, no `globalServices`) — out of scope: pre-existing, repo-wide, not loaded for bundled discovery (the TS `manifest` export is authoritative). Resolve as part of the broader `provides:`→`contributes:` plugin.yaml migration.
- **Learnings for future iterations:** US-101 (ProxyService Live Layer) inherits two deferred US-114 items — (a) end-to-end coverage that runs the shipped `traefik` `ServiceConfig.command` via `meta:global:install`→provider start, and (b) the dashboard exposure hardening (`--api.insecure`/`:8080`).
---
